/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars*/
import { $protobuf, $Reader, $Writer, $util, $root } from './root.js';

export const Authentication = $root.Authentication = (() => {

    /**
     * Namespace Authentication.
     * @exports Authentication
     * @namespace
     */
    const Authentication = {};

    /**
     * SupportedLanguage enum.
     * @name Authentication.SupportedLanguage
     * @enum {number}
     * @property {number} ENGLISH=0 ENGLISH value
     * @property {number} ARABIC=1 ARABIC value
     * @property {number} BRITISH=2 BRITISH value
     * @property {number} CHINESE=3 CHINESE value
     * @property {number} CHINESE_HONG_KONG=4 CHINESE_HONG_KONG value
     * @property {number} CHINESE_TAIWAN=5 CHINESE_TAIWAN value
     * @property {number} DUTCH=6 DUTCH value
     * @property {number} FRENCH=7 FRENCH value
     * @property {number} GERMAN=8 GERMAN value
     * @property {number} GREEK=9 GREEK value
     * @property {number} HEBREW=10 HEBREW value
     * @property {number} ITALIAN=11 ITALIAN value
     * @property {number} JAPANESE=12 JAPANESE value
     * @property {number} KOREAN=13 KOREAN value
     * @property {number} POLISH=14 POLISH value
     * @property {number} PORTUGUESE=15 PORTUGUESE value
     * @property {number} PORTUGUESE_BRAZIL=16 PORTUGUESE_BRAZIL value
     * @property {number} ROMANIAN=17 ROMANIAN value
     * @property {number} RUSSIAN=18 RUSSIAN value
     * @property {number} SLOVAK=19 SLOVAK value
     * @property {number} SPANISH=20 SPANISH value
     * @property {number} FINNISH=21 FINNISH value
     * @property {number} SWEDISH=22 SWEDISH value
     */
    Authentication.SupportedLanguage = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "ENGLISH"] = 0;
        values[valuesById[1] = "ARABIC"] = 1;
        values[valuesById[2] = "BRITISH"] = 2;
        values[valuesById[3] = "CHINESE"] = 3;
        values[valuesById[4] = "CHINESE_HONG_KONG"] = 4;
        values[valuesById[5] = "CHINESE_TAIWAN"] = 5;
        values[valuesById[6] = "DUTCH"] = 6;
        values[valuesById[7] = "FRENCH"] = 7;
        values[valuesById[8] = "GERMAN"] = 8;
        values[valuesById[9] = "GREEK"] = 9;
        values[valuesById[10] = "HEBREW"] = 10;
        values[valuesById[11] = "ITALIAN"] = 11;
        values[valuesById[12] = "JAPANESE"] = 12;
        values[valuesById[13] = "KOREAN"] = 13;
        values[valuesById[14] = "POLISH"] = 14;
        values[valuesById[15] = "PORTUGUESE"] = 15;
        values[valuesById[16] = "PORTUGUESE_BRAZIL"] = 16;
        values[valuesById[17] = "ROMANIAN"] = 17;
        values[valuesById[18] = "RUSSIAN"] = 18;
        values[valuesById[19] = "SLOVAK"] = 19;
        values[valuesById[20] = "SPANISH"] = 20;
        values[valuesById[21] = "FINNISH"] = 21;
        values[valuesById[22] = "SWEDISH"] = 22;
        return values;
    })();

    /**
     * LoginType enum.
     * @name Authentication.LoginType
     * @enum {number}
     * @property {number} NORMAL=0 NORMAL value
     * @property {number} SSO=1 SSO value
     * @property {number} BIO=2 BIO value
     * @property {number} ALTERNATE=3 ALTERNATE value
     * @property {number} OFFLINE=4 OFFLINE value
     * @property {number} FORGOT_PASSWORD=5 FORGOT_PASSWORD value
     * @property {number} PASSKEY_BIO=6 PASSKEY_BIO value
     */
    Authentication.LoginType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "NORMAL"] = 0;
        values[valuesById[1] = "SSO"] = 1;
        values[valuesById[2] = "BIO"] = 2;
        values[valuesById[3] = "ALTERNATE"] = 3;
        values[valuesById[4] = "OFFLINE"] = 4;
        values[valuesById[5] = "FORGOT_PASSWORD"] = 5;
        values[valuesById[6] = "PASSKEY_BIO"] = 6;
        return values;
    })();

    /**
     * DeviceStatus enum.
     * @name Authentication.DeviceStatus
     * @enum {number}
     * @property {number} DEVICE_NEEDS_APPROVAL=0 DEVICE_NEEDS_APPROVAL value
     * @property {number} DEVICE_OK=1 DEVICE_OK value
     * @property {number} DEVICE_DISABLED_BY_USER=2 DEVICE_DISABLED_BY_USER value
     * @property {number} DEVICE_LOCKED_BY_ADMIN=3 DEVICE_LOCKED_BY_ADMIN value
     */
    Authentication.DeviceStatus = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "DEVICE_NEEDS_APPROVAL"] = 0;
        values[valuesById[1] = "DEVICE_OK"] = 1;
        values[valuesById[2] = "DEVICE_DISABLED_BY_USER"] = 2;
        values[valuesById[3] = "DEVICE_LOCKED_BY_ADMIN"] = 3;
        return values;
    })();

    /**
     * LicenseStatus enum.
     * @name Authentication.LicenseStatus
     * @enum {number}
     * @property {number} OTHER=0 OTHER value
     * @property {number} ACTIVE=1 ACTIVE value
     * @property {number} EXPIRED=2 EXPIRED value
     * @property {number} DISABLED=3 DISABLED value
     */
    Authentication.LicenseStatus = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "OTHER"] = 0;
        values[valuesById[1] = "ACTIVE"] = 1;
        values[valuesById[2] = "EXPIRED"] = 2;
        values[valuesById[3] = "DISABLED"] = 3;
        return values;
    })();

    /**
     * AccountType enum.
     * @name Authentication.AccountType
     * @enum {number}
     * @property {number} CONSUMER=0 CONSUMER value
     * @property {number} FAMILY=1 FAMILY value
     * @property {number} ENTERPRISE=2 ENTERPRISE value
     */
    Authentication.AccountType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "CONSUMER"] = 0;
        values[valuesById[1] = "FAMILY"] = 1;
        values[valuesById[2] = "ENTERPRISE"] = 2;
        return values;
    })();

    /**
     * SessionTokenType enum.
     * @name Authentication.SessionTokenType
     * @enum {number}
     * @property {number} NO_RESTRICTION=0 NO_RESTRICTION value
     * @property {number} ACCOUNT_RECOVERY=1 ACCOUNT_RECOVERY value
     * @property {number} SHARE_ACCOUNT=2 SHARE_ACCOUNT value
     * @property {number} PURCHASE=3 PURCHASE value
     * @property {number} RESTRICT=4 RESTRICT value
     * @property {number} ACCEPT_INVITE=5 ACCEPT_INVITE value
     * @property {number} SUPPORT_SERVER=6 SUPPORT_SERVER value
     * @property {number} ENTERPRISE_CREATION=7 ENTERPRISE_CREATION value
     * @property {number} EXPIRED_BUT_ALLOWED_TO_SYNC=8 EXPIRED_BUT_ALLOWED_TO_SYNC value
     * @property {number} ACCEPT_FAMILY_INVITE=9 ACCEPT_FAMILY_INVITE value
     * @property {number} ENTERPRISE_CREATION_PURCHASED=10 ENTERPRISE_CREATION_PURCHASED value
     * @property {number} EMERGENCY_ACCESS=11 EMERGENCY_ACCESS value
     */
    Authentication.SessionTokenType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "NO_RESTRICTION"] = 0;
        values[valuesById[1] = "ACCOUNT_RECOVERY"] = 1;
        values[valuesById[2] = "SHARE_ACCOUNT"] = 2;
        values[valuesById[3] = "PURCHASE"] = 3;
        values[valuesById[4] = "RESTRICT"] = 4;
        values[valuesById[5] = "ACCEPT_INVITE"] = 5;
        values[valuesById[6] = "SUPPORT_SERVER"] = 6;
        values[valuesById[7] = "ENTERPRISE_CREATION"] = 7;
        values[valuesById[8] = "EXPIRED_BUT_ALLOWED_TO_SYNC"] = 8;
        values[valuesById[9] = "ACCEPT_FAMILY_INVITE"] = 9;
        values[valuesById[10] = "ENTERPRISE_CREATION_PURCHASED"] = 10;
        values[valuesById[11] = "EMERGENCY_ACCESS"] = 11;
        return values;
    })();

    /**
     * Version enum.
     * @name Authentication.Version
     * @enum {number}
     * @property {number} invalid_version=0 invalid_version value
     * @property {number} default_version=1 default_version value
     * @property {number} second_version=2 second_version value
     */
    Authentication.Version = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "invalid_version"] = 0;
        values[valuesById[1] = "default_version"] = 1;
        values[valuesById[2] = "second_version"] = 2;
        return values;
    })();

    /**
     * MasterPasswordReentryActionType enum.
     * @name Authentication.MasterPasswordReentryActionType
     * @enum {number}
     * @property {number} UNMASK=0 UNMASK value
     * @property {number} COPY=1 COPY value
     */
    Authentication.MasterPasswordReentryActionType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "UNMASK"] = 0;
        values[valuesById[1] = "COPY"] = 1;
        return values;
    })();

    Authentication.QrcMessageKey = (function() {

        /**
         * Properties of a QrcMessageKey.
         * @memberof Authentication
         * @interface IQrcMessageKey
         * @property {Uint8Array|null} [clientEcPublicKey] QrcMessageKey clientEcPublicKey
         * @property {Uint8Array|null} [mlKemEncapsulatedKey] QrcMessageKey mlKemEncapsulatedKey
         * @property {Uint8Array|null} [data] QrcMessageKey data
         * @property {number|null} [msgVersion] QrcMessageKey msgVersion
         * @property {number|null} [ecKeyId] QrcMessageKey ecKeyId
         */

        /**
         * Constructs a new QrcMessageKey.
         * @memberof Authentication
         * @classdesc Represents a QrcMessageKey.
         * @implements IQrcMessageKey
         * @constructor
         * @param {Authentication.IQrcMessageKey=} [properties] Properties to set
         */
        function QrcMessageKey(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * QrcMessageKey clientEcPublicKey.
         * @member {Uint8Array} clientEcPublicKey
         * @memberof Authentication.QrcMessageKey
         * @instance
         */
        QrcMessageKey.prototype.clientEcPublicKey = $util.newBuffer([]);

        /**
         * QrcMessageKey mlKemEncapsulatedKey.
         * @member {Uint8Array} mlKemEncapsulatedKey
         * @memberof Authentication.QrcMessageKey
         * @instance
         */
        QrcMessageKey.prototype.mlKemEncapsulatedKey = $util.newBuffer([]);

        /**
         * QrcMessageKey data.
         * @member {Uint8Array} data
         * @memberof Authentication.QrcMessageKey
         * @instance
         */
        QrcMessageKey.prototype.data = $util.newBuffer([]);

        /**
         * QrcMessageKey msgVersion.
         * @member {number} msgVersion
         * @memberof Authentication.QrcMessageKey
         * @instance
         */
        QrcMessageKey.prototype.msgVersion = 0;

        /**
         * QrcMessageKey ecKeyId.
         * @member {number} ecKeyId
         * @memberof Authentication.QrcMessageKey
         * @instance
         */
        QrcMessageKey.prototype.ecKeyId = 0;

        /**
         * Creates a new QrcMessageKey instance using the specified properties.
         * @function create
         * @memberof Authentication.QrcMessageKey
         * @static
         * @param {Authentication.IQrcMessageKey=} [properties] Properties to set
         * @returns {Authentication.QrcMessageKey} QrcMessageKey instance
         */
        QrcMessageKey.create = function create(properties) {
            return new QrcMessageKey(properties);
        };

        /**
         * Encodes the specified QrcMessageKey message. Does not implicitly {@link Authentication.QrcMessageKey.verify|verify} messages.
         * @function encode
         * @memberof Authentication.QrcMessageKey
         * @static
         * @param {Authentication.IQrcMessageKey} message QrcMessageKey message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QrcMessageKey.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.clientEcPublicKey != null && Object.hasOwnProperty.call(message, "clientEcPublicKey"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.clientEcPublicKey);
            if (message.mlKemEncapsulatedKey != null && Object.hasOwnProperty.call(message, "mlKemEncapsulatedKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.mlKemEncapsulatedKey);
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.data);
            if (message.msgVersion != null && Object.hasOwnProperty.call(message, "msgVersion"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.msgVersion);
            if (message.ecKeyId != null && Object.hasOwnProperty.call(message, "ecKeyId"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.ecKeyId);
            return writer;
        };

        /**
         * Decodes a QrcMessageKey message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.QrcMessageKey
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.QrcMessageKey} QrcMessageKey
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QrcMessageKey.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.QrcMessageKey();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.clientEcPublicKey = reader.bytes();
                        break;
                    }
                case 2: {
                        message.mlKemEncapsulatedKey = reader.bytes();
                        break;
                    }
                case 3: {
                        message.data = reader.bytes();
                        break;
                    }
                case 4: {
                        message.msgVersion = reader.int32();
                        break;
                    }
                case 5: {
                        message.ecKeyId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a QrcMessageKey message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.QrcMessageKey
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.QrcMessageKey} QrcMessageKey
         */
        QrcMessageKey.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.QrcMessageKey)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.QrcMessageKey: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.QrcMessageKey();
            if (object.clientEcPublicKey != null)
                if (typeof object.clientEcPublicKey === "string")
                    $util.base64.decode(object.clientEcPublicKey, message.clientEcPublicKey = $util.newBuffer($util.base64.length(object.clientEcPublicKey)), 0);
                else if (object.clientEcPublicKey.length >= 0)
                    message.clientEcPublicKey = object.clientEcPublicKey;
            if (object.mlKemEncapsulatedKey != null)
                if (typeof object.mlKemEncapsulatedKey === "string")
                    $util.base64.decode(object.mlKemEncapsulatedKey, message.mlKemEncapsulatedKey = $util.newBuffer($util.base64.length(object.mlKemEncapsulatedKey)), 0);
                else if (object.mlKemEncapsulatedKey.length >= 0)
                    message.mlKemEncapsulatedKey = object.mlKemEncapsulatedKey;
            if (object.data != null)
                if (typeof object.data === "string")
                    $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                else if (object.data.length >= 0)
                    message.data = object.data;
            if (object.msgVersion != null)
                message.msgVersion = object.msgVersion | 0;
            if (object.ecKeyId != null)
                message.ecKeyId = object.ecKeyId | 0;
            return message;
        };

        /**
         * Creates a plain object from a QrcMessageKey message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.QrcMessageKey
         * @static
         * @param {Authentication.QrcMessageKey} message QrcMessageKey
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        QrcMessageKey.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.clientEcPublicKey = "";
                else {
                    object.clientEcPublicKey = [];
                    if (options.bytes !== Array)
                        object.clientEcPublicKey = $util.newBuffer(object.clientEcPublicKey);
                }
                if (options.bytes === String)
                    object.mlKemEncapsulatedKey = "";
                else {
                    object.mlKemEncapsulatedKey = [];
                    if (options.bytes !== Array)
                        object.mlKemEncapsulatedKey = $util.newBuffer(object.mlKemEncapsulatedKey);
                }
                if (options.bytes === String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== Array)
                        object.data = $util.newBuffer(object.data);
                }
                object.msgVersion = 0;
                object.ecKeyId = 0;
            }
            if (message.clientEcPublicKey != null && Object.hasOwnProperty.call(message, "clientEcPublicKey"))
                object.clientEcPublicKey = options.bytes === String ? $util.base64.encode(message.clientEcPublicKey, 0, message.clientEcPublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.clientEcPublicKey) : message.clientEcPublicKey;
            if (message.mlKemEncapsulatedKey != null && Object.hasOwnProperty.call(message, "mlKemEncapsulatedKey"))
                object.mlKemEncapsulatedKey = options.bytes === String ? $util.base64.encode(message.mlKemEncapsulatedKey, 0, message.mlKemEncapsulatedKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.mlKemEncapsulatedKey) : message.mlKemEncapsulatedKey;
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            if (message.msgVersion != null && Object.hasOwnProperty.call(message, "msgVersion"))
                object.msgVersion = message.msgVersion;
            if (message.ecKeyId != null && Object.hasOwnProperty.call(message, "ecKeyId"))
                object.ecKeyId = message.ecKeyId;
            return object;
        };

        /**
         * Converts this QrcMessageKey to JSON.
         * @function toJSON
         * @memberof Authentication.QrcMessageKey
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        QrcMessageKey.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for QrcMessageKey
         * @function getTypeUrl
         * @memberof Authentication.QrcMessageKey
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        QrcMessageKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.QrcMessageKey";
        };

        return QrcMessageKey;
    })();

    Authentication.ApiRequest = (function() {

        /**
         * Properties of an ApiRequest.
         * @memberof Authentication
         * @interface IApiRequest
         * @property {Uint8Array|null} [encryptedTransmissionKey] ApiRequest encryptedTransmissionKey
         * @property {number|null} [publicKeyId] ApiRequest publicKeyId
         * @property {string|null} [locale] ApiRequest locale
         * @property {Uint8Array|null} [encryptedPayload] ApiRequest encryptedPayload
         * @property {number|null} [encryptionType] ApiRequest encryptionType
         * @property {string|null} [recaptcha] ApiRequest recaptcha
         * @property {string|null} [subEnvironment] ApiRequest subEnvironment
         * @property {Authentication.IQrcMessageKey|null} [qrcMessageKey] ApiRequest qrcMessageKey
         */

        /**
         * Constructs a new ApiRequest.
         * @memberof Authentication
         * @classdesc Represents an ApiRequest.
         * @implements IApiRequest
         * @constructor
         * @param {Authentication.IApiRequest=} [properties] Properties to set
         */
        function ApiRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ApiRequest encryptedTransmissionKey.
         * @member {Uint8Array} encryptedTransmissionKey
         * @memberof Authentication.ApiRequest
         * @instance
         */
        ApiRequest.prototype.encryptedTransmissionKey = $util.newBuffer([]);

        /**
         * ApiRequest publicKeyId.
         * @member {number} publicKeyId
         * @memberof Authentication.ApiRequest
         * @instance
         */
        ApiRequest.prototype.publicKeyId = 0;

        /**
         * ApiRequest locale.
         * @member {string} locale
         * @memberof Authentication.ApiRequest
         * @instance
         */
        ApiRequest.prototype.locale = "";

        /**
         * ApiRequest encryptedPayload.
         * @member {Uint8Array} encryptedPayload
         * @memberof Authentication.ApiRequest
         * @instance
         */
        ApiRequest.prototype.encryptedPayload = $util.newBuffer([]);

        /**
         * ApiRequest encryptionType.
         * @member {number} encryptionType
         * @memberof Authentication.ApiRequest
         * @instance
         */
        ApiRequest.prototype.encryptionType = 0;

        /**
         * ApiRequest recaptcha.
         * @member {string} recaptcha
         * @memberof Authentication.ApiRequest
         * @instance
         */
        ApiRequest.prototype.recaptcha = "";

        /**
         * ApiRequest subEnvironment.
         * @member {string} subEnvironment
         * @memberof Authentication.ApiRequest
         * @instance
         */
        ApiRequest.prototype.subEnvironment = "";

        /**
         * ApiRequest qrcMessageKey.
         * @member {Authentication.IQrcMessageKey|null|undefined} qrcMessageKey
         * @memberof Authentication.ApiRequest
         * @instance
         */
        ApiRequest.prototype.qrcMessageKey = null;

        /**
         * Creates a new ApiRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.ApiRequest
         * @static
         * @param {Authentication.IApiRequest=} [properties] Properties to set
         * @returns {Authentication.ApiRequest} ApiRequest instance
         */
        ApiRequest.create = function create(properties) {
            return new ApiRequest(properties);
        };

        /**
         * Encodes the specified ApiRequest message. Does not implicitly {@link Authentication.ApiRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ApiRequest
         * @static
         * @param {Authentication.IApiRequest} message ApiRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ApiRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedTransmissionKey != null && Object.hasOwnProperty.call(message, "encryptedTransmissionKey"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedTransmissionKey);
            if (message.publicKeyId != null && Object.hasOwnProperty.call(message, "publicKeyId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.publicKeyId);
            if (message.locale != null && Object.hasOwnProperty.call(message, "locale"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.locale);
            if (message.encryptedPayload != null && Object.hasOwnProperty.call(message, "encryptedPayload"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.encryptedPayload);
            if (message.encryptionType != null && Object.hasOwnProperty.call(message, "encryptionType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.encryptionType);
            if (message.recaptcha != null && Object.hasOwnProperty.call(message, "recaptcha"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.recaptcha);
            if (message.subEnvironment != null && Object.hasOwnProperty.call(message, "subEnvironment"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.subEnvironment);
            if (message.qrcMessageKey != null && Object.hasOwnProperty.call(message, "qrcMessageKey"))
                $root.Authentication.QrcMessageKey.encode(message.qrcMessageKey, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes an ApiRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ApiRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ApiRequest} ApiRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ApiRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ApiRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedTransmissionKey = reader.bytes();
                        break;
                    }
                case 2: {
                        message.publicKeyId = reader.int32();
                        break;
                    }
                case 3: {
                        message.locale = reader.string();
                        break;
                    }
                case 4: {
                        message.encryptedPayload = reader.bytes();
                        break;
                    }
                case 5: {
                        message.encryptionType = reader.int32();
                        break;
                    }
                case 6: {
                        message.recaptcha = reader.string();
                        break;
                    }
                case 7: {
                        message.subEnvironment = reader.string();
                        break;
                    }
                case 8: {
                        message.qrcMessageKey = $root.Authentication.QrcMessageKey.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an ApiRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ApiRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ApiRequest} ApiRequest
         */
        ApiRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ApiRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ApiRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ApiRequest();
            if (object.encryptedTransmissionKey != null)
                if (typeof object.encryptedTransmissionKey === "string")
                    $util.base64.decode(object.encryptedTransmissionKey, message.encryptedTransmissionKey = $util.newBuffer($util.base64.length(object.encryptedTransmissionKey)), 0);
                else if (object.encryptedTransmissionKey.length >= 0)
                    message.encryptedTransmissionKey = object.encryptedTransmissionKey;
            if (object.publicKeyId != null)
                message.publicKeyId = object.publicKeyId | 0;
            if (object.locale != null)
                message.locale = String(object.locale);
            if (object.encryptedPayload != null)
                if (typeof object.encryptedPayload === "string")
                    $util.base64.decode(object.encryptedPayload, message.encryptedPayload = $util.newBuffer($util.base64.length(object.encryptedPayload)), 0);
                else if (object.encryptedPayload.length >= 0)
                    message.encryptedPayload = object.encryptedPayload;
            if (object.encryptionType != null)
                message.encryptionType = object.encryptionType | 0;
            if (object.recaptcha != null)
                message.recaptcha = String(object.recaptcha);
            if (object.subEnvironment != null)
                message.subEnvironment = String(object.subEnvironment);
            if (object.qrcMessageKey != null) {
                if (!$util.isObject(object.qrcMessageKey))
                    throw TypeError(".Authentication.ApiRequest.qrcMessageKey: object expected");
                message.qrcMessageKey = $root.Authentication.QrcMessageKey.fromObject(object.qrcMessageKey, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an ApiRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ApiRequest
         * @static
         * @param {Authentication.ApiRequest} message ApiRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ApiRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.encryptedTransmissionKey = "";
                else {
                    object.encryptedTransmissionKey = [];
                    if (options.bytes !== Array)
                        object.encryptedTransmissionKey = $util.newBuffer(object.encryptedTransmissionKey);
                }
                object.publicKeyId = 0;
                object.locale = "";
                if (options.bytes === String)
                    object.encryptedPayload = "";
                else {
                    object.encryptedPayload = [];
                    if (options.bytes !== Array)
                        object.encryptedPayload = $util.newBuffer(object.encryptedPayload);
                }
                object.encryptionType = 0;
                object.recaptcha = "";
                object.subEnvironment = "";
                object.qrcMessageKey = null;
            }
            if (message.encryptedTransmissionKey != null && Object.hasOwnProperty.call(message, "encryptedTransmissionKey"))
                object.encryptedTransmissionKey = options.bytes === String ? $util.base64.encode(message.encryptedTransmissionKey, 0, message.encryptedTransmissionKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedTransmissionKey) : message.encryptedTransmissionKey;
            if (message.publicKeyId != null && Object.hasOwnProperty.call(message, "publicKeyId"))
                object.publicKeyId = message.publicKeyId;
            if (message.locale != null && Object.hasOwnProperty.call(message, "locale"))
                object.locale = message.locale;
            if (message.encryptedPayload != null && Object.hasOwnProperty.call(message, "encryptedPayload"))
                object.encryptedPayload = options.bytes === String ? $util.base64.encode(message.encryptedPayload, 0, message.encryptedPayload.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedPayload) : message.encryptedPayload;
            if (message.encryptionType != null && Object.hasOwnProperty.call(message, "encryptionType"))
                object.encryptionType = message.encryptionType;
            if (message.recaptcha != null && Object.hasOwnProperty.call(message, "recaptcha"))
                object.recaptcha = message.recaptcha;
            if (message.subEnvironment != null && Object.hasOwnProperty.call(message, "subEnvironment"))
                object.subEnvironment = message.subEnvironment;
            if (message.qrcMessageKey != null && Object.hasOwnProperty.call(message, "qrcMessageKey"))
                object.qrcMessageKey = $root.Authentication.QrcMessageKey.toObject(message.qrcMessageKey, options, q + 1);
            return object;
        };

        /**
         * Converts this ApiRequest to JSON.
         * @function toJSON
         * @memberof Authentication.ApiRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ApiRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ApiRequest
         * @function getTypeUrl
         * @memberof Authentication.ApiRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ApiRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ApiRequest";
        };

        return ApiRequest;
    })();

    Authentication.ApiRequestPayload = (function() {

        /**
         * Properties of an ApiRequestPayload.
         * @memberof Authentication
         * @interface IApiRequestPayload
         * @property {Uint8Array|null} [payload] ApiRequestPayload payload
         * @property {Uint8Array|null} [encryptedSessionToken] ApiRequestPayload encryptedSessionToken
         * @property {Uint8Array|null} [timeToken] ApiRequestPayload timeToken
         * @property {number|null} [apiVersion] ApiRequestPayload apiVersion
         */

        /**
         * Constructs a new ApiRequestPayload.
         * @memberof Authentication
         * @classdesc Represents an ApiRequestPayload.
         * @implements IApiRequestPayload
         * @constructor
         * @param {Authentication.IApiRequestPayload=} [properties] Properties to set
         */
        function ApiRequestPayload(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ApiRequestPayload payload.
         * @member {Uint8Array} payload
         * @memberof Authentication.ApiRequestPayload
         * @instance
         */
        ApiRequestPayload.prototype.payload = $util.newBuffer([]);

        /**
         * ApiRequestPayload encryptedSessionToken.
         * @member {Uint8Array} encryptedSessionToken
         * @memberof Authentication.ApiRequestPayload
         * @instance
         */
        ApiRequestPayload.prototype.encryptedSessionToken = $util.newBuffer([]);

        /**
         * ApiRequestPayload timeToken.
         * @member {Uint8Array} timeToken
         * @memberof Authentication.ApiRequestPayload
         * @instance
         */
        ApiRequestPayload.prototype.timeToken = $util.newBuffer([]);

        /**
         * ApiRequestPayload apiVersion.
         * @member {number} apiVersion
         * @memberof Authentication.ApiRequestPayload
         * @instance
         */
        ApiRequestPayload.prototype.apiVersion = 0;

        /**
         * Creates a new ApiRequestPayload instance using the specified properties.
         * @function create
         * @memberof Authentication.ApiRequestPayload
         * @static
         * @param {Authentication.IApiRequestPayload=} [properties] Properties to set
         * @returns {Authentication.ApiRequestPayload} ApiRequestPayload instance
         */
        ApiRequestPayload.create = function create(properties) {
            return new ApiRequestPayload(properties);
        };

        /**
         * Encodes the specified ApiRequestPayload message. Does not implicitly {@link Authentication.ApiRequestPayload.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ApiRequestPayload
         * @static
         * @param {Authentication.IApiRequestPayload} message ApiRequestPayload message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ApiRequestPayload.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.payload);
            if (message.encryptedSessionToken != null && Object.hasOwnProperty.call(message, "encryptedSessionToken"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encryptedSessionToken);
            if (message.timeToken != null && Object.hasOwnProperty.call(message, "timeToken"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.timeToken);
            if (message.apiVersion != null && Object.hasOwnProperty.call(message, "apiVersion"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.apiVersion);
            return writer;
        };

        /**
         * Decodes an ApiRequestPayload message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ApiRequestPayload
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ApiRequestPayload} ApiRequestPayload
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ApiRequestPayload.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ApiRequestPayload();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.payload = reader.bytes();
                        break;
                    }
                case 2: {
                        message.encryptedSessionToken = reader.bytes();
                        break;
                    }
                case 3: {
                        message.timeToken = reader.bytes();
                        break;
                    }
                case 4: {
                        message.apiVersion = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an ApiRequestPayload message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ApiRequestPayload
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ApiRequestPayload} ApiRequestPayload
         */
        ApiRequestPayload.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ApiRequestPayload)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ApiRequestPayload: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ApiRequestPayload();
            if (object.payload != null)
                if (typeof object.payload === "string")
                    $util.base64.decode(object.payload, message.payload = $util.newBuffer($util.base64.length(object.payload)), 0);
                else if (object.payload.length >= 0)
                    message.payload = object.payload;
            if (object.encryptedSessionToken != null)
                if (typeof object.encryptedSessionToken === "string")
                    $util.base64.decode(object.encryptedSessionToken, message.encryptedSessionToken = $util.newBuffer($util.base64.length(object.encryptedSessionToken)), 0);
                else if (object.encryptedSessionToken.length >= 0)
                    message.encryptedSessionToken = object.encryptedSessionToken;
            if (object.timeToken != null)
                if (typeof object.timeToken === "string")
                    $util.base64.decode(object.timeToken, message.timeToken = $util.newBuffer($util.base64.length(object.timeToken)), 0);
                else if (object.timeToken.length >= 0)
                    message.timeToken = object.timeToken;
            if (object.apiVersion != null)
                message.apiVersion = object.apiVersion | 0;
            return message;
        };

        /**
         * Creates a plain object from an ApiRequestPayload message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ApiRequestPayload
         * @static
         * @param {Authentication.ApiRequestPayload} message ApiRequestPayload
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ApiRequestPayload.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.payload = "";
                else {
                    object.payload = [];
                    if (options.bytes !== Array)
                        object.payload = $util.newBuffer(object.payload);
                }
                if (options.bytes === String)
                    object.encryptedSessionToken = "";
                else {
                    object.encryptedSessionToken = [];
                    if (options.bytes !== Array)
                        object.encryptedSessionToken = $util.newBuffer(object.encryptedSessionToken);
                }
                if (options.bytes === String)
                    object.timeToken = "";
                else {
                    object.timeToken = [];
                    if (options.bytes !== Array)
                        object.timeToken = $util.newBuffer(object.timeToken);
                }
                object.apiVersion = 0;
            }
            if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                object.payload = options.bytes === String ? $util.base64.encode(message.payload, 0, message.payload.length) : options.bytes === Array ? Array.prototype.slice.call(message.payload) : message.payload;
            if (message.encryptedSessionToken != null && Object.hasOwnProperty.call(message, "encryptedSessionToken"))
                object.encryptedSessionToken = options.bytes === String ? $util.base64.encode(message.encryptedSessionToken, 0, message.encryptedSessionToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedSessionToken) : message.encryptedSessionToken;
            if (message.timeToken != null && Object.hasOwnProperty.call(message, "timeToken"))
                object.timeToken = options.bytes === String ? $util.base64.encode(message.timeToken, 0, message.timeToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.timeToken) : message.timeToken;
            if (message.apiVersion != null && Object.hasOwnProperty.call(message, "apiVersion"))
                object.apiVersion = message.apiVersion;
            return object;
        };

        /**
         * Converts this ApiRequestPayload to JSON.
         * @function toJSON
         * @memberof Authentication.ApiRequestPayload
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ApiRequestPayload.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ApiRequestPayload
         * @function getTypeUrl
         * @memberof Authentication.ApiRequestPayload
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ApiRequestPayload.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ApiRequestPayload";
        };

        return ApiRequestPayload;
    })();

    Authentication.Transform = (function() {

        /**
         * Properties of a Transform.
         * @memberof Authentication
         * @interface ITransform
         * @property {Uint8Array|null} [key] Transform key
         * @property {Uint8Array|null} [encryptedDeviceToken] Transform encryptedDeviceToken
         */

        /**
         * Constructs a new Transform.
         * @memberof Authentication
         * @classdesc Represents a Transform.
         * @implements ITransform
         * @constructor
         * @param {Authentication.ITransform=} [properties] Properties to set
         */
        function Transform(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Transform key.
         * @member {Uint8Array} key
         * @memberof Authentication.Transform
         * @instance
         */
        Transform.prototype.key = $util.newBuffer([]);

        /**
         * Transform encryptedDeviceToken.
         * @member {Uint8Array} encryptedDeviceToken
         * @memberof Authentication.Transform
         * @instance
         */
        Transform.prototype.encryptedDeviceToken = $util.newBuffer([]);

        /**
         * Creates a new Transform instance using the specified properties.
         * @function create
         * @memberof Authentication.Transform
         * @static
         * @param {Authentication.ITransform=} [properties] Properties to set
         * @returns {Authentication.Transform} Transform instance
         */
        Transform.create = function create(properties) {
            return new Transform(properties);
        };

        /**
         * Encodes the specified Transform message. Does not implicitly {@link Authentication.Transform.verify|verify} messages.
         * @function encode
         * @memberof Authentication.Transform
         * @static
         * @param {Authentication.ITransform} message Transform message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Transform.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.key);
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encryptedDeviceToken);
            return writer;
        };

        /**
         * Decodes a Transform message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.Transform
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.Transform} Transform
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Transform.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.Transform();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.key = reader.bytes();
                        break;
                    }
                case 2: {
                        message.encryptedDeviceToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a Transform message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.Transform
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.Transform} Transform
         */
        Transform.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.Transform)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.Transform: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.Transform();
            if (object.key != null)
                if (typeof object.key === "string")
                    $util.base64.decode(object.key, message.key = $util.newBuffer($util.base64.length(object.key)), 0);
                else if (object.key.length >= 0)
                    message.key = object.key;
            if (object.encryptedDeviceToken != null)
                if (typeof object.encryptedDeviceToken === "string")
                    $util.base64.decode(object.encryptedDeviceToken, message.encryptedDeviceToken = $util.newBuffer($util.base64.length(object.encryptedDeviceToken)), 0);
                else if (object.encryptedDeviceToken.length >= 0)
                    message.encryptedDeviceToken = object.encryptedDeviceToken;
            return message;
        };

        /**
         * Creates a plain object from a Transform message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.Transform
         * @static
         * @param {Authentication.Transform} message Transform
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Transform.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.key = "";
                else {
                    object.key = [];
                    if (options.bytes !== Array)
                        object.key = $util.newBuffer(object.key);
                }
                if (options.bytes === String)
                    object.encryptedDeviceToken = "";
                else {
                    object.encryptedDeviceToken = [];
                    if (options.bytes !== Array)
                        object.encryptedDeviceToken = $util.newBuffer(object.encryptedDeviceToken);
                }
            }
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                object.key = options.bytes === String ? $util.base64.encode(message.key, 0, message.key.length) : options.bytes === Array ? Array.prototype.slice.call(message.key) : message.key;
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                object.encryptedDeviceToken = options.bytes === String ? $util.base64.encode(message.encryptedDeviceToken, 0, message.encryptedDeviceToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDeviceToken) : message.encryptedDeviceToken;
            return object;
        };

        /**
         * Converts this Transform to JSON.
         * @function toJSON
         * @memberof Authentication.Transform
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Transform.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Transform
         * @function getTypeUrl
         * @memberof Authentication.Transform
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Transform.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.Transform";
        };

        return Transform;
    })();

    Authentication.DeviceRequest = (function() {

        /**
         * Properties of a DeviceRequest.
         * @memberof Authentication
         * @interface IDeviceRequest
         * @property {string|null} [clientVersion] DeviceRequest clientVersion
         * @property {string|null} [deviceName] DeviceRequest deviceName
         * @property {string|null} [devicePlatform] DeviceRequest devicePlatform
         * @property {Authentication.ClientFormFactor|null} [clientFormFactor] DeviceRequest clientFormFactor
         * @property {string|null} [username] DeviceRequest username
         */

        /**
         * Constructs a new DeviceRequest.
         * @memberof Authentication
         * @classdesc Represents a DeviceRequest.
         * @implements IDeviceRequest
         * @constructor
         * @param {Authentication.IDeviceRequest=} [properties] Properties to set
         */
        function DeviceRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeviceRequest clientVersion.
         * @member {string} clientVersion
         * @memberof Authentication.DeviceRequest
         * @instance
         */
        DeviceRequest.prototype.clientVersion = "";

        /**
         * DeviceRequest deviceName.
         * @member {string} deviceName
         * @memberof Authentication.DeviceRequest
         * @instance
         */
        DeviceRequest.prototype.deviceName = "";

        /**
         * DeviceRequest devicePlatform.
         * @member {string} devicePlatform
         * @memberof Authentication.DeviceRequest
         * @instance
         */
        DeviceRequest.prototype.devicePlatform = "";

        /**
         * DeviceRequest clientFormFactor.
         * @member {Authentication.ClientFormFactor} clientFormFactor
         * @memberof Authentication.DeviceRequest
         * @instance
         */
        DeviceRequest.prototype.clientFormFactor = 0;

        /**
         * DeviceRequest username.
         * @member {string} username
         * @memberof Authentication.DeviceRequest
         * @instance
         */
        DeviceRequest.prototype.username = "";

        /**
         * Creates a new DeviceRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.DeviceRequest
         * @static
         * @param {Authentication.IDeviceRequest=} [properties] Properties to set
         * @returns {Authentication.DeviceRequest} DeviceRequest instance
         */
        DeviceRequest.create = function create(properties) {
            return new DeviceRequest(properties);
        };

        /**
         * Encodes the specified DeviceRequest message. Does not implicitly {@link Authentication.DeviceRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.DeviceRequest
         * @static
         * @param {Authentication.IDeviceRequest} message DeviceRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.clientVersion);
            if (message.deviceName != null && Object.hasOwnProperty.call(message, "deviceName"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.deviceName);
            if (message.devicePlatform != null && Object.hasOwnProperty.call(message, "devicePlatform"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.devicePlatform);
            if (message.clientFormFactor != null && Object.hasOwnProperty.call(message, "clientFormFactor"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.clientFormFactor);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.username);
            return writer;
        };

        /**
         * Decodes a DeviceRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.DeviceRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.DeviceRequest} DeviceRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.DeviceRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.clientVersion = reader.string();
                        break;
                    }
                case 2: {
                        message.deviceName = reader.string();
                        break;
                    }
                case 3: {
                        message.devicePlatform = reader.string();
                        break;
                    }
                case 4: {
                        message.clientFormFactor = reader.int32();
                        break;
                    }
                case 5: {
                        message.username = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a DeviceRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.DeviceRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.DeviceRequest} DeviceRequest
         */
        DeviceRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.DeviceRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.DeviceRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.DeviceRequest();
            if (object.clientVersion != null)
                message.clientVersion = String(object.clientVersion);
            if (object.deviceName != null)
                message.deviceName = String(object.deviceName);
            if (object.devicePlatform != null)
                message.devicePlatform = String(object.devicePlatform);
            switch (object.clientFormFactor) {
            default:
                if (typeof object.clientFormFactor === "number") {
                    message.clientFormFactor = object.clientFormFactor;
                    break;
                }
                break;
            case "FF_EMPTY":
            case 0:
                message.clientFormFactor = 0;
                break;
            case "FF_PHONE":
            case 1:
                message.clientFormFactor = 1;
                break;
            case "FF_TABLET":
            case 2:
                message.clientFormFactor = 2;
                break;
            case "FF_WATCH":
            case 3:
                message.clientFormFactor = 3;
                break;
            }
            if (object.username != null)
                message.username = String(object.username);
            return message;
        };

        /**
         * Creates a plain object from a DeviceRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.DeviceRequest
         * @static
         * @param {Authentication.DeviceRequest} message DeviceRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeviceRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.clientVersion = "";
                object.deviceName = "";
                object.devicePlatform = "";
                object.clientFormFactor = options.enums === String ? "FF_EMPTY" : 0;
                object.username = "";
            }
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                object.clientVersion = message.clientVersion;
            if (message.deviceName != null && Object.hasOwnProperty.call(message, "deviceName"))
                object.deviceName = message.deviceName;
            if (message.devicePlatform != null && Object.hasOwnProperty.call(message, "devicePlatform"))
                object.devicePlatform = message.devicePlatform;
            if (message.clientFormFactor != null && Object.hasOwnProperty.call(message, "clientFormFactor"))
                object.clientFormFactor = options.enums === String ? $root.Authentication.ClientFormFactor[message.clientFormFactor] === undefined ? message.clientFormFactor : $root.Authentication.ClientFormFactor[message.clientFormFactor] : message.clientFormFactor;
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            return object;
        };

        /**
         * Converts this DeviceRequest to JSON.
         * @function toJSON
         * @memberof Authentication.DeviceRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeviceRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeviceRequest
         * @function getTypeUrl
         * @memberof Authentication.DeviceRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeviceRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.DeviceRequest";
        };

        return DeviceRequest;
    })();

    Authentication.AuthRequest = (function() {

        /**
         * Properties of an AuthRequest.
         * @memberof Authentication
         * @interface IAuthRequest
         * @property {string|null} [clientVersion] AuthRequest clientVersion
         * @property {string|null} [username] AuthRequest username
         * @property {Uint8Array|null} [encryptedDeviceToken] AuthRequest encryptedDeviceToken
         */

        /**
         * Constructs a new AuthRequest.
         * @memberof Authentication
         * @classdesc Represents an AuthRequest.
         * @implements IAuthRequest
         * @constructor
         * @param {Authentication.IAuthRequest=} [properties] Properties to set
         */
        function AuthRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AuthRequest clientVersion.
         * @member {string} clientVersion
         * @memberof Authentication.AuthRequest
         * @instance
         */
        AuthRequest.prototype.clientVersion = "";

        /**
         * AuthRequest username.
         * @member {string} username
         * @memberof Authentication.AuthRequest
         * @instance
         */
        AuthRequest.prototype.username = "";

        /**
         * AuthRequest encryptedDeviceToken.
         * @member {Uint8Array} encryptedDeviceToken
         * @memberof Authentication.AuthRequest
         * @instance
         */
        AuthRequest.prototype.encryptedDeviceToken = $util.newBuffer([]);

        /**
         * Creates a new AuthRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.AuthRequest
         * @static
         * @param {Authentication.IAuthRequest=} [properties] Properties to set
         * @returns {Authentication.AuthRequest} AuthRequest instance
         */
        AuthRequest.create = function create(properties) {
            return new AuthRequest(properties);
        };

        /**
         * Encodes the specified AuthRequest message. Does not implicitly {@link Authentication.AuthRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.AuthRequest
         * @static
         * @param {Authentication.IAuthRequest} message AuthRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AuthRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.clientVersion);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.username);
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.encryptedDeviceToken);
            return writer;
        };

        /**
         * Decodes an AuthRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.AuthRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.AuthRequest} AuthRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AuthRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.AuthRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.clientVersion = reader.string();
                        break;
                    }
                case 2: {
                        message.username = reader.string();
                        break;
                    }
                case 3: {
                        message.encryptedDeviceToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an AuthRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.AuthRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.AuthRequest} AuthRequest
         */
        AuthRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.AuthRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.AuthRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.AuthRequest();
            if (object.clientVersion != null)
                message.clientVersion = String(object.clientVersion);
            if (object.username != null)
                message.username = String(object.username);
            if (object.encryptedDeviceToken != null)
                if (typeof object.encryptedDeviceToken === "string")
                    $util.base64.decode(object.encryptedDeviceToken, message.encryptedDeviceToken = $util.newBuffer($util.base64.length(object.encryptedDeviceToken)), 0);
                else if (object.encryptedDeviceToken.length >= 0)
                    message.encryptedDeviceToken = object.encryptedDeviceToken;
            return message;
        };

        /**
         * Creates a plain object from an AuthRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.AuthRequest
         * @static
         * @param {Authentication.AuthRequest} message AuthRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AuthRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.clientVersion = "";
                object.username = "";
                if (options.bytes === String)
                    object.encryptedDeviceToken = "";
                else {
                    object.encryptedDeviceToken = [];
                    if (options.bytes !== Array)
                        object.encryptedDeviceToken = $util.newBuffer(object.encryptedDeviceToken);
                }
            }
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                object.clientVersion = message.clientVersion;
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                object.encryptedDeviceToken = options.bytes === String ? $util.base64.encode(message.encryptedDeviceToken, 0, message.encryptedDeviceToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDeviceToken) : message.encryptedDeviceToken;
            return object;
        };

        /**
         * Converts this AuthRequest to JSON.
         * @function toJSON
         * @memberof Authentication.AuthRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AuthRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AuthRequest
         * @function getTypeUrl
         * @memberof Authentication.AuthRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AuthRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.AuthRequest";
        };

        return AuthRequest;
    })();

    Authentication.NewUserMinimumParams = (function() {

        /**
         * Properties of a NewUserMinimumParams.
         * @memberof Authentication
         * @interface INewUserMinimumParams
         * @property {number|null} [minimumIterations] NewUserMinimumParams minimumIterations
         * @property {Array.<string>|null} [passwordMatchRegex] NewUserMinimumParams passwordMatchRegex
         * @property {Array.<string>|null} [passwordMatchDescription] NewUserMinimumParams passwordMatchDescription
         * @property {boolean|null} [isEnterpriseDomain] NewUserMinimumParams isEnterpriseDomain
         * @property {Uint8Array|null} [enterpriseEccPublicKey] NewUserMinimumParams enterpriseEccPublicKey
         * @property {boolean|null} [forbidKeyType2] NewUserMinimumParams forbidKeyType2
         */

        /**
         * Constructs a new NewUserMinimumParams.
         * @memberof Authentication
         * @classdesc Represents a NewUserMinimumParams.
         * @implements INewUserMinimumParams
         * @constructor
         * @param {Authentication.INewUserMinimumParams=} [properties] Properties to set
         */
        function NewUserMinimumParams(properties) {
            this.passwordMatchRegex = [];
            this.passwordMatchDescription = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NewUserMinimumParams minimumIterations.
         * @member {number} minimumIterations
         * @memberof Authentication.NewUserMinimumParams
         * @instance
         */
        NewUserMinimumParams.prototype.minimumIterations = 0;

        /**
         * NewUserMinimumParams passwordMatchRegex.
         * @member {Array.<string>} passwordMatchRegex
         * @memberof Authentication.NewUserMinimumParams
         * @instance
         */
        NewUserMinimumParams.prototype.passwordMatchRegex = $util.emptyArray;

        /**
         * NewUserMinimumParams passwordMatchDescription.
         * @member {Array.<string>} passwordMatchDescription
         * @memberof Authentication.NewUserMinimumParams
         * @instance
         */
        NewUserMinimumParams.prototype.passwordMatchDescription = $util.emptyArray;

        /**
         * NewUserMinimumParams isEnterpriseDomain.
         * @member {boolean} isEnterpriseDomain
         * @memberof Authentication.NewUserMinimumParams
         * @instance
         */
        NewUserMinimumParams.prototype.isEnterpriseDomain = false;

        /**
         * NewUserMinimumParams enterpriseEccPublicKey.
         * @member {Uint8Array} enterpriseEccPublicKey
         * @memberof Authentication.NewUserMinimumParams
         * @instance
         */
        NewUserMinimumParams.prototype.enterpriseEccPublicKey = $util.newBuffer([]);

        /**
         * NewUserMinimumParams forbidKeyType2.
         * @member {boolean} forbidKeyType2
         * @memberof Authentication.NewUserMinimumParams
         * @instance
         */
        NewUserMinimumParams.prototype.forbidKeyType2 = false;

        /**
         * Creates a new NewUserMinimumParams instance using the specified properties.
         * @function create
         * @memberof Authentication.NewUserMinimumParams
         * @static
         * @param {Authentication.INewUserMinimumParams=} [properties] Properties to set
         * @returns {Authentication.NewUserMinimumParams} NewUserMinimumParams instance
         */
        NewUserMinimumParams.create = function create(properties) {
            return new NewUserMinimumParams(properties);
        };

        /**
         * Encodes the specified NewUserMinimumParams message. Does not implicitly {@link Authentication.NewUserMinimumParams.verify|verify} messages.
         * @function encode
         * @memberof Authentication.NewUserMinimumParams
         * @static
         * @param {Authentication.INewUserMinimumParams} message NewUserMinimumParams message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NewUserMinimumParams.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.minimumIterations != null && Object.hasOwnProperty.call(message, "minimumIterations"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.minimumIterations);
            if (message.passwordMatchRegex != null && message.passwordMatchRegex.length)
                for (let i = 0; i < message.passwordMatchRegex.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.passwordMatchRegex[i]);
            if (message.passwordMatchDescription != null && message.passwordMatchDescription.length)
                for (let i = 0; i < message.passwordMatchDescription.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.passwordMatchDescription[i]);
            if (message.isEnterpriseDomain != null && Object.hasOwnProperty.call(message, "isEnterpriseDomain"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.isEnterpriseDomain);
            if (message.enterpriseEccPublicKey != null && Object.hasOwnProperty.call(message, "enterpriseEccPublicKey"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.enterpriseEccPublicKey);
            if (message.forbidKeyType2 != null && Object.hasOwnProperty.call(message, "forbidKeyType2"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.forbidKeyType2);
            return writer;
        };

        /**
         * Decodes a NewUserMinimumParams message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.NewUserMinimumParams
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.NewUserMinimumParams} NewUserMinimumParams
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NewUserMinimumParams.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.NewUserMinimumParams();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.minimumIterations = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.passwordMatchRegex && message.passwordMatchRegex.length))
                            message.passwordMatchRegex = [];
                        message.passwordMatchRegex.push(reader.string());
                        break;
                    }
                case 3: {
                        if (!(message.passwordMatchDescription && message.passwordMatchDescription.length))
                            message.passwordMatchDescription = [];
                        message.passwordMatchDescription.push(reader.string());
                        break;
                    }
                case 4: {
                        message.isEnterpriseDomain = reader.bool();
                        break;
                    }
                case 5: {
                        message.enterpriseEccPublicKey = reader.bytes();
                        break;
                    }
                case 6: {
                        message.forbidKeyType2 = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a NewUserMinimumParams message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.NewUserMinimumParams
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.NewUserMinimumParams} NewUserMinimumParams
         */
        NewUserMinimumParams.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.NewUserMinimumParams)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.NewUserMinimumParams: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.NewUserMinimumParams();
            if (object.minimumIterations != null)
                message.minimumIterations = object.minimumIterations | 0;
            if (object.passwordMatchRegex) {
                if (!Array.isArray(object.passwordMatchRegex))
                    throw TypeError(".Authentication.NewUserMinimumParams.passwordMatchRegex: array expected");
                message.passwordMatchRegex = [];
                for (let i = 0; i < object.passwordMatchRegex.length; ++i)
                    message.passwordMatchRegex[i] = String(object.passwordMatchRegex[i]);
            }
            if (object.passwordMatchDescription) {
                if (!Array.isArray(object.passwordMatchDescription))
                    throw TypeError(".Authentication.NewUserMinimumParams.passwordMatchDescription: array expected");
                message.passwordMatchDescription = [];
                for (let i = 0; i < object.passwordMatchDescription.length; ++i)
                    message.passwordMatchDescription[i] = String(object.passwordMatchDescription[i]);
            }
            if (object.isEnterpriseDomain != null)
                message.isEnterpriseDomain = Boolean(object.isEnterpriseDomain);
            if (object.enterpriseEccPublicKey != null)
                if (typeof object.enterpriseEccPublicKey === "string")
                    $util.base64.decode(object.enterpriseEccPublicKey, message.enterpriseEccPublicKey = $util.newBuffer($util.base64.length(object.enterpriseEccPublicKey)), 0);
                else if (object.enterpriseEccPublicKey.length >= 0)
                    message.enterpriseEccPublicKey = object.enterpriseEccPublicKey;
            if (object.forbidKeyType2 != null)
                message.forbidKeyType2 = Boolean(object.forbidKeyType2);
            return message;
        };

        /**
         * Creates a plain object from a NewUserMinimumParams message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.NewUserMinimumParams
         * @static
         * @param {Authentication.NewUserMinimumParams} message NewUserMinimumParams
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NewUserMinimumParams.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.passwordMatchRegex = [];
                object.passwordMatchDescription = [];
            }
            if (options.defaults) {
                object.minimumIterations = 0;
                object.isEnterpriseDomain = false;
                if (options.bytes === String)
                    object.enterpriseEccPublicKey = "";
                else {
                    object.enterpriseEccPublicKey = [];
                    if (options.bytes !== Array)
                        object.enterpriseEccPublicKey = $util.newBuffer(object.enterpriseEccPublicKey);
                }
                object.forbidKeyType2 = false;
            }
            if (message.minimumIterations != null && Object.hasOwnProperty.call(message, "minimumIterations"))
                object.minimumIterations = message.minimumIterations;
            if (message.passwordMatchRegex && message.passwordMatchRegex.length) {
                object.passwordMatchRegex = [];
                for (let j = 0; j < message.passwordMatchRegex.length; ++j)
                    object.passwordMatchRegex[j] = message.passwordMatchRegex[j];
            }
            if (message.passwordMatchDescription && message.passwordMatchDescription.length) {
                object.passwordMatchDescription = [];
                for (let j = 0; j < message.passwordMatchDescription.length; ++j)
                    object.passwordMatchDescription[j] = message.passwordMatchDescription[j];
            }
            if (message.isEnterpriseDomain != null && Object.hasOwnProperty.call(message, "isEnterpriseDomain"))
                object.isEnterpriseDomain = message.isEnterpriseDomain;
            if (message.enterpriseEccPublicKey != null && Object.hasOwnProperty.call(message, "enterpriseEccPublicKey"))
                object.enterpriseEccPublicKey = options.bytes === String ? $util.base64.encode(message.enterpriseEccPublicKey, 0, message.enterpriseEccPublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.enterpriseEccPublicKey) : message.enterpriseEccPublicKey;
            if (message.forbidKeyType2 != null && Object.hasOwnProperty.call(message, "forbidKeyType2"))
                object.forbidKeyType2 = message.forbidKeyType2;
            return object;
        };

        /**
         * Converts this NewUserMinimumParams to JSON.
         * @function toJSON
         * @memberof Authentication.NewUserMinimumParams
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NewUserMinimumParams.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NewUserMinimumParams
         * @function getTypeUrl
         * @memberof Authentication.NewUserMinimumParams
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NewUserMinimumParams.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.NewUserMinimumParams";
        };

        return NewUserMinimumParams;
    })();

    Authentication.PreLoginRequest = (function() {

        /**
         * Properties of a PreLoginRequest.
         * @memberof Authentication
         * @interface IPreLoginRequest
         * @property {Authentication.IAuthRequest|null} [authRequest] PreLoginRequest authRequest
         * @property {Authentication.LoginType|null} [loginType] PreLoginRequest loginType
         * @property {Uint8Array|null} [twoFactorToken] PreLoginRequest twoFactorToken
         */

        /**
         * Constructs a new PreLoginRequest.
         * @memberof Authentication
         * @classdesc Represents a PreLoginRequest.
         * @implements IPreLoginRequest
         * @constructor
         * @param {Authentication.IPreLoginRequest=} [properties] Properties to set
         */
        function PreLoginRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PreLoginRequest authRequest.
         * @member {Authentication.IAuthRequest|null|undefined} authRequest
         * @memberof Authentication.PreLoginRequest
         * @instance
         */
        PreLoginRequest.prototype.authRequest = null;

        /**
         * PreLoginRequest loginType.
         * @member {Authentication.LoginType} loginType
         * @memberof Authentication.PreLoginRequest
         * @instance
         */
        PreLoginRequest.prototype.loginType = 0;

        /**
         * PreLoginRequest twoFactorToken.
         * @member {Uint8Array} twoFactorToken
         * @memberof Authentication.PreLoginRequest
         * @instance
         */
        PreLoginRequest.prototype.twoFactorToken = $util.newBuffer([]);

        /**
         * Creates a new PreLoginRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.PreLoginRequest
         * @static
         * @param {Authentication.IPreLoginRequest=} [properties] Properties to set
         * @returns {Authentication.PreLoginRequest} PreLoginRequest instance
         */
        PreLoginRequest.create = function create(properties) {
            return new PreLoginRequest(properties);
        };

        /**
         * Encodes the specified PreLoginRequest message. Does not implicitly {@link Authentication.PreLoginRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.PreLoginRequest
         * @static
         * @param {Authentication.IPreLoginRequest} message PreLoginRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PreLoginRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.authRequest != null && Object.hasOwnProperty.call(message, "authRequest"))
                $root.Authentication.AuthRequest.encode(message.authRequest, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.loginType != null && Object.hasOwnProperty.call(message, "loginType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.loginType);
            if (message.twoFactorToken != null && Object.hasOwnProperty.call(message, "twoFactorToken"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.twoFactorToken);
            return writer;
        };

        /**
         * Decodes a PreLoginRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.PreLoginRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.PreLoginRequest} PreLoginRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PreLoginRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.PreLoginRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.authRequest = $root.Authentication.AuthRequest.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        message.loginType = reader.int32();
                        break;
                    }
                case 3: {
                        message.twoFactorToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a PreLoginRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.PreLoginRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.PreLoginRequest} PreLoginRequest
         */
        PreLoginRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.PreLoginRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.PreLoginRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.PreLoginRequest();
            if (object.authRequest != null) {
                if (!$util.isObject(object.authRequest))
                    throw TypeError(".Authentication.PreLoginRequest.authRequest: object expected");
                message.authRequest = $root.Authentication.AuthRequest.fromObject(object.authRequest, long + 1);
            }
            switch (object.loginType) {
            default:
                if (typeof object.loginType === "number") {
                    message.loginType = object.loginType;
                    break;
                }
                break;
            case "NORMAL":
            case 0:
                message.loginType = 0;
                break;
            case "SSO":
            case 1:
                message.loginType = 1;
                break;
            case "BIO":
            case 2:
                message.loginType = 2;
                break;
            case "ALTERNATE":
            case 3:
                message.loginType = 3;
                break;
            case "OFFLINE":
            case 4:
                message.loginType = 4;
                break;
            case "FORGOT_PASSWORD":
            case 5:
                message.loginType = 5;
                break;
            case "PASSKEY_BIO":
            case 6:
                message.loginType = 6;
                break;
            }
            if (object.twoFactorToken != null)
                if (typeof object.twoFactorToken === "string")
                    $util.base64.decode(object.twoFactorToken, message.twoFactorToken = $util.newBuffer($util.base64.length(object.twoFactorToken)), 0);
                else if (object.twoFactorToken.length >= 0)
                    message.twoFactorToken = object.twoFactorToken;
            return message;
        };

        /**
         * Creates a plain object from a PreLoginRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.PreLoginRequest
         * @static
         * @param {Authentication.PreLoginRequest} message PreLoginRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PreLoginRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.authRequest = null;
                object.loginType = options.enums === String ? "NORMAL" : 0;
                if (options.bytes === String)
                    object.twoFactorToken = "";
                else {
                    object.twoFactorToken = [];
                    if (options.bytes !== Array)
                        object.twoFactorToken = $util.newBuffer(object.twoFactorToken);
                }
            }
            if (message.authRequest != null && Object.hasOwnProperty.call(message, "authRequest"))
                object.authRequest = $root.Authentication.AuthRequest.toObject(message.authRequest, options, q + 1);
            if (message.loginType != null && Object.hasOwnProperty.call(message, "loginType"))
                object.loginType = options.enums === String ? $root.Authentication.LoginType[message.loginType] === undefined ? message.loginType : $root.Authentication.LoginType[message.loginType] : message.loginType;
            if (message.twoFactorToken != null && Object.hasOwnProperty.call(message, "twoFactorToken"))
                object.twoFactorToken = options.bytes === String ? $util.base64.encode(message.twoFactorToken, 0, message.twoFactorToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.twoFactorToken) : message.twoFactorToken;
            return object;
        };

        /**
         * Converts this PreLoginRequest to JSON.
         * @function toJSON
         * @memberof Authentication.PreLoginRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PreLoginRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PreLoginRequest
         * @function getTypeUrl
         * @memberof Authentication.PreLoginRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PreLoginRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.PreLoginRequest";
        };

        return PreLoginRequest;
    })();

    Authentication.LoginRequest = (function() {

        /**
         * Properties of a LoginRequest.
         * @memberof Authentication
         * @interface ILoginRequest
         * @property {Authentication.IAuthRequest|null} [authRequest] LoginRequest authRequest
         * @property {Authentication.LoginType|null} [loginType] LoginRequest loginType
         * @property {Uint8Array|null} [authenticationHashPrime] LoginRequest authenticationHashPrime
         * @property {Uint8Array|null} [encryptedLoginToken] LoginRequest encryptedLoginToken
         * @property {Uint8Array|null} [authResponse] LoginRequest authResponse
         * @property {number|null} [mcEnterpriseId] LoginRequest mcEnterpriseId
         * @property {string|null} [pushToken] LoginRequest pushToken
         * @property {string|null} [platform] LoginRequest platform
         */

        /**
         * Constructs a new LoginRequest.
         * @memberof Authentication
         * @classdesc Represents a LoginRequest.
         * @implements ILoginRequest
         * @constructor
         * @param {Authentication.ILoginRequest=} [properties] Properties to set
         */
        function LoginRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LoginRequest authRequest.
         * @member {Authentication.IAuthRequest|null|undefined} authRequest
         * @memberof Authentication.LoginRequest
         * @instance
         */
        LoginRequest.prototype.authRequest = null;

        /**
         * LoginRequest loginType.
         * @member {Authentication.LoginType} loginType
         * @memberof Authentication.LoginRequest
         * @instance
         */
        LoginRequest.prototype.loginType = 0;

        /**
         * LoginRequest authenticationHashPrime.
         * @member {Uint8Array} authenticationHashPrime
         * @memberof Authentication.LoginRequest
         * @instance
         */
        LoginRequest.prototype.authenticationHashPrime = $util.newBuffer([]);

        /**
         * LoginRequest encryptedLoginToken.
         * @member {Uint8Array} encryptedLoginToken
         * @memberof Authentication.LoginRequest
         * @instance
         */
        LoginRequest.prototype.encryptedLoginToken = $util.newBuffer([]);

        /**
         * LoginRequest authResponse.
         * @member {Uint8Array} authResponse
         * @memberof Authentication.LoginRequest
         * @instance
         */
        LoginRequest.prototype.authResponse = $util.newBuffer([]);

        /**
         * LoginRequest mcEnterpriseId.
         * @member {number} mcEnterpriseId
         * @memberof Authentication.LoginRequest
         * @instance
         */
        LoginRequest.prototype.mcEnterpriseId = 0;

        /**
         * LoginRequest pushToken.
         * @member {string} pushToken
         * @memberof Authentication.LoginRequest
         * @instance
         */
        LoginRequest.prototype.pushToken = "";

        /**
         * LoginRequest platform.
         * @member {string} platform
         * @memberof Authentication.LoginRequest
         * @instance
         */
        LoginRequest.prototype.platform = "";

        /**
         * Creates a new LoginRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.LoginRequest
         * @static
         * @param {Authentication.ILoginRequest=} [properties] Properties to set
         * @returns {Authentication.LoginRequest} LoginRequest instance
         */
        LoginRequest.create = function create(properties) {
            return new LoginRequest(properties);
        };

        /**
         * Encodes the specified LoginRequest message. Does not implicitly {@link Authentication.LoginRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.LoginRequest
         * @static
         * @param {Authentication.ILoginRequest} message LoginRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LoginRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.authRequest != null && Object.hasOwnProperty.call(message, "authRequest"))
                $root.Authentication.AuthRequest.encode(message.authRequest, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.loginType != null && Object.hasOwnProperty.call(message, "loginType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.loginType);
            if (message.authenticationHashPrime != null && Object.hasOwnProperty.call(message, "authenticationHashPrime"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.authenticationHashPrime);
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.encryptedLoginToken);
            if (message.authResponse != null && Object.hasOwnProperty.call(message, "authResponse"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.authResponse);
            if (message.mcEnterpriseId != null && Object.hasOwnProperty.call(message, "mcEnterpriseId"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.mcEnterpriseId);
            if (message.pushToken != null && Object.hasOwnProperty.call(message, "pushToken"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.pushToken);
            if (message.platform != null && Object.hasOwnProperty.call(message, "platform"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.platform);
            return writer;
        };

        /**
         * Decodes a LoginRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.LoginRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.LoginRequest} LoginRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LoginRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.LoginRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.authRequest = $root.Authentication.AuthRequest.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        message.loginType = reader.int32();
                        break;
                    }
                case 3: {
                        message.authenticationHashPrime = reader.bytes();
                        break;
                    }
                case 4: {
                        message.encryptedLoginToken = reader.bytes();
                        break;
                    }
                case 5: {
                        message.authResponse = reader.bytes();
                        break;
                    }
                case 6: {
                        message.mcEnterpriseId = reader.int32();
                        break;
                    }
                case 7: {
                        message.pushToken = reader.string();
                        break;
                    }
                case 8: {
                        message.platform = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a LoginRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.LoginRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.LoginRequest} LoginRequest
         */
        LoginRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.LoginRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.LoginRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.LoginRequest();
            if (object.authRequest != null) {
                if (!$util.isObject(object.authRequest))
                    throw TypeError(".Authentication.LoginRequest.authRequest: object expected");
                message.authRequest = $root.Authentication.AuthRequest.fromObject(object.authRequest, long + 1);
            }
            switch (object.loginType) {
            default:
                if (typeof object.loginType === "number") {
                    message.loginType = object.loginType;
                    break;
                }
                break;
            case "NORMAL":
            case 0:
                message.loginType = 0;
                break;
            case "SSO":
            case 1:
                message.loginType = 1;
                break;
            case "BIO":
            case 2:
                message.loginType = 2;
                break;
            case "ALTERNATE":
            case 3:
                message.loginType = 3;
                break;
            case "OFFLINE":
            case 4:
                message.loginType = 4;
                break;
            case "FORGOT_PASSWORD":
            case 5:
                message.loginType = 5;
                break;
            case "PASSKEY_BIO":
            case 6:
                message.loginType = 6;
                break;
            }
            if (object.authenticationHashPrime != null)
                if (typeof object.authenticationHashPrime === "string")
                    $util.base64.decode(object.authenticationHashPrime, message.authenticationHashPrime = $util.newBuffer($util.base64.length(object.authenticationHashPrime)), 0);
                else if (object.authenticationHashPrime.length >= 0)
                    message.authenticationHashPrime = object.authenticationHashPrime;
            if (object.encryptedLoginToken != null)
                if (typeof object.encryptedLoginToken === "string")
                    $util.base64.decode(object.encryptedLoginToken, message.encryptedLoginToken = $util.newBuffer($util.base64.length(object.encryptedLoginToken)), 0);
                else if (object.encryptedLoginToken.length >= 0)
                    message.encryptedLoginToken = object.encryptedLoginToken;
            if (object.authResponse != null)
                if (typeof object.authResponse === "string")
                    $util.base64.decode(object.authResponse, message.authResponse = $util.newBuffer($util.base64.length(object.authResponse)), 0);
                else if (object.authResponse.length >= 0)
                    message.authResponse = object.authResponse;
            if (object.mcEnterpriseId != null)
                message.mcEnterpriseId = object.mcEnterpriseId | 0;
            if (object.pushToken != null)
                message.pushToken = String(object.pushToken);
            if (object.platform != null)
                message.platform = String(object.platform);
            return message;
        };

        /**
         * Creates a plain object from a LoginRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.LoginRequest
         * @static
         * @param {Authentication.LoginRequest} message LoginRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LoginRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.authRequest = null;
                object.loginType = options.enums === String ? "NORMAL" : 0;
                if (options.bytes === String)
                    object.authenticationHashPrime = "";
                else {
                    object.authenticationHashPrime = [];
                    if (options.bytes !== Array)
                        object.authenticationHashPrime = $util.newBuffer(object.authenticationHashPrime);
                }
                if (options.bytes === String)
                    object.encryptedLoginToken = "";
                else {
                    object.encryptedLoginToken = [];
                    if (options.bytes !== Array)
                        object.encryptedLoginToken = $util.newBuffer(object.encryptedLoginToken);
                }
                if (options.bytes === String)
                    object.authResponse = "";
                else {
                    object.authResponse = [];
                    if (options.bytes !== Array)
                        object.authResponse = $util.newBuffer(object.authResponse);
                }
                object.mcEnterpriseId = 0;
                object.pushToken = "";
                object.platform = "";
            }
            if (message.authRequest != null && Object.hasOwnProperty.call(message, "authRequest"))
                object.authRequest = $root.Authentication.AuthRequest.toObject(message.authRequest, options, q + 1);
            if (message.loginType != null && Object.hasOwnProperty.call(message, "loginType"))
                object.loginType = options.enums === String ? $root.Authentication.LoginType[message.loginType] === undefined ? message.loginType : $root.Authentication.LoginType[message.loginType] : message.loginType;
            if (message.authenticationHashPrime != null && Object.hasOwnProperty.call(message, "authenticationHashPrime"))
                object.authenticationHashPrime = options.bytes === String ? $util.base64.encode(message.authenticationHashPrime, 0, message.authenticationHashPrime.length) : options.bytes === Array ? Array.prototype.slice.call(message.authenticationHashPrime) : message.authenticationHashPrime;
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                object.encryptedLoginToken = options.bytes === String ? $util.base64.encode(message.encryptedLoginToken, 0, message.encryptedLoginToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedLoginToken) : message.encryptedLoginToken;
            if (message.authResponse != null && Object.hasOwnProperty.call(message, "authResponse"))
                object.authResponse = options.bytes === String ? $util.base64.encode(message.authResponse, 0, message.authResponse.length) : options.bytes === Array ? Array.prototype.slice.call(message.authResponse) : message.authResponse;
            if (message.mcEnterpriseId != null && Object.hasOwnProperty.call(message, "mcEnterpriseId"))
                object.mcEnterpriseId = message.mcEnterpriseId;
            if (message.pushToken != null && Object.hasOwnProperty.call(message, "pushToken"))
                object.pushToken = message.pushToken;
            if (message.platform != null && Object.hasOwnProperty.call(message, "platform"))
                object.platform = message.platform;
            return object;
        };

        /**
         * Converts this LoginRequest to JSON.
         * @function toJSON
         * @memberof Authentication.LoginRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LoginRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LoginRequest
         * @function getTypeUrl
         * @memberof Authentication.LoginRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LoginRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.LoginRequest";
        };

        return LoginRequest;
    })();

    Authentication.DeviceResponse = (function() {

        /**
         * Properties of a DeviceResponse.
         * @memberof Authentication
         * @interface IDeviceResponse
         * @property {Uint8Array|null} [encryptedDeviceToken] DeviceResponse encryptedDeviceToken
         * @property {Authentication.DeviceStatus|null} [status] DeviceResponse status
         */

        /**
         * Constructs a new DeviceResponse.
         * @memberof Authentication
         * @classdesc Represents a DeviceResponse.
         * @implements IDeviceResponse
         * @constructor
         * @param {Authentication.IDeviceResponse=} [properties] Properties to set
         */
        function DeviceResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeviceResponse encryptedDeviceToken.
         * @member {Uint8Array} encryptedDeviceToken
         * @memberof Authentication.DeviceResponse
         * @instance
         */
        DeviceResponse.prototype.encryptedDeviceToken = $util.newBuffer([]);

        /**
         * DeviceResponse status.
         * @member {Authentication.DeviceStatus} status
         * @memberof Authentication.DeviceResponse
         * @instance
         */
        DeviceResponse.prototype.status = 0;

        /**
         * Creates a new DeviceResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.DeviceResponse
         * @static
         * @param {Authentication.IDeviceResponse=} [properties] Properties to set
         * @returns {Authentication.DeviceResponse} DeviceResponse instance
         */
        DeviceResponse.create = function create(properties) {
            return new DeviceResponse(properties);
        };

        /**
         * Encodes the specified DeviceResponse message. Does not implicitly {@link Authentication.DeviceResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.DeviceResponse
         * @static
         * @param {Authentication.IDeviceResponse} message DeviceResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedDeviceToken);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.status);
            return writer;
        };

        /**
         * Decodes a DeviceResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.DeviceResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.DeviceResponse} DeviceResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.DeviceResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedDeviceToken = reader.bytes();
                        break;
                    }
                case 2: {
                        message.status = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a DeviceResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.DeviceResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.DeviceResponse} DeviceResponse
         */
        DeviceResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.DeviceResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.DeviceResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.DeviceResponse();
            if (object.encryptedDeviceToken != null)
                if (typeof object.encryptedDeviceToken === "string")
                    $util.base64.decode(object.encryptedDeviceToken, message.encryptedDeviceToken = $util.newBuffer($util.base64.length(object.encryptedDeviceToken)), 0);
                else if (object.encryptedDeviceToken.length >= 0)
                    message.encryptedDeviceToken = object.encryptedDeviceToken;
            switch (object.status) {
            default:
                if (typeof object.status === "number") {
                    message.status = object.status;
                    break;
                }
                break;
            case "DEVICE_NEEDS_APPROVAL":
            case 0:
                message.status = 0;
                break;
            case "DEVICE_OK":
            case 1:
                message.status = 1;
                break;
            case "DEVICE_DISABLED_BY_USER":
            case 2:
                message.status = 2;
                break;
            case "DEVICE_LOCKED_BY_ADMIN":
            case 3:
                message.status = 3;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a DeviceResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.DeviceResponse
         * @static
         * @param {Authentication.DeviceResponse} message DeviceResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeviceResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.encryptedDeviceToken = "";
                else {
                    object.encryptedDeviceToken = [];
                    if (options.bytes !== Array)
                        object.encryptedDeviceToken = $util.newBuffer(object.encryptedDeviceToken);
                }
                object.status = options.enums === String ? "DEVICE_NEEDS_APPROVAL" : 0;
            }
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                object.encryptedDeviceToken = options.bytes === String ? $util.base64.encode(message.encryptedDeviceToken, 0, message.encryptedDeviceToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDeviceToken) : message.encryptedDeviceToken;
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                object.status = options.enums === String ? $root.Authentication.DeviceStatus[message.status] === undefined ? message.status : $root.Authentication.DeviceStatus[message.status] : message.status;
            return object;
        };

        /**
         * Converts this DeviceResponse to JSON.
         * @function toJSON
         * @memberof Authentication.DeviceResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeviceResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeviceResponse
         * @function getTypeUrl
         * @memberof Authentication.DeviceResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeviceResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.DeviceResponse";
        };

        return DeviceResponse;
    })();

    Authentication.Salt = (function() {

        /**
         * Properties of a Salt.
         * @memberof Authentication
         * @interface ISalt
         * @property {number|null} [iterations] Salt iterations
         * @property {Uint8Array|null} [salt] Salt salt
         * @property {number|null} [algorithm] Salt algorithm
         * @property {Uint8Array|null} [uid] Salt uid
         * @property {string|null} [name] Salt name
         */

        /**
         * Constructs a new Salt.
         * @memberof Authentication
         * @classdesc Represents a Salt.
         * @implements ISalt
         * @constructor
         * @param {Authentication.ISalt=} [properties] Properties to set
         */
        function Salt(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Salt iterations.
         * @member {number} iterations
         * @memberof Authentication.Salt
         * @instance
         */
        Salt.prototype.iterations = 0;

        /**
         * Salt salt.
         * @member {Uint8Array} salt
         * @memberof Authentication.Salt
         * @instance
         */
        Salt.prototype.salt = $util.newBuffer([]);

        /**
         * Salt algorithm.
         * @member {number} algorithm
         * @memberof Authentication.Salt
         * @instance
         */
        Salt.prototype.algorithm = 0;

        /**
         * Salt uid.
         * @member {Uint8Array} uid
         * @memberof Authentication.Salt
         * @instance
         */
        Salt.prototype.uid = $util.newBuffer([]);

        /**
         * Salt name.
         * @member {string} name
         * @memberof Authentication.Salt
         * @instance
         */
        Salt.prototype.name = "";

        /**
         * Creates a new Salt instance using the specified properties.
         * @function create
         * @memberof Authentication.Salt
         * @static
         * @param {Authentication.ISalt=} [properties] Properties to set
         * @returns {Authentication.Salt} Salt instance
         */
        Salt.create = function create(properties) {
            return new Salt(properties);
        };

        /**
         * Encodes the specified Salt message. Does not implicitly {@link Authentication.Salt.verify|verify} messages.
         * @function encode
         * @memberof Authentication.Salt
         * @static
         * @param {Authentication.ISalt} message Salt message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Salt.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.iterations != null && Object.hasOwnProperty.call(message, "iterations"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.iterations);
            if (message.salt != null && Object.hasOwnProperty.call(message, "salt"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.salt);
            if (message.algorithm != null && Object.hasOwnProperty.call(message, "algorithm"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.algorithm);
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.uid);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.name);
            return writer;
        };

        /**
         * Decodes a Salt message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.Salt
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.Salt} Salt
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Salt.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.Salt();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.iterations = reader.int32();
                        break;
                    }
                case 2: {
                        message.salt = reader.bytes();
                        break;
                    }
                case 3: {
                        message.algorithm = reader.int32();
                        break;
                    }
                case 4: {
                        message.uid = reader.bytes();
                        break;
                    }
                case 5: {
                        message.name = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a Salt message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.Salt
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.Salt} Salt
         */
        Salt.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.Salt)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.Salt: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.Salt();
            if (object.iterations != null)
                message.iterations = object.iterations | 0;
            if (object.salt != null)
                if (typeof object.salt === "string")
                    $util.base64.decode(object.salt, message.salt = $util.newBuffer($util.base64.length(object.salt)), 0);
                else if (object.salt.length >= 0)
                    message.salt = object.salt;
            if (object.algorithm != null)
                message.algorithm = object.algorithm | 0;
            if (object.uid != null)
                if (typeof object.uid === "string")
                    $util.base64.decode(object.uid, message.uid = $util.newBuffer($util.base64.length(object.uid)), 0);
                else if (object.uid.length >= 0)
                    message.uid = object.uid;
            if (object.name != null)
                message.name = String(object.name);
            return message;
        };

        /**
         * Creates a plain object from a Salt message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.Salt
         * @static
         * @param {Authentication.Salt} message Salt
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Salt.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.iterations = 0;
                if (options.bytes === String)
                    object.salt = "";
                else {
                    object.salt = [];
                    if (options.bytes !== Array)
                        object.salt = $util.newBuffer(object.salt);
                }
                object.algorithm = 0;
                if (options.bytes === String)
                    object.uid = "";
                else {
                    object.uid = [];
                    if (options.bytes !== Array)
                        object.uid = $util.newBuffer(object.uid);
                }
                object.name = "";
            }
            if (message.iterations != null && Object.hasOwnProperty.call(message, "iterations"))
                object.iterations = message.iterations;
            if (message.salt != null && Object.hasOwnProperty.call(message, "salt"))
                object.salt = options.bytes === String ? $util.base64.encode(message.salt, 0, message.salt.length) : options.bytes === Array ? Array.prototype.slice.call(message.salt) : message.salt;
            if (message.algorithm != null && Object.hasOwnProperty.call(message, "algorithm"))
                object.algorithm = message.algorithm;
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                object.uid = options.bytes === String ? $util.base64.encode(message.uid, 0, message.uid.length) : options.bytes === Array ? Array.prototype.slice.call(message.uid) : message.uid;
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            return object;
        };

        /**
         * Converts this Salt to JSON.
         * @function toJSON
         * @memberof Authentication.Salt
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Salt.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Salt
         * @function getTypeUrl
         * @memberof Authentication.Salt
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Salt.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.Salt";
        };

        return Salt;
    })();

    Authentication.TwoFactorChannel = (function() {

        /**
         * Properties of a TwoFactorChannel.
         * @memberof Authentication
         * @interface ITwoFactorChannel
         * @property {number|null} [type] TwoFactorChannel type
         */

        /**
         * Constructs a new TwoFactorChannel.
         * @memberof Authentication
         * @classdesc Represents a TwoFactorChannel.
         * @implements ITwoFactorChannel
         * @constructor
         * @param {Authentication.ITwoFactorChannel=} [properties] Properties to set
         */
        function TwoFactorChannel(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TwoFactorChannel type.
         * @member {number} type
         * @memberof Authentication.TwoFactorChannel
         * @instance
         */
        TwoFactorChannel.prototype.type = 0;

        /**
         * Creates a new TwoFactorChannel instance using the specified properties.
         * @function create
         * @memberof Authentication.TwoFactorChannel
         * @static
         * @param {Authentication.ITwoFactorChannel=} [properties] Properties to set
         * @returns {Authentication.TwoFactorChannel} TwoFactorChannel instance
         */
        TwoFactorChannel.create = function create(properties) {
            return new TwoFactorChannel(properties);
        };

        /**
         * Encodes the specified TwoFactorChannel message. Does not implicitly {@link Authentication.TwoFactorChannel.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TwoFactorChannel
         * @static
         * @param {Authentication.ITwoFactorChannel} message TwoFactorChannel message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TwoFactorChannel.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
            return writer;
        };

        /**
         * Decodes a TwoFactorChannel message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TwoFactorChannel
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TwoFactorChannel} TwoFactorChannel
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TwoFactorChannel.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TwoFactorChannel();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.type = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TwoFactorChannel message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TwoFactorChannel
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TwoFactorChannel} TwoFactorChannel
         */
        TwoFactorChannel.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TwoFactorChannel)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TwoFactorChannel: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TwoFactorChannel();
            if (object.type != null)
                message.type = object.type | 0;
            return message;
        };

        /**
         * Creates a plain object from a TwoFactorChannel message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TwoFactorChannel
         * @static
         * @param {Authentication.TwoFactorChannel} message TwoFactorChannel
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TwoFactorChannel.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.type = 0;
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                object.type = message.type;
            return object;
        };

        /**
         * Converts this TwoFactorChannel to JSON.
         * @function toJSON
         * @memberof Authentication.TwoFactorChannel
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TwoFactorChannel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TwoFactorChannel
         * @function getTypeUrl
         * @memberof Authentication.TwoFactorChannel
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TwoFactorChannel.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TwoFactorChannel";
        };

        return TwoFactorChannel;
    })();

    /**
     * LoginMethod enum.
     * @name Authentication.LoginMethod
     * @enum {number}
     * @property {number} INVALID_LOGINMETHOD=0 INVALID_LOGINMETHOD value
     * @property {number} EXISTING_ACCOUNT=1 EXISTING_ACCOUNT value
     * @property {number} SSO_DOMAIN=2 SSO_DOMAIN value
     * @property {number} AFTER_SSO=3 AFTER_SSO value
     * @property {number} NEW_ACCOUNT=4 NEW_ACCOUNT value
     */
    Authentication.LoginMethod = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "INVALID_LOGINMETHOD"] = 0;
        values[valuesById[1] = "EXISTING_ACCOUNT"] = 1;
        values[valuesById[2] = "SSO_DOMAIN"] = 2;
        values[valuesById[3] = "AFTER_SSO"] = 3;
        values[valuesById[4] = "NEW_ACCOUNT"] = 4;
        return values;
    })();

    /**
     * LoginState enum.
     * @name Authentication.LoginState
     * @enum {number}
     * @property {number} INVALID_LOGINSTATE=0 INVALID_LOGINSTATE value
     * @property {number} LOGGED_OUT=1 LOGGED_OUT value
     * @property {number} DEVICE_APPROVAL_REQUIRED=2 DEVICE_APPROVAL_REQUIRED value
     * @property {number} DEVICE_LOCKED=3 DEVICE_LOCKED value
     * @property {number} ACCOUNT_LOCKED=4 ACCOUNT_LOCKED value
     * @property {number} DEVICE_ACCOUNT_LOCKED=5 DEVICE_ACCOUNT_LOCKED value
     * @property {number} UPGRADE=6 UPGRADE value
     * @property {number} LICENSE_EXPIRED=7 LICENSE_EXPIRED value
     * @property {number} REGION_REDIRECT=8 REGION_REDIRECT value
     * @property {number} REDIRECT_CLOUD_SSO=9 REDIRECT_CLOUD_SSO value
     * @property {number} REDIRECT_ONSITE_SSO=10 REDIRECT_ONSITE_SSO value
     * @property {number} REQUIRES_2FA=12 REQUIRES_2FA value
     * @property {number} REQUIRES_AUTH_HASH=13 REQUIRES_AUTH_HASH value
     * @property {number} REQUIRES_USERNAME=14 REQUIRES_USERNAME value
     * @property {number} AFTER_CLOUD_SSO_LOGIN=15 AFTER_CLOUD_SSO_LOGIN value
     * @property {number} REQUIRES_ACCOUNT_CREATION=16 REQUIRES_ACCOUNT_CREATION value
     * @property {number} REQUIRES_DEVICE_ENCRYPTED_DATA_KEY=17 REQUIRES_DEVICE_ENCRYPTED_DATA_KEY value
     * @property {number} LOGIN_TOKEN_EXPIRED=18 LOGIN_TOKEN_EXPIRED value
     * @property {number} PASSKEY_INITIATE_CHALLENGE=19 PASSKEY_INITIATE_CHALLENGE value
     * @property {number} PASSKEY_AUTH_REQUIRED=20 PASSKEY_AUTH_REQUIRED value
     * @property {number} PASSKEY_VERIFY_AUTHENTICATION=21 PASSKEY_VERIFY_AUTHENTICATION value
     * @property {number} AFTER_PASSKEY_LOGIN=22 AFTER_PASSKEY_LOGIN value
     * @property {number} LOGGED_IN=99 LOGGED_IN value
     */
    Authentication.LoginState = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "INVALID_LOGINSTATE"] = 0;
        values[valuesById[1] = "LOGGED_OUT"] = 1;
        values[valuesById[2] = "DEVICE_APPROVAL_REQUIRED"] = 2;
        values[valuesById[3] = "DEVICE_LOCKED"] = 3;
        values[valuesById[4] = "ACCOUNT_LOCKED"] = 4;
        values[valuesById[5] = "DEVICE_ACCOUNT_LOCKED"] = 5;
        values[valuesById[6] = "UPGRADE"] = 6;
        values[valuesById[7] = "LICENSE_EXPIRED"] = 7;
        values[valuesById[8] = "REGION_REDIRECT"] = 8;
        values[valuesById[9] = "REDIRECT_CLOUD_SSO"] = 9;
        values[valuesById[10] = "REDIRECT_ONSITE_SSO"] = 10;
        values[valuesById[12] = "REQUIRES_2FA"] = 12;
        values[valuesById[13] = "REQUIRES_AUTH_HASH"] = 13;
        values[valuesById[14] = "REQUIRES_USERNAME"] = 14;
        values[valuesById[15] = "AFTER_CLOUD_SSO_LOGIN"] = 15;
        values[valuesById[16] = "REQUIRES_ACCOUNT_CREATION"] = 16;
        values[valuesById[17] = "REQUIRES_DEVICE_ENCRYPTED_DATA_KEY"] = 17;
        values[valuesById[18] = "LOGIN_TOKEN_EXPIRED"] = 18;
        values[valuesById[19] = "PASSKEY_INITIATE_CHALLENGE"] = 19;
        values[valuesById[20] = "PASSKEY_AUTH_REQUIRED"] = 20;
        values[valuesById[21] = "PASSKEY_VERIFY_AUTHENTICATION"] = 21;
        values[valuesById[22] = "AFTER_PASSKEY_LOGIN"] = 22;
        values[valuesById[99] = "LOGGED_IN"] = 99;
        return values;
    })();

    /**
     * EncryptedDataKeyType enum.
     * @name Authentication.EncryptedDataKeyType
     * @enum {number}
     * @property {number} NO_KEY=0 NO_KEY value
     * @property {number} BY_DEVICE_PUBLIC_KEY=1 BY_DEVICE_PUBLIC_KEY value
     * @property {number} BY_PASSWORD=2 BY_PASSWORD value
     * @property {number} BY_ALTERNATE=3 BY_ALTERNATE value
     * @property {number} BY_BIO=4 BY_BIO value
     */
    Authentication.EncryptedDataKeyType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "NO_KEY"] = 0;
        values[valuesById[1] = "BY_DEVICE_PUBLIC_KEY"] = 1;
        values[valuesById[2] = "BY_PASSWORD"] = 2;
        values[valuesById[3] = "BY_ALTERNATE"] = 3;
        values[valuesById[4] = "BY_BIO"] = 4;
        return values;
    })();

    Authentication.StartLoginRequest = (function() {

        /**
         * Properties of a StartLoginRequest.
         * @memberof Authentication
         * @interface IStartLoginRequest
         * @property {Uint8Array|null} [encryptedDeviceToken] StartLoginRequest encryptedDeviceToken
         * @property {string|null} [username] StartLoginRequest username
         * @property {string|null} [clientVersion] StartLoginRequest clientVersion
         * @property {Uint8Array|null} [messageSessionUid] StartLoginRequest messageSessionUid
         * @property {Uint8Array|null} [encryptedLoginToken] StartLoginRequest encryptedLoginToken
         * @property {Authentication.LoginType|null} [loginType] StartLoginRequest loginType
         * @property {number|null} [mcEnterpriseId] StartLoginRequest mcEnterpriseId
         * @property {Authentication.LoginMethod|null} [loginMethod] StartLoginRequest loginMethod
         * @property {boolean|null} [forceNewLogin] StartLoginRequest forceNewLogin
         * @property {Uint8Array|null} [cloneCode] StartLoginRequest cloneCode
         * @property {string|null} [v2TwoFactorToken] StartLoginRequest v2TwoFactorToken
         * @property {Uint8Array|null} [accountUid] StartLoginRequest accountUid
         * @property {Uint8Array|null} [fromSessionToken] StartLoginRequest fromSessionToken
         */

        /**
         * Constructs a new StartLoginRequest.
         * @memberof Authentication
         * @classdesc Represents a StartLoginRequest.
         * @implements IStartLoginRequest
         * @constructor
         * @param {Authentication.IStartLoginRequest=} [properties] Properties to set
         */
        function StartLoginRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * StartLoginRequest encryptedDeviceToken.
         * @member {Uint8Array} encryptedDeviceToken
         * @memberof Authentication.StartLoginRequest
         * @instance
         */
        StartLoginRequest.prototype.encryptedDeviceToken = $util.newBuffer([]);

        /**
         * StartLoginRequest username.
         * @member {string} username
         * @memberof Authentication.StartLoginRequest
         * @instance
         */
        StartLoginRequest.prototype.username = "";

        /**
         * StartLoginRequest clientVersion.
         * @member {string} clientVersion
         * @memberof Authentication.StartLoginRequest
         * @instance
         */
        StartLoginRequest.prototype.clientVersion = "";

        /**
         * StartLoginRequest messageSessionUid.
         * @member {Uint8Array} messageSessionUid
         * @memberof Authentication.StartLoginRequest
         * @instance
         */
        StartLoginRequest.prototype.messageSessionUid = $util.newBuffer([]);

        /**
         * StartLoginRequest encryptedLoginToken.
         * @member {Uint8Array} encryptedLoginToken
         * @memberof Authentication.StartLoginRequest
         * @instance
         */
        StartLoginRequest.prototype.encryptedLoginToken = $util.newBuffer([]);

        /**
         * StartLoginRequest loginType.
         * @member {Authentication.LoginType} loginType
         * @memberof Authentication.StartLoginRequest
         * @instance
         */
        StartLoginRequest.prototype.loginType = 0;

        /**
         * StartLoginRequest mcEnterpriseId.
         * @member {number} mcEnterpriseId
         * @memberof Authentication.StartLoginRequest
         * @instance
         */
        StartLoginRequest.prototype.mcEnterpriseId = 0;

        /**
         * StartLoginRequest loginMethod.
         * @member {Authentication.LoginMethod} loginMethod
         * @memberof Authentication.StartLoginRequest
         * @instance
         */
        StartLoginRequest.prototype.loginMethod = 0;

        /**
         * StartLoginRequest forceNewLogin.
         * @member {boolean} forceNewLogin
         * @memberof Authentication.StartLoginRequest
         * @instance
         */
        StartLoginRequest.prototype.forceNewLogin = false;

        /**
         * StartLoginRequest cloneCode.
         * @member {Uint8Array} cloneCode
         * @memberof Authentication.StartLoginRequest
         * @instance
         */
        StartLoginRequest.prototype.cloneCode = $util.newBuffer([]);

        /**
         * StartLoginRequest v2TwoFactorToken.
         * @member {string} v2TwoFactorToken
         * @memberof Authentication.StartLoginRequest
         * @instance
         */
        StartLoginRequest.prototype.v2TwoFactorToken = "";

        /**
         * StartLoginRequest accountUid.
         * @member {Uint8Array} accountUid
         * @memberof Authentication.StartLoginRequest
         * @instance
         */
        StartLoginRequest.prototype.accountUid = $util.newBuffer([]);

        /**
         * StartLoginRequest fromSessionToken.
         * @member {Uint8Array} fromSessionToken
         * @memberof Authentication.StartLoginRequest
         * @instance
         */
        StartLoginRequest.prototype.fromSessionToken = $util.newBuffer([]);

        /**
         * Creates a new StartLoginRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.StartLoginRequest
         * @static
         * @param {Authentication.IStartLoginRequest=} [properties] Properties to set
         * @returns {Authentication.StartLoginRequest} StartLoginRequest instance
         */
        StartLoginRequest.create = function create(properties) {
            return new StartLoginRequest(properties);
        };

        /**
         * Encodes the specified StartLoginRequest message. Does not implicitly {@link Authentication.StartLoginRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.StartLoginRequest
         * @static
         * @param {Authentication.IStartLoginRequest} message StartLoginRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        StartLoginRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedDeviceToken);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.username);
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.clientVersion);
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.messageSessionUid);
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.encryptedLoginToken);
            if (message.loginType != null && Object.hasOwnProperty.call(message, "loginType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.loginType);
            if (message.mcEnterpriseId != null && Object.hasOwnProperty.call(message, "mcEnterpriseId"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.mcEnterpriseId);
            if (message.loginMethod != null && Object.hasOwnProperty.call(message, "loginMethod"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.loginMethod);
            if (message.forceNewLogin != null && Object.hasOwnProperty.call(message, "forceNewLogin"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.forceNewLogin);
            if (message.cloneCode != null && Object.hasOwnProperty.call(message, "cloneCode"))
                writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.cloneCode);
            if (message.v2TwoFactorToken != null && Object.hasOwnProperty.call(message, "v2TwoFactorToken"))
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.v2TwoFactorToken);
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                writer.uint32(/* id 12, wireType 2 =*/98).bytes(message.accountUid);
            if (message.fromSessionToken != null && Object.hasOwnProperty.call(message, "fromSessionToken"))
                writer.uint32(/* id 13, wireType 2 =*/106).bytes(message.fromSessionToken);
            return writer;
        };

        /**
         * Decodes a StartLoginRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.StartLoginRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.StartLoginRequest} StartLoginRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        StartLoginRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.StartLoginRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedDeviceToken = reader.bytes();
                        break;
                    }
                case 2: {
                        message.username = reader.string();
                        break;
                    }
                case 3: {
                        message.clientVersion = reader.string();
                        break;
                    }
                case 4: {
                        message.messageSessionUid = reader.bytes();
                        break;
                    }
                case 5: {
                        message.encryptedLoginToken = reader.bytes();
                        break;
                    }
                case 6: {
                        message.loginType = reader.int32();
                        break;
                    }
                case 7: {
                        message.mcEnterpriseId = reader.int32();
                        break;
                    }
                case 8: {
                        message.loginMethod = reader.int32();
                        break;
                    }
                case 9: {
                        message.forceNewLogin = reader.bool();
                        break;
                    }
                case 10: {
                        message.cloneCode = reader.bytes();
                        break;
                    }
                case 11: {
                        message.v2TwoFactorToken = reader.string();
                        break;
                    }
                case 12: {
                        message.accountUid = reader.bytes();
                        break;
                    }
                case 13: {
                        message.fromSessionToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a StartLoginRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.StartLoginRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.StartLoginRequest} StartLoginRequest
         */
        StartLoginRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.StartLoginRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.StartLoginRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.StartLoginRequest();
            if (object.encryptedDeviceToken != null)
                if (typeof object.encryptedDeviceToken === "string")
                    $util.base64.decode(object.encryptedDeviceToken, message.encryptedDeviceToken = $util.newBuffer($util.base64.length(object.encryptedDeviceToken)), 0);
                else if (object.encryptedDeviceToken.length >= 0)
                    message.encryptedDeviceToken = object.encryptedDeviceToken;
            if (object.username != null)
                message.username = String(object.username);
            if (object.clientVersion != null)
                message.clientVersion = String(object.clientVersion);
            if (object.messageSessionUid != null)
                if (typeof object.messageSessionUid === "string")
                    $util.base64.decode(object.messageSessionUid, message.messageSessionUid = $util.newBuffer($util.base64.length(object.messageSessionUid)), 0);
                else if (object.messageSessionUid.length >= 0)
                    message.messageSessionUid = object.messageSessionUid;
            if (object.encryptedLoginToken != null)
                if (typeof object.encryptedLoginToken === "string")
                    $util.base64.decode(object.encryptedLoginToken, message.encryptedLoginToken = $util.newBuffer($util.base64.length(object.encryptedLoginToken)), 0);
                else if (object.encryptedLoginToken.length >= 0)
                    message.encryptedLoginToken = object.encryptedLoginToken;
            switch (object.loginType) {
            default:
                if (typeof object.loginType === "number") {
                    message.loginType = object.loginType;
                    break;
                }
                break;
            case "NORMAL":
            case 0:
                message.loginType = 0;
                break;
            case "SSO":
            case 1:
                message.loginType = 1;
                break;
            case "BIO":
            case 2:
                message.loginType = 2;
                break;
            case "ALTERNATE":
            case 3:
                message.loginType = 3;
                break;
            case "OFFLINE":
            case 4:
                message.loginType = 4;
                break;
            case "FORGOT_PASSWORD":
            case 5:
                message.loginType = 5;
                break;
            case "PASSKEY_BIO":
            case 6:
                message.loginType = 6;
                break;
            }
            if (object.mcEnterpriseId != null)
                message.mcEnterpriseId = object.mcEnterpriseId | 0;
            switch (object.loginMethod) {
            default:
                if (typeof object.loginMethod === "number") {
                    message.loginMethod = object.loginMethod;
                    break;
                }
                break;
            case "INVALID_LOGINMETHOD":
            case 0:
                message.loginMethod = 0;
                break;
            case "EXISTING_ACCOUNT":
            case 1:
                message.loginMethod = 1;
                break;
            case "SSO_DOMAIN":
            case 2:
                message.loginMethod = 2;
                break;
            case "AFTER_SSO":
            case 3:
                message.loginMethod = 3;
                break;
            case "NEW_ACCOUNT":
            case 4:
                message.loginMethod = 4;
                break;
            }
            if (object.forceNewLogin != null)
                message.forceNewLogin = Boolean(object.forceNewLogin);
            if (object.cloneCode != null)
                if (typeof object.cloneCode === "string")
                    $util.base64.decode(object.cloneCode, message.cloneCode = $util.newBuffer($util.base64.length(object.cloneCode)), 0);
                else if (object.cloneCode.length >= 0)
                    message.cloneCode = object.cloneCode;
            if (object.v2TwoFactorToken != null)
                message.v2TwoFactorToken = String(object.v2TwoFactorToken);
            if (object.accountUid != null)
                if (typeof object.accountUid === "string")
                    $util.base64.decode(object.accountUid, message.accountUid = $util.newBuffer($util.base64.length(object.accountUid)), 0);
                else if (object.accountUid.length >= 0)
                    message.accountUid = object.accountUid;
            if (object.fromSessionToken != null)
                if (typeof object.fromSessionToken === "string")
                    $util.base64.decode(object.fromSessionToken, message.fromSessionToken = $util.newBuffer($util.base64.length(object.fromSessionToken)), 0);
                else if (object.fromSessionToken.length >= 0)
                    message.fromSessionToken = object.fromSessionToken;
            return message;
        };

        /**
         * Creates a plain object from a StartLoginRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.StartLoginRequest
         * @static
         * @param {Authentication.StartLoginRequest} message StartLoginRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        StartLoginRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.encryptedDeviceToken = "";
                else {
                    object.encryptedDeviceToken = [];
                    if (options.bytes !== Array)
                        object.encryptedDeviceToken = $util.newBuffer(object.encryptedDeviceToken);
                }
                object.username = "";
                object.clientVersion = "";
                if (options.bytes === String)
                    object.messageSessionUid = "";
                else {
                    object.messageSessionUid = [];
                    if (options.bytes !== Array)
                        object.messageSessionUid = $util.newBuffer(object.messageSessionUid);
                }
                if (options.bytes === String)
                    object.encryptedLoginToken = "";
                else {
                    object.encryptedLoginToken = [];
                    if (options.bytes !== Array)
                        object.encryptedLoginToken = $util.newBuffer(object.encryptedLoginToken);
                }
                object.loginType = options.enums === String ? "NORMAL" : 0;
                object.mcEnterpriseId = 0;
                object.loginMethod = options.enums === String ? "INVALID_LOGINMETHOD" : 0;
                object.forceNewLogin = false;
                if (options.bytes === String)
                    object.cloneCode = "";
                else {
                    object.cloneCode = [];
                    if (options.bytes !== Array)
                        object.cloneCode = $util.newBuffer(object.cloneCode);
                }
                object.v2TwoFactorToken = "";
                if (options.bytes === String)
                    object.accountUid = "";
                else {
                    object.accountUid = [];
                    if (options.bytes !== Array)
                        object.accountUid = $util.newBuffer(object.accountUid);
                }
                if (options.bytes === String)
                    object.fromSessionToken = "";
                else {
                    object.fromSessionToken = [];
                    if (options.bytes !== Array)
                        object.fromSessionToken = $util.newBuffer(object.fromSessionToken);
                }
            }
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                object.encryptedDeviceToken = options.bytes === String ? $util.base64.encode(message.encryptedDeviceToken, 0, message.encryptedDeviceToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDeviceToken) : message.encryptedDeviceToken;
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                object.clientVersion = message.clientVersion;
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                object.messageSessionUid = options.bytes === String ? $util.base64.encode(message.messageSessionUid, 0, message.messageSessionUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.messageSessionUid) : message.messageSessionUid;
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                object.encryptedLoginToken = options.bytes === String ? $util.base64.encode(message.encryptedLoginToken, 0, message.encryptedLoginToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedLoginToken) : message.encryptedLoginToken;
            if (message.loginType != null && Object.hasOwnProperty.call(message, "loginType"))
                object.loginType = options.enums === String ? $root.Authentication.LoginType[message.loginType] === undefined ? message.loginType : $root.Authentication.LoginType[message.loginType] : message.loginType;
            if (message.mcEnterpriseId != null && Object.hasOwnProperty.call(message, "mcEnterpriseId"))
                object.mcEnterpriseId = message.mcEnterpriseId;
            if (message.loginMethod != null && Object.hasOwnProperty.call(message, "loginMethod"))
                object.loginMethod = options.enums === String ? $root.Authentication.LoginMethod[message.loginMethod] === undefined ? message.loginMethod : $root.Authentication.LoginMethod[message.loginMethod] : message.loginMethod;
            if (message.forceNewLogin != null && Object.hasOwnProperty.call(message, "forceNewLogin"))
                object.forceNewLogin = message.forceNewLogin;
            if (message.cloneCode != null && Object.hasOwnProperty.call(message, "cloneCode"))
                object.cloneCode = options.bytes === String ? $util.base64.encode(message.cloneCode, 0, message.cloneCode.length) : options.bytes === Array ? Array.prototype.slice.call(message.cloneCode) : message.cloneCode;
            if (message.v2TwoFactorToken != null && Object.hasOwnProperty.call(message, "v2TwoFactorToken"))
                object.v2TwoFactorToken = message.v2TwoFactorToken;
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                object.accountUid = options.bytes === String ? $util.base64.encode(message.accountUid, 0, message.accountUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.accountUid) : message.accountUid;
            if (message.fromSessionToken != null && Object.hasOwnProperty.call(message, "fromSessionToken"))
                object.fromSessionToken = options.bytes === String ? $util.base64.encode(message.fromSessionToken, 0, message.fromSessionToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.fromSessionToken) : message.fromSessionToken;
            return object;
        };

        /**
         * Converts this StartLoginRequest to JSON.
         * @function toJSON
         * @memberof Authentication.StartLoginRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        StartLoginRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for StartLoginRequest
         * @function getTypeUrl
         * @memberof Authentication.StartLoginRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        StartLoginRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.StartLoginRequest";
        };

        return StartLoginRequest;
    })();

    Authentication.KeysInfo = (function() {

        /**
         * Properties of a KeysInfo.
         * @memberof Authentication
         * @interface IKeysInfo
         * @property {Uint8Array|null} [encryptionParams] KeysInfo encryptionParams
         * @property {Uint8Array|null} [encryptedDataKey] KeysInfo encryptedDataKey
         * @property {number|null} [dataKeyBackupDate] KeysInfo dataKeyBackupDate
         * @property {Uint8Array|null} [userAuthUid] KeysInfo userAuthUid
         * @property {Uint8Array|null} [encryptedPrivateKey] KeysInfo encryptedPrivateKey
         * @property {Uint8Array|null} [encryptedEccPrivateKey] KeysInfo encryptedEccPrivateKey
         * @property {Uint8Array|null} [eccPublicKey] KeysInfo eccPublicKey
         */

        /**
         * Constructs a new KeysInfo.
         * @memberof Authentication
         * @classdesc Represents a KeysInfo.
         * @implements IKeysInfo
         * @constructor
         * @param {Authentication.IKeysInfo=} [properties] Properties to set
         */
        function KeysInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * KeysInfo encryptionParams.
         * @member {Uint8Array} encryptionParams
         * @memberof Authentication.KeysInfo
         * @instance
         */
        KeysInfo.prototype.encryptionParams = $util.newBuffer([]);

        /**
         * KeysInfo encryptedDataKey.
         * @member {Uint8Array} encryptedDataKey
         * @memberof Authentication.KeysInfo
         * @instance
         */
        KeysInfo.prototype.encryptedDataKey = $util.newBuffer([]);

        /**
         * KeysInfo dataKeyBackupDate.
         * @member {number} dataKeyBackupDate
         * @memberof Authentication.KeysInfo
         * @instance
         */
        KeysInfo.prototype.dataKeyBackupDate = 0;

        /**
         * KeysInfo userAuthUid.
         * @member {Uint8Array} userAuthUid
         * @memberof Authentication.KeysInfo
         * @instance
         */
        KeysInfo.prototype.userAuthUid = $util.newBuffer([]);

        /**
         * KeysInfo encryptedPrivateKey.
         * @member {Uint8Array} encryptedPrivateKey
         * @memberof Authentication.KeysInfo
         * @instance
         */
        KeysInfo.prototype.encryptedPrivateKey = $util.newBuffer([]);

        /**
         * KeysInfo encryptedEccPrivateKey.
         * @member {Uint8Array} encryptedEccPrivateKey
         * @memberof Authentication.KeysInfo
         * @instance
         */
        KeysInfo.prototype.encryptedEccPrivateKey = $util.newBuffer([]);

        /**
         * KeysInfo eccPublicKey.
         * @member {Uint8Array} eccPublicKey
         * @memberof Authentication.KeysInfo
         * @instance
         */
        KeysInfo.prototype.eccPublicKey = $util.newBuffer([]);

        /**
         * Creates a new KeysInfo instance using the specified properties.
         * @function create
         * @memberof Authentication.KeysInfo
         * @static
         * @param {Authentication.IKeysInfo=} [properties] Properties to set
         * @returns {Authentication.KeysInfo} KeysInfo instance
         */
        KeysInfo.create = function create(properties) {
            return new KeysInfo(properties);
        };

        /**
         * Encodes the specified KeysInfo message. Does not implicitly {@link Authentication.KeysInfo.verify|verify} messages.
         * @function encode
         * @memberof Authentication.KeysInfo
         * @static
         * @param {Authentication.IKeysInfo} message KeysInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeysInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptionParams != null && Object.hasOwnProperty.call(message, "encryptionParams"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptionParams);
            if (message.encryptedDataKey != null && Object.hasOwnProperty.call(message, "encryptedDataKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encryptedDataKey);
            if (message.dataKeyBackupDate != null && Object.hasOwnProperty.call(message, "dataKeyBackupDate"))
                writer.uint32(/* id 3, wireType 1 =*/25).double(message.dataKeyBackupDate);
            if (message.userAuthUid != null && Object.hasOwnProperty.call(message, "userAuthUid"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.userAuthUid);
            if (message.encryptedPrivateKey != null && Object.hasOwnProperty.call(message, "encryptedPrivateKey"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.encryptedPrivateKey);
            if (message.encryptedEccPrivateKey != null && Object.hasOwnProperty.call(message, "encryptedEccPrivateKey"))
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.encryptedEccPrivateKey);
            if (message.eccPublicKey != null && Object.hasOwnProperty.call(message, "eccPublicKey"))
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.eccPublicKey);
            return writer;
        };

        /**
         * Decodes a KeysInfo message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.KeysInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.KeysInfo} KeysInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeysInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.KeysInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptionParams = reader.bytes();
                        break;
                    }
                case 2: {
                        message.encryptedDataKey = reader.bytes();
                        break;
                    }
                case 3: {
                        message.dataKeyBackupDate = reader.double();
                        break;
                    }
                case 4: {
                        message.userAuthUid = reader.bytes();
                        break;
                    }
                case 5: {
                        message.encryptedPrivateKey = reader.bytes();
                        break;
                    }
                case 6: {
                        message.encryptedEccPrivateKey = reader.bytes();
                        break;
                    }
                case 7: {
                        message.eccPublicKey = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a KeysInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.KeysInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.KeysInfo} KeysInfo
         */
        KeysInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.KeysInfo)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.KeysInfo: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.KeysInfo();
            if (object.encryptionParams != null)
                if (typeof object.encryptionParams === "string")
                    $util.base64.decode(object.encryptionParams, message.encryptionParams = $util.newBuffer($util.base64.length(object.encryptionParams)), 0);
                else if (object.encryptionParams.length >= 0)
                    message.encryptionParams = object.encryptionParams;
            if (object.encryptedDataKey != null)
                if (typeof object.encryptedDataKey === "string")
                    $util.base64.decode(object.encryptedDataKey, message.encryptedDataKey = $util.newBuffer($util.base64.length(object.encryptedDataKey)), 0);
                else if (object.encryptedDataKey.length >= 0)
                    message.encryptedDataKey = object.encryptedDataKey;
            if (object.dataKeyBackupDate != null)
                message.dataKeyBackupDate = Number(object.dataKeyBackupDate);
            if (object.userAuthUid != null)
                if (typeof object.userAuthUid === "string")
                    $util.base64.decode(object.userAuthUid, message.userAuthUid = $util.newBuffer($util.base64.length(object.userAuthUid)), 0);
                else if (object.userAuthUid.length >= 0)
                    message.userAuthUid = object.userAuthUid;
            if (object.encryptedPrivateKey != null)
                if (typeof object.encryptedPrivateKey === "string")
                    $util.base64.decode(object.encryptedPrivateKey, message.encryptedPrivateKey = $util.newBuffer($util.base64.length(object.encryptedPrivateKey)), 0);
                else if (object.encryptedPrivateKey.length >= 0)
                    message.encryptedPrivateKey = object.encryptedPrivateKey;
            if (object.encryptedEccPrivateKey != null)
                if (typeof object.encryptedEccPrivateKey === "string")
                    $util.base64.decode(object.encryptedEccPrivateKey, message.encryptedEccPrivateKey = $util.newBuffer($util.base64.length(object.encryptedEccPrivateKey)), 0);
                else if (object.encryptedEccPrivateKey.length >= 0)
                    message.encryptedEccPrivateKey = object.encryptedEccPrivateKey;
            if (object.eccPublicKey != null)
                if (typeof object.eccPublicKey === "string")
                    $util.base64.decode(object.eccPublicKey, message.eccPublicKey = $util.newBuffer($util.base64.length(object.eccPublicKey)), 0);
                else if (object.eccPublicKey.length >= 0)
                    message.eccPublicKey = object.eccPublicKey;
            return message;
        };

        /**
         * Creates a plain object from a KeysInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.KeysInfo
         * @static
         * @param {Authentication.KeysInfo} message KeysInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        KeysInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.encryptionParams = "";
                else {
                    object.encryptionParams = [];
                    if (options.bytes !== Array)
                        object.encryptionParams = $util.newBuffer(object.encryptionParams);
                }
                if (options.bytes === String)
                    object.encryptedDataKey = "";
                else {
                    object.encryptedDataKey = [];
                    if (options.bytes !== Array)
                        object.encryptedDataKey = $util.newBuffer(object.encryptedDataKey);
                }
                object.dataKeyBackupDate = 0;
                if (options.bytes === String)
                    object.userAuthUid = "";
                else {
                    object.userAuthUid = [];
                    if (options.bytes !== Array)
                        object.userAuthUid = $util.newBuffer(object.userAuthUid);
                }
                if (options.bytes === String)
                    object.encryptedPrivateKey = "";
                else {
                    object.encryptedPrivateKey = [];
                    if (options.bytes !== Array)
                        object.encryptedPrivateKey = $util.newBuffer(object.encryptedPrivateKey);
                }
                if (options.bytes === String)
                    object.encryptedEccPrivateKey = "";
                else {
                    object.encryptedEccPrivateKey = [];
                    if (options.bytes !== Array)
                        object.encryptedEccPrivateKey = $util.newBuffer(object.encryptedEccPrivateKey);
                }
                if (options.bytes === String)
                    object.eccPublicKey = "";
                else {
                    object.eccPublicKey = [];
                    if (options.bytes !== Array)
                        object.eccPublicKey = $util.newBuffer(object.eccPublicKey);
                }
            }
            if (message.encryptionParams != null && Object.hasOwnProperty.call(message, "encryptionParams"))
                object.encryptionParams = options.bytes === String ? $util.base64.encode(message.encryptionParams, 0, message.encryptionParams.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptionParams) : message.encryptionParams;
            if (message.encryptedDataKey != null && Object.hasOwnProperty.call(message, "encryptedDataKey"))
                object.encryptedDataKey = options.bytes === String ? $util.base64.encode(message.encryptedDataKey, 0, message.encryptedDataKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDataKey) : message.encryptedDataKey;
            if (message.dataKeyBackupDate != null && Object.hasOwnProperty.call(message, "dataKeyBackupDate"))
                object.dataKeyBackupDate = options.json && !isFinite(message.dataKeyBackupDate) ? String(message.dataKeyBackupDate) : message.dataKeyBackupDate;
            if (message.userAuthUid != null && Object.hasOwnProperty.call(message, "userAuthUid"))
                object.userAuthUid = options.bytes === String ? $util.base64.encode(message.userAuthUid, 0, message.userAuthUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.userAuthUid) : message.userAuthUid;
            if (message.encryptedPrivateKey != null && Object.hasOwnProperty.call(message, "encryptedPrivateKey"))
                object.encryptedPrivateKey = options.bytes === String ? $util.base64.encode(message.encryptedPrivateKey, 0, message.encryptedPrivateKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedPrivateKey) : message.encryptedPrivateKey;
            if (message.encryptedEccPrivateKey != null && Object.hasOwnProperty.call(message, "encryptedEccPrivateKey"))
                object.encryptedEccPrivateKey = options.bytes === String ? $util.base64.encode(message.encryptedEccPrivateKey, 0, message.encryptedEccPrivateKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedEccPrivateKey) : message.encryptedEccPrivateKey;
            if (message.eccPublicKey != null && Object.hasOwnProperty.call(message, "eccPublicKey"))
                object.eccPublicKey = options.bytes === String ? $util.base64.encode(message.eccPublicKey, 0, message.eccPublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.eccPublicKey) : message.eccPublicKey;
            return object;
        };

        /**
         * Converts this KeysInfo to JSON.
         * @function toJSON
         * @memberof Authentication.KeysInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        KeysInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for KeysInfo
         * @function getTypeUrl
         * @memberof Authentication.KeysInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        KeysInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.KeysInfo";
        };

        return KeysInfo;
    })();

    Authentication.LoginResponse = (function() {

        /**
         * Properties of a LoginResponse.
         * @memberof Authentication
         * @interface ILoginResponse
         * @property {Authentication.LoginState|null} [loginState] LoginResponse loginState
         * @property {Uint8Array|null} [accountUid] LoginResponse accountUid
         * @property {string|null} [primaryUsername] LoginResponse primaryUsername
         * @property {Uint8Array|null} [encryptedDataKey] LoginResponse encryptedDataKey
         * @property {Authentication.EncryptedDataKeyType|null} [encryptedDataKeyType] LoginResponse encryptedDataKeyType
         * @property {Uint8Array|null} [encryptedLoginToken] LoginResponse encryptedLoginToken
         * @property {Uint8Array|null} [encryptedSessionToken] LoginResponse encryptedSessionToken
         * @property {Authentication.SessionTokenType|null} [sessionTokenType] LoginResponse sessionTokenType
         * @property {string|null} [message] LoginResponse message
         * @property {string|null} [url] LoginResponse url
         * @property {Array.<Authentication.ITwoFactorChannelInfo>|null} [channels] LoginResponse channels
         * @property {Array.<Authentication.ISalt>|null} [salt] LoginResponse salt
         * @property {Uint8Array|null} [cloneCode] LoginResponse cloneCode
         * @property {string|null} [stateSpecificValue] LoginResponse stateSpecificValue
         * @property {string|null} [ssoClientVersion] LoginResponse ssoClientVersion
         * @property {string|null} [sessionTokenTypeModifier] LoginResponse sessionTokenTypeModifier
         * @property {Authentication.IKeysInfo|null} [keysInfo] LoginResponse keysInfo
         * @property {Uint8Array|null} [clientKey] LoginResponse clientKey
         */

        /**
         * Constructs a new LoginResponse.
         * @memberof Authentication
         * @classdesc Represents a LoginResponse.
         * @implements ILoginResponse
         * @constructor
         * @param {Authentication.ILoginResponse=} [properties] Properties to set
         */
        function LoginResponse(properties) {
            this.channels = [];
            this.salt = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LoginResponse loginState.
         * @member {Authentication.LoginState} loginState
         * @memberof Authentication.LoginResponse
         * @instance
         */
        LoginResponse.prototype.loginState = 0;

        /**
         * LoginResponse accountUid.
         * @member {Uint8Array} accountUid
         * @memberof Authentication.LoginResponse
         * @instance
         */
        LoginResponse.prototype.accountUid = $util.newBuffer([]);

        /**
         * LoginResponse primaryUsername.
         * @member {string} primaryUsername
         * @memberof Authentication.LoginResponse
         * @instance
         */
        LoginResponse.prototype.primaryUsername = "";

        /**
         * LoginResponse encryptedDataKey.
         * @member {Uint8Array} encryptedDataKey
         * @memberof Authentication.LoginResponse
         * @instance
         */
        LoginResponse.prototype.encryptedDataKey = $util.newBuffer([]);

        /**
         * LoginResponse encryptedDataKeyType.
         * @member {Authentication.EncryptedDataKeyType} encryptedDataKeyType
         * @memberof Authentication.LoginResponse
         * @instance
         */
        LoginResponse.prototype.encryptedDataKeyType = 0;

        /**
         * LoginResponse encryptedLoginToken.
         * @member {Uint8Array} encryptedLoginToken
         * @memberof Authentication.LoginResponse
         * @instance
         */
        LoginResponse.prototype.encryptedLoginToken = $util.newBuffer([]);

        /**
         * LoginResponse encryptedSessionToken.
         * @member {Uint8Array} encryptedSessionToken
         * @memberof Authentication.LoginResponse
         * @instance
         */
        LoginResponse.prototype.encryptedSessionToken = $util.newBuffer([]);

        /**
         * LoginResponse sessionTokenType.
         * @member {Authentication.SessionTokenType} sessionTokenType
         * @memberof Authentication.LoginResponse
         * @instance
         */
        LoginResponse.prototype.sessionTokenType = 0;

        /**
         * LoginResponse message.
         * @member {string} message
         * @memberof Authentication.LoginResponse
         * @instance
         */
        LoginResponse.prototype.message = "";

        /**
         * LoginResponse url.
         * @member {string} url
         * @memberof Authentication.LoginResponse
         * @instance
         */
        LoginResponse.prototype.url = "";

        /**
         * LoginResponse channels.
         * @member {Array.<Authentication.ITwoFactorChannelInfo>} channels
         * @memberof Authentication.LoginResponse
         * @instance
         */
        LoginResponse.prototype.channels = $util.emptyArray;

        /**
         * LoginResponse salt.
         * @member {Array.<Authentication.ISalt>} salt
         * @memberof Authentication.LoginResponse
         * @instance
         */
        LoginResponse.prototype.salt = $util.emptyArray;

        /**
         * LoginResponse cloneCode.
         * @member {Uint8Array} cloneCode
         * @memberof Authentication.LoginResponse
         * @instance
         */
        LoginResponse.prototype.cloneCode = $util.newBuffer([]);

        /**
         * LoginResponse stateSpecificValue.
         * @member {string} stateSpecificValue
         * @memberof Authentication.LoginResponse
         * @instance
         */
        LoginResponse.prototype.stateSpecificValue = "";

        /**
         * LoginResponse ssoClientVersion.
         * @member {string} ssoClientVersion
         * @memberof Authentication.LoginResponse
         * @instance
         */
        LoginResponse.prototype.ssoClientVersion = "";

        /**
         * LoginResponse sessionTokenTypeModifier.
         * @member {string} sessionTokenTypeModifier
         * @memberof Authentication.LoginResponse
         * @instance
         */
        LoginResponse.prototype.sessionTokenTypeModifier = "";

        /**
         * LoginResponse keysInfo.
         * @member {Authentication.IKeysInfo|null|undefined} keysInfo
         * @memberof Authentication.LoginResponse
         * @instance
         */
        LoginResponse.prototype.keysInfo = null;

        /**
         * LoginResponse clientKey.
         * @member {Uint8Array} clientKey
         * @memberof Authentication.LoginResponse
         * @instance
         */
        LoginResponse.prototype.clientKey = $util.newBuffer([]);

        /**
         * Creates a new LoginResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.LoginResponse
         * @static
         * @param {Authentication.ILoginResponse=} [properties] Properties to set
         * @returns {Authentication.LoginResponse} LoginResponse instance
         */
        LoginResponse.create = function create(properties) {
            return new LoginResponse(properties);
        };

        /**
         * Encodes the specified LoginResponse message. Does not implicitly {@link Authentication.LoginResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.LoginResponse
         * @static
         * @param {Authentication.ILoginResponse} message LoginResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LoginResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.loginState != null && Object.hasOwnProperty.call(message, "loginState"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.loginState);
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.accountUid);
            if (message.primaryUsername != null && Object.hasOwnProperty.call(message, "primaryUsername"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.primaryUsername);
            if (message.encryptedDataKey != null && Object.hasOwnProperty.call(message, "encryptedDataKey"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.encryptedDataKey);
            if (message.encryptedDataKeyType != null && Object.hasOwnProperty.call(message, "encryptedDataKeyType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.encryptedDataKeyType);
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.encryptedLoginToken);
            if (message.encryptedSessionToken != null && Object.hasOwnProperty.call(message, "encryptedSessionToken"))
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.encryptedSessionToken);
            if (message.sessionTokenType != null && Object.hasOwnProperty.call(message, "sessionTokenType"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.sessionTokenType);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.message);
            if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.url);
            if (message.channels != null && message.channels.length)
                for (let i = 0; i < message.channels.length; ++i)
                    $root.Authentication.TwoFactorChannelInfo.encode(message.channels[i], writer.uint32(/* id 11, wireType 2 =*/90).fork(), q + 1).ldelim();
            if (message.salt != null && message.salt.length)
                for (let i = 0; i < message.salt.length; ++i)
                    $root.Authentication.Salt.encode(message.salt[i], writer.uint32(/* id 12, wireType 2 =*/98).fork(), q + 1).ldelim();
            if (message.cloneCode != null && Object.hasOwnProperty.call(message, "cloneCode"))
                writer.uint32(/* id 13, wireType 2 =*/106).bytes(message.cloneCode);
            if (message.stateSpecificValue != null && Object.hasOwnProperty.call(message, "stateSpecificValue"))
                writer.uint32(/* id 14, wireType 2 =*/114).string(message.stateSpecificValue);
            if (message.ssoClientVersion != null && Object.hasOwnProperty.call(message, "ssoClientVersion"))
                writer.uint32(/* id 15, wireType 2 =*/122).string(message.ssoClientVersion);
            if (message.sessionTokenTypeModifier != null && Object.hasOwnProperty.call(message, "sessionTokenTypeModifier"))
                writer.uint32(/* id 16, wireType 2 =*/130).string(message.sessionTokenTypeModifier);
            if (message.keysInfo != null && Object.hasOwnProperty.call(message, "keysInfo"))
                $root.Authentication.KeysInfo.encode(message.keysInfo, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
            if (message.clientKey != null && Object.hasOwnProperty.call(message, "clientKey"))
                writer.uint32(/* id 18, wireType 2 =*/146).bytes(message.clientKey);
            return writer;
        };

        /**
         * Decodes a LoginResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.LoginResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.LoginResponse} LoginResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LoginResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.LoginResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.loginState = reader.int32();
                        break;
                    }
                case 2: {
                        message.accountUid = reader.bytes();
                        break;
                    }
                case 3: {
                        message.primaryUsername = reader.string();
                        break;
                    }
                case 4: {
                        message.encryptedDataKey = reader.bytes();
                        break;
                    }
                case 5: {
                        message.encryptedDataKeyType = reader.int32();
                        break;
                    }
                case 6: {
                        message.encryptedLoginToken = reader.bytes();
                        break;
                    }
                case 7: {
                        message.encryptedSessionToken = reader.bytes();
                        break;
                    }
                case 8: {
                        message.sessionTokenType = reader.int32();
                        break;
                    }
                case 9: {
                        message.message = reader.string();
                        break;
                    }
                case 10: {
                        message.url = reader.string();
                        break;
                    }
                case 11: {
                        if (!(message.channels && message.channels.length))
                            message.channels = [];
                        message.channels.push($root.Authentication.TwoFactorChannelInfo.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 12: {
                        if (!(message.salt && message.salt.length))
                            message.salt = [];
                        message.salt.push($root.Authentication.Salt.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 13: {
                        message.cloneCode = reader.bytes();
                        break;
                    }
                case 14: {
                        message.stateSpecificValue = reader.string();
                        break;
                    }
                case 15: {
                        message.ssoClientVersion = reader.string();
                        break;
                    }
                case 16: {
                        message.sessionTokenTypeModifier = reader.string();
                        break;
                    }
                case 17: {
                        message.keysInfo = $root.Authentication.KeysInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 18: {
                        message.clientKey = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a LoginResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.LoginResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.LoginResponse} LoginResponse
         */
        LoginResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.LoginResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.LoginResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.LoginResponse();
            switch (object.loginState) {
            default:
                if (typeof object.loginState === "number") {
                    message.loginState = object.loginState;
                    break;
                }
                break;
            case "INVALID_LOGINSTATE":
            case 0:
                message.loginState = 0;
                break;
            case "LOGGED_OUT":
            case 1:
                message.loginState = 1;
                break;
            case "DEVICE_APPROVAL_REQUIRED":
            case 2:
                message.loginState = 2;
                break;
            case "DEVICE_LOCKED":
            case 3:
                message.loginState = 3;
                break;
            case "ACCOUNT_LOCKED":
            case 4:
                message.loginState = 4;
                break;
            case "DEVICE_ACCOUNT_LOCKED":
            case 5:
                message.loginState = 5;
                break;
            case "UPGRADE":
            case 6:
                message.loginState = 6;
                break;
            case "LICENSE_EXPIRED":
            case 7:
                message.loginState = 7;
                break;
            case "REGION_REDIRECT":
            case 8:
                message.loginState = 8;
                break;
            case "REDIRECT_CLOUD_SSO":
            case 9:
                message.loginState = 9;
                break;
            case "REDIRECT_ONSITE_SSO":
            case 10:
                message.loginState = 10;
                break;
            case "REQUIRES_2FA":
            case 12:
                message.loginState = 12;
                break;
            case "REQUIRES_AUTH_HASH":
            case 13:
                message.loginState = 13;
                break;
            case "REQUIRES_USERNAME":
            case 14:
                message.loginState = 14;
                break;
            case "AFTER_CLOUD_SSO_LOGIN":
            case 15:
                message.loginState = 15;
                break;
            case "REQUIRES_ACCOUNT_CREATION":
            case 16:
                message.loginState = 16;
                break;
            case "REQUIRES_DEVICE_ENCRYPTED_DATA_KEY":
            case 17:
                message.loginState = 17;
                break;
            case "LOGIN_TOKEN_EXPIRED":
            case 18:
                message.loginState = 18;
                break;
            case "PASSKEY_INITIATE_CHALLENGE":
            case 19:
                message.loginState = 19;
                break;
            case "PASSKEY_AUTH_REQUIRED":
            case 20:
                message.loginState = 20;
                break;
            case "PASSKEY_VERIFY_AUTHENTICATION":
            case 21:
                message.loginState = 21;
                break;
            case "AFTER_PASSKEY_LOGIN":
            case 22:
                message.loginState = 22;
                break;
            case "LOGGED_IN":
            case 99:
                message.loginState = 99;
                break;
            }
            if (object.accountUid != null)
                if (typeof object.accountUid === "string")
                    $util.base64.decode(object.accountUid, message.accountUid = $util.newBuffer($util.base64.length(object.accountUid)), 0);
                else if (object.accountUid.length >= 0)
                    message.accountUid = object.accountUid;
            if (object.primaryUsername != null)
                message.primaryUsername = String(object.primaryUsername);
            if (object.encryptedDataKey != null)
                if (typeof object.encryptedDataKey === "string")
                    $util.base64.decode(object.encryptedDataKey, message.encryptedDataKey = $util.newBuffer($util.base64.length(object.encryptedDataKey)), 0);
                else if (object.encryptedDataKey.length >= 0)
                    message.encryptedDataKey = object.encryptedDataKey;
            switch (object.encryptedDataKeyType) {
            default:
                if (typeof object.encryptedDataKeyType === "number") {
                    message.encryptedDataKeyType = object.encryptedDataKeyType;
                    break;
                }
                break;
            case "NO_KEY":
            case 0:
                message.encryptedDataKeyType = 0;
                break;
            case "BY_DEVICE_PUBLIC_KEY":
            case 1:
                message.encryptedDataKeyType = 1;
                break;
            case "BY_PASSWORD":
            case 2:
                message.encryptedDataKeyType = 2;
                break;
            case "BY_ALTERNATE":
            case 3:
                message.encryptedDataKeyType = 3;
                break;
            case "BY_BIO":
            case 4:
                message.encryptedDataKeyType = 4;
                break;
            }
            if (object.encryptedLoginToken != null)
                if (typeof object.encryptedLoginToken === "string")
                    $util.base64.decode(object.encryptedLoginToken, message.encryptedLoginToken = $util.newBuffer($util.base64.length(object.encryptedLoginToken)), 0);
                else if (object.encryptedLoginToken.length >= 0)
                    message.encryptedLoginToken = object.encryptedLoginToken;
            if (object.encryptedSessionToken != null)
                if (typeof object.encryptedSessionToken === "string")
                    $util.base64.decode(object.encryptedSessionToken, message.encryptedSessionToken = $util.newBuffer($util.base64.length(object.encryptedSessionToken)), 0);
                else if (object.encryptedSessionToken.length >= 0)
                    message.encryptedSessionToken = object.encryptedSessionToken;
            switch (object.sessionTokenType) {
            default:
                if (typeof object.sessionTokenType === "number") {
                    message.sessionTokenType = object.sessionTokenType;
                    break;
                }
                break;
            case "NO_RESTRICTION":
            case 0:
                message.sessionTokenType = 0;
                break;
            case "ACCOUNT_RECOVERY":
            case 1:
                message.sessionTokenType = 1;
                break;
            case "SHARE_ACCOUNT":
            case 2:
                message.sessionTokenType = 2;
                break;
            case "PURCHASE":
            case 3:
                message.sessionTokenType = 3;
                break;
            case "RESTRICT":
            case 4:
                message.sessionTokenType = 4;
                break;
            case "ACCEPT_INVITE":
            case 5:
                message.sessionTokenType = 5;
                break;
            case "SUPPORT_SERVER":
            case 6:
                message.sessionTokenType = 6;
                break;
            case "ENTERPRISE_CREATION":
            case 7:
                message.sessionTokenType = 7;
                break;
            case "EXPIRED_BUT_ALLOWED_TO_SYNC":
            case 8:
                message.sessionTokenType = 8;
                break;
            case "ACCEPT_FAMILY_INVITE":
            case 9:
                message.sessionTokenType = 9;
                break;
            case "ENTERPRISE_CREATION_PURCHASED":
            case 10:
                message.sessionTokenType = 10;
                break;
            case "EMERGENCY_ACCESS":
            case 11:
                message.sessionTokenType = 11;
                break;
            }
            if (object.message != null)
                message.message = String(object.message);
            if (object.url != null)
                message.url = String(object.url);
            if (object.channels) {
                if (!Array.isArray(object.channels))
                    throw TypeError(".Authentication.LoginResponse.channels: array expected");
                message.channels = [];
                for (let i = 0; i < object.channels.length; ++i) {
                    if (!$util.isObject(object.channels[i]))
                        throw TypeError(".Authentication.LoginResponse.channels: object expected");
                    message.channels[i] = $root.Authentication.TwoFactorChannelInfo.fromObject(object.channels[i], long + 1);
                }
            }
            if (object.salt) {
                if (!Array.isArray(object.salt))
                    throw TypeError(".Authentication.LoginResponse.salt: array expected");
                message.salt = [];
                for (let i = 0; i < object.salt.length; ++i) {
                    if (!$util.isObject(object.salt[i]))
                        throw TypeError(".Authentication.LoginResponse.salt: object expected");
                    message.salt[i] = $root.Authentication.Salt.fromObject(object.salt[i], long + 1);
                }
            }
            if (object.cloneCode != null)
                if (typeof object.cloneCode === "string")
                    $util.base64.decode(object.cloneCode, message.cloneCode = $util.newBuffer($util.base64.length(object.cloneCode)), 0);
                else if (object.cloneCode.length >= 0)
                    message.cloneCode = object.cloneCode;
            if (object.stateSpecificValue != null)
                message.stateSpecificValue = String(object.stateSpecificValue);
            if (object.ssoClientVersion != null)
                message.ssoClientVersion = String(object.ssoClientVersion);
            if (object.sessionTokenTypeModifier != null)
                message.sessionTokenTypeModifier = String(object.sessionTokenTypeModifier);
            if (object.keysInfo != null) {
                if (!$util.isObject(object.keysInfo))
                    throw TypeError(".Authentication.LoginResponse.keysInfo: object expected");
                message.keysInfo = $root.Authentication.KeysInfo.fromObject(object.keysInfo, long + 1);
            }
            if (object.clientKey != null)
                if (typeof object.clientKey === "string")
                    $util.base64.decode(object.clientKey, message.clientKey = $util.newBuffer($util.base64.length(object.clientKey)), 0);
                else if (object.clientKey.length >= 0)
                    message.clientKey = object.clientKey;
            return message;
        };

        /**
         * Creates a plain object from a LoginResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.LoginResponse
         * @static
         * @param {Authentication.LoginResponse} message LoginResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LoginResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.channels = [];
                object.salt = [];
            }
            if (options.defaults) {
                object.loginState = options.enums === String ? "INVALID_LOGINSTATE" : 0;
                if (options.bytes === String)
                    object.accountUid = "";
                else {
                    object.accountUid = [];
                    if (options.bytes !== Array)
                        object.accountUid = $util.newBuffer(object.accountUid);
                }
                object.primaryUsername = "";
                if (options.bytes === String)
                    object.encryptedDataKey = "";
                else {
                    object.encryptedDataKey = [];
                    if (options.bytes !== Array)
                        object.encryptedDataKey = $util.newBuffer(object.encryptedDataKey);
                }
                object.encryptedDataKeyType = options.enums === String ? "NO_KEY" : 0;
                if (options.bytes === String)
                    object.encryptedLoginToken = "";
                else {
                    object.encryptedLoginToken = [];
                    if (options.bytes !== Array)
                        object.encryptedLoginToken = $util.newBuffer(object.encryptedLoginToken);
                }
                if (options.bytes === String)
                    object.encryptedSessionToken = "";
                else {
                    object.encryptedSessionToken = [];
                    if (options.bytes !== Array)
                        object.encryptedSessionToken = $util.newBuffer(object.encryptedSessionToken);
                }
                object.sessionTokenType = options.enums === String ? "NO_RESTRICTION" : 0;
                object.message = "";
                object.url = "";
                if (options.bytes === String)
                    object.cloneCode = "";
                else {
                    object.cloneCode = [];
                    if (options.bytes !== Array)
                        object.cloneCode = $util.newBuffer(object.cloneCode);
                }
                object.stateSpecificValue = "";
                object.ssoClientVersion = "";
                object.sessionTokenTypeModifier = "";
                object.keysInfo = null;
                if (options.bytes === String)
                    object.clientKey = "";
                else {
                    object.clientKey = [];
                    if (options.bytes !== Array)
                        object.clientKey = $util.newBuffer(object.clientKey);
                }
            }
            if (message.loginState != null && Object.hasOwnProperty.call(message, "loginState"))
                object.loginState = options.enums === String ? $root.Authentication.LoginState[message.loginState] === undefined ? message.loginState : $root.Authentication.LoginState[message.loginState] : message.loginState;
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                object.accountUid = options.bytes === String ? $util.base64.encode(message.accountUid, 0, message.accountUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.accountUid) : message.accountUid;
            if (message.primaryUsername != null && Object.hasOwnProperty.call(message, "primaryUsername"))
                object.primaryUsername = message.primaryUsername;
            if (message.encryptedDataKey != null && Object.hasOwnProperty.call(message, "encryptedDataKey"))
                object.encryptedDataKey = options.bytes === String ? $util.base64.encode(message.encryptedDataKey, 0, message.encryptedDataKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDataKey) : message.encryptedDataKey;
            if (message.encryptedDataKeyType != null && Object.hasOwnProperty.call(message, "encryptedDataKeyType"))
                object.encryptedDataKeyType = options.enums === String ? $root.Authentication.EncryptedDataKeyType[message.encryptedDataKeyType] === undefined ? message.encryptedDataKeyType : $root.Authentication.EncryptedDataKeyType[message.encryptedDataKeyType] : message.encryptedDataKeyType;
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                object.encryptedLoginToken = options.bytes === String ? $util.base64.encode(message.encryptedLoginToken, 0, message.encryptedLoginToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedLoginToken) : message.encryptedLoginToken;
            if (message.encryptedSessionToken != null && Object.hasOwnProperty.call(message, "encryptedSessionToken"))
                object.encryptedSessionToken = options.bytes === String ? $util.base64.encode(message.encryptedSessionToken, 0, message.encryptedSessionToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedSessionToken) : message.encryptedSessionToken;
            if (message.sessionTokenType != null && Object.hasOwnProperty.call(message, "sessionTokenType"))
                object.sessionTokenType = options.enums === String ? $root.Authentication.SessionTokenType[message.sessionTokenType] === undefined ? message.sessionTokenType : $root.Authentication.SessionTokenType[message.sessionTokenType] : message.sessionTokenType;
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                object.message = message.message;
            if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                object.url = message.url;
            if (message.channels && message.channels.length) {
                object.channels = [];
                for (let j = 0; j < message.channels.length; ++j)
                    object.channels[j] = $root.Authentication.TwoFactorChannelInfo.toObject(message.channels[j], options, q + 1);
            }
            if (message.salt && message.salt.length) {
                object.salt = [];
                for (let j = 0; j < message.salt.length; ++j)
                    object.salt[j] = $root.Authentication.Salt.toObject(message.salt[j], options, q + 1);
            }
            if (message.cloneCode != null && Object.hasOwnProperty.call(message, "cloneCode"))
                object.cloneCode = options.bytes === String ? $util.base64.encode(message.cloneCode, 0, message.cloneCode.length) : options.bytes === Array ? Array.prototype.slice.call(message.cloneCode) : message.cloneCode;
            if (message.stateSpecificValue != null && Object.hasOwnProperty.call(message, "stateSpecificValue"))
                object.stateSpecificValue = message.stateSpecificValue;
            if (message.ssoClientVersion != null && Object.hasOwnProperty.call(message, "ssoClientVersion"))
                object.ssoClientVersion = message.ssoClientVersion;
            if (message.sessionTokenTypeModifier != null && Object.hasOwnProperty.call(message, "sessionTokenTypeModifier"))
                object.sessionTokenTypeModifier = message.sessionTokenTypeModifier;
            if (message.keysInfo != null && Object.hasOwnProperty.call(message, "keysInfo"))
                object.keysInfo = $root.Authentication.KeysInfo.toObject(message.keysInfo, options, q + 1);
            if (message.clientKey != null && Object.hasOwnProperty.call(message, "clientKey"))
                object.clientKey = options.bytes === String ? $util.base64.encode(message.clientKey, 0, message.clientKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.clientKey) : message.clientKey;
            return object;
        };

        /**
         * Converts this LoginResponse to JSON.
         * @function toJSON
         * @memberof Authentication.LoginResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LoginResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LoginResponse
         * @function getTypeUrl
         * @memberof Authentication.LoginResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LoginResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.LoginResponse";
        };

        return LoginResponse;
    })();

    Authentication.SwitchListElement = (function() {

        /**
         * Properties of a SwitchListElement.
         * @memberof Authentication
         * @interface ISwitchListElement
         * @property {string|null} [username] SwitchListElement username
         * @property {string|null} [fullName] SwitchListElement fullName
         * @property {boolean|null} [authRequired] SwitchListElement authRequired
         * @property {boolean|null} [isLinked] SwitchListElement isLinked
         * @property {string|null} [profilePicUrl] SwitchListElement profilePicUrl
         */

        /**
         * Constructs a new SwitchListElement.
         * @memberof Authentication
         * @classdesc Represents a SwitchListElement.
         * @implements ISwitchListElement
         * @constructor
         * @param {Authentication.ISwitchListElement=} [properties] Properties to set
         */
        function SwitchListElement(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SwitchListElement username.
         * @member {string} username
         * @memberof Authentication.SwitchListElement
         * @instance
         */
        SwitchListElement.prototype.username = "";

        /**
         * SwitchListElement fullName.
         * @member {string} fullName
         * @memberof Authentication.SwitchListElement
         * @instance
         */
        SwitchListElement.prototype.fullName = "";

        /**
         * SwitchListElement authRequired.
         * @member {boolean} authRequired
         * @memberof Authentication.SwitchListElement
         * @instance
         */
        SwitchListElement.prototype.authRequired = false;

        /**
         * SwitchListElement isLinked.
         * @member {boolean} isLinked
         * @memberof Authentication.SwitchListElement
         * @instance
         */
        SwitchListElement.prototype.isLinked = false;

        /**
         * SwitchListElement profilePicUrl.
         * @member {string} profilePicUrl
         * @memberof Authentication.SwitchListElement
         * @instance
         */
        SwitchListElement.prototype.profilePicUrl = "";

        /**
         * Creates a new SwitchListElement instance using the specified properties.
         * @function create
         * @memberof Authentication.SwitchListElement
         * @static
         * @param {Authentication.ISwitchListElement=} [properties] Properties to set
         * @returns {Authentication.SwitchListElement} SwitchListElement instance
         */
        SwitchListElement.create = function create(properties) {
            return new SwitchListElement(properties);
        };

        /**
         * Encodes the specified SwitchListElement message. Does not implicitly {@link Authentication.SwitchListElement.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SwitchListElement
         * @static
         * @param {Authentication.ISwitchListElement} message SwitchListElement message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SwitchListElement.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.username);
            if (message.fullName != null && Object.hasOwnProperty.call(message, "fullName"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.fullName);
            if (message.authRequired != null && Object.hasOwnProperty.call(message, "authRequired"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.authRequired);
            if (message.isLinked != null && Object.hasOwnProperty.call(message, "isLinked"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.isLinked);
            if (message.profilePicUrl != null && Object.hasOwnProperty.call(message, "profilePicUrl"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.profilePicUrl);
            return writer;
        };

        /**
         * Decodes a SwitchListElement message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SwitchListElement
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SwitchListElement} SwitchListElement
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SwitchListElement.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SwitchListElement();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.username = reader.string();
                        break;
                    }
                case 2: {
                        message.fullName = reader.string();
                        break;
                    }
                case 3: {
                        message.authRequired = reader.bool();
                        break;
                    }
                case 4: {
                        message.isLinked = reader.bool();
                        break;
                    }
                case 5: {
                        message.profilePicUrl = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SwitchListElement message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SwitchListElement
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SwitchListElement} SwitchListElement
         */
        SwitchListElement.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SwitchListElement)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SwitchListElement: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SwitchListElement();
            if (object.username != null)
                message.username = String(object.username);
            if (object.fullName != null)
                message.fullName = String(object.fullName);
            if (object.authRequired != null)
                message.authRequired = Boolean(object.authRequired);
            if (object.isLinked != null)
                message.isLinked = Boolean(object.isLinked);
            if (object.profilePicUrl != null)
                message.profilePicUrl = String(object.profilePicUrl);
            return message;
        };

        /**
         * Creates a plain object from a SwitchListElement message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SwitchListElement
         * @static
         * @param {Authentication.SwitchListElement} message SwitchListElement
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SwitchListElement.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.username = "";
                object.fullName = "";
                object.authRequired = false;
                object.isLinked = false;
                object.profilePicUrl = "";
            }
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.fullName != null && Object.hasOwnProperty.call(message, "fullName"))
                object.fullName = message.fullName;
            if (message.authRequired != null && Object.hasOwnProperty.call(message, "authRequired"))
                object.authRequired = message.authRequired;
            if (message.isLinked != null && Object.hasOwnProperty.call(message, "isLinked"))
                object.isLinked = message.isLinked;
            if (message.profilePicUrl != null && Object.hasOwnProperty.call(message, "profilePicUrl"))
                object.profilePicUrl = message.profilePicUrl;
            return object;
        };

        /**
         * Converts this SwitchListElement to JSON.
         * @function toJSON
         * @memberof Authentication.SwitchListElement
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SwitchListElement.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SwitchListElement
         * @function getTypeUrl
         * @memberof Authentication.SwitchListElement
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SwitchListElement.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SwitchListElement";
        };

        return SwitchListElement;
    })();

    Authentication.SwitchListResponse = (function() {

        /**
         * Properties of a SwitchListResponse.
         * @memberof Authentication
         * @interface ISwitchListResponse
         * @property {Array.<Authentication.ISwitchListElement>|null} [elements] SwitchListResponse elements
         */

        /**
         * Constructs a new SwitchListResponse.
         * @memberof Authentication
         * @classdesc Represents a SwitchListResponse.
         * @implements ISwitchListResponse
         * @constructor
         * @param {Authentication.ISwitchListResponse=} [properties] Properties to set
         */
        function SwitchListResponse(properties) {
            this.elements = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SwitchListResponse elements.
         * @member {Array.<Authentication.ISwitchListElement>} elements
         * @memberof Authentication.SwitchListResponse
         * @instance
         */
        SwitchListResponse.prototype.elements = $util.emptyArray;

        /**
         * Creates a new SwitchListResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.SwitchListResponse
         * @static
         * @param {Authentication.ISwitchListResponse=} [properties] Properties to set
         * @returns {Authentication.SwitchListResponse} SwitchListResponse instance
         */
        SwitchListResponse.create = function create(properties) {
            return new SwitchListResponse(properties);
        };

        /**
         * Encodes the specified SwitchListResponse message. Does not implicitly {@link Authentication.SwitchListResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SwitchListResponse
         * @static
         * @param {Authentication.ISwitchListResponse} message SwitchListResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SwitchListResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.elements != null && message.elements.length)
                for (let i = 0; i < message.elements.length; ++i)
                    $root.Authentication.SwitchListElement.encode(message.elements[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes a SwitchListResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SwitchListResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SwitchListResponse} SwitchListResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SwitchListResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SwitchListResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.elements && message.elements.length))
                            message.elements = [];
                        message.elements.push($root.Authentication.SwitchListElement.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SwitchListResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SwitchListResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SwitchListResponse} SwitchListResponse
         */
        SwitchListResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SwitchListResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SwitchListResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SwitchListResponse();
            if (object.elements) {
                if (!Array.isArray(object.elements))
                    throw TypeError(".Authentication.SwitchListResponse.elements: array expected");
                message.elements = [];
                for (let i = 0; i < object.elements.length; ++i) {
                    if (!$util.isObject(object.elements[i]))
                        throw TypeError(".Authentication.SwitchListResponse.elements: object expected");
                    message.elements[i] = $root.Authentication.SwitchListElement.fromObject(object.elements[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SwitchListResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SwitchListResponse
         * @static
         * @param {Authentication.SwitchListResponse} message SwitchListResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SwitchListResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.elements = [];
            if (message.elements && message.elements.length) {
                object.elements = [];
                for (let j = 0; j < message.elements.length; ++j)
                    object.elements[j] = $root.Authentication.SwitchListElement.toObject(message.elements[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this SwitchListResponse to JSON.
         * @function toJSON
         * @memberof Authentication.SwitchListResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SwitchListResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SwitchListResponse
         * @function getTypeUrl
         * @memberof Authentication.SwitchListResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SwitchListResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SwitchListResponse";
        };

        return SwitchListResponse;
    })();

    Authentication.SsoUserInfo = (function() {

        /**
         * Properties of a SsoUserInfo.
         * @memberof Authentication
         * @interface ISsoUserInfo
         * @property {string|null} [companyName] SsoUserInfo companyName
         * @property {string|null} [samlRequest] SsoUserInfo samlRequest
         * @property {string|null} [samlRequestType] SsoUserInfo samlRequestType
         * @property {string|null} [ssoDomainName] SsoUserInfo ssoDomainName
         * @property {string|null} [loginUrl] SsoUserInfo loginUrl
         * @property {string|null} [logoutUrl] SsoUserInfo logoutUrl
         */

        /**
         * Constructs a new SsoUserInfo.
         * @memberof Authentication
         * @classdesc Represents a SsoUserInfo.
         * @implements ISsoUserInfo
         * @constructor
         * @param {Authentication.ISsoUserInfo=} [properties] Properties to set
         */
        function SsoUserInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SsoUserInfo companyName.
         * @member {string} companyName
         * @memberof Authentication.SsoUserInfo
         * @instance
         */
        SsoUserInfo.prototype.companyName = "";

        /**
         * SsoUserInfo samlRequest.
         * @member {string} samlRequest
         * @memberof Authentication.SsoUserInfo
         * @instance
         */
        SsoUserInfo.prototype.samlRequest = "";

        /**
         * SsoUserInfo samlRequestType.
         * @member {string} samlRequestType
         * @memberof Authentication.SsoUserInfo
         * @instance
         */
        SsoUserInfo.prototype.samlRequestType = "";

        /**
         * SsoUserInfo ssoDomainName.
         * @member {string} ssoDomainName
         * @memberof Authentication.SsoUserInfo
         * @instance
         */
        SsoUserInfo.prototype.ssoDomainName = "";

        /**
         * SsoUserInfo loginUrl.
         * @member {string} loginUrl
         * @memberof Authentication.SsoUserInfo
         * @instance
         */
        SsoUserInfo.prototype.loginUrl = "";

        /**
         * SsoUserInfo logoutUrl.
         * @member {string} logoutUrl
         * @memberof Authentication.SsoUserInfo
         * @instance
         */
        SsoUserInfo.prototype.logoutUrl = "";

        /**
         * Creates a new SsoUserInfo instance using the specified properties.
         * @function create
         * @memberof Authentication.SsoUserInfo
         * @static
         * @param {Authentication.ISsoUserInfo=} [properties] Properties to set
         * @returns {Authentication.SsoUserInfo} SsoUserInfo instance
         */
        SsoUserInfo.create = function create(properties) {
            return new SsoUserInfo(properties);
        };

        /**
         * Encodes the specified SsoUserInfo message. Does not implicitly {@link Authentication.SsoUserInfo.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SsoUserInfo
         * @static
         * @param {Authentication.ISsoUserInfo} message SsoUserInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SsoUserInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.companyName != null && Object.hasOwnProperty.call(message, "companyName"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.companyName);
            if (message.samlRequest != null && Object.hasOwnProperty.call(message, "samlRequest"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.samlRequest);
            if (message.samlRequestType != null && Object.hasOwnProperty.call(message, "samlRequestType"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.samlRequestType);
            if (message.ssoDomainName != null && Object.hasOwnProperty.call(message, "ssoDomainName"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ssoDomainName);
            if (message.loginUrl != null && Object.hasOwnProperty.call(message, "loginUrl"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.loginUrl);
            if (message.logoutUrl != null && Object.hasOwnProperty.call(message, "logoutUrl"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.logoutUrl);
            return writer;
        };

        /**
         * Decodes a SsoUserInfo message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SsoUserInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SsoUserInfo} SsoUserInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SsoUserInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SsoUserInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.companyName = reader.string();
                        break;
                    }
                case 2: {
                        message.samlRequest = reader.string();
                        break;
                    }
                case 3: {
                        message.samlRequestType = reader.string();
                        break;
                    }
                case 4: {
                        message.ssoDomainName = reader.string();
                        break;
                    }
                case 5: {
                        message.loginUrl = reader.string();
                        break;
                    }
                case 6: {
                        message.logoutUrl = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SsoUserInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SsoUserInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SsoUserInfo} SsoUserInfo
         */
        SsoUserInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SsoUserInfo)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SsoUserInfo: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SsoUserInfo();
            if (object.companyName != null)
                message.companyName = String(object.companyName);
            if (object.samlRequest != null)
                message.samlRequest = String(object.samlRequest);
            if (object.samlRequestType != null)
                message.samlRequestType = String(object.samlRequestType);
            if (object.ssoDomainName != null)
                message.ssoDomainName = String(object.ssoDomainName);
            if (object.loginUrl != null)
                message.loginUrl = String(object.loginUrl);
            if (object.logoutUrl != null)
                message.logoutUrl = String(object.logoutUrl);
            return message;
        };

        /**
         * Creates a plain object from a SsoUserInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SsoUserInfo
         * @static
         * @param {Authentication.SsoUserInfo} message SsoUserInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SsoUserInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.companyName = "";
                object.samlRequest = "";
                object.samlRequestType = "";
                object.ssoDomainName = "";
                object.loginUrl = "";
                object.logoutUrl = "";
            }
            if (message.companyName != null && Object.hasOwnProperty.call(message, "companyName"))
                object.companyName = message.companyName;
            if (message.samlRequest != null && Object.hasOwnProperty.call(message, "samlRequest"))
                object.samlRequest = message.samlRequest;
            if (message.samlRequestType != null && Object.hasOwnProperty.call(message, "samlRequestType"))
                object.samlRequestType = message.samlRequestType;
            if (message.ssoDomainName != null && Object.hasOwnProperty.call(message, "ssoDomainName"))
                object.ssoDomainName = message.ssoDomainName;
            if (message.loginUrl != null && Object.hasOwnProperty.call(message, "loginUrl"))
                object.loginUrl = message.loginUrl;
            if (message.logoutUrl != null && Object.hasOwnProperty.call(message, "logoutUrl"))
                object.logoutUrl = message.logoutUrl;
            return object;
        };

        /**
         * Converts this SsoUserInfo to JSON.
         * @function toJSON
         * @memberof Authentication.SsoUserInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SsoUserInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SsoUserInfo
         * @function getTypeUrl
         * @memberof Authentication.SsoUserInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SsoUserInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SsoUserInfo";
        };

        return SsoUserInfo;
    })();

    Authentication.PreLoginResponse = (function() {

        /**
         * Properties of a PreLoginResponse.
         * @memberof Authentication
         * @interface IPreLoginResponse
         * @property {Authentication.DeviceStatus|null} [deviceStatus] PreLoginResponse deviceStatus
         * @property {Array.<Authentication.ISalt>|null} [salt] PreLoginResponse salt
         * @property {Array.<Authentication.ITwoFactorChannel>|null} [OBSOLETE_FIELD] PreLoginResponse OBSOLETE_FIELD
         * @property {Authentication.ISsoUserInfo|null} [ssoUserInfo] PreLoginResponse ssoUserInfo
         */

        /**
         * Constructs a new PreLoginResponse.
         * @memberof Authentication
         * @classdesc Represents a PreLoginResponse.
         * @implements IPreLoginResponse
         * @constructor
         * @param {Authentication.IPreLoginResponse=} [properties] Properties to set
         */
        function PreLoginResponse(properties) {
            this.salt = [];
            this.OBSOLETE_FIELD = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PreLoginResponse deviceStatus.
         * @member {Authentication.DeviceStatus} deviceStatus
         * @memberof Authentication.PreLoginResponse
         * @instance
         */
        PreLoginResponse.prototype.deviceStatus = 0;

        /**
         * PreLoginResponse salt.
         * @member {Array.<Authentication.ISalt>} salt
         * @memberof Authentication.PreLoginResponse
         * @instance
         */
        PreLoginResponse.prototype.salt = $util.emptyArray;

        /**
         * PreLoginResponse OBSOLETE_FIELD.
         * @member {Array.<Authentication.ITwoFactorChannel>} OBSOLETE_FIELD
         * @memberof Authentication.PreLoginResponse
         * @instance
         */
        PreLoginResponse.prototype.OBSOLETE_FIELD = $util.emptyArray;

        /**
         * PreLoginResponse ssoUserInfo.
         * @member {Authentication.ISsoUserInfo|null|undefined} ssoUserInfo
         * @memberof Authentication.PreLoginResponse
         * @instance
         */
        PreLoginResponse.prototype.ssoUserInfo = null;

        /**
         * Creates a new PreLoginResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.PreLoginResponse
         * @static
         * @param {Authentication.IPreLoginResponse=} [properties] Properties to set
         * @returns {Authentication.PreLoginResponse} PreLoginResponse instance
         */
        PreLoginResponse.create = function create(properties) {
            return new PreLoginResponse(properties);
        };

        /**
         * Encodes the specified PreLoginResponse message. Does not implicitly {@link Authentication.PreLoginResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.PreLoginResponse
         * @static
         * @param {Authentication.IPreLoginResponse} message PreLoginResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PreLoginResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.deviceStatus != null && Object.hasOwnProperty.call(message, "deviceStatus"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.deviceStatus);
            if (message.salt != null && message.salt.length)
                for (let i = 0; i < message.salt.length; ++i)
                    $root.Authentication.Salt.encode(message.salt[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.OBSOLETE_FIELD != null && message.OBSOLETE_FIELD.length)
                for (let i = 0; i < message.OBSOLETE_FIELD.length; ++i)
                    $root.Authentication.TwoFactorChannel.encode(message.OBSOLETE_FIELD[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.ssoUserInfo != null && Object.hasOwnProperty.call(message, "ssoUserInfo"))
                $root.Authentication.SsoUserInfo.encode(message.ssoUserInfo, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes a PreLoginResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.PreLoginResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.PreLoginResponse} PreLoginResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PreLoginResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.PreLoginResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.deviceStatus = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.salt && message.salt.length))
                            message.salt = [];
                        message.salt.push($root.Authentication.Salt.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 3: {
                        if (!(message.OBSOLETE_FIELD && message.OBSOLETE_FIELD.length))
                            message.OBSOLETE_FIELD = [];
                        message.OBSOLETE_FIELD.push($root.Authentication.TwoFactorChannel.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 4: {
                        message.ssoUserInfo = $root.Authentication.SsoUserInfo.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a PreLoginResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.PreLoginResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.PreLoginResponse} PreLoginResponse
         */
        PreLoginResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.PreLoginResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.PreLoginResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.PreLoginResponse();
            switch (object.deviceStatus) {
            default:
                if (typeof object.deviceStatus === "number") {
                    message.deviceStatus = object.deviceStatus;
                    break;
                }
                break;
            case "DEVICE_NEEDS_APPROVAL":
            case 0:
                message.deviceStatus = 0;
                break;
            case "DEVICE_OK":
            case 1:
                message.deviceStatus = 1;
                break;
            case "DEVICE_DISABLED_BY_USER":
            case 2:
                message.deviceStatus = 2;
                break;
            case "DEVICE_LOCKED_BY_ADMIN":
            case 3:
                message.deviceStatus = 3;
                break;
            }
            if (object.salt) {
                if (!Array.isArray(object.salt))
                    throw TypeError(".Authentication.PreLoginResponse.salt: array expected");
                message.salt = [];
                for (let i = 0; i < object.salt.length; ++i) {
                    if (!$util.isObject(object.salt[i]))
                        throw TypeError(".Authentication.PreLoginResponse.salt: object expected");
                    message.salt[i] = $root.Authentication.Salt.fromObject(object.salt[i], long + 1);
                }
            }
            if (object.OBSOLETE_FIELD) {
                if (!Array.isArray(object.OBSOLETE_FIELD))
                    throw TypeError(".Authentication.PreLoginResponse.OBSOLETE_FIELD: array expected");
                message.OBSOLETE_FIELD = [];
                for (let i = 0; i < object.OBSOLETE_FIELD.length; ++i) {
                    if (!$util.isObject(object.OBSOLETE_FIELD[i]))
                        throw TypeError(".Authentication.PreLoginResponse.OBSOLETE_FIELD: object expected");
                    message.OBSOLETE_FIELD[i] = $root.Authentication.TwoFactorChannel.fromObject(object.OBSOLETE_FIELD[i], long + 1);
                }
            }
            if (object.ssoUserInfo != null) {
                if (!$util.isObject(object.ssoUserInfo))
                    throw TypeError(".Authentication.PreLoginResponse.ssoUserInfo: object expected");
                message.ssoUserInfo = $root.Authentication.SsoUserInfo.fromObject(object.ssoUserInfo, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a PreLoginResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.PreLoginResponse
         * @static
         * @param {Authentication.PreLoginResponse} message PreLoginResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PreLoginResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.salt = [];
                object.OBSOLETE_FIELD = [];
            }
            if (options.defaults) {
                object.deviceStatus = options.enums === String ? "DEVICE_NEEDS_APPROVAL" : 0;
                object.ssoUserInfo = null;
            }
            if (message.deviceStatus != null && Object.hasOwnProperty.call(message, "deviceStatus"))
                object.deviceStatus = options.enums === String ? $root.Authentication.DeviceStatus[message.deviceStatus] === undefined ? message.deviceStatus : $root.Authentication.DeviceStatus[message.deviceStatus] : message.deviceStatus;
            if (message.salt && message.salt.length) {
                object.salt = [];
                for (let j = 0; j < message.salt.length; ++j)
                    object.salt[j] = $root.Authentication.Salt.toObject(message.salt[j], options, q + 1);
            }
            if (message.OBSOLETE_FIELD && message.OBSOLETE_FIELD.length) {
                object.OBSOLETE_FIELD = [];
                for (let j = 0; j < message.OBSOLETE_FIELD.length; ++j)
                    object.OBSOLETE_FIELD[j] = $root.Authentication.TwoFactorChannel.toObject(message.OBSOLETE_FIELD[j], options, q + 1);
            }
            if (message.ssoUserInfo != null && Object.hasOwnProperty.call(message, "ssoUserInfo"))
                object.ssoUserInfo = $root.Authentication.SsoUserInfo.toObject(message.ssoUserInfo, options, q + 1);
            return object;
        };

        /**
         * Converts this PreLoginResponse to JSON.
         * @function toJSON
         * @memberof Authentication.PreLoginResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PreLoginResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PreLoginResponse
         * @function getTypeUrl
         * @memberof Authentication.PreLoginResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PreLoginResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.PreLoginResponse";
        };

        return PreLoginResponse;
    })();

    Authentication.LoginAsUserRequest = (function() {

        /**
         * Properties of a LoginAsUserRequest.
         * @memberof Authentication
         * @interface ILoginAsUserRequest
         * @property {string|null} [username] LoginAsUserRequest username
         */

        /**
         * Constructs a new LoginAsUserRequest.
         * @memberof Authentication
         * @classdesc Represents a LoginAsUserRequest.
         * @implements ILoginAsUserRequest
         * @constructor
         * @param {Authentication.ILoginAsUserRequest=} [properties] Properties to set
         */
        function LoginAsUserRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LoginAsUserRequest username.
         * @member {string} username
         * @memberof Authentication.LoginAsUserRequest
         * @instance
         */
        LoginAsUserRequest.prototype.username = "";

        /**
         * Creates a new LoginAsUserRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.LoginAsUserRequest
         * @static
         * @param {Authentication.ILoginAsUserRequest=} [properties] Properties to set
         * @returns {Authentication.LoginAsUserRequest} LoginAsUserRequest instance
         */
        LoginAsUserRequest.create = function create(properties) {
            return new LoginAsUserRequest(properties);
        };

        /**
         * Encodes the specified LoginAsUserRequest message. Does not implicitly {@link Authentication.LoginAsUserRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.LoginAsUserRequest
         * @static
         * @param {Authentication.ILoginAsUserRequest} message LoginAsUserRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LoginAsUserRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.username);
            return writer;
        };

        /**
         * Decodes a LoginAsUserRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.LoginAsUserRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.LoginAsUserRequest} LoginAsUserRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LoginAsUserRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.LoginAsUserRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.username = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a LoginAsUserRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.LoginAsUserRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.LoginAsUserRequest} LoginAsUserRequest
         */
        LoginAsUserRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.LoginAsUserRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.LoginAsUserRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.LoginAsUserRequest();
            if (object.username != null)
                message.username = String(object.username);
            return message;
        };

        /**
         * Creates a plain object from a LoginAsUserRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.LoginAsUserRequest
         * @static
         * @param {Authentication.LoginAsUserRequest} message LoginAsUserRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LoginAsUserRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.username = "";
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            return object;
        };

        /**
         * Converts this LoginAsUserRequest to JSON.
         * @function toJSON
         * @memberof Authentication.LoginAsUserRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LoginAsUserRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LoginAsUserRequest
         * @function getTypeUrl
         * @memberof Authentication.LoginAsUserRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LoginAsUserRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.LoginAsUserRequest";
        };

        return LoginAsUserRequest;
    })();

    Authentication.LoginAsUserResponse = (function() {

        /**
         * Properties of a LoginAsUserResponse.
         * @memberof Authentication
         * @interface ILoginAsUserResponse
         * @property {Uint8Array|null} [encryptedSessionToken] LoginAsUserResponse encryptedSessionToken
         * @property {Uint8Array|null} [encryptedSharedAccountKey] LoginAsUserResponse encryptedSharedAccountKey
         */

        /**
         * Constructs a new LoginAsUserResponse.
         * @memberof Authentication
         * @classdesc Represents a LoginAsUserResponse.
         * @implements ILoginAsUserResponse
         * @constructor
         * @param {Authentication.ILoginAsUserResponse=} [properties] Properties to set
         */
        function LoginAsUserResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LoginAsUserResponse encryptedSessionToken.
         * @member {Uint8Array} encryptedSessionToken
         * @memberof Authentication.LoginAsUserResponse
         * @instance
         */
        LoginAsUserResponse.prototype.encryptedSessionToken = $util.newBuffer([]);

        /**
         * LoginAsUserResponse encryptedSharedAccountKey.
         * @member {Uint8Array} encryptedSharedAccountKey
         * @memberof Authentication.LoginAsUserResponse
         * @instance
         */
        LoginAsUserResponse.prototype.encryptedSharedAccountKey = $util.newBuffer([]);

        /**
         * Creates a new LoginAsUserResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.LoginAsUserResponse
         * @static
         * @param {Authentication.ILoginAsUserResponse=} [properties] Properties to set
         * @returns {Authentication.LoginAsUserResponse} LoginAsUserResponse instance
         */
        LoginAsUserResponse.create = function create(properties) {
            return new LoginAsUserResponse(properties);
        };

        /**
         * Encodes the specified LoginAsUserResponse message. Does not implicitly {@link Authentication.LoginAsUserResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.LoginAsUserResponse
         * @static
         * @param {Authentication.ILoginAsUserResponse} message LoginAsUserResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LoginAsUserResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedSessionToken != null && Object.hasOwnProperty.call(message, "encryptedSessionToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedSessionToken);
            if (message.encryptedSharedAccountKey != null && Object.hasOwnProperty.call(message, "encryptedSharedAccountKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encryptedSharedAccountKey);
            return writer;
        };

        /**
         * Decodes a LoginAsUserResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.LoginAsUserResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.LoginAsUserResponse} LoginAsUserResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LoginAsUserResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.LoginAsUserResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedSessionToken = reader.bytes();
                        break;
                    }
                case 2: {
                        message.encryptedSharedAccountKey = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a LoginAsUserResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.LoginAsUserResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.LoginAsUserResponse} LoginAsUserResponse
         */
        LoginAsUserResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.LoginAsUserResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.LoginAsUserResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.LoginAsUserResponse();
            if (object.encryptedSessionToken != null)
                if (typeof object.encryptedSessionToken === "string")
                    $util.base64.decode(object.encryptedSessionToken, message.encryptedSessionToken = $util.newBuffer($util.base64.length(object.encryptedSessionToken)), 0);
                else if (object.encryptedSessionToken.length >= 0)
                    message.encryptedSessionToken = object.encryptedSessionToken;
            if (object.encryptedSharedAccountKey != null)
                if (typeof object.encryptedSharedAccountKey === "string")
                    $util.base64.decode(object.encryptedSharedAccountKey, message.encryptedSharedAccountKey = $util.newBuffer($util.base64.length(object.encryptedSharedAccountKey)), 0);
                else if (object.encryptedSharedAccountKey.length >= 0)
                    message.encryptedSharedAccountKey = object.encryptedSharedAccountKey;
            return message;
        };

        /**
         * Creates a plain object from a LoginAsUserResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.LoginAsUserResponse
         * @static
         * @param {Authentication.LoginAsUserResponse} message LoginAsUserResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LoginAsUserResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.encryptedSessionToken = "";
                else {
                    object.encryptedSessionToken = [];
                    if (options.bytes !== Array)
                        object.encryptedSessionToken = $util.newBuffer(object.encryptedSessionToken);
                }
                if (options.bytes === String)
                    object.encryptedSharedAccountKey = "";
                else {
                    object.encryptedSharedAccountKey = [];
                    if (options.bytes !== Array)
                        object.encryptedSharedAccountKey = $util.newBuffer(object.encryptedSharedAccountKey);
                }
            }
            if (message.encryptedSessionToken != null && Object.hasOwnProperty.call(message, "encryptedSessionToken"))
                object.encryptedSessionToken = options.bytes === String ? $util.base64.encode(message.encryptedSessionToken, 0, message.encryptedSessionToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedSessionToken) : message.encryptedSessionToken;
            if (message.encryptedSharedAccountKey != null && Object.hasOwnProperty.call(message, "encryptedSharedAccountKey"))
                object.encryptedSharedAccountKey = options.bytes === String ? $util.base64.encode(message.encryptedSharedAccountKey, 0, message.encryptedSharedAccountKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedSharedAccountKey) : message.encryptedSharedAccountKey;
            return object;
        };

        /**
         * Converts this LoginAsUserResponse to JSON.
         * @function toJSON
         * @memberof Authentication.LoginAsUserResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LoginAsUserResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LoginAsUserResponse
         * @function getTypeUrl
         * @memberof Authentication.LoginAsUserResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LoginAsUserResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.LoginAsUserResponse";
        };

        return LoginAsUserResponse;
    })();

    /**
     * PasswordMethod enum.
     * @name Authentication.PasswordMethod
     * @enum {number}
     * @property {number} ENTERED=0 ENTERED value
     * @property {number} BIOMETRICS=1 BIOMETRICS value
     */
    Authentication.PasswordMethod = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "ENTERED"] = 0;
        values[valuesById[1] = "BIOMETRICS"] = 1;
        return values;
    })();

    Authentication.ValidateAuthHashRequest = (function() {

        /**
         * Properties of a ValidateAuthHashRequest.
         * @memberof Authentication
         * @interface IValidateAuthHashRequest
         * @property {Authentication.PasswordMethod|null} [passwordMethod] ValidateAuthHashRequest passwordMethod
         * @property {Uint8Array|null} [authResponse] ValidateAuthHashRequest authResponse
         * @property {Uint8Array|null} [encryptedLoginToken] ValidateAuthHashRequest encryptedLoginToken
         */

        /**
         * Constructs a new ValidateAuthHashRequest.
         * @memberof Authentication
         * @classdesc Represents a ValidateAuthHashRequest.
         * @implements IValidateAuthHashRequest
         * @constructor
         * @param {Authentication.IValidateAuthHashRequest=} [properties] Properties to set
         */
        function ValidateAuthHashRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ValidateAuthHashRequest passwordMethod.
         * @member {Authentication.PasswordMethod} passwordMethod
         * @memberof Authentication.ValidateAuthHashRequest
         * @instance
         */
        ValidateAuthHashRequest.prototype.passwordMethod = 0;

        /**
         * ValidateAuthHashRequest authResponse.
         * @member {Uint8Array} authResponse
         * @memberof Authentication.ValidateAuthHashRequest
         * @instance
         */
        ValidateAuthHashRequest.prototype.authResponse = $util.newBuffer([]);

        /**
         * ValidateAuthHashRequest encryptedLoginToken.
         * @member {Uint8Array} encryptedLoginToken
         * @memberof Authentication.ValidateAuthHashRequest
         * @instance
         */
        ValidateAuthHashRequest.prototype.encryptedLoginToken = $util.newBuffer([]);

        /**
         * Creates a new ValidateAuthHashRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.ValidateAuthHashRequest
         * @static
         * @param {Authentication.IValidateAuthHashRequest=} [properties] Properties to set
         * @returns {Authentication.ValidateAuthHashRequest} ValidateAuthHashRequest instance
         */
        ValidateAuthHashRequest.create = function create(properties) {
            return new ValidateAuthHashRequest(properties);
        };

        /**
         * Encodes the specified ValidateAuthHashRequest message. Does not implicitly {@link Authentication.ValidateAuthHashRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ValidateAuthHashRequest
         * @static
         * @param {Authentication.IValidateAuthHashRequest} message ValidateAuthHashRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ValidateAuthHashRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.passwordMethod != null && Object.hasOwnProperty.call(message, "passwordMethod"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.passwordMethod);
            if (message.authResponse != null && Object.hasOwnProperty.call(message, "authResponse"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.authResponse);
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.encryptedLoginToken);
            return writer;
        };

        /**
         * Decodes a ValidateAuthHashRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ValidateAuthHashRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ValidateAuthHashRequest} ValidateAuthHashRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ValidateAuthHashRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ValidateAuthHashRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.passwordMethod = reader.int32();
                        break;
                    }
                case 2: {
                        message.authResponse = reader.bytes();
                        break;
                    }
                case 3: {
                        message.encryptedLoginToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a ValidateAuthHashRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ValidateAuthHashRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ValidateAuthHashRequest} ValidateAuthHashRequest
         */
        ValidateAuthHashRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ValidateAuthHashRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ValidateAuthHashRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ValidateAuthHashRequest();
            switch (object.passwordMethod) {
            default:
                if (typeof object.passwordMethod === "number") {
                    message.passwordMethod = object.passwordMethod;
                    break;
                }
                break;
            case "ENTERED":
            case 0:
                message.passwordMethod = 0;
                break;
            case "BIOMETRICS":
            case 1:
                message.passwordMethod = 1;
                break;
            }
            if (object.authResponse != null)
                if (typeof object.authResponse === "string")
                    $util.base64.decode(object.authResponse, message.authResponse = $util.newBuffer($util.base64.length(object.authResponse)), 0);
                else if (object.authResponse.length >= 0)
                    message.authResponse = object.authResponse;
            if (object.encryptedLoginToken != null)
                if (typeof object.encryptedLoginToken === "string")
                    $util.base64.decode(object.encryptedLoginToken, message.encryptedLoginToken = $util.newBuffer($util.base64.length(object.encryptedLoginToken)), 0);
                else if (object.encryptedLoginToken.length >= 0)
                    message.encryptedLoginToken = object.encryptedLoginToken;
            return message;
        };

        /**
         * Creates a plain object from a ValidateAuthHashRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ValidateAuthHashRequest
         * @static
         * @param {Authentication.ValidateAuthHashRequest} message ValidateAuthHashRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ValidateAuthHashRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.passwordMethod = options.enums === String ? "ENTERED" : 0;
                if (options.bytes === String)
                    object.authResponse = "";
                else {
                    object.authResponse = [];
                    if (options.bytes !== Array)
                        object.authResponse = $util.newBuffer(object.authResponse);
                }
                if (options.bytes === String)
                    object.encryptedLoginToken = "";
                else {
                    object.encryptedLoginToken = [];
                    if (options.bytes !== Array)
                        object.encryptedLoginToken = $util.newBuffer(object.encryptedLoginToken);
                }
            }
            if (message.passwordMethod != null && Object.hasOwnProperty.call(message, "passwordMethod"))
                object.passwordMethod = options.enums === String ? $root.Authentication.PasswordMethod[message.passwordMethod] === undefined ? message.passwordMethod : $root.Authentication.PasswordMethod[message.passwordMethod] : message.passwordMethod;
            if (message.authResponse != null && Object.hasOwnProperty.call(message, "authResponse"))
                object.authResponse = options.bytes === String ? $util.base64.encode(message.authResponse, 0, message.authResponse.length) : options.bytes === Array ? Array.prototype.slice.call(message.authResponse) : message.authResponse;
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                object.encryptedLoginToken = options.bytes === String ? $util.base64.encode(message.encryptedLoginToken, 0, message.encryptedLoginToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedLoginToken) : message.encryptedLoginToken;
            return object;
        };

        /**
         * Converts this ValidateAuthHashRequest to JSON.
         * @function toJSON
         * @memberof Authentication.ValidateAuthHashRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ValidateAuthHashRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ValidateAuthHashRequest
         * @function getTypeUrl
         * @memberof Authentication.ValidateAuthHashRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ValidateAuthHashRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ValidateAuthHashRequest";
        };

        return ValidateAuthHashRequest;
    })();

    /**
     * TwoFactorPushType enum.
     * @name Authentication.TwoFactorPushType
     * @enum {number}
     * @property {number} TWO_FA_PUSH_NONE=0 TWO_FA_PUSH_NONE value
     * @property {number} TWO_FA_PUSH_SMS=1 TWO_FA_PUSH_SMS value
     * @property {number} TWO_FA_PUSH_KEEPER=2 TWO_FA_PUSH_KEEPER value
     * @property {number} TWO_FA_PUSH_DUO_PUSH=3 TWO_FA_PUSH_DUO_PUSH value
     * @property {number} TWO_FA_PUSH_DUO_TEXT=4 TWO_FA_PUSH_DUO_TEXT value
     * @property {number} TWO_FA_PUSH_DUO_CALL=5 TWO_FA_PUSH_DUO_CALL value
     * @property {number} TWO_FA_PUSH_DNA=6 TWO_FA_PUSH_DNA value
     */
    Authentication.TwoFactorPushType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "TWO_FA_PUSH_NONE"] = 0;
        values[valuesById[1] = "TWO_FA_PUSH_SMS"] = 1;
        values[valuesById[2] = "TWO_FA_PUSH_KEEPER"] = 2;
        values[valuesById[3] = "TWO_FA_PUSH_DUO_PUSH"] = 3;
        values[valuesById[4] = "TWO_FA_PUSH_DUO_TEXT"] = 4;
        values[valuesById[5] = "TWO_FA_PUSH_DUO_CALL"] = 5;
        values[valuesById[6] = "TWO_FA_PUSH_DNA"] = 6;
        return values;
    })();

    /**
     * TwoFactorValueType enum.
     * @name Authentication.TwoFactorValueType
     * @enum {number}
     * @property {number} TWO_FA_CODE_NONE=0 TWO_FA_CODE_NONE value
     * @property {number} TWO_FA_CODE_TOTP=1 TWO_FA_CODE_TOTP value
     * @property {number} TWO_FA_CODE_SMS=2 TWO_FA_CODE_SMS value
     * @property {number} TWO_FA_CODE_DUO=3 TWO_FA_CODE_DUO value
     * @property {number} TWO_FA_CODE_RSA=4 TWO_FA_CODE_RSA value
     * @property {number} TWO_FA_RESP_U2F=5 TWO_FA_RESP_U2F value
     * @property {number} TWO_FA_RESP_WEBAUTHN=6 TWO_FA_RESP_WEBAUTHN value
     * @property {number} TWO_FA_CODE_DNA=7 TWO_FA_CODE_DNA value
     */
    Authentication.TwoFactorValueType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "TWO_FA_CODE_NONE"] = 0;
        values[valuesById[1] = "TWO_FA_CODE_TOTP"] = 1;
        values[valuesById[2] = "TWO_FA_CODE_SMS"] = 2;
        values[valuesById[3] = "TWO_FA_CODE_DUO"] = 3;
        values[valuesById[4] = "TWO_FA_CODE_RSA"] = 4;
        values[valuesById[5] = "TWO_FA_RESP_U2F"] = 5;
        values[valuesById[6] = "TWO_FA_RESP_WEBAUTHN"] = 6;
        values[valuesById[7] = "TWO_FA_CODE_DNA"] = 7;
        return values;
    })();

    /**
     * TwoFactorChannelType enum.
     * @name Authentication.TwoFactorChannelType
     * @enum {number}
     * @property {number} TWO_FA_CT_NONE=0 TWO_FA_CT_NONE value
     * @property {number} TWO_FA_CT_TOTP=1 TWO_FA_CT_TOTP value
     * @property {number} TWO_FA_CT_SMS=2 TWO_FA_CT_SMS value
     * @property {number} TWO_FA_CT_DUO=3 TWO_FA_CT_DUO value
     * @property {number} TWO_FA_CT_RSA=4 TWO_FA_CT_RSA value
     * @property {number} TWO_FA_CT_BACKUP=5 TWO_FA_CT_BACKUP value
     * @property {number} TWO_FA_CT_U2F=6 TWO_FA_CT_U2F value
     * @property {number} TWO_FA_CT_WEBAUTHN=7 TWO_FA_CT_WEBAUTHN value
     * @property {number} TWO_FA_CT_KEEPER=8 TWO_FA_CT_KEEPER value
     * @property {number} TWO_FA_CT_DNA=9 TWO_FA_CT_DNA value
     */
    Authentication.TwoFactorChannelType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "TWO_FA_CT_NONE"] = 0;
        values[valuesById[1] = "TWO_FA_CT_TOTP"] = 1;
        values[valuesById[2] = "TWO_FA_CT_SMS"] = 2;
        values[valuesById[3] = "TWO_FA_CT_DUO"] = 3;
        values[valuesById[4] = "TWO_FA_CT_RSA"] = 4;
        values[valuesById[5] = "TWO_FA_CT_BACKUP"] = 5;
        values[valuesById[6] = "TWO_FA_CT_U2F"] = 6;
        values[valuesById[7] = "TWO_FA_CT_WEBAUTHN"] = 7;
        values[valuesById[8] = "TWO_FA_CT_KEEPER"] = 8;
        values[valuesById[9] = "TWO_FA_CT_DNA"] = 9;
        return values;
    })();

    Authentication.TwoFactorChannelInfo = (function() {

        /**
         * Properties of a TwoFactorChannelInfo.
         * @memberof Authentication
         * @interface ITwoFactorChannelInfo
         * @property {Authentication.TwoFactorChannelType|null} [channelType] TwoFactorChannelInfo channelType
         * @property {Uint8Array|null} [channelUid] TwoFactorChannelInfo channelUid
         * @property {string|null} [channelName] TwoFactorChannelInfo channelName
         * @property {string|null} [challenge] TwoFactorChannelInfo challenge
         * @property {Array.<string>|null} [capabilities] TwoFactorChannelInfo capabilities
         * @property {string|null} [phoneNumber] TwoFactorChannelInfo phoneNumber
         * @property {Authentication.TwoFactorExpiration|null} [maxExpiration] TwoFactorChannelInfo maxExpiration
         * @property {number|null} [createdOn] TwoFactorChannelInfo createdOn
         * @property {Authentication.TwoFactorExpiration|null} [lastFrequency] TwoFactorChannelInfo lastFrequency
         */

        /**
         * Constructs a new TwoFactorChannelInfo.
         * @memberof Authentication
         * @classdesc Represents a TwoFactorChannelInfo.
         * @implements ITwoFactorChannelInfo
         * @constructor
         * @param {Authentication.ITwoFactorChannelInfo=} [properties] Properties to set
         */
        function TwoFactorChannelInfo(properties) {
            this.capabilities = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TwoFactorChannelInfo channelType.
         * @member {Authentication.TwoFactorChannelType} channelType
         * @memberof Authentication.TwoFactorChannelInfo
         * @instance
         */
        TwoFactorChannelInfo.prototype.channelType = 0;

        /**
         * TwoFactorChannelInfo channelUid.
         * @member {Uint8Array} channelUid
         * @memberof Authentication.TwoFactorChannelInfo
         * @instance
         */
        TwoFactorChannelInfo.prototype.channelUid = $util.newBuffer([]);

        /**
         * TwoFactorChannelInfo channelName.
         * @member {string} channelName
         * @memberof Authentication.TwoFactorChannelInfo
         * @instance
         */
        TwoFactorChannelInfo.prototype.channelName = "";

        /**
         * TwoFactorChannelInfo challenge.
         * @member {string} challenge
         * @memberof Authentication.TwoFactorChannelInfo
         * @instance
         */
        TwoFactorChannelInfo.prototype.challenge = "";

        /**
         * TwoFactorChannelInfo capabilities.
         * @member {Array.<string>} capabilities
         * @memberof Authentication.TwoFactorChannelInfo
         * @instance
         */
        TwoFactorChannelInfo.prototype.capabilities = $util.emptyArray;

        /**
         * TwoFactorChannelInfo phoneNumber.
         * @member {string} phoneNumber
         * @memberof Authentication.TwoFactorChannelInfo
         * @instance
         */
        TwoFactorChannelInfo.prototype.phoneNumber = "";

        /**
         * TwoFactorChannelInfo maxExpiration.
         * @member {Authentication.TwoFactorExpiration} maxExpiration
         * @memberof Authentication.TwoFactorChannelInfo
         * @instance
         */
        TwoFactorChannelInfo.prototype.maxExpiration = 0;

        /**
         * TwoFactorChannelInfo createdOn.
         * @member {number} createdOn
         * @memberof Authentication.TwoFactorChannelInfo
         * @instance
         */
        TwoFactorChannelInfo.prototype.createdOn = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * TwoFactorChannelInfo lastFrequency.
         * @member {Authentication.TwoFactorExpiration} lastFrequency
         * @memberof Authentication.TwoFactorChannelInfo
         * @instance
         */
        TwoFactorChannelInfo.prototype.lastFrequency = 0;

        /**
         * Creates a new TwoFactorChannelInfo instance using the specified properties.
         * @function create
         * @memberof Authentication.TwoFactorChannelInfo
         * @static
         * @param {Authentication.ITwoFactorChannelInfo=} [properties] Properties to set
         * @returns {Authentication.TwoFactorChannelInfo} TwoFactorChannelInfo instance
         */
        TwoFactorChannelInfo.create = function create(properties) {
            return new TwoFactorChannelInfo(properties);
        };

        /**
         * Encodes the specified TwoFactorChannelInfo message. Does not implicitly {@link Authentication.TwoFactorChannelInfo.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TwoFactorChannelInfo
         * @static
         * @param {Authentication.ITwoFactorChannelInfo} message TwoFactorChannelInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TwoFactorChannelInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.channelType != null && Object.hasOwnProperty.call(message, "channelType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.channelType);
            if (message.channelUid != null && Object.hasOwnProperty.call(message, "channelUid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.channelUid);
            if (message.channelName != null && Object.hasOwnProperty.call(message, "channelName"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.channelName);
            if (message.challenge != null && Object.hasOwnProperty.call(message, "challenge"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.challenge);
            if (message.capabilities != null && message.capabilities.length)
                for (let i = 0; i < message.capabilities.length; ++i)
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.capabilities[i]);
            if (message.phoneNumber != null && Object.hasOwnProperty.call(message, "phoneNumber"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.phoneNumber);
            if (message.maxExpiration != null && Object.hasOwnProperty.call(message, "maxExpiration"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.maxExpiration);
            if (message.createdOn != null && Object.hasOwnProperty.call(message, "createdOn"))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.createdOn);
            if (message.lastFrequency != null && Object.hasOwnProperty.call(message, "lastFrequency"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.lastFrequency);
            return writer;
        };

        /**
         * Decodes a TwoFactorChannelInfo message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TwoFactorChannelInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TwoFactorChannelInfo} TwoFactorChannelInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TwoFactorChannelInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TwoFactorChannelInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.channelType = reader.int32();
                        break;
                    }
                case 2: {
                        message.channelUid = reader.bytes();
                        break;
                    }
                case 3: {
                        message.channelName = reader.string();
                        break;
                    }
                case 4: {
                        message.challenge = reader.string();
                        break;
                    }
                case 5: {
                        if (!(message.capabilities && message.capabilities.length))
                            message.capabilities = [];
                        message.capabilities.push(reader.string());
                        break;
                    }
                case 6: {
                        message.phoneNumber = reader.string();
                        break;
                    }
                case 7: {
                        message.maxExpiration = reader.int32();
                        break;
                    }
                case 8: {
                        message.createdOn = reader.int64();
                        break;
                    }
                case 9: {
                        message.lastFrequency = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TwoFactorChannelInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TwoFactorChannelInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TwoFactorChannelInfo} TwoFactorChannelInfo
         */
        TwoFactorChannelInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TwoFactorChannelInfo)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TwoFactorChannelInfo: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TwoFactorChannelInfo();
            switch (object.channelType) {
            default:
                if (typeof object.channelType === "number") {
                    message.channelType = object.channelType;
                    break;
                }
                break;
            case "TWO_FA_CT_NONE":
            case 0:
                message.channelType = 0;
                break;
            case "TWO_FA_CT_TOTP":
            case 1:
                message.channelType = 1;
                break;
            case "TWO_FA_CT_SMS":
            case 2:
                message.channelType = 2;
                break;
            case "TWO_FA_CT_DUO":
            case 3:
                message.channelType = 3;
                break;
            case "TWO_FA_CT_RSA":
            case 4:
                message.channelType = 4;
                break;
            case "TWO_FA_CT_BACKUP":
            case 5:
                message.channelType = 5;
                break;
            case "TWO_FA_CT_U2F":
            case 6:
                message.channelType = 6;
                break;
            case "TWO_FA_CT_WEBAUTHN":
            case 7:
                message.channelType = 7;
                break;
            case "TWO_FA_CT_KEEPER":
            case 8:
                message.channelType = 8;
                break;
            case "TWO_FA_CT_DNA":
            case 9:
                message.channelType = 9;
                break;
            }
            if (object.channelUid != null)
                if (typeof object.channelUid === "string")
                    $util.base64.decode(object.channelUid, message.channelUid = $util.newBuffer($util.base64.length(object.channelUid)), 0);
                else if (object.channelUid.length >= 0)
                    message.channelUid = object.channelUid;
            if (object.channelName != null)
                message.channelName = String(object.channelName);
            if (object.challenge != null)
                message.challenge = String(object.challenge);
            if (object.capabilities) {
                if (!Array.isArray(object.capabilities))
                    throw TypeError(".Authentication.TwoFactorChannelInfo.capabilities: array expected");
                message.capabilities = [];
                for (let i = 0; i < object.capabilities.length; ++i)
                    message.capabilities[i] = String(object.capabilities[i]);
            }
            if (object.phoneNumber != null)
                message.phoneNumber = String(object.phoneNumber);
            switch (object.maxExpiration) {
            default:
                if (typeof object.maxExpiration === "number") {
                    message.maxExpiration = object.maxExpiration;
                    break;
                }
                break;
            case "TWO_FA_EXP_IMMEDIATELY":
            case 0:
                message.maxExpiration = 0;
                break;
            case "TWO_FA_EXP_5_MINUTES":
            case 1:
                message.maxExpiration = 1;
                break;
            case "TWO_FA_EXP_12_HOURS":
            case 2:
                message.maxExpiration = 2;
                break;
            case "TWO_FA_EXP_24_HOURS":
            case 3:
                message.maxExpiration = 3;
                break;
            case "TWO_FA_EXP_30_DAYS":
            case 4:
                message.maxExpiration = 4;
                break;
            case "TWO_FA_EXP_NEVER":
            case 5:
                message.maxExpiration = 5;
                break;
            }
            if (object.createdOn != null)
                if ($util.Long)
                    message.createdOn = $util.Long.fromValue(object.createdOn, false);
                else if (typeof object.createdOn === "string")
                    message.createdOn = parseInt(object.createdOn, 10);
                else if (typeof object.createdOn === "number")
                    message.createdOn = object.createdOn;
                else if (typeof object.createdOn === "object")
                    message.createdOn = new $util.LongBits(object.createdOn.low >>> 0, object.createdOn.high >>> 0).toNumber();
            switch (object.lastFrequency) {
            default:
                if (typeof object.lastFrequency === "number") {
                    message.lastFrequency = object.lastFrequency;
                    break;
                }
                break;
            case "TWO_FA_EXP_IMMEDIATELY":
            case 0:
                message.lastFrequency = 0;
                break;
            case "TWO_FA_EXP_5_MINUTES":
            case 1:
                message.lastFrequency = 1;
                break;
            case "TWO_FA_EXP_12_HOURS":
            case 2:
                message.lastFrequency = 2;
                break;
            case "TWO_FA_EXP_24_HOURS":
            case 3:
                message.lastFrequency = 3;
                break;
            case "TWO_FA_EXP_30_DAYS":
            case 4:
                message.lastFrequency = 4;
                break;
            case "TWO_FA_EXP_NEVER":
            case 5:
                message.lastFrequency = 5;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a TwoFactorChannelInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TwoFactorChannelInfo
         * @static
         * @param {Authentication.TwoFactorChannelInfo} message TwoFactorChannelInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TwoFactorChannelInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.capabilities = [];
            if (options.defaults) {
                object.channelType = options.enums === String ? "TWO_FA_CT_NONE" : 0;
                if (options.bytes === String)
                    object.channelUid = "";
                else {
                    object.channelUid = [];
                    if (options.bytes !== Array)
                        object.channelUid = $util.newBuffer(object.channelUid);
                }
                object.channelName = "";
                object.challenge = "";
                object.phoneNumber = "";
                object.maxExpiration = options.enums === String ? "TWO_FA_EXP_IMMEDIATELY" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.createdOn = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.createdOn = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.lastFrequency = options.enums === String ? "TWO_FA_EXP_IMMEDIATELY" : 0;
            }
            if (message.channelType != null && Object.hasOwnProperty.call(message, "channelType"))
                object.channelType = options.enums === String ? $root.Authentication.TwoFactorChannelType[message.channelType] === undefined ? message.channelType : $root.Authentication.TwoFactorChannelType[message.channelType] : message.channelType;
            if (message.channelUid != null && Object.hasOwnProperty.call(message, "channelUid"))
                object.channelUid = options.bytes === String ? $util.base64.encode(message.channelUid, 0, message.channelUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.channelUid) : message.channelUid;
            if (message.channelName != null && Object.hasOwnProperty.call(message, "channelName"))
                object.channelName = message.channelName;
            if (message.challenge != null && Object.hasOwnProperty.call(message, "challenge"))
                object.challenge = message.challenge;
            if (message.capabilities && message.capabilities.length) {
                object.capabilities = [];
                for (let j = 0; j < message.capabilities.length; ++j)
                    object.capabilities[j] = message.capabilities[j];
            }
            if (message.phoneNumber != null && Object.hasOwnProperty.call(message, "phoneNumber"))
                object.phoneNumber = message.phoneNumber;
            if (message.maxExpiration != null && Object.hasOwnProperty.call(message, "maxExpiration"))
                object.maxExpiration = options.enums === String ? $root.Authentication.TwoFactorExpiration[message.maxExpiration] === undefined ? message.maxExpiration : $root.Authentication.TwoFactorExpiration[message.maxExpiration] : message.maxExpiration;
            if (message.createdOn != null && Object.hasOwnProperty.call(message, "createdOn"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.createdOn = typeof message.createdOn === "number" ? BigInt(message.createdOn) : $util.Long.fromBits(message.createdOn.low >>> 0, message.createdOn.high >>> 0, false).toBigInt();
                else if (typeof message.createdOn === "number")
                    object.createdOn = options.longs === String ? String(message.createdOn) : message.createdOn;
                else
                    object.createdOn = options.longs === String ? $util.Long.prototype.toString.call(message.createdOn) : options.longs === Number ? new $util.LongBits(message.createdOn.low >>> 0, message.createdOn.high >>> 0).toNumber() : message.createdOn;
            if (message.lastFrequency != null && Object.hasOwnProperty.call(message, "lastFrequency"))
                object.lastFrequency = options.enums === String ? $root.Authentication.TwoFactorExpiration[message.lastFrequency] === undefined ? message.lastFrequency : $root.Authentication.TwoFactorExpiration[message.lastFrequency] : message.lastFrequency;
            return object;
        };

        /**
         * Converts this TwoFactorChannelInfo to JSON.
         * @function toJSON
         * @memberof Authentication.TwoFactorChannelInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TwoFactorChannelInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TwoFactorChannelInfo
         * @function getTypeUrl
         * @memberof Authentication.TwoFactorChannelInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TwoFactorChannelInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TwoFactorChannelInfo";
        };

        return TwoFactorChannelInfo;
    })();

    /**
     * TwoFactorExpiration enum.
     * @name Authentication.TwoFactorExpiration
     * @enum {number}
     * @property {number} TWO_FA_EXP_IMMEDIATELY=0 TWO_FA_EXP_IMMEDIATELY value
     * @property {number} TWO_FA_EXP_5_MINUTES=1 TWO_FA_EXP_5_MINUTES value
     * @property {number} TWO_FA_EXP_12_HOURS=2 TWO_FA_EXP_12_HOURS value
     * @property {number} TWO_FA_EXP_24_HOURS=3 TWO_FA_EXP_24_HOURS value
     * @property {number} TWO_FA_EXP_30_DAYS=4 TWO_FA_EXP_30_DAYS value
     * @property {number} TWO_FA_EXP_NEVER=5 TWO_FA_EXP_NEVER value
     */
    Authentication.TwoFactorExpiration = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "TWO_FA_EXP_IMMEDIATELY"] = 0;
        values[valuesById[1] = "TWO_FA_EXP_5_MINUTES"] = 1;
        values[valuesById[2] = "TWO_FA_EXP_12_HOURS"] = 2;
        values[valuesById[3] = "TWO_FA_EXP_24_HOURS"] = 3;
        values[valuesById[4] = "TWO_FA_EXP_30_DAYS"] = 4;
        values[valuesById[5] = "TWO_FA_EXP_NEVER"] = 5;
        return values;
    })();

    Authentication.TwoFactorDuoStatus = (function() {

        /**
         * Properties of a TwoFactorDuoStatus.
         * @memberof Authentication
         * @interface ITwoFactorDuoStatus
         * @property {Array.<string>|null} [capabilities] TwoFactorDuoStatus capabilities
         * @property {string|null} [phoneNumber] TwoFactorDuoStatus phoneNumber
         * @property {string|null} [enrollUrl] TwoFactorDuoStatus enrollUrl
         * @property {string|null} [message] TwoFactorDuoStatus message
         */

        /**
         * Constructs a new TwoFactorDuoStatus.
         * @memberof Authentication
         * @classdesc Represents a TwoFactorDuoStatus.
         * @implements ITwoFactorDuoStatus
         * @constructor
         * @param {Authentication.ITwoFactorDuoStatus=} [properties] Properties to set
         */
        function TwoFactorDuoStatus(properties) {
            this.capabilities = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TwoFactorDuoStatus capabilities.
         * @member {Array.<string>} capabilities
         * @memberof Authentication.TwoFactorDuoStatus
         * @instance
         */
        TwoFactorDuoStatus.prototype.capabilities = $util.emptyArray;

        /**
         * TwoFactorDuoStatus phoneNumber.
         * @member {string} phoneNumber
         * @memberof Authentication.TwoFactorDuoStatus
         * @instance
         */
        TwoFactorDuoStatus.prototype.phoneNumber = "";

        /**
         * TwoFactorDuoStatus enrollUrl.
         * @member {string} enrollUrl
         * @memberof Authentication.TwoFactorDuoStatus
         * @instance
         */
        TwoFactorDuoStatus.prototype.enrollUrl = "";

        /**
         * TwoFactorDuoStatus message.
         * @member {string} message
         * @memberof Authentication.TwoFactorDuoStatus
         * @instance
         */
        TwoFactorDuoStatus.prototype.message = "";

        /**
         * Creates a new TwoFactorDuoStatus instance using the specified properties.
         * @function create
         * @memberof Authentication.TwoFactorDuoStatus
         * @static
         * @param {Authentication.ITwoFactorDuoStatus=} [properties] Properties to set
         * @returns {Authentication.TwoFactorDuoStatus} TwoFactorDuoStatus instance
         */
        TwoFactorDuoStatus.create = function create(properties) {
            return new TwoFactorDuoStatus(properties);
        };

        /**
         * Encodes the specified TwoFactorDuoStatus message. Does not implicitly {@link Authentication.TwoFactorDuoStatus.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TwoFactorDuoStatus
         * @static
         * @param {Authentication.ITwoFactorDuoStatus} message TwoFactorDuoStatus message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TwoFactorDuoStatus.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.capabilities != null && message.capabilities.length)
                for (let i = 0; i < message.capabilities.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.capabilities[i]);
            if (message.phoneNumber != null && Object.hasOwnProperty.call(message, "phoneNumber"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.phoneNumber);
            if (message.enrollUrl != null && Object.hasOwnProperty.call(message, "enrollUrl"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.enrollUrl);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.message);
            return writer;
        };

        /**
         * Decodes a TwoFactorDuoStatus message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TwoFactorDuoStatus
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TwoFactorDuoStatus} TwoFactorDuoStatus
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TwoFactorDuoStatus.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TwoFactorDuoStatus();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.capabilities && message.capabilities.length))
                            message.capabilities = [];
                        message.capabilities.push(reader.string());
                        break;
                    }
                case 2: {
                        message.phoneNumber = reader.string();
                        break;
                    }
                case 3: {
                        message.enrollUrl = reader.string();
                        break;
                    }
                case 4: {
                        message.message = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TwoFactorDuoStatus message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TwoFactorDuoStatus
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TwoFactorDuoStatus} TwoFactorDuoStatus
         */
        TwoFactorDuoStatus.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TwoFactorDuoStatus)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TwoFactorDuoStatus: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TwoFactorDuoStatus();
            if (object.capabilities) {
                if (!Array.isArray(object.capabilities))
                    throw TypeError(".Authentication.TwoFactorDuoStatus.capabilities: array expected");
                message.capabilities = [];
                for (let i = 0; i < object.capabilities.length; ++i)
                    message.capabilities[i] = String(object.capabilities[i]);
            }
            if (object.phoneNumber != null)
                message.phoneNumber = String(object.phoneNumber);
            if (object.enrollUrl != null)
                message.enrollUrl = String(object.enrollUrl);
            if (object.message != null)
                message.message = String(object.message);
            return message;
        };

        /**
         * Creates a plain object from a TwoFactorDuoStatus message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TwoFactorDuoStatus
         * @static
         * @param {Authentication.TwoFactorDuoStatus} message TwoFactorDuoStatus
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TwoFactorDuoStatus.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.capabilities = [];
            if (options.defaults) {
                object.phoneNumber = "";
                object.enrollUrl = "";
                object.message = "";
            }
            if (message.capabilities && message.capabilities.length) {
                object.capabilities = [];
                for (let j = 0; j < message.capabilities.length; ++j)
                    object.capabilities[j] = message.capabilities[j];
            }
            if (message.phoneNumber != null && Object.hasOwnProperty.call(message, "phoneNumber"))
                object.phoneNumber = message.phoneNumber;
            if (message.enrollUrl != null && Object.hasOwnProperty.call(message, "enrollUrl"))
                object.enrollUrl = message.enrollUrl;
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                object.message = message.message;
            return object;
        };

        /**
         * Converts this TwoFactorDuoStatus to JSON.
         * @function toJSON
         * @memberof Authentication.TwoFactorDuoStatus
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TwoFactorDuoStatus.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TwoFactorDuoStatus
         * @function getTypeUrl
         * @memberof Authentication.TwoFactorDuoStatus
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TwoFactorDuoStatus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TwoFactorDuoStatus";
        };

        return TwoFactorDuoStatus;
    })();

    Authentication.TwoFactorAddRequest = (function() {

        /**
         * Properties of a TwoFactorAddRequest.
         * @memberof Authentication
         * @interface ITwoFactorAddRequest
         * @property {Authentication.TwoFactorChannelType|null} [channelType] TwoFactorAddRequest channelType
         * @property {Uint8Array|null} [channelUid] TwoFactorAddRequest channelUid
         * @property {string|null} [channelName] TwoFactorAddRequest channelName
         * @property {string|null} [phoneNumber] TwoFactorAddRequest phoneNumber
         * @property {Authentication.TwoFactorPushType|null} [duoPushType] TwoFactorAddRequest duoPushType
         */

        /**
         * Constructs a new TwoFactorAddRequest.
         * @memberof Authentication
         * @classdesc Represents a TwoFactorAddRequest.
         * @implements ITwoFactorAddRequest
         * @constructor
         * @param {Authentication.ITwoFactorAddRequest=} [properties] Properties to set
         */
        function TwoFactorAddRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TwoFactorAddRequest channelType.
         * @member {Authentication.TwoFactorChannelType} channelType
         * @memberof Authentication.TwoFactorAddRequest
         * @instance
         */
        TwoFactorAddRequest.prototype.channelType = 0;

        /**
         * TwoFactorAddRequest channelUid.
         * @member {Uint8Array} channelUid
         * @memberof Authentication.TwoFactorAddRequest
         * @instance
         */
        TwoFactorAddRequest.prototype.channelUid = $util.newBuffer([]);

        /**
         * TwoFactorAddRequest channelName.
         * @member {string} channelName
         * @memberof Authentication.TwoFactorAddRequest
         * @instance
         */
        TwoFactorAddRequest.prototype.channelName = "";

        /**
         * TwoFactorAddRequest phoneNumber.
         * @member {string} phoneNumber
         * @memberof Authentication.TwoFactorAddRequest
         * @instance
         */
        TwoFactorAddRequest.prototype.phoneNumber = "";

        /**
         * TwoFactorAddRequest duoPushType.
         * @member {Authentication.TwoFactorPushType} duoPushType
         * @memberof Authentication.TwoFactorAddRequest
         * @instance
         */
        TwoFactorAddRequest.prototype.duoPushType = 0;

        /**
         * Creates a new TwoFactorAddRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.TwoFactorAddRequest
         * @static
         * @param {Authentication.ITwoFactorAddRequest=} [properties] Properties to set
         * @returns {Authentication.TwoFactorAddRequest} TwoFactorAddRequest instance
         */
        TwoFactorAddRequest.create = function create(properties) {
            return new TwoFactorAddRequest(properties);
        };

        /**
         * Encodes the specified TwoFactorAddRequest message. Does not implicitly {@link Authentication.TwoFactorAddRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TwoFactorAddRequest
         * @static
         * @param {Authentication.ITwoFactorAddRequest} message TwoFactorAddRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TwoFactorAddRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.channelType != null && Object.hasOwnProperty.call(message, "channelType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.channelType);
            if (message.channelUid != null && Object.hasOwnProperty.call(message, "channelUid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.channelUid);
            if (message.channelName != null && Object.hasOwnProperty.call(message, "channelName"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.channelName);
            if (message.phoneNumber != null && Object.hasOwnProperty.call(message, "phoneNumber"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.phoneNumber);
            if (message.duoPushType != null && Object.hasOwnProperty.call(message, "duoPushType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.duoPushType);
            return writer;
        };

        /**
         * Decodes a TwoFactorAddRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TwoFactorAddRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TwoFactorAddRequest} TwoFactorAddRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TwoFactorAddRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TwoFactorAddRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.channelType = reader.int32();
                        break;
                    }
                case 2: {
                        message.channelUid = reader.bytes();
                        break;
                    }
                case 3: {
                        message.channelName = reader.string();
                        break;
                    }
                case 4: {
                        message.phoneNumber = reader.string();
                        break;
                    }
                case 5: {
                        message.duoPushType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TwoFactorAddRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TwoFactorAddRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TwoFactorAddRequest} TwoFactorAddRequest
         */
        TwoFactorAddRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TwoFactorAddRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TwoFactorAddRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TwoFactorAddRequest();
            switch (object.channelType) {
            default:
                if (typeof object.channelType === "number") {
                    message.channelType = object.channelType;
                    break;
                }
                break;
            case "TWO_FA_CT_NONE":
            case 0:
                message.channelType = 0;
                break;
            case "TWO_FA_CT_TOTP":
            case 1:
                message.channelType = 1;
                break;
            case "TWO_FA_CT_SMS":
            case 2:
                message.channelType = 2;
                break;
            case "TWO_FA_CT_DUO":
            case 3:
                message.channelType = 3;
                break;
            case "TWO_FA_CT_RSA":
            case 4:
                message.channelType = 4;
                break;
            case "TWO_FA_CT_BACKUP":
            case 5:
                message.channelType = 5;
                break;
            case "TWO_FA_CT_U2F":
            case 6:
                message.channelType = 6;
                break;
            case "TWO_FA_CT_WEBAUTHN":
            case 7:
                message.channelType = 7;
                break;
            case "TWO_FA_CT_KEEPER":
            case 8:
                message.channelType = 8;
                break;
            case "TWO_FA_CT_DNA":
            case 9:
                message.channelType = 9;
                break;
            }
            if (object.channelUid != null)
                if (typeof object.channelUid === "string")
                    $util.base64.decode(object.channelUid, message.channelUid = $util.newBuffer($util.base64.length(object.channelUid)), 0);
                else if (object.channelUid.length >= 0)
                    message.channelUid = object.channelUid;
            if (object.channelName != null)
                message.channelName = String(object.channelName);
            if (object.phoneNumber != null)
                message.phoneNumber = String(object.phoneNumber);
            switch (object.duoPushType) {
            default:
                if (typeof object.duoPushType === "number") {
                    message.duoPushType = object.duoPushType;
                    break;
                }
                break;
            case "TWO_FA_PUSH_NONE":
            case 0:
                message.duoPushType = 0;
                break;
            case "TWO_FA_PUSH_SMS":
            case 1:
                message.duoPushType = 1;
                break;
            case "TWO_FA_PUSH_KEEPER":
            case 2:
                message.duoPushType = 2;
                break;
            case "TWO_FA_PUSH_DUO_PUSH":
            case 3:
                message.duoPushType = 3;
                break;
            case "TWO_FA_PUSH_DUO_TEXT":
            case 4:
                message.duoPushType = 4;
                break;
            case "TWO_FA_PUSH_DUO_CALL":
            case 5:
                message.duoPushType = 5;
                break;
            case "TWO_FA_PUSH_DNA":
            case 6:
                message.duoPushType = 6;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a TwoFactorAddRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TwoFactorAddRequest
         * @static
         * @param {Authentication.TwoFactorAddRequest} message TwoFactorAddRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TwoFactorAddRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.channelType = options.enums === String ? "TWO_FA_CT_NONE" : 0;
                if (options.bytes === String)
                    object.channelUid = "";
                else {
                    object.channelUid = [];
                    if (options.bytes !== Array)
                        object.channelUid = $util.newBuffer(object.channelUid);
                }
                object.channelName = "";
                object.phoneNumber = "";
                object.duoPushType = options.enums === String ? "TWO_FA_PUSH_NONE" : 0;
            }
            if (message.channelType != null && Object.hasOwnProperty.call(message, "channelType"))
                object.channelType = options.enums === String ? $root.Authentication.TwoFactorChannelType[message.channelType] === undefined ? message.channelType : $root.Authentication.TwoFactorChannelType[message.channelType] : message.channelType;
            if (message.channelUid != null && Object.hasOwnProperty.call(message, "channelUid"))
                object.channelUid = options.bytes === String ? $util.base64.encode(message.channelUid, 0, message.channelUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.channelUid) : message.channelUid;
            if (message.channelName != null && Object.hasOwnProperty.call(message, "channelName"))
                object.channelName = message.channelName;
            if (message.phoneNumber != null && Object.hasOwnProperty.call(message, "phoneNumber"))
                object.phoneNumber = message.phoneNumber;
            if (message.duoPushType != null && Object.hasOwnProperty.call(message, "duoPushType"))
                object.duoPushType = options.enums === String ? $root.Authentication.TwoFactorPushType[message.duoPushType] === undefined ? message.duoPushType : $root.Authentication.TwoFactorPushType[message.duoPushType] : message.duoPushType;
            return object;
        };

        /**
         * Converts this TwoFactorAddRequest to JSON.
         * @function toJSON
         * @memberof Authentication.TwoFactorAddRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TwoFactorAddRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TwoFactorAddRequest
         * @function getTypeUrl
         * @memberof Authentication.TwoFactorAddRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TwoFactorAddRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TwoFactorAddRequest";
        };

        return TwoFactorAddRequest;
    })();

    Authentication.TwoFactorRenameRequest = (function() {

        /**
         * Properties of a TwoFactorRenameRequest.
         * @memberof Authentication
         * @interface ITwoFactorRenameRequest
         * @property {Uint8Array|null} [channelUid] TwoFactorRenameRequest channelUid
         * @property {string|null} [channelName] TwoFactorRenameRequest channelName
         */

        /**
         * Constructs a new TwoFactorRenameRequest.
         * @memberof Authentication
         * @classdesc Represents a TwoFactorRenameRequest.
         * @implements ITwoFactorRenameRequest
         * @constructor
         * @param {Authentication.ITwoFactorRenameRequest=} [properties] Properties to set
         */
        function TwoFactorRenameRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TwoFactorRenameRequest channelUid.
         * @member {Uint8Array} channelUid
         * @memberof Authentication.TwoFactorRenameRequest
         * @instance
         */
        TwoFactorRenameRequest.prototype.channelUid = $util.newBuffer([]);

        /**
         * TwoFactorRenameRequest channelName.
         * @member {string} channelName
         * @memberof Authentication.TwoFactorRenameRequest
         * @instance
         */
        TwoFactorRenameRequest.prototype.channelName = "";

        /**
         * Creates a new TwoFactorRenameRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.TwoFactorRenameRequest
         * @static
         * @param {Authentication.ITwoFactorRenameRequest=} [properties] Properties to set
         * @returns {Authentication.TwoFactorRenameRequest} TwoFactorRenameRequest instance
         */
        TwoFactorRenameRequest.create = function create(properties) {
            return new TwoFactorRenameRequest(properties);
        };

        /**
         * Encodes the specified TwoFactorRenameRequest message. Does not implicitly {@link Authentication.TwoFactorRenameRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TwoFactorRenameRequest
         * @static
         * @param {Authentication.ITwoFactorRenameRequest} message TwoFactorRenameRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TwoFactorRenameRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.channelUid != null && Object.hasOwnProperty.call(message, "channelUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.channelUid);
            if (message.channelName != null && Object.hasOwnProperty.call(message, "channelName"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.channelName);
            return writer;
        };

        /**
         * Decodes a TwoFactorRenameRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TwoFactorRenameRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TwoFactorRenameRequest} TwoFactorRenameRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TwoFactorRenameRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TwoFactorRenameRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.channelUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.channelName = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TwoFactorRenameRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TwoFactorRenameRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TwoFactorRenameRequest} TwoFactorRenameRequest
         */
        TwoFactorRenameRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TwoFactorRenameRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TwoFactorRenameRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TwoFactorRenameRequest();
            if (object.channelUid != null)
                if (typeof object.channelUid === "string")
                    $util.base64.decode(object.channelUid, message.channelUid = $util.newBuffer($util.base64.length(object.channelUid)), 0);
                else if (object.channelUid.length >= 0)
                    message.channelUid = object.channelUid;
            if (object.channelName != null)
                message.channelName = String(object.channelName);
            return message;
        };

        /**
         * Creates a plain object from a TwoFactorRenameRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TwoFactorRenameRequest
         * @static
         * @param {Authentication.TwoFactorRenameRequest} message TwoFactorRenameRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TwoFactorRenameRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.channelUid = "";
                else {
                    object.channelUid = [];
                    if (options.bytes !== Array)
                        object.channelUid = $util.newBuffer(object.channelUid);
                }
                object.channelName = "";
            }
            if (message.channelUid != null && Object.hasOwnProperty.call(message, "channelUid"))
                object.channelUid = options.bytes === String ? $util.base64.encode(message.channelUid, 0, message.channelUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.channelUid) : message.channelUid;
            if (message.channelName != null && Object.hasOwnProperty.call(message, "channelName"))
                object.channelName = message.channelName;
            return object;
        };

        /**
         * Converts this TwoFactorRenameRequest to JSON.
         * @function toJSON
         * @memberof Authentication.TwoFactorRenameRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TwoFactorRenameRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TwoFactorRenameRequest
         * @function getTypeUrl
         * @memberof Authentication.TwoFactorRenameRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TwoFactorRenameRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TwoFactorRenameRequest";
        };

        return TwoFactorRenameRequest;
    })();

    Authentication.TwoFactorAddResponse = (function() {

        /**
         * Properties of a TwoFactorAddResponse.
         * @memberof Authentication
         * @interface ITwoFactorAddResponse
         * @property {string|null} [challenge] TwoFactorAddResponse challenge
         * @property {Array.<string>|null} [backupKeys] TwoFactorAddResponse backupKeys
         */

        /**
         * Constructs a new TwoFactorAddResponse.
         * @memberof Authentication
         * @classdesc Represents a TwoFactorAddResponse.
         * @implements ITwoFactorAddResponse
         * @constructor
         * @param {Authentication.ITwoFactorAddResponse=} [properties] Properties to set
         */
        function TwoFactorAddResponse(properties) {
            this.backupKeys = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TwoFactorAddResponse challenge.
         * @member {string} challenge
         * @memberof Authentication.TwoFactorAddResponse
         * @instance
         */
        TwoFactorAddResponse.prototype.challenge = "";

        /**
         * TwoFactorAddResponse backupKeys.
         * @member {Array.<string>} backupKeys
         * @memberof Authentication.TwoFactorAddResponse
         * @instance
         */
        TwoFactorAddResponse.prototype.backupKeys = $util.emptyArray;

        /**
         * Creates a new TwoFactorAddResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.TwoFactorAddResponse
         * @static
         * @param {Authentication.ITwoFactorAddResponse=} [properties] Properties to set
         * @returns {Authentication.TwoFactorAddResponse} TwoFactorAddResponse instance
         */
        TwoFactorAddResponse.create = function create(properties) {
            return new TwoFactorAddResponse(properties);
        };

        /**
         * Encodes the specified TwoFactorAddResponse message. Does not implicitly {@link Authentication.TwoFactorAddResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TwoFactorAddResponse
         * @static
         * @param {Authentication.ITwoFactorAddResponse} message TwoFactorAddResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TwoFactorAddResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.challenge != null && Object.hasOwnProperty.call(message, "challenge"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.challenge);
            if (message.backupKeys != null && message.backupKeys.length)
                for (let i = 0; i < message.backupKeys.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.backupKeys[i]);
            return writer;
        };

        /**
         * Decodes a TwoFactorAddResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TwoFactorAddResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TwoFactorAddResponse} TwoFactorAddResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TwoFactorAddResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TwoFactorAddResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.challenge = reader.string();
                        break;
                    }
                case 2: {
                        if (!(message.backupKeys && message.backupKeys.length))
                            message.backupKeys = [];
                        message.backupKeys.push(reader.string());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TwoFactorAddResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TwoFactorAddResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TwoFactorAddResponse} TwoFactorAddResponse
         */
        TwoFactorAddResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TwoFactorAddResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TwoFactorAddResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TwoFactorAddResponse();
            if (object.challenge != null)
                message.challenge = String(object.challenge);
            if (object.backupKeys) {
                if (!Array.isArray(object.backupKeys))
                    throw TypeError(".Authentication.TwoFactorAddResponse.backupKeys: array expected");
                message.backupKeys = [];
                for (let i = 0; i < object.backupKeys.length; ++i)
                    message.backupKeys[i] = String(object.backupKeys[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a TwoFactorAddResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TwoFactorAddResponse
         * @static
         * @param {Authentication.TwoFactorAddResponse} message TwoFactorAddResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TwoFactorAddResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.backupKeys = [];
            if (options.defaults)
                object.challenge = "";
            if (message.challenge != null && Object.hasOwnProperty.call(message, "challenge"))
                object.challenge = message.challenge;
            if (message.backupKeys && message.backupKeys.length) {
                object.backupKeys = [];
                for (let j = 0; j < message.backupKeys.length; ++j)
                    object.backupKeys[j] = message.backupKeys[j];
            }
            return object;
        };

        /**
         * Converts this TwoFactorAddResponse to JSON.
         * @function toJSON
         * @memberof Authentication.TwoFactorAddResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TwoFactorAddResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TwoFactorAddResponse
         * @function getTypeUrl
         * @memberof Authentication.TwoFactorAddResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TwoFactorAddResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TwoFactorAddResponse";
        };

        return TwoFactorAddResponse;
    })();

    Authentication.TwoFactorDeleteRequest = (function() {

        /**
         * Properties of a TwoFactorDeleteRequest.
         * @memberof Authentication
         * @interface ITwoFactorDeleteRequest
         * @property {Uint8Array|null} [channelUid] TwoFactorDeleteRequest channelUid
         */

        /**
         * Constructs a new TwoFactorDeleteRequest.
         * @memberof Authentication
         * @classdesc Represents a TwoFactorDeleteRequest.
         * @implements ITwoFactorDeleteRequest
         * @constructor
         * @param {Authentication.ITwoFactorDeleteRequest=} [properties] Properties to set
         */
        function TwoFactorDeleteRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TwoFactorDeleteRequest channelUid.
         * @member {Uint8Array} channelUid
         * @memberof Authentication.TwoFactorDeleteRequest
         * @instance
         */
        TwoFactorDeleteRequest.prototype.channelUid = $util.newBuffer([]);

        /**
         * Creates a new TwoFactorDeleteRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.TwoFactorDeleteRequest
         * @static
         * @param {Authentication.ITwoFactorDeleteRequest=} [properties] Properties to set
         * @returns {Authentication.TwoFactorDeleteRequest} TwoFactorDeleteRequest instance
         */
        TwoFactorDeleteRequest.create = function create(properties) {
            return new TwoFactorDeleteRequest(properties);
        };

        /**
         * Encodes the specified TwoFactorDeleteRequest message. Does not implicitly {@link Authentication.TwoFactorDeleteRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TwoFactorDeleteRequest
         * @static
         * @param {Authentication.ITwoFactorDeleteRequest} message TwoFactorDeleteRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TwoFactorDeleteRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.channelUid != null && Object.hasOwnProperty.call(message, "channelUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.channelUid);
            return writer;
        };

        /**
         * Decodes a TwoFactorDeleteRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TwoFactorDeleteRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TwoFactorDeleteRequest} TwoFactorDeleteRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TwoFactorDeleteRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TwoFactorDeleteRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.channelUid = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TwoFactorDeleteRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TwoFactorDeleteRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TwoFactorDeleteRequest} TwoFactorDeleteRequest
         */
        TwoFactorDeleteRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TwoFactorDeleteRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TwoFactorDeleteRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TwoFactorDeleteRequest();
            if (object.channelUid != null)
                if (typeof object.channelUid === "string")
                    $util.base64.decode(object.channelUid, message.channelUid = $util.newBuffer($util.base64.length(object.channelUid)), 0);
                else if (object.channelUid.length >= 0)
                    message.channelUid = object.channelUid;
            return message;
        };

        /**
         * Creates a plain object from a TwoFactorDeleteRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TwoFactorDeleteRequest
         * @static
         * @param {Authentication.TwoFactorDeleteRequest} message TwoFactorDeleteRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TwoFactorDeleteRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === String)
                    object.channelUid = "";
                else {
                    object.channelUid = [];
                    if (options.bytes !== Array)
                        object.channelUid = $util.newBuffer(object.channelUid);
                }
            if (message.channelUid != null && Object.hasOwnProperty.call(message, "channelUid"))
                object.channelUid = options.bytes === String ? $util.base64.encode(message.channelUid, 0, message.channelUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.channelUid) : message.channelUid;
            return object;
        };

        /**
         * Converts this TwoFactorDeleteRequest to JSON.
         * @function toJSON
         * @memberof Authentication.TwoFactorDeleteRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TwoFactorDeleteRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TwoFactorDeleteRequest
         * @function getTypeUrl
         * @memberof Authentication.TwoFactorDeleteRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TwoFactorDeleteRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TwoFactorDeleteRequest";
        };

        return TwoFactorDeleteRequest;
    })();

    Authentication.TwoFactorListResponse = (function() {

        /**
         * Properties of a TwoFactorListResponse.
         * @memberof Authentication
         * @interface ITwoFactorListResponse
         * @property {Array.<Authentication.ITwoFactorChannelInfo>|null} [channels] TwoFactorListResponse channels
         * @property {number|null} [expireOn] TwoFactorListResponse expireOn
         */

        /**
         * Constructs a new TwoFactorListResponse.
         * @memberof Authentication
         * @classdesc Represents a TwoFactorListResponse.
         * @implements ITwoFactorListResponse
         * @constructor
         * @param {Authentication.ITwoFactorListResponse=} [properties] Properties to set
         */
        function TwoFactorListResponse(properties) {
            this.channels = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TwoFactorListResponse channels.
         * @member {Array.<Authentication.ITwoFactorChannelInfo>} channels
         * @memberof Authentication.TwoFactorListResponse
         * @instance
         */
        TwoFactorListResponse.prototype.channels = $util.emptyArray;

        /**
         * TwoFactorListResponse expireOn.
         * @member {number} expireOn
         * @memberof Authentication.TwoFactorListResponse
         * @instance
         */
        TwoFactorListResponse.prototype.expireOn = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new TwoFactorListResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.TwoFactorListResponse
         * @static
         * @param {Authentication.ITwoFactorListResponse=} [properties] Properties to set
         * @returns {Authentication.TwoFactorListResponse} TwoFactorListResponse instance
         */
        TwoFactorListResponse.create = function create(properties) {
            return new TwoFactorListResponse(properties);
        };

        /**
         * Encodes the specified TwoFactorListResponse message. Does not implicitly {@link Authentication.TwoFactorListResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TwoFactorListResponse
         * @static
         * @param {Authentication.ITwoFactorListResponse} message TwoFactorListResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TwoFactorListResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.channels != null && message.channels.length)
                for (let i = 0; i < message.channels.length; ++i)
                    $root.Authentication.TwoFactorChannelInfo.encode(message.channels[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.expireOn != null && Object.hasOwnProperty.call(message, "expireOn"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.expireOn);
            return writer;
        };

        /**
         * Decodes a TwoFactorListResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TwoFactorListResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TwoFactorListResponse} TwoFactorListResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TwoFactorListResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TwoFactorListResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.channels && message.channels.length))
                            message.channels = [];
                        message.channels.push($root.Authentication.TwoFactorChannelInfo.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 2: {
                        message.expireOn = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TwoFactorListResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TwoFactorListResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TwoFactorListResponse} TwoFactorListResponse
         */
        TwoFactorListResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TwoFactorListResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TwoFactorListResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TwoFactorListResponse();
            if (object.channels) {
                if (!Array.isArray(object.channels))
                    throw TypeError(".Authentication.TwoFactorListResponse.channels: array expected");
                message.channels = [];
                for (let i = 0; i < object.channels.length; ++i) {
                    if (!$util.isObject(object.channels[i]))
                        throw TypeError(".Authentication.TwoFactorListResponse.channels: object expected");
                    message.channels[i] = $root.Authentication.TwoFactorChannelInfo.fromObject(object.channels[i], long + 1);
                }
            }
            if (object.expireOn != null)
                if ($util.Long)
                    message.expireOn = $util.Long.fromValue(object.expireOn, false);
                else if (typeof object.expireOn === "string")
                    message.expireOn = parseInt(object.expireOn, 10);
                else if (typeof object.expireOn === "number")
                    message.expireOn = object.expireOn;
                else if (typeof object.expireOn === "object")
                    message.expireOn = new $util.LongBits(object.expireOn.low >>> 0, object.expireOn.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a TwoFactorListResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TwoFactorListResponse
         * @static
         * @param {Authentication.TwoFactorListResponse} message TwoFactorListResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TwoFactorListResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.channels = [];
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expireOn = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.expireOn = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            if (message.channels && message.channels.length) {
                object.channels = [];
                for (let j = 0; j < message.channels.length; ++j)
                    object.channels[j] = $root.Authentication.TwoFactorChannelInfo.toObject(message.channels[j], options, q + 1);
            }
            if (message.expireOn != null && Object.hasOwnProperty.call(message, "expireOn"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.expireOn = typeof message.expireOn === "number" ? BigInt(message.expireOn) : $util.Long.fromBits(message.expireOn.low >>> 0, message.expireOn.high >>> 0, false).toBigInt();
                else if (typeof message.expireOn === "number")
                    object.expireOn = options.longs === String ? String(message.expireOn) : message.expireOn;
                else
                    object.expireOn = options.longs === String ? $util.Long.prototype.toString.call(message.expireOn) : options.longs === Number ? new $util.LongBits(message.expireOn.low >>> 0, message.expireOn.high >>> 0).toNumber() : message.expireOn;
            return object;
        };

        /**
         * Converts this TwoFactorListResponse to JSON.
         * @function toJSON
         * @memberof Authentication.TwoFactorListResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TwoFactorListResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TwoFactorListResponse
         * @function getTypeUrl
         * @memberof Authentication.TwoFactorListResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TwoFactorListResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TwoFactorListResponse";
        };

        return TwoFactorListResponse;
    })();

    Authentication.TwoFactorUpdateExpirationRequest = (function() {

        /**
         * Properties of a TwoFactorUpdateExpirationRequest.
         * @memberof Authentication
         * @interface ITwoFactorUpdateExpirationRequest
         * @property {Authentication.TwoFactorExpiration|null} [expireIn] TwoFactorUpdateExpirationRequest expireIn
         */

        /**
         * Constructs a new TwoFactorUpdateExpirationRequest.
         * @memberof Authentication
         * @classdesc Represents a TwoFactorUpdateExpirationRequest.
         * @implements ITwoFactorUpdateExpirationRequest
         * @constructor
         * @param {Authentication.ITwoFactorUpdateExpirationRequest=} [properties] Properties to set
         */
        function TwoFactorUpdateExpirationRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TwoFactorUpdateExpirationRequest expireIn.
         * @member {Authentication.TwoFactorExpiration} expireIn
         * @memberof Authentication.TwoFactorUpdateExpirationRequest
         * @instance
         */
        TwoFactorUpdateExpirationRequest.prototype.expireIn = 0;

        /**
         * Creates a new TwoFactorUpdateExpirationRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.TwoFactorUpdateExpirationRequest
         * @static
         * @param {Authentication.ITwoFactorUpdateExpirationRequest=} [properties] Properties to set
         * @returns {Authentication.TwoFactorUpdateExpirationRequest} TwoFactorUpdateExpirationRequest instance
         */
        TwoFactorUpdateExpirationRequest.create = function create(properties) {
            return new TwoFactorUpdateExpirationRequest(properties);
        };

        /**
         * Encodes the specified TwoFactorUpdateExpirationRequest message. Does not implicitly {@link Authentication.TwoFactorUpdateExpirationRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TwoFactorUpdateExpirationRequest
         * @static
         * @param {Authentication.ITwoFactorUpdateExpirationRequest} message TwoFactorUpdateExpirationRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TwoFactorUpdateExpirationRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.expireIn != null && Object.hasOwnProperty.call(message, "expireIn"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.expireIn);
            return writer;
        };

        /**
         * Decodes a TwoFactorUpdateExpirationRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TwoFactorUpdateExpirationRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TwoFactorUpdateExpirationRequest} TwoFactorUpdateExpirationRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TwoFactorUpdateExpirationRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TwoFactorUpdateExpirationRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.expireIn = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TwoFactorUpdateExpirationRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TwoFactorUpdateExpirationRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TwoFactorUpdateExpirationRequest} TwoFactorUpdateExpirationRequest
         */
        TwoFactorUpdateExpirationRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TwoFactorUpdateExpirationRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TwoFactorUpdateExpirationRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TwoFactorUpdateExpirationRequest();
            switch (object.expireIn) {
            default:
                if (typeof object.expireIn === "number") {
                    message.expireIn = object.expireIn;
                    break;
                }
                break;
            case "TWO_FA_EXP_IMMEDIATELY":
            case 0:
                message.expireIn = 0;
                break;
            case "TWO_FA_EXP_5_MINUTES":
            case 1:
                message.expireIn = 1;
                break;
            case "TWO_FA_EXP_12_HOURS":
            case 2:
                message.expireIn = 2;
                break;
            case "TWO_FA_EXP_24_HOURS":
            case 3:
                message.expireIn = 3;
                break;
            case "TWO_FA_EXP_30_DAYS":
            case 4:
                message.expireIn = 4;
                break;
            case "TWO_FA_EXP_NEVER":
            case 5:
                message.expireIn = 5;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a TwoFactorUpdateExpirationRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TwoFactorUpdateExpirationRequest
         * @static
         * @param {Authentication.TwoFactorUpdateExpirationRequest} message TwoFactorUpdateExpirationRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TwoFactorUpdateExpirationRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.expireIn = options.enums === String ? "TWO_FA_EXP_IMMEDIATELY" : 0;
            if (message.expireIn != null && Object.hasOwnProperty.call(message, "expireIn"))
                object.expireIn = options.enums === String ? $root.Authentication.TwoFactorExpiration[message.expireIn] === undefined ? message.expireIn : $root.Authentication.TwoFactorExpiration[message.expireIn] : message.expireIn;
            return object;
        };

        /**
         * Converts this TwoFactorUpdateExpirationRequest to JSON.
         * @function toJSON
         * @memberof Authentication.TwoFactorUpdateExpirationRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TwoFactorUpdateExpirationRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TwoFactorUpdateExpirationRequest
         * @function getTypeUrl
         * @memberof Authentication.TwoFactorUpdateExpirationRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TwoFactorUpdateExpirationRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TwoFactorUpdateExpirationRequest";
        };

        return TwoFactorUpdateExpirationRequest;
    })();

    Authentication.TwoFactorValidateRequest = (function() {

        /**
         * Properties of a TwoFactorValidateRequest.
         * @memberof Authentication
         * @interface ITwoFactorValidateRequest
         * @property {Uint8Array|null} [encryptedLoginToken] TwoFactorValidateRequest encryptedLoginToken
         * @property {Authentication.TwoFactorValueType|null} [valueType] TwoFactorValidateRequest valueType
         * @property {string|null} [value] TwoFactorValidateRequest value
         * @property {Uint8Array|null} [channelUid] TwoFactorValidateRequest channelUid
         * @property {Authentication.TwoFactorExpiration|null} [expireIn] TwoFactorValidateRequest expireIn
         */

        /**
         * Constructs a new TwoFactorValidateRequest.
         * @memberof Authentication
         * @classdesc Represents a TwoFactorValidateRequest.
         * @implements ITwoFactorValidateRequest
         * @constructor
         * @param {Authentication.ITwoFactorValidateRequest=} [properties] Properties to set
         */
        function TwoFactorValidateRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TwoFactorValidateRequest encryptedLoginToken.
         * @member {Uint8Array} encryptedLoginToken
         * @memberof Authentication.TwoFactorValidateRequest
         * @instance
         */
        TwoFactorValidateRequest.prototype.encryptedLoginToken = $util.newBuffer([]);

        /**
         * TwoFactorValidateRequest valueType.
         * @member {Authentication.TwoFactorValueType} valueType
         * @memberof Authentication.TwoFactorValidateRequest
         * @instance
         */
        TwoFactorValidateRequest.prototype.valueType = 0;

        /**
         * TwoFactorValidateRequest value.
         * @member {string} value
         * @memberof Authentication.TwoFactorValidateRequest
         * @instance
         */
        TwoFactorValidateRequest.prototype.value = "";

        /**
         * TwoFactorValidateRequest channelUid.
         * @member {Uint8Array} channelUid
         * @memberof Authentication.TwoFactorValidateRequest
         * @instance
         */
        TwoFactorValidateRequest.prototype.channelUid = $util.newBuffer([]);

        /**
         * TwoFactorValidateRequest expireIn.
         * @member {Authentication.TwoFactorExpiration} expireIn
         * @memberof Authentication.TwoFactorValidateRequest
         * @instance
         */
        TwoFactorValidateRequest.prototype.expireIn = 0;

        /**
         * Creates a new TwoFactorValidateRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.TwoFactorValidateRequest
         * @static
         * @param {Authentication.ITwoFactorValidateRequest=} [properties] Properties to set
         * @returns {Authentication.TwoFactorValidateRequest} TwoFactorValidateRequest instance
         */
        TwoFactorValidateRequest.create = function create(properties) {
            return new TwoFactorValidateRequest(properties);
        };

        /**
         * Encodes the specified TwoFactorValidateRequest message. Does not implicitly {@link Authentication.TwoFactorValidateRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TwoFactorValidateRequest
         * @static
         * @param {Authentication.ITwoFactorValidateRequest} message TwoFactorValidateRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TwoFactorValidateRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedLoginToken);
            if (message.valueType != null && Object.hasOwnProperty.call(message, "valueType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.valueType);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.value);
            if (message.channelUid != null && Object.hasOwnProperty.call(message, "channelUid"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.channelUid);
            if (message.expireIn != null && Object.hasOwnProperty.call(message, "expireIn"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.expireIn);
            return writer;
        };

        /**
         * Decodes a TwoFactorValidateRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TwoFactorValidateRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TwoFactorValidateRequest} TwoFactorValidateRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TwoFactorValidateRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TwoFactorValidateRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedLoginToken = reader.bytes();
                        break;
                    }
                case 2: {
                        message.valueType = reader.int32();
                        break;
                    }
                case 3: {
                        message.value = reader.string();
                        break;
                    }
                case 4: {
                        message.channelUid = reader.bytes();
                        break;
                    }
                case 5: {
                        message.expireIn = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TwoFactorValidateRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TwoFactorValidateRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TwoFactorValidateRequest} TwoFactorValidateRequest
         */
        TwoFactorValidateRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TwoFactorValidateRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TwoFactorValidateRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TwoFactorValidateRequest();
            if (object.encryptedLoginToken != null)
                if (typeof object.encryptedLoginToken === "string")
                    $util.base64.decode(object.encryptedLoginToken, message.encryptedLoginToken = $util.newBuffer($util.base64.length(object.encryptedLoginToken)), 0);
                else if (object.encryptedLoginToken.length >= 0)
                    message.encryptedLoginToken = object.encryptedLoginToken;
            switch (object.valueType) {
            default:
                if (typeof object.valueType === "number") {
                    message.valueType = object.valueType;
                    break;
                }
                break;
            case "TWO_FA_CODE_NONE":
            case 0:
                message.valueType = 0;
                break;
            case "TWO_FA_CODE_TOTP":
            case 1:
                message.valueType = 1;
                break;
            case "TWO_FA_CODE_SMS":
            case 2:
                message.valueType = 2;
                break;
            case "TWO_FA_CODE_DUO":
            case 3:
                message.valueType = 3;
                break;
            case "TWO_FA_CODE_RSA":
            case 4:
                message.valueType = 4;
                break;
            case "TWO_FA_RESP_U2F":
            case 5:
                message.valueType = 5;
                break;
            case "TWO_FA_RESP_WEBAUTHN":
            case 6:
                message.valueType = 6;
                break;
            case "TWO_FA_CODE_DNA":
            case 7:
                message.valueType = 7;
                break;
            }
            if (object.value != null)
                message.value = String(object.value);
            if (object.channelUid != null)
                if (typeof object.channelUid === "string")
                    $util.base64.decode(object.channelUid, message.channelUid = $util.newBuffer($util.base64.length(object.channelUid)), 0);
                else if (object.channelUid.length >= 0)
                    message.channelUid = object.channelUid;
            switch (object.expireIn) {
            default:
                if (typeof object.expireIn === "number") {
                    message.expireIn = object.expireIn;
                    break;
                }
                break;
            case "TWO_FA_EXP_IMMEDIATELY":
            case 0:
                message.expireIn = 0;
                break;
            case "TWO_FA_EXP_5_MINUTES":
            case 1:
                message.expireIn = 1;
                break;
            case "TWO_FA_EXP_12_HOURS":
            case 2:
                message.expireIn = 2;
                break;
            case "TWO_FA_EXP_24_HOURS":
            case 3:
                message.expireIn = 3;
                break;
            case "TWO_FA_EXP_30_DAYS":
            case 4:
                message.expireIn = 4;
                break;
            case "TWO_FA_EXP_NEVER":
            case 5:
                message.expireIn = 5;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a TwoFactorValidateRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TwoFactorValidateRequest
         * @static
         * @param {Authentication.TwoFactorValidateRequest} message TwoFactorValidateRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TwoFactorValidateRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.encryptedLoginToken = "";
                else {
                    object.encryptedLoginToken = [];
                    if (options.bytes !== Array)
                        object.encryptedLoginToken = $util.newBuffer(object.encryptedLoginToken);
                }
                object.valueType = options.enums === String ? "TWO_FA_CODE_NONE" : 0;
                object.value = "";
                if (options.bytes === String)
                    object.channelUid = "";
                else {
                    object.channelUid = [];
                    if (options.bytes !== Array)
                        object.channelUid = $util.newBuffer(object.channelUid);
                }
                object.expireIn = options.enums === String ? "TWO_FA_EXP_IMMEDIATELY" : 0;
            }
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                object.encryptedLoginToken = options.bytes === String ? $util.base64.encode(message.encryptedLoginToken, 0, message.encryptedLoginToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedLoginToken) : message.encryptedLoginToken;
            if (message.valueType != null && Object.hasOwnProperty.call(message, "valueType"))
                object.valueType = options.enums === String ? $root.Authentication.TwoFactorValueType[message.valueType] === undefined ? message.valueType : $root.Authentication.TwoFactorValueType[message.valueType] : message.valueType;
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                object.value = message.value;
            if (message.channelUid != null && Object.hasOwnProperty.call(message, "channelUid"))
                object.channelUid = options.bytes === String ? $util.base64.encode(message.channelUid, 0, message.channelUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.channelUid) : message.channelUid;
            if (message.expireIn != null && Object.hasOwnProperty.call(message, "expireIn"))
                object.expireIn = options.enums === String ? $root.Authentication.TwoFactorExpiration[message.expireIn] === undefined ? message.expireIn : $root.Authentication.TwoFactorExpiration[message.expireIn] : message.expireIn;
            return object;
        };

        /**
         * Converts this TwoFactorValidateRequest to JSON.
         * @function toJSON
         * @memberof Authentication.TwoFactorValidateRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TwoFactorValidateRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TwoFactorValidateRequest
         * @function getTypeUrl
         * @memberof Authentication.TwoFactorValidateRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TwoFactorValidateRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TwoFactorValidateRequest";
        };

        return TwoFactorValidateRequest;
    })();

    Authentication.TwoFactorValidateResponse = (function() {

        /**
         * Properties of a TwoFactorValidateResponse.
         * @memberof Authentication
         * @interface ITwoFactorValidateResponse
         * @property {Uint8Array|null} [encryptedLoginToken] TwoFactorValidateResponse encryptedLoginToken
         */

        /**
         * Constructs a new TwoFactorValidateResponse.
         * @memberof Authentication
         * @classdesc Represents a TwoFactorValidateResponse.
         * @implements ITwoFactorValidateResponse
         * @constructor
         * @param {Authentication.ITwoFactorValidateResponse=} [properties] Properties to set
         */
        function TwoFactorValidateResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TwoFactorValidateResponse encryptedLoginToken.
         * @member {Uint8Array} encryptedLoginToken
         * @memberof Authentication.TwoFactorValidateResponse
         * @instance
         */
        TwoFactorValidateResponse.prototype.encryptedLoginToken = $util.newBuffer([]);

        /**
         * Creates a new TwoFactorValidateResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.TwoFactorValidateResponse
         * @static
         * @param {Authentication.ITwoFactorValidateResponse=} [properties] Properties to set
         * @returns {Authentication.TwoFactorValidateResponse} TwoFactorValidateResponse instance
         */
        TwoFactorValidateResponse.create = function create(properties) {
            return new TwoFactorValidateResponse(properties);
        };

        /**
         * Encodes the specified TwoFactorValidateResponse message. Does not implicitly {@link Authentication.TwoFactorValidateResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TwoFactorValidateResponse
         * @static
         * @param {Authentication.ITwoFactorValidateResponse} message TwoFactorValidateResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TwoFactorValidateResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedLoginToken);
            return writer;
        };

        /**
         * Decodes a TwoFactorValidateResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TwoFactorValidateResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TwoFactorValidateResponse} TwoFactorValidateResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TwoFactorValidateResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TwoFactorValidateResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedLoginToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TwoFactorValidateResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TwoFactorValidateResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TwoFactorValidateResponse} TwoFactorValidateResponse
         */
        TwoFactorValidateResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TwoFactorValidateResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TwoFactorValidateResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TwoFactorValidateResponse();
            if (object.encryptedLoginToken != null)
                if (typeof object.encryptedLoginToken === "string")
                    $util.base64.decode(object.encryptedLoginToken, message.encryptedLoginToken = $util.newBuffer($util.base64.length(object.encryptedLoginToken)), 0);
                else if (object.encryptedLoginToken.length >= 0)
                    message.encryptedLoginToken = object.encryptedLoginToken;
            return message;
        };

        /**
         * Creates a plain object from a TwoFactorValidateResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TwoFactorValidateResponse
         * @static
         * @param {Authentication.TwoFactorValidateResponse} message TwoFactorValidateResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TwoFactorValidateResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === String)
                    object.encryptedLoginToken = "";
                else {
                    object.encryptedLoginToken = [];
                    if (options.bytes !== Array)
                        object.encryptedLoginToken = $util.newBuffer(object.encryptedLoginToken);
                }
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                object.encryptedLoginToken = options.bytes === String ? $util.base64.encode(message.encryptedLoginToken, 0, message.encryptedLoginToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedLoginToken) : message.encryptedLoginToken;
            return object;
        };

        /**
         * Converts this TwoFactorValidateResponse to JSON.
         * @function toJSON
         * @memberof Authentication.TwoFactorValidateResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TwoFactorValidateResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TwoFactorValidateResponse
         * @function getTypeUrl
         * @memberof Authentication.TwoFactorValidateResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TwoFactorValidateResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TwoFactorValidateResponse";
        };

        return TwoFactorValidateResponse;
    })();

    Authentication.TwoFactorSendPushRequest = (function() {

        /**
         * Properties of a TwoFactorSendPushRequest.
         * @memberof Authentication
         * @interface ITwoFactorSendPushRequest
         * @property {Uint8Array|null} [encryptedLoginToken] TwoFactorSendPushRequest encryptedLoginToken
         * @property {Authentication.TwoFactorPushType|null} [pushType] TwoFactorSendPushRequest pushType
         * @property {Uint8Array|null} [channelUid] TwoFactorSendPushRequest channelUid
         * @property {Authentication.TwoFactorExpiration|null} [expireIn] TwoFactorSendPushRequest expireIn
         */

        /**
         * Constructs a new TwoFactorSendPushRequest.
         * @memberof Authentication
         * @classdesc Represents a TwoFactorSendPushRequest.
         * @implements ITwoFactorSendPushRequest
         * @constructor
         * @param {Authentication.ITwoFactorSendPushRequest=} [properties] Properties to set
         */
        function TwoFactorSendPushRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TwoFactorSendPushRequest encryptedLoginToken.
         * @member {Uint8Array} encryptedLoginToken
         * @memberof Authentication.TwoFactorSendPushRequest
         * @instance
         */
        TwoFactorSendPushRequest.prototype.encryptedLoginToken = $util.newBuffer([]);

        /**
         * TwoFactorSendPushRequest pushType.
         * @member {Authentication.TwoFactorPushType} pushType
         * @memberof Authentication.TwoFactorSendPushRequest
         * @instance
         */
        TwoFactorSendPushRequest.prototype.pushType = 0;

        /**
         * TwoFactorSendPushRequest channelUid.
         * @member {Uint8Array} channelUid
         * @memberof Authentication.TwoFactorSendPushRequest
         * @instance
         */
        TwoFactorSendPushRequest.prototype.channelUid = $util.newBuffer([]);

        /**
         * TwoFactorSendPushRequest expireIn.
         * @member {Authentication.TwoFactorExpiration} expireIn
         * @memberof Authentication.TwoFactorSendPushRequest
         * @instance
         */
        TwoFactorSendPushRequest.prototype.expireIn = 0;

        /**
         * Creates a new TwoFactorSendPushRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.TwoFactorSendPushRequest
         * @static
         * @param {Authentication.ITwoFactorSendPushRequest=} [properties] Properties to set
         * @returns {Authentication.TwoFactorSendPushRequest} TwoFactorSendPushRequest instance
         */
        TwoFactorSendPushRequest.create = function create(properties) {
            return new TwoFactorSendPushRequest(properties);
        };

        /**
         * Encodes the specified TwoFactorSendPushRequest message. Does not implicitly {@link Authentication.TwoFactorSendPushRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TwoFactorSendPushRequest
         * @static
         * @param {Authentication.ITwoFactorSendPushRequest} message TwoFactorSendPushRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TwoFactorSendPushRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedLoginToken);
            if (message.pushType != null && Object.hasOwnProperty.call(message, "pushType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.pushType);
            if (message.channelUid != null && Object.hasOwnProperty.call(message, "channelUid"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.channelUid);
            if (message.expireIn != null && Object.hasOwnProperty.call(message, "expireIn"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.expireIn);
            return writer;
        };

        /**
         * Decodes a TwoFactorSendPushRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TwoFactorSendPushRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TwoFactorSendPushRequest} TwoFactorSendPushRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TwoFactorSendPushRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TwoFactorSendPushRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedLoginToken = reader.bytes();
                        break;
                    }
                case 2: {
                        message.pushType = reader.int32();
                        break;
                    }
                case 3: {
                        message.channelUid = reader.bytes();
                        break;
                    }
                case 4: {
                        message.expireIn = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TwoFactorSendPushRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TwoFactorSendPushRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TwoFactorSendPushRequest} TwoFactorSendPushRequest
         */
        TwoFactorSendPushRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TwoFactorSendPushRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TwoFactorSendPushRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TwoFactorSendPushRequest();
            if (object.encryptedLoginToken != null)
                if (typeof object.encryptedLoginToken === "string")
                    $util.base64.decode(object.encryptedLoginToken, message.encryptedLoginToken = $util.newBuffer($util.base64.length(object.encryptedLoginToken)), 0);
                else if (object.encryptedLoginToken.length >= 0)
                    message.encryptedLoginToken = object.encryptedLoginToken;
            switch (object.pushType) {
            default:
                if (typeof object.pushType === "number") {
                    message.pushType = object.pushType;
                    break;
                }
                break;
            case "TWO_FA_PUSH_NONE":
            case 0:
                message.pushType = 0;
                break;
            case "TWO_FA_PUSH_SMS":
            case 1:
                message.pushType = 1;
                break;
            case "TWO_FA_PUSH_KEEPER":
            case 2:
                message.pushType = 2;
                break;
            case "TWO_FA_PUSH_DUO_PUSH":
            case 3:
                message.pushType = 3;
                break;
            case "TWO_FA_PUSH_DUO_TEXT":
            case 4:
                message.pushType = 4;
                break;
            case "TWO_FA_PUSH_DUO_CALL":
            case 5:
                message.pushType = 5;
                break;
            case "TWO_FA_PUSH_DNA":
            case 6:
                message.pushType = 6;
                break;
            }
            if (object.channelUid != null)
                if (typeof object.channelUid === "string")
                    $util.base64.decode(object.channelUid, message.channelUid = $util.newBuffer($util.base64.length(object.channelUid)), 0);
                else if (object.channelUid.length >= 0)
                    message.channelUid = object.channelUid;
            switch (object.expireIn) {
            default:
                if (typeof object.expireIn === "number") {
                    message.expireIn = object.expireIn;
                    break;
                }
                break;
            case "TWO_FA_EXP_IMMEDIATELY":
            case 0:
                message.expireIn = 0;
                break;
            case "TWO_FA_EXP_5_MINUTES":
            case 1:
                message.expireIn = 1;
                break;
            case "TWO_FA_EXP_12_HOURS":
            case 2:
                message.expireIn = 2;
                break;
            case "TWO_FA_EXP_24_HOURS":
            case 3:
                message.expireIn = 3;
                break;
            case "TWO_FA_EXP_30_DAYS":
            case 4:
                message.expireIn = 4;
                break;
            case "TWO_FA_EXP_NEVER":
            case 5:
                message.expireIn = 5;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a TwoFactorSendPushRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TwoFactorSendPushRequest
         * @static
         * @param {Authentication.TwoFactorSendPushRequest} message TwoFactorSendPushRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TwoFactorSendPushRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.encryptedLoginToken = "";
                else {
                    object.encryptedLoginToken = [];
                    if (options.bytes !== Array)
                        object.encryptedLoginToken = $util.newBuffer(object.encryptedLoginToken);
                }
                object.pushType = options.enums === String ? "TWO_FA_PUSH_NONE" : 0;
                if (options.bytes === String)
                    object.channelUid = "";
                else {
                    object.channelUid = [];
                    if (options.bytes !== Array)
                        object.channelUid = $util.newBuffer(object.channelUid);
                }
                object.expireIn = options.enums === String ? "TWO_FA_EXP_IMMEDIATELY" : 0;
            }
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                object.encryptedLoginToken = options.bytes === String ? $util.base64.encode(message.encryptedLoginToken, 0, message.encryptedLoginToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedLoginToken) : message.encryptedLoginToken;
            if (message.pushType != null && Object.hasOwnProperty.call(message, "pushType"))
                object.pushType = options.enums === String ? $root.Authentication.TwoFactorPushType[message.pushType] === undefined ? message.pushType : $root.Authentication.TwoFactorPushType[message.pushType] : message.pushType;
            if (message.channelUid != null && Object.hasOwnProperty.call(message, "channelUid"))
                object.channelUid = options.bytes === String ? $util.base64.encode(message.channelUid, 0, message.channelUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.channelUid) : message.channelUid;
            if (message.expireIn != null && Object.hasOwnProperty.call(message, "expireIn"))
                object.expireIn = options.enums === String ? $root.Authentication.TwoFactorExpiration[message.expireIn] === undefined ? message.expireIn : $root.Authentication.TwoFactorExpiration[message.expireIn] : message.expireIn;
            return object;
        };

        /**
         * Converts this TwoFactorSendPushRequest to JSON.
         * @function toJSON
         * @memberof Authentication.TwoFactorSendPushRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TwoFactorSendPushRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TwoFactorSendPushRequest
         * @function getTypeUrl
         * @memberof Authentication.TwoFactorSendPushRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TwoFactorSendPushRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TwoFactorSendPushRequest";
        };

        return TwoFactorSendPushRequest;
    })();

    Authentication.License = (function() {

        /**
         * Properties of a License.
         * @memberof Authentication
         * @interface ILicense
         * @property {number|null} [created] License created
         * @property {number|null} [expiration] License expiration
         * @property {Authentication.LicenseStatus|null} [licenseStatus] License licenseStatus
         * @property {boolean|null} [paid] License paid
         * @property {string|null} [message] License message
         */

        /**
         * Constructs a new License.
         * @memberof Authentication
         * @classdesc Represents a License.
         * @implements ILicense
         * @constructor
         * @param {Authentication.ILicense=} [properties] Properties to set
         */
        function License(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * License created.
         * @member {number} created
         * @memberof Authentication.License
         * @instance
         */
        License.prototype.created = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * License expiration.
         * @member {number} expiration
         * @memberof Authentication.License
         * @instance
         */
        License.prototype.expiration = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * License licenseStatus.
         * @member {Authentication.LicenseStatus} licenseStatus
         * @memberof Authentication.License
         * @instance
         */
        License.prototype.licenseStatus = 0;

        /**
         * License paid.
         * @member {boolean} paid
         * @memberof Authentication.License
         * @instance
         */
        License.prototype.paid = false;

        /**
         * License message.
         * @member {string} message
         * @memberof Authentication.License
         * @instance
         */
        License.prototype.message = "";

        /**
         * Creates a new License instance using the specified properties.
         * @function create
         * @memberof Authentication.License
         * @static
         * @param {Authentication.ILicense=} [properties] Properties to set
         * @returns {Authentication.License} License instance
         */
        License.create = function create(properties) {
            return new License(properties);
        };

        /**
         * Encodes the specified License message. Does not implicitly {@link Authentication.License.verify|verify} messages.
         * @function encode
         * @memberof Authentication.License
         * @static
         * @param {Authentication.ILicense} message License message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        License.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.created != null && Object.hasOwnProperty.call(message, "created"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.created);
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.expiration);
            if (message.licenseStatus != null && Object.hasOwnProperty.call(message, "licenseStatus"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.licenseStatus);
            if (message.paid != null && Object.hasOwnProperty.call(message, "paid"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.paid);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.message);
            return writer;
        };

        /**
         * Decodes a License message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.License
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.License} License
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        License.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.License();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.created = reader.int64();
                        break;
                    }
                case 2: {
                        message.expiration = reader.int64();
                        break;
                    }
                case 3: {
                        message.licenseStatus = reader.int32();
                        break;
                    }
                case 4: {
                        message.paid = reader.bool();
                        break;
                    }
                case 5: {
                        message.message = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a License message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.License
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.License} License
         */
        License.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.License)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.License: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.License();
            if (object.created != null)
                if ($util.Long)
                    message.created = $util.Long.fromValue(object.created, false);
                else if (typeof object.created === "string")
                    message.created = parseInt(object.created, 10);
                else if (typeof object.created === "number")
                    message.created = object.created;
                else if (typeof object.created === "object")
                    message.created = new $util.LongBits(object.created.low >>> 0, object.created.high >>> 0).toNumber();
            if (object.expiration != null)
                if ($util.Long)
                    message.expiration = $util.Long.fromValue(object.expiration, false);
                else if (typeof object.expiration === "string")
                    message.expiration = parseInt(object.expiration, 10);
                else if (typeof object.expiration === "number")
                    message.expiration = object.expiration;
                else if (typeof object.expiration === "object")
                    message.expiration = new $util.LongBits(object.expiration.low >>> 0, object.expiration.high >>> 0).toNumber();
            switch (object.licenseStatus) {
            default:
                if (typeof object.licenseStatus === "number") {
                    message.licenseStatus = object.licenseStatus;
                    break;
                }
                break;
            case "OTHER":
            case 0:
                message.licenseStatus = 0;
                break;
            case "ACTIVE":
            case 1:
                message.licenseStatus = 1;
                break;
            case "EXPIRED":
            case 2:
                message.licenseStatus = 2;
                break;
            case "DISABLED":
            case 3:
                message.licenseStatus = 3;
                break;
            }
            if (object.paid != null)
                message.paid = Boolean(object.paid);
            if (object.message != null)
                message.message = String(object.message);
            return message;
        };

        /**
         * Creates a plain object from a License message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.License
         * @static
         * @param {Authentication.License} message License
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        License.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.created = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.created = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expiration = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.expiration = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.licenseStatus = options.enums === String ? "OTHER" : 0;
                object.paid = false;
                object.message = "";
            }
            if (message.created != null && Object.hasOwnProperty.call(message, "created"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.created = typeof message.created === "number" ? BigInt(message.created) : $util.Long.fromBits(message.created.low >>> 0, message.created.high >>> 0, false).toBigInt();
                else if (typeof message.created === "number")
                    object.created = options.longs === String ? String(message.created) : message.created;
                else
                    object.created = options.longs === String ? $util.Long.prototype.toString.call(message.created) : options.longs === Number ? new $util.LongBits(message.created.low >>> 0, message.created.high >>> 0).toNumber() : message.created;
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.expiration = typeof message.expiration === "number" ? BigInt(message.expiration) : $util.Long.fromBits(message.expiration.low >>> 0, message.expiration.high >>> 0, false).toBigInt();
                else if (typeof message.expiration === "number")
                    object.expiration = options.longs === String ? String(message.expiration) : message.expiration;
                else
                    object.expiration = options.longs === String ? $util.Long.prototype.toString.call(message.expiration) : options.longs === Number ? new $util.LongBits(message.expiration.low >>> 0, message.expiration.high >>> 0).toNumber() : message.expiration;
            if (message.licenseStatus != null && Object.hasOwnProperty.call(message, "licenseStatus"))
                object.licenseStatus = options.enums === String ? $root.Authentication.LicenseStatus[message.licenseStatus] === undefined ? message.licenseStatus : $root.Authentication.LicenseStatus[message.licenseStatus] : message.licenseStatus;
            if (message.paid != null && Object.hasOwnProperty.call(message, "paid"))
                object.paid = message.paid;
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                object.message = message.message;
            return object;
        };

        /**
         * Converts this License to JSON.
         * @function toJSON
         * @memberof Authentication.License
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        License.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for License
         * @function getTypeUrl
         * @memberof Authentication.License
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        License.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.License";
        };

        return License;
    })();

    /**
     * LicenseType enum.
     * @name Authentication.LicenseType
     * @enum {number}
     * @property {number} VAULT=0 VAULT value
     * @property {number} CHAT=1 CHAT value
     * @property {number} STORAGE=2 STORAGE value
     * @property {number} BREACHWATCH=3 BREACHWATCH value
     */
    Authentication.LicenseType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "VAULT"] = 0;
        values[valuesById[1] = "CHAT"] = 1;
        values[valuesById[2] = "STORAGE"] = 2;
        values[valuesById[3] = "BREACHWATCH"] = 3;
        return values;
    })();

    Authentication.OwnerlessRecord = (function() {

        /**
         * Properties of an OwnerlessRecord.
         * @memberof Authentication
         * @interface IOwnerlessRecord
         * @property {Uint8Array|null} [recordUid] OwnerlessRecord recordUid
         * @property {Uint8Array|null} [recordKey] OwnerlessRecord recordKey
         * @property {number|null} [status] OwnerlessRecord status
         */

        /**
         * Constructs a new OwnerlessRecord.
         * @memberof Authentication
         * @classdesc Represents an OwnerlessRecord.
         * @implements IOwnerlessRecord
         * @constructor
         * @param {Authentication.IOwnerlessRecord=} [properties] Properties to set
         */
        function OwnerlessRecord(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * OwnerlessRecord recordUid.
         * @member {Uint8Array} recordUid
         * @memberof Authentication.OwnerlessRecord
         * @instance
         */
        OwnerlessRecord.prototype.recordUid = $util.newBuffer([]);

        /**
         * OwnerlessRecord recordKey.
         * @member {Uint8Array} recordKey
         * @memberof Authentication.OwnerlessRecord
         * @instance
         */
        OwnerlessRecord.prototype.recordKey = $util.newBuffer([]);

        /**
         * OwnerlessRecord status.
         * @member {number} status
         * @memberof Authentication.OwnerlessRecord
         * @instance
         */
        OwnerlessRecord.prototype.status = 0;

        /**
         * Creates a new OwnerlessRecord instance using the specified properties.
         * @function create
         * @memberof Authentication.OwnerlessRecord
         * @static
         * @param {Authentication.IOwnerlessRecord=} [properties] Properties to set
         * @returns {Authentication.OwnerlessRecord} OwnerlessRecord instance
         */
        OwnerlessRecord.create = function create(properties) {
            return new OwnerlessRecord(properties);
        };

        /**
         * Encodes the specified OwnerlessRecord message. Does not implicitly {@link Authentication.OwnerlessRecord.verify|verify} messages.
         * @function encode
         * @memberof Authentication.OwnerlessRecord
         * @static
         * @param {Authentication.IOwnerlessRecord} message OwnerlessRecord message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OwnerlessRecord.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.recordUid);
            if (message.recordKey != null && Object.hasOwnProperty.call(message, "recordKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.recordKey);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.status);
            return writer;
        };

        /**
         * Decodes an OwnerlessRecord message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.OwnerlessRecord
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.OwnerlessRecord} OwnerlessRecord
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OwnerlessRecord.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.OwnerlessRecord();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.recordUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.recordKey = reader.bytes();
                        break;
                    }
                case 3: {
                        message.status = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an OwnerlessRecord message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.OwnerlessRecord
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.OwnerlessRecord} OwnerlessRecord
         */
        OwnerlessRecord.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.OwnerlessRecord)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.OwnerlessRecord: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.OwnerlessRecord();
            if (object.recordUid != null)
                if (typeof object.recordUid === "string")
                    $util.base64.decode(object.recordUid, message.recordUid = $util.newBuffer($util.base64.length(object.recordUid)), 0);
                else if (object.recordUid.length >= 0)
                    message.recordUid = object.recordUid;
            if (object.recordKey != null)
                if (typeof object.recordKey === "string")
                    $util.base64.decode(object.recordKey, message.recordKey = $util.newBuffer($util.base64.length(object.recordKey)), 0);
                else if (object.recordKey.length >= 0)
                    message.recordKey = object.recordKey;
            if (object.status != null)
                message.status = object.status | 0;
            return message;
        };

        /**
         * Creates a plain object from an OwnerlessRecord message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.OwnerlessRecord
         * @static
         * @param {Authentication.OwnerlessRecord} message OwnerlessRecord
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        OwnerlessRecord.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.recordUid = "";
                else {
                    object.recordUid = [];
                    if (options.bytes !== Array)
                        object.recordUid = $util.newBuffer(object.recordUid);
                }
                if (options.bytes === String)
                    object.recordKey = "";
                else {
                    object.recordKey = [];
                    if (options.bytes !== Array)
                        object.recordKey = $util.newBuffer(object.recordKey);
                }
                object.status = 0;
            }
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                object.recordUid = options.bytes === String ? $util.base64.encode(message.recordUid, 0, message.recordUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.recordUid) : message.recordUid;
            if (message.recordKey != null && Object.hasOwnProperty.call(message, "recordKey"))
                object.recordKey = options.bytes === String ? $util.base64.encode(message.recordKey, 0, message.recordKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.recordKey) : message.recordKey;
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this OwnerlessRecord to JSON.
         * @function toJSON
         * @memberof Authentication.OwnerlessRecord
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        OwnerlessRecord.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for OwnerlessRecord
         * @function getTypeUrl
         * @memberof Authentication.OwnerlessRecord
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        OwnerlessRecord.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.OwnerlessRecord";
        };

        return OwnerlessRecord;
    })();

    Authentication.OwnerlessRecords = (function() {

        /**
         * Properties of an OwnerlessRecords.
         * @memberof Authentication
         * @interface IOwnerlessRecords
         * @property {Array.<Authentication.IOwnerlessRecord>|null} [ownerlessRecord] OwnerlessRecords ownerlessRecord
         */

        /**
         * Constructs a new OwnerlessRecords.
         * @memberof Authentication
         * @classdesc Represents an OwnerlessRecords.
         * @implements IOwnerlessRecords
         * @constructor
         * @param {Authentication.IOwnerlessRecords=} [properties] Properties to set
         */
        function OwnerlessRecords(properties) {
            this.ownerlessRecord = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * OwnerlessRecords ownerlessRecord.
         * @member {Array.<Authentication.IOwnerlessRecord>} ownerlessRecord
         * @memberof Authentication.OwnerlessRecords
         * @instance
         */
        OwnerlessRecords.prototype.ownerlessRecord = $util.emptyArray;

        /**
         * Creates a new OwnerlessRecords instance using the specified properties.
         * @function create
         * @memberof Authentication.OwnerlessRecords
         * @static
         * @param {Authentication.IOwnerlessRecords=} [properties] Properties to set
         * @returns {Authentication.OwnerlessRecords} OwnerlessRecords instance
         */
        OwnerlessRecords.create = function create(properties) {
            return new OwnerlessRecords(properties);
        };

        /**
         * Encodes the specified OwnerlessRecords message. Does not implicitly {@link Authentication.OwnerlessRecords.verify|verify} messages.
         * @function encode
         * @memberof Authentication.OwnerlessRecords
         * @static
         * @param {Authentication.IOwnerlessRecords} message OwnerlessRecords message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        OwnerlessRecords.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ownerlessRecord != null && message.ownerlessRecord.length)
                for (let i = 0; i < message.ownerlessRecord.length; ++i)
                    $root.Authentication.OwnerlessRecord.encode(message.ownerlessRecord[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes an OwnerlessRecords message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.OwnerlessRecords
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.OwnerlessRecords} OwnerlessRecords
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        OwnerlessRecords.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.OwnerlessRecords();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.ownerlessRecord && message.ownerlessRecord.length))
                            message.ownerlessRecord = [];
                        message.ownerlessRecord.push($root.Authentication.OwnerlessRecord.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an OwnerlessRecords message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.OwnerlessRecords
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.OwnerlessRecords} OwnerlessRecords
         */
        OwnerlessRecords.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.OwnerlessRecords)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.OwnerlessRecords: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.OwnerlessRecords();
            if (object.ownerlessRecord) {
                if (!Array.isArray(object.ownerlessRecord))
                    throw TypeError(".Authentication.OwnerlessRecords.ownerlessRecord: array expected");
                message.ownerlessRecord = [];
                for (let i = 0; i < object.ownerlessRecord.length; ++i) {
                    if (!$util.isObject(object.ownerlessRecord[i]))
                        throw TypeError(".Authentication.OwnerlessRecords.ownerlessRecord: object expected");
                    message.ownerlessRecord[i] = $root.Authentication.OwnerlessRecord.fromObject(object.ownerlessRecord[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an OwnerlessRecords message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.OwnerlessRecords
         * @static
         * @param {Authentication.OwnerlessRecords} message OwnerlessRecords
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        OwnerlessRecords.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.ownerlessRecord = [];
            if (message.ownerlessRecord && message.ownerlessRecord.length) {
                object.ownerlessRecord = [];
                for (let j = 0; j < message.ownerlessRecord.length; ++j)
                    object.ownerlessRecord[j] = $root.Authentication.OwnerlessRecord.toObject(message.ownerlessRecord[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this OwnerlessRecords to JSON.
         * @function toJSON
         * @memberof Authentication.OwnerlessRecords
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        OwnerlessRecords.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for OwnerlessRecords
         * @function getTypeUrl
         * @memberof Authentication.OwnerlessRecords
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        OwnerlessRecords.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.OwnerlessRecords";
        };

        return OwnerlessRecords;
    })();

    Authentication.UserAuthRequest = (function() {

        /**
         * Properties of a UserAuthRequest.
         * @memberof Authentication
         * @interface IUserAuthRequest
         * @property {Uint8Array|null} [uid] UserAuthRequest uid
         * @property {Uint8Array|null} [salt] UserAuthRequest salt
         * @property {number|null} [iterations] UserAuthRequest iterations
         * @property {Uint8Array|null} [encryptedClientKey] UserAuthRequest encryptedClientKey
         * @property {Uint8Array|null} [authHash] UserAuthRequest authHash
         * @property {Uint8Array|null} [encryptedDataKey] UserAuthRequest encryptedDataKey
         * @property {Authentication.LoginType|null} [loginType] UserAuthRequest loginType
         * @property {string|null} [name] UserAuthRequest name
         * @property {number|null} [algorithm] UserAuthRequest algorithm
         */

        /**
         * Constructs a new UserAuthRequest.
         * @memberof Authentication
         * @classdesc Represents a UserAuthRequest.
         * @implements IUserAuthRequest
         * @constructor
         * @param {Authentication.IUserAuthRequest=} [properties] Properties to set
         */
        function UserAuthRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UserAuthRequest uid.
         * @member {Uint8Array} uid
         * @memberof Authentication.UserAuthRequest
         * @instance
         */
        UserAuthRequest.prototype.uid = $util.newBuffer([]);

        /**
         * UserAuthRequest salt.
         * @member {Uint8Array} salt
         * @memberof Authentication.UserAuthRequest
         * @instance
         */
        UserAuthRequest.prototype.salt = $util.newBuffer([]);

        /**
         * UserAuthRequest iterations.
         * @member {number} iterations
         * @memberof Authentication.UserAuthRequest
         * @instance
         */
        UserAuthRequest.prototype.iterations = 0;

        /**
         * UserAuthRequest encryptedClientKey.
         * @member {Uint8Array} encryptedClientKey
         * @memberof Authentication.UserAuthRequest
         * @instance
         */
        UserAuthRequest.prototype.encryptedClientKey = $util.newBuffer([]);

        /**
         * UserAuthRequest authHash.
         * @member {Uint8Array} authHash
         * @memberof Authentication.UserAuthRequest
         * @instance
         */
        UserAuthRequest.prototype.authHash = $util.newBuffer([]);

        /**
         * UserAuthRequest encryptedDataKey.
         * @member {Uint8Array} encryptedDataKey
         * @memberof Authentication.UserAuthRequest
         * @instance
         */
        UserAuthRequest.prototype.encryptedDataKey = $util.newBuffer([]);

        /**
         * UserAuthRequest loginType.
         * @member {Authentication.LoginType} loginType
         * @memberof Authentication.UserAuthRequest
         * @instance
         */
        UserAuthRequest.prototype.loginType = 0;

        /**
         * UserAuthRequest name.
         * @member {string} name
         * @memberof Authentication.UserAuthRequest
         * @instance
         */
        UserAuthRequest.prototype.name = "";

        /**
         * UserAuthRequest algorithm.
         * @member {number} algorithm
         * @memberof Authentication.UserAuthRequest
         * @instance
         */
        UserAuthRequest.prototype.algorithm = 0;

        /**
         * Creates a new UserAuthRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.UserAuthRequest
         * @static
         * @param {Authentication.IUserAuthRequest=} [properties] Properties to set
         * @returns {Authentication.UserAuthRequest} UserAuthRequest instance
         */
        UserAuthRequest.create = function create(properties) {
            return new UserAuthRequest(properties);
        };

        /**
         * Encodes the specified UserAuthRequest message. Does not implicitly {@link Authentication.UserAuthRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.UserAuthRequest
         * @static
         * @param {Authentication.IUserAuthRequest} message UserAuthRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserAuthRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.uid);
            if (message.salt != null && Object.hasOwnProperty.call(message, "salt"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.salt);
            if (message.iterations != null && Object.hasOwnProperty.call(message, "iterations"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.iterations);
            if (message.encryptedClientKey != null && Object.hasOwnProperty.call(message, "encryptedClientKey"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.encryptedClientKey);
            if (message.authHash != null && Object.hasOwnProperty.call(message, "authHash"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.authHash);
            if (message.encryptedDataKey != null && Object.hasOwnProperty.call(message, "encryptedDataKey"))
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.encryptedDataKey);
            if (message.loginType != null && Object.hasOwnProperty.call(message, "loginType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.loginType);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.name);
            if (message.algorithm != null && Object.hasOwnProperty.call(message, "algorithm"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.algorithm);
            return writer;
        };

        /**
         * Decodes a UserAuthRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.UserAuthRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.UserAuthRequest} UserAuthRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserAuthRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.UserAuthRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.salt = reader.bytes();
                        break;
                    }
                case 3: {
                        message.iterations = reader.int32();
                        break;
                    }
                case 4: {
                        message.encryptedClientKey = reader.bytes();
                        break;
                    }
                case 5: {
                        message.authHash = reader.bytes();
                        break;
                    }
                case 6: {
                        message.encryptedDataKey = reader.bytes();
                        break;
                    }
                case 7: {
                        message.loginType = reader.int32();
                        break;
                    }
                case 8: {
                        message.name = reader.string();
                        break;
                    }
                case 9: {
                        message.algorithm = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a UserAuthRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.UserAuthRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.UserAuthRequest} UserAuthRequest
         */
        UserAuthRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.UserAuthRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.UserAuthRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.UserAuthRequest();
            if (object.uid != null)
                if (typeof object.uid === "string")
                    $util.base64.decode(object.uid, message.uid = $util.newBuffer($util.base64.length(object.uid)), 0);
                else if (object.uid.length >= 0)
                    message.uid = object.uid;
            if (object.salt != null)
                if (typeof object.salt === "string")
                    $util.base64.decode(object.salt, message.salt = $util.newBuffer($util.base64.length(object.salt)), 0);
                else if (object.salt.length >= 0)
                    message.salt = object.salt;
            if (object.iterations != null)
                message.iterations = object.iterations | 0;
            if (object.encryptedClientKey != null)
                if (typeof object.encryptedClientKey === "string")
                    $util.base64.decode(object.encryptedClientKey, message.encryptedClientKey = $util.newBuffer($util.base64.length(object.encryptedClientKey)), 0);
                else if (object.encryptedClientKey.length >= 0)
                    message.encryptedClientKey = object.encryptedClientKey;
            if (object.authHash != null)
                if (typeof object.authHash === "string")
                    $util.base64.decode(object.authHash, message.authHash = $util.newBuffer($util.base64.length(object.authHash)), 0);
                else if (object.authHash.length >= 0)
                    message.authHash = object.authHash;
            if (object.encryptedDataKey != null)
                if (typeof object.encryptedDataKey === "string")
                    $util.base64.decode(object.encryptedDataKey, message.encryptedDataKey = $util.newBuffer($util.base64.length(object.encryptedDataKey)), 0);
                else if (object.encryptedDataKey.length >= 0)
                    message.encryptedDataKey = object.encryptedDataKey;
            switch (object.loginType) {
            default:
                if (typeof object.loginType === "number") {
                    message.loginType = object.loginType;
                    break;
                }
                break;
            case "NORMAL":
            case 0:
                message.loginType = 0;
                break;
            case "SSO":
            case 1:
                message.loginType = 1;
                break;
            case "BIO":
            case 2:
                message.loginType = 2;
                break;
            case "ALTERNATE":
            case 3:
                message.loginType = 3;
                break;
            case "OFFLINE":
            case 4:
                message.loginType = 4;
                break;
            case "FORGOT_PASSWORD":
            case 5:
                message.loginType = 5;
                break;
            case "PASSKEY_BIO":
            case 6:
                message.loginType = 6;
                break;
            }
            if (object.name != null)
                message.name = String(object.name);
            if (object.algorithm != null)
                message.algorithm = object.algorithm | 0;
            return message;
        };

        /**
         * Creates a plain object from a UserAuthRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.UserAuthRequest
         * @static
         * @param {Authentication.UserAuthRequest} message UserAuthRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UserAuthRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.uid = "";
                else {
                    object.uid = [];
                    if (options.bytes !== Array)
                        object.uid = $util.newBuffer(object.uid);
                }
                if (options.bytes === String)
                    object.salt = "";
                else {
                    object.salt = [];
                    if (options.bytes !== Array)
                        object.salt = $util.newBuffer(object.salt);
                }
                object.iterations = 0;
                if (options.bytes === String)
                    object.encryptedClientKey = "";
                else {
                    object.encryptedClientKey = [];
                    if (options.bytes !== Array)
                        object.encryptedClientKey = $util.newBuffer(object.encryptedClientKey);
                }
                if (options.bytes === String)
                    object.authHash = "";
                else {
                    object.authHash = [];
                    if (options.bytes !== Array)
                        object.authHash = $util.newBuffer(object.authHash);
                }
                if (options.bytes === String)
                    object.encryptedDataKey = "";
                else {
                    object.encryptedDataKey = [];
                    if (options.bytes !== Array)
                        object.encryptedDataKey = $util.newBuffer(object.encryptedDataKey);
                }
                object.loginType = options.enums === String ? "NORMAL" : 0;
                object.name = "";
                object.algorithm = 0;
            }
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                object.uid = options.bytes === String ? $util.base64.encode(message.uid, 0, message.uid.length) : options.bytes === Array ? Array.prototype.slice.call(message.uid) : message.uid;
            if (message.salt != null && Object.hasOwnProperty.call(message, "salt"))
                object.salt = options.bytes === String ? $util.base64.encode(message.salt, 0, message.salt.length) : options.bytes === Array ? Array.prototype.slice.call(message.salt) : message.salt;
            if (message.iterations != null && Object.hasOwnProperty.call(message, "iterations"))
                object.iterations = message.iterations;
            if (message.encryptedClientKey != null && Object.hasOwnProperty.call(message, "encryptedClientKey"))
                object.encryptedClientKey = options.bytes === String ? $util.base64.encode(message.encryptedClientKey, 0, message.encryptedClientKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedClientKey) : message.encryptedClientKey;
            if (message.authHash != null && Object.hasOwnProperty.call(message, "authHash"))
                object.authHash = options.bytes === String ? $util.base64.encode(message.authHash, 0, message.authHash.length) : options.bytes === Array ? Array.prototype.slice.call(message.authHash) : message.authHash;
            if (message.encryptedDataKey != null && Object.hasOwnProperty.call(message, "encryptedDataKey"))
                object.encryptedDataKey = options.bytes === String ? $util.base64.encode(message.encryptedDataKey, 0, message.encryptedDataKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDataKey) : message.encryptedDataKey;
            if (message.loginType != null && Object.hasOwnProperty.call(message, "loginType"))
                object.loginType = options.enums === String ? $root.Authentication.LoginType[message.loginType] === undefined ? message.loginType : $root.Authentication.LoginType[message.loginType] : message.loginType;
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.algorithm != null && Object.hasOwnProperty.call(message, "algorithm"))
                object.algorithm = message.algorithm;
            return object;
        };

        /**
         * Converts this UserAuthRequest to JSON.
         * @function toJSON
         * @memberof Authentication.UserAuthRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UserAuthRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UserAuthRequest
         * @function getTypeUrl
         * @memberof Authentication.UserAuthRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UserAuthRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.UserAuthRequest";
        };

        return UserAuthRequest;
    })();

    Authentication.UidRequest = (function() {

        /**
         * Properties of an UidRequest.
         * @memberof Authentication
         * @interface IUidRequest
         * @property {Array.<Uint8Array>|null} [uid] UidRequest uid
         */

        /**
         * Constructs a new UidRequest.
         * @memberof Authentication
         * @classdesc Represents an UidRequest.
         * @implements IUidRequest
         * @constructor
         * @param {Authentication.IUidRequest=} [properties] Properties to set
         */
        function UidRequest(properties) {
            this.uid = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UidRequest uid.
         * @member {Array.<Uint8Array>} uid
         * @memberof Authentication.UidRequest
         * @instance
         */
        UidRequest.prototype.uid = $util.emptyArray;

        /**
         * Creates a new UidRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.UidRequest
         * @static
         * @param {Authentication.IUidRequest=} [properties] Properties to set
         * @returns {Authentication.UidRequest} UidRequest instance
         */
        UidRequest.create = function create(properties) {
            return new UidRequest(properties);
        };

        /**
         * Encodes the specified UidRequest message. Does not implicitly {@link Authentication.UidRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.UidRequest
         * @static
         * @param {Authentication.IUidRequest} message UidRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UidRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.uid != null && message.uid.length)
                for (let i = 0; i < message.uid.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.uid[i]);
            return writer;
        };

        /**
         * Decodes an UidRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.UidRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.UidRequest} UidRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UidRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.UidRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.uid && message.uid.length))
                            message.uid = [];
                        message.uid.push(reader.bytes());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an UidRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.UidRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.UidRequest} UidRequest
         */
        UidRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.UidRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.UidRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.UidRequest();
            if (object.uid) {
                if (!Array.isArray(object.uid))
                    throw TypeError(".Authentication.UidRequest.uid: array expected");
                message.uid = [];
                for (let i = 0; i < object.uid.length; ++i)
                    if (typeof object.uid[i] === "string")
                        $util.base64.decode(object.uid[i], message.uid[i] = $util.newBuffer($util.base64.length(object.uid[i])), 0);
                    else if (object.uid[i].length >= 0)
                        message.uid[i] = object.uid[i];
            }
            return message;
        };

        /**
         * Creates a plain object from an UidRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.UidRequest
         * @static
         * @param {Authentication.UidRequest} message UidRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UidRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.uid = [];
            if (message.uid && message.uid.length) {
                object.uid = [];
                for (let j = 0; j < message.uid.length; ++j)
                    object.uid[j] = options.bytes === String ? $util.base64.encode(message.uid[j], 0, message.uid[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.uid[j]) : message.uid[j];
            }
            return object;
        };

        /**
         * Converts this UidRequest to JSON.
         * @function toJSON
         * @memberof Authentication.UidRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UidRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UidRequest
         * @function getTypeUrl
         * @memberof Authentication.UidRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UidRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.UidRequest";
        };

        return UidRequest;
    })();

    Authentication.DeviceUpdateRequest = (function() {

        /**
         * Properties of a DeviceUpdateRequest.
         * @memberof Authentication
         * @interface IDeviceUpdateRequest
         * @property {Uint8Array|null} [encryptedDeviceToken] DeviceUpdateRequest encryptedDeviceToken
         * @property {string|null} [clientVersion] DeviceUpdateRequest clientVersion
         * @property {string|null} [deviceName] DeviceUpdateRequest deviceName
         * @property {Uint8Array|null} [devicePublicKey] DeviceUpdateRequest devicePublicKey
         * @property {Authentication.DeviceStatus|null} [deviceStatus] DeviceUpdateRequest deviceStatus
         * @property {string|null} [devicePlatform] DeviceUpdateRequest devicePlatform
         * @property {Authentication.ClientFormFactor|null} [clientFormFactor] DeviceUpdateRequest clientFormFactor
         */

        /**
         * Constructs a new DeviceUpdateRequest.
         * @memberof Authentication
         * @classdesc Represents a DeviceUpdateRequest.
         * @implements IDeviceUpdateRequest
         * @constructor
         * @param {Authentication.IDeviceUpdateRequest=} [properties] Properties to set
         */
        function DeviceUpdateRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeviceUpdateRequest encryptedDeviceToken.
         * @member {Uint8Array} encryptedDeviceToken
         * @memberof Authentication.DeviceUpdateRequest
         * @instance
         */
        DeviceUpdateRequest.prototype.encryptedDeviceToken = $util.newBuffer([]);

        /**
         * DeviceUpdateRequest clientVersion.
         * @member {string} clientVersion
         * @memberof Authentication.DeviceUpdateRequest
         * @instance
         */
        DeviceUpdateRequest.prototype.clientVersion = "";

        /**
         * DeviceUpdateRequest deviceName.
         * @member {string} deviceName
         * @memberof Authentication.DeviceUpdateRequest
         * @instance
         */
        DeviceUpdateRequest.prototype.deviceName = "";

        /**
         * DeviceUpdateRequest devicePublicKey.
         * @member {Uint8Array} devicePublicKey
         * @memberof Authentication.DeviceUpdateRequest
         * @instance
         */
        DeviceUpdateRequest.prototype.devicePublicKey = $util.newBuffer([]);

        /**
         * DeviceUpdateRequest deviceStatus.
         * @member {Authentication.DeviceStatus} deviceStatus
         * @memberof Authentication.DeviceUpdateRequest
         * @instance
         */
        DeviceUpdateRequest.prototype.deviceStatus = 0;

        /**
         * DeviceUpdateRequest devicePlatform.
         * @member {string} devicePlatform
         * @memberof Authentication.DeviceUpdateRequest
         * @instance
         */
        DeviceUpdateRequest.prototype.devicePlatform = "";

        /**
         * DeviceUpdateRequest clientFormFactor.
         * @member {Authentication.ClientFormFactor} clientFormFactor
         * @memberof Authentication.DeviceUpdateRequest
         * @instance
         */
        DeviceUpdateRequest.prototype.clientFormFactor = 0;

        /**
         * Creates a new DeviceUpdateRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.DeviceUpdateRequest
         * @static
         * @param {Authentication.IDeviceUpdateRequest=} [properties] Properties to set
         * @returns {Authentication.DeviceUpdateRequest} DeviceUpdateRequest instance
         */
        DeviceUpdateRequest.create = function create(properties) {
            return new DeviceUpdateRequest(properties);
        };

        /**
         * Encodes the specified DeviceUpdateRequest message. Does not implicitly {@link Authentication.DeviceUpdateRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.DeviceUpdateRequest
         * @static
         * @param {Authentication.IDeviceUpdateRequest} message DeviceUpdateRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceUpdateRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedDeviceToken);
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.clientVersion);
            if (message.deviceName != null && Object.hasOwnProperty.call(message, "deviceName"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.deviceName);
            if (message.devicePublicKey != null && Object.hasOwnProperty.call(message, "devicePublicKey"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.devicePublicKey);
            if (message.deviceStatus != null && Object.hasOwnProperty.call(message, "deviceStatus"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.deviceStatus);
            if (message.devicePlatform != null && Object.hasOwnProperty.call(message, "devicePlatform"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.devicePlatform);
            if (message.clientFormFactor != null && Object.hasOwnProperty.call(message, "clientFormFactor"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.clientFormFactor);
            return writer;
        };

        /**
         * Decodes a DeviceUpdateRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.DeviceUpdateRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.DeviceUpdateRequest} DeviceUpdateRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceUpdateRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.DeviceUpdateRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedDeviceToken = reader.bytes();
                        break;
                    }
                case 2: {
                        message.clientVersion = reader.string();
                        break;
                    }
                case 3: {
                        message.deviceName = reader.string();
                        break;
                    }
                case 4: {
                        message.devicePublicKey = reader.bytes();
                        break;
                    }
                case 5: {
                        message.deviceStatus = reader.int32();
                        break;
                    }
                case 6: {
                        message.devicePlatform = reader.string();
                        break;
                    }
                case 7: {
                        message.clientFormFactor = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a DeviceUpdateRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.DeviceUpdateRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.DeviceUpdateRequest} DeviceUpdateRequest
         */
        DeviceUpdateRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.DeviceUpdateRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.DeviceUpdateRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.DeviceUpdateRequest();
            if (object.encryptedDeviceToken != null)
                if (typeof object.encryptedDeviceToken === "string")
                    $util.base64.decode(object.encryptedDeviceToken, message.encryptedDeviceToken = $util.newBuffer($util.base64.length(object.encryptedDeviceToken)), 0);
                else if (object.encryptedDeviceToken.length >= 0)
                    message.encryptedDeviceToken = object.encryptedDeviceToken;
            if (object.clientVersion != null)
                message.clientVersion = String(object.clientVersion);
            if (object.deviceName != null)
                message.deviceName = String(object.deviceName);
            if (object.devicePublicKey != null)
                if (typeof object.devicePublicKey === "string")
                    $util.base64.decode(object.devicePublicKey, message.devicePublicKey = $util.newBuffer($util.base64.length(object.devicePublicKey)), 0);
                else if (object.devicePublicKey.length >= 0)
                    message.devicePublicKey = object.devicePublicKey;
            switch (object.deviceStatus) {
            default:
                if (typeof object.deviceStatus === "number") {
                    message.deviceStatus = object.deviceStatus;
                    break;
                }
                break;
            case "DEVICE_NEEDS_APPROVAL":
            case 0:
                message.deviceStatus = 0;
                break;
            case "DEVICE_OK":
            case 1:
                message.deviceStatus = 1;
                break;
            case "DEVICE_DISABLED_BY_USER":
            case 2:
                message.deviceStatus = 2;
                break;
            case "DEVICE_LOCKED_BY_ADMIN":
            case 3:
                message.deviceStatus = 3;
                break;
            }
            if (object.devicePlatform != null)
                message.devicePlatform = String(object.devicePlatform);
            switch (object.clientFormFactor) {
            default:
                if (typeof object.clientFormFactor === "number") {
                    message.clientFormFactor = object.clientFormFactor;
                    break;
                }
                break;
            case "FF_EMPTY":
            case 0:
                message.clientFormFactor = 0;
                break;
            case "FF_PHONE":
            case 1:
                message.clientFormFactor = 1;
                break;
            case "FF_TABLET":
            case 2:
                message.clientFormFactor = 2;
                break;
            case "FF_WATCH":
            case 3:
                message.clientFormFactor = 3;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a DeviceUpdateRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.DeviceUpdateRequest
         * @static
         * @param {Authentication.DeviceUpdateRequest} message DeviceUpdateRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeviceUpdateRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.encryptedDeviceToken = "";
                else {
                    object.encryptedDeviceToken = [];
                    if (options.bytes !== Array)
                        object.encryptedDeviceToken = $util.newBuffer(object.encryptedDeviceToken);
                }
                object.clientVersion = "";
                object.deviceName = "";
                if (options.bytes === String)
                    object.devicePublicKey = "";
                else {
                    object.devicePublicKey = [];
                    if (options.bytes !== Array)
                        object.devicePublicKey = $util.newBuffer(object.devicePublicKey);
                }
                object.deviceStatus = options.enums === String ? "DEVICE_NEEDS_APPROVAL" : 0;
                object.devicePlatform = "";
                object.clientFormFactor = options.enums === String ? "FF_EMPTY" : 0;
            }
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                object.encryptedDeviceToken = options.bytes === String ? $util.base64.encode(message.encryptedDeviceToken, 0, message.encryptedDeviceToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDeviceToken) : message.encryptedDeviceToken;
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                object.clientVersion = message.clientVersion;
            if (message.deviceName != null && Object.hasOwnProperty.call(message, "deviceName"))
                object.deviceName = message.deviceName;
            if (message.devicePublicKey != null && Object.hasOwnProperty.call(message, "devicePublicKey"))
                object.devicePublicKey = options.bytes === String ? $util.base64.encode(message.devicePublicKey, 0, message.devicePublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.devicePublicKey) : message.devicePublicKey;
            if (message.deviceStatus != null && Object.hasOwnProperty.call(message, "deviceStatus"))
                object.deviceStatus = options.enums === String ? $root.Authentication.DeviceStatus[message.deviceStatus] === undefined ? message.deviceStatus : $root.Authentication.DeviceStatus[message.deviceStatus] : message.deviceStatus;
            if (message.devicePlatform != null && Object.hasOwnProperty.call(message, "devicePlatform"))
                object.devicePlatform = message.devicePlatform;
            if (message.clientFormFactor != null && Object.hasOwnProperty.call(message, "clientFormFactor"))
                object.clientFormFactor = options.enums === String ? $root.Authentication.ClientFormFactor[message.clientFormFactor] === undefined ? message.clientFormFactor : $root.Authentication.ClientFormFactor[message.clientFormFactor] : message.clientFormFactor;
            return object;
        };

        /**
         * Converts this DeviceUpdateRequest to JSON.
         * @function toJSON
         * @memberof Authentication.DeviceUpdateRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeviceUpdateRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeviceUpdateRequest
         * @function getTypeUrl
         * @memberof Authentication.DeviceUpdateRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeviceUpdateRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.DeviceUpdateRequest";
        };

        return DeviceUpdateRequest;
    })();

    Authentication.DeviceUpdateResponse = (function() {

        /**
         * Properties of a DeviceUpdateResponse.
         * @memberof Authentication
         * @interface IDeviceUpdateResponse
         * @property {Uint8Array|null} [encryptedDeviceToken] DeviceUpdateResponse encryptedDeviceToken
         * @property {string|null} [clientVersion] DeviceUpdateResponse clientVersion
         * @property {string|null} [deviceName] DeviceUpdateResponse deviceName
         * @property {Uint8Array|null} [devicePublicKey] DeviceUpdateResponse devicePublicKey
         * @property {Authentication.DeviceStatus|null} [deviceStatus] DeviceUpdateResponse deviceStatus
         * @property {string|null} [devicePlatform] DeviceUpdateResponse devicePlatform
         * @property {Authentication.ClientFormFactor|null} [clientFormFactor] DeviceUpdateResponse clientFormFactor
         */

        /**
         * Constructs a new DeviceUpdateResponse.
         * @memberof Authentication
         * @classdesc Represents a DeviceUpdateResponse.
         * @implements IDeviceUpdateResponse
         * @constructor
         * @param {Authentication.IDeviceUpdateResponse=} [properties] Properties to set
         */
        function DeviceUpdateResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeviceUpdateResponse encryptedDeviceToken.
         * @member {Uint8Array} encryptedDeviceToken
         * @memberof Authentication.DeviceUpdateResponse
         * @instance
         */
        DeviceUpdateResponse.prototype.encryptedDeviceToken = $util.newBuffer([]);

        /**
         * DeviceUpdateResponse clientVersion.
         * @member {string} clientVersion
         * @memberof Authentication.DeviceUpdateResponse
         * @instance
         */
        DeviceUpdateResponse.prototype.clientVersion = "";

        /**
         * DeviceUpdateResponse deviceName.
         * @member {string} deviceName
         * @memberof Authentication.DeviceUpdateResponse
         * @instance
         */
        DeviceUpdateResponse.prototype.deviceName = "";

        /**
         * DeviceUpdateResponse devicePublicKey.
         * @member {Uint8Array} devicePublicKey
         * @memberof Authentication.DeviceUpdateResponse
         * @instance
         */
        DeviceUpdateResponse.prototype.devicePublicKey = $util.newBuffer([]);

        /**
         * DeviceUpdateResponse deviceStatus.
         * @member {Authentication.DeviceStatus} deviceStatus
         * @memberof Authentication.DeviceUpdateResponse
         * @instance
         */
        DeviceUpdateResponse.prototype.deviceStatus = 0;

        /**
         * DeviceUpdateResponse devicePlatform.
         * @member {string} devicePlatform
         * @memberof Authentication.DeviceUpdateResponse
         * @instance
         */
        DeviceUpdateResponse.prototype.devicePlatform = "";

        /**
         * DeviceUpdateResponse clientFormFactor.
         * @member {Authentication.ClientFormFactor} clientFormFactor
         * @memberof Authentication.DeviceUpdateResponse
         * @instance
         */
        DeviceUpdateResponse.prototype.clientFormFactor = 0;

        /**
         * Creates a new DeviceUpdateResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.DeviceUpdateResponse
         * @static
         * @param {Authentication.IDeviceUpdateResponse=} [properties] Properties to set
         * @returns {Authentication.DeviceUpdateResponse} DeviceUpdateResponse instance
         */
        DeviceUpdateResponse.create = function create(properties) {
            return new DeviceUpdateResponse(properties);
        };

        /**
         * Encodes the specified DeviceUpdateResponse message. Does not implicitly {@link Authentication.DeviceUpdateResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.DeviceUpdateResponse
         * @static
         * @param {Authentication.IDeviceUpdateResponse} message DeviceUpdateResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceUpdateResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedDeviceToken);
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.clientVersion);
            if (message.deviceName != null && Object.hasOwnProperty.call(message, "deviceName"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.deviceName);
            if (message.devicePublicKey != null && Object.hasOwnProperty.call(message, "devicePublicKey"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.devicePublicKey);
            if (message.deviceStatus != null && Object.hasOwnProperty.call(message, "deviceStatus"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.deviceStatus);
            if (message.devicePlatform != null && Object.hasOwnProperty.call(message, "devicePlatform"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.devicePlatform);
            if (message.clientFormFactor != null && Object.hasOwnProperty.call(message, "clientFormFactor"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.clientFormFactor);
            return writer;
        };

        /**
         * Decodes a DeviceUpdateResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.DeviceUpdateResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.DeviceUpdateResponse} DeviceUpdateResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceUpdateResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.DeviceUpdateResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedDeviceToken = reader.bytes();
                        break;
                    }
                case 2: {
                        message.clientVersion = reader.string();
                        break;
                    }
                case 3: {
                        message.deviceName = reader.string();
                        break;
                    }
                case 4: {
                        message.devicePublicKey = reader.bytes();
                        break;
                    }
                case 5: {
                        message.deviceStatus = reader.int32();
                        break;
                    }
                case 6: {
                        message.devicePlatform = reader.string();
                        break;
                    }
                case 7: {
                        message.clientFormFactor = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a DeviceUpdateResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.DeviceUpdateResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.DeviceUpdateResponse} DeviceUpdateResponse
         */
        DeviceUpdateResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.DeviceUpdateResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.DeviceUpdateResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.DeviceUpdateResponse();
            if (object.encryptedDeviceToken != null)
                if (typeof object.encryptedDeviceToken === "string")
                    $util.base64.decode(object.encryptedDeviceToken, message.encryptedDeviceToken = $util.newBuffer($util.base64.length(object.encryptedDeviceToken)), 0);
                else if (object.encryptedDeviceToken.length >= 0)
                    message.encryptedDeviceToken = object.encryptedDeviceToken;
            if (object.clientVersion != null)
                message.clientVersion = String(object.clientVersion);
            if (object.deviceName != null)
                message.deviceName = String(object.deviceName);
            if (object.devicePublicKey != null)
                if (typeof object.devicePublicKey === "string")
                    $util.base64.decode(object.devicePublicKey, message.devicePublicKey = $util.newBuffer($util.base64.length(object.devicePublicKey)), 0);
                else if (object.devicePublicKey.length >= 0)
                    message.devicePublicKey = object.devicePublicKey;
            switch (object.deviceStatus) {
            default:
                if (typeof object.deviceStatus === "number") {
                    message.deviceStatus = object.deviceStatus;
                    break;
                }
                break;
            case "DEVICE_NEEDS_APPROVAL":
            case 0:
                message.deviceStatus = 0;
                break;
            case "DEVICE_OK":
            case 1:
                message.deviceStatus = 1;
                break;
            case "DEVICE_DISABLED_BY_USER":
            case 2:
                message.deviceStatus = 2;
                break;
            case "DEVICE_LOCKED_BY_ADMIN":
            case 3:
                message.deviceStatus = 3;
                break;
            }
            if (object.devicePlatform != null)
                message.devicePlatform = String(object.devicePlatform);
            switch (object.clientFormFactor) {
            default:
                if (typeof object.clientFormFactor === "number") {
                    message.clientFormFactor = object.clientFormFactor;
                    break;
                }
                break;
            case "FF_EMPTY":
            case 0:
                message.clientFormFactor = 0;
                break;
            case "FF_PHONE":
            case 1:
                message.clientFormFactor = 1;
                break;
            case "FF_TABLET":
            case 2:
                message.clientFormFactor = 2;
                break;
            case "FF_WATCH":
            case 3:
                message.clientFormFactor = 3;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a DeviceUpdateResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.DeviceUpdateResponse
         * @static
         * @param {Authentication.DeviceUpdateResponse} message DeviceUpdateResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeviceUpdateResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.encryptedDeviceToken = "";
                else {
                    object.encryptedDeviceToken = [];
                    if (options.bytes !== Array)
                        object.encryptedDeviceToken = $util.newBuffer(object.encryptedDeviceToken);
                }
                object.clientVersion = "";
                object.deviceName = "";
                if (options.bytes === String)
                    object.devicePublicKey = "";
                else {
                    object.devicePublicKey = [];
                    if (options.bytes !== Array)
                        object.devicePublicKey = $util.newBuffer(object.devicePublicKey);
                }
                object.deviceStatus = options.enums === String ? "DEVICE_NEEDS_APPROVAL" : 0;
                object.devicePlatform = "";
                object.clientFormFactor = options.enums === String ? "FF_EMPTY" : 0;
            }
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                object.encryptedDeviceToken = options.bytes === String ? $util.base64.encode(message.encryptedDeviceToken, 0, message.encryptedDeviceToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDeviceToken) : message.encryptedDeviceToken;
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                object.clientVersion = message.clientVersion;
            if (message.deviceName != null && Object.hasOwnProperty.call(message, "deviceName"))
                object.deviceName = message.deviceName;
            if (message.devicePublicKey != null && Object.hasOwnProperty.call(message, "devicePublicKey"))
                object.devicePublicKey = options.bytes === String ? $util.base64.encode(message.devicePublicKey, 0, message.devicePublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.devicePublicKey) : message.devicePublicKey;
            if (message.deviceStatus != null && Object.hasOwnProperty.call(message, "deviceStatus"))
                object.deviceStatus = options.enums === String ? $root.Authentication.DeviceStatus[message.deviceStatus] === undefined ? message.deviceStatus : $root.Authentication.DeviceStatus[message.deviceStatus] : message.deviceStatus;
            if (message.devicePlatform != null && Object.hasOwnProperty.call(message, "devicePlatform"))
                object.devicePlatform = message.devicePlatform;
            if (message.clientFormFactor != null && Object.hasOwnProperty.call(message, "clientFormFactor"))
                object.clientFormFactor = options.enums === String ? $root.Authentication.ClientFormFactor[message.clientFormFactor] === undefined ? message.clientFormFactor : $root.Authentication.ClientFormFactor[message.clientFormFactor] : message.clientFormFactor;
            return object;
        };

        /**
         * Converts this DeviceUpdateResponse to JSON.
         * @function toJSON
         * @memberof Authentication.DeviceUpdateResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeviceUpdateResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeviceUpdateResponse
         * @function getTypeUrl
         * @memberof Authentication.DeviceUpdateResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeviceUpdateResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.DeviceUpdateResponse";
        };

        return DeviceUpdateResponse;
    })();

    Authentication.RegisterDeviceInRegionRequest = (function() {

        /**
         * Properties of a RegisterDeviceInRegionRequest.
         * @memberof Authentication
         * @interface IRegisterDeviceInRegionRequest
         * @property {Uint8Array|null} [encryptedDeviceToken] RegisterDeviceInRegionRequest encryptedDeviceToken
         * @property {string|null} [clientVersion] RegisterDeviceInRegionRequest clientVersion
         * @property {string|null} [deviceName] RegisterDeviceInRegionRequest deviceName
         * @property {Uint8Array|null} [devicePublicKey] RegisterDeviceInRegionRequest devicePublicKey
         * @property {string|null} [devicePlatform] RegisterDeviceInRegionRequest devicePlatform
         * @property {Authentication.ClientFormFactor|null} [clientFormFactor] RegisterDeviceInRegionRequest clientFormFactor
         */

        /**
         * Constructs a new RegisterDeviceInRegionRequest.
         * @memberof Authentication
         * @classdesc Represents a RegisterDeviceInRegionRequest.
         * @implements IRegisterDeviceInRegionRequest
         * @constructor
         * @param {Authentication.IRegisterDeviceInRegionRequest=} [properties] Properties to set
         */
        function RegisterDeviceInRegionRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RegisterDeviceInRegionRequest encryptedDeviceToken.
         * @member {Uint8Array} encryptedDeviceToken
         * @memberof Authentication.RegisterDeviceInRegionRequest
         * @instance
         */
        RegisterDeviceInRegionRequest.prototype.encryptedDeviceToken = $util.newBuffer([]);

        /**
         * RegisterDeviceInRegionRequest clientVersion.
         * @member {string} clientVersion
         * @memberof Authentication.RegisterDeviceInRegionRequest
         * @instance
         */
        RegisterDeviceInRegionRequest.prototype.clientVersion = "";

        /**
         * RegisterDeviceInRegionRequest deviceName.
         * @member {string} deviceName
         * @memberof Authentication.RegisterDeviceInRegionRequest
         * @instance
         */
        RegisterDeviceInRegionRequest.prototype.deviceName = "";

        /**
         * RegisterDeviceInRegionRequest devicePublicKey.
         * @member {Uint8Array} devicePublicKey
         * @memberof Authentication.RegisterDeviceInRegionRequest
         * @instance
         */
        RegisterDeviceInRegionRequest.prototype.devicePublicKey = $util.newBuffer([]);

        /**
         * RegisterDeviceInRegionRequest devicePlatform.
         * @member {string} devicePlatform
         * @memberof Authentication.RegisterDeviceInRegionRequest
         * @instance
         */
        RegisterDeviceInRegionRequest.prototype.devicePlatform = "";

        /**
         * RegisterDeviceInRegionRequest clientFormFactor.
         * @member {Authentication.ClientFormFactor} clientFormFactor
         * @memberof Authentication.RegisterDeviceInRegionRequest
         * @instance
         */
        RegisterDeviceInRegionRequest.prototype.clientFormFactor = 0;

        /**
         * Creates a new RegisterDeviceInRegionRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.RegisterDeviceInRegionRequest
         * @static
         * @param {Authentication.IRegisterDeviceInRegionRequest=} [properties] Properties to set
         * @returns {Authentication.RegisterDeviceInRegionRequest} RegisterDeviceInRegionRequest instance
         */
        RegisterDeviceInRegionRequest.create = function create(properties) {
            return new RegisterDeviceInRegionRequest(properties);
        };

        /**
         * Encodes the specified RegisterDeviceInRegionRequest message. Does not implicitly {@link Authentication.RegisterDeviceInRegionRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.RegisterDeviceInRegionRequest
         * @static
         * @param {Authentication.IRegisterDeviceInRegionRequest} message RegisterDeviceInRegionRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RegisterDeviceInRegionRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedDeviceToken);
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.clientVersion);
            if (message.deviceName != null && Object.hasOwnProperty.call(message, "deviceName"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.deviceName);
            if (message.devicePublicKey != null && Object.hasOwnProperty.call(message, "devicePublicKey"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.devicePublicKey);
            if (message.devicePlatform != null && Object.hasOwnProperty.call(message, "devicePlatform"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.devicePlatform);
            if (message.clientFormFactor != null && Object.hasOwnProperty.call(message, "clientFormFactor"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.clientFormFactor);
            return writer;
        };

        /**
         * Decodes a RegisterDeviceInRegionRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.RegisterDeviceInRegionRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.RegisterDeviceInRegionRequest} RegisterDeviceInRegionRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RegisterDeviceInRegionRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.RegisterDeviceInRegionRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedDeviceToken = reader.bytes();
                        break;
                    }
                case 2: {
                        message.clientVersion = reader.string();
                        break;
                    }
                case 3: {
                        message.deviceName = reader.string();
                        break;
                    }
                case 4: {
                        message.devicePublicKey = reader.bytes();
                        break;
                    }
                case 5: {
                        message.devicePlatform = reader.string();
                        break;
                    }
                case 6: {
                        message.clientFormFactor = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a RegisterDeviceInRegionRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.RegisterDeviceInRegionRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.RegisterDeviceInRegionRequest} RegisterDeviceInRegionRequest
         */
        RegisterDeviceInRegionRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.RegisterDeviceInRegionRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.RegisterDeviceInRegionRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.RegisterDeviceInRegionRequest();
            if (object.encryptedDeviceToken != null)
                if (typeof object.encryptedDeviceToken === "string")
                    $util.base64.decode(object.encryptedDeviceToken, message.encryptedDeviceToken = $util.newBuffer($util.base64.length(object.encryptedDeviceToken)), 0);
                else if (object.encryptedDeviceToken.length >= 0)
                    message.encryptedDeviceToken = object.encryptedDeviceToken;
            if (object.clientVersion != null)
                message.clientVersion = String(object.clientVersion);
            if (object.deviceName != null)
                message.deviceName = String(object.deviceName);
            if (object.devicePublicKey != null)
                if (typeof object.devicePublicKey === "string")
                    $util.base64.decode(object.devicePublicKey, message.devicePublicKey = $util.newBuffer($util.base64.length(object.devicePublicKey)), 0);
                else if (object.devicePublicKey.length >= 0)
                    message.devicePublicKey = object.devicePublicKey;
            if (object.devicePlatform != null)
                message.devicePlatform = String(object.devicePlatform);
            switch (object.clientFormFactor) {
            default:
                if (typeof object.clientFormFactor === "number") {
                    message.clientFormFactor = object.clientFormFactor;
                    break;
                }
                break;
            case "FF_EMPTY":
            case 0:
                message.clientFormFactor = 0;
                break;
            case "FF_PHONE":
            case 1:
                message.clientFormFactor = 1;
                break;
            case "FF_TABLET":
            case 2:
                message.clientFormFactor = 2;
                break;
            case "FF_WATCH":
            case 3:
                message.clientFormFactor = 3;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a RegisterDeviceInRegionRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.RegisterDeviceInRegionRequest
         * @static
         * @param {Authentication.RegisterDeviceInRegionRequest} message RegisterDeviceInRegionRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RegisterDeviceInRegionRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.encryptedDeviceToken = "";
                else {
                    object.encryptedDeviceToken = [];
                    if (options.bytes !== Array)
                        object.encryptedDeviceToken = $util.newBuffer(object.encryptedDeviceToken);
                }
                object.clientVersion = "";
                object.deviceName = "";
                if (options.bytes === String)
                    object.devicePublicKey = "";
                else {
                    object.devicePublicKey = [];
                    if (options.bytes !== Array)
                        object.devicePublicKey = $util.newBuffer(object.devicePublicKey);
                }
                object.devicePlatform = "";
                object.clientFormFactor = options.enums === String ? "FF_EMPTY" : 0;
            }
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                object.encryptedDeviceToken = options.bytes === String ? $util.base64.encode(message.encryptedDeviceToken, 0, message.encryptedDeviceToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDeviceToken) : message.encryptedDeviceToken;
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                object.clientVersion = message.clientVersion;
            if (message.deviceName != null && Object.hasOwnProperty.call(message, "deviceName"))
                object.deviceName = message.deviceName;
            if (message.devicePublicKey != null && Object.hasOwnProperty.call(message, "devicePublicKey"))
                object.devicePublicKey = options.bytes === String ? $util.base64.encode(message.devicePublicKey, 0, message.devicePublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.devicePublicKey) : message.devicePublicKey;
            if (message.devicePlatform != null && Object.hasOwnProperty.call(message, "devicePlatform"))
                object.devicePlatform = message.devicePlatform;
            if (message.clientFormFactor != null && Object.hasOwnProperty.call(message, "clientFormFactor"))
                object.clientFormFactor = options.enums === String ? $root.Authentication.ClientFormFactor[message.clientFormFactor] === undefined ? message.clientFormFactor : $root.Authentication.ClientFormFactor[message.clientFormFactor] : message.clientFormFactor;
            return object;
        };

        /**
         * Converts this RegisterDeviceInRegionRequest to JSON.
         * @function toJSON
         * @memberof Authentication.RegisterDeviceInRegionRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RegisterDeviceInRegionRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RegisterDeviceInRegionRequest
         * @function getTypeUrl
         * @memberof Authentication.RegisterDeviceInRegionRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RegisterDeviceInRegionRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.RegisterDeviceInRegionRequest";
        };

        return RegisterDeviceInRegionRequest;
    })();

    Authentication.RegistrationRequest = (function() {

        /**
         * Properties of a RegistrationRequest.
         * @memberof Authentication
         * @interface IRegistrationRequest
         * @property {Authentication.IAuthRequest|null} [authRequest] RegistrationRequest authRequest
         * @property {Authentication.IUserAuthRequest|null} [userAuthRequest] RegistrationRequest userAuthRequest
         * @property {Uint8Array|null} [encryptedClientKey] RegistrationRequest encryptedClientKey
         * @property {Uint8Array|null} [encryptedPrivateKey] RegistrationRequest encryptedPrivateKey
         * @property {Uint8Array|null} [publicKey] RegistrationRequest publicKey
         * @property {string|null} [verificationCode] RegistrationRequest verificationCode
         * @property {Uint8Array|null} [deprecatedAuthHashHash] RegistrationRequest deprecatedAuthHashHash
         * @property {Uint8Array|null} [deprecatedEncryptedClientKey] RegistrationRequest deprecatedEncryptedClientKey
         * @property {Uint8Array|null} [deprecatedEncryptedPrivateKey] RegistrationRequest deprecatedEncryptedPrivateKey
         * @property {Uint8Array|null} [deprecatedEncryptionParams] RegistrationRequest deprecatedEncryptionParams
         */

        /**
         * Constructs a new RegistrationRequest.
         * @memberof Authentication
         * @classdesc Represents a RegistrationRequest.
         * @implements IRegistrationRequest
         * @constructor
         * @param {Authentication.IRegistrationRequest=} [properties] Properties to set
         */
        function RegistrationRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RegistrationRequest authRequest.
         * @member {Authentication.IAuthRequest|null|undefined} authRequest
         * @memberof Authentication.RegistrationRequest
         * @instance
         */
        RegistrationRequest.prototype.authRequest = null;

        /**
         * RegistrationRequest userAuthRequest.
         * @member {Authentication.IUserAuthRequest|null|undefined} userAuthRequest
         * @memberof Authentication.RegistrationRequest
         * @instance
         */
        RegistrationRequest.prototype.userAuthRequest = null;

        /**
         * RegistrationRequest encryptedClientKey.
         * @member {Uint8Array} encryptedClientKey
         * @memberof Authentication.RegistrationRequest
         * @instance
         */
        RegistrationRequest.prototype.encryptedClientKey = $util.newBuffer([]);

        /**
         * RegistrationRequest encryptedPrivateKey.
         * @member {Uint8Array} encryptedPrivateKey
         * @memberof Authentication.RegistrationRequest
         * @instance
         */
        RegistrationRequest.prototype.encryptedPrivateKey = $util.newBuffer([]);

        /**
         * RegistrationRequest publicKey.
         * @member {Uint8Array} publicKey
         * @memberof Authentication.RegistrationRequest
         * @instance
         */
        RegistrationRequest.prototype.publicKey = $util.newBuffer([]);

        /**
         * RegistrationRequest verificationCode.
         * @member {string} verificationCode
         * @memberof Authentication.RegistrationRequest
         * @instance
         */
        RegistrationRequest.prototype.verificationCode = "";

        /**
         * RegistrationRequest deprecatedAuthHashHash.
         * @member {Uint8Array} deprecatedAuthHashHash
         * @memberof Authentication.RegistrationRequest
         * @instance
         */
        RegistrationRequest.prototype.deprecatedAuthHashHash = $util.newBuffer([]);

        /**
         * RegistrationRequest deprecatedEncryptedClientKey.
         * @member {Uint8Array} deprecatedEncryptedClientKey
         * @memberof Authentication.RegistrationRequest
         * @instance
         */
        RegistrationRequest.prototype.deprecatedEncryptedClientKey = $util.newBuffer([]);

        /**
         * RegistrationRequest deprecatedEncryptedPrivateKey.
         * @member {Uint8Array} deprecatedEncryptedPrivateKey
         * @memberof Authentication.RegistrationRequest
         * @instance
         */
        RegistrationRequest.prototype.deprecatedEncryptedPrivateKey = $util.newBuffer([]);

        /**
         * RegistrationRequest deprecatedEncryptionParams.
         * @member {Uint8Array} deprecatedEncryptionParams
         * @memberof Authentication.RegistrationRequest
         * @instance
         */
        RegistrationRequest.prototype.deprecatedEncryptionParams = $util.newBuffer([]);

        /**
         * Creates a new RegistrationRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.RegistrationRequest
         * @static
         * @param {Authentication.IRegistrationRequest=} [properties] Properties to set
         * @returns {Authentication.RegistrationRequest} RegistrationRequest instance
         */
        RegistrationRequest.create = function create(properties) {
            return new RegistrationRequest(properties);
        };

        /**
         * Encodes the specified RegistrationRequest message. Does not implicitly {@link Authentication.RegistrationRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.RegistrationRequest
         * @static
         * @param {Authentication.IRegistrationRequest} message RegistrationRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RegistrationRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.authRequest != null && Object.hasOwnProperty.call(message, "authRequest"))
                $root.Authentication.AuthRequest.encode(message.authRequest, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.userAuthRequest != null && Object.hasOwnProperty.call(message, "userAuthRequest"))
                $root.Authentication.UserAuthRequest.encode(message.userAuthRequest, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.encryptedClientKey != null && Object.hasOwnProperty.call(message, "encryptedClientKey"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.encryptedClientKey);
            if (message.encryptedPrivateKey != null && Object.hasOwnProperty.call(message, "encryptedPrivateKey"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.encryptedPrivateKey);
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.publicKey);
            if (message.verificationCode != null && Object.hasOwnProperty.call(message, "verificationCode"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.verificationCode);
            if (message.deprecatedAuthHashHash != null && Object.hasOwnProperty.call(message, "deprecatedAuthHashHash"))
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.deprecatedAuthHashHash);
            if (message.deprecatedEncryptedClientKey != null && Object.hasOwnProperty.call(message, "deprecatedEncryptedClientKey"))
                writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.deprecatedEncryptedClientKey);
            if (message.deprecatedEncryptedPrivateKey != null && Object.hasOwnProperty.call(message, "deprecatedEncryptedPrivateKey"))
                writer.uint32(/* id 9, wireType 2 =*/74).bytes(message.deprecatedEncryptedPrivateKey);
            if (message.deprecatedEncryptionParams != null && Object.hasOwnProperty.call(message, "deprecatedEncryptionParams"))
                writer.uint32(/* id 10, wireType 2 =*/82).bytes(message.deprecatedEncryptionParams);
            return writer;
        };

        /**
         * Decodes a RegistrationRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.RegistrationRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.RegistrationRequest} RegistrationRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RegistrationRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.RegistrationRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.authRequest = $root.Authentication.AuthRequest.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        message.userAuthRequest = $root.Authentication.UserAuthRequest.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 3: {
                        message.encryptedClientKey = reader.bytes();
                        break;
                    }
                case 4: {
                        message.encryptedPrivateKey = reader.bytes();
                        break;
                    }
                case 5: {
                        message.publicKey = reader.bytes();
                        break;
                    }
                case 6: {
                        message.verificationCode = reader.string();
                        break;
                    }
                case 7: {
                        message.deprecatedAuthHashHash = reader.bytes();
                        break;
                    }
                case 8: {
                        message.deprecatedEncryptedClientKey = reader.bytes();
                        break;
                    }
                case 9: {
                        message.deprecatedEncryptedPrivateKey = reader.bytes();
                        break;
                    }
                case 10: {
                        message.deprecatedEncryptionParams = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a RegistrationRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.RegistrationRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.RegistrationRequest} RegistrationRequest
         */
        RegistrationRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.RegistrationRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.RegistrationRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.RegistrationRequest();
            if (object.authRequest != null) {
                if (!$util.isObject(object.authRequest))
                    throw TypeError(".Authentication.RegistrationRequest.authRequest: object expected");
                message.authRequest = $root.Authentication.AuthRequest.fromObject(object.authRequest, long + 1);
            }
            if (object.userAuthRequest != null) {
                if (!$util.isObject(object.userAuthRequest))
                    throw TypeError(".Authentication.RegistrationRequest.userAuthRequest: object expected");
                message.userAuthRequest = $root.Authentication.UserAuthRequest.fromObject(object.userAuthRequest, long + 1);
            }
            if (object.encryptedClientKey != null)
                if (typeof object.encryptedClientKey === "string")
                    $util.base64.decode(object.encryptedClientKey, message.encryptedClientKey = $util.newBuffer($util.base64.length(object.encryptedClientKey)), 0);
                else if (object.encryptedClientKey.length >= 0)
                    message.encryptedClientKey = object.encryptedClientKey;
            if (object.encryptedPrivateKey != null)
                if (typeof object.encryptedPrivateKey === "string")
                    $util.base64.decode(object.encryptedPrivateKey, message.encryptedPrivateKey = $util.newBuffer($util.base64.length(object.encryptedPrivateKey)), 0);
                else if (object.encryptedPrivateKey.length >= 0)
                    message.encryptedPrivateKey = object.encryptedPrivateKey;
            if (object.publicKey != null)
                if (typeof object.publicKey === "string")
                    $util.base64.decode(object.publicKey, message.publicKey = $util.newBuffer($util.base64.length(object.publicKey)), 0);
                else if (object.publicKey.length >= 0)
                    message.publicKey = object.publicKey;
            if (object.verificationCode != null)
                message.verificationCode = String(object.verificationCode);
            if (object.deprecatedAuthHashHash != null)
                if (typeof object.deprecatedAuthHashHash === "string")
                    $util.base64.decode(object.deprecatedAuthHashHash, message.deprecatedAuthHashHash = $util.newBuffer($util.base64.length(object.deprecatedAuthHashHash)), 0);
                else if (object.deprecatedAuthHashHash.length >= 0)
                    message.deprecatedAuthHashHash = object.deprecatedAuthHashHash;
            if (object.deprecatedEncryptedClientKey != null)
                if (typeof object.deprecatedEncryptedClientKey === "string")
                    $util.base64.decode(object.deprecatedEncryptedClientKey, message.deprecatedEncryptedClientKey = $util.newBuffer($util.base64.length(object.deprecatedEncryptedClientKey)), 0);
                else if (object.deprecatedEncryptedClientKey.length >= 0)
                    message.deprecatedEncryptedClientKey = object.deprecatedEncryptedClientKey;
            if (object.deprecatedEncryptedPrivateKey != null)
                if (typeof object.deprecatedEncryptedPrivateKey === "string")
                    $util.base64.decode(object.deprecatedEncryptedPrivateKey, message.deprecatedEncryptedPrivateKey = $util.newBuffer($util.base64.length(object.deprecatedEncryptedPrivateKey)), 0);
                else if (object.deprecatedEncryptedPrivateKey.length >= 0)
                    message.deprecatedEncryptedPrivateKey = object.deprecatedEncryptedPrivateKey;
            if (object.deprecatedEncryptionParams != null)
                if (typeof object.deprecatedEncryptionParams === "string")
                    $util.base64.decode(object.deprecatedEncryptionParams, message.deprecatedEncryptionParams = $util.newBuffer($util.base64.length(object.deprecatedEncryptionParams)), 0);
                else if (object.deprecatedEncryptionParams.length >= 0)
                    message.deprecatedEncryptionParams = object.deprecatedEncryptionParams;
            return message;
        };

        /**
         * Creates a plain object from a RegistrationRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.RegistrationRequest
         * @static
         * @param {Authentication.RegistrationRequest} message RegistrationRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RegistrationRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.authRequest = null;
                object.userAuthRequest = null;
                if (options.bytes === String)
                    object.encryptedClientKey = "";
                else {
                    object.encryptedClientKey = [];
                    if (options.bytes !== Array)
                        object.encryptedClientKey = $util.newBuffer(object.encryptedClientKey);
                }
                if (options.bytes === String)
                    object.encryptedPrivateKey = "";
                else {
                    object.encryptedPrivateKey = [];
                    if (options.bytes !== Array)
                        object.encryptedPrivateKey = $util.newBuffer(object.encryptedPrivateKey);
                }
                if (options.bytes === String)
                    object.publicKey = "";
                else {
                    object.publicKey = [];
                    if (options.bytes !== Array)
                        object.publicKey = $util.newBuffer(object.publicKey);
                }
                object.verificationCode = "";
                if (options.bytes === String)
                    object.deprecatedAuthHashHash = "";
                else {
                    object.deprecatedAuthHashHash = [];
                    if (options.bytes !== Array)
                        object.deprecatedAuthHashHash = $util.newBuffer(object.deprecatedAuthHashHash);
                }
                if (options.bytes === String)
                    object.deprecatedEncryptedClientKey = "";
                else {
                    object.deprecatedEncryptedClientKey = [];
                    if (options.bytes !== Array)
                        object.deprecatedEncryptedClientKey = $util.newBuffer(object.deprecatedEncryptedClientKey);
                }
                if (options.bytes === String)
                    object.deprecatedEncryptedPrivateKey = "";
                else {
                    object.deprecatedEncryptedPrivateKey = [];
                    if (options.bytes !== Array)
                        object.deprecatedEncryptedPrivateKey = $util.newBuffer(object.deprecatedEncryptedPrivateKey);
                }
                if (options.bytes === String)
                    object.deprecatedEncryptionParams = "";
                else {
                    object.deprecatedEncryptionParams = [];
                    if (options.bytes !== Array)
                        object.deprecatedEncryptionParams = $util.newBuffer(object.deprecatedEncryptionParams);
                }
            }
            if (message.authRequest != null && Object.hasOwnProperty.call(message, "authRequest"))
                object.authRequest = $root.Authentication.AuthRequest.toObject(message.authRequest, options, q + 1);
            if (message.userAuthRequest != null && Object.hasOwnProperty.call(message, "userAuthRequest"))
                object.userAuthRequest = $root.Authentication.UserAuthRequest.toObject(message.userAuthRequest, options, q + 1);
            if (message.encryptedClientKey != null && Object.hasOwnProperty.call(message, "encryptedClientKey"))
                object.encryptedClientKey = options.bytes === String ? $util.base64.encode(message.encryptedClientKey, 0, message.encryptedClientKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedClientKey) : message.encryptedClientKey;
            if (message.encryptedPrivateKey != null && Object.hasOwnProperty.call(message, "encryptedPrivateKey"))
                object.encryptedPrivateKey = options.bytes === String ? $util.base64.encode(message.encryptedPrivateKey, 0, message.encryptedPrivateKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedPrivateKey) : message.encryptedPrivateKey;
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                object.publicKey = options.bytes === String ? $util.base64.encode(message.publicKey, 0, message.publicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.publicKey) : message.publicKey;
            if (message.verificationCode != null && Object.hasOwnProperty.call(message, "verificationCode"))
                object.verificationCode = message.verificationCode;
            if (message.deprecatedAuthHashHash != null && Object.hasOwnProperty.call(message, "deprecatedAuthHashHash"))
                object.deprecatedAuthHashHash = options.bytes === String ? $util.base64.encode(message.deprecatedAuthHashHash, 0, message.deprecatedAuthHashHash.length) : options.bytes === Array ? Array.prototype.slice.call(message.deprecatedAuthHashHash) : message.deprecatedAuthHashHash;
            if (message.deprecatedEncryptedClientKey != null && Object.hasOwnProperty.call(message, "deprecatedEncryptedClientKey"))
                object.deprecatedEncryptedClientKey = options.bytes === String ? $util.base64.encode(message.deprecatedEncryptedClientKey, 0, message.deprecatedEncryptedClientKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.deprecatedEncryptedClientKey) : message.deprecatedEncryptedClientKey;
            if (message.deprecatedEncryptedPrivateKey != null && Object.hasOwnProperty.call(message, "deprecatedEncryptedPrivateKey"))
                object.deprecatedEncryptedPrivateKey = options.bytes === String ? $util.base64.encode(message.deprecatedEncryptedPrivateKey, 0, message.deprecatedEncryptedPrivateKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.deprecatedEncryptedPrivateKey) : message.deprecatedEncryptedPrivateKey;
            if (message.deprecatedEncryptionParams != null && Object.hasOwnProperty.call(message, "deprecatedEncryptionParams"))
                object.deprecatedEncryptionParams = options.bytes === String ? $util.base64.encode(message.deprecatedEncryptionParams, 0, message.deprecatedEncryptionParams.length) : options.bytes === Array ? Array.prototype.slice.call(message.deprecatedEncryptionParams) : message.deprecatedEncryptionParams;
            return object;
        };

        /**
         * Converts this RegistrationRequest to JSON.
         * @function toJSON
         * @memberof Authentication.RegistrationRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RegistrationRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RegistrationRequest
         * @function getTypeUrl
         * @memberof Authentication.RegistrationRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RegistrationRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.RegistrationRequest";
        };

        return RegistrationRequest;
    })();

    Authentication.ConvertUserToV3Request = (function() {

        /**
         * Properties of a ConvertUserToV3Request.
         * @memberof Authentication
         * @interface IConvertUserToV3Request
         * @property {Authentication.IAuthRequest|null} [authRequest] ConvertUserToV3Request authRequest
         * @property {Authentication.IUserAuthRequest|null} [userAuthRequest] ConvertUserToV3Request userAuthRequest
         * @property {Uint8Array|null} [encryptedClientKey] ConvertUserToV3Request encryptedClientKey
         * @property {Uint8Array|null} [encryptedPrivateKey] ConvertUserToV3Request encryptedPrivateKey
         * @property {Uint8Array|null} [publicKey] ConvertUserToV3Request publicKey
         */

        /**
         * Constructs a new ConvertUserToV3Request.
         * @memberof Authentication
         * @classdesc Represents a ConvertUserToV3Request.
         * @implements IConvertUserToV3Request
         * @constructor
         * @param {Authentication.IConvertUserToV3Request=} [properties] Properties to set
         */
        function ConvertUserToV3Request(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ConvertUserToV3Request authRequest.
         * @member {Authentication.IAuthRequest|null|undefined} authRequest
         * @memberof Authentication.ConvertUserToV3Request
         * @instance
         */
        ConvertUserToV3Request.prototype.authRequest = null;

        /**
         * ConvertUserToV3Request userAuthRequest.
         * @member {Authentication.IUserAuthRequest|null|undefined} userAuthRequest
         * @memberof Authentication.ConvertUserToV3Request
         * @instance
         */
        ConvertUserToV3Request.prototype.userAuthRequest = null;

        /**
         * ConvertUserToV3Request encryptedClientKey.
         * @member {Uint8Array} encryptedClientKey
         * @memberof Authentication.ConvertUserToV3Request
         * @instance
         */
        ConvertUserToV3Request.prototype.encryptedClientKey = $util.newBuffer([]);

        /**
         * ConvertUserToV3Request encryptedPrivateKey.
         * @member {Uint8Array} encryptedPrivateKey
         * @memberof Authentication.ConvertUserToV3Request
         * @instance
         */
        ConvertUserToV3Request.prototype.encryptedPrivateKey = $util.newBuffer([]);

        /**
         * ConvertUserToV3Request publicKey.
         * @member {Uint8Array} publicKey
         * @memberof Authentication.ConvertUserToV3Request
         * @instance
         */
        ConvertUserToV3Request.prototype.publicKey = $util.newBuffer([]);

        /**
         * Creates a new ConvertUserToV3Request instance using the specified properties.
         * @function create
         * @memberof Authentication.ConvertUserToV3Request
         * @static
         * @param {Authentication.IConvertUserToV3Request=} [properties] Properties to set
         * @returns {Authentication.ConvertUserToV3Request} ConvertUserToV3Request instance
         */
        ConvertUserToV3Request.create = function create(properties) {
            return new ConvertUserToV3Request(properties);
        };

        /**
         * Encodes the specified ConvertUserToV3Request message. Does not implicitly {@link Authentication.ConvertUserToV3Request.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ConvertUserToV3Request
         * @static
         * @param {Authentication.IConvertUserToV3Request} message ConvertUserToV3Request message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ConvertUserToV3Request.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.authRequest != null && Object.hasOwnProperty.call(message, "authRequest"))
                $root.Authentication.AuthRequest.encode(message.authRequest, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.userAuthRequest != null && Object.hasOwnProperty.call(message, "userAuthRequest"))
                $root.Authentication.UserAuthRequest.encode(message.userAuthRequest, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.encryptedClientKey != null && Object.hasOwnProperty.call(message, "encryptedClientKey"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.encryptedClientKey);
            if (message.encryptedPrivateKey != null && Object.hasOwnProperty.call(message, "encryptedPrivateKey"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.encryptedPrivateKey);
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.publicKey);
            return writer;
        };

        /**
         * Decodes a ConvertUserToV3Request message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ConvertUserToV3Request
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ConvertUserToV3Request} ConvertUserToV3Request
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ConvertUserToV3Request.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ConvertUserToV3Request();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.authRequest = $root.Authentication.AuthRequest.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        message.userAuthRequest = $root.Authentication.UserAuthRequest.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 3: {
                        message.encryptedClientKey = reader.bytes();
                        break;
                    }
                case 4: {
                        message.encryptedPrivateKey = reader.bytes();
                        break;
                    }
                case 5: {
                        message.publicKey = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a ConvertUserToV3Request message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ConvertUserToV3Request
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ConvertUserToV3Request} ConvertUserToV3Request
         */
        ConvertUserToV3Request.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ConvertUserToV3Request)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ConvertUserToV3Request: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ConvertUserToV3Request();
            if (object.authRequest != null) {
                if (!$util.isObject(object.authRequest))
                    throw TypeError(".Authentication.ConvertUserToV3Request.authRequest: object expected");
                message.authRequest = $root.Authentication.AuthRequest.fromObject(object.authRequest, long + 1);
            }
            if (object.userAuthRequest != null) {
                if (!$util.isObject(object.userAuthRequest))
                    throw TypeError(".Authentication.ConvertUserToV3Request.userAuthRequest: object expected");
                message.userAuthRequest = $root.Authentication.UserAuthRequest.fromObject(object.userAuthRequest, long + 1);
            }
            if (object.encryptedClientKey != null)
                if (typeof object.encryptedClientKey === "string")
                    $util.base64.decode(object.encryptedClientKey, message.encryptedClientKey = $util.newBuffer($util.base64.length(object.encryptedClientKey)), 0);
                else if (object.encryptedClientKey.length >= 0)
                    message.encryptedClientKey = object.encryptedClientKey;
            if (object.encryptedPrivateKey != null)
                if (typeof object.encryptedPrivateKey === "string")
                    $util.base64.decode(object.encryptedPrivateKey, message.encryptedPrivateKey = $util.newBuffer($util.base64.length(object.encryptedPrivateKey)), 0);
                else if (object.encryptedPrivateKey.length >= 0)
                    message.encryptedPrivateKey = object.encryptedPrivateKey;
            if (object.publicKey != null)
                if (typeof object.publicKey === "string")
                    $util.base64.decode(object.publicKey, message.publicKey = $util.newBuffer($util.base64.length(object.publicKey)), 0);
                else if (object.publicKey.length >= 0)
                    message.publicKey = object.publicKey;
            return message;
        };

        /**
         * Creates a plain object from a ConvertUserToV3Request message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ConvertUserToV3Request
         * @static
         * @param {Authentication.ConvertUserToV3Request} message ConvertUserToV3Request
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ConvertUserToV3Request.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.authRequest = null;
                object.userAuthRequest = null;
                if (options.bytes === String)
                    object.encryptedClientKey = "";
                else {
                    object.encryptedClientKey = [];
                    if (options.bytes !== Array)
                        object.encryptedClientKey = $util.newBuffer(object.encryptedClientKey);
                }
                if (options.bytes === String)
                    object.encryptedPrivateKey = "";
                else {
                    object.encryptedPrivateKey = [];
                    if (options.bytes !== Array)
                        object.encryptedPrivateKey = $util.newBuffer(object.encryptedPrivateKey);
                }
                if (options.bytes === String)
                    object.publicKey = "";
                else {
                    object.publicKey = [];
                    if (options.bytes !== Array)
                        object.publicKey = $util.newBuffer(object.publicKey);
                }
            }
            if (message.authRequest != null && Object.hasOwnProperty.call(message, "authRequest"))
                object.authRequest = $root.Authentication.AuthRequest.toObject(message.authRequest, options, q + 1);
            if (message.userAuthRequest != null && Object.hasOwnProperty.call(message, "userAuthRequest"))
                object.userAuthRequest = $root.Authentication.UserAuthRequest.toObject(message.userAuthRequest, options, q + 1);
            if (message.encryptedClientKey != null && Object.hasOwnProperty.call(message, "encryptedClientKey"))
                object.encryptedClientKey = options.bytes === String ? $util.base64.encode(message.encryptedClientKey, 0, message.encryptedClientKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedClientKey) : message.encryptedClientKey;
            if (message.encryptedPrivateKey != null && Object.hasOwnProperty.call(message, "encryptedPrivateKey"))
                object.encryptedPrivateKey = options.bytes === String ? $util.base64.encode(message.encryptedPrivateKey, 0, message.encryptedPrivateKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedPrivateKey) : message.encryptedPrivateKey;
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                object.publicKey = options.bytes === String ? $util.base64.encode(message.publicKey, 0, message.publicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.publicKey) : message.publicKey;
            return object;
        };

        /**
         * Converts this ConvertUserToV3Request to JSON.
         * @function toJSON
         * @memberof Authentication.ConvertUserToV3Request
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ConvertUserToV3Request.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ConvertUserToV3Request
         * @function getTypeUrl
         * @memberof Authentication.ConvertUserToV3Request
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ConvertUserToV3Request.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ConvertUserToV3Request";
        };

        return ConvertUserToV3Request;
    })();

    Authentication.RevisionResponse = (function() {

        /**
         * Properties of a RevisionResponse.
         * @memberof Authentication
         * @interface IRevisionResponse
         * @property {number|null} [revision] RevisionResponse revision
         */

        /**
         * Constructs a new RevisionResponse.
         * @memberof Authentication
         * @classdesc Represents a RevisionResponse.
         * @implements IRevisionResponse
         * @constructor
         * @param {Authentication.IRevisionResponse=} [properties] Properties to set
         */
        function RevisionResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RevisionResponse revision.
         * @member {number} revision
         * @memberof Authentication.RevisionResponse
         * @instance
         */
        RevisionResponse.prototype.revision = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new RevisionResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.RevisionResponse
         * @static
         * @param {Authentication.IRevisionResponse=} [properties] Properties to set
         * @returns {Authentication.RevisionResponse} RevisionResponse instance
         */
        RevisionResponse.create = function create(properties) {
            return new RevisionResponse(properties);
        };

        /**
         * Encodes the specified RevisionResponse message. Does not implicitly {@link Authentication.RevisionResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.RevisionResponse
         * @static
         * @param {Authentication.IRevisionResponse} message RevisionResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RevisionResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.revision);
            return writer;
        };

        /**
         * Decodes a RevisionResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.RevisionResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.RevisionResponse} RevisionResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RevisionResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.RevisionResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.revision = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a RevisionResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.RevisionResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.RevisionResponse} RevisionResponse
         */
        RevisionResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.RevisionResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.RevisionResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.RevisionResponse();
            if (object.revision != null)
                if ($util.Long)
                    message.revision = $util.Long.fromValue(object.revision, false);
                else if (typeof object.revision === "string")
                    message.revision = parseInt(object.revision, 10);
                else if (typeof object.revision === "number")
                    message.revision = object.revision;
                else if (typeof object.revision === "object")
                    message.revision = new $util.LongBits(object.revision.low >>> 0, object.revision.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a RevisionResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.RevisionResponse
         * @static
         * @param {Authentication.RevisionResponse} message RevisionResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RevisionResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.revision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.revision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.revision = typeof message.revision === "number" ? BigInt(message.revision) : $util.Long.fromBits(message.revision.low >>> 0, message.revision.high >>> 0, false).toBigInt();
                else if (typeof message.revision === "number")
                    object.revision = options.longs === String ? String(message.revision) : message.revision;
                else
                    object.revision = options.longs === String ? $util.Long.prototype.toString.call(message.revision) : options.longs === Number ? new $util.LongBits(message.revision.low >>> 0, message.revision.high >>> 0).toNumber() : message.revision;
            return object;
        };

        /**
         * Converts this RevisionResponse to JSON.
         * @function toJSON
         * @memberof Authentication.RevisionResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RevisionResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RevisionResponse
         * @function getTypeUrl
         * @memberof Authentication.RevisionResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RevisionResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.RevisionResponse";
        };

        return RevisionResponse;
    })();

    Authentication.ChangeEmailRequest = (function() {

        /**
         * Properties of a ChangeEmailRequest.
         * @memberof Authentication
         * @interface IChangeEmailRequest
         * @property {string|null} [newEmail] ChangeEmailRequest newEmail
         */

        /**
         * Constructs a new ChangeEmailRequest.
         * @memberof Authentication
         * @classdesc Represents a ChangeEmailRequest.
         * @implements IChangeEmailRequest
         * @constructor
         * @param {Authentication.IChangeEmailRequest=} [properties] Properties to set
         */
        function ChangeEmailRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChangeEmailRequest newEmail.
         * @member {string} newEmail
         * @memberof Authentication.ChangeEmailRequest
         * @instance
         */
        ChangeEmailRequest.prototype.newEmail = "";

        /**
         * Creates a new ChangeEmailRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.ChangeEmailRequest
         * @static
         * @param {Authentication.IChangeEmailRequest=} [properties] Properties to set
         * @returns {Authentication.ChangeEmailRequest} ChangeEmailRequest instance
         */
        ChangeEmailRequest.create = function create(properties) {
            return new ChangeEmailRequest(properties);
        };

        /**
         * Encodes the specified ChangeEmailRequest message. Does not implicitly {@link Authentication.ChangeEmailRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ChangeEmailRequest
         * @static
         * @param {Authentication.IChangeEmailRequest} message ChangeEmailRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangeEmailRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.newEmail != null && Object.hasOwnProperty.call(message, "newEmail"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.newEmail);
            return writer;
        };

        /**
         * Decodes a ChangeEmailRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ChangeEmailRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ChangeEmailRequest} ChangeEmailRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangeEmailRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ChangeEmailRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.newEmail = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a ChangeEmailRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ChangeEmailRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ChangeEmailRequest} ChangeEmailRequest
         */
        ChangeEmailRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ChangeEmailRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ChangeEmailRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ChangeEmailRequest();
            if (object.newEmail != null)
                message.newEmail = String(object.newEmail);
            return message;
        };

        /**
         * Creates a plain object from a ChangeEmailRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ChangeEmailRequest
         * @static
         * @param {Authentication.ChangeEmailRequest} message ChangeEmailRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChangeEmailRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.newEmail = "";
            if (message.newEmail != null && Object.hasOwnProperty.call(message, "newEmail"))
                object.newEmail = message.newEmail;
            return object;
        };

        /**
         * Converts this ChangeEmailRequest to JSON.
         * @function toJSON
         * @memberof Authentication.ChangeEmailRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChangeEmailRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChangeEmailRequest
         * @function getTypeUrl
         * @memberof Authentication.ChangeEmailRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChangeEmailRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ChangeEmailRequest";
        };

        return ChangeEmailRequest;
    })();

    Authentication.ChangeEmailResponse = (function() {

        /**
         * Properties of a ChangeEmailResponse.
         * @memberof Authentication
         * @interface IChangeEmailResponse
         * @property {Uint8Array|null} [encryptedChangeEmailToken] ChangeEmailResponse encryptedChangeEmailToken
         */

        /**
         * Constructs a new ChangeEmailResponse.
         * @memberof Authentication
         * @classdesc Represents a ChangeEmailResponse.
         * @implements IChangeEmailResponse
         * @constructor
         * @param {Authentication.IChangeEmailResponse=} [properties] Properties to set
         */
        function ChangeEmailResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChangeEmailResponse encryptedChangeEmailToken.
         * @member {Uint8Array} encryptedChangeEmailToken
         * @memberof Authentication.ChangeEmailResponse
         * @instance
         */
        ChangeEmailResponse.prototype.encryptedChangeEmailToken = $util.newBuffer([]);

        /**
         * Creates a new ChangeEmailResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.ChangeEmailResponse
         * @static
         * @param {Authentication.IChangeEmailResponse=} [properties] Properties to set
         * @returns {Authentication.ChangeEmailResponse} ChangeEmailResponse instance
         */
        ChangeEmailResponse.create = function create(properties) {
            return new ChangeEmailResponse(properties);
        };

        /**
         * Encodes the specified ChangeEmailResponse message. Does not implicitly {@link Authentication.ChangeEmailResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ChangeEmailResponse
         * @static
         * @param {Authentication.IChangeEmailResponse} message ChangeEmailResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangeEmailResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedChangeEmailToken != null && Object.hasOwnProperty.call(message, "encryptedChangeEmailToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedChangeEmailToken);
            return writer;
        };

        /**
         * Decodes a ChangeEmailResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ChangeEmailResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ChangeEmailResponse} ChangeEmailResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangeEmailResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ChangeEmailResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedChangeEmailToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a ChangeEmailResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ChangeEmailResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ChangeEmailResponse} ChangeEmailResponse
         */
        ChangeEmailResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ChangeEmailResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ChangeEmailResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ChangeEmailResponse();
            if (object.encryptedChangeEmailToken != null)
                if (typeof object.encryptedChangeEmailToken === "string")
                    $util.base64.decode(object.encryptedChangeEmailToken, message.encryptedChangeEmailToken = $util.newBuffer($util.base64.length(object.encryptedChangeEmailToken)), 0);
                else if (object.encryptedChangeEmailToken.length >= 0)
                    message.encryptedChangeEmailToken = object.encryptedChangeEmailToken;
            return message;
        };

        /**
         * Creates a plain object from a ChangeEmailResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ChangeEmailResponse
         * @static
         * @param {Authentication.ChangeEmailResponse} message ChangeEmailResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChangeEmailResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === String)
                    object.encryptedChangeEmailToken = "";
                else {
                    object.encryptedChangeEmailToken = [];
                    if (options.bytes !== Array)
                        object.encryptedChangeEmailToken = $util.newBuffer(object.encryptedChangeEmailToken);
                }
            if (message.encryptedChangeEmailToken != null && Object.hasOwnProperty.call(message, "encryptedChangeEmailToken"))
                object.encryptedChangeEmailToken = options.bytes === String ? $util.base64.encode(message.encryptedChangeEmailToken, 0, message.encryptedChangeEmailToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedChangeEmailToken) : message.encryptedChangeEmailToken;
            return object;
        };

        /**
         * Converts this ChangeEmailResponse to JSON.
         * @function toJSON
         * @memberof Authentication.ChangeEmailResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChangeEmailResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChangeEmailResponse
         * @function getTypeUrl
         * @memberof Authentication.ChangeEmailResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChangeEmailResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ChangeEmailResponse";
        };

        return ChangeEmailResponse;
    })();

    Authentication.EmailVerificationLinkResponse = (function() {

        /**
         * Properties of an EmailVerificationLinkResponse.
         * @memberof Authentication
         * @interface IEmailVerificationLinkResponse
         * @property {boolean|null} [emailVerified] EmailVerificationLinkResponse emailVerified
         */

        /**
         * Constructs a new EmailVerificationLinkResponse.
         * @memberof Authentication
         * @classdesc Represents an EmailVerificationLinkResponse.
         * @implements IEmailVerificationLinkResponse
         * @constructor
         * @param {Authentication.IEmailVerificationLinkResponse=} [properties] Properties to set
         */
        function EmailVerificationLinkResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EmailVerificationLinkResponse emailVerified.
         * @member {boolean} emailVerified
         * @memberof Authentication.EmailVerificationLinkResponse
         * @instance
         */
        EmailVerificationLinkResponse.prototype.emailVerified = false;

        /**
         * Creates a new EmailVerificationLinkResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.EmailVerificationLinkResponse
         * @static
         * @param {Authentication.IEmailVerificationLinkResponse=} [properties] Properties to set
         * @returns {Authentication.EmailVerificationLinkResponse} EmailVerificationLinkResponse instance
         */
        EmailVerificationLinkResponse.create = function create(properties) {
            return new EmailVerificationLinkResponse(properties);
        };

        /**
         * Encodes the specified EmailVerificationLinkResponse message. Does not implicitly {@link Authentication.EmailVerificationLinkResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.EmailVerificationLinkResponse
         * @static
         * @param {Authentication.IEmailVerificationLinkResponse} message EmailVerificationLinkResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EmailVerificationLinkResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.emailVerified != null && Object.hasOwnProperty.call(message, "emailVerified"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.emailVerified);
            return writer;
        };

        /**
         * Decodes an EmailVerificationLinkResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.EmailVerificationLinkResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.EmailVerificationLinkResponse} EmailVerificationLinkResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EmailVerificationLinkResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.EmailVerificationLinkResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.emailVerified = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an EmailVerificationLinkResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.EmailVerificationLinkResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.EmailVerificationLinkResponse} EmailVerificationLinkResponse
         */
        EmailVerificationLinkResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.EmailVerificationLinkResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.EmailVerificationLinkResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.EmailVerificationLinkResponse();
            if (object.emailVerified != null)
                message.emailVerified = Boolean(object.emailVerified);
            return message;
        };

        /**
         * Creates a plain object from an EmailVerificationLinkResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.EmailVerificationLinkResponse
         * @static
         * @param {Authentication.EmailVerificationLinkResponse} message EmailVerificationLinkResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EmailVerificationLinkResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.emailVerified = false;
            if (message.emailVerified != null && Object.hasOwnProperty.call(message, "emailVerified"))
                object.emailVerified = message.emailVerified;
            return object;
        };

        /**
         * Converts this EmailVerificationLinkResponse to JSON.
         * @function toJSON
         * @memberof Authentication.EmailVerificationLinkResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EmailVerificationLinkResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EmailVerificationLinkResponse
         * @function getTypeUrl
         * @memberof Authentication.EmailVerificationLinkResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EmailVerificationLinkResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.EmailVerificationLinkResponse";
        };

        return EmailVerificationLinkResponse;
    })();

    Authentication.SecurityData = (function() {

        /**
         * Properties of a SecurityData.
         * @memberof Authentication
         * @interface ISecurityData
         * @property {Uint8Array|null} [uid] SecurityData uid
         * @property {Uint8Array|null} [data] SecurityData data
         */

        /**
         * Constructs a new SecurityData.
         * @memberof Authentication
         * @classdesc Represents a SecurityData.
         * @implements ISecurityData
         * @constructor
         * @param {Authentication.ISecurityData=} [properties] Properties to set
         */
        function SecurityData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SecurityData uid.
         * @member {Uint8Array} uid
         * @memberof Authentication.SecurityData
         * @instance
         */
        SecurityData.prototype.uid = $util.newBuffer([]);

        /**
         * SecurityData data.
         * @member {Uint8Array} data
         * @memberof Authentication.SecurityData
         * @instance
         */
        SecurityData.prototype.data = $util.newBuffer([]);

        /**
         * Creates a new SecurityData instance using the specified properties.
         * @function create
         * @memberof Authentication.SecurityData
         * @static
         * @param {Authentication.ISecurityData=} [properties] Properties to set
         * @returns {Authentication.SecurityData} SecurityData instance
         */
        SecurityData.create = function create(properties) {
            return new SecurityData(properties);
        };

        /**
         * Encodes the specified SecurityData message. Does not implicitly {@link Authentication.SecurityData.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SecurityData
         * @static
         * @param {Authentication.ISecurityData} message SecurityData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SecurityData.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.uid);
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.data);
            return writer;
        };

        /**
         * Decodes a SecurityData message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SecurityData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SecurityData} SecurityData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SecurityData.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SecurityData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.data = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SecurityData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SecurityData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SecurityData} SecurityData
         */
        SecurityData.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SecurityData)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SecurityData: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SecurityData();
            if (object.uid != null)
                if (typeof object.uid === "string")
                    $util.base64.decode(object.uid, message.uid = $util.newBuffer($util.base64.length(object.uid)), 0);
                else if (object.uid.length >= 0)
                    message.uid = object.uid;
            if (object.data != null)
                if (typeof object.data === "string")
                    $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                else if (object.data.length >= 0)
                    message.data = object.data;
            return message;
        };

        /**
         * Creates a plain object from a SecurityData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SecurityData
         * @static
         * @param {Authentication.SecurityData} message SecurityData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SecurityData.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.uid = "";
                else {
                    object.uid = [];
                    if (options.bytes !== Array)
                        object.uid = $util.newBuffer(object.uid);
                }
                if (options.bytes === String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== Array)
                        object.data = $util.newBuffer(object.data);
                }
            }
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                object.uid = options.bytes === String ? $util.base64.encode(message.uid, 0, message.uid.length) : options.bytes === Array ? Array.prototype.slice.call(message.uid) : message.uid;
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            return object;
        };

        /**
         * Converts this SecurityData to JSON.
         * @function toJSON
         * @memberof Authentication.SecurityData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SecurityData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SecurityData
         * @function getTypeUrl
         * @memberof Authentication.SecurityData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SecurityData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SecurityData";
        };

        return SecurityData;
    })();

    Authentication.SecurityScoreData = (function() {

        /**
         * Properties of a SecurityScoreData.
         * @memberof Authentication
         * @interface ISecurityScoreData
         * @property {Uint8Array|null} [uid] SecurityScoreData uid
         * @property {Uint8Array|null} [data] SecurityScoreData data
         * @property {number|null} [revision] SecurityScoreData revision
         */

        /**
         * Constructs a new SecurityScoreData.
         * @memberof Authentication
         * @classdesc Represents a SecurityScoreData.
         * @implements ISecurityScoreData
         * @constructor
         * @param {Authentication.ISecurityScoreData=} [properties] Properties to set
         */
        function SecurityScoreData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SecurityScoreData uid.
         * @member {Uint8Array} uid
         * @memberof Authentication.SecurityScoreData
         * @instance
         */
        SecurityScoreData.prototype.uid = $util.newBuffer([]);

        /**
         * SecurityScoreData data.
         * @member {Uint8Array} data
         * @memberof Authentication.SecurityScoreData
         * @instance
         */
        SecurityScoreData.prototype.data = $util.newBuffer([]);

        /**
         * SecurityScoreData revision.
         * @member {number} revision
         * @memberof Authentication.SecurityScoreData
         * @instance
         */
        SecurityScoreData.prototype.revision = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new SecurityScoreData instance using the specified properties.
         * @function create
         * @memberof Authentication.SecurityScoreData
         * @static
         * @param {Authentication.ISecurityScoreData=} [properties] Properties to set
         * @returns {Authentication.SecurityScoreData} SecurityScoreData instance
         */
        SecurityScoreData.create = function create(properties) {
            return new SecurityScoreData(properties);
        };

        /**
         * Encodes the specified SecurityScoreData message. Does not implicitly {@link Authentication.SecurityScoreData.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SecurityScoreData
         * @static
         * @param {Authentication.ISecurityScoreData} message SecurityScoreData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SecurityScoreData.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.uid);
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.data);
            if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.revision);
            return writer;
        };

        /**
         * Decodes a SecurityScoreData message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SecurityScoreData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SecurityScoreData} SecurityScoreData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SecurityScoreData.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SecurityScoreData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.data = reader.bytes();
                        break;
                    }
                case 3: {
                        message.revision = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SecurityScoreData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SecurityScoreData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SecurityScoreData} SecurityScoreData
         */
        SecurityScoreData.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SecurityScoreData)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SecurityScoreData: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SecurityScoreData();
            if (object.uid != null)
                if (typeof object.uid === "string")
                    $util.base64.decode(object.uid, message.uid = $util.newBuffer($util.base64.length(object.uid)), 0);
                else if (object.uid.length >= 0)
                    message.uid = object.uid;
            if (object.data != null)
                if (typeof object.data === "string")
                    $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                else if (object.data.length >= 0)
                    message.data = object.data;
            if (object.revision != null)
                if ($util.Long)
                    message.revision = $util.Long.fromValue(object.revision, false);
                else if (typeof object.revision === "string")
                    message.revision = parseInt(object.revision, 10);
                else if (typeof object.revision === "number")
                    message.revision = object.revision;
                else if (typeof object.revision === "object")
                    message.revision = new $util.LongBits(object.revision.low >>> 0, object.revision.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a SecurityScoreData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SecurityScoreData
         * @static
         * @param {Authentication.SecurityScoreData} message SecurityScoreData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SecurityScoreData.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.uid = "";
                else {
                    object.uid = [];
                    if (options.bytes !== Array)
                        object.uid = $util.newBuffer(object.uid);
                }
                if (options.bytes === String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== Array)
                        object.data = $util.newBuffer(object.data);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.revision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.revision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                object.uid = options.bytes === String ? $util.base64.encode(message.uid, 0, message.uid.length) : options.bytes === Array ? Array.prototype.slice.call(message.uid) : message.uid;
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.revision = typeof message.revision === "number" ? BigInt(message.revision) : $util.Long.fromBits(message.revision.low >>> 0, message.revision.high >>> 0, false).toBigInt();
                else if (typeof message.revision === "number")
                    object.revision = options.longs === String ? String(message.revision) : message.revision;
                else
                    object.revision = options.longs === String ? $util.Long.prototype.toString.call(message.revision) : options.longs === Number ? new $util.LongBits(message.revision.low >>> 0, message.revision.high >>> 0).toNumber() : message.revision;
            return object;
        };

        /**
         * Converts this SecurityScoreData to JSON.
         * @function toJSON
         * @memberof Authentication.SecurityScoreData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SecurityScoreData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SecurityScoreData
         * @function getTypeUrl
         * @memberof Authentication.SecurityScoreData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SecurityScoreData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SecurityScoreData";
        };

        return SecurityScoreData;
    })();

    Authentication.SecurityDataRequest = (function() {

        /**
         * Properties of a SecurityDataRequest.
         * @memberof Authentication
         * @interface ISecurityDataRequest
         * @property {Array.<Authentication.ISecurityData>|null} [recordSecurityData] SecurityDataRequest recordSecurityData
         * @property {Array.<Authentication.ISecurityData>|null} [masterPasswordSecurityData] SecurityDataRequest masterPasswordSecurityData
         * @property {Enterprise.EncryptedKeyType|null} [encryptionType] SecurityDataRequest encryptionType
         * @property {Array.<Authentication.ISecurityScoreData>|null} [recordSecurityScoreData] SecurityDataRequest recordSecurityScoreData
         */

        /**
         * Constructs a new SecurityDataRequest.
         * @memberof Authentication
         * @classdesc Represents a SecurityDataRequest.
         * @implements ISecurityDataRequest
         * @constructor
         * @param {Authentication.ISecurityDataRequest=} [properties] Properties to set
         */
        function SecurityDataRequest(properties) {
            this.recordSecurityData = [];
            this.masterPasswordSecurityData = [];
            this.recordSecurityScoreData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SecurityDataRequest recordSecurityData.
         * @member {Array.<Authentication.ISecurityData>} recordSecurityData
         * @memberof Authentication.SecurityDataRequest
         * @instance
         */
        SecurityDataRequest.prototype.recordSecurityData = $util.emptyArray;

        /**
         * SecurityDataRequest masterPasswordSecurityData.
         * @member {Array.<Authentication.ISecurityData>} masterPasswordSecurityData
         * @memberof Authentication.SecurityDataRequest
         * @instance
         */
        SecurityDataRequest.prototype.masterPasswordSecurityData = $util.emptyArray;

        /**
         * SecurityDataRequest encryptionType.
         * @member {Enterprise.EncryptedKeyType} encryptionType
         * @memberof Authentication.SecurityDataRequest
         * @instance
         */
        SecurityDataRequest.prototype.encryptionType = 0;

        /**
         * SecurityDataRequest recordSecurityScoreData.
         * @member {Array.<Authentication.ISecurityScoreData>} recordSecurityScoreData
         * @memberof Authentication.SecurityDataRequest
         * @instance
         */
        SecurityDataRequest.prototype.recordSecurityScoreData = $util.emptyArray;

        /**
         * Creates a new SecurityDataRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.SecurityDataRequest
         * @static
         * @param {Authentication.ISecurityDataRequest=} [properties] Properties to set
         * @returns {Authentication.SecurityDataRequest} SecurityDataRequest instance
         */
        SecurityDataRequest.create = function create(properties) {
            return new SecurityDataRequest(properties);
        };

        /**
         * Encodes the specified SecurityDataRequest message. Does not implicitly {@link Authentication.SecurityDataRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SecurityDataRequest
         * @static
         * @param {Authentication.ISecurityDataRequest} message SecurityDataRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SecurityDataRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.recordSecurityData != null && message.recordSecurityData.length)
                for (let i = 0; i < message.recordSecurityData.length; ++i)
                    $root.Authentication.SecurityData.encode(message.recordSecurityData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.masterPasswordSecurityData != null && message.masterPasswordSecurityData.length)
                for (let i = 0; i < message.masterPasswordSecurityData.length; ++i)
                    $root.Authentication.SecurityData.encode(message.masterPasswordSecurityData[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.encryptionType != null && Object.hasOwnProperty.call(message, "encryptionType"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.encryptionType);
            if (message.recordSecurityScoreData != null && message.recordSecurityScoreData.length)
                for (let i = 0; i < message.recordSecurityScoreData.length; ++i)
                    $root.Authentication.SecurityScoreData.encode(message.recordSecurityScoreData[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes a SecurityDataRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SecurityDataRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SecurityDataRequest} SecurityDataRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SecurityDataRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SecurityDataRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.recordSecurityData && message.recordSecurityData.length))
                            message.recordSecurityData = [];
                        message.recordSecurityData.push($root.Authentication.SecurityData.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 2: {
                        if (!(message.masterPasswordSecurityData && message.masterPasswordSecurityData.length))
                            message.masterPasswordSecurityData = [];
                        message.masterPasswordSecurityData.push($root.Authentication.SecurityData.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 3: {
                        message.encryptionType = reader.int32();
                        break;
                    }
                case 4: {
                        if (!(message.recordSecurityScoreData && message.recordSecurityScoreData.length))
                            message.recordSecurityScoreData = [];
                        message.recordSecurityScoreData.push($root.Authentication.SecurityScoreData.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SecurityDataRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SecurityDataRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SecurityDataRequest} SecurityDataRequest
         */
        SecurityDataRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SecurityDataRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SecurityDataRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SecurityDataRequest();
            if (object.recordSecurityData) {
                if (!Array.isArray(object.recordSecurityData))
                    throw TypeError(".Authentication.SecurityDataRequest.recordSecurityData: array expected");
                message.recordSecurityData = [];
                for (let i = 0; i < object.recordSecurityData.length; ++i) {
                    if (!$util.isObject(object.recordSecurityData[i]))
                        throw TypeError(".Authentication.SecurityDataRequest.recordSecurityData: object expected");
                    message.recordSecurityData[i] = $root.Authentication.SecurityData.fromObject(object.recordSecurityData[i], long + 1);
                }
            }
            if (object.masterPasswordSecurityData) {
                if (!Array.isArray(object.masterPasswordSecurityData))
                    throw TypeError(".Authentication.SecurityDataRequest.masterPasswordSecurityData: array expected");
                message.masterPasswordSecurityData = [];
                for (let i = 0; i < object.masterPasswordSecurityData.length; ++i) {
                    if (!$util.isObject(object.masterPasswordSecurityData[i]))
                        throw TypeError(".Authentication.SecurityDataRequest.masterPasswordSecurityData: object expected");
                    message.masterPasswordSecurityData[i] = $root.Authentication.SecurityData.fromObject(object.masterPasswordSecurityData[i], long + 1);
                }
            }
            switch (object.encryptionType) {
            default:
                if (typeof object.encryptionType === "number") {
                    message.encryptionType = object.encryptionType;
                    break;
                }
                break;
            case "KT_NO_KEY":
            case 0:
                message.encryptionType = 0;
                break;
            case "KT_ENCRYPTED_BY_DATA_KEY":
            case 1:
                message.encryptionType = 1;
                break;
            case "KT_ENCRYPTED_BY_PUBLIC_KEY":
            case 2:
                message.encryptionType = 2;
                break;
            case "KT_ENCRYPTED_BY_DATA_KEY_GCM":
            case 3:
                message.encryptionType = 3;
                break;
            case "KT_ENCRYPTED_BY_PUBLIC_KEY_ECC":
            case 4:
                message.encryptionType = 4;
                break;
            }
            if (object.recordSecurityScoreData) {
                if (!Array.isArray(object.recordSecurityScoreData))
                    throw TypeError(".Authentication.SecurityDataRequest.recordSecurityScoreData: array expected");
                message.recordSecurityScoreData = [];
                for (let i = 0; i < object.recordSecurityScoreData.length; ++i) {
                    if (!$util.isObject(object.recordSecurityScoreData[i]))
                        throw TypeError(".Authentication.SecurityDataRequest.recordSecurityScoreData: object expected");
                    message.recordSecurityScoreData[i] = $root.Authentication.SecurityScoreData.fromObject(object.recordSecurityScoreData[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SecurityDataRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SecurityDataRequest
         * @static
         * @param {Authentication.SecurityDataRequest} message SecurityDataRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SecurityDataRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.recordSecurityData = [];
                object.masterPasswordSecurityData = [];
                object.recordSecurityScoreData = [];
            }
            if (options.defaults)
                object.encryptionType = options.enums === String ? "KT_NO_KEY" : 0;
            if (message.recordSecurityData && message.recordSecurityData.length) {
                object.recordSecurityData = [];
                for (let j = 0; j < message.recordSecurityData.length; ++j)
                    object.recordSecurityData[j] = $root.Authentication.SecurityData.toObject(message.recordSecurityData[j], options, q + 1);
            }
            if (message.masterPasswordSecurityData && message.masterPasswordSecurityData.length) {
                object.masterPasswordSecurityData = [];
                for (let j = 0; j < message.masterPasswordSecurityData.length; ++j)
                    object.masterPasswordSecurityData[j] = $root.Authentication.SecurityData.toObject(message.masterPasswordSecurityData[j], options, q + 1);
            }
            if (message.encryptionType != null && Object.hasOwnProperty.call(message, "encryptionType"))
                object.encryptionType = options.enums === String ? $root.Enterprise.EncryptedKeyType[message.encryptionType] === undefined ? message.encryptionType : $root.Enterprise.EncryptedKeyType[message.encryptionType] : message.encryptionType;
            if (message.recordSecurityScoreData && message.recordSecurityScoreData.length) {
                object.recordSecurityScoreData = [];
                for (let j = 0; j < message.recordSecurityScoreData.length; ++j)
                    object.recordSecurityScoreData[j] = $root.Authentication.SecurityScoreData.toObject(message.recordSecurityScoreData[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this SecurityDataRequest to JSON.
         * @function toJSON
         * @memberof Authentication.SecurityDataRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SecurityDataRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SecurityDataRequest
         * @function getTypeUrl
         * @memberof Authentication.SecurityDataRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SecurityDataRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SecurityDataRequest";
        };

        return SecurityDataRequest;
    })();

    Authentication.SecurityReportIncrementalData = (function() {

        /**
         * Properties of a SecurityReportIncrementalData.
         * @memberof Authentication
         * @interface ISecurityReportIncrementalData
         * @property {number|null} [enterpriseUserId] SecurityReportIncrementalData enterpriseUserId
         * @property {Uint8Array|null} [currentSecurityData] SecurityReportIncrementalData currentSecurityData
         * @property {number|null} [currentSecurityDataRevision] SecurityReportIncrementalData currentSecurityDataRevision
         * @property {Uint8Array|null} [oldSecurityData] SecurityReportIncrementalData oldSecurityData
         * @property {number|null} [oldSecurityDataRevision] SecurityReportIncrementalData oldSecurityDataRevision
         * @property {Enterprise.EncryptedKeyType|null} [currentDataEncryptionType] SecurityReportIncrementalData currentDataEncryptionType
         * @property {Enterprise.EncryptedKeyType|null} [oldDataEncryptionType] SecurityReportIncrementalData oldDataEncryptionType
         * @property {Uint8Array|null} [recordUid] SecurityReportIncrementalData recordUid
         */

        /**
         * Constructs a new SecurityReportIncrementalData.
         * @memberof Authentication
         * @classdesc Represents a SecurityReportIncrementalData.
         * @implements ISecurityReportIncrementalData
         * @constructor
         * @param {Authentication.ISecurityReportIncrementalData=} [properties] Properties to set
         */
        function SecurityReportIncrementalData(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SecurityReportIncrementalData enterpriseUserId.
         * @member {number} enterpriseUserId
         * @memberof Authentication.SecurityReportIncrementalData
         * @instance
         */
        SecurityReportIncrementalData.prototype.enterpriseUserId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SecurityReportIncrementalData currentSecurityData.
         * @member {Uint8Array} currentSecurityData
         * @memberof Authentication.SecurityReportIncrementalData
         * @instance
         */
        SecurityReportIncrementalData.prototype.currentSecurityData = $util.newBuffer([]);

        /**
         * SecurityReportIncrementalData currentSecurityDataRevision.
         * @member {number} currentSecurityDataRevision
         * @memberof Authentication.SecurityReportIncrementalData
         * @instance
         */
        SecurityReportIncrementalData.prototype.currentSecurityDataRevision = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SecurityReportIncrementalData oldSecurityData.
         * @member {Uint8Array} oldSecurityData
         * @memberof Authentication.SecurityReportIncrementalData
         * @instance
         */
        SecurityReportIncrementalData.prototype.oldSecurityData = $util.newBuffer([]);

        /**
         * SecurityReportIncrementalData oldSecurityDataRevision.
         * @member {number} oldSecurityDataRevision
         * @memberof Authentication.SecurityReportIncrementalData
         * @instance
         */
        SecurityReportIncrementalData.prototype.oldSecurityDataRevision = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SecurityReportIncrementalData currentDataEncryptionType.
         * @member {Enterprise.EncryptedKeyType} currentDataEncryptionType
         * @memberof Authentication.SecurityReportIncrementalData
         * @instance
         */
        SecurityReportIncrementalData.prototype.currentDataEncryptionType = 0;

        /**
         * SecurityReportIncrementalData oldDataEncryptionType.
         * @member {Enterprise.EncryptedKeyType} oldDataEncryptionType
         * @memberof Authentication.SecurityReportIncrementalData
         * @instance
         */
        SecurityReportIncrementalData.prototype.oldDataEncryptionType = 0;

        /**
         * SecurityReportIncrementalData recordUid.
         * @member {Uint8Array} recordUid
         * @memberof Authentication.SecurityReportIncrementalData
         * @instance
         */
        SecurityReportIncrementalData.prototype.recordUid = $util.newBuffer([]);

        /**
         * Creates a new SecurityReportIncrementalData instance using the specified properties.
         * @function create
         * @memberof Authentication.SecurityReportIncrementalData
         * @static
         * @param {Authentication.ISecurityReportIncrementalData=} [properties] Properties to set
         * @returns {Authentication.SecurityReportIncrementalData} SecurityReportIncrementalData instance
         */
        SecurityReportIncrementalData.create = function create(properties) {
            return new SecurityReportIncrementalData(properties);
        };

        /**
         * Encodes the specified SecurityReportIncrementalData message. Does not implicitly {@link Authentication.SecurityReportIncrementalData.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SecurityReportIncrementalData
         * @static
         * @param {Authentication.ISecurityReportIncrementalData} message SecurityReportIncrementalData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SecurityReportIncrementalData.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.enterpriseUserId);
            if (message.currentSecurityData != null && Object.hasOwnProperty.call(message, "currentSecurityData"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.currentSecurityData);
            if (message.currentSecurityDataRevision != null && Object.hasOwnProperty.call(message, "currentSecurityDataRevision"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.currentSecurityDataRevision);
            if (message.oldSecurityData != null && Object.hasOwnProperty.call(message, "oldSecurityData"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.oldSecurityData);
            if (message.oldSecurityDataRevision != null && Object.hasOwnProperty.call(message, "oldSecurityDataRevision"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.oldSecurityDataRevision);
            if (message.currentDataEncryptionType != null && Object.hasOwnProperty.call(message, "currentDataEncryptionType"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.currentDataEncryptionType);
            if (message.oldDataEncryptionType != null && Object.hasOwnProperty.call(message, "oldDataEncryptionType"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.oldDataEncryptionType);
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.recordUid);
            return writer;
        };

        /**
         * Decodes a SecurityReportIncrementalData message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SecurityReportIncrementalData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SecurityReportIncrementalData} SecurityReportIncrementalData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SecurityReportIncrementalData.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SecurityReportIncrementalData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.enterpriseUserId = reader.int64();
                        break;
                    }
                case 2: {
                        message.currentSecurityData = reader.bytes();
                        break;
                    }
                case 3: {
                        message.currentSecurityDataRevision = reader.int64();
                        break;
                    }
                case 4: {
                        message.oldSecurityData = reader.bytes();
                        break;
                    }
                case 5: {
                        message.oldSecurityDataRevision = reader.int64();
                        break;
                    }
                case 6: {
                        message.currentDataEncryptionType = reader.int32();
                        break;
                    }
                case 7: {
                        message.oldDataEncryptionType = reader.int32();
                        break;
                    }
                case 8: {
                        message.recordUid = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SecurityReportIncrementalData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SecurityReportIncrementalData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SecurityReportIncrementalData} SecurityReportIncrementalData
         */
        SecurityReportIncrementalData.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SecurityReportIncrementalData)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SecurityReportIncrementalData: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SecurityReportIncrementalData();
            if (object.enterpriseUserId != null)
                if ($util.Long)
                    message.enterpriseUserId = $util.Long.fromValue(object.enterpriseUserId, false);
                else if (typeof object.enterpriseUserId === "string")
                    message.enterpriseUserId = parseInt(object.enterpriseUserId, 10);
                else if (typeof object.enterpriseUserId === "number")
                    message.enterpriseUserId = object.enterpriseUserId;
                else if (typeof object.enterpriseUserId === "object")
                    message.enterpriseUserId = new $util.LongBits(object.enterpriseUserId.low >>> 0, object.enterpriseUserId.high >>> 0).toNumber();
            if (object.currentSecurityData != null)
                if (typeof object.currentSecurityData === "string")
                    $util.base64.decode(object.currentSecurityData, message.currentSecurityData = $util.newBuffer($util.base64.length(object.currentSecurityData)), 0);
                else if (object.currentSecurityData.length >= 0)
                    message.currentSecurityData = object.currentSecurityData;
            if (object.currentSecurityDataRevision != null)
                if ($util.Long)
                    message.currentSecurityDataRevision = $util.Long.fromValue(object.currentSecurityDataRevision, false);
                else if (typeof object.currentSecurityDataRevision === "string")
                    message.currentSecurityDataRevision = parseInt(object.currentSecurityDataRevision, 10);
                else if (typeof object.currentSecurityDataRevision === "number")
                    message.currentSecurityDataRevision = object.currentSecurityDataRevision;
                else if (typeof object.currentSecurityDataRevision === "object")
                    message.currentSecurityDataRevision = new $util.LongBits(object.currentSecurityDataRevision.low >>> 0, object.currentSecurityDataRevision.high >>> 0).toNumber();
            if (object.oldSecurityData != null)
                if (typeof object.oldSecurityData === "string")
                    $util.base64.decode(object.oldSecurityData, message.oldSecurityData = $util.newBuffer($util.base64.length(object.oldSecurityData)), 0);
                else if (object.oldSecurityData.length >= 0)
                    message.oldSecurityData = object.oldSecurityData;
            if (object.oldSecurityDataRevision != null)
                if ($util.Long)
                    message.oldSecurityDataRevision = $util.Long.fromValue(object.oldSecurityDataRevision, false);
                else if (typeof object.oldSecurityDataRevision === "string")
                    message.oldSecurityDataRevision = parseInt(object.oldSecurityDataRevision, 10);
                else if (typeof object.oldSecurityDataRevision === "number")
                    message.oldSecurityDataRevision = object.oldSecurityDataRevision;
                else if (typeof object.oldSecurityDataRevision === "object")
                    message.oldSecurityDataRevision = new $util.LongBits(object.oldSecurityDataRevision.low >>> 0, object.oldSecurityDataRevision.high >>> 0).toNumber();
            switch (object.currentDataEncryptionType) {
            default:
                if (typeof object.currentDataEncryptionType === "number") {
                    message.currentDataEncryptionType = object.currentDataEncryptionType;
                    break;
                }
                break;
            case "KT_NO_KEY":
            case 0:
                message.currentDataEncryptionType = 0;
                break;
            case "KT_ENCRYPTED_BY_DATA_KEY":
            case 1:
                message.currentDataEncryptionType = 1;
                break;
            case "KT_ENCRYPTED_BY_PUBLIC_KEY":
            case 2:
                message.currentDataEncryptionType = 2;
                break;
            case "KT_ENCRYPTED_BY_DATA_KEY_GCM":
            case 3:
                message.currentDataEncryptionType = 3;
                break;
            case "KT_ENCRYPTED_BY_PUBLIC_KEY_ECC":
            case 4:
                message.currentDataEncryptionType = 4;
                break;
            }
            switch (object.oldDataEncryptionType) {
            default:
                if (typeof object.oldDataEncryptionType === "number") {
                    message.oldDataEncryptionType = object.oldDataEncryptionType;
                    break;
                }
                break;
            case "KT_NO_KEY":
            case 0:
                message.oldDataEncryptionType = 0;
                break;
            case "KT_ENCRYPTED_BY_DATA_KEY":
            case 1:
                message.oldDataEncryptionType = 1;
                break;
            case "KT_ENCRYPTED_BY_PUBLIC_KEY":
            case 2:
                message.oldDataEncryptionType = 2;
                break;
            case "KT_ENCRYPTED_BY_DATA_KEY_GCM":
            case 3:
                message.oldDataEncryptionType = 3;
                break;
            case "KT_ENCRYPTED_BY_PUBLIC_KEY_ECC":
            case 4:
                message.oldDataEncryptionType = 4;
                break;
            }
            if (object.recordUid != null)
                if (typeof object.recordUid === "string")
                    $util.base64.decode(object.recordUid, message.recordUid = $util.newBuffer($util.base64.length(object.recordUid)), 0);
                else if (object.recordUid.length >= 0)
                    message.recordUid = object.recordUid;
            return message;
        };

        /**
         * Creates a plain object from a SecurityReportIncrementalData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SecurityReportIncrementalData
         * @static
         * @param {Authentication.SecurityReportIncrementalData} message SecurityReportIncrementalData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SecurityReportIncrementalData.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.enterpriseUserId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.enterpriseUserId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.currentSecurityData = "";
                else {
                    object.currentSecurityData = [];
                    if (options.bytes !== Array)
                        object.currentSecurityData = $util.newBuffer(object.currentSecurityData);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.currentSecurityDataRevision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.currentSecurityDataRevision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.oldSecurityData = "";
                else {
                    object.oldSecurityData = [];
                    if (options.bytes !== Array)
                        object.oldSecurityData = $util.newBuffer(object.oldSecurityData);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.oldSecurityDataRevision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.oldSecurityDataRevision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.currentDataEncryptionType = options.enums === String ? "KT_NO_KEY" : 0;
                object.oldDataEncryptionType = options.enums === String ? "KT_NO_KEY" : 0;
                if (options.bytes === String)
                    object.recordUid = "";
                else {
                    object.recordUid = [];
                    if (options.bytes !== Array)
                        object.recordUid = $util.newBuffer(object.recordUid);
                }
            }
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.enterpriseUserId = typeof message.enterpriseUserId === "number" ? BigInt(message.enterpriseUserId) : $util.Long.fromBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0, false).toBigInt();
                else if (typeof message.enterpriseUserId === "number")
                    object.enterpriseUserId = options.longs === String ? String(message.enterpriseUserId) : message.enterpriseUserId;
                else
                    object.enterpriseUserId = options.longs === String ? $util.Long.prototype.toString.call(message.enterpriseUserId) : options.longs === Number ? new $util.LongBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0).toNumber() : message.enterpriseUserId;
            if (message.currentSecurityData != null && Object.hasOwnProperty.call(message, "currentSecurityData"))
                object.currentSecurityData = options.bytes === String ? $util.base64.encode(message.currentSecurityData, 0, message.currentSecurityData.length) : options.bytes === Array ? Array.prototype.slice.call(message.currentSecurityData) : message.currentSecurityData;
            if (message.currentSecurityDataRevision != null && Object.hasOwnProperty.call(message, "currentSecurityDataRevision"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.currentSecurityDataRevision = typeof message.currentSecurityDataRevision === "number" ? BigInt(message.currentSecurityDataRevision) : $util.Long.fromBits(message.currentSecurityDataRevision.low >>> 0, message.currentSecurityDataRevision.high >>> 0, false).toBigInt();
                else if (typeof message.currentSecurityDataRevision === "number")
                    object.currentSecurityDataRevision = options.longs === String ? String(message.currentSecurityDataRevision) : message.currentSecurityDataRevision;
                else
                    object.currentSecurityDataRevision = options.longs === String ? $util.Long.prototype.toString.call(message.currentSecurityDataRevision) : options.longs === Number ? new $util.LongBits(message.currentSecurityDataRevision.low >>> 0, message.currentSecurityDataRevision.high >>> 0).toNumber() : message.currentSecurityDataRevision;
            if (message.oldSecurityData != null && Object.hasOwnProperty.call(message, "oldSecurityData"))
                object.oldSecurityData = options.bytes === String ? $util.base64.encode(message.oldSecurityData, 0, message.oldSecurityData.length) : options.bytes === Array ? Array.prototype.slice.call(message.oldSecurityData) : message.oldSecurityData;
            if (message.oldSecurityDataRevision != null && Object.hasOwnProperty.call(message, "oldSecurityDataRevision"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.oldSecurityDataRevision = typeof message.oldSecurityDataRevision === "number" ? BigInt(message.oldSecurityDataRevision) : $util.Long.fromBits(message.oldSecurityDataRevision.low >>> 0, message.oldSecurityDataRevision.high >>> 0, false).toBigInt();
                else if (typeof message.oldSecurityDataRevision === "number")
                    object.oldSecurityDataRevision = options.longs === String ? String(message.oldSecurityDataRevision) : message.oldSecurityDataRevision;
                else
                    object.oldSecurityDataRevision = options.longs === String ? $util.Long.prototype.toString.call(message.oldSecurityDataRevision) : options.longs === Number ? new $util.LongBits(message.oldSecurityDataRevision.low >>> 0, message.oldSecurityDataRevision.high >>> 0).toNumber() : message.oldSecurityDataRevision;
            if (message.currentDataEncryptionType != null && Object.hasOwnProperty.call(message, "currentDataEncryptionType"))
                object.currentDataEncryptionType = options.enums === String ? $root.Enterprise.EncryptedKeyType[message.currentDataEncryptionType] === undefined ? message.currentDataEncryptionType : $root.Enterprise.EncryptedKeyType[message.currentDataEncryptionType] : message.currentDataEncryptionType;
            if (message.oldDataEncryptionType != null && Object.hasOwnProperty.call(message, "oldDataEncryptionType"))
                object.oldDataEncryptionType = options.enums === String ? $root.Enterprise.EncryptedKeyType[message.oldDataEncryptionType] === undefined ? message.oldDataEncryptionType : $root.Enterprise.EncryptedKeyType[message.oldDataEncryptionType] : message.oldDataEncryptionType;
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                object.recordUid = options.bytes === String ? $util.base64.encode(message.recordUid, 0, message.recordUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.recordUid) : message.recordUid;
            return object;
        };

        /**
         * Converts this SecurityReportIncrementalData to JSON.
         * @function toJSON
         * @memberof Authentication.SecurityReportIncrementalData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SecurityReportIncrementalData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SecurityReportIncrementalData
         * @function getTypeUrl
         * @memberof Authentication.SecurityReportIncrementalData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SecurityReportIncrementalData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SecurityReportIncrementalData";
        };

        return SecurityReportIncrementalData;
    })();

    Authentication.SecurityReport = (function() {

        /**
         * Properties of a SecurityReport.
         * @memberof Authentication
         * @interface ISecurityReport
         * @property {number|null} [enterpriseUserId] SecurityReport enterpriseUserId
         * @property {Uint8Array|null} [encryptedReportData] SecurityReport encryptedReportData
         * @property {number|null} [revision] SecurityReport revision
         * @property {string|null} [twoFactor] SecurityReport twoFactor
         * @property {number|null} [lastLogin] SecurityReport lastLogin
         * @property {number|null} [numberOfReusedPassword] SecurityReport numberOfReusedPassword
         * @property {Array.<Authentication.ISecurityReportIncrementalData>|null} [securityReportIncrementalData] SecurityReport securityReportIncrementalData
         * @property {number|null} [userId] SecurityReport userId
         * @property {boolean|null} [hasOldEncryption] SecurityReport hasOldEncryption
         */

        /**
         * Constructs a new SecurityReport.
         * @memberof Authentication
         * @classdesc Represents a SecurityReport.
         * @implements ISecurityReport
         * @constructor
         * @param {Authentication.ISecurityReport=} [properties] Properties to set
         */
        function SecurityReport(properties) {
            this.securityReportIncrementalData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SecurityReport enterpriseUserId.
         * @member {number} enterpriseUserId
         * @memberof Authentication.SecurityReport
         * @instance
         */
        SecurityReport.prototype.enterpriseUserId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SecurityReport encryptedReportData.
         * @member {Uint8Array} encryptedReportData
         * @memberof Authentication.SecurityReport
         * @instance
         */
        SecurityReport.prototype.encryptedReportData = $util.newBuffer([]);

        /**
         * SecurityReport revision.
         * @member {number} revision
         * @memberof Authentication.SecurityReport
         * @instance
         */
        SecurityReport.prototype.revision = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SecurityReport twoFactor.
         * @member {string} twoFactor
         * @memberof Authentication.SecurityReport
         * @instance
         */
        SecurityReport.prototype.twoFactor = "";

        /**
         * SecurityReport lastLogin.
         * @member {number} lastLogin
         * @memberof Authentication.SecurityReport
         * @instance
         */
        SecurityReport.prototype.lastLogin = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SecurityReport numberOfReusedPassword.
         * @member {number} numberOfReusedPassword
         * @memberof Authentication.SecurityReport
         * @instance
         */
        SecurityReport.prototype.numberOfReusedPassword = 0;

        /**
         * SecurityReport securityReportIncrementalData.
         * @member {Array.<Authentication.ISecurityReportIncrementalData>} securityReportIncrementalData
         * @memberof Authentication.SecurityReport
         * @instance
         */
        SecurityReport.prototype.securityReportIncrementalData = $util.emptyArray;

        /**
         * SecurityReport userId.
         * @member {number} userId
         * @memberof Authentication.SecurityReport
         * @instance
         */
        SecurityReport.prototype.userId = 0;

        /**
         * SecurityReport hasOldEncryption.
         * @member {boolean} hasOldEncryption
         * @memberof Authentication.SecurityReport
         * @instance
         */
        SecurityReport.prototype.hasOldEncryption = false;

        /**
         * Creates a new SecurityReport instance using the specified properties.
         * @function create
         * @memberof Authentication.SecurityReport
         * @static
         * @param {Authentication.ISecurityReport=} [properties] Properties to set
         * @returns {Authentication.SecurityReport} SecurityReport instance
         */
        SecurityReport.create = function create(properties) {
            return new SecurityReport(properties);
        };

        /**
         * Encodes the specified SecurityReport message. Does not implicitly {@link Authentication.SecurityReport.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SecurityReport
         * @static
         * @param {Authentication.ISecurityReport} message SecurityReport message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SecurityReport.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.enterpriseUserId);
            if (message.encryptedReportData != null && Object.hasOwnProperty.call(message, "encryptedReportData"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encryptedReportData);
            if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.revision);
            if (message.twoFactor != null && Object.hasOwnProperty.call(message, "twoFactor"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.twoFactor);
            if (message.lastLogin != null && Object.hasOwnProperty.call(message, "lastLogin"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.lastLogin);
            if (message.numberOfReusedPassword != null && Object.hasOwnProperty.call(message, "numberOfReusedPassword"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.numberOfReusedPassword);
            if (message.securityReportIncrementalData != null && message.securityReportIncrementalData.length)
                for (let i = 0; i < message.securityReportIncrementalData.length; ++i)
                    $root.Authentication.SecurityReportIncrementalData.encode(message.securityReportIncrementalData[i], writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.userId);
            if (message.hasOldEncryption != null && Object.hasOwnProperty.call(message, "hasOldEncryption"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.hasOldEncryption);
            return writer;
        };

        /**
         * Decodes a SecurityReport message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SecurityReport
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SecurityReport} SecurityReport
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SecurityReport.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SecurityReport();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.enterpriseUserId = reader.int64();
                        break;
                    }
                case 2: {
                        message.encryptedReportData = reader.bytes();
                        break;
                    }
                case 3: {
                        message.revision = reader.int64();
                        break;
                    }
                case 4: {
                        message.twoFactor = reader.string();
                        break;
                    }
                case 5: {
                        message.lastLogin = reader.int64();
                        break;
                    }
                case 6: {
                        message.numberOfReusedPassword = reader.int32();
                        break;
                    }
                case 7: {
                        if (!(message.securityReportIncrementalData && message.securityReportIncrementalData.length))
                            message.securityReportIncrementalData = [];
                        message.securityReportIncrementalData.push($root.Authentication.SecurityReportIncrementalData.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 8: {
                        message.userId = reader.int32();
                        break;
                    }
                case 9: {
                        message.hasOldEncryption = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SecurityReport message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SecurityReport
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SecurityReport} SecurityReport
         */
        SecurityReport.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SecurityReport)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SecurityReport: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SecurityReport();
            if (object.enterpriseUserId != null)
                if ($util.Long)
                    message.enterpriseUserId = $util.Long.fromValue(object.enterpriseUserId, false);
                else if (typeof object.enterpriseUserId === "string")
                    message.enterpriseUserId = parseInt(object.enterpriseUserId, 10);
                else if (typeof object.enterpriseUserId === "number")
                    message.enterpriseUserId = object.enterpriseUserId;
                else if (typeof object.enterpriseUserId === "object")
                    message.enterpriseUserId = new $util.LongBits(object.enterpriseUserId.low >>> 0, object.enterpriseUserId.high >>> 0).toNumber();
            if (object.encryptedReportData != null)
                if (typeof object.encryptedReportData === "string")
                    $util.base64.decode(object.encryptedReportData, message.encryptedReportData = $util.newBuffer($util.base64.length(object.encryptedReportData)), 0);
                else if (object.encryptedReportData.length >= 0)
                    message.encryptedReportData = object.encryptedReportData;
            if (object.revision != null)
                if ($util.Long)
                    message.revision = $util.Long.fromValue(object.revision, false);
                else if (typeof object.revision === "string")
                    message.revision = parseInt(object.revision, 10);
                else if (typeof object.revision === "number")
                    message.revision = object.revision;
                else if (typeof object.revision === "object")
                    message.revision = new $util.LongBits(object.revision.low >>> 0, object.revision.high >>> 0).toNumber();
            if (object.twoFactor != null)
                message.twoFactor = String(object.twoFactor);
            if (object.lastLogin != null)
                if ($util.Long)
                    message.lastLogin = $util.Long.fromValue(object.lastLogin, false);
                else if (typeof object.lastLogin === "string")
                    message.lastLogin = parseInt(object.lastLogin, 10);
                else if (typeof object.lastLogin === "number")
                    message.lastLogin = object.lastLogin;
                else if (typeof object.lastLogin === "object")
                    message.lastLogin = new $util.LongBits(object.lastLogin.low >>> 0, object.lastLogin.high >>> 0).toNumber();
            if (object.numberOfReusedPassword != null)
                message.numberOfReusedPassword = object.numberOfReusedPassword | 0;
            if (object.securityReportIncrementalData) {
                if (!Array.isArray(object.securityReportIncrementalData))
                    throw TypeError(".Authentication.SecurityReport.securityReportIncrementalData: array expected");
                message.securityReportIncrementalData = [];
                for (let i = 0; i < object.securityReportIncrementalData.length; ++i) {
                    if (!$util.isObject(object.securityReportIncrementalData[i]))
                        throw TypeError(".Authentication.SecurityReport.securityReportIncrementalData: object expected");
                    message.securityReportIncrementalData[i] = $root.Authentication.SecurityReportIncrementalData.fromObject(object.securityReportIncrementalData[i], long + 1);
                }
            }
            if (object.userId != null)
                message.userId = object.userId | 0;
            if (object.hasOldEncryption != null)
                message.hasOldEncryption = Boolean(object.hasOldEncryption);
            return message;
        };

        /**
         * Creates a plain object from a SecurityReport message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SecurityReport
         * @static
         * @param {Authentication.SecurityReport} message SecurityReport
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SecurityReport.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.securityReportIncrementalData = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.enterpriseUserId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.enterpriseUserId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.encryptedReportData = "";
                else {
                    object.encryptedReportData = [];
                    if (options.bytes !== Array)
                        object.encryptedReportData = $util.newBuffer(object.encryptedReportData);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.revision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.revision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.twoFactor = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastLogin = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastLogin = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.numberOfReusedPassword = 0;
                object.userId = 0;
                object.hasOldEncryption = false;
            }
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.enterpriseUserId = typeof message.enterpriseUserId === "number" ? BigInt(message.enterpriseUserId) : $util.Long.fromBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0, false).toBigInt();
                else if (typeof message.enterpriseUserId === "number")
                    object.enterpriseUserId = options.longs === String ? String(message.enterpriseUserId) : message.enterpriseUserId;
                else
                    object.enterpriseUserId = options.longs === String ? $util.Long.prototype.toString.call(message.enterpriseUserId) : options.longs === Number ? new $util.LongBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0).toNumber() : message.enterpriseUserId;
            if (message.encryptedReportData != null && Object.hasOwnProperty.call(message, "encryptedReportData"))
                object.encryptedReportData = options.bytes === String ? $util.base64.encode(message.encryptedReportData, 0, message.encryptedReportData.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedReportData) : message.encryptedReportData;
            if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.revision = typeof message.revision === "number" ? BigInt(message.revision) : $util.Long.fromBits(message.revision.low >>> 0, message.revision.high >>> 0, false).toBigInt();
                else if (typeof message.revision === "number")
                    object.revision = options.longs === String ? String(message.revision) : message.revision;
                else
                    object.revision = options.longs === String ? $util.Long.prototype.toString.call(message.revision) : options.longs === Number ? new $util.LongBits(message.revision.low >>> 0, message.revision.high >>> 0).toNumber() : message.revision;
            if (message.twoFactor != null && Object.hasOwnProperty.call(message, "twoFactor"))
                object.twoFactor = message.twoFactor;
            if (message.lastLogin != null && Object.hasOwnProperty.call(message, "lastLogin"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastLogin = typeof message.lastLogin === "number" ? BigInt(message.lastLogin) : $util.Long.fromBits(message.lastLogin.low >>> 0, message.lastLogin.high >>> 0, false).toBigInt();
                else if (typeof message.lastLogin === "number")
                    object.lastLogin = options.longs === String ? String(message.lastLogin) : message.lastLogin;
                else
                    object.lastLogin = options.longs === String ? $util.Long.prototype.toString.call(message.lastLogin) : options.longs === Number ? new $util.LongBits(message.lastLogin.low >>> 0, message.lastLogin.high >>> 0).toNumber() : message.lastLogin;
            if (message.numberOfReusedPassword != null && Object.hasOwnProperty.call(message, "numberOfReusedPassword"))
                object.numberOfReusedPassword = message.numberOfReusedPassword;
            if (message.securityReportIncrementalData && message.securityReportIncrementalData.length) {
                object.securityReportIncrementalData = [];
                for (let j = 0; j < message.securityReportIncrementalData.length; ++j)
                    object.securityReportIncrementalData[j] = $root.Authentication.SecurityReportIncrementalData.toObject(message.securityReportIncrementalData[j], options, q + 1);
            }
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                object.userId = message.userId;
            if (message.hasOldEncryption != null && Object.hasOwnProperty.call(message, "hasOldEncryption"))
                object.hasOldEncryption = message.hasOldEncryption;
            return object;
        };

        /**
         * Converts this SecurityReport to JSON.
         * @function toJSON
         * @memberof Authentication.SecurityReport
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SecurityReport.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SecurityReport
         * @function getTypeUrl
         * @memberof Authentication.SecurityReport
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SecurityReport.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SecurityReport";
        };

        return SecurityReport;
    })();

    Authentication.SecurityReportSaveRequest = (function() {

        /**
         * Properties of a SecurityReportSaveRequest.
         * @memberof Authentication
         * @interface ISecurityReportSaveRequest
         * @property {Array.<Authentication.ISecurityReport>|null} [securityReport] SecurityReportSaveRequest securityReport
         * @property {Uint8Array|null} [continuationToken] SecurityReportSaveRequest continuationToken
         */

        /**
         * Constructs a new SecurityReportSaveRequest.
         * @memberof Authentication
         * @classdesc Represents a SecurityReportSaveRequest.
         * @implements ISecurityReportSaveRequest
         * @constructor
         * @param {Authentication.ISecurityReportSaveRequest=} [properties] Properties to set
         */
        function SecurityReportSaveRequest(properties) {
            this.securityReport = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SecurityReportSaveRequest securityReport.
         * @member {Array.<Authentication.ISecurityReport>} securityReport
         * @memberof Authentication.SecurityReportSaveRequest
         * @instance
         */
        SecurityReportSaveRequest.prototype.securityReport = $util.emptyArray;

        /**
         * SecurityReportSaveRequest continuationToken.
         * @member {Uint8Array} continuationToken
         * @memberof Authentication.SecurityReportSaveRequest
         * @instance
         */
        SecurityReportSaveRequest.prototype.continuationToken = $util.newBuffer([]);

        /**
         * Creates a new SecurityReportSaveRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.SecurityReportSaveRequest
         * @static
         * @param {Authentication.ISecurityReportSaveRequest=} [properties] Properties to set
         * @returns {Authentication.SecurityReportSaveRequest} SecurityReportSaveRequest instance
         */
        SecurityReportSaveRequest.create = function create(properties) {
            return new SecurityReportSaveRequest(properties);
        };

        /**
         * Encodes the specified SecurityReportSaveRequest message. Does not implicitly {@link Authentication.SecurityReportSaveRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SecurityReportSaveRequest
         * @static
         * @param {Authentication.ISecurityReportSaveRequest} message SecurityReportSaveRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SecurityReportSaveRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.securityReport != null && message.securityReport.length)
                for (let i = 0; i < message.securityReport.length; ++i)
                    $root.Authentication.SecurityReport.encode(message.securityReport[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.continuationToken != null && Object.hasOwnProperty.call(message, "continuationToken"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.continuationToken);
            return writer;
        };

        /**
         * Decodes a SecurityReportSaveRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SecurityReportSaveRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SecurityReportSaveRequest} SecurityReportSaveRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SecurityReportSaveRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SecurityReportSaveRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.securityReport && message.securityReport.length))
                            message.securityReport = [];
                        message.securityReport.push($root.Authentication.SecurityReport.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 2: {
                        message.continuationToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SecurityReportSaveRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SecurityReportSaveRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SecurityReportSaveRequest} SecurityReportSaveRequest
         */
        SecurityReportSaveRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SecurityReportSaveRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SecurityReportSaveRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SecurityReportSaveRequest();
            if (object.securityReport) {
                if (!Array.isArray(object.securityReport))
                    throw TypeError(".Authentication.SecurityReportSaveRequest.securityReport: array expected");
                message.securityReport = [];
                for (let i = 0; i < object.securityReport.length; ++i) {
                    if (!$util.isObject(object.securityReport[i]))
                        throw TypeError(".Authentication.SecurityReportSaveRequest.securityReport: object expected");
                    message.securityReport[i] = $root.Authentication.SecurityReport.fromObject(object.securityReport[i], long + 1);
                }
            }
            if (object.continuationToken != null)
                if (typeof object.continuationToken === "string")
                    $util.base64.decode(object.continuationToken, message.continuationToken = $util.newBuffer($util.base64.length(object.continuationToken)), 0);
                else if (object.continuationToken.length >= 0)
                    message.continuationToken = object.continuationToken;
            return message;
        };

        /**
         * Creates a plain object from a SecurityReportSaveRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SecurityReportSaveRequest
         * @static
         * @param {Authentication.SecurityReportSaveRequest} message SecurityReportSaveRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SecurityReportSaveRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.securityReport = [];
            if (options.defaults)
                if (options.bytes === String)
                    object.continuationToken = "";
                else {
                    object.continuationToken = [];
                    if (options.bytes !== Array)
                        object.continuationToken = $util.newBuffer(object.continuationToken);
                }
            if (message.securityReport && message.securityReport.length) {
                object.securityReport = [];
                for (let j = 0; j < message.securityReport.length; ++j)
                    object.securityReport[j] = $root.Authentication.SecurityReport.toObject(message.securityReport[j], options, q + 1);
            }
            if (message.continuationToken != null && Object.hasOwnProperty.call(message, "continuationToken"))
                object.continuationToken = options.bytes === String ? $util.base64.encode(message.continuationToken, 0, message.continuationToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.continuationToken) : message.continuationToken;
            return object;
        };

        /**
         * Converts this SecurityReportSaveRequest to JSON.
         * @function toJSON
         * @memberof Authentication.SecurityReportSaveRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SecurityReportSaveRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SecurityReportSaveRequest
         * @function getTypeUrl
         * @memberof Authentication.SecurityReportSaveRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SecurityReportSaveRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SecurityReportSaveRequest";
        };

        return SecurityReportSaveRequest;
    })();

    Authentication.SecurityReportRequest = (function() {

        /**
         * Properties of a SecurityReportRequest.
         * @memberof Authentication
         * @interface ISecurityReportRequest
         * @property {number|null} [fromPage] SecurityReportRequest fromPage
         */

        /**
         * Constructs a new SecurityReportRequest.
         * @memberof Authentication
         * @classdesc Represents a SecurityReportRequest.
         * @implements ISecurityReportRequest
         * @constructor
         * @param {Authentication.ISecurityReportRequest=} [properties] Properties to set
         */
        function SecurityReportRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SecurityReportRequest fromPage.
         * @member {number} fromPage
         * @memberof Authentication.SecurityReportRequest
         * @instance
         */
        SecurityReportRequest.prototype.fromPage = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new SecurityReportRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.SecurityReportRequest
         * @static
         * @param {Authentication.ISecurityReportRequest=} [properties] Properties to set
         * @returns {Authentication.SecurityReportRequest} SecurityReportRequest instance
         */
        SecurityReportRequest.create = function create(properties) {
            return new SecurityReportRequest(properties);
        };

        /**
         * Encodes the specified SecurityReportRequest message. Does not implicitly {@link Authentication.SecurityReportRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SecurityReportRequest
         * @static
         * @param {Authentication.ISecurityReportRequest} message SecurityReportRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SecurityReportRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.fromPage != null && Object.hasOwnProperty.call(message, "fromPage"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.fromPage);
            return writer;
        };

        /**
         * Decodes a SecurityReportRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SecurityReportRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SecurityReportRequest} SecurityReportRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SecurityReportRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SecurityReportRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.fromPage = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SecurityReportRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SecurityReportRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SecurityReportRequest} SecurityReportRequest
         */
        SecurityReportRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SecurityReportRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SecurityReportRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SecurityReportRequest();
            if (object.fromPage != null)
                if ($util.Long)
                    message.fromPage = $util.Long.fromValue(object.fromPage, false);
                else if (typeof object.fromPage === "string")
                    message.fromPage = parseInt(object.fromPage, 10);
                else if (typeof object.fromPage === "number")
                    message.fromPage = object.fromPage;
                else if (typeof object.fromPage === "object")
                    message.fromPage = new $util.LongBits(object.fromPage.low >>> 0, object.fromPage.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a SecurityReportRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SecurityReportRequest
         * @static
         * @param {Authentication.SecurityReportRequest} message SecurityReportRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SecurityReportRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.fromPage = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.fromPage = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            if (message.fromPage != null && Object.hasOwnProperty.call(message, "fromPage"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.fromPage = typeof message.fromPage === "number" ? BigInt(message.fromPage) : $util.Long.fromBits(message.fromPage.low >>> 0, message.fromPage.high >>> 0, false).toBigInt();
                else if (typeof message.fromPage === "number")
                    object.fromPage = options.longs === String ? String(message.fromPage) : message.fromPage;
                else
                    object.fromPage = options.longs === String ? $util.Long.prototype.toString.call(message.fromPage) : options.longs === Number ? new $util.LongBits(message.fromPage.low >>> 0, message.fromPage.high >>> 0).toNumber() : message.fromPage;
            return object;
        };

        /**
         * Converts this SecurityReportRequest to JSON.
         * @function toJSON
         * @memberof Authentication.SecurityReportRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SecurityReportRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SecurityReportRequest
         * @function getTypeUrl
         * @memberof Authentication.SecurityReportRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SecurityReportRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SecurityReportRequest";
        };

        return SecurityReportRequest;
    })();

    Authentication.SecurityReportResponse = (function() {

        /**
         * Properties of a SecurityReportResponse.
         * @memberof Authentication
         * @interface ISecurityReportResponse
         * @property {Uint8Array|null} [enterprisePrivateKey] SecurityReportResponse enterprisePrivateKey
         * @property {Array.<Authentication.ISecurityReport>|null} [securityReport] SecurityReportResponse securityReport
         * @property {number|null} [asOfRevision] SecurityReportResponse asOfRevision
         * @property {number|null} [fromPage] SecurityReportResponse fromPage
         * @property {number|null} [toPage] SecurityReportResponse toPage
         * @property {boolean|null} [complete] SecurityReportResponse complete
         * @property {Uint8Array|null} [enterpriseEccPrivateKey] SecurityReportResponse enterpriseEccPrivateKey
         * @property {boolean|null} [hasIncrementalData] SecurityReportResponse hasIncrementalData
         */

        /**
         * Constructs a new SecurityReportResponse.
         * @memberof Authentication
         * @classdesc Represents a SecurityReportResponse.
         * @implements ISecurityReportResponse
         * @constructor
         * @param {Authentication.ISecurityReportResponse=} [properties] Properties to set
         */
        function SecurityReportResponse(properties) {
            this.securityReport = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SecurityReportResponse enterprisePrivateKey.
         * @member {Uint8Array} enterprisePrivateKey
         * @memberof Authentication.SecurityReportResponse
         * @instance
         */
        SecurityReportResponse.prototype.enterprisePrivateKey = $util.newBuffer([]);

        /**
         * SecurityReportResponse securityReport.
         * @member {Array.<Authentication.ISecurityReport>} securityReport
         * @memberof Authentication.SecurityReportResponse
         * @instance
         */
        SecurityReportResponse.prototype.securityReport = $util.emptyArray;

        /**
         * SecurityReportResponse asOfRevision.
         * @member {number} asOfRevision
         * @memberof Authentication.SecurityReportResponse
         * @instance
         */
        SecurityReportResponse.prototype.asOfRevision = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SecurityReportResponse fromPage.
         * @member {number} fromPage
         * @memberof Authentication.SecurityReportResponse
         * @instance
         */
        SecurityReportResponse.prototype.fromPage = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SecurityReportResponse toPage.
         * @member {number} toPage
         * @memberof Authentication.SecurityReportResponse
         * @instance
         */
        SecurityReportResponse.prototype.toPage = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SecurityReportResponse complete.
         * @member {boolean} complete
         * @memberof Authentication.SecurityReportResponse
         * @instance
         */
        SecurityReportResponse.prototype.complete = false;

        /**
         * SecurityReportResponse enterpriseEccPrivateKey.
         * @member {Uint8Array} enterpriseEccPrivateKey
         * @memberof Authentication.SecurityReportResponse
         * @instance
         */
        SecurityReportResponse.prototype.enterpriseEccPrivateKey = $util.newBuffer([]);

        /**
         * SecurityReportResponse hasIncrementalData.
         * @member {boolean} hasIncrementalData
         * @memberof Authentication.SecurityReportResponse
         * @instance
         */
        SecurityReportResponse.prototype.hasIncrementalData = false;

        /**
         * Creates a new SecurityReportResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.SecurityReportResponse
         * @static
         * @param {Authentication.ISecurityReportResponse=} [properties] Properties to set
         * @returns {Authentication.SecurityReportResponse} SecurityReportResponse instance
         */
        SecurityReportResponse.create = function create(properties) {
            return new SecurityReportResponse(properties);
        };

        /**
         * Encodes the specified SecurityReportResponse message. Does not implicitly {@link Authentication.SecurityReportResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SecurityReportResponse
         * @static
         * @param {Authentication.ISecurityReportResponse} message SecurityReportResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SecurityReportResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.enterprisePrivateKey != null && Object.hasOwnProperty.call(message, "enterprisePrivateKey"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.enterprisePrivateKey);
            if (message.securityReport != null && message.securityReport.length)
                for (let i = 0; i < message.securityReport.length; ++i)
                    $root.Authentication.SecurityReport.encode(message.securityReport[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.asOfRevision != null && Object.hasOwnProperty.call(message, "asOfRevision"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.asOfRevision);
            if (message.fromPage != null && Object.hasOwnProperty.call(message, "fromPage"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.fromPage);
            if (message.toPage != null && Object.hasOwnProperty.call(message, "toPage"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.toPage);
            if (message.complete != null && Object.hasOwnProperty.call(message, "complete"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.complete);
            if (message.enterpriseEccPrivateKey != null && Object.hasOwnProperty.call(message, "enterpriseEccPrivateKey"))
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.enterpriseEccPrivateKey);
            if (message.hasIncrementalData != null && Object.hasOwnProperty.call(message, "hasIncrementalData"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.hasIncrementalData);
            return writer;
        };

        /**
         * Decodes a SecurityReportResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SecurityReportResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SecurityReportResponse} SecurityReportResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SecurityReportResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SecurityReportResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.enterprisePrivateKey = reader.bytes();
                        break;
                    }
                case 2: {
                        if (!(message.securityReport && message.securityReport.length))
                            message.securityReport = [];
                        message.securityReport.push($root.Authentication.SecurityReport.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 3: {
                        message.asOfRevision = reader.int64();
                        break;
                    }
                case 4: {
                        message.fromPage = reader.int64();
                        break;
                    }
                case 5: {
                        message.toPage = reader.int64();
                        break;
                    }
                case 6: {
                        message.complete = reader.bool();
                        break;
                    }
                case 7: {
                        message.enterpriseEccPrivateKey = reader.bytes();
                        break;
                    }
                case 8: {
                        message.hasIncrementalData = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SecurityReportResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SecurityReportResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SecurityReportResponse} SecurityReportResponse
         */
        SecurityReportResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SecurityReportResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SecurityReportResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SecurityReportResponse();
            if (object.enterprisePrivateKey != null)
                if (typeof object.enterprisePrivateKey === "string")
                    $util.base64.decode(object.enterprisePrivateKey, message.enterprisePrivateKey = $util.newBuffer($util.base64.length(object.enterprisePrivateKey)), 0);
                else if (object.enterprisePrivateKey.length >= 0)
                    message.enterprisePrivateKey = object.enterprisePrivateKey;
            if (object.securityReport) {
                if (!Array.isArray(object.securityReport))
                    throw TypeError(".Authentication.SecurityReportResponse.securityReport: array expected");
                message.securityReport = [];
                for (let i = 0; i < object.securityReport.length; ++i) {
                    if (!$util.isObject(object.securityReport[i]))
                        throw TypeError(".Authentication.SecurityReportResponse.securityReport: object expected");
                    message.securityReport[i] = $root.Authentication.SecurityReport.fromObject(object.securityReport[i], long + 1);
                }
            }
            if (object.asOfRevision != null)
                if ($util.Long)
                    message.asOfRevision = $util.Long.fromValue(object.asOfRevision, false);
                else if (typeof object.asOfRevision === "string")
                    message.asOfRevision = parseInt(object.asOfRevision, 10);
                else if (typeof object.asOfRevision === "number")
                    message.asOfRevision = object.asOfRevision;
                else if (typeof object.asOfRevision === "object")
                    message.asOfRevision = new $util.LongBits(object.asOfRevision.low >>> 0, object.asOfRevision.high >>> 0).toNumber();
            if (object.fromPage != null)
                if ($util.Long)
                    message.fromPage = $util.Long.fromValue(object.fromPage, false);
                else if (typeof object.fromPage === "string")
                    message.fromPage = parseInt(object.fromPage, 10);
                else if (typeof object.fromPage === "number")
                    message.fromPage = object.fromPage;
                else if (typeof object.fromPage === "object")
                    message.fromPage = new $util.LongBits(object.fromPage.low >>> 0, object.fromPage.high >>> 0).toNumber();
            if (object.toPage != null)
                if ($util.Long)
                    message.toPage = $util.Long.fromValue(object.toPage, false);
                else if (typeof object.toPage === "string")
                    message.toPage = parseInt(object.toPage, 10);
                else if (typeof object.toPage === "number")
                    message.toPage = object.toPage;
                else if (typeof object.toPage === "object")
                    message.toPage = new $util.LongBits(object.toPage.low >>> 0, object.toPage.high >>> 0).toNumber();
            if (object.complete != null)
                message.complete = Boolean(object.complete);
            if (object.enterpriseEccPrivateKey != null)
                if (typeof object.enterpriseEccPrivateKey === "string")
                    $util.base64.decode(object.enterpriseEccPrivateKey, message.enterpriseEccPrivateKey = $util.newBuffer($util.base64.length(object.enterpriseEccPrivateKey)), 0);
                else if (object.enterpriseEccPrivateKey.length >= 0)
                    message.enterpriseEccPrivateKey = object.enterpriseEccPrivateKey;
            if (object.hasIncrementalData != null)
                message.hasIncrementalData = Boolean(object.hasIncrementalData);
            return message;
        };

        /**
         * Creates a plain object from a SecurityReportResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SecurityReportResponse
         * @static
         * @param {Authentication.SecurityReportResponse} message SecurityReportResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SecurityReportResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.securityReport = [];
            if (options.defaults) {
                if (options.bytes === String)
                    object.enterprisePrivateKey = "";
                else {
                    object.enterprisePrivateKey = [];
                    if (options.bytes !== Array)
                        object.enterprisePrivateKey = $util.newBuffer(object.enterprisePrivateKey);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.asOfRevision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.asOfRevision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.fromPage = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.fromPage = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.toPage = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.toPage = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.complete = false;
                if (options.bytes === String)
                    object.enterpriseEccPrivateKey = "";
                else {
                    object.enterpriseEccPrivateKey = [];
                    if (options.bytes !== Array)
                        object.enterpriseEccPrivateKey = $util.newBuffer(object.enterpriseEccPrivateKey);
                }
                object.hasIncrementalData = false;
            }
            if (message.enterprisePrivateKey != null && Object.hasOwnProperty.call(message, "enterprisePrivateKey"))
                object.enterprisePrivateKey = options.bytes === String ? $util.base64.encode(message.enterprisePrivateKey, 0, message.enterprisePrivateKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.enterprisePrivateKey) : message.enterprisePrivateKey;
            if (message.securityReport && message.securityReport.length) {
                object.securityReport = [];
                for (let j = 0; j < message.securityReport.length; ++j)
                    object.securityReport[j] = $root.Authentication.SecurityReport.toObject(message.securityReport[j], options, q + 1);
            }
            if (message.asOfRevision != null && Object.hasOwnProperty.call(message, "asOfRevision"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.asOfRevision = typeof message.asOfRevision === "number" ? BigInt(message.asOfRevision) : $util.Long.fromBits(message.asOfRevision.low >>> 0, message.asOfRevision.high >>> 0, false).toBigInt();
                else if (typeof message.asOfRevision === "number")
                    object.asOfRevision = options.longs === String ? String(message.asOfRevision) : message.asOfRevision;
                else
                    object.asOfRevision = options.longs === String ? $util.Long.prototype.toString.call(message.asOfRevision) : options.longs === Number ? new $util.LongBits(message.asOfRevision.low >>> 0, message.asOfRevision.high >>> 0).toNumber() : message.asOfRevision;
            if (message.fromPage != null && Object.hasOwnProperty.call(message, "fromPage"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.fromPage = typeof message.fromPage === "number" ? BigInt(message.fromPage) : $util.Long.fromBits(message.fromPage.low >>> 0, message.fromPage.high >>> 0, false).toBigInt();
                else if (typeof message.fromPage === "number")
                    object.fromPage = options.longs === String ? String(message.fromPage) : message.fromPage;
                else
                    object.fromPage = options.longs === String ? $util.Long.prototype.toString.call(message.fromPage) : options.longs === Number ? new $util.LongBits(message.fromPage.low >>> 0, message.fromPage.high >>> 0).toNumber() : message.fromPage;
            if (message.toPage != null && Object.hasOwnProperty.call(message, "toPage"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.toPage = typeof message.toPage === "number" ? BigInt(message.toPage) : $util.Long.fromBits(message.toPage.low >>> 0, message.toPage.high >>> 0, false).toBigInt();
                else if (typeof message.toPage === "number")
                    object.toPage = options.longs === String ? String(message.toPage) : message.toPage;
                else
                    object.toPage = options.longs === String ? $util.Long.prototype.toString.call(message.toPage) : options.longs === Number ? new $util.LongBits(message.toPage.low >>> 0, message.toPage.high >>> 0).toNumber() : message.toPage;
            if (message.complete != null && Object.hasOwnProperty.call(message, "complete"))
                object.complete = message.complete;
            if (message.enterpriseEccPrivateKey != null && Object.hasOwnProperty.call(message, "enterpriseEccPrivateKey"))
                object.enterpriseEccPrivateKey = options.bytes === String ? $util.base64.encode(message.enterpriseEccPrivateKey, 0, message.enterpriseEccPrivateKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.enterpriseEccPrivateKey) : message.enterpriseEccPrivateKey;
            if (message.hasIncrementalData != null && Object.hasOwnProperty.call(message, "hasIncrementalData"))
                object.hasIncrementalData = message.hasIncrementalData;
            return object;
        };

        /**
         * Converts this SecurityReportResponse to JSON.
         * @function toJSON
         * @memberof Authentication.SecurityReportResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SecurityReportResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SecurityReportResponse
         * @function getTypeUrl
         * @memberof Authentication.SecurityReportResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SecurityReportResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SecurityReportResponse";
        };

        return SecurityReportResponse;
    })();

    Authentication.IncrementalSecurityDataRequest = (function() {

        /**
         * Properties of an IncrementalSecurityDataRequest.
         * @memberof Authentication
         * @interface IIncrementalSecurityDataRequest
         * @property {Uint8Array|null} [continuationToken] IncrementalSecurityDataRequest continuationToken
         */

        /**
         * Constructs a new IncrementalSecurityDataRequest.
         * @memberof Authentication
         * @classdesc Represents an IncrementalSecurityDataRequest.
         * @implements IIncrementalSecurityDataRequest
         * @constructor
         * @param {Authentication.IIncrementalSecurityDataRequest=} [properties] Properties to set
         */
        function IncrementalSecurityDataRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * IncrementalSecurityDataRequest continuationToken.
         * @member {Uint8Array} continuationToken
         * @memberof Authentication.IncrementalSecurityDataRequest
         * @instance
         */
        IncrementalSecurityDataRequest.prototype.continuationToken = $util.newBuffer([]);

        /**
         * Creates a new IncrementalSecurityDataRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.IncrementalSecurityDataRequest
         * @static
         * @param {Authentication.IIncrementalSecurityDataRequest=} [properties] Properties to set
         * @returns {Authentication.IncrementalSecurityDataRequest} IncrementalSecurityDataRequest instance
         */
        IncrementalSecurityDataRequest.create = function create(properties) {
            return new IncrementalSecurityDataRequest(properties);
        };

        /**
         * Encodes the specified IncrementalSecurityDataRequest message. Does not implicitly {@link Authentication.IncrementalSecurityDataRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.IncrementalSecurityDataRequest
         * @static
         * @param {Authentication.IIncrementalSecurityDataRequest} message IncrementalSecurityDataRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IncrementalSecurityDataRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.continuationToken != null && Object.hasOwnProperty.call(message, "continuationToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.continuationToken);
            return writer;
        };

        /**
         * Decodes an IncrementalSecurityDataRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.IncrementalSecurityDataRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.IncrementalSecurityDataRequest} IncrementalSecurityDataRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IncrementalSecurityDataRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.IncrementalSecurityDataRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.continuationToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an IncrementalSecurityDataRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.IncrementalSecurityDataRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.IncrementalSecurityDataRequest} IncrementalSecurityDataRequest
         */
        IncrementalSecurityDataRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.IncrementalSecurityDataRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.IncrementalSecurityDataRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.IncrementalSecurityDataRequest();
            if (object.continuationToken != null)
                if (typeof object.continuationToken === "string")
                    $util.base64.decode(object.continuationToken, message.continuationToken = $util.newBuffer($util.base64.length(object.continuationToken)), 0);
                else if (object.continuationToken.length >= 0)
                    message.continuationToken = object.continuationToken;
            return message;
        };

        /**
         * Creates a plain object from an IncrementalSecurityDataRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.IncrementalSecurityDataRequest
         * @static
         * @param {Authentication.IncrementalSecurityDataRequest} message IncrementalSecurityDataRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        IncrementalSecurityDataRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === String)
                    object.continuationToken = "";
                else {
                    object.continuationToken = [];
                    if (options.bytes !== Array)
                        object.continuationToken = $util.newBuffer(object.continuationToken);
                }
            if (message.continuationToken != null && Object.hasOwnProperty.call(message, "continuationToken"))
                object.continuationToken = options.bytes === String ? $util.base64.encode(message.continuationToken, 0, message.continuationToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.continuationToken) : message.continuationToken;
            return object;
        };

        /**
         * Converts this IncrementalSecurityDataRequest to JSON.
         * @function toJSON
         * @memberof Authentication.IncrementalSecurityDataRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        IncrementalSecurityDataRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for IncrementalSecurityDataRequest
         * @function getTypeUrl
         * @memberof Authentication.IncrementalSecurityDataRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        IncrementalSecurityDataRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.IncrementalSecurityDataRequest";
        };

        return IncrementalSecurityDataRequest;
    })();

    Authentication.IncrementalSecurityDataResponse = (function() {

        /**
         * Properties of an IncrementalSecurityDataResponse.
         * @memberof Authentication
         * @interface IIncrementalSecurityDataResponse
         * @property {Array.<Authentication.ISecurityReportIncrementalData>|null} [securityReportIncrementalData] IncrementalSecurityDataResponse securityReportIncrementalData
         * @property {Uint8Array|null} [continuationToken] IncrementalSecurityDataResponse continuationToken
         */

        /**
         * Constructs a new IncrementalSecurityDataResponse.
         * @memberof Authentication
         * @classdesc Represents an IncrementalSecurityDataResponse.
         * @implements IIncrementalSecurityDataResponse
         * @constructor
         * @param {Authentication.IIncrementalSecurityDataResponse=} [properties] Properties to set
         */
        function IncrementalSecurityDataResponse(properties) {
            this.securityReportIncrementalData = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * IncrementalSecurityDataResponse securityReportIncrementalData.
         * @member {Array.<Authentication.ISecurityReportIncrementalData>} securityReportIncrementalData
         * @memberof Authentication.IncrementalSecurityDataResponse
         * @instance
         */
        IncrementalSecurityDataResponse.prototype.securityReportIncrementalData = $util.emptyArray;

        /**
         * IncrementalSecurityDataResponse continuationToken.
         * @member {Uint8Array} continuationToken
         * @memberof Authentication.IncrementalSecurityDataResponse
         * @instance
         */
        IncrementalSecurityDataResponse.prototype.continuationToken = $util.newBuffer([]);

        /**
         * Creates a new IncrementalSecurityDataResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.IncrementalSecurityDataResponse
         * @static
         * @param {Authentication.IIncrementalSecurityDataResponse=} [properties] Properties to set
         * @returns {Authentication.IncrementalSecurityDataResponse} IncrementalSecurityDataResponse instance
         */
        IncrementalSecurityDataResponse.create = function create(properties) {
            return new IncrementalSecurityDataResponse(properties);
        };

        /**
         * Encodes the specified IncrementalSecurityDataResponse message. Does not implicitly {@link Authentication.IncrementalSecurityDataResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.IncrementalSecurityDataResponse
         * @static
         * @param {Authentication.IIncrementalSecurityDataResponse} message IncrementalSecurityDataResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IncrementalSecurityDataResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.securityReportIncrementalData != null && message.securityReportIncrementalData.length)
                for (let i = 0; i < message.securityReportIncrementalData.length; ++i)
                    $root.Authentication.SecurityReportIncrementalData.encode(message.securityReportIncrementalData[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.continuationToken != null && Object.hasOwnProperty.call(message, "continuationToken"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.continuationToken);
            return writer;
        };

        /**
         * Decodes an IncrementalSecurityDataResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.IncrementalSecurityDataResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.IncrementalSecurityDataResponse} IncrementalSecurityDataResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IncrementalSecurityDataResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.IncrementalSecurityDataResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.securityReportIncrementalData && message.securityReportIncrementalData.length))
                            message.securityReportIncrementalData = [];
                        message.securityReportIncrementalData.push($root.Authentication.SecurityReportIncrementalData.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 2: {
                        message.continuationToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an IncrementalSecurityDataResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.IncrementalSecurityDataResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.IncrementalSecurityDataResponse} IncrementalSecurityDataResponse
         */
        IncrementalSecurityDataResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.IncrementalSecurityDataResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.IncrementalSecurityDataResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.IncrementalSecurityDataResponse();
            if (object.securityReportIncrementalData) {
                if (!Array.isArray(object.securityReportIncrementalData))
                    throw TypeError(".Authentication.IncrementalSecurityDataResponse.securityReportIncrementalData: array expected");
                message.securityReportIncrementalData = [];
                for (let i = 0; i < object.securityReportIncrementalData.length; ++i) {
                    if (!$util.isObject(object.securityReportIncrementalData[i]))
                        throw TypeError(".Authentication.IncrementalSecurityDataResponse.securityReportIncrementalData: object expected");
                    message.securityReportIncrementalData[i] = $root.Authentication.SecurityReportIncrementalData.fromObject(object.securityReportIncrementalData[i], long + 1);
                }
            }
            if (object.continuationToken != null)
                if (typeof object.continuationToken === "string")
                    $util.base64.decode(object.continuationToken, message.continuationToken = $util.newBuffer($util.base64.length(object.continuationToken)), 0);
                else if (object.continuationToken.length >= 0)
                    message.continuationToken = object.continuationToken;
            return message;
        };

        /**
         * Creates a plain object from an IncrementalSecurityDataResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.IncrementalSecurityDataResponse
         * @static
         * @param {Authentication.IncrementalSecurityDataResponse} message IncrementalSecurityDataResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        IncrementalSecurityDataResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.securityReportIncrementalData = [];
            if (options.defaults)
                if (options.bytes === String)
                    object.continuationToken = "";
                else {
                    object.continuationToken = [];
                    if (options.bytes !== Array)
                        object.continuationToken = $util.newBuffer(object.continuationToken);
                }
            if (message.securityReportIncrementalData && message.securityReportIncrementalData.length) {
                object.securityReportIncrementalData = [];
                for (let j = 0; j < message.securityReportIncrementalData.length; ++j)
                    object.securityReportIncrementalData[j] = $root.Authentication.SecurityReportIncrementalData.toObject(message.securityReportIncrementalData[j], options, q + 1);
            }
            if (message.continuationToken != null && Object.hasOwnProperty.call(message, "continuationToken"))
                object.continuationToken = options.bytes === String ? $util.base64.encode(message.continuationToken, 0, message.continuationToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.continuationToken) : message.continuationToken;
            return object;
        };

        /**
         * Converts this IncrementalSecurityDataResponse to JSON.
         * @function toJSON
         * @memberof Authentication.IncrementalSecurityDataResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        IncrementalSecurityDataResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for IncrementalSecurityDataResponse
         * @function getTypeUrl
         * @memberof Authentication.IncrementalSecurityDataResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        IncrementalSecurityDataResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.IncrementalSecurityDataResponse";
        };

        return IncrementalSecurityDataResponse;
    })();

    Authentication.ReusedPasswordsRequest = (function() {

        /**
         * Properties of a ReusedPasswordsRequest.
         * @memberof Authentication
         * @interface IReusedPasswordsRequest
         * @property {number|null} [count] ReusedPasswordsRequest count
         */

        /**
         * Constructs a new ReusedPasswordsRequest.
         * @memberof Authentication
         * @classdesc Represents a ReusedPasswordsRequest.
         * @implements IReusedPasswordsRequest
         * @constructor
         * @param {Authentication.IReusedPasswordsRequest=} [properties] Properties to set
         */
        function ReusedPasswordsRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReusedPasswordsRequest count.
         * @member {number} count
         * @memberof Authentication.ReusedPasswordsRequest
         * @instance
         */
        ReusedPasswordsRequest.prototype.count = 0;

        /**
         * Creates a new ReusedPasswordsRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.ReusedPasswordsRequest
         * @static
         * @param {Authentication.IReusedPasswordsRequest=} [properties] Properties to set
         * @returns {Authentication.ReusedPasswordsRequest} ReusedPasswordsRequest instance
         */
        ReusedPasswordsRequest.create = function create(properties) {
            return new ReusedPasswordsRequest(properties);
        };

        /**
         * Encodes the specified ReusedPasswordsRequest message. Does not implicitly {@link Authentication.ReusedPasswordsRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ReusedPasswordsRequest
         * @static
         * @param {Authentication.IReusedPasswordsRequest} message ReusedPasswordsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReusedPasswordsRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.count);
            return writer;
        };

        /**
         * Decodes a ReusedPasswordsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ReusedPasswordsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ReusedPasswordsRequest} ReusedPasswordsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReusedPasswordsRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ReusedPasswordsRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.count = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a ReusedPasswordsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ReusedPasswordsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ReusedPasswordsRequest} ReusedPasswordsRequest
         */
        ReusedPasswordsRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ReusedPasswordsRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ReusedPasswordsRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ReusedPasswordsRequest();
            if (object.count != null)
                message.count = object.count | 0;
            return message;
        };

        /**
         * Creates a plain object from a ReusedPasswordsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ReusedPasswordsRequest
         * @static
         * @param {Authentication.ReusedPasswordsRequest} message ReusedPasswordsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReusedPasswordsRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.count = 0;
            if (message.count != null && Object.hasOwnProperty.call(message, "count"))
                object.count = message.count;
            return object;
        };

        /**
         * Converts this ReusedPasswordsRequest to JSON.
         * @function toJSON
         * @memberof Authentication.ReusedPasswordsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReusedPasswordsRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReusedPasswordsRequest
         * @function getTypeUrl
         * @memberof Authentication.ReusedPasswordsRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReusedPasswordsRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ReusedPasswordsRequest";
        };

        return ReusedPasswordsRequest;
    })();

    Authentication.SummaryConsoleReport = (function() {

        /**
         * Properties of a SummaryConsoleReport.
         * @memberof Authentication
         * @interface ISummaryConsoleReport
         * @property {number|null} [reportType] SummaryConsoleReport reportType
         * @property {Uint8Array|null} [reportData] SummaryConsoleReport reportData
         */

        /**
         * Constructs a new SummaryConsoleReport.
         * @memberof Authentication
         * @classdesc Represents a SummaryConsoleReport.
         * @implements ISummaryConsoleReport
         * @constructor
         * @param {Authentication.ISummaryConsoleReport=} [properties] Properties to set
         */
        function SummaryConsoleReport(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SummaryConsoleReport reportType.
         * @member {number} reportType
         * @memberof Authentication.SummaryConsoleReport
         * @instance
         */
        SummaryConsoleReport.prototype.reportType = 0;

        /**
         * SummaryConsoleReport reportData.
         * @member {Uint8Array} reportData
         * @memberof Authentication.SummaryConsoleReport
         * @instance
         */
        SummaryConsoleReport.prototype.reportData = $util.newBuffer([]);

        /**
         * Creates a new SummaryConsoleReport instance using the specified properties.
         * @function create
         * @memberof Authentication.SummaryConsoleReport
         * @static
         * @param {Authentication.ISummaryConsoleReport=} [properties] Properties to set
         * @returns {Authentication.SummaryConsoleReport} SummaryConsoleReport instance
         */
        SummaryConsoleReport.create = function create(properties) {
            return new SummaryConsoleReport(properties);
        };

        /**
         * Encodes the specified SummaryConsoleReport message. Does not implicitly {@link Authentication.SummaryConsoleReport.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SummaryConsoleReport
         * @static
         * @param {Authentication.ISummaryConsoleReport} message SummaryConsoleReport message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SummaryConsoleReport.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.reportType != null && Object.hasOwnProperty.call(message, "reportType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.reportType);
            if (message.reportData != null && Object.hasOwnProperty.call(message, "reportData"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.reportData);
            return writer;
        };

        /**
         * Decodes a SummaryConsoleReport message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SummaryConsoleReport
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SummaryConsoleReport} SummaryConsoleReport
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SummaryConsoleReport.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SummaryConsoleReport();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.reportType = reader.int32();
                        break;
                    }
                case 2: {
                        message.reportData = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SummaryConsoleReport message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SummaryConsoleReport
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SummaryConsoleReport} SummaryConsoleReport
         */
        SummaryConsoleReport.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SummaryConsoleReport)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SummaryConsoleReport: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SummaryConsoleReport();
            if (object.reportType != null)
                message.reportType = object.reportType | 0;
            if (object.reportData != null)
                if (typeof object.reportData === "string")
                    $util.base64.decode(object.reportData, message.reportData = $util.newBuffer($util.base64.length(object.reportData)), 0);
                else if (object.reportData.length >= 0)
                    message.reportData = object.reportData;
            return message;
        };

        /**
         * Creates a plain object from a SummaryConsoleReport message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SummaryConsoleReport
         * @static
         * @param {Authentication.SummaryConsoleReport} message SummaryConsoleReport
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SummaryConsoleReport.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.reportType = 0;
                if (options.bytes === String)
                    object.reportData = "";
                else {
                    object.reportData = [];
                    if (options.bytes !== Array)
                        object.reportData = $util.newBuffer(object.reportData);
                }
            }
            if (message.reportType != null && Object.hasOwnProperty.call(message, "reportType"))
                object.reportType = message.reportType;
            if (message.reportData != null && Object.hasOwnProperty.call(message, "reportData"))
                object.reportData = options.bytes === String ? $util.base64.encode(message.reportData, 0, message.reportData.length) : options.bytes === Array ? Array.prototype.slice.call(message.reportData) : message.reportData;
            return object;
        };

        /**
         * Converts this SummaryConsoleReport to JSON.
         * @function toJSON
         * @memberof Authentication.SummaryConsoleReport
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SummaryConsoleReport.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SummaryConsoleReport
         * @function getTypeUrl
         * @memberof Authentication.SummaryConsoleReport
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SummaryConsoleReport.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SummaryConsoleReport";
        };

        return SummaryConsoleReport;
    })();

    /**
     * ObjectTypes enum.
     * @name Authentication.ObjectTypes
     * @enum {number}
     * @property {number} RECORD=0 RECORD value
     * @property {number} SHARED_FOLDER_USER=1 SHARED_FOLDER_USER value
     * @property {number} SHARED_FOLDER_TEAM=2 SHARED_FOLDER_TEAM value
     * @property {number} USER_FOLDER=3 USER_FOLDER value
     * @property {number} TEAM_USER=4 TEAM_USER value
     */
    Authentication.ObjectTypes = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "RECORD"] = 0;
        values[valuesById[1] = "SHARED_FOLDER_USER"] = 1;
        values[valuesById[2] = "SHARED_FOLDER_TEAM"] = 2;
        values[valuesById[3] = "USER_FOLDER"] = 3;
        values[valuesById[4] = "TEAM_USER"] = 4;
        return values;
    })();

    Authentication.ChangeToKeyTypeOne = (function() {

        /**
         * Properties of a ChangeToKeyTypeOne.
         * @memberof Authentication
         * @interface IChangeToKeyTypeOne
         * @property {Authentication.ObjectTypes|null} [objectType] ChangeToKeyTypeOne objectType
         * @property {Uint8Array|null} [primaryUid] ChangeToKeyTypeOne primaryUid
         * @property {Uint8Array|null} [secondaryUid] ChangeToKeyTypeOne secondaryUid
         * @property {Uint8Array|null} [key] ChangeToKeyTypeOne key
         */

        /**
         * Constructs a new ChangeToKeyTypeOne.
         * @memberof Authentication
         * @classdesc Represents a ChangeToKeyTypeOne.
         * @implements IChangeToKeyTypeOne
         * @constructor
         * @param {Authentication.IChangeToKeyTypeOne=} [properties] Properties to set
         */
        function ChangeToKeyTypeOne(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChangeToKeyTypeOne objectType.
         * @member {Authentication.ObjectTypes} objectType
         * @memberof Authentication.ChangeToKeyTypeOne
         * @instance
         */
        ChangeToKeyTypeOne.prototype.objectType = 0;

        /**
         * ChangeToKeyTypeOne primaryUid.
         * @member {Uint8Array} primaryUid
         * @memberof Authentication.ChangeToKeyTypeOne
         * @instance
         */
        ChangeToKeyTypeOne.prototype.primaryUid = $util.newBuffer([]);

        /**
         * ChangeToKeyTypeOne secondaryUid.
         * @member {Uint8Array} secondaryUid
         * @memberof Authentication.ChangeToKeyTypeOne
         * @instance
         */
        ChangeToKeyTypeOne.prototype.secondaryUid = $util.newBuffer([]);

        /**
         * ChangeToKeyTypeOne key.
         * @member {Uint8Array} key
         * @memberof Authentication.ChangeToKeyTypeOne
         * @instance
         */
        ChangeToKeyTypeOne.prototype.key = $util.newBuffer([]);

        /**
         * Creates a new ChangeToKeyTypeOne instance using the specified properties.
         * @function create
         * @memberof Authentication.ChangeToKeyTypeOne
         * @static
         * @param {Authentication.IChangeToKeyTypeOne=} [properties] Properties to set
         * @returns {Authentication.ChangeToKeyTypeOne} ChangeToKeyTypeOne instance
         */
        ChangeToKeyTypeOne.create = function create(properties) {
            return new ChangeToKeyTypeOne(properties);
        };

        /**
         * Encodes the specified ChangeToKeyTypeOne message. Does not implicitly {@link Authentication.ChangeToKeyTypeOne.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ChangeToKeyTypeOne
         * @static
         * @param {Authentication.IChangeToKeyTypeOne} message ChangeToKeyTypeOne message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangeToKeyTypeOne.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.objectType != null && Object.hasOwnProperty.call(message, "objectType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.objectType);
            if (message.primaryUid != null && Object.hasOwnProperty.call(message, "primaryUid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.primaryUid);
            if (message.secondaryUid != null && Object.hasOwnProperty.call(message, "secondaryUid"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.secondaryUid);
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.key);
            return writer;
        };

        /**
         * Decodes a ChangeToKeyTypeOne message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ChangeToKeyTypeOne
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ChangeToKeyTypeOne} ChangeToKeyTypeOne
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangeToKeyTypeOne.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ChangeToKeyTypeOne();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.objectType = reader.int32();
                        break;
                    }
                case 2: {
                        message.primaryUid = reader.bytes();
                        break;
                    }
                case 3: {
                        message.secondaryUid = reader.bytes();
                        break;
                    }
                case 4: {
                        message.key = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a ChangeToKeyTypeOne message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ChangeToKeyTypeOne
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ChangeToKeyTypeOne} ChangeToKeyTypeOne
         */
        ChangeToKeyTypeOne.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ChangeToKeyTypeOne)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ChangeToKeyTypeOne: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ChangeToKeyTypeOne();
            switch (object.objectType) {
            default:
                if (typeof object.objectType === "number") {
                    message.objectType = object.objectType;
                    break;
                }
                break;
            case "RECORD":
            case 0:
                message.objectType = 0;
                break;
            case "SHARED_FOLDER_USER":
            case 1:
                message.objectType = 1;
                break;
            case "SHARED_FOLDER_TEAM":
            case 2:
                message.objectType = 2;
                break;
            case "USER_FOLDER":
            case 3:
                message.objectType = 3;
                break;
            case "TEAM_USER":
            case 4:
                message.objectType = 4;
                break;
            }
            if (object.primaryUid != null)
                if (typeof object.primaryUid === "string")
                    $util.base64.decode(object.primaryUid, message.primaryUid = $util.newBuffer($util.base64.length(object.primaryUid)), 0);
                else if (object.primaryUid.length >= 0)
                    message.primaryUid = object.primaryUid;
            if (object.secondaryUid != null)
                if (typeof object.secondaryUid === "string")
                    $util.base64.decode(object.secondaryUid, message.secondaryUid = $util.newBuffer($util.base64.length(object.secondaryUid)), 0);
                else if (object.secondaryUid.length >= 0)
                    message.secondaryUid = object.secondaryUid;
            if (object.key != null)
                if (typeof object.key === "string")
                    $util.base64.decode(object.key, message.key = $util.newBuffer($util.base64.length(object.key)), 0);
                else if (object.key.length >= 0)
                    message.key = object.key;
            return message;
        };

        /**
         * Creates a plain object from a ChangeToKeyTypeOne message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ChangeToKeyTypeOne
         * @static
         * @param {Authentication.ChangeToKeyTypeOne} message ChangeToKeyTypeOne
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChangeToKeyTypeOne.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.objectType = options.enums === String ? "RECORD" : 0;
                if (options.bytes === String)
                    object.primaryUid = "";
                else {
                    object.primaryUid = [];
                    if (options.bytes !== Array)
                        object.primaryUid = $util.newBuffer(object.primaryUid);
                }
                if (options.bytes === String)
                    object.secondaryUid = "";
                else {
                    object.secondaryUid = [];
                    if (options.bytes !== Array)
                        object.secondaryUid = $util.newBuffer(object.secondaryUid);
                }
                if (options.bytes === String)
                    object.key = "";
                else {
                    object.key = [];
                    if (options.bytes !== Array)
                        object.key = $util.newBuffer(object.key);
                }
            }
            if (message.objectType != null && Object.hasOwnProperty.call(message, "objectType"))
                object.objectType = options.enums === String ? $root.Authentication.ObjectTypes[message.objectType] === undefined ? message.objectType : $root.Authentication.ObjectTypes[message.objectType] : message.objectType;
            if (message.primaryUid != null && Object.hasOwnProperty.call(message, "primaryUid"))
                object.primaryUid = options.bytes === String ? $util.base64.encode(message.primaryUid, 0, message.primaryUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.primaryUid) : message.primaryUid;
            if (message.secondaryUid != null && Object.hasOwnProperty.call(message, "secondaryUid"))
                object.secondaryUid = options.bytes === String ? $util.base64.encode(message.secondaryUid, 0, message.secondaryUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.secondaryUid) : message.secondaryUid;
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                object.key = options.bytes === String ? $util.base64.encode(message.key, 0, message.key.length) : options.bytes === Array ? Array.prototype.slice.call(message.key) : message.key;
            return object;
        };

        /**
         * Converts this ChangeToKeyTypeOne to JSON.
         * @function toJSON
         * @memberof Authentication.ChangeToKeyTypeOne
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChangeToKeyTypeOne.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChangeToKeyTypeOne
         * @function getTypeUrl
         * @memberof Authentication.ChangeToKeyTypeOne
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChangeToKeyTypeOne.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ChangeToKeyTypeOne";
        };

        return ChangeToKeyTypeOne;
    })();

    Authentication.ChangeToKeyTypeOneRequest = (function() {

        /**
         * Properties of a ChangeToKeyTypeOneRequest.
         * @memberof Authentication
         * @interface IChangeToKeyTypeOneRequest
         * @property {Array.<Authentication.IChangeToKeyTypeOne>|null} [changeToKeyTypeOne] ChangeToKeyTypeOneRequest changeToKeyTypeOne
         */

        /**
         * Constructs a new ChangeToKeyTypeOneRequest.
         * @memberof Authentication
         * @classdesc Represents a ChangeToKeyTypeOneRequest.
         * @implements IChangeToKeyTypeOneRequest
         * @constructor
         * @param {Authentication.IChangeToKeyTypeOneRequest=} [properties] Properties to set
         */
        function ChangeToKeyTypeOneRequest(properties) {
            this.changeToKeyTypeOne = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChangeToKeyTypeOneRequest changeToKeyTypeOne.
         * @member {Array.<Authentication.IChangeToKeyTypeOne>} changeToKeyTypeOne
         * @memberof Authentication.ChangeToKeyTypeOneRequest
         * @instance
         */
        ChangeToKeyTypeOneRequest.prototype.changeToKeyTypeOne = $util.emptyArray;

        /**
         * Creates a new ChangeToKeyTypeOneRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.ChangeToKeyTypeOneRequest
         * @static
         * @param {Authentication.IChangeToKeyTypeOneRequest=} [properties] Properties to set
         * @returns {Authentication.ChangeToKeyTypeOneRequest} ChangeToKeyTypeOneRequest instance
         */
        ChangeToKeyTypeOneRequest.create = function create(properties) {
            return new ChangeToKeyTypeOneRequest(properties);
        };

        /**
         * Encodes the specified ChangeToKeyTypeOneRequest message. Does not implicitly {@link Authentication.ChangeToKeyTypeOneRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ChangeToKeyTypeOneRequest
         * @static
         * @param {Authentication.IChangeToKeyTypeOneRequest} message ChangeToKeyTypeOneRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangeToKeyTypeOneRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.changeToKeyTypeOne != null && message.changeToKeyTypeOne.length)
                for (let i = 0; i < message.changeToKeyTypeOne.length; ++i)
                    $root.Authentication.ChangeToKeyTypeOne.encode(message.changeToKeyTypeOne[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes a ChangeToKeyTypeOneRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ChangeToKeyTypeOneRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ChangeToKeyTypeOneRequest} ChangeToKeyTypeOneRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangeToKeyTypeOneRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ChangeToKeyTypeOneRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.changeToKeyTypeOne && message.changeToKeyTypeOne.length))
                            message.changeToKeyTypeOne = [];
                        message.changeToKeyTypeOne.push($root.Authentication.ChangeToKeyTypeOne.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a ChangeToKeyTypeOneRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ChangeToKeyTypeOneRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ChangeToKeyTypeOneRequest} ChangeToKeyTypeOneRequest
         */
        ChangeToKeyTypeOneRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ChangeToKeyTypeOneRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ChangeToKeyTypeOneRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ChangeToKeyTypeOneRequest();
            if (object.changeToKeyTypeOne) {
                if (!Array.isArray(object.changeToKeyTypeOne))
                    throw TypeError(".Authentication.ChangeToKeyTypeOneRequest.changeToKeyTypeOne: array expected");
                message.changeToKeyTypeOne = [];
                for (let i = 0; i < object.changeToKeyTypeOne.length; ++i) {
                    if (!$util.isObject(object.changeToKeyTypeOne[i]))
                        throw TypeError(".Authentication.ChangeToKeyTypeOneRequest.changeToKeyTypeOne: object expected");
                    message.changeToKeyTypeOne[i] = $root.Authentication.ChangeToKeyTypeOne.fromObject(object.changeToKeyTypeOne[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ChangeToKeyTypeOneRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ChangeToKeyTypeOneRequest
         * @static
         * @param {Authentication.ChangeToKeyTypeOneRequest} message ChangeToKeyTypeOneRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChangeToKeyTypeOneRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.changeToKeyTypeOne = [];
            if (message.changeToKeyTypeOne && message.changeToKeyTypeOne.length) {
                object.changeToKeyTypeOne = [];
                for (let j = 0; j < message.changeToKeyTypeOne.length; ++j)
                    object.changeToKeyTypeOne[j] = $root.Authentication.ChangeToKeyTypeOne.toObject(message.changeToKeyTypeOne[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this ChangeToKeyTypeOneRequest to JSON.
         * @function toJSON
         * @memberof Authentication.ChangeToKeyTypeOneRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChangeToKeyTypeOneRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChangeToKeyTypeOneRequest
         * @function getTypeUrl
         * @memberof Authentication.ChangeToKeyTypeOneRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChangeToKeyTypeOneRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ChangeToKeyTypeOneRequest";
        };

        return ChangeToKeyTypeOneRequest;
    })();

    Authentication.ChangeToKeyTypeOneStatus = (function() {

        /**
         * Properties of a ChangeToKeyTypeOneStatus.
         * @memberof Authentication
         * @interface IChangeToKeyTypeOneStatus
         * @property {Uint8Array|null} [uid] ChangeToKeyTypeOneStatus uid
         * @property {string|null} [type] ChangeToKeyTypeOneStatus type
         * @property {string|null} [status] ChangeToKeyTypeOneStatus status
         * @property {string|null} [reason] ChangeToKeyTypeOneStatus reason
         */

        /**
         * Constructs a new ChangeToKeyTypeOneStatus.
         * @memberof Authentication
         * @classdesc Represents a ChangeToKeyTypeOneStatus.
         * @implements IChangeToKeyTypeOneStatus
         * @constructor
         * @param {Authentication.IChangeToKeyTypeOneStatus=} [properties] Properties to set
         */
        function ChangeToKeyTypeOneStatus(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChangeToKeyTypeOneStatus uid.
         * @member {Uint8Array} uid
         * @memberof Authentication.ChangeToKeyTypeOneStatus
         * @instance
         */
        ChangeToKeyTypeOneStatus.prototype.uid = $util.newBuffer([]);

        /**
         * ChangeToKeyTypeOneStatus type.
         * @member {string} type
         * @memberof Authentication.ChangeToKeyTypeOneStatus
         * @instance
         */
        ChangeToKeyTypeOneStatus.prototype.type = "";

        /**
         * ChangeToKeyTypeOneStatus status.
         * @member {string} status
         * @memberof Authentication.ChangeToKeyTypeOneStatus
         * @instance
         */
        ChangeToKeyTypeOneStatus.prototype.status = "";

        /**
         * ChangeToKeyTypeOneStatus reason.
         * @member {string} reason
         * @memberof Authentication.ChangeToKeyTypeOneStatus
         * @instance
         */
        ChangeToKeyTypeOneStatus.prototype.reason = "";

        /**
         * Creates a new ChangeToKeyTypeOneStatus instance using the specified properties.
         * @function create
         * @memberof Authentication.ChangeToKeyTypeOneStatus
         * @static
         * @param {Authentication.IChangeToKeyTypeOneStatus=} [properties] Properties to set
         * @returns {Authentication.ChangeToKeyTypeOneStatus} ChangeToKeyTypeOneStatus instance
         */
        ChangeToKeyTypeOneStatus.create = function create(properties) {
            return new ChangeToKeyTypeOneStatus(properties);
        };

        /**
         * Encodes the specified ChangeToKeyTypeOneStatus message. Does not implicitly {@link Authentication.ChangeToKeyTypeOneStatus.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ChangeToKeyTypeOneStatus
         * @static
         * @param {Authentication.IChangeToKeyTypeOneStatus} message ChangeToKeyTypeOneStatus message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangeToKeyTypeOneStatus.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.uid);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.type);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.status);
            if (message.reason != null && Object.hasOwnProperty.call(message, "reason"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.reason);
            return writer;
        };

        /**
         * Decodes a ChangeToKeyTypeOneStatus message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ChangeToKeyTypeOneStatus
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ChangeToKeyTypeOneStatus} ChangeToKeyTypeOneStatus
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangeToKeyTypeOneStatus.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ChangeToKeyTypeOneStatus();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.type = reader.string();
                        break;
                    }
                case 3: {
                        message.status = reader.string();
                        break;
                    }
                case 4: {
                        message.reason = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a ChangeToKeyTypeOneStatus message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ChangeToKeyTypeOneStatus
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ChangeToKeyTypeOneStatus} ChangeToKeyTypeOneStatus
         */
        ChangeToKeyTypeOneStatus.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ChangeToKeyTypeOneStatus)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ChangeToKeyTypeOneStatus: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ChangeToKeyTypeOneStatus();
            if (object.uid != null)
                if (typeof object.uid === "string")
                    $util.base64.decode(object.uid, message.uid = $util.newBuffer($util.base64.length(object.uid)), 0);
                else if (object.uid.length >= 0)
                    message.uid = object.uid;
            if (object.type != null)
                message.type = String(object.type);
            if (object.status != null)
                message.status = String(object.status);
            if (object.reason != null)
                message.reason = String(object.reason);
            return message;
        };

        /**
         * Creates a plain object from a ChangeToKeyTypeOneStatus message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ChangeToKeyTypeOneStatus
         * @static
         * @param {Authentication.ChangeToKeyTypeOneStatus} message ChangeToKeyTypeOneStatus
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChangeToKeyTypeOneStatus.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.uid = "";
                else {
                    object.uid = [];
                    if (options.bytes !== Array)
                        object.uid = $util.newBuffer(object.uid);
                }
                object.type = "";
                object.status = "";
                object.reason = "";
            }
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                object.uid = options.bytes === String ? $util.base64.encode(message.uid, 0, message.uid.length) : options.bytes === Array ? Array.prototype.slice.call(message.uid) : message.uid;
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                object.type = message.type;
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            if (message.reason != null && Object.hasOwnProperty.call(message, "reason"))
                object.reason = message.reason;
            return object;
        };

        /**
         * Converts this ChangeToKeyTypeOneStatus to JSON.
         * @function toJSON
         * @memberof Authentication.ChangeToKeyTypeOneStatus
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChangeToKeyTypeOneStatus.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChangeToKeyTypeOneStatus
         * @function getTypeUrl
         * @memberof Authentication.ChangeToKeyTypeOneStatus
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChangeToKeyTypeOneStatus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ChangeToKeyTypeOneStatus";
        };

        return ChangeToKeyTypeOneStatus;
    })();

    Authentication.ChangeToKeyTypeOneResponse = (function() {

        /**
         * Properties of a ChangeToKeyTypeOneResponse.
         * @memberof Authentication
         * @interface IChangeToKeyTypeOneResponse
         * @property {Array.<Authentication.IChangeToKeyTypeOneStatus>|null} [changeToKeyTypeOneStatus] ChangeToKeyTypeOneResponse changeToKeyTypeOneStatus
         */

        /**
         * Constructs a new ChangeToKeyTypeOneResponse.
         * @memberof Authentication
         * @classdesc Represents a ChangeToKeyTypeOneResponse.
         * @implements IChangeToKeyTypeOneResponse
         * @constructor
         * @param {Authentication.IChangeToKeyTypeOneResponse=} [properties] Properties to set
         */
        function ChangeToKeyTypeOneResponse(properties) {
            this.changeToKeyTypeOneStatus = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChangeToKeyTypeOneResponse changeToKeyTypeOneStatus.
         * @member {Array.<Authentication.IChangeToKeyTypeOneStatus>} changeToKeyTypeOneStatus
         * @memberof Authentication.ChangeToKeyTypeOneResponse
         * @instance
         */
        ChangeToKeyTypeOneResponse.prototype.changeToKeyTypeOneStatus = $util.emptyArray;

        /**
         * Creates a new ChangeToKeyTypeOneResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.ChangeToKeyTypeOneResponse
         * @static
         * @param {Authentication.IChangeToKeyTypeOneResponse=} [properties] Properties to set
         * @returns {Authentication.ChangeToKeyTypeOneResponse} ChangeToKeyTypeOneResponse instance
         */
        ChangeToKeyTypeOneResponse.create = function create(properties) {
            return new ChangeToKeyTypeOneResponse(properties);
        };

        /**
         * Encodes the specified ChangeToKeyTypeOneResponse message. Does not implicitly {@link Authentication.ChangeToKeyTypeOneResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ChangeToKeyTypeOneResponse
         * @static
         * @param {Authentication.IChangeToKeyTypeOneResponse} message ChangeToKeyTypeOneResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangeToKeyTypeOneResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.changeToKeyTypeOneStatus != null && message.changeToKeyTypeOneStatus.length)
                for (let i = 0; i < message.changeToKeyTypeOneStatus.length; ++i)
                    $root.Authentication.ChangeToKeyTypeOneStatus.encode(message.changeToKeyTypeOneStatus[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes a ChangeToKeyTypeOneResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ChangeToKeyTypeOneResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ChangeToKeyTypeOneResponse} ChangeToKeyTypeOneResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangeToKeyTypeOneResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ChangeToKeyTypeOneResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.changeToKeyTypeOneStatus && message.changeToKeyTypeOneStatus.length))
                            message.changeToKeyTypeOneStatus = [];
                        message.changeToKeyTypeOneStatus.push($root.Authentication.ChangeToKeyTypeOneStatus.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a ChangeToKeyTypeOneResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ChangeToKeyTypeOneResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ChangeToKeyTypeOneResponse} ChangeToKeyTypeOneResponse
         */
        ChangeToKeyTypeOneResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ChangeToKeyTypeOneResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ChangeToKeyTypeOneResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ChangeToKeyTypeOneResponse();
            if (object.changeToKeyTypeOneStatus) {
                if (!Array.isArray(object.changeToKeyTypeOneStatus))
                    throw TypeError(".Authentication.ChangeToKeyTypeOneResponse.changeToKeyTypeOneStatus: array expected");
                message.changeToKeyTypeOneStatus = [];
                for (let i = 0; i < object.changeToKeyTypeOneStatus.length; ++i) {
                    if (!$util.isObject(object.changeToKeyTypeOneStatus[i]))
                        throw TypeError(".Authentication.ChangeToKeyTypeOneResponse.changeToKeyTypeOneStatus: object expected");
                    message.changeToKeyTypeOneStatus[i] = $root.Authentication.ChangeToKeyTypeOneStatus.fromObject(object.changeToKeyTypeOneStatus[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ChangeToKeyTypeOneResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ChangeToKeyTypeOneResponse
         * @static
         * @param {Authentication.ChangeToKeyTypeOneResponse} message ChangeToKeyTypeOneResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChangeToKeyTypeOneResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.changeToKeyTypeOneStatus = [];
            if (message.changeToKeyTypeOneStatus && message.changeToKeyTypeOneStatus.length) {
                object.changeToKeyTypeOneStatus = [];
                for (let j = 0; j < message.changeToKeyTypeOneStatus.length; ++j)
                    object.changeToKeyTypeOneStatus[j] = $root.Authentication.ChangeToKeyTypeOneStatus.toObject(message.changeToKeyTypeOneStatus[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this ChangeToKeyTypeOneResponse to JSON.
         * @function toJSON
         * @memberof Authentication.ChangeToKeyTypeOneResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChangeToKeyTypeOneResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChangeToKeyTypeOneResponse
         * @function getTypeUrl
         * @memberof Authentication.ChangeToKeyTypeOneResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChangeToKeyTypeOneResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ChangeToKeyTypeOneResponse";
        };

        return ChangeToKeyTypeOneResponse;
    })();

    /**
     * EncryptedObjectType enum.
     * @name Authentication.EncryptedObjectType
     * @enum {number}
     * @property {number} EOT_UNSPECIFIED=0 EOT_UNSPECIFIED value
     * @property {number} EOT_RECORD_KEY=1 EOT_RECORD_KEY value
     * @property {number} EOT_SHARED_FOLDER_USER_KEY=2 EOT_SHARED_FOLDER_USER_KEY value
     * @property {number} EOT_SHARED_FOLDER_TEAM_KEY=3 EOT_SHARED_FOLDER_TEAM_KEY value
     * @property {number} EOT_TEAM_USER_KEY=4 EOT_TEAM_USER_KEY value
     * @property {number} EOT_USER_FOLDER_KEY=5 EOT_USER_FOLDER_KEY value
     * @property {number} EOT_SECURITY_DATA=6 EOT_SECURITY_DATA value
     * @property {number} EOT_SECURITY_DATA_MASTER_PASSWORD=7 EOT_SECURITY_DATA_MASTER_PASSWORD value
     * @property {number} EOT_EMERGENCY_ACCESS_KEY=8 EOT_EMERGENCY_ACCESS_KEY value
     * @property {number} EOT_V2_RECORD_KEY=9 EOT_V2_RECORD_KEY value
     */
    Authentication.EncryptedObjectType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "EOT_UNSPECIFIED"] = 0;
        values[valuesById[1] = "EOT_RECORD_KEY"] = 1;
        values[valuesById[2] = "EOT_SHARED_FOLDER_USER_KEY"] = 2;
        values[valuesById[3] = "EOT_SHARED_FOLDER_TEAM_KEY"] = 3;
        values[valuesById[4] = "EOT_TEAM_USER_KEY"] = 4;
        values[valuesById[5] = "EOT_USER_FOLDER_KEY"] = 5;
        values[valuesById[6] = "EOT_SECURITY_DATA"] = 6;
        values[valuesById[7] = "EOT_SECURITY_DATA_MASTER_PASSWORD"] = 7;
        values[valuesById[8] = "EOT_EMERGENCY_ACCESS_KEY"] = 8;
        values[valuesById[9] = "EOT_V2_RECORD_KEY"] = 9;
        return values;
    })();

    Authentication.GetChangeKeyTypesRequest = (function() {

        /**
         * Properties of a GetChangeKeyTypesRequest.
         * @memberof Authentication
         * @interface IGetChangeKeyTypesRequest
         * @property {Array.<Authentication.EncryptedObjectType>|null} [onlyTheseObjects] GetChangeKeyTypesRequest onlyTheseObjects
         * @property {number|null} [limit] GetChangeKeyTypesRequest limit
         * @property {boolean|null} [includeRecommended] GetChangeKeyTypesRequest includeRecommended
         * @property {boolean|null} [includeKeys] GetChangeKeyTypesRequest includeKeys
         * @property {boolean|null} [includeAllowedKeyTypes] GetChangeKeyTypesRequest includeAllowedKeyTypes
         */

        /**
         * Constructs a new GetChangeKeyTypesRequest.
         * @memberof Authentication
         * @classdesc Represents a GetChangeKeyTypesRequest.
         * @implements IGetChangeKeyTypesRequest
         * @constructor
         * @param {Authentication.IGetChangeKeyTypesRequest=} [properties] Properties to set
         */
        function GetChangeKeyTypesRequest(properties) {
            this.onlyTheseObjects = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetChangeKeyTypesRequest onlyTheseObjects.
         * @member {Array.<Authentication.EncryptedObjectType>} onlyTheseObjects
         * @memberof Authentication.GetChangeKeyTypesRequest
         * @instance
         */
        GetChangeKeyTypesRequest.prototype.onlyTheseObjects = $util.emptyArray;

        /**
         * GetChangeKeyTypesRequest limit.
         * @member {number} limit
         * @memberof Authentication.GetChangeKeyTypesRequest
         * @instance
         */
        GetChangeKeyTypesRequest.prototype.limit = 0;

        /**
         * GetChangeKeyTypesRequest includeRecommended.
         * @member {boolean} includeRecommended
         * @memberof Authentication.GetChangeKeyTypesRequest
         * @instance
         */
        GetChangeKeyTypesRequest.prototype.includeRecommended = false;

        /**
         * GetChangeKeyTypesRequest includeKeys.
         * @member {boolean} includeKeys
         * @memberof Authentication.GetChangeKeyTypesRequest
         * @instance
         */
        GetChangeKeyTypesRequest.prototype.includeKeys = false;

        /**
         * GetChangeKeyTypesRequest includeAllowedKeyTypes.
         * @member {boolean} includeAllowedKeyTypes
         * @memberof Authentication.GetChangeKeyTypesRequest
         * @instance
         */
        GetChangeKeyTypesRequest.prototype.includeAllowedKeyTypes = false;

        /**
         * Creates a new GetChangeKeyTypesRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.GetChangeKeyTypesRequest
         * @static
         * @param {Authentication.IGetChangeKeyTypesRequest=} [properties] Properties to set
         * @returns {Authentication.GetChangeKeyTypesRequest} GetChangeKeyTypesRequest instance
         */
        GetChangeKeyTypesRequest.create = function create(properties) {
            return new GetChangeKeyTypesRequest(properties);
        };

        /**
         * Encodes the specified GetChangeKeyTypesRequest message. Does not implicitly {@link Authentication.GetChangeKeyTypesRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.GetChangeKeyTypesRequest
         * @static
         * @param {Authentication.IGetChangeKeyTypesRequest} message GetChangeKeyTypesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetChangeKeyTypesRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.onlyTheseObjects != null && message.onlyTheseObjects.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.onlyTheseObjects.length; ++i)
                    writer.int32(message.onlyTheseObjects[i]);
                writer.ldelim();
            }
            if (message.limit != null && Object.hasOwnProperty.call(message, "limit"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.limit);
            if (message.includeRecommended != null && Object.hasOwnProperty.call(message, "includeRecommended"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.includeRecommended);
            if (message.includeKeys != null && Object.hasOwnProperty.call(message, "includeKeys"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.includeKeys);
            if (message.includeAllowedKeyTypes != null && Object.hasOwnProperty.call(message, "includeAllowedKeyTypes"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.includeAllowedKeyTypes);
            return writer;
        };

        /**
         * Decodes a GetChangeKeyTypesRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.GetChangeKeyTypesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.GetChangeKeyTypesRequest} GetChangeKeyTypesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetChangeKeyTypesRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.GetChangeKeyTypesRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.onlyTheseObjects && message.onlyTheseObjects.length))
                            message.onlyTheseObjects = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.onlyTheseObjects.push(reader.int32());
                        } else
                            message.onlyTheseObjects.push(reader.int32());
                        break;
                    }
                case 2: {
                        message.limit = reader.int32();
                        break;
                    }
                case 3: {
                        message.includeRecommended = reader.bool();
                        break;
                    }
                case 4: {
                        message.includeKeys = reader.bool();
                        break;
                    }
                case 5: {
                        message.includeAllowedKeyTypes = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a GetChangeKeyTypesRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.GetChangeKeyTypesRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.GetChangeKeyTypesRequest} GetChangeKeyTypesRequest
         */
        GetChangeKeyTypesRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.GetChangeKeyTypesRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.GetChangeKeyTypesRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.GetChangeKeyTypesRequest();
            if (object.onlyTheseObjects) {
                if (!Array.isArray(object.onlyTheseObjects))
                    throw TypeError(".Authentication.GetChangeKeyTypesRequest.onlyTheseObjects: array expected");
                message.onlyTheseObjects = [];
                for (let i = 0; i < object.onlyTheseObjects.length; ++i)
                    switch (object.onlyTheseObjects[i]) {
                    default:
                        if (typeof object.onlyTheseObjects[i] === "number") {
                            message.onlyTheseObjects[i] = object.onlyTheseObjects[i];
                            break;
                        }
                    case "EOT_UNSPECIFIED":
                    case 0:
                        message.onlyTheseObjects[i] = 0;
                        break;
                    case "EOT_RECORD_KEY":
                    case 1:
                        message.onlyTheseObjects[i] = 1;
                        break;
                    case "EOT_SHARED_FOLDER_USER_KEY":
                    case 2:
                        message.onlyTheseObjects[i] = 2;
                        break;
                    case "EOT_SHARED_FOLDER_TEAM_KEY":
                    case 3:
                        message.onlyTheseObjects[i] = 3;
                        break;
                    case "EOT_TEAM_USER_KEY":
                    case 4:
                        message.onlyTheseObjects[i] = 4;
                        break;
                    case "EOT_USER_FOLDER_KEY":
                    case 5:
                        message.onlyTheseObjects[i] = 5;
                        break;
                    case "EOT_SECURITY_DATA":
                    case 6:
                        message.onlyTheseObjects[i] = 6;
                        break;
                    case "EOT_SECURITY_DATA_MASTER_PASSWORD":
                    case 7:
                        message.onlyTheseObjects[i] = 7;
                        break;
                    case "EOT_EMERGENCY_ACCESS_KEY":
                    case 8:
                        message.onlyTheseObjects[i] = 8;
                        break;
                    case "EOT_V2_RECORD_KEY":
                    case 9:
                        message.onlyTheseObjects[i] = 9;
                        break;
                    }
            }
            if (object.limit != null)
                message.limit = object.limit | 0;
            if (object.includeRecommended != null)
                message.includeRecommended = Boolean(object.includeRecommended);
            if (object.includeKeys != null)
                message.includeKeys = Boolean(object.includeKeys);
            if (object.includeAllowedKeyTypes != null)
                message.includeAllowedKeyTypes = Boolean(object.includeAllowedKeyTypes);
            return message;
        };

        /**
         * Creates a plain object from a GetChangeKeyTypesRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.GetChangeKeyTypesRequest
         * @static
         * @param {Authentication.GetChangeKeyTypesRequest} message GetChangeKeyTypesRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetChangeKeyTypesRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.onlyTheseObjects = [];
            if (options.defaults) {
                object.limit = 0;
                object.includeRecommended = false;
                object.includeKeys = false;
                object.includeAllowedKeyTypes = false;
            }
            if (message.onlyTheseObjects && message.onlyTheseObjects.length) {
                object.onlyTheseObjects = [];
                for (let j = 0; j < message.onlyTheseObjects.length; ++j)
                    object.onlyTheseObjects[j] = options.enums === String ? $root.Authentication.EncryptedObjectType[message.onlyTheseObjects[j]] === undefined ? message.onlyTheseObjects[j] : $root.Authentication.EncryptedObjectType[message.onlyTheseObjects[j]] : message.onlyTheseObjects[j];
            }
            if (message.limit != null && Object.hasOwnProperty.call(message, "limit"))
                object.limit = message.limit;
            if (message.includeRecommended != null && Object.hasOwnProperty.call(message, "includeRecommended"))
                object.includeRecommended = message.includeRecommended;
            if (message.includeKeys != null && Object.hasOwnProperty.call(message, "includeKeys"))
                object.includeKeys = message.includeKeys;
            if (message.includeAllowedKeyTypes != null && Object.hasOwnProperty.call(message, "includeAllowedKeyTypes"))
                object.includeAllowedKeyTypes = message.includeAllowedKeyTypes;
            return object;
        };

        /**
         * Converts this GetChangeKeyTypesRequest to JSON.
         * @function toJSON
         * @memberof Authentication.GetChangeKeyTypesRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetChangeKeyTypesRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetChangeKeyTypesRequest
         * @function getTypeUrl
         * @memberof Authentication.GetChangeKeyTypesRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetChangeKeyTypesRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.GetChangeKeyTypesRequest";
        };

        return GetChangeKeyTypesRequest;
    })();

    Authentication.GetChangeKeyTypesResponse = (function() {

        /**
         * Properties of a GetChangeKeyTypesResponse.
         * @memberof Authentication
         * @interface IGetChangeKeyTypesResponse
         * @property {Array.<Authentication.IChangeKeyType>|null} [keys] GetChangeKeyTypesResponse keys
         * @property {Array.<Authentication.IAllowedKeyTypes>|null} [allowedKeyTypes] GetChangeKeyTypesResponse allowedKeyTypes
         */

        /**
         * Constructs a new GetChangeKeyTypesResponse.
         * @memberof Authentication
         * @classdesc Represents a GetChangeKeyTypesResponse.
         * @implements IGetChangeKeyTypesResponse
         * @constructor
         * @param {Authentication.IGetChangeKeyTypesResponse=} [properties] Properties to set
         */
        function GetChangeKeyTypesResponse(properties) {
            this.keys = [];
            this.allowedKeyTypes = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetChangeKeyTypesResponse keys.
         * @member {Array.<Authentication.IChangeKeyType>} keys
         * @memberof Authentication.GetChangeKeyTypesResponse
         * @instance
         */
        GetChangeKeyTypesResponse.prototype.keys = $util.emptyArray;

        /**
         * GetChangeKeyTypesResponse allowedKeyTypes.
         * @member {Array.<Authentication.IAllowedKeyTypes>} allowedKeyTypes
         * @memberof Authentication.GetChangeKeyTypesResponse
         * @instance
         */
        GetChangeKeyTypesResponse.prototype.allowedKeyTypes = $util.emptyArray;

        /**
         * Creates a new GetChangeKeyTypesResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.GetChangeKeyTypesResponse
         * @static
         * @param {Authentication.IGetChangeKeyTypesResponse=} [properties] Properties to set
         * @returns {Authentication.GetChangeKeyTypesResponse} GetChangeKeyTypesResponse instance
         */
        GetChangeKeyTypesResponse.create = function create(properties) {
            return new GetChangeKeyTypesResponse(properties);
        };

        /**
         * Encodes the specified GetChangeKeyTypesResponse message. Does not implicitly {@link Authentication.GetChangeKeyTypesResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.GetChangeKeyTypesResponse
         * @static
         * @param {Authentication.IGetChangeKeyTypesResponse} message GetChangeKeyTypesResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetChangeKeyTypesResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.keys != null && message.keys.length)
                for (let i = 0; i < message.keys.length; ++i)
                    $root.Authentication.ChangeKeyType.encode(message.keys[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.allowedKeyTypes != null && message.allowedKeyTypes.length)
                for (let i = 0; i < message.allowedKeyTypes.length; ++i)
                    $root.Authentication.AllowedKeyTypes.encode(message.allowedKeyTypes[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes a GetChangeKeyTypesResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.GetChangeKeyTypesResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.GetChangeKeyTypesResponse} GetChangeKeyTypesResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetChangeKeyTypesResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.GetChangeKeyTypesResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.keys && message.keys.length))
                            message.keys = [];
                        message.keys.push($root.Authentication.ChangeKeyType.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 2: {
                        if (!(message.allowedKeyTypes && message.allowedKeyTypes.length))
                            message.allowedKeyTypes = [];
                        message.allowedKeyTypes.push($root.Authentication.AllowedKeyTypes.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a GetChangeKeyTypesResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.GetChangeKeyTypesResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.GetChangeKeyTypesResponse} GetChangeKeyTypesResponse
         */
        GetChangeKeyTypesResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.GetChangeKeyTypesResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.GetChangeKeyTypesResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.GetChangeKeyTypesResponse();
            if (object.keys) {
                if (!Array.isArray(object.keys))
                    throw TypeError(".Authentication.GetChangeKeyTypesResponse.keys: array expected");
                message.keys = [];
                for (let i = 0; i < object.keys.length; ++i) {
                    if (!$util.isObject(object.keys[i]))
                        throw TypeError(".Authentication.GetChangeKeyTypesResponse.keys: object expected");
                    message.keys[i] = $root.Authentication.ChangeKeyType.fromObject(object.keys[i], long + 1);
                }
            }
            if (object.allowedKeyTypes) {
                if (!Array.isArray(object.allowedKeyTypes))
                    throw TypeError(".Authentication.GetChangeKeyTypesResponse.allowedKeyTypes: array expected");
                message.allowedKeyTypes = [];
                for (let i = 0; i < object.allowedKeyTypes.length; ++i) {
                    if (!$util.isObject(object.allowedKeyTypes[i]))
                        throw TypeError(".Authentication.GetChangeKeyTypesResponse.allowedKeyTypes: object expected");
                    message.allowedKeyTypes[i] = $root.Authentication.AllowedKeyTypes.fromObject(object.allowedKeyTypes[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GetChangeKeyTypesResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.GetChangeKeyTypesResponse
         * @static
         * @param {Authentication.GetChangeKeyTypesResponse} message GetChangeKeyTypesResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetChangeKeyTypesResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.keys = [];
                object.allowedKeyTypes = [];
            }
            if (message.keys && message.keys.length) {
                object.keys = [];
                for (let j = 0; j < message.keys.length; ++j)
                    object.keys[j] = $root.Authentication.ChangeKeyType.toObject(message.keys[j], options, q + 1);
            }
            if (message.allowedKeyTypes && message.allowedKeyTypes.length) {
                object.allowedKeyTypes = [];
                for (let j = 0; j < message.allowedKeyTypes.length; ++j)
                    object.allowedKeyTypes[j] = $root.Authentication.AllowedKeyTypes.toObject(message.allowedKeyTypes[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this GetChangeKeyTypesResponse to JSON.
         * @function toJSON
         * @memberof Authentication.GetChangeKeyTypesResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetChangeKeyTypesResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetChangeKeyTypesResponse
         * @function getTypeUrl
         * @memberof Authentication.GetChangeKeyTypesResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetChangeKeyTypesResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.GetChangeKeyTypesResponse";
        };

        return GetChangeKeyTypesResponse;
    })();

    Authentication.AllowedKeyTypes = (function() {

        /**
         * Properties of an AllowedKeyTypes.
         * @memberof Authentication
         * @interface IAllowedKeyTypes
         * @property {Authentication.EncryptedObjectType|null} [objectType] AllowedKeyTypes objectType
         * @property {Array.<Enterprise.EncryptedKeyType>|null} [allowedKeyTypes] AllowedKeyTypes allowedKeyTypes
         */

        /**
         * Constructs a new AllowedKeyTypes.
         * @memberof Authentication
         * @classdesc Represents an AllowedKeyTypes.
         * @implements IAllowedKeyTypes
         * @constructor
         * @param {Authentication.IAllowedKeyTypes=} [properties] Properties to set
         */
        function AllowedKeyTypes(properties) {
            this.allowedKeyTypes = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AllowedKeyTypes objectType.
         * @member {Authentication.EncryptedObjectType} objectType
         * @memberof Authentication.AllowedKeyTypes
         * @instance
         */
        AllowedKeyTypes.prototype.objectType = 0;

        /**
         * AllowedKeyTypes allowedKeyTypes.
         * @member {Array.<Enterprise.EncryptedKeyType>} allowedKeyTypes
         * @memberof Authentication.AllowedKeyTypes
         * @instance
         */
        AllowedKeyTypes.prototype.allowedKeyTypes = $util.emptyArray;

        /**
         * Creates a new AllowedKeyTypes instance using the specified properties.
         * @function create
         * @memberof Authentication.AllowedKeyTypes
         * @static
         * @param {Authentication.IAllowedKeyTypes=} [properties] Properties to set
         * @returns {Authentication.AllowedKeyTypes} AllowedKeyTypes instance
         */
        AllowedKeyTypes.create = function create(properties) {
            return new AllowedKeyTypes(properties);
        };

        /**
         * Encodes the specified AllowedKeyTypes message. Does not implicitly {@link Authentication.AllowedKeyTypes.verify|verify} messages.
         * @function encode
         * @memberof Authentication.AllowedKeyTypes
         * @static
         * @param {Authentication.IAllowedKeyTypes} message AllowedKeyTypes message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AllowedKeyTypes.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.objectType != null && Object.hasOwnProperty.call(message, "objectType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.objectType);
            if (message.allowedKeyTypes != null && message.allowedKeyTypes.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.allowedKeyTypes.length; ++i)
                    writer.int32(message.allowedKeyTypes[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Decodes an AllowedKeyTypes message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.AllowedKeyTypes
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.AllowedKeyTypes} AllowedKeyTypes
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AllowedKeyTypes.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.AllowedKeyTypes();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.objectType = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.allowedKeyTypes && message.allowedKeyTypes.length))
                            message.allowedKeyTypes = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.allowedKeyTypes.push(reader.int32());
                        } else
                            message.allowedKeyTypes.push(reader.int32());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an AllowedKeyTypes message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.AllowedKeyTypes
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.AllowedKeyTypes} AllowedKeyTypes
         */
        AllowedKeyTypes.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.AllowedKeyTypes)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.AllowedKeyTypes: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.AllowedKeyTypes();
            switch (object.objectType) {
            default:
                if (typeof object.objectType === "number") {
                    message.objectType = object.objectType;
                    break;
                }
                break;
            case "EOT_UNSPECIFIED":
            case 0:
                message.objectType = 0;
                break;
            case "EOT_RECORD_KEY":
            case 1:
                message.objectType = 1;
                break;
            case "EOT_SHARED_FOLDER_USER_KEY":
            case 2:
                message.objectType = 2;
                break;
            case "EOT_SHARED_FOLDER_TEAM_KEY":
            case 3:
                message.objectType = 3;
                break;
            case "EOT_TEAM_USER_KEY":
            case 4:
                message.objectType = 4;
                break;
            case "EOT_USER_FOLDER_KEY":
            case 5:
                message.objectType = 5;
                break;
            case "EOT_SECURITY_DATA":
            case 6:
                message.objectType = 6;
                break;
            case "EOT_SECURITY_DATA_MASTER_PASSWORD":
            case 7:
                message.objectType = 7;
                break;
            case "EOT_EMERGENCY_ACCESS_KEY":
            case 8:
                message.objectType = 8;
                break;
            case "EOT_V2_RECORD_KEY":
            case 9:
                message.objectType = 9;
                break;
            }
            if (object.allowedKeyTypes) {
                if (!Array.isArray(object.allowedKeyTypes))
                    throw TypeError(".Authentication.AllowedKeyTypes.allowedKeyTypes: array expected");
                message.allowedKeyTypes = [];
                for (let i = 0; i < object.allowedKeyTypes.length; ++i)
                    switch (object.allowedKeyTypes[i]) {
                    default:
                        if (typeof object.allowedKeyTypes[i] === "number") {
                            message.allowedKeyTypes[i] = object.allowedKeyTypes[i];
                            break;
                        }
                    case "KT_NO_KEY":
                    case 0:
                        message.allowedKeyTypes[i] = 0;
                        break;
                    case "KT_ENCRYPTED_BY_DATA_KEY":
                    case 1:
                        message.allowedKeyTypes[i] = 1;
                        break;
                    case "KT_ENCRYPTED_BY_PUBLIC_KEY":
                    case 2:
                        message.allowedKeyTypes[i] = 2;
                        break;
                    case "KT_ENCRYPTED_BY_DATA_KEY_GCM":
                    case 3:
                        message.allowedKeyTypes[i] = 3;
                        break;
                    case "KT_ENCRYPTED_BY_PUBLIC_KEY_ECC":
                    case 4:
                        message.allowedKeyTypes[i] = 4;
                        break;
                    }
            }
            return message;
        };

        /**
         * Creates a plain object from an AllowedKeyTypes message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.AllowedKeyTypes
         * @static
         * @param {Authentication.AllowedKeyTypes} message AllowedKeyTypes
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AllowedKeyTypes.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.allowedKeyTypes = [];
            if (options.defaults)
                object.objectType = options.enums === String ? "EOT_UNSPECIFIED" : 0;
            if (message.objectType != null && Object.hasOwnProperty.call(message, "objectType"))
                object.objectType = options.enums === String ? $root.Authentication.EncryptedObjectType[message.objectType] === undefined ? message.objectType : $root.Authentication.EncryptedObjectType[message.objectType] : message.objectType;
            if (message.allowedKeyTypes && message.allowedKeyTypes.length) {
                object.allowedKeyTypes = [];
                for (let j = 0; j < message.allowedKeyTypes.length; ++j)
                    object.allowedKeyTypes[j] = options.enums === String ? $root.Enterprise.EncryptedKeyType[message.allowedKeyTypes[j]] === undefined ? message.allowedKeyTypes[j] : $root.Enterprise.EncryptedKeyType[message.allowedKeyTypes[j]] : message.allowedKeyTypes[j];
            }
            return object;
        };

        /**
         * Converts this AllowedKeyTypes to JSON.
         * @function toJSON
         * @memberof Authentication.AllowedKeyTypes
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AllowedKeyTypes.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AllowedKeyTypes
         * @function getTypeUrl
         * @memberof Authentication.AllowedKeyTypes
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AllowedKeyTypes.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.AllowedKeyTypes";
        };

        return AllowedKeyTypes;
    })();

    Authentication.ChangeKeyTypes = (function() {

        /**
         * Properties of a ChangeKeyTypes.
         * @memberof Authentication
         * @interface IChangeKeyTypes
         * @property {Array.<Authentication.IChangeKeyType>|null} [keys] ChangeKeyTypes keys
         */

        /**
         * Constructs a new ChangeKeyTypes.
         * @memberof Authentication
         * @classdesc Represents a ChangeKeyTypes.
         * @implements IChangeKeyTypes
         * @constructor
         * @param {Authentication.IChangeKeyTypes=} [properties] Properties to set
         */
        function ChangeKeyTypes(properties) {
            this.keys = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChangeKeyTypes keys.
         * @member {Array.<Authentication.IChangeKeyType>} keys
         * @memberof Authentication.ChangeKeyTypes
         * @instance
         */
        ChangeKeyTypes.prototype.keys = $util.emptyArray;

        /**
         * Creates a new ChangeKeyTypes instance using the specified properties.
         * @function create
         * @memberof Authentication.ChangeKeyTypes
         * @static
         * @param {Authentication.IChangeKeyTypes=} [properties] Properties to set
         * @returns {Authentication.ChangeKeyTypes} ChangeKeyTypes instance
         */
        ChangeKeyTypes.create = function create(properties) {
            return new ChangeKeyTypes(properties);
        };

        /**
         * Encodes the specified ChangeKeyTypes message. Does not implicitly {@link Authentication.ChangeKeyTypes.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ChangeKeyTypes
         * @static
         * @param {Authentication.IChangeKeyTypes} message ChangeKeyTypes message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangeKeyTypes.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.keys != null && message.keys.length)
                for (let i = 0; i < message.keys.length; ++i)
                    $root.Authentication.ChangeKeyType.encode(message.keys[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes a ChangeKeyTypes message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ChangeKeyTypes
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ChangeKeyTypes} ChangeKeyTypes
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangeKeyTypes.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ChangeKeyTypes();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.keys && message.keys.length))
                            message.keys = [];
                        message.keys.push($root.Authentication.ChangeKeyType.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a ChangeKeyTypes message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ChangeKeyTypes
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ChangeKeyTypes} ChangeKeyTypes
         */
        ChangeKeyTypes.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ChangeKeyTypes)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ChangeKeyTypes: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ChangeKeyTypes();
            if (object.keys) {
                if (!Array.isArray(object.keys))
                    throw TypeError(".Authentication.ChangeKeyTypes.keys: array expected");
                message.keys = [];
                for (let i = 0; i < object.keys.length; ++i) {
                    if (!$util.isObject(object.keys[i]))
                        throw TypeError(".Authentication.ChangeKeyTypes.keys: object expected");
                    message.keys[i] = $root.Authentication.ChangeKeyType.fromObject(object.keys[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ChangeKeyTypes message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ChangeKeyTypes
         * @static
         * @param {Authentication.ChangeKeyTypes} message ChangeKeyTypes
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChangeKeyTypes.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.keys = [];
            if (message.keys && message.keys.length) {
                object.keys = [];
                for (let j = 0; j < message.keys.length; ++j)
                    object.keys[j] = $root.Authentication.ChangeKeyType.toObject(message.keys[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this ChangeKeyTypes to JSON.
         * @function toJSON
         * @memberof Authentication.ChangeKeyTypes
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChangeKeyTypes.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChangeKeyTypes
         * @function getTypeUrl
         * @memberof Authentication.ChangeKeyTypes
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChangeKeyTypes.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ChangeKeyTypes";
        };

        return ChangeKeyTypes;
    })();

    Authentication.ChangeKeyType = (function() {

        /**
         * Properties of a ChangeKeyType.
         * @memberof Authentication
         * @interface IChangeKeyType
         * @property {Authentication.EncryptedObjectType|null} [objectType] ChangeKeyType objectType
         * @property {Uint8Array|null} [uid] ChangeKeyType uid
         * @property {Uint8Array|null} [secondaryUid] ChangeKeyType secondaryUid
         * @property {Uint8Array|null} [key] ChangeKeyType key
         * @property {Enterprise.EncryptedKeyType|null} [keyType] ChangeKeyType keyType
         * @property {Authentication.GenericStatus|null} [status] ChangeKeyType status
         */

        /**
         * Constructs a new ChangeKeyType.
         * @memberof Authentication
         * @classdesc Represents a ChangeKeyType.
         * @implements IChangeKeyType
         * @constructor
         * @param {Authentication.IChangeKeyType=} [properties] Properties to set
         */
        function ChangeKeyType(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChangeKeyType objectType.
         * @member {Authentication.EncryptedObjectType} objectType
         * @memberof Authentication.ChangeKeyType
         * @instance
         */
        ChangeKeyType.prototype.objectType = 0;

        /**
         * ChangeKeyType uid.
         * @member {Uint8Array} uid
         * @memberof Authentication.ChangeKeyType
         * @instance
         */
        ChangeKeyType.prototype.uid = $util.newBuffer([]);

        /**
         * ChangeKeyType secondaryUid.
         * @member {Uint8Array} secondaryUid
         * @memberof Authentication.ChangeKeyType
         * @instance
         */
        ChangeKeyType.prototype.secondaryUid = $util.newBuffer([]);

        /**
         * ChangeKeyType key.
         * @member {Uint8Array} key
         * @memberof Authentication.ChangeKeyType
         * @instance
         */
        ChangeKeyType.prototype.key = $util.newBuffer([]);

        /**
         * ChangeKeyType keyType.
         * @member {Enterprise.EncryptedKeyType} keyType
         * @memberof Authentication.ChangeKeyType
         * @instance
         */
        ChangeKeyType.prototype.keyType = 0;

        /**
         * ChangeKeyType status.
         * @member {Authentication.GenericStatus} status
         * @memberof Authentication.ChangeKeyType
         * @instance
         */
        ChangeKeyType.prototype.status = 0;

        /**
         * Creates a new ChangeKeyType instance using the specified properties.
         * @function create
         * @memberof Authentication.ChangeKeyType
         * @static
         * @param {Authentication.IChangeKeyType=} [properties] Properties to set
         * @returns {Authentication.ChangeKeyType} ChangeKeyType instance
         */
        ChangeKeyType.create = function create(properties) {
            return new ChangeKeyType(properties);
        };

        /**
         * Encodes the specified ChangeKeyType message. Does not implicitly {@link Authentication.ChangeKeyType.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ChangeKeyType
         * @static
         * @param {Authentication.IChangeKeyType} message ChangeKeyType message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangeKeyType.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.objectType != null && Object.hasOwnProperty.call(message, "objectType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.objectType);
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.uid);
            if (message.secondaryUid != null && Object.hasOwnProperty.call(message, "secondaryUid"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.secondaryUid);
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.key);
            if (message.keyType != null && Object.hasOwnProperty.call(message, "keyType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.keyType);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.status);
            return writer;
        };

        /**
         * Decodes a ChangeKeyType message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ChangeKeyType
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ChangeKeyType} ChangeKeyType
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangeKeyType.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ChangeKeyType();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.objectType = reader.int32();
                        break;
                    }
                case 2: {
                        message.uid = reader.bytes();
                        break;
                    }
                case 3: {
                        message.secondaryUid = reader.bytes();
                        break;
                    }
                case 4: {
                        message.key = reader.bytes();
                        break;
                    }
                case 5: {
                        message.keyType = reader.int32();
                        break;
                    }
                case 6: {
                        message.status = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a ChangeKeyType message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ChangeKeyType
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ChangeKeyType} ChangeKeyType
         */
        ChangeKeyType.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ChangeKeyType)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ChangeKeyType: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ChangeKeyType();
            switch (object.objectType) {
            default:
                if (typeof object.objectType === "number") {
                    message.objectType = object.objectType;
                    break;
                }
                break;
            case "EOT_UNSPECIFIED":
            case 0:
                message.objectType = 0;
                break;
            case "EOT_RECORD_KEY":
            case 1:
                message.objectType = 1;
                break;
            case "EOT_SHARED_FOLDER_USER_KEY":
            case 2:
                message.objectType = 2;
                break;
            case "EOT_SHARED_FOLDER_TEAM_KEY":
            case 3:
                message.objectType = 3;
                break;
            case "EOT_TEAM_USER_KEY":
            case 4:
                message.objectType = 4;
                break;
            case "EOT_USER_FOLDER_KEY":
            case 5:
                message.objectType = 5;
                break;
            case "EOT_SECURITY_DATA":
            case 6:
                message.objectType = 6;
                break;
            case "EOT_SECURITY_DATA_MASTER_PASSWORD":
            case 7:
                message.objectType = 7;
                break;
            case "EOT_EMERGENCY_ACCESS_KEY":
            case 8:
                message.objectType = 8;
                break;
            case "EOT_V2_RECORD_KEY":
            case 9:
                message.objectType = 9;
                break;
            }
            if (object.uid != null)
                if (typeof object.uid === "string")
                    $util.base64.decode(object.uid, message.uid = $util.newBuffer($util.base64.length(object.uid)), 0);
                else if (object.uid.length >= 0)
                    message.uid = object.uid;
            if (object.secondaryUid != null)
                if (typeof object.secondaryUid === "string")
                    $util.base64.decode(object.secondaryUid, message.secondaryUid = $util.newBuffer($util.base64.length(object.secondaryUid)), 0);
                else if (object.secondaryUid.length >= 0)
                    message.secondaryUid = object.secondaryUid;
            if (object.key != null)
                if (typeof object.key === "string")
                    $util.base64.decode(object.key, message.key = $util.newBuffer($util.base64.length(object.key)), 0);
                else if (object.key.length >= 0)
                    message.key = object.key;
            switch (object.keyType) {
            default:
                if (typeof object.keyType === "number") {
                    message.keyType = object.keyType;
                    break;
                }
                break;
            case "KT_NO_KEY":
            case 0:
                message.keyType = 0;
                break;
            case "KT_ENCRYPTED_BY_DATA_KEY":
            case 1:
                message.keyType = 1;
                break;
            case "KT_ENCRYPTED_BY_PUBLIC_KEY":
            case 2:
                message.keyType = 2;
                break;
            case "KT_ENCRYPTED_BY_DATA_KEY_GCM":
            case 3:
                message.keyType = 3;
                break;
            case "KT_ENCRYPTED_BY_PUBLIC_KEY_ECC":
            case 4:
                message.keyType = 4;
                break;
            }
            switch (object.status) {
            default:
                if (typeof object.status === "number") {
                    message.status = object.status;
                    break;
                }
                break;
            case "SUCCESS":
            case 0:
                message.status = 0;
                break;
            case "INVALID_OBJECT":
            case 1:
                message.status = 1;
                break;
            case "ALREADY_EXISTS":
            case 2:
                message.status = 2;
                break;
            case "ACCESS_DENIED":
            case 3:
                message.status = 3;
                break;
            case "LICENSE_SEAT_EXCEEDED":
            case 4:
                message.status = 4;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a ChangeKeyType message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ChangeKeyType
         * @static
         * @param {Authentication.ChangeKeyType} message ChangeKeyType
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChangeKeyType.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.objectType = options.enums === String ? "EOT_UNSPECIFIED" : 0;
                if (options.bytes === String)
                    object.uid = "";
                else {
                    object.uid = [];
                    if (options.bytes !== Array)
                        object.uid = $util.newBuffer(object.uid);
                }
                if (options.bytes === String)
                    object.secondaryUid = "";
                else {
                    object.secondaryUid = [];
                    if (options.bytes !== Array)
                        object.secondaryUid = $util.newBuffer(object.secondaryUid);
                }
                if (options.bytes === String)
                    object.key = "";
                else {
                    object.key = [];
                    if (options.bytes !== Array)
                        object.key = $util.newBuffer(object.key);
                }
                object.keyType = options.enums === String ? "KT_NO_KEY" : 0;
                object.status = options.enums === String ? "SUCCESS" : 0;
            }
            if (message.objectType != null && Object.hasOwnProperty.call(message, "objectType"))
                object.objectType = options.enums === String ? $root.Authentication.EncryptedObjectType[message.objectType] === undefined ? message.objectType : $root.Authentication.EncryptedObjectType[message.objectType] : message.objectType;
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                object.uid = options.bytes === String ? $util.base64.encode(message.uid, 0, message.uid.length) : options.bytes === Array ? Array.prototype.slice.call(message.uid) : message.uid;
            if (message.secondaryUid != null && Object.hasOwnProperty.call(message, "secondaryUid"))
                object.secondaryUid = options.bytes === String ? $util.base64.encode(message.secondaryUid, 0, message.secondaryUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.secondaryUid) : message.secondaryUid;
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                object.key = options.bytes === String ? $util.base64.encode(message.key, 0, message.key.length) : options.bytes === Array ? Array.prototype.slice.call(message.key) : message.key;
            if (message.keyType != null && Object.hasOwnProperty.call(message, "keyType"))
                object.keyType = options.enums === String ? $root.Enterprise.EncryptedKeyType[message.keyType] === undefined ? message.keyType : $root.Enterprise.EncryptedKeyType[message.keyType] : message.keyType;
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                object.status = options.enums === String ? $root.Authentication.GenericStatus[message.status] === undefined ? message.status : $root.Authentication.GenericStatus[message.status] : message.status;
            return object;
        };

        /**
         * Converts this ChangeKeyType to JSON.
         * @function toJSON
         * @memberof Authentication.ChangeKeyType
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChangeKeyType.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChangeKeyType
         * @function getTypeUrl
         * @memberof Authentication.ChangeKeyType
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChangeKeyType.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ChangeKeyType";
        };

        return ChangeKeyType;
    })();

    Authentication.SetKey = (function() {

        /**
         * Properties of a SetKey.
         * @memberof Authentication
         * @interface ISetKey
         * @property {number|null} [id] SetKey id
         * @property {Uint8Array|null} [key] SetKey key
         */

        /**
         * Constructs a new SetKey.
         * @memberof Authentication
         * @classdesc Represents a SetKey.
         * @implements ISetKey
         * @constructor
         * @param {Authentication.ISetKey=} [properties] Properties to set
         */
        function SetKey(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SetKey id.
         * @member {number} id
         * @memberof Authentication.SetKey
         * @instance
         */
        SetKey.prototype.id = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SetKey key.
         * @member {Uint8Array} key
         * @memberof Authentication.SetKey
         * @instance
         */
        SetKey.prototype.key = $util.newBuffer([]);

        /**
         * Creates a new SetKey instance using the specified properties.
         * @function create
         * @memberof Authentication.SetKey
         * @static
         * @param {Authentication.ISetKey=} [properties] Properties to set
         * @returns {Authentication.SetKey} SetKey instance
         */
        SetKey.create = function create(properties) {
            return new SetKey(properties);
        };

        /**
         * Encodes the specified SetKey message. Does not implicitly {@link Authentication.SetKey.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SetKey
         * @static
         * @param {Authentication.ISetKey} message SetKey message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetKey.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.id);
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.key);
            return writer;
        };

        /**
         * Decodes a SetKey message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SetKey
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SetKey} SetKey
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetKey.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SetKey();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int64();
                        break;
                    }
                case 2: {
                        message.key = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SetKey message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SetKey
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SetKey} SetKey
         */
        SetKey.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SetKey)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SetKey: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SetKey();
            if (object.id != null)
                if ($util.Long)
                    message.id = $util.Long.fromValue(object.id, false);
                else if (typeof object.id === "string")
                    message.id = parseInt(object.id, 10);
                else if (typeof object.id === "number")
                    message.id = object.id;
                else if (typeof object.id === "object")
                    message.id = new $util.LongBits(object.id.low >>> 0, object.id.high >>> 0).toNumber();
            if (object.key != null)
                if (typeof object.key === "string")
                    $util.base64.decode(object.key, message.key = $util.newBuffer($util.base64.length(object.key)), 0);
                else if (object.key.length >= 0)
                    message.key = object.key;
            return message;
        };

        /**
         * Creates a plain object from a SetKey message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SetKey
         * @static
         * @param {Authentication.SetKey} message SetKey
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SetKey.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.id = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.id = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.key = "";
                else {
                    object.key = [];
                    if (options.bytes !== Array)
                        object.key = $util.newBuffer(object.key);
                }
            }
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.id = typeof message.id === "number" ? BigInt(message.id) : $util.Long.fromBits(message.id.low >>> 0, message.id.high >>> 0, false).toBigInt();
                else if (typeof message.id === "number")
                    object.id = options.longs === String ? String(message.id) : message.id;
                else
                    object.id = options.longs === String ? $util.Long.prototype.toString.call(message.id) : options.longs === Number ? new $util.LongBits(message.id.low >>> 0, message.id.high >>> 0).toNumber() : message.id;
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                object.key = options.bytes === String ? $util.base64.encode(message.key, 0, message.key.length) : options.bytes === Array ? Array.prototype.slice.call(message.key) : message.key;
            return object;
        };

        /**
         * Converts this SetKey to JSON.
         * @function toJSON
         * @memberof Authentication.SetKey
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SetKey.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SetKey
         * @function getTypeUrl
         * @memberof Authentication.SetKey
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SetKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SetKey";
        };

        return SetKey;
    })();

    Authentication.SetKeyRequest = (function() {

        /**
         * Properties of a SetKeyRequest.
         * @memberof Authentication
         * @interface ISetKeyRequest
         * @property {Array.<Authentication.ISetKey>|null} [keys] SetKeyRequest keys
         */

        /**
         * Constructs a new SetKeyRequest.
         * @memberof Authentication
         * @classdesc Represents a SetKeyRequest.
         * @implements ISetKeyRequest
         * @constructor
         * @param {Authentication.ISetKeyRequest=} [properties] Properties to set
         */
        function SetKeyRequest(properties) {
            this.keys = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SetKeyRequest keys.
         * @member {Array.<Authentication.ISetKey>} keys
         * @memberof Authentication.SetKeyRequest
         * @instance
         */
        SetKeyRequest.prototype.keys = $util.emptyArray;

        /**
         * Creates a new SetKeyRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.SetKeyRequest
         * @static
         * @param {Authentication.ISetKeyRequest=} [properties] Properties to set
         * @returns {Authentication.SetKeyRequest} SetKeyRequest instance
         */
        SetKeyRequest.create = function create(properties) {
            return new SetKeyRequest(properties);
        };

        /**
         * Encodes the specified SetKeyRequest message. Does not implicitly {@link Authentication.SetKeyRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SetKeyRequest
         * @static
         * @param {Authentication.ISetKeyRequest} message SetKeyRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetKeyRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.keys != null && message.keys.length)
                for (let i = 0; i < message.keys.length; ++i)
                    $root.Authentication.SetKey.encode(message.keys[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes a SetKeyRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SetKeyRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SetKeyRequest} SetKeyRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetKeyRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SetKeyRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.keys && message.keys.length))
                            message.keys = [];
                        message.keys.push($root.Authentication.SetKey.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SetKeyRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SetKeyRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SetKeyRequest} SetKeyRequest
         */
        SetKeyRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SetKeyRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SetKeyRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SetKeyRequest();
            if (object.keys) {
                if (!Array.isArray(object.keys))
                    throw TypeError(".Authentication.SetKeyRequest.keys: array expected");
                message.keys = [];
                for (let i = 0; i < object.keys.length; ++i) {
                    if (!$util.isObject(object.keys[i]))
                        throw TypeError(".Authentication.SetKeyRequest.keys: object expected");
                    message.keys[i] = $root.Authentication.SetKey.fromObject(object.keys[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SetKeyRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SetKeyRequest
         * @static
         * @param {Authentication.SetKeyRequest} message SetKeyRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SetKeyRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.keys = [];
            if (message.keys && message.keys.length) {
                object.keys = [];
                for (let j = 0; j < message.keys.length; ++j)
                    object.keys[j] = $root.Authentication.SetKey.toObject(message.keys[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this SetKeyRequest to JSON.
         * @function toJSON
         * @memberof Authentication.SetKeyRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SetKeyRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SetKeyRequest
         * @function getTypeUrl
         * @memberof Authentication.SetKeyRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SetKeyRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SetKeyRequest";
        };

        return SetKeyRequest;
    })();

    Authentication.CreateUserRequest = (function() {

        /**
         * Properties of a CreateUserRequest.
         * @memberof Authentication
         * @interface ICreateUserRequest
         * @property {string|null} [username] CreateUserRequest username
         * @property {Uint8Array|null} [authVerifier] CreateUserRequest authVerifier
         * @property {Uint8Array|null} [encryptionParams] CreateUserRequest encryptionParams
         * @property {Uint8Array|null} [rsaPublicKey] CreateUserRequest rsaPublicKey
         * @property {Uint8Array|null} [rsaEncryptedPrivateKey] CreateUserRequest rsaEncryptedPrivateKey
         * @property {Uint8Array|null} [eccPublicKey] CreateUserRequest eccPublicKey
         * @property {Uint8Array|null} [eccEncryptedPrivateKey] CreateUserRequest eccEncryptedPrivateKey
         * @property {Uint8Array|null} [encryptedDeviceToken] CreateUserRequest encryptedDeviceToken
         * @property {Uint8Array|null} [encryptedClientKey] CreateUserRequest encryptedClientKey
         * @property {string|null} [clientVersion] CreateUserRequest clientVersion
         * @property {Uint8Array|null} [encryptedDeviceDataKey] CreateUserRequest encryptedDeviceDataKey
         * @property {Uint8Array|null} [encryptedLoginToken] CreateUserRequest encryptedLoginToken
         * @property {Uint8Array|null} [messageSessionUid] CreateUserRequest messageSessionUid
         * @property {string|null} [installReferrer] CreateUserRequest installReferrer
         * @property {number|null} [mccMNC] CreateUserRequest mccMNC
         * @property {string|null} [mfg] CreateUserRequest mfg
         * @property {string|null} [model] CreateUserRequest model
         * @property {string|null} [brand] CreateUserRequest brand
         * @property {string|null} [product] CreateUserRequest product
         * @property {string|null} [device] CreateUserRequest device
         * @property {string|null} [carrier] CreateUserRequest carrier
         * @property {string|null} [verificationCode] CreateUserRequest verificationCode
         * @property {Enterprise.IEnterpriseRegistration|null} [enterpriseRegistration] CreateUserRequest enterpriseRegistration
         * @property {Uint8Array|null} [encryptedVerificationToken] CreateUserRequest encryptedVerificationToken
         * @property {Uint8Array|null} [enterpriseUsersDataKey] CreateUserRequest enterpriseUsersDataKey
         */

        /**
         * Constructs a new CreateUserRequest.
         * @memberof Authentication
         * @classdesc Represents a CreateUserRequest.
         * @implements ICreateUserRequest
         * @constructor
         * @param {Authentication.ICreateUserRequest=} [properties] Properties to set
         */
        function CreateUserRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CreateUserRequest username.
         * @member {string} username
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.username = "";

        /**
         * CreateUserRequest authVerifier.
         * @member {Uint8Array} authVerifier
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.authVerifier = $util.newBuffer([]);

        /**
         * CreateUserRequest encryptionParams.
         * @member {Uint8Array} encryptionParams
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.encryptionParams = $util.newBuffer([]);

        /**
         * CreateUserRequest rsaPublicKey.
         * @member {Uint8Array} rsaPublicKey
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.rsaPublicKey = $util.newBuffer([]);

        /**
         * CreateUserRequest rsaEncryptedPrivateKey.
         * @member {Uint8Array} rsaEncryptedPrivateKey
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.rsaEncryptedPrivateKey = $util.newBuffer([]);

        /**
         * CreateUserRequest eccPublicKey.
         * @member {Uint8Array} eccPublicKey
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.eccPublicKey = $util.newBuffer([]);

        /**
         * CreateUserRequest eccEncryptedPrivateKey.
         * @member {Uint8Array} eccEncryptedPrivateKey
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.eccEncryptedPrivateKey = $util.newBuffer([]);

        /**
         * CreateUserRequest encryptedDeviceToken.
         * @member {Uint8Array} encryptedDeviceToken
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.encryptedDeviceToken = $util.newBuffer([]);

        /**
         * CreateUserRequest encryptedClientKey.
         * @member {Uint8Array} encryptedClientKey
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.encryptedClientKey = $util.newBuffer([]);

        /**
         * CreateUserRequest clientVersion.
         * @member {string} clientVersion
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.clientVersion = "";

        /**
         * CreateUserRequest encryptedDeviceDataKey.
         * @member {Uint8Array} encryptedDeviceDataKey
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.encryptedDeviceDataKey = $util.newBuffer([]);

        /**
         * CreateUserRequest encryptedLoginToken.
         * @member {Uint8Array} encryptedLoginToken
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.encryptedLoginToken = $util.newBuffer([]);

        /**
         * CreateUserRequest messageSessionUid.
         * @member {Uint8Array} messageSessionUid
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.messageSessionUid = $util.newBuffer([]);

        /**
         * CreateUserRequest installReferrer.
         * @member {string} installReferrer
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.installReferrer = "";

        /**
         * CreateUserRequest mccMNC.
         * @member {number} mccMNC
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.mccMNC = 0;

        /**
         * CreateUserRequest mfg.
         * @member {string} mfg
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.mfg = "";

        /**
         * CreateUserRequest model.
         * @member {string} model
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.model = "";

        /**
         * CreateUserRequest brand.
         * @member {string} brand
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.brand = "";

        /**
         * CreateUserRequest product.
         * @member {string} product
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.product = "";

        /**
         * CreateUserRequest device.
         * @member {string} device
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.device = "";

        /**
         * CreateUserRequest carrier.
         * @member {string} carrier
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.carrier = "";

        /**
         * CreateUserRequest verificationCode.
         * @member {string} verificationCode
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.verificationCode = "";

        /**
         * CreateUserRequest enterpriseRegistration.
         * @member {Enterprise.IEnterpriseRegistration|null|undefined} enterpriseRegistration
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.enterpriseRegistration = null;

        /**
         * CreateUserRequest encryptedVerificationToken.
         * @member {Uint8Array} encryptedVerificationToken
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.encryptedVerificationToken = $util.newBuffer([]);

        /**
         * CreateUserRequest enterpriseUsersDataKey.
         * @member {Uint8Array} enterpriseUsersDataKey
         * @memberof Authentication.CreateUserRequest
         * @instance
         */
        CreateUserRequest.prototype.enterpriseUsersDataKey = $util.newBuffer([]);

        /**
         * Creates a new CreateUserRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.CreateUserRequest
         * @static
         * @param {Authentication.ICreateUserRequest=} [properties] Properties to set
         * @returns {Authentication.CreateUserRequest} CreateUserRequest instance
         */
        CreateUserRequest.create = function create(properties) {
            return new CreateUserRequest(properties);
        };

        /**
         * Encodes the specified CreateUserRequest message. Does not implicitly {@link Authentication.CreateUserRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.CreateUserRequest
         * @static
         * @param {Authentication.ICreateUserRequest} message CreateUserRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CreateUserRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.username);
            if (message.authVerifier != null && Object.hasOwnProperty.call(message, "authVerifier"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.authVerifier);
            if (message.encryptionParams != null && Object.hasOwnProperty.call(message, "encryptionParams"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.encryptionParams);
            if (message.rsaPublicKey != null && Object.hasOwnProperty.call(message, "rsaPublicKey"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.rsaPublicKey);
            if (message.rsaEncryptedPrivateKey != null && Object.hasOwnProperty.call(message, "rsaEncryptedPrivateKey"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.rsaEncryptedPrivateKey);
            if (message.eccPublicKey != null && Object.hasOwnProperty.call(message, "eccPublicKey"))
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.eccPublicKey);
            if (message.eccEncryptedPrivateKey != null && Object.hasOwnProperty.call(message, "eccEncryptedPrivateKey"))
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.eccEncryptedPrivateKey);
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                writer.uint32(/* id 8, wireType 2 =*/66).bytes(message.encryptedDeviceToken);
            if (message.encryptedClientKey != null && Object.hasOwnProperty.call(message, "encryptedClientKey"))
                writer.uint32(/* id 9, wireType 2 =*/74).bytes(message.encryptedClientKey);
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.clientVersion);
            if (message.encryptedDeviceDataKey != null && Object.hasOwnProperty.call(message, "encryptedDeviceDataKey"))
                writer.uint32(/* id 11, wireType 2 =*/90).bytes(message.encryptedDeviceDataKey);
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                writer.uint32(/* id 12, wireType 2 =*/98).bytes(message.encryptedLoginToken);
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                writer.uint32(/* id 13, wireType 2 =*/106).bytes(message.messageSessionUid);
            if (message.installReferrer != null && Object.hasOwnProperty.call(message, "installReferrer"))
                writer.uint32(/* id 14, wireType 2 =*/114).string(message.installReferrer);
            if (message.mccMNC != null && Object.hasOwnProperty.call(message, "mccMNC"))
                writer.uint32(/* id 15, wireType 0 =*/120).int32(message.mccMNC);
            if (message.mfg != null && Object.hasOwnProperty.call(message, "mfg"))
                writer.uint32(/* id 16, wireType 2 =*/130).string(message.mfg);
            if (message.model != null && Object.hasOwnProperty.call(message, "model"))
                writer.uint32(/* id 17, wireType 2 =*/138).string(message.model);
            if (message.brand != null && Object.hasOwnProperty.call(message, "brand"))
                writer.uint32(/* id 18, wireType 2 =*/146).string(message.brand);
            if (message.product != null && Object.hasOwnProperty.call(message, "product"))
                writer.uint32(/* id 19, wireType 2 =*/154).string(message.product);
            if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                writer.uint32(/* id 20, wireType 2 =*/162).string(message.device);
            if (message.carrier != null && Object.hasOwnProperty.call(message, "carrier"))
                writer.uint32(/* id 21, wireType 2 =*/170).string(message.carrier);
            if (message.verificationCode != null && Object.hasOwnProperty.call(message, "verificationCode"))
                writer.uint32(/* id 22, wireType 2 =*/178).string(message.verificationCode);
            if (message.enterpriseRegistration != null && Object.hasOwnProperty.call(message, "enterpriseRegistration"))
                $root.Enterprise.EnterpriseRegistration.encode(message.enterpriseRegistration, writer.uint32(/* id 23, wireType 2 =*/186).fork(), q + 1).ldelim();
            if (message.encryptedVerificationToken != null && Object.hasOwnProperty.call(message, "encryptedVerificationToken"))
                writer.uint32(/* id 24, wireType 2 =*/194).bytes(message.encryptedVerificationToken);
            if (message.enterpriseUsersDataKey != null && Object.hasOwnProperty.call(message, "enterpriseUsersDataKey"))
                writer.uint32(/* id 25, wireType 2 =*/202).bytes(message.enterpriseUsersDataKey);
            return writer;
        };

        /**
         * Decodes a CreateUserRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.CreateUserRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.CreateUserRequest} CreateUserRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CreateUserRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.CreateUserRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.username = reader.string();
                        break;
                    }
                case 2: {
                        message.authVerifier = reader.bytes();
                        break;
                    }
                case 3: {
                        message.encryptionParams = reader.bytes();
                        break;
                    }
                case 4: {
                        message.rsaPublicKey = reader.bytes();
                        break;
                    }
                case 5: {
                        message.rsaEncryptedPrivateKey = reader.bytes();
                        break;
                    }
                case 6: {
                        message.eccPublicKey = reader.bytes();
                        break;
                    }
                case 7: {
                        message.eccEncryptedPrivateKey = reader.bytes();
                        break;
                    }
                case 8: {
                        message.encryptedDeviceToken = reader.bytes();
                        break;
                    }
                case 9: {
                        message.encryptedClientKey = reader.bytes();
                        break;
                    }
                case 10: {
                        message.clientVersion = reader.string();
                        break;
                    }
                case 11: {
                        message.encryptedDeviceDataKey = reader.bytes();
                        break;
                    }
                case 12: {
                        message.encryptedLoginToken = reader.bytes();
                        break;
                    }
                case 13: {
                        message.messageSessionUid = reader.bytes();
                        break;
                    }
                case 14: {
                        message.installReferrer = reader.string();
                        break;
                    }
                case 15: {
                        message.mccMNC = reader.int32();
                        break;
                    }
                case 16: {
                        message.mfg = reader.string();
                        break;
                    }
                case 17: {
                        message.model = reader.string();
                        break;
                    }
                case 18: {
                        message.brand = reader.string();
                        break;
                    }
                case 19: {
                        message.product = reader.string();
                        break;
                    }
                case 20: {
                        message.device = reader.string();
                        break;
                    }
                case 21: {
                        message.carrier = reader.string();
                        break;
                    }
                case 22: {
                        message.verificationCode = reader.string();
                        break;
                    }
                case 23: {
                        message.enterpriseRegistration = $root.Enterprise.EnterpriseRegistration.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 24: {
                        message.encryptedVerificationToken = reader.bytes();
                        break;
                    }
                case 25: {
                        message.enterpriseUsersDataKey = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a CreateUserRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.CreateUserRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.CreateUserRequest} CreateUserRequest
         */
        CreateUserRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.CreateUserRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.CreateUserRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.CreateUserRequest();
            if (object.username != null)
                message.username = String(object.username);
            if (object.authVerifier != null)
                if (typeof object.authVerifier === "string")
                    $util.base64.decode(object.authVerifier, message.authVerifier = $util.newBuffer($util.base64.length(object.authVerifier)), 0);
                else if (object.authVerifier.length >= 0)
                    message.authVerifier = object.authVerifier;
            if (object.encryptionParams != null)
                if (typeof object.encryptionParams === "string")
                    $util.base64.decode(object.encryptionParams, message.encryptionParams = $util.newBuffer($util.base64.length(object.encryptionParams)), 0);
                else if (object.encryptionParams.length >= 0)
                    message.encryptionParams = object.encryptionParams;
            if (object.rsaPublicKey != null)
                if (typeof object.rsaPublicKey === "string")
                    $util.base64.decode(object.rsaPublicKey, message.rsaPublicKey = $util.newBuffer($util.base64.length(object.rsaPublicKey)), 0);
                else if (object.rsaPublicKey.length >= 0)
                    message.rsaPublicKey = object.rsaPublicKey;
            if (object.rsaEncryptedPrivateKey != null)
                if (typeof object.rsaEncryptedPrivateKey === "string")
                    $util.base64.decode(object.rsaEncryptedPrivateKey, message.rsaEncryptedPrivateKey = $util.newBuffer($util.base64.length(object.rsaEncryptedPrivateKey)), 0);
                else if (object.rsaEncryptedPrivateKey.length >= 0)
                    message.rsaEncryptedPrivateKey = object.rsaEncryptedPrivateKey;
            if (object.eccPublicKey != null)
                if (typeof object.eccPublicKey === "string")
                    $util.base64.decode(object.eccPublicKey, message.eccPublicKey = $util.newBuffer($util.base64.length(object.eccPublicKey)), 0);
                else if (object.eccPublicKey.length >= 0)
                    message.eccPublicKey = object.eccPublicKey;
            if (object.eccEncryptedPrivateKey != null)
                if (typeof object.eccEncryptedPrivateKey === "string")
                    $util.base64.decode(object.eccEncryptedPrivateKey, message.eccEncryptedPrivateKey = $util.newBuffer($util.base64.length(object.eccEncryptedPrivateKey)), 0);
                else if (object.eccEncryptedPrivateKey.length >= 0)
                    message.eccEncryptedPrivateKey = object.eccEncryptedPrivateKey;
            if (object.encryptedDeviceToken != null)
                if (typeof object.encryptedDeviceToken === "string")
                    $util.base64.decode(object.encryptedDeviceToken, message.encryptedDeviceToken = $util.newBuffer($util.base64.length(object.encryptedDeviceToken)), 0);
                else if (object.encryptedDeviceToken.length >= 0)
                    message.encryptedDeviceToken = object.encryptedDeviceToken;
            if (object.encryptedClientKey != null)
                if (typeof object.encryptedClientKey === "string")
                    $util.base64.decode(object.encryptedClientKey, message.encryptedClientKey = $util.newBuffer($util.base64.length(object.encryptedClientKey)), 0);
                else if (object.encryptedClientKey.length >= 0)
                    message.encryptedClientKey = object.encryptedClientKey;
            if (object.clientVersion != null)
                message.clientVersion = String(object.clientVersion);
            if (object.encryptedDeviceDataKey != null)
                if (typeof object.encryptedDeviceDataKey === "string")
                    $util.base64.decode(object.encryptedDeviceDataKey, message.encryptedDeviceDataKey = $util.newBuffer($util.base64.length(object.encryptedDeviceDataKey)), 0);
                else if (object.encryptedDeviceDataKey.length >= 0)
                    message.encryptedDeviceDataKey = object.encryptedDeviceDataKey;
            if (object.encryptedLoginToken != null)
                if (typeof object.encryptedLoginToken === "string")
                    $util.base64.decode(object.encryptedLoginToken, message.encryptedLoginToken = $util.newBuffer($util.base64.length(object.encryptedLoginToken)), 0);
                else if (object.encryptedLoginToken.length >= 0)
                    message.encryptedLoginToken = object.encryptedLoginToken;
            if (object.messageSessionUid != null)
                if (typeof object.messageSessionUid === "string")
                    $util.base64.decode(object.messageSessionUid, message.messageSessionUid = $util.newBuffer($util.base64.length(object.messageSessionUid)), 0);
                else if (object.messageSessionUid.length >= 0)
                    message.messageSessionUid = object.messageSessionUid;
            if (object.installReferrer != null)
                message.installReferrer = String(object.installReferrer);
            if (object.mccMNC != null)
                message.mccMNC = object.mccMNC | 0;
            if (object.mfg != null)
                message.mfg = String(object.mfg);
            if (object.model != null)
                message.model = String(object.model);
            if (object.brand != null)
                message.brand = String(object.brand);
            if (object.product != null)
                message.product = String(object.product);
            if (object.device != null)
                message.device = String(object.device);
            if (object.carrier != null)
                message.carrier = String(object.carrier);
            if (object.verificationCode != null)
                message.verificationCode = String(object.verificationCode);
            if (object.enterpriseRegistration != null) {
                if (!$util.isObject(object.enterpriseRegistration))
                    throw TypeError(".Authentication.CreateUserRequest.enterpriseRegistration: object expected");
                message.enterpriseRegistration = $root.Enterprise.EnterpriseRegistration.fromObject(object.enterpriseRegistration, long + 1);
            }
            if (object.encryptedVerificationToken != null)
                if (typeof object.encryptedVerificationToken === "string")
                    $util.base64.decode(object.encryptedVerificationToken, message.encryptedVerificationToken = $util.newBuffer($util.base64.length(object.encryptedVerificationToken)), 0);
                else if (object.encryptedVerificationToken.length >= 0)
                    message.encryptedVerificationToken = object.encryptedVerificationToken;
            if (object.enterpriseUsersDataKey != null)
                if (typeof object.enterpriseUsersDataKey === "string")
                    $util.base64.decode(object.enterpriseUsersDataKey, message.enterpriseUsersDataKey = $util.newBuffer($util.base64.length(object.enterpriseUsersDataKey)), 0);
                else if (object.enterpriseUsersDataKey.length >= 0)
                    message.enterpriseUsersDataKey = object.enterpriseUsersDataKey;
            return message;
        };

        /**
         * Creates a plain object from a CreateUserRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.CreateUserRequest
         * @static
         * @param {Authentication.CreateUserRequest} message CreateUserRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CreateUserRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.username = "";
                if (options.bytes === String)
                    object.authVerifier = "";
                else {
                    object.authVerifier = [];
                    if (options.bytes !== Array)
                        object.authVerifier = $util.newBuffer(object.authVerifier);
                }
                if (options.bytes === String)
                    object.encryptionParams = "";
                else {
                    object.encryptionParams = [];
                    if (options.bytes !== Array)
                        object.encryptionParams = $util.newBuffer(object.encryptionParams);
                }
                if (options.bytes === String)
                    object.rsaPublicKey = "";
                else {
                    object.rsaPublicKey = [];
                    if (options.bytes !== Array)
                        object.rsaPublicKey = $util.newBuffer(object.rsaPublicKey);
                }
                if (options.bytes === String)
                    object.rsaEncryptedPrivateKey = "";
                else {
                    object.rsaEncryptedPrivateKey = [];
                    if (options.bytes !== Array)
                        object.rsaEncryptedPrivateKey = $util.newBuffer(object.rsaEncryptedPrivateKey);
                }
                if (options.bytes === String)
                    object.eccPublicKey = "";
                else {
                    object.eccPublicKey = [];
                    if (options.bytes !== Array)
                        object.eccPublicKey = $util.newBuffer(object.eccPublicKey);
                }
                if (options.bytes === String)
                    object.eccEncryptedPrivateKey = "";
                else {
                    object.eccEncryptedPrivateKey = [];
                    if (options.bytes !== Array)
                        object.eccEncryptedPrivateKey = $util.newBuffer(object.eccEncryptedPrivateKey);
                }
                if (options.bytes === String)
                    object.encryptedDeviceToken = "";
                else {
                    object.encryptedDeviceToken = [];
                    if (options.bytes !== Array)
                        object.encryptedDeviceToken = $util.newBuffer(object.encryptedDeviceToken);
                }
                if (options.bytes === String)
                    object.encryptedClientKey = "";
                else {
                    object.encryptedClientKey = [];
                    if (options.bytes !== Array)
                        object.encryptedClientKey = $util.newBuffer(object.encryptedClientKey);
                }
                object.clientVersion = "";
                if (options.bytes === String)
                    object.encryptedDeviceDataKey = "";
                else {
                    object.encryptedDeviceDataKey = [];
                    if (options.bytes !== Array)
                        object.encryptedDeviceDataKey = $util.newBuffer(object.encryptedDeviceDataKey);
                }
                if (options.bytes === String)
                    object.encryptedLoginToken = "";
                else {
                    object.encryptedLoginToken = [];
                    if (options.bytes !== Array)
                        object.encryptedLoginToken = $util.newBuffer(object.encryptedLoginToken);
                }
                if (options.bytes === String)
                    object.messageSessionUid = "";
                else {
                    object.messageSessionUid = [];
                    if (options.bytes !== Array)
                        object.messageSessionUid = $util.newBuffer(object.messageSessionUid);
                }
                object.installReferrer = "";
                object.mccMNC = 0;
                object.mfg = "";
                object.model = "";
                object.brand = "";
                object.product = "";
                object.device = "";
                object.carrier = "";
                object.verificationCode = "";
                object.enterpriseRegistration = null;
                if (options.bytes === String)
                    object.encryptedVerificationToken = "";
                else {
                    object.encryptedVerificationToken = [];
                    if (options.bytes !== Array)
                        object.encryptedVerificationToken = $util.newBuffer(object.encryptedVerificationToken);
                }
                if (options.bytes === String)
                    object.enterpriseUsersDataKey = "";
                else {
                    object.enterpriseUsersDataKey = [];
                    if (options.bytes !== Array)
                        object.enterpriseUsersDataKey = $util.newBuffer(object.enterpriseUsersDataKey);
                }
            }
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.authVerifier != null && Object.hasOwnProperty.call(message, "authVerifier"))
                object.authVerifier = options.bytes === String ? $util.base64.encode(message.authVerifier, 0, message.authVerifier.length) : options.bytes === Array ? Array.prototype.slice.call(message.authVerifier) : message.authVerifier;
            if (message.encryptionParams != null && Object.hasOwnProperty.call(message, "encryptionParams"))
                object.encryptionParams = options.bytes === String ? $util.base64.encode(message.encryptionParams, 0, message.encryptionParams.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptionParams) : message.encryptionParams;
            if (message.rsaPublicKey != null && Object.hasOwnProperty.call(message, "rsaPublicKey"))
                object.rsaPublicKey = options.bytes === String ? $util.base64.encode(message.rsaPublicKey, 0, message.rsaPublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.rsaPublicKey) : message.rsaPublicKey;
            if (message.rsaEncryptedPrivateKey != null && Object.hasOwnProperty.call(message, "rsaEncryptedPrivateKey"))
                object.rsaEncryptedPrivateKey = options.bytes === String ? $util.base64.encode(message.rsaEncryptedPrivateKey, 0, message.rsaEncryptedPrivateKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.rsaEncryptedPrivateKey) : message.rsaEncryptedPrivateKey;
            if (message.eccPublicKey != null && Object.hasOwnProperty.call(message, "eccPublicKey"))
                object.eccPublicKey = options.bytes === String ? $util.base64.encode(message.eccPublicKey, 0, message.eccPublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.eccPublicKey) : message.eccPublicKey;
            if (message.eccEncryptedPrivateKey != null && Object.hasOwnProperty.call(message, "eccEncryptedPrivateKey"))
                object.eccEncryptedPrivateKey = options.bytes === String ? $util.base64.encode(message.eccEncryptedPrivateKey, 0, message.eccEncryptedPrivateKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.eccEncryptedPrivateKey) : message.eccEncryptedPrivateKey;
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                object.encryptedDeviceToken = options.bytes === String ? $util.base64.encode(message.encryptedDeviceToken, 0, message.encryptedDeviceToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDeviceToken) : message.encryptedDeviceToken;
            if (message.encryptedClientKey != null && Object.hasOwnProperty.call(message, "encryptedClientKey"))
                object.encryptedClientKey = options.bytes === String ? $util.base64.encode(message.encryptedClientKey, 0, message.encryptedClientKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedClientKey) : message.encryptedClientKey;
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                object.clientVersion = message.clientVersion;
            if (message.encryptedDeviceDataKey != null && Object.hasOwnProperty.call(message, "encryptedDeviceDataKey"))
                object.encryptedDeviceDataKey = options.bytes === String ? $util.base64.encode(message.encryptedDeviceDataKey, 0, message.encryptedDeviceDataKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDeviceDataKey) : message.encryptedDeviceDataKey;
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                object.encryptedLoginToken = options.bytes === String ? $util.base64.encode(message.encryptedLoginToken, 0, message.encryptedLoginToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedLoginToken) : message.encryptedLoginToken;
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                object.messageSessionUid = options.bytes === String ? $util.base64.encode(message.messageSessionUid, 0, message.messageSessionUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.messageSessionUid) : message.messageSessionUid;
            if (message.installReferrer != null && Object.hasOwnProperty.call(message, "installReferrer"))
                object.installReferrer = message.installReferrer;
            if (message.mccMNC != null && Object.hasOwnProperty.call(message, "mccMNC"))
                object.mccMNC = message.mccMNC;
            if (message.mfg != null && Object.hasOwnProperty.call(message, "mfg"))
                object.mfg = message.mfg;
            if (message.model != null && Object.hasOwnProperty.call(message, "model"))
                object.model = message.model;
            if (message.brand != null && Object.hasOwnProperty.call(message, "brand"))
                object.brand = message.brand;
            if (message.product != null && Object.hasOwnProperty.call(message, "product"))
                object.product = message.product;
            if (message.device != null && Object.hasOwnProperty.call(message, "device"))
                object.device = message.device;
            if (message.carrier != null && Object.hasOwnProperty.call(message, "carrier"))
                object.carrier = message.carrier;
            if (message.verificationCode != null && Object.hasOwnProperty.call(message, "verificationCode"))
                object.verificationCode = message.verificationCode;
            if (message.enterpriseRegistration != null && Object.hasOwnProperty.call(message, "enterpriseRegistration"))
                object.enterpriseRegistration = $root.Enterprise.EnterpriseRegistration.toObject(message.enterpriseRegistration, options, q + 1);
            if (message.encryptedVerificationToken != null && Object.hasOwnProperty.call(message, "encryptedVerificationToken"))
                object.encryptedVerificationToken = options.bytes === String ? $util.base64.encode(message.encryptedVerificationToken, 0, message.encryptedVerificationToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedVerificationToken) : message.encryptedVerificationToken;
            if (message.enterpriseUsersDataKey != null && Object.hasOwnProperty.call(message, "enterpriseUsersDataKey"))
                object.enterpriseUsersDataKey = options.bytes === String ? $util.base64.encode(message.enterpriseUsersDataKey, 0, message.enterpriseUsersDataKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.enterpriseUsersDataKey) : message.enterpriseUsersDataKey;
            return object;
        };

        /**
         * Converts this CreateUserRequest to JSON.
         * @function toJSON
         * @memberof Authentication.CreateUserRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CreateUserRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CreateUserRequest
         * @function getTypeUrl
         * @memberof Authentication.CreateUserRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CreateUserRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.CreateUserRequest";
        };

        return CreateUserRequest;
    })();

    Authentication.NodeEnforcementAddOrUpdateRequest = (function() {

        /**
         * Properties of a NodeEnforcementAddOrUpdateRequest.
         * @memberof Authentication
         * @interface INodeEnforcementAddOrUpdateRequest
         * @property {number|null} [nodeId] NodeEnforcementAddOrUpdateRequest nodeId
         * @property {string|null} [enforcement] NodeEnforcementAddOrUpdateRequest enforcement
         * @property {string|null} [value] NodeEnforcementAddOrUpdateRequest value
         */

        /**
         * Constructs a new NodeEnforcementAddOrUpdateRequest.
         * @memberof Authentication
         * @classdesc Represents a NodeEnforcementAddOrUpdateRequest.
         * @implements INodeEnforcementAddOrUpdateRequest
         * @constructor
         * @param {Authentication.INodeEnforcementAddOrUpdateRequest=} [properties] Properties to set
         */
        function NodeEnforcementAddOrUpdateRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NodeEnforcementAddOrUpdateRequest nodeId.
         * @member {number} nodeId
         * @memberof Authentication.NodeEnforcementAddOrUpdateRequest
         * @instance
         */
        NodeEnforcementAddOrUpdateRequest.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * NodeEnforcementAddOrUpdateRequest enforcement.
         * @member {string} enforcement
         * @memberof Authentication.NodeEnforcementAddOrUpdateRequest
         * @instance
         */
        NodeEnforcementAddOrUpdateRequest.prototype.enforcement = "";

        /**
         * NodeEnforcementAddOrUpdateRequest value.
         * @member {string} value
         * @memberof Authentication.NodeEnforcementAddOrUpdateRequest
         * @instance
         */
        NodeEnforcementAddOrUpdateRequest.prototype.value = "";

        /**
         * Creates a new NodeEnforcementAddOrUpdateRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.NodeEnforcementAddOrUpdateRequest
         * @static
         * @param {Authentication.INodeEnforcementAddOrUpdateRequest=} [properties] Properties to set
         * @returns {Authentication.NodeEnforcementAddOrUpdateRequest} NodeEnforcementAddOrUpdateRequest instance
         */
        NodeEnforcementAddOrUpdateRequest.create = function create(properties) {
            return new NodeEnforcementAddOrUpdateRequest(properties);
        };

        /**
         * Encodes the specified NodeEnforcementAddOrUpdateRequest message. Does not implicitly {@link Authentication.NodeEnforcementAddOrUpdateRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.NodeEnforcementAddOrUpdateRequest
         * @static
         * @param {Authentication.INodeEnforcementAddOrUpdateRequest} message NodeEnforcementAddOrUpdateRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeEnforcementAddOrUpdateRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.nodeId != null && Object.hasOwnProperty.call(message, "nodeId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.nodeId);
            if (message.enforcement != null && Object.hasOwnProperty.call(message, "enforcement"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.enforcement);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.value);
            return writer;
        };

        /**
         * Decodes a NodeEnforcementAddOrUpdateRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.NodeEnforcementAddOrUpdateRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.NodeEnforcementAddOrUpdateRequest} NodeEnforcementAddOrUpdateRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeEnforcementAddOrUpdateRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.NodeEnforcementAddOrUpdateRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.nodeId = reader.int64();
                        break;
                    }
                case 2: {
                        message.enforcement = reader.string();
                        break;
                    }
                case 3: {
                        message.value = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a NodeEnforcementAddOrUpdateRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.NodeEnforcementAddOrUpdateRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.NodeEnforcementAddOrUpdateRequest} NodeEnforcementAddOrUpdateRequest
         */
        NodeEnforcementAddOrUpdateRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.NodeEnforcementAddOrUpdateRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.NodeEnforcementAddOrUpdateRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.NodeEnforcementAddOrUpdateRequest();
            if (object.nodeId != null)
                if ($util.Long)
                    message.nodeId = $util.Long.fromValue(object.nodeId, false);
                else if (typeof object.nodeId === "string")
                    message.nodeId = parseInt(object.nodeId, 10);
                else if (typeof object.nodeId === "number")
                    message.nodeId = object.nodeId;
                else if (typeof object.nodeId === "object")
                    message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber();
            if (object.enforcement != null)
                message.enforcement = String(object.enforcement);
            if (object.value != null)
                message.value = String(object.value);
            return message;
        };

        /**
         * Creates a plain object from a NodeEnforcementAddOrUpdateRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.NodeEnforcementAddOrUpdateRequest
         * @static
         * @param {Authentication.NodeEnforcementAddOrUpdateRequest} message NodeEnforcementAddOrUpdateRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NodeEnforcementAddOrUpdateRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.nodeId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.nodeId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.enforcement = "";
                object.value = "";
            }
            if (message.nodeId != null && Object.hasOwnProperty.call(message, "nodeId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.nodeId = typeof message.nodeId === "number" ? BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, false).toBigInt();
                else if (typeof message.nodeId === "number")
                    object.nodeId = options.longs === String ? String(message.nodeId) : message.nodeId;
                else
                    object.nodeId = options.longs === String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber() : message.nodeId;
            if (message.enforcement != null && Object.hasOwnProperty.call(message, "enforcement"))
                object.enforcement = message.enforcement;
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                object.value = message.value;
            return object;
        };

        /**
         * Converts this NodeEnforcementAddOrUpdateRequest to JSON.
         * @function toJSON
         * @memberof Authentication.NodeEnforcementAddOrUpdateRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NodeEnforcementAddOrUpdateRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NodeEnforcementAddOrUpdateRequest
         * @function getTypeUrl
         * @memberof Authentication.NodeEnforcementAddOrUpdateRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NodeEnforcementAddOrUpdateRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.NodeEnforcementAddOrUpdateRequest";
        };

        return NodeEnforcementAddOrUpdateRequest;
    })();

    Authentication.NodeEnforcementRemoveRequest = (function() {

        /**
         * Properties of a NodeEnforcementRemoveRequest.
         * @memberof Authentication
         * @interface INodeEnforcementRemoveRequest
         * @property {number|null} [nodeId] NodeEnforcementRemoveRequest nodeId
         * @property {string|null} [enforcement] NodeEnforcementRemoveRequest enforcement
         */

        /**
         * Constructs a new NodeEnforcementRemoveRequest.
         * @memberof Authentication
         * @classdesc Represents a NodeEnforcementRemoveRequest.
         * @implements INodeEnforcementRemoveRequest
         * @constructor
         * @param {Authentication.INodeEnforcementRemoveRequest=} [properties] Properties to set
         */
        function NodeEnforcementRemoveRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NodeEnforcementRemoveRequest nodeId.
         * @member {number} nodeId
         * @memberof Authentication.NodeEnforcementRemoveRequest
         * @instance
         */
        NodeEnforcementRemoveRequest.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * NodeEnforcementRemoveRequest enforcement.
         * @member {string} enforcement
         * @memberof Authentication.NodeEnforcementRemoveRequest
         * @instance
         */
        NodeEnforcementRemoveRequest.prototype.enforcement = "";

        /**
         * Creates a new NodeEnforcementRemoveRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.NodeEnforcementRemoveRequest
         * @static
         * @param {Authentication.INodeEnforcementRemoveRequest=} [properties] Properties to set
         * @returns {Authentication.NodeEnforcementRemoveRequest} NodeEnforcementRemoveRequest instance
         */
        NodeEnforcementRemoveRequest.create = function create(properties) {
            return new NodeEnforcementRemoveRequest(properties);
        };

        /**
         * Encodes the specified NodeEnforcementRemoveRequest message. Does not implicitly {@link Authentication.NodeEnforcementRemoveRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.NodeEnforcementRemoveRequest
         * @static
         * @param {Authentication.INodeEnforcementRemoveRequest} message NodeEnforcementRemoveRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NodeEnforcementRemoveRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.nodeId != null && Object.hasOwnProperty.call(message, "nodeId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.nodeId);
            if (message.enforcement != null && Object.hasOwnProperty.call(message, "enforcement"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.enforcement);
            return writer;
        };

        /**
         * Decodes a NodeEnforcementRemoveRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.NodeEnforcementRemoveRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.NodeEnforcementRemoveRequest} NodeEnforcementRemoveRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NodeEnforcementRemoveRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.NodeEnforcementRemoveRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.nodeId = reader.int64();
                        break;
                    }
                case 2: {
                        message.enforcement = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a NodeEnforcementRemoveRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.NodeEnforcementRemoveRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.NodeEnforcementRemoveRequest} NodeEnforcementRemoveRequest
         */
        NodeEnforcementRemoveRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.NodeEnforcementRemoveRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.NodeEnforcementRemoveRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.NodeEnforcementRemoveRequest();
            if (object.nodeId != null)
                if ($util.Long)
                    message.nodeId = $util.Long.fromValue(object.nodeId, false);
                else if (typeof object.nodeId === "string")
                    message.nodeId = parseInt(object.nodeId, 10);
                else if (typeof object.nodeId === "number")
                    message.nodeId = object.nodeId;
                else if (typeof object.nodeId === "object")
                    message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber();
            if (object.enforcement != null)
                message.enforcement = String(object.enforcement);
            return message;
        };

        /**
         * Creates a plain object from a NodeEnforcementRemoveRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.NodeEnforcementRemoveRequest
         * @static
         * @param {Authentication.NodeEnforcementRemoveRequest} message NodeEnforcementRemoveRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NodeEnforcementRemoveRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.nodeId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.nodeId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.enforcement = "";
            }
            if (message.nodeId != null && Object.hasOwnProperty.call(message, "nodeId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.nodeId = typeof message.nodeId === "number" ? BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, false).toBigInt();
                else if (typeof message.nodeId === "number")
                    object.nodeId = options.longs === String ? String(message.nodeId) : message.nodeId;
                else
                    object.nodeId = options.longs === String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber() : message.nodeId;
            if (message.enforcement != null && Object.hasOwnProperty.call(message, "enforcement"))
                object.enforcement = message.enforcement;
            return object;
        };

        /**
         * Converts this NodeEnforcementRemoveRequest to JSON.
         * @function toJSON
         * @memberof Authentication.NodeEnforcementRemoveRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NodeEnforcementRemoveRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NodeEnforcementRemoveRequest
         * @function getTypeUrl
         * @memberof Authentication.NodeEnforcementRemoveRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NodeEnforcementRemoveRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.NodeEnforcementRemoveRequest";
        };

        return NodeEnforcementRemoveRequest;
    })();

    Authentication.ApiRequestByKey = (function() {

        /**
         * Properties of an ApiRequestByKey.
         * @memberof Authentication
         * @interface IApiRequestByKey
         * @property {number|null} [keyId] ApiRequestByKey keyId
         * @property {Uint8Array|null} [payload] ApiRequestByKey payload
         * @property {string|null} [username] ApiRequestByKey username
         * @property {string|null} [locale] ApiRequestByKey locale
         * @property {Authentication.SupportedLanguage|null} [supportedLanguage] ApiRequestByKey supportedLanguage
         * @property {number|null} [type] ApiRequestByKey type
         * @property {string|null} [parentThreadId] ApiRequestByKey parentThreadId
         */

        /**
         * Constructs a new ApiRequestByKey.
         * @memberof Authentication
         * @classdesc Represents an ApiRequestByKey.
         * @implements IApiRequestByKey
         * @constructor
         * @param {Authentication.IApiRequestByKey=} [properties] Properties to set
         */
        function ApiRequestByKey(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ApiRequestByKey keyId.
         * @member {number} keyId
         * @memberof Authentication.ApiRequestByKey
         * @instance
         */
        ApiRequestByKey.prototype.keyId = 0;

        /**
         * ApiRequestByKey payload.
         * @member {Uint8Array} payload
         * @memberof Authentication.ApiRequestByKey
         * @instance
         */
        ApiRequestByKey.prototype.payload = $util.newBuffer([]);

        /**
         * ApiRequestByKey username.
         * @member {string} username
         * @memberof Authentication.ApiRequestByKey
         * @instance
         */
        ApiRequestByKey.prototype.username = "";

        /**
         * ApiRequestByKey locale.
         * @member {string} locale
         * @memberof Authentication.ApiRequestByKey
         * @instance
         */
        ApiRequestByKey.prototype.locale = "";

        /**
         * ApiRequestByKey supportedLanguage.
         * @member {Authentication.SupportedLanguage} supportedLanguage
         * @memberof Authentication.ApiRequestByKey
         * @instance
         */
        ApiRequestByKey.prototype.supportedLanguage = 0;

        /**
         * ApiRequestByKey type.
         * @member {number} type
         * @memberof Authentication.ApiRequestByKey
         * @instance
         */
        ApiRequestByKey.prototype.type = 0;

        /**
         * ApiRequestByKey parentThreadId.
         * @member {string} parentThreadId
         * @memberof Authentication.ApiRequestByKey
         * @instance
         */
        ApiRequestByKey.prototype.parentThreadId = "";

        /**
         * Creates a new ApiRequestByKey instance using the specified properties.
         * @function create
         * @memberof Authentication.ApiRequestByKey
         * @static
         * @param {Authentication.IApiRequestByKey=} [properties] Properties to set
         * @returns {Authentication.ApiRequestByKey} ApiRequestByKey instance
         */
        ApiRequestByKey.create = function create(properties) {
            return new ApiRequestByKey(properties);
        };

        /**
         * Encodes the specified ApiRequestByKey message. Does not implicitly {@link Authentication.ApiRequestByKey.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ApiRequestByKey
         * @static
         * @param {Authentication.IApiRequestByKey} message ApiRequestByKey message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ApiRequestByKey.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.keyId);
            if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.payload);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.username);
            if (message.locale != null && Object.hasOwnProperty.call(message, "locale"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.locale);
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.supportedLanguage);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.type);
            if (message.parentThreadId != null && Object.hasOwnProperty.call(message, "parentThreadId"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.parentThreadId);
            return writer;
        };

        /**
         * Decodes an ApiRequestByKey message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ApiRequestByKey
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ApiRequestByKey} ApiRequestByKey
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ApiRequestByKey.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ApiRequestByKey();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.keyId = reader.int32();
                        break;
                    }
                case 2: {
                        message.payload = reader.bytes();
                        break;
                    }
                case 3: {
                        message.username = reader.string();
                        break;
                    }
                case 4: {
                        message.locale = reader.string();
                        break;
                    }
                case 5: {
                        message.supportedLanguage = reader.int32();
                        break;
                    }
                case 6: {
                        message.type = reader.int32();
                        break;
                    }
                case 7: {
                        message.parentThreadId = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an ApiRequestByKey message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ApiRequestByKey
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ApiRequestByKey} ApiRequestByKey
         */
        ApiRequestByKey.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ApiRequestByKey)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ApiRequestByKey: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ApiRequestByKey();
            if (object.keyId != null)
                message.keyId = object.keyId | 0;
            if (object.payload != null)
                if (typeof object.payload === "string")
                    $util.base64.decode(object.payload, message.payload = $util.newBuffer($util.base64.length(object.payload)), 0);
                else if (object.payload.length >= 0)
                    message.payload = object.payload;
            if (object.username != null)
                message.username = String(object.username);
            if (object.locale != null)
                message.locale = String(object.locale);
            switch (object.supportedLanguage) {
            default:
                if (typeof object.supportedLanguage === "number") {
                    message.supportedLanguage = object.supportedLanguage;
                    break;
                }
                break;
            case "ENGLISH":
            case 0:
                message.supportedLanguage = 0;
                break;
            case "ARABIC":
            case 1:
                message.supportedLanguage = 1;
                break;
            case "BRITISH":
            case 2:
                message.supportedLanguage = 2;
                break;
            case "CHINESE":
            case 3:
                message.supportedLanguage = 3;
                break;
            case "CHINESE_HONG_KONG":
            case 4:
                message.supportedLanguage = 4;
                break;
            case "CHINESE_TAIWAN":
            case 5:
                message.supportedLanguage = 5;
                break;
            case "DUTCH":
            case 6:
                message.supportedLanguage = 6;
                break;
            case "FRENCH":
            case 7:
                message.supportedLanguage = 7;
                break;
            case "GERMAN":
            case 8:
                message.supportedLanguage = 8;
                break;
            case "GREEK":
            case 9:
                message.supportedLanguage = 9;
                break;
            case "HEBREW":
            case 10:
                message.supportedLanguage = 10;
                break;
            case "ITALIAN":
            case 11:
                message.supportedLanguage = 11;
                break;
            case "JAPANESE":
            case 12:
                message.supportedLanguage = 12;
                break;
            case "KOREAN":
            case 13:
                message.supportedLanguage = 13;
                break;
            case "POLISH":
            case 14:
                message.supportedLanguage = 14;
                break;
            case "PORTUGUESE":
            case 15:
                message.supportedLanguage = 15;
                break;
            case "PORTUGUESE_BRAZIL":
            case 16:
                message.supportedLanguage = 16;
                break;
            case "ROMANIAN":
            case 17:
                message.supportedLanguage = 17;
                break;
            case "RUSSIAN":
            case 18:
                message.supportedLanguage = 18;
                break;
            case "SLOVAK":
            case 19:
                message.supportedLanguage = 19;
                break;
            case "SPANISH":
            case 20:
                message.supportedLanguage = 20;
                break;
            case "FINNISH":
            case 21:
                message.supportedLanguage = 21;
                break;
            case "SWEDISH":
            case 22:
                message.supportedLanguage = 22;
                break;
            }
            if (object.type != null)
                message.type = object.type | 0;
            if (object.parentThreadId != null)
                message.parentThreadId = String(object.parentThreadId);
            return message;
        };

        /**
         * Creates a plain object from an ApiRequestByKey message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ApiRequestByKey
         * @static
         * @param {Authentication.ApiRequestByKey} message ApiRequestByKey
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ApiRequestByKey.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.keyId = 0;
                if (options.bytes === String)
                    object.payload = "";
                else {
                    object.payload = [];
                    if (options.bytes !== Array)
                        object.payload = $util.newBuffer(object.payload);
                }
                object.username = "";
                object.locale = "";
                object.supportedLanguage = options.enums === String ? "ENGLISH" : 0;
                object.type = 0;
                object.parentThreadId = "";
            }
            if (message.keyId != null && Object.hasOwnProperty.call(message, "keyId"))
                object.keyId = message.keyId;
            if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                object.payload = options.bytes === String ? $util.base64.encode(message.payload, 0, message.payload.length) : options.bytes === Array ? Array.prototype.slice.call(message.payload) : message.payload;
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.locale != null && Object.hasOwnProperty.call(message, "locale"))
                object.locale = message.locale;
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                object.supportedLanguage = options.enums === String ? $root.Authentication.SupportedLanguage[message.supportedLanguage] === undefined ? message.supportedLanguage : $root.Authentication.SupportedLanguage[message.supportedLanguage] : message.supportedLanguage;
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                object.type = message.type;
            if (message.parentThreadId != null && Object.hasOwnProperty.call(message, "parentThreadId"))
                object.parentThreadId = message.parentThreadId;
            return object;
        };

        /**
         * Converts this ApiRequestByKey to JSON.
         * @function toJSON
         * @memberof Authentication.ApiRequestByKey
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ApiRequestByKey.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ApiRequestByKey
         * @function getTypeUrl
         * @memberof Authentication.ApiRequestByKey
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ApiRequestByKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ApiRequestByKey";
        };

        return ApiRequestByKey;
    })();

    Authentication.ApiRequestByKAtoKAKey = (function() {

        /**
         * Properties of an ApiRequestByKAtoKAKey.
         * @memberof Authentication
         * @interface IApiRequestByKAtoKAKey
         * @property {Authentication.Region|null} [sourceRegion] ApiRequestByKAtoKAKey sourceRegion
         * @property {Uint8Array|null} [payload] ApiRequestByKAtoKAKey payload
         * @property {Authentication.SupportedLanguage|null} [supportedLanguage] ApiRequestByKAtoKAKey supportedLanguage
         * @property {Authentication.Region|null} [destinationRegion] ApiRequestByKAtoKAKey destinationRegion
         */

        /**
         * Constructs a new ApiRequestByKAtoKAKey.
         * @memberof Authentication
         * @classdesc Represents an ApiRequestByKAtoKAKey.
         * @implements IApiRequestByKAtoKAKey
         * @constructor
         * @param {Authentication.IApiRequestByKAtoKAKey=} [properties] Properties to set
         */
        function ApiRequestByKAtoKAKey(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ApiRequestByKAtoKAKey sourceRegion.
         * @member {Authentication.Region} sourceRegion
         * @memberof Authentication.ApiRequestByKAtoKAKey
         * @instance
         */
        ApiRequestByKAtoKAKey.prototype.sourceRegion = 0;

        /**
         * ApiRequestByKAtoKAKey payload.
         * @member {Uint8Array} payload
         * @memberof Authentication.ApiRequestByKAtoKAKey
         * @instance
         */
        ApiRequestByKAtoKAKey.prototype.payload = $util.newBuffer([]);

        /**
         * ApiRequestByKAtoKAKey supportedLanguage.
         * @member {Authentication.SupportedLanguage} supportedLanguage
         * @memberof Authentication.ApiRequestByKAtoKAKey
         * @instance
         */
        ApiRequestByKAtoKAKey.prototype.supportedLanguage = 0;

        /**
         * ApiRequestByKAtoKAKey destinationRegion.
         * @member {Authentication.Region} destinationRegion
         * @memberof Authentication.ApiRequestByKAtoKAKey
         * @instance
         */
        ApiRequestByKAtoKAKey.prototype.destinationRegion = 0;

        /**
         * Creates a new ApiRequestByKAtoKAKey instance using the specified properties.
         * @function create
         * @memberof Authentication.ApiRequestByKAtoKAKey
         * @static
         * @param {Authentication.IApiRequestByKAtoKAKey=} [properties] Properties to set
         * @returns {Authentication.ApiRequestByKAtoKAKey} ApiRequestByKAtoKAKey instance
         */
        ApiRequestByKAtoKAKey.create = function create(properties) {
            return new ApiRequestByKAtoKAKey(properties);
        };

        /**
         * Encodes the specified ApiRequestByKAtoKAKey message. Does not implicitly {@link Authentication.ApiRequestByKAtoKAKey.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ApiRequestByKAtoKAKey
         * @static
         * @param {Authentication.IApiRequestByKAtoKAKey} message ApiRequestByKAtoKAKey message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ApiRequestByKAtoKAKey.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.sourceRegion != null && Object.hasOwnProperty.call(message, "sourceRegion"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.sourceRegion);
            if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.payload);
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.supportedLanguage);
            if (message.destinationRegion != null && Object.hasOwnProperty.call(message, "destinationRegion"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.destinationRegion);
            return writer;
        };

        /**
         * Decodes an ApiRequestByKAtoKAKey message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ApiRequestByKAtoKAKey
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ApiRequestByKAtoKAKey} ApiRequestByKAtoKAKey
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ApiRequestByKAtoKAKey.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ApiRequestByKAtoKAKey();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.sourceRegion = reader.int32();
                        break;
                    }
                case 2: {
                        message.payload = reader.bytes();
                        break;
                    }
                case 3: {
                        message.supportedLanguage = reader.int32();
                        break;
                    }
                case 4: {
                        message.destinationRegion = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an ApiRequestByKAtoKAKey message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ApiRequestByKAtoKAKey
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ApiRequestByKAtoKAKey} ApiRequestByKAtoKAKey
         */
        ApiRequestByKAtoKAKey.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ApiRequestByKAtoKAKey)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ApiRequestByKAtoKAKey: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ApiRequestByKAtoKAKey();
            switch (object.sourceRegion) {
            default:
                if (typeof object.sourceRegion === "number") {
                    message.sourceRegion = object.sourceRegion;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                message.sourceRegion = 0;
                break;
            case "eu":
            case 1:
                message.sourceRegion = 1;
                break;
            case "us":
            case 2:
                message.sourceRegion = 2;
                break;
            case "usgov":
            case 3:
                message.sourceRegion = 3;
                break;
            case "au":
            case 4:
                message.sourceRegion = 4;
                break;
            case "jp":
            case 5:
                message.sourceRegion = 5;
                break;
            case "ca":
            case 6:
                message.sourceRegion = 6;
                break;
            }
            if (object.payload != null)
                if (typeof object.payload === "string")
                    $util.base64.decode(object.payload, message.payload = $util.newBuffer($util.base64.length(object.payload)), 0);
                else if (object.payload.length >= 0)
                    message.payload = object.payload;
            switch (object.supportedLanguage) {
            default:
                if (typeof object.supportedLanguage === "number") {
                    message.supportedLanguage = object.supportedLanguage;
                    break;
                }
                break;
            case "ENGLISH":
            case 0:
                message.supportedLanguage = 0;
                break;
            case "ARABIC":
            case 1:
                message.supportedLanguage = 1;
                break;
            case "BRITISH":
            case 2:
                message.supportedLanguage = 2;
                break;
            case "CHINESE":
            case 3:
                message.supportedLanguage = 3;
                break;
            case "CHINESE_HONG_KONG":
            case 4:
                message.supportedLanguage = 4;
                break;
            case "CHINESE_TAIWAN":
            case 5:
                message.supportedLanguage = 5;
                break;
            case "DUTCH":
            case 6:
                message.supportedLanguage = 6;
                break;
            case "FRENCH":
            case 7:
                message.supportedLanguage = 7;
                break;
            case "GERMAN":
            case 8:
                message.supportedLanguage = 8;
                break;
            case "GREEK":
            case 9:
                message.supportedLanguage = 9;
                break;
            case "HEBREW":
            case 10:
                message.supportedLanguage = 10;
                break;
            case "ITALIAN":
            case 11:
                message.supportedLanguage = 11;
                break;
            case "JAPANESE":
            case 12:
                message.supportedLanguage = 12;
                break;
            case "KOREAN":
            case 13:
                message.supportedLanguage = 13;
                break;
            case "POLISH":
            case 14:
                message.supportedLanguage = 14;
                break;
            case "PORTUGUESE":
            case 15:
                message.supportedLanguage = 15;
                break;
            case "PORTUGUESE_BRAZIL":
            case 16:
                message.supportedLanguage = 16;
                break;
            case "ROMANIAN":
            case 17:
                message.supportedLanguage = 17;
                break;
            case "RUSSIAN":
            case 18:
                message.supportedLanguage = 18;
                break;
            case "SLOVAK":
            case 19:
                message.supportedLanguage = 19;
                break;
            case "SPANISH":
            case 20:
                message.supportedLanguage = 20;
                break;
            case "FINNISH":
            case 21:
                message.supportedLanguage = 21;
                break;
            case "SWEDISH":
            case 22:
                message.supportedLanguage = 22;
                break;
            }
            switch (object.destinationRegion) {
            default:
                if (typeof object.destinationRegion === "number") {
                    message.destinationRegion = object.destinationRegion;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                message.destinationRegion = 0;
                break;
            case "eu":
            case 1:
                message.destinationRegion = 1;
                break;
            case "us":
            case 2:
                message.destinationRegion = 2;
                break;
            case "usgov":
            case 3:
                message.destinationRegion = 3;
                break;
            case "au":
            case 4:
                message.destinationRegion = 4;
                break;
            case "jp":
            case 5:
                message.destinationRegion = 5;
                break;
            case "ca":
            case 6:
                message.destinationRegion = 6;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from an ApiRequestByKAtoKAKey message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ApiRequestByKAtoKAKey
         * @static
         * @param {Authentication.ApiRequestByKAtoKAKey} message ApiRequestByKAtoKAKey
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ApiRequestByKAtoKAKey.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.sourceRegion = options.enums === String ? "UNKNOWN" : 0;
                if (options.bytes === String)
                    object.payload = "";
                else {
                    object.payload = [];
                    if (options.bytes !== Array)
                        object.payload = $util.newBuffer(object.payload);
                }
                object.supportedLanguage = options.enums === String ? "ENGLISH" : 0;
                object.destinationRegion = options.enums === String ? "UNKNOWN" : 0;
            }
            if (message.sourceRegion != null && Object.hasOwnProperty.call(message, "sourceRegion"))
                object.sourceRegion = options.enums === String ? $root.Authentication.Region[message.sourceRegion] === undefined ? message.sourceRegion : $root.Authentication.Region[message.sourceRegion] : message.sourceRegion;
            if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                object.payload = options.bytes === String ? $util.base64.encode(message.payload, 0, message.payload.length) : options.bytes === Array ? Array.prototype.slice.call(message.payload) : message.payload;
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                object.supportedLanguage = options.enums === String ? $root.Authentication.SupportedLanguage[message.supportedLanguage] === undefined ? message.supportedLanguage : $root.Authentication.SupportedLanguage[message.supportedLanguage] : message.supportedLanguage;
            if (message.destinationRegion != null && Object.hasOwnProperty.call(message, "destinationRegion"))
                object.destinationRegion = options.enums === String ? $root.Authentication.Region[message.destinationRegion] === undefined ? message.destinationRegion : $root.Authentication.Region[message.destinationRegion] : message.destinationRegion;
            return object;
        };

        /**
         * Converts this ApiRequestByKAtoKAKey to JSON.
         * @function toJSON
         * @memberof Authentication.ApiRequestByKAtoKAKey
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ApiRequestByKAtoKAKey.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ApiRequestByKAtoKAKey
         * @function getTypeUrl
         * @memberof Authentication.ApiRequestByKAtoKAKey
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ApiRequestByKAtoKAKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ApiRequestByKAtoKAKey";
        };

        return ApiRequestByKAtoKAKey;
    })();

    Authentication.MemcacheRequest = (function() {

        /**
         * Properties of a MemcacheRequest.
         * @memberof Authentication
         * @interface IMemcacheRequest
         * @property {string|null} [key] MemcacheRequest key
         * @property {number|null} [userId] MemcacheRequest userId
         */

        /**
         * Constructs a new MemcacheRequest.
         * @memberof Authentication
         * @classdesc Represents a MemcacheRequest.
         * @implements IMemcacheRequest
         * @constructor
         * @param {Authentication.IMemcacheRequest=} [properties] Properties to set
         */
        function MemcacheRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MemcacheRequest key.
         * @member {string} key
         * @memberof Authentication.MemcacheRequest
         * @instance
         */
        MemcacheRequest.prototype.key = "";

        /**
         * MemcacheRequest userId.
         * @member {number} userId
         * @memberof Authentication.MemcacheRequest
         * @instance
         */
        MemcacheRequest.prototype.userId = 0;

        /**
         * Creates a new MemcacheRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.MemcacheRequest
         * @static
         * @param {Authentication.IMemcacheRequest=} [properties] Properties to set
         * @returns {Authentication.MemcacheRequest} MemcacheRequest instance
         */
        MemcacheRequest.create = function create(properties) {
            return new MemcacheRequest(properties);
        };

        /**
         * Encodes the specified MemcacheRequest message. Does not implicitly {@link Authentication.MemcacheRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.MemcacheRequest
         * @static
         * @param {Authentication.IMemcacheRequest} message MemcacheRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MemcacheRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.userId);
            return writer;
        };

        /**
         * Decodes a MemcacheRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.MemcacheRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.MemcacheRequest} MemcacheRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MemcacheRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.MemcacheRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.key = reader.string();
                        break;
                    }
                case 2: {
                        message.userId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a MemcacheRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.MemcacheRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.MemcacheRequest} MemcacheRequest
         */
        MemcacheRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.MemcacheRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.MemcacheRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.MemcacheRequest();
            if (object.key != null)
                message.key = String(object.key);
            if (object.userId != null)
                message.userId = object.userId | 0;
            return message;
        };

        /**
         * Creates a plain object from a MemcacheRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.MemcacheRequest
         * @static
         * @param {Authentication.MemcacheRequest} message MemcacheRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MemcacheRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.key = "";
                object.userId = 0;
            }
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                object.key = message.key;
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                object.userId = message.userId;
            return object;
        };

        /**
         * Converts this MemcacheRequest to JSON.
         * @function toJSON
         * @memberof Authentication.MemcacheRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MemcacheRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MemcacheRequest
         * @function getTypeUrl
         * @memberof Authentication.MemcacheRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MemcacheRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.MemcacheRequest";
        };

        return MemcacheRequest;
    })();

    Authentication.MemcacheResponse = (function() {

        /**
         * Properties of a MemcacheResponse.
         * @memberof Authentication
         * @interface IMemcacheResponse
         * @property {string|null} [key] MemcacheResponse key
         * @property {string|null} [value] MemcacheResponse value
         */

        /**
         * Constructs a new MemcacheResponse.
         * @memberof Authentication
         * @classdesc Represents a MemcacheResponse.
         * @implements IMemcacheResponse
         * @constructor
         * @param {Authentication.IMemcacheResponse=} [properties] Properties to set
         */
        function MemcacheResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MemcacheResponse key.
         * @member {string} key
         * @memberof Authentication.MemcacheResponse
         * @instance
         */
        MemcacheResponse.prototype.key = "";

        /**
         * MemcacheResponse value.
         * @member {string} value
         * @memberof Authentication.MemcacheResponse
         * @instance
         */
        MemcacheResponse.prototype.value = "";

        /**
         * Creates a new MemcacheResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.MemcacheResponse
         * @static
         * @param {Authentication.IMemcacheResponse=} [properties] Properties to set
         * @returns {Authentication.MemcacheResponse} MemcacheResponse instance
         */
        MemcacheResponse.create = function create(properties) {
            return new MemcacheResponse(properties);
        };

        /**
         * Encodes the specified MemcacheResponse message. Does not implicitly {@link Authentication.MemcacheResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.MemcacheResponse
         * @static
         * @param {Authentication.IMemcacheResponse} message MemcacheResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MemcacheResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.value);
            return writer;
        };

        /**
         * Decodes a MemcacheResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.MemcacheResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.MemcacheResponse} MemcacheResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MemcacheResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.MemcacheResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.key = reader.string();
                        break;
                    }
                case 2: {
                        message.value = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a MemcacheResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.MemcacheResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.MemcacheResponse} MemcacheResponse
         */
        MemcacheResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.MemcacheResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.MemcacheResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.MemcacheResponse();
            if (object.key != null)
                message.key = String(object.key);
            if (object.value != null)
                message.value = String(object.value);
            return message;
        };

        /**
         * Creates a plain object from a MemcacheResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.MemcacheResponse
         * @static
         * @param {Authentication.MemcacheResponse} message MemcacheResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MemcacheResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.key = "";
                object.value = "";
            }
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                object.key = message.key;
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                object.value = message.value;
            return object;
        };

        /**
         * Converts this MemcacheResponse to JSON.
         * @function toJSON
         * @memberof Authentication.MemcacheResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MemcacheResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MemcacheResponse
         * @function getTypeUrl
         * @memberof Authentication.MemcacheResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MemcacheResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.MemcacheResponse";
        };

        return MemcacheResponse;
    })();

    Authentication.MasterPasswordReentryRequest = (function() {

        /**
         * Properties of a MasterPasswordReentryRequest.
         * @memberof Authentication
         * @interface IMasterPasswordReentryRequest
         * @property {string|null} [pbkdf2Password] MasterPasswordReentryRequest pbkdf2Password
         * @property {Authentication.MasterPasswordReentryActionType|null} [action] MasterPasswordReentryRequest action
         */

        /**
         * Constructs a new MasterPasswordReentryRequest.
         * @memberof Authentication
         * @classdesc Represents a MasterPasswordReentryRequest.
         * @implements IMasterPasswordReentryRequest
         * @constructor
         * @param {Authentication.IMasterPasswordReentryRequest=} [properties] Properties to set
         */
        function MasterPasswordReentryRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MasterPasswordReentryRequest pbkdf2Password.
         * @member {string} pbkdf2Password
         * @memberof Authentication.MasterPasswordReentryRequest
         * @instance
         */
        MasterPasswordReentryRequest.prototype.pbkdf2Password = "";

        /**
         * MasterPasswordReentryRequest action.
         * @member {Authentication.MasterPasswordReentryActionType} action
         * @memberof Authentication.MasterPasswordReentryRequest
         * @instance
         */
        MasterPasswordReentryRequest.prototype.action = 0;

        /**
         * Creates a new MasterPasswordReentryRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.MasterPasswordReentryRequest
         * @static
         * @param {Authentication.IMasterPasswordReentryRequest=} [properties] Properties to set
         * @returns {Authentication.MasterPasswordReentryRequest} MasterPasswordReentryRequest instance
         */
        MasterPasswordReentryRequest.create = function create(properties) {
            return new MasterPasswordReentryRequest(properties);
        };

        /**
         * Encodes the specified MasterPasswordReentryRequest message. Does not implicitly {@link Authentication.MasterPasswordReentryRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.MasterPasswordReentryRequest
         * @static
         * @param {Authentication.IMasterPasswordReentryRequest} message MasterPasswordReentryRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MasterPasswordReentryRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.pbkdf2Password != null && Object.hasOwnProperty.call(message, "pbkdf2Password"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.pbkdf2Password);
            if (message.action != null && Object.hasOwnProperty.call(message, "action"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.action);
            return writer;
        };

        /**
         * Decodes a MasterPasswordReentryRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.MasterPasswordReentryRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.MasterPasswordReentryRequest} MasterPasswordReentryRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MasterPasswordReentryRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.MasterPasswordReentryRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.pbkdf2Password = reader.string();
                        break;
                    }
                case 2: {
                        message.action = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a MasterPasswordReentryRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.MasterPasswordReentryRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.MasterPasswordReentryRequest} MasterPasswordReentryRequest
         */
        MasterPasswordReentryRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.MasterPasswordReentryRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.MasterPasswordReentryRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.MasterPasswordReentryRequest();
            if (object.pbkdf2Password != null)
                message.pbkdf2Password = String(object.pbkdf2Password);
            switch (object.action) {
            default:
                if (typeof object.action === "number") {
                    message.action = object.action;
                    break;
                }
                break;
            case "UNMASK":
            case 0:
                message.action = 0;
                break;
            case "COPY":
            case 1:
                message.action = 1;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a MasterPasswordReentryRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.MasterPasswordReentryRequest
         * @static
         * @param {Authentication.MasterPasswordReentryRequest} message MasterPasswordReentryRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MasterPasswordReentryRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.pbkdf2Password = "";
                object.action = options.enums === String ? "UNMASK" : 0;
            }
            if (message.pbkdf2Password != null && Object.hasOwnProperty.call(message, "pbkdf2Password"))
                object.pbkdf2Password = message.pbkdf2Password;
            if (message.action != null && Object.hasOwnProperty.call(message, "action"))
                object.action = options.enums === String ? $root.Authentication.MasterPasswordReentryActionType[message.action] === undefined ? message.action : $root.Authentication.MasterPasswordReentryActionType[message.action] : message.action;
            return object;
        };

        /**
         * Converts this MasterPasswordReentryRequest to JSON.
         * @function toJSON
         * @memberof Authentication.MasterPasswordReentryRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MasterPasswordReentryRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MasterPasswordReentryRequest
         * @function getTypeUrl
         * @memberof Authentication.MasterPasswordReentryRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MasterPasswordReentryRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.MasterPasswordReentryRequest";
        };

        return MasterPasswordReentryRequest;
    })();

    /**
     * MasterPasswordReentryStatus enum.
     * @name Authentication.MasterPasswordReentryStatus
     * @enum {number}
     * @property {number} MP_UNKNOWN=0 MP_UNKNOWN value
     * @property {number} MP_SUCCESS=1 MP_SUCCESS value
     * @property {number} MP_FAILURE=2 MP_FAILURE value
     */
    Authentication.MasterPasswordReentryStatus = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "MP_UNKNOWN"] = 0;
        values[valuesById[1] = "MP_SUCCESS"] = 1;
        values[valuesById[2] = "MP_FAILURE"] = 2;
        return values;
    })();

    Authentication.MasterPasswordReentryResponse = (function() {

        /**
         * Properties of a MasterPasswordReentryResponse.
         * @memberof Authentication
         * @interface IMasterPasswordReentryResponse
         * @property {Authentication.MasterPasswordReentryStatus|null} [status] MasterPasswordReentryResponse status
         */

        /**
         * Constructs a new MasterPasswordReentryResponse.
         * @memberof Authentication
         * @classdesc Represents a MasterPasswordReentryResponse.
         * @implements IMasterPasswordReentryResponse
         * @constructor
         * @param {Authentication.IMasterPasswordReentryResponse=} [properties] Properties to set
         */
        function MasterPasswordReentryResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MasterPasswordReentryResponse status.
         * @member {Authentication.MasterPasswordReentryStatus} status
         * @memberof Authentication.MasterPasswordReentryResponse
         * @instance
         */
        MasterPasswordReentryResponse.prototype.status = 0;

        /**
         * Creates a new MasterPasswordReentryResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.MasterPasswordReentryResponse
         * @static
         * @param {Authentication.IMasterPasswordReentryResponse=} [properties] Properties to set
         * @returns {Authentication.MasterPasswordReentryResponse} MasterPasswordReentryResponse instance
         */
        MasterPasswordReentryResponse.create = function create(properties) {
            return new MasterPasswordReentryResponse(properties);
        };

        /**
         * Encodes the specified MasterPasswordReentryResponse message. Does not implicitly {@link Authentication.MasterPasswordReentryResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.MasterPasswordReentryResponse
         * @static
         * @param {Authentication.IMasterPasswordReentryResponse} message MasterPasswordReentryResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MasterPasswordReentryResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.status);
            return writer;
        };

        /**
         * Decodes a MasterPasswordReentryResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.MasterPasswordReentryResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.MasterPasswordReentryResponse} MasterPasswordReentryResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MasterPasswordReentryResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.MasterPasswordReentryResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.status = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a MasterPasswordReentryResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.MasterPasswordReentryResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.MasterPasswordReentryResponse} MasterPasswordReentryResponse
         */
        MasterPasswordReentryResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.MasterPasswordReentryResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.MasterPasswordReentryResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.MasterPasswordReentryResponse();
            switch (object.status) {
            default:
                if (typeof object.status === "number") {
                    message.status = object.status;
                    break;
                }
                break;
            case "MP_UNKNOWN":
            case 0:
                message.status = 0;
                break;
            case "MP_SUCCESS":
            case 1:
                message.status = 1;
                break;
            case "MP_FAILURE":
            case 2:
                message.status = 2;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a MasterPasswordReentryResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.MasterPasswordReentryResponse
         * @static
         * @param {Authentication.MasterPasswordReentryResponse} message MasterPasswordReentryResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MasterPasswordReentryResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.status = options.enums === String ? "MP_UNKNOWN" : 0;
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                object.status = options.enums === String ? $root.Authentication.MasterPasswordReentryStatus[message.status] === undefined ? message.status : $root.Authentication.MasterPasswordReentryStatus[message.status] : message.status;
            return object;
        };

        /**
         * Converts this MasterPasswordReentryResponse to JSON.
         * @function toJSON
         * @memberof Authentication.MasterPasswordReentryResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MasterPasswordReentryResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MasterPasswordReentryResponse
         * @function getTypeUrl
         * @memberof Authentication.MasterPasswordReentryResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MasterPasswordReentryResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.MasterPasswordReentryResponse";
        };

        return MasterPasswordReentryResponse;
    })();

    Authentication.DeviceRegistrationRequest = (function() {

        /**
         * Properties of a DeviceRegistrationRequest.
         * @memberof Authentication
         * @interface IDeviceRegistrationRequest
         * @property {string|null} [clientVersion] DeviceRegistrationRequest clientVersion
         * @property {string|null} [deviceName] DeviceRegistrationRequest deviceName
         * @property {Uint8Array|null} [devicePublicKey] DeviceRegistrationRequest devicePublicKey
         * @property {string|null} [devicePlatform] DeviceRegistrationRequest devicePlatform
         * @property {Authentication.ClientFormFactor|null} [clientFormFactor] DeviceRegistrationRequest clientFormFactor
         * @property {string|null} [username] DeviceRegistrationRequest username
         */

        /**
         * Constructs a new DeviceRegistrationRequest.
         * @memberof Authentication
         * @classdesc Represents a DeviceRegistrationRequest.
         * @implements IDeviceRegistrationRequest
         * @constructor
         * @param {Authentication.IDeviceRegistrationRequest=} [properties] Properties to set
         */
        function DeviceRegistrationRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeviceRegistrationRequest clientVersion.
         * @member {string} clientVersion
         * @memberof Authentication.DeviceRegistrationRequest
         * @instance
         */
        DeviceRegistrationRequest.prototype.clientVersion = "";

        /**
         * DeviceRegistrationRequest deviceName.
         * @member {string} deviceName
         * @memberof Authentication.DeviceRegistrationRequest
         * @instance
         */
        DeviceRegistrationRequest.prototype.deviceName = "";

        /**
         * DeviceRegistrationRequest devicePublicKey.
         * @member {Uint8Array} devicePublicKey
         * @memberof Authentication.DeviceRegistrationRequest
         * @instance
         */
        DeviceRegistrationRequest.prototype.devicePublicKey = $util.newBuffer([]);

        /**
         * DeviceRegistrationRequest devicePlatform.
         * @member {string} devicePlatform
         * @memberof Authentication.DeviceRegistrationRequest
         * @instance
         */
        DeviceRegistrationRequest.prototype.devicePlatform = "";

        /**
         * DeviceRegistrationRequest clientFormFactor.
         * @member {Authentication.ClientFormFactor} clientFormFactor
         * @memberof Authentication.DeviceRegistrationRequest
         * @instance
         */
        DeviceRegistrationRequest.prototype.clientFormFactor = 0;

        /**
         * DeviceRegistrationRequest username.
         * @member {string} username
         * @memberof Authentication.DeviceRegistrationRequest
         * @instance
         */
        DeviceRegistrationRequest.prototype.username = "";

        /**
         * Creates a new DeviceRegistrationRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.DeviceRegistrationRequest
         * @static
         * @param {Authentication.IDeviceRegistrationRequest=} [properties] Properties to set
         * @returns {Authentication.DeviceRegistrationRequest} DeviceRegistrationRequest instance
         */
        DeviceRegistrationRequest.create = function create(properties) {
            return new DeviceRegistrationRequest(properties);
        };

        /**
         * Encodes the specified DeviceRegistrationRequest message. Does not implicitly {@link Authentication.DeviceRegistrationRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.DeviceRegistrationRequest
         * @static
         * @param {Authentication.IDeviceRegistrationRequest} message DeviceRegistrationRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceRegistrationRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.clientVersion);
            if (message.deviceName != null && Object.hasOwnProperty.call(message, "deviceName"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.deviceName);
            if (message.devicePublicKey != null && Object.hasOwnProperty.call(message, "devicePublicKey"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.devicePublicKey);
            if (message.devicePlatform != null && Object.hasOwnProperty.call(message, "devicePlatform"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.devicePlatform);
            if (message.clientFormFactor != null && Object.hasOwnProperty.call(message, "clientFormFactor"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.clientFormFactor);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.username);
            return writer;
        };

        /**
         * Decodes a DeviceRegistrationRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.DeviceRegistrationRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.DeviceRegistrationRequest} DeviceRegistrationRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceRegistrationRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.DeviceRegistrationRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.clientVersion = reader.string();
                        break;
                    }
                case 2: {
                        message.deviceName = reader.string();
                        break;
                    }
                case 3: {
                        message.devicePublicKey = reader.bytes();
                        break;
                    }
                case 4: {
                        message.devicePlatform = reader.string();
                        break;
                    }
                case 5: {
                        message.clientFormFactor = reader.int32();
                        break;
                    }
                case 6: {
                        message.username = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a DeviceRegistrationRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.DeviceRegistrationRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.DeviceRegistrationRequest} DeviceRegistrationRequest
         */
        DeviceRegistrationRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.DeviceRegistrationRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.DeviceRegistrationRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.DeviceRegistrationRequest();
            if (object.clientVersion != null)
                message.clientVersion = String(object.clientVersion);
            if (object.deviceName != null)
                message.deviceName = String(object.deviceName);
            if (object.devicePublicKey != null)
                if (typeof object.devicePublicKey === "string")
                    $util.base64.decode(object.devicePublicKey, message.devicePublicKey = $util.newBuffer($util.base64.length(object.devicePublicKey)), 0);
                else if (object.devicePublicKey.length >= 0)
                    message.devicePublicKey = object.devicePublicKey;
            if (object.devicePlatform != null)
                message.devicePlatform = String(object.devicePlatform);
            switch (object.clientFormFactor) {
            default:
                if (typeof object.clientFormFactor === "number") {
                    message.clientFormFactor = object.clientFormFactor;
                    break;
                }
                break;
            case "FF_EMPTY":
            case 0:
                message.clientFormFactor = 0;
                break;
            case "FF_PHONE":
            case 1:
                message.clientFormFactor = 1;
                break;
            case "FF_TABLET":
            case 2:
                message.clientFormFactor = 2;
                break;
            case "FF_WATCH":
            case 3:
                message.clientFormFactor = 3;
                break;
            }
            if (object.username != null)
                message.username = String(object.username);
            return message;
        };

        /**
         * Creates a plain object from a DeviceRegistrationRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.DeviceRegistrationRequest
         * @static
         * @param {Authentication.DeviceRegistrationRequest} message DeviceRegistrationRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeviceRegistrationRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.clientVersion = "";
                object.deviceName = "";
                if (options.bytes === String)
                    object.devicePublicKey = "";
                else {
                    object.devicePublicKey = [];
                    if (options.bytes !== Array)
                        object.devicePublicKey = $util.newBuffer(object.devicePublicKey);
                }
                object.devicePlatform = "";
                object.clientFormFactor = options.enums === String ? "FF_EMPTY" : 0;
                object.username = "";
            }
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                object.clientVersion = message.clientVersion;
            if (message.deviceName != null && Object.hasOwnProperty.call(message, "deviceName"))
                object.deviceName = message.deviceName;
            if (message.devicePublicKey != null && Object.hasOwnProperty.call(message, "devicePublicKey"))
                object.devicePublicKey = options.bytes === String ? $util.base64.encode(message.devicePublicKey, 0, message.devicePublicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.devicePublicKey) : message.devicePublicKey;
            if (message.devicePlatform != null && Object.hasOwnProperty.call(message, "devicePlatform"))
                object.devicePlatform = message.devicePlatform;
            if (message.clientFormFactor != null && Object.hasOwnProperty.call(message, "clientFormFactor"))
                object.clientFormFactor = options.enums === String ? $root.Authentication.ClientFormFactor[message.clientFormFactor] === undefined ? message.clientFormFactor : $root.Authentication.ClientFormFactor[message.clientFormFactor] : message.clientFormFactor;
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            return object;
        };

        /**
         * Converts this DeviceRegistrationRequest to JSON.
         * @function toJSON
         * @memberof Authentication.DeviceRegistrationRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeviceRegistrationRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeviceRegistrationRequest
         * @function getTypeUrl
         * @memberof Authentication.DeviceRegistrationRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeviceRegistrationRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.DeviceRegistrationRequest";
        };

        return DeviceRegistrationRequest;
    })();

    Authentication.DeviceVerificationRequest = (function() {

        /**
         * Properties of a DeviceVerificationRequest.
         * @memberof Authentication
         * @interface IDeviceVerificationRequest
         * @property {Uint8Array|null} [encryptedDeviceToken] DeviceVerificationRequest encryptedDeviceToken
         * @property {string|null} [username] DeviceVerificationRequest username
         * @property {string|null} [verificationChannel] DeviceVerificationRequest verificationChannel
         * @property {Uint8Array|null} [messageSessionUid] DeviceVerificationRequest messageSessionUid
         * @property {string|null} [clientVersion] DeviceVerificationRequest clientVersion
         */

        /**
         * Constructs a new DeviceVerificationRequest.
         * @memberof Authentication
         * @classdesc Represents a DeviceVerificationRequest.
         * @implements IDeviceVerificationRequest
         * @constructor
         * @param {Authentication.IDeviceVerificationRequest=} [properties] Properties to set
         */
        function DeviceVerificationRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeviceVerificationRequest encryptedDeviceToken.
         * @member {Uint8Array} encryptedDeviceToken
         * @memberof Authentication.DeviceVerificationRequest
         * @instance
         */
        DeviceVerificationRequest.prototype.encryptedDeviceToken = $util.newBuffer([]);

        /**
         * DeviceVerificationRequest username.
         * @member {string} username
         * @memberof Authentication.DeviceVerificationRequest
         * @instance
         */
        DeviceVerificationRequest.prototype.username = "";

        /**
         * DeviceVerificationRequest verificationChannel.
         * @member {string} verificationChannel
         * @memberof Authentication.DeviceVerificationRequest
         * @instance
         */
        DeviceVerificationRequest.prototype.verificationChannel = "";

        /**
         * DeviceVerificationRequest messageSessionUid.
         * @member {Uint8Array} messageSessionUid
         * @memberof Authentication.DeviceVerificationRequest
         * @instance
         */
        DeviceVerificationRequest.prototype.messageSessionUid = $util.newBuffer([]);

        /**
         * DeviceVerificationRequest clientVersion.
         * @member {string} clientVersion
         * @memberof Authentication.DeviceVerificationRequest
         * @instance
         */
        DeviceVerificationRequest.prototype.clientVersion = "";

        /**
         * Creates a new DeviceVerificationRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.DeviceVerificationRequest
         * @static
         * @param {Authentication.IDeviceVerificationRequest=} [properties] Properties to set
         * @returns {Authentication.DeviceVerificationRequest} DeviceVerificationRequest instance
         */
        DeviceVerificationRequest.create = function create(properties) {
            return new DeviceVerificationRequest(properties);
        };

        /**
         * Encodes the specified DeviceVerificationRequest message. Does not implicitly {@link Authentication.DeviceVerificationRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.DeviceVerificationRequest
         * @static
         * @param {Authentication.IDeviceVerificationRequest} message DeviceVerificationRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceVerificationRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedDeviceToken);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.username);
            if (message.verificationChannel != null && Object.hasOwnProperty.call(message, "verificationChannel"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.verificationChannel);
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.messageSessionUid);
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.clientVersion);
            return writer;
        };

        /**
         * Decodes a DeviceVerificationRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.DeviceVerificationRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.DeviceVerificationRequest} DeviceVerificationRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceVerificationRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.DeviceVerificationRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedDeviceToken = reader.bytes();
                        break;
                    }
                case 2: {
                        message.username = reader.string();
                        break;
                    }
                case 3: {
                        message.verificationChannel = reader.string();
                        break;
                    }
                case 4: {
                        message.messageSessionUid = reader.bytes();
                        break;
                    }
                case 5: {
                        message.clientVersion = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a DeviceVerificationRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.DeviceVerificationRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.DeviceVerificationRequest} DeviceVerificationRequest
         */
        DeviceVerificationRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.DeviceVerificationRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.DeviceVerificationRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.DeviceVerificationRequest();
            if (object.encryptedDeviceToken != null)
                if (typeof object.encryptedDeviceToken === "string")
                    $util.base64.decode(object.encryptedDeviceToken, message.encryptedDeviceToken = $util.newBuffer($util.base64.length(object.encryptedDeviceToken)), 0);
                else if (object.encryptedDeviceToken.length >= 0)
                    message.encryptedDeviceToken = object.encryptedDeviceToken;
            if (object.username != null)
                message.username = String(object.username);
            if (object.verificationChannel != null)
                message.verificationChannel = String(object.verificationChannel);
            if (object.messageSessionUid != null)
                if (typeof object.messageSessionUid === "string")
                    $util.base64.decode(object.messageSessionUid, message.messageSessionUid = $util.newBuffer($util.base64.length(object.messageSessionUid)), 0);
                else if (object.messageSessionUid.length >= 0)
                    message.messageSessionUid = object.messageSessionUid;
            if (object.clientVersion != null)
                message.clientVersion = String(object.clientVersion);
            return message;
        };

        /**
         * Creates a plain object from a DeviceVerificationRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.DeviceVerificationRequest
         * @static
         * @param {Authentication.DeviceVerificationRequest} message DeviceVerificationRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeviceVerificationRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.encryptedDeviceToken = "";
                else {
                    object.encryptedDeviceToken = [];
                    if (options.bytes !== Array)
                        object.encryptedDeviceToken = $util.newBuffer(object.encryptedDeviceToken);
                }
                object.username = "";
                object.verificationChannel = "";
                if (options.bytes === String)
                    object.messageSessionUid = "";
                else {
                    object.messageSessionUid = [];
                    if (options.bytes !== Array)
                        object.messageSessionUid = $util.newBuffer(object.messageSessionUid);
                }
                object.clientVersion = "";
            }
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                object.encryptedDeviceToken = options.bytes === String ? $util.base64.encode(message.encryptedDeviceToken, 0, message.encryptedDeviceToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDeviceToken) : message.encryptedDeviceToken;
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.verificationChannel != null && Object.hasOwnProperty.call(message, "verificationChannel"))
                object.verificationChannel = message.verificationChannel;
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                object.messageSessionUid = options.bytes === String ? $util.base64.encode(message.messageSessionUid, 0, message.messageSessionUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.messageSessionUid) : message.messageSessionUid;
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                object.clientVersion = message.clientVersion;
            return object;
        };

        /**
         * Converts this DeviceVerificationRequest to JSON.
         * @function toJSON
         * @memberof Authentication.DeviceVerificationRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeviceVerificationRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeviceVerificationRequest
         * @function getTypeUrl
         * @memberof Authentication.DeviceVerificationRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeviceVerificationRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.DeviceVerificationRequest";
        };

        return DeviceVerificationRequest;
    })();

    Authentication.DeviceVerificationResponse = (function() {

        /**
         * Properties of a DeviceVerificationResponse.
         * @memberof Authentication
         * @interface IDeviceVerificationResponse
         * @property {Uint8Array|null} [encryptedDeviceToken] DeviceVerificationResponse encryptedDeviceToken
         * @property {string|null} [username] DeviceVerificationResponse username
         * @property {Uint8Array|null} [messageSessionUid] DeviceVerificationResponse messageSessionUid
         * @property {string|null} [clientVersion] DeviceVerificationResponse clientVersion
         * @property {Authentication.DeviceStatus|null} [deviceStatus] DeviceVerificationResponse deviceStatus
         */

        /**
         * Constructs a new DeviceVerificationResponse.
         * @memberof Authentication
         * @classdesc Represents a DeviceVerificationResponse.
         * @implements IDeviceVerificationResponse
         * @constructor
         * @param {Authentication.IDeviceVerificationResponse=} [properties] Properties to set
         */
        function DeviceVerificationResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeviceVerificationResponse encryptedDeviceToken.
         * @member {Uint8Array} encryptedDeviceToken
         * @memberof Authentication.DeviceVerificationResponse
         * @instance
         */
        DeviceVerificationResponse.prototype.encryptedDeviceToken = $util.newBuffer([]);

        /**
         * DeviceVerificationResponse username.
         * @member {string} username
         * @memberof Authentication.DeviceVerificationResponse
         * @instance
         */
        DeviceVerificationResponse.prototype.username = "";

        /**
         * DeviceVerificationResponse messageSessionUid.
         * @member {Uint8Array} messageSessionUid
         * @memberof Authentication.DeviceVerificationResponse
         * @instance
         */
        DeviceVerificationResponse.prototype.messageSessionUid = $util.newBuffer([]);

        /**
         * DeviceVerificationResponse clientVersion.
         * @member {string} clientVersion
         * @memberof Authentication.DeviceVerificationResponse
         * @instance
         */
        DeviceVerificationResponse.prototype.clientVersion = "";

        /**
         * DeviceVerificationResponse deviceStatus.
         * @member {Authentication.DeviceStatus} deviceStatus
         * @memberof Authentication.DeviceVerificationResponse
         * @instance
         */
        DeviceVerificationResponse.prototype.deviceStatus = 0;

        /**
         * Creates a new DeviceVerificationResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.DeviceVerificationResponse
         * @static
         * @param {Authentication.IDeviceVerificationResponse=} [properties] Properties to set
         * @returns {Authentication.DeviceVerificationResponse} DeviceVerificationResponse instance
         */
        DeviceVerificationResponse.create = function create(properties) {
            return new DeviceVerificationResponse(properties);
        };

        /**
         * Encodes the specified DeviceVerificationResponse message. Does not implicitly {@link Authentication.DeviceVerificationResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.DeviceVerificationResponse
         * @static
         * @param {Authentication.IDeviceVerificationResponse} message DeviceVerificationResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceVerificationResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedDeviceToken);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.username);
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.messageSessionUid);
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.clientVersion);
            if (message.deviceStatus != null && Object.hasOwnProperty.call(message, "deviceStatus"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.deviceStatus);
            return writer;
        };

        /**
         * Decodes a DeviceVerificationResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.DeviceVerificationResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.DeviceVerificationResponse} DeviceVerificationResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceVerificationResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.DeviceVerificationResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedDeviceToken = reader.bytes();
                        break;
                    }
                case 2: {
                        message.username = reader.string();
                        break;
                    }
                case 3: {
                        message.messageSessionUid = reader.bytes();
                        break;
                    }
                case 4: {
                        message.clientVersion = reader.string();
                        break;
                    }
                case 5: {
                        message.deviceStatus = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a DeviceVerificationResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.DeviceVerificationResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.DeviceVerificationResponse} DeviceVerificationResponse
         */
        DeviceVerificationResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.DeviceVerificationResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.DeviceVerificationResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.DeviceVerificationResponse();
            if (object.encryptedDeviceToken != null)
                if (typeof object.encryptedDeviceToken === "string")
                    $util.base64.decode(object.encryptedDeviceToken, message.encryptedDeviceToken = $util.newBuffer($util.base64.length(object.encryptedDeviceToken)), 0);
                else if (object.encryptedDeviceToken.length >= 0)
                    message.encryptedDeviceToken = object.encryptedDeviceToken;
            if (object.username != null)
                message.username = String(object.username);
            if (object.messageSessionUid != null)
                if (typeof object.messageSessionUid === "string")
                    $util.base64.decode(object.messageSessionUid, message.messageSessionUid = $util.newBuffer($util.base64.length(object.messageSessionUid)), 0);
                else if (object.messageSessionUid.length >= 0)
                    message.messageSessionUid = object.messageSessionUid;
            if (object.clientVersion != null)
                message.clientVersion = String(object.clientVersion);
            switch (object.deviceStatus) {
            default:
                if (typeof object.deviceStatus === "number") {
                    message.deviceStatus = object.deviceStatus;
                    break;
                }
                break;
            case "DEVICE_NEEDS_APPROVAL":
            case 0:
                message.deviceStatus = 0;
                break;
            case "DEVICE_OK":
            case 1:
                message.deviceStatus = 1;
                break;
            case "DEVICE_DISABLED_BY_USER":
            case 2:
                message.deviceStatus = 2;
                break;
            case "DEVICE_LOCKED_BY_ADMIN":
            case 3:
                message.deviceStatus = 3;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a DeviceVerificationResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.DeviceVerificationResponse
         * @static
         * @param {Authentication.DeviceVerificationResponse} message DeviceVerificationResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeviceVerificationResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.encryptedDeviceToken = "";
                else {
                    object.encryptedDeviceToken = [];
                    if (options.bytes !== Array)
                        object.encryptedDeviceToken = $util.newBuffer(object.encryptedDeviceToken);
                }
                object.username = "";
                if (options.bytes === String)
                    object.messageSessionUid = "";
                else {
                    object.messageSessionUid = [];
                    if (options.bytes !== Array)
                        object.messageSessionUid = $util.newBuffer(object.messageSessionUid);
                }
                object.clientVersion = "";
                object.deviceStatus = options.enums === String ? "DEVICE_NEEDS_APPROVAL" : 0;
            }
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                object.encryptedDeviceToken = options.bytes === String ? $util.base64.encode(message.encryptedDeviceToken, 0, message.encryptedDeviceToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDeviceToken) : message.encryptedDeviceToken;
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                object.messageSessionUid = options.bytes === String ? $util.base64.encode(message.messageSessionUid, 0, message.messageSessionUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.messageSessionUid) : message.messageSessionUid;
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                object.clientVersion = message.clientVersion;
            if (message.deviceStatus != null && Object.hasOwnProperty.call(message, "deviceStatus"))
                object.deviceStatus = options.enums === String ? $root.Authentication.DeviceStatus[message.deviceStatus] === undefined ? message.deviceStatus : $root.Authentication.DeviceStatus[message.deviceStatus] : message.deviceStatus;
            return object;
        };

        /**
         * Converts this DeviceVerificationResponse to JSON.
         * @function toJSON
         * @memberof Authentication.DeviceVerificationResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeviceVerificationResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeviceVerificationResponse
         * @function getTypeUrl
         * @memberof Authentication.DeviceVerificationResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeviceVerificationResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.DeviceVerificationResponse";
        };

        return DeviceVerificationResponse;
    })();

    Authentication.DeviceApprovalRequest = (function() {

        /**
         * Properties of a DeviceApprovalRequest.
         * @memberof Authentication
         * @interface IDeviceApprovalRequest
         * @property {string|null} [email] DeviceApprovalRequest email
         * @property {string|null} [twoFactorChannel] DeviceApprovalRequest twoFactorChannel
         * @property {string|null} [clientVersion] DeviceApprovalRequest clientVersion
         * @property {string|null} [locale] DeviceApprovalRequest locale
         * @property {Uint8Array|null} [encryptedDeviceToken] DeviceApprovalRequest encryptedDeviceToken
         * @property {string|null} [totpCode] DeviceApprovalRequest totpCode
         * @property {string|null} [deviceIp] DeviceApprovalRequest deviceIp
         * @property {string|null} [deviceTokenExpireDays] DeviceApprovalRequest deviceTokenExpireDays
         */

        /**
         * Constructs a new DeviceApprovalRequest.
         * @memberof Authentication
         * @classdesc Represents a DeviceApprovalRequest.
         * @implements IDeviceApprovalRequest
         * @constructor
         * @param {Authentication.IDeviceApprovalRequest=} [properties] Properties to set
         */
        function DeviceApprovalRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeviceApprovalRequest email.
         * @member {string} email
         * @memberof Authentication.DeviceApprovalRequest
         * @instance
         */
        DeviceApprovalRequest.prototype.email = "";

        /**
         * DeviceApprovalRequest twoFactorChannel.
         * @member {string} twoFactorChannel
         * @memberof Authentication.DeviceApprovalRequest
         * @instance
         */
        DeviceApprovalRequest.prototype.twoFactorChannel = "";

        /**
         * DeviceApprovalRequest clientVersion.
         * @member {string} clientVersion
         * @memberof Authentication.DeviceApprovalRequest
         * @instance
         */
        DeviceApprovalRequest.prototype.clientVersion = "";

        /**
         * DeviceApprovalRequest locale.
         * @member {string} locale
         * @memberof Authentication.DeviceApprovalRequest
         * @instance
         */
        DeviceApprovalRequest.prototype.locale = "";

        /**
         * DeviceApprovalRequest encryptedDeviceToken.
         * @member {Uint8Array} encryptedDeviceToken
         * @memberof Authentication.DeviceApprovalRequest
         * @instance
         */
        DeviceApprovalRequest.prototype.encryptedDeviceToken = $util.newBuffer([]);

        /**
         * DeviceApprovalRequest totpCode.
         * @member {string} totpCode
         * @memberof Authentication.DeviceApprovalRequest
         * @instance
         */
        DeviceApprovalRequest.prototype.totpCode = "";

        /**
         * DeviceApprovalRequest deviceIp.
         * @member {string} deviceIp
         * @memberof Authentication.DeviceApprovalRequest
         * @instance
         */
        DeviceApprovalRequest.prototype.deviceIp = "";

        /**
         * DeviceApprovalRequest deviceTokenExpireDays.
         * @member {string} deviceTokenExpireDays
         * @memberof Authentication.DeviceApprovalRequest
         * @instance
         */
        DeviceApprovalRequest.prototype.deviceTokenExpireDays = "";

        /**
         * Creates a new DeviceApprovalRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.DeviceApprovalRequest
         * @static
         * @param {Authentication.IDeviceApprovalRequest=} [properties] Properties to set
         * @returns {Authentication.DeviceApprovalRequest} DeviceApprovalRequest instance
         */
        DeviceApprovalRequest.create = function create(properties) {
            return new DeviceApprovalRequest(properties);
        };

        /**
         * Encodes the specified DeviceApprovalRequest message. Does not implicitly {@link Authentication.DeviceApprovalRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.DeviceApprovalRequest
         * @static
         * @param {Authentication.IDeviceApprovalRequest} message DeviceApprovalRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceApprovalRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.email);
            if (message.twoFactorChannel != null && Object.hasOwnProperty.call(message, "twoFactorChannel"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.twoFactorChannel);
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.clientVersion);
            if (message.locale != null && Object.hasOwnProperty.call(message, "locale"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.locale);
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.encryptedDeviceToken);
            if (message.totpCode != null && Object.hasOwnProperty.call(message, "totpCode"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.totpCode);
            if (message.deviceIp != null && Object.hasOwnProperty.call(message, "deviceIp"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.deviceIp);
            if (message.deviceTokenExpireDays != null && Object.hasOwnProperty.call(message, "deviceTokenExpireDays"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.deviceTokenExpireDays);
            return writer;
        };

        /**
         * Decodes a DeviceApprovalRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.DeviceApprovalRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.DeviceApprovalRequest} DeviceApprovalRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceApprovalRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.DeviceApprovalRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.email = reader.string();
                        break;
                    }
                case 2: {
                        message.twoFactorChannel = reader.string();
                        break;
                    }
                case 3: {
                        message.clientVersion = reader.string();
                        break;
                    }
                case 4: {
                        message.locale = reader.string();
                        break;
                    }
                case 5: {
                        message.encryptedDeviceToken = reader.bytes();
                        break;
                    }
                case 6: {
                        message.totpCode = reader.string();
                        break;
                    }
                case 7: {
                        message.deviceIp = reader.string();
                        break;
                    }
                case 8: {
                        message.deviceTokenExpireDays = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a DeviceApprovalRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.DeviceApprovalRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.DeviceApprovalRequest} DeviceApprovalRequest
         */
        DeviceApprovalRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.DeviceApprovalRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.DeviceApprovalRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.DeviceApprovalRequest();
            if (object.email != null)
                message.email = String(object.email);
            if (object.twoFactorChannel != null)
                message.twoFactorChannel = String(object.twoFactorChannel);
            if (object.clientVersion != null)
                message.clientVersion = String(object.clientVersion);
            if (object.locale != null)
                message.locale = String(object.locale);
            if (object.encryptedDeviceToken != null)
                if (typeof object.encryptedDeviceToken === "string")
                    $util.base64.decode(object.encryptedDeviceToken, message.encryptedDeviceToken = $util.newBuffer($util.base64.length(object.encryptedDeviceToken)), 0);
                else if (object.encryptedDeviceToken.length >= 0)
                    message.encryptedDeviceToken = object.encryptedDeviceToken;
            if (object.totpCode != null)
                message.totpCode = String(object.totpCode);
            if (object.deviceIp != null)
                message.deviceIp = String(object.deviceIp);
            if (object.deviceTokenExpireDays != null)
                message.deviceTokenExpireDays = String(object.deviceTokenExpireDays);
            return message;
        };

        /**
         * Creates a plain object from a DeviceApprovalRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.DeviceApprovalRequest
         * @static
         * @param {Authentication.DeviceApprovalRequest} message DeviceApprovalRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeviceApprovalRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.email = "";
                object.twoFactorChannel = "";
                object.clientVersion = "";
                object.locale = "";
                if (options.bytes === String)
                    object.encryptedDeviceToken = "";
                else {
                    object.encryptedDeviceToken = [];
                    if (options.bytes !== Array)
                        object.encryptedDeviceToken = $util.newBuffer(object.encryptedDeviceToken);
                }
                object.totpCode = "";
                object.deviceIp = "";
                object.deviceTokenExpireDays = "";
            }
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                object.email = message.email;
            if (message.twoFactorChannel != null && Object.hasOwnProperty.call(message, "twoFactorChannel"))
                object.twoFactorChannel = message.twoFactorChannel;
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                object.clientVersion = message.clientVersion;
            if (message.locale != null && Object.hasOwnProperty.call(message, "locale"))
                object.locale = message.locale;
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                object.encryptedDeviceToken = options.bytes === String ? $util.base64.encode(message.encryptedDeviceToken, 0, message.encryptedDeviceToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDeviceToken) : message.encryptedDeviceToken;
            if (message.totpCode != null && Object.hasOwnProperty.call(message, "totpCode"))
                object.totpCode = message.totpCode;
            if (message.deviceIp != null && Object.hasOwnProperty.call(message, "deviceIp"))
                object.deviceIp = message.deviceIp;
            if (message.deviceTokenExpireDays != null && Object.hasOwnProperty.call(message, "deviceTokenExpireDays"))
                object.deviceTokenExpireDays = message.deviceTokenExpireDays;
            return object;
        };

        /**
         * Converts this DeviceApprovalRequest to JSON.
         * @function toJSON
         * @memberof Authentication.DeviceApprovalRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeviceApprovalRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeviceApprovalRequest
         * @function getTypeUrl
         * @memberof Authentication.DeviceApprovalRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeviceApprovalRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.DeviceApprovalRequest";
        };

        return DeviceApprovalRequest;
    })();

    Authentication.DeviceApprovalResponse = (function() {

        /**
         * Properties of a DeviceApprovalResponse.
         * @memberof Authentication
         * @interface IDeviceApprovalResponse
         * @property {Uint8Array|null} [encryptedTwoFactorToken] DeviceApprovalResponse encryptedTwoFactorToken
         */

        /**
         * Constructs a new DeviceApprovalResponse.
         * @memberof Authentication
         * @classdesc Represents a DeviceApprovalResponse.
         * @implements IDeviceApprovalResponse
         * @constructor
         * @param {Authentication.IDeviceApprovalResponse=} [properties] Properties to set
         */
        function DeviceApprovalResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeviceApprovalResponse encryptedTwoFactorToken.
         * @member {Uint8Array} encryptedTwoFactorToken
         * @memberof Authentication.DeviceApprovalResponse
         * @instance
         */
        DeviceApprovalResponse.prototype.encryptedTwoFactorToken = $util.newBuffer([]);

        /**
         * Creates a new DeviceApprovalResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.DeviceApprovalResponse
         * @static
         * @param {Authentication.IDeviceApprovalResponse=} [properties] Properties to set
         * @returns {Authentication.DeviceApprovalResponse} DeviceApprovalResponse instance
         */
        DeviceApprovalResponse.create = function create(properties) {
            return new DeviceApprovalResponse(properties);
        };

        /**
         * Encodes the specified DeviceApprovalResponse message. Does not implicitly {@link Authentication.DeviceApprovalResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.DeviceApprovalResponse
         * @static
         * @param {Authentication.IDeviceApprovalResponse} message DeviceApprovalResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceApprovalResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedTwoFactorToken != null && Object.hasOwnProperty.call(message, "encryptedTwoFactorToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedTwoFactorToken);
            return writer;
        };

        /**
         * Decodes a DeviceApprovalResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.DeviceApprovalResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.DeviceApprovalResponse} DeviceApprovalResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceApprovalResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.DeviceApprovalResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedTwoFactorToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a DeviceApprovalResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.DeviceApprovalResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.DeviceApprovalResponse} DeviceApprovalResponse
         */
        DeviceApprovalResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.DeviceApprovalResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.DeviceApprovalResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.DeviceApprovalResponse();
            if (object.encryptedTwoFactorToken != null)
                if (typeof object.encryptedTwoFactorToken === "string")
                    $util.base64.decode(object.encryptedTwoFactorToken, message.encryptedTwoFactorToken = $util.newBuffer($util.base64.length(object.encryptedTwoFactorToken)), 0);
                else if (object.encryptedTwoFactorToken.length >= 0)
                    message.encryptedTwoFactorToken = object.encryptedTwoFactorToken;
            return message;
        };

        /**
         * Creates a plain object from a DeviceApprovalResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.DeviceApprovalResponse
         * @static
         * @param {Authentication.DeviceApprovalResponse} message DeviceApprovalResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeviceApprovalResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === String)
                    object.encryptedTwoFactorToken = "";
                else {
                    object.encryptedTwoFactorToken = [];
                    if (options.bytes !== Array)
                        object.encryptedTwoFactorToken = $util.newBuffer(object.encryptedTwoFactorToken);
                }
            if (message.encryptedTwoFactorToken != null && Object.hasOwnProperty.call(message, "encryptedTwoFactorToken"))
                object.encryptedTwoFactorToken = options.bytes === String ? $util.base64.encode(message.encryptedTwoFactorToken, 0, message.encryptedTwoFactorToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedTwoFactorToken) : message.encryptedTwoFactorToken;
            return object;
        };

        /**
         * Converts this DeviceApprovalResponse to JSON.
         * @function toJSON
         * @memberof Authentication.DeviceApprovalResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeviceApprovalResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeviceApprovalResponse
         * @function getTypeUrl
         * @memberof Authentication.DeviceApprovalResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeviceApprovalResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.DeviceApprovalResponse";
        };

        return DeviceApprovalResponse;
    })();

    Authentication.ApproveDeviceRequest = (function() {

        /**
         * Properties of an ApproveDeviceRequest.
         * @memberof Authentication
         * @interface IApproveDeviceRequest
         * @property {Uint8Array|null} [encryptedDeviceToken] ApproveDeviceRequest encryptedDeviceToken
         * @property {Uint8Array|null} [encryptedDeviceDataKey] ApproveDeviceRequest encryptedDeviceDataKey
         * @property {boolean|null} [denyApproval] ApproveDeviceRequest denyApproval
         * @property {boolean|null} [linkDevice] ApproveDeviceRequest linkDevice
         */

        /**
         * Constructs a new ApproveDeviceRequest.
         * @memberof Authentication
         * @classdesc Represents an ApproveDeviceRequest.
         * @implements IApproveDeviceRequest
         * @constructor
         * @param {Authentication.IApproveDeviceRequest=} [properties] Properties to set
         */
        function ApproveDeviceRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ApproveDeviceRequest encryptedDeviceToken.
         * @member {Uint8Array} encryptedDeviceToken
         * @memberof Authentication.ApproveDeviceRequest
         * @instance
         */
        ApproveDeviceRequest.prototype.encryptedDeviceToken = $util.newBuffer([]);

        /**
         * ApproveDeviceRequest encryptedDeviceDataKey.
         * @member {Uint8Array} encryptedDeviceDataKey
         * @memberof Authentication.ApproveDeviceRequest
         * @instance
         */
        ApproveDeviceRequest.prototype.encryptedDeviceDataKey = $util.newBuffer([]);

        /**
         * ApproveDeviceRequest denyApproval.
         * @member {boolean} denyApproval
         * @memberof Authentication.ApproveDeviceRequest
         * @instance
         */
        ApproveDeviceRequest.prototype.denyApproval = false;

        /**
         * ApproveDeviceRequest linkDevice.
         * @member {boolean} linkDevice
         * @memberof Authentication.ApproveDeviceRequest
         * @instance
         */
        ApproveDeviceRequest.prototype.linkDevice = false;

        /**
         * Creates a new ApproveDeviceRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.ApproveDeviceRequest
         * @static
         * @param {Authentication.IApproveDeviceRequest=} [properties] Properties to set
         * @returns {Authentication.ApproveDeviceRequest} ApproveDeviceRequest instance
         */
        ApproveDeviceRequest.create = function create(properties) {
            return new ApproveDeviceRequest(properties);
        };

        /**
         * Encodes the specified ApproveDeviceRequest message. Does not implicitly {@link Authentication.ApproveDeviceRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ApproveDeviceRequest
         * @static
         * @param {Authentication.IApproveDeviceRequest} message ApproveDeviceRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ApproveDeviceRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedDeviceToken);
            if (message.encryptedDeviceDataKey != null && Object.hasOwnProperty.call(message, "encryptedDeviceDataKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encryptedDeviceDataKey);
            if (message.denyApproval != null && Object.hasOwnProperty.call(message, "denyApproval"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.denyApproval);
            if (message.linkDevice != null && Object.hasOwnProperty.call(message, "linkDevice"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.linkDevice);
            return writer;
        };

        /**
         * Decodes an ApproveDeviceRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ApproveDeviceRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ApproveDeviceRequest} ApproveDeviceRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ApproveDeviceRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ApproveDeviceRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedDeviceToken = reader.bytes();
                        break;
                    }
                case 2: {
                        message.encryptedDeviceDataKey = reader.bytes();
                        break;
                    }
                case 3: {
                        message.denyApproval = reader.bool();
                        break;
                    }
                case 4: {
                        message.linkDevice = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an ApproveDeviceRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ApproveDeviceRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ApproveDeviceRequest} ApproveDeviceRequest
         */
        ApproveDeviceRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ApproveDeviceRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ApproveDeviceRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ApproveDeviceRequest();
            if (object.encryptedDeviceToken != null)
                if (typeof object.encryptedDeviceToken === "string")
                    $util.base64.decode(object.encryptedDeviceToken, message.encryptedDeviceToken = $util.newBuffer($util.base64.length(object.encryptedDeviceToken)), 0);
                else if (object.encryptedDeviceToken.length >= 0)
                    message.encryptedDeviceToken = object.encryptedDeviceToken;
            if (object.encryptedDeviceDataKey != null)
                if (typeof object.encryptedDeviceDataKey === "string")
                    $util.base64.decode(object.encryptedDeviceDataKey, message.encryptedDeviceDataKey = $util.newBuffer($util.base64.length(object.encryptedDeviceDataKey)), 0);
                else if (object.encryptedDeviceDataKey.length >= 0)
                    message.encryptedDeviceDataKey = object.encryptedDeviceDataKey;
            if (object.denyApproval != null)
                message.denyApproval = Boolean(object.denyApproval);
            if (object.linkDevice != null)
                message.linkDevice = Boolean(object.linkDevice);
            return message;
        };

        /**
         * Creates a plain object from an ApproveDeviceRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ApproveDeviceRequest
         * @static
         * @param {Authentication.ApproveDeviceRequest} message ApproveDeviceRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ApproveDeviceRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.encryptedDeviceToken = "";
                else {
                    object.encryptedDeviceToken = [];
                    if (options.bytes !== Array)
                        object.encryptedDeviceToken = $util.newBuffer(object.encryptedDeviceToken);
                }
                if (options.bytes === String)
                    object.encryptedDeviceDataKey = "";
                else {
                    object.encryptedDeviceDataKey = [];
                    if (options.bytes !== Array)
                        object.encryptedDeviceDataKey = $util.newBuffer(object.encryptedDeviceDataKey);
                }
                object.denyApproval = false;
                object.linkDevice = false;
            }
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                object.encryptedDeviceToken = options.bytes === String ? $util.base64.encode(message.encryptedDeviceToken, 0, message.encryptedDeviceToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDeviceToken) : message.encryptedDeviceToken;
            if (message.encryptedDeviceDataKey != null && Object.hasOwnProperty.call(message, "encryptedDeviceDataKey"))
                object.encryptedDeviceDataKey = options.bytes === String ? $util.base64.encode(message.encryptedDeviceDataKey, 0, message.encryptedDeviceDataKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDeviceDataKey) : message.encryptedDeviceDataKey;
            if (message.denyApproval != null && Object.hasOwnProperty.call(message, "denyApproval"))
                object.denyApproval = message.denyApproval;
            if (message.linkDevice != null && Object.hasOwnProperty.call(message, "linkDevice"))
                object.linkDevice = message.linkDevice;
            return object;
        };

        /**
         * Converts this ApproveDeviceRequest to JSON.
         * @function toJSON
         * @memberof Authentication.ApproveDeviceRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ApproveDeviceRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ApproveDeviceRequest
         * @function getTypeUrl
         * @memberof Authentication.ApproveDeviceRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ApproveDeviceRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ApproveDeviceRequest";
        };

        return ApproveDeviceRequest;
    })();

    Authentication.EnterpriseUserAliasRequest = (function() {

        /**
         * Properties of an EnterpriseUserAliasRequest.
         * @memberof Authentication
         * @interface IEnterpriseUserAliasRequest
         * @property {number|null} [enterpriseUserId] EnterpriseUserAliasRequest enterpriseUserId
         * @property {string|null} [alias] EnterpriseUserAliasRequest alias
         */

        /**
         * Constructs a new EnterpriseUserAliasRequest.
         * @memberof Authentication
         * @classdesc Represents an EnterpriseUserAliasRequest.
         * @implements IEnterpriseUserAliasRequest
         * @constructor
         * @param {Authentication.IEnterpriseUserAliasRequest=} [properties] Properties to set
         */
        function EnterpriseUserAliasRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EnterpriseUserAliasRequest enterpriseUserId.
         * @member {number} enterpriseUserId
         * @memberof Authentication.EnterpriseUserAliasRequest
         * @instance
         */
        EnterpriseUserAliasRequest.prototype.enterpriseUserId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EnterpriseUserAliasRequest alias.
         * @member {string} alias
         * @memberof Authentication.EnterpriseUserAliasRequest
         * @instance
         */
        EnterpriseUserAliasRequest.prototype.alias = "";

        /**
         * Creates a new EnterpriseUserAliasRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.EnterpriseUserAliasRequest
         * @static
         * @param {Authentication.IEnterpriseUserAliasRequest=} [properties] Properties to set
         * @returns {Authentication.EnterpriseUserAliasRequest} EnterpriseUserAliasRequest instance
         */
        EnterpriseUserAliasRequest.create = function create(properties) {
            return new EnterpriseUserAliasRequest(properties);
        };

        /**
         * Encodes the specified EnterpriseUserAliasRequest message. Does not implicitly {@link Authentication.EnterpriseUserAliasRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.EnterpriseUserAliasRequest
         * @static
         * @param {Authentication.IEnterpriseUserAliasRequest} message EnterpriseUserAliasRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterpriseUserAliasRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.enterpriseUserId);
            if (message.alias != null && Object.hasOwnProperty.call(message, "alias"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.alias);
            return writer;
        };

        /**
         * Decodes an EnterpriseUserAliasRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.EnterpriseUserAliasRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.EnterpriseUserAliasRequest} EnterpriseUserAliasRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterpriseUserAliasRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.EnterpriseUserAliasRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.enterpriseUserId = reader.int64();
                        break;
                    }
                case 2: {
                        message.alias = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an EnterpriseUserAliasRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.EnterpriseUserAliasRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.EnterpriseUserAliasRequest} EnterpriseUserAliasRequest
         */
        EnterpriseUserAliasRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.EnterpriseUserAliasRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.EnterpriseUserAliasRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.EnterpriseUserAliasRequest();
            if (object.enterpriseUserId != null)
                if ($util.Long)
                    message.enterpriseUserId = $util.Long.fromValue(object.enterpriseUserId, false);
                else if (typeof object.enterpriseUserId === "string")
                    message.enterpriseUserId = parseInt(object.enterpriseUserId, 10);
                else if (typeof object.enterpriseUserId === "number")
                    message.enterpriseUserId = object.enterpriseUserId;
                else if (typeof object.enterpriseUserId === "object")
                    message.enterpriseUserId = new $util.LongBits(object.enterpriseUserId.low >>> 0, object.enterpriseUserId.high >>> 0).toNumber();
            if (object.alias != null)
                message.alias = String(object.alias);
            return message;
        };

        /**
         * Creates a plain object from an EnterpriseUserAliasRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.EnterpriseUserAliasRequest
         * @static
         * @param {Authentication.EnterpriseUserAliasRequest} message EnterpriseUserAliasRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EnterpriseUserAliasRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.enterpriseUserId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.enterpriseUserId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.alias = "";
            }
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.enterpriseUserId = typeof message.enterpriseUserId === "number" ? BigInt(message.enterpriseUserId) : $util.Long.fromBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0, false).toBigInt();
                else if (typeof message.enterpriseUserId === "number")
                    object.enterpriseUserId = options.longs === String ? String(message.enterpriseUserId) : message.enterpriseUserId;
                else
                    object.enterpriseUserId = options.longs === String ? $util.Long.prototype.toString.call(message.enterpriseUserId) : options.longs === Number ? new $util.LongBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0).toNumber() : message.enterpriseUserId;
            if (message.alias != null && Object.hasOwnProperty.call(message, "alias"))
                object.alias = message.alias;
            return object;
        };

        /**
         * Converts this EnterpriseUserAliasRequest to JSON.
         * @function toJSON
         * @memberof Authentication.EnterpriseUserAliasRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EnterpriseUserAliasRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EnterpriseUserAliasRequest
         * @function getTypeUrl
         * @memberof Authentication.EnterpriseUserAliasRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EnterpriseUserAliasRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.EnterpriseUserAliasRequest";
        };

        return EnterpriseUserAliasRequest;
    })();

    Authentication.EnterpriseUserAddAliasRequest = (function() {

        /**
         * Properties of an EnterpriseUserAddAliasRequest.
         * @memberof Authentication
         * @interface IEnterpriseUserAddAliasRequest
         * @property {number|null} [enterpriseUserId] EnterpriseUserAddAliasRequest enterpriseUserId
         * @property {string|null} [alias] EnterpriseUserAddAliasRequest alias
         * @property {boolean|null} [primary] EnterpriseUserAddAliasRequest primary
         */

        /**
         * Constructs a new EnterpriseUserAddAliasRequest.
         * @memberof Authentication
         * @classdesc Represents an EnterpriseUserAddAliasRequest.
         * @implements IEnterpriseUserAddAliasRequest
         * @constructor
         * @param {Authentication.IEnterpriseUserAddAliasRequest=} [properties] Properties to set
         */
        function EnterpriseUserAddAliasRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EnterpriseUserAddAliasRequest enterpriseUserId.
         * @member {number} enterpriseUserId
         * @memberof Authentication.EnterpriseUserAddAliasRequest
         * @instance
         */
        EnterpriseUserAddAliasRequest.prototype.enterpriseUserId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EnterpriseUserAddAliasRequest alias.
         * @member {string} alias
         * @memberof Authentication.EnterpriseUserAddAliasRequest
         * @instance
         */
        EnterpriseUserAddAliasRequest.prototype.alias = "";

        /**
         * EnterpriseUserAddAliasRequest primary.
         * @member {boolean} primary
         * @memberof Authentication.EnterpriseUserAddAliasRequest
         * @instance
         */
        EnterpriseUserAddAliasRequest.prototype.primary = false;

        /**
         * Creates a new EnterpriseUserAddAliasRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.EnterpriseUserAddAliasRequest
         * @static
         * @param {Authentication.IEnterpriseUserAddAliasRequest=} [properties] Properties to set
         * @returns {Authentication.EnterpriseUserAddAliasRequest} EnterpriseUserAddAliasRequest instance
         */
        EnterpriseUserAddAliasRequest.create = function create(properties) {
            return new EnterpriseUserAddAliasRequest(properties);
        };

        /**
         * Encodes the specified EnterpriseUserAddAliasRequest message. Does not implicitly {@link Authentication.EnterpriseUserAddAliasRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.EnterpriseUserAddAliasRequest
         * @static
         * @param {Authentication.IEnterpriseUserAddAliasRequest} message EnterpriseUserAddAliasRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterpriseUserAddAliasRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.enterpriseUserId);
            if (message.alias != null && Object.hasOwnProperty.call(message, "alias"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.alias);
            if (message.primary != null && Object.hasOwnProperty.call(message, "primary"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.primary);
            return writer;
        };

        /**
         * Decodes an EnterpriseUserAddAliasRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.EnterpriseUserAddAliasRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.EnterpriseUserAddAliasRequest} EnterpriseUserAddAliasRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterpriseUserAddAliasRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.EnterpriseUserAddAliasRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.enterpriseUserId = reader.int64();
                        break;
                    }
                case 2: {
                        message.alias = reader.string();
                        break;
                    }
                case 3: {
                        message.primary = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an EnterpriseUserAddAliasRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.EnterpriseUserAddAliasRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.EnterpriseUserAddAliasRequest} EnterpriseUserAddAliasRequest
         */
        EnterpriseUserAddAliasRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.EnterpriseUserAddAliasRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.EnterpriseUserAddAliasRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.EnterpriseUserAddAliasRequest();
            if (object.enterpriseUserId != null)
                if ($util.Long)
                    message.enterpriseUserId = $util.Long.fromValue(object.enterpriseUserId, false);
                else if (typeof object.enterpriseUserId === "string")
                    message.enterpriseUserId = parseInt(object.enterpriseUserId, 10);
                else if (typeof object.enterpriseUserId === "number")
                    message.enterpriseUserId = object.enterpriseUserId;
                else if (typeof object.enterpriseUserId === "object")
                    message.enterpriseUserId = new $util.LongBits(object.enterpriseUserId.low >>> 0, object.enterpriseUserId.high >>> 0).toNumber();
            if (object.alias != null)
                message.alias = String(object.alias);
            if (object.primary != null)
                message.primary = Boolean(object.primary);
            return message;
        };

        /**
         * Creates a plain object from an EnterpriseUserAddAliasRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.EnterpriseUserAddAliasRequest
         * @static
         * @param {Authentication.EnterpriseUserAddAliasRequest} message EnterpriseUserAddAliasRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EnterpriseUserAddAliasRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.enterpriseUserId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.enterpriseUserId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.alias = "";
                object.primary = false;
            }
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.enterpriseUserId = typeof message.enterpriseUserId === "number" ? BigInt(message.enterpriseUserId) : $util.Long.fromBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0, false).toBigInt();
                else if (typeof message.enterpriseUserId === "number")
                    object.enterpriseUserId = options.longs === String ? String(message.enterpriseUserId) : message.enterpriseUserId;
                else
                    object.enterpriseUserId = options.longs === String ? $util.Long.prototype.toString.call(message.enterpriseUserId) : options.longs === Number ? new $util.LongBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0).toNumber() : message.enterpriseUserId;
            if (message.alias != null && Object.hasOwnProperty.call(message, "alias"))
                object.alias = message.alias;
            if (message.primary != null && Object.hasOwnProperty.call(message, "primary"))
                object.primary = message.primary;
            return object;
        };

        /**
         * Converts this EnterpriseUserAddAliasRequest to JSON.
         * @function toJSON
         * @memberof Authentication.EnterpriseUserAddAliasRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EnterpriseUserAddAliasRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EnterpriseUserAddAliasRequest
         * @function getTypeUrl
         * @memberof Authentication.EnterpriseUserAddAliasRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EnterpriseUserAddAliasRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.EnterpriseUserAddAliasRequest";
        };

        return EnterpriseUserAddAliasRequest;
    })();

    Authentication.EnterpriseUserAddAliasRequestV2 = (function() {

        /**
         * Properties of an EnterpriseUserAddAliasRequestV2.
         * @memberof Authentication
         * @interface IEnterpriseUserAddAliasRequestV2
         * @property {Array.<Authentication.IEnterpriseUserAddAliasRequest>|null} [enterpriseUserAddAliasRequest] EnterpriseUserAddAliasRequestV2 enterpriseUserAddAliasRequest
         */

        /**
         * Constructs a new EnterpriseUserAddAliasRequestV2.
         * @memberof Authentication
         * @classdesc Represents an EnterpriseUserAddAliasRequestV2.
         * @implements IEnterpriseUserAddAliasRequestV2
         * @constructor
         * @param {Authentication.IEnterpriseUserAddAliasRequestV2=} [properties] Properties to set
         */
        function EnterpriseUserAddAliasRequestV2(properties) {
            this.enterpriseUserAddAliasRequest = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EnterpriseUserAddAliasRequestV2 enterpriseUserAddAliasRequest.
         * @member {Array.<Authentication.IEnterpriseUserAddAliasRequest>} enterpriseUserAddAliasRequest
         * @memberof Authentication.EnterpriseUserAddAliasRequestV2
         * @instance
         */
        EnterpriseUserAddAliasRequestV2.prototype.enterpriseUserAddAliasRequest = $util.emptyArray;

        /**
         * Creates a new EnterpriseUserAddAliasRequestV2 instance using the specified properties.
         * @function create
         * @memberof Authentication.EnterpriseUserAddAliasRequestV2
         * @static
         * @param {Authentication.IEnterpriseUserAddAliasRequestV2=} [properties] Properties to set
         * @returns {Authentication.EnterpriseUserAddAliasRequestV2} EnterpriseUserAddAliasRequestV2 instance
         */
        EnterpriseUserAddAliasRequestV2.create = function create(properties) {
            return new EnterpriseUserAddAliasRequestV2(properties);
        };

        /**
         * Encodes the specified EnterpriseUserAddAliasRequestV2 message. Does not implicitly {@link Authentication.EnterpriseUserAddAliasRequestV2.verify|verify} messages.
         * @function encode
         * @memberof Authentication.EnterpriseUserAddAliasRequestV2
         * @static
         * @param {Authentication.IEnterpriseUserAddAliasRequestV2} message EnterpriseUserAddAliasRequestV2 message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterpriseUserAddAliasRequestV2.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.enterpriseUserAddAliasRequest != null && message.enterpriseUserAddAliasRequest.length)
                for (let i = 0; i < message.enterpriseUserAddAliasRequest.length; ++i)
                    $root.Authentication.EnterpriseUserAddAliasRequest.encode(message.enterpriseUserAddAliasRequest[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes an EnterpriseUserAddAliasRequestV2 message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.EnterpriseUserAddAliasRequestV2
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.EnterpriseUserAddAliasRequestV2} EnterpriseUserAddAliasRequestV2
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterpriseUserAddAliasRequestV2.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.EnterpriseUserAddAliasRequestV2();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.enterpriseUserAddAliasRequest && message.enterpriseUserAddAliasRequest.length))
                            message.enterpriseUserAddAliasRequest = [];
                        message.enterpriseUserAddAliasRequest.push($root.Authentication.EnterpriseUserAddAliasRequest.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an EnterpriseUserAddAliasRequestV2 message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.EnterpriseUserAddAliasRequestV2
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.EnterpriseUserAddAliasRequestV2} EnterpriseUserAddAliasRequestV2
         */
        EnterpriseUserAddAliasRequestV2.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.EnterpriseUserAddAliasRequestV2)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.EnterpriseUserAddAliasRequestV2: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.EnterpriseUserAddAliasRequestV2();
            if (object.enterpriseUserAddAliasRequest) {
                if (!Array.isArray(object.enterpriseUserAddAliasRequest))
                    throw TypeError(".Authentication.EnterpriseUserAddAliasRequestV2.enterpriseUserAddAliasRequest: array expected");
                message.enterpriseUserAddAliasRequest = [];
                for (let i = 0; i < object.enterpriseUserAddAliasRequest.length; ++i) {
                    if (!$util.isObject(object.enterpriseUserAddAliasRequest[i]))
                        throw TypeError(".Authentication.EnterpriseUserAddAliasRequestV2.enterpriseUserAddAliasRequest: object expected");
                    message.enterpriseUserAddAliasRequest[i] = $root.Authentication.EnterpriseUserAddAliasRequest.fromObject(object.enterpriseUserAddAliasRequest[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an EnterpriseUserAddAliasRequestV2 message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.EnterpriseUserAddAliasRequestV2
         * @static
         * @param {Authentication.EnterpriseUserAddAliasRequestV2} message EnterpriseUserAddAliasRequestV2
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EnterpriseUserAddAliasRequestV2.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.enterpriseUserAddAliasRequest = [];
            if (message.enterpriseUserAddAliasRequest && message.enterpriseUserAddAliasRequest.length) {
                object.enterpriseUserAddAliasRequest = [];
                for (let j = 0; j < message.enterpriseUserAddAliasRequest.length; ++j)
                    object.enterpriseUserAddAliasRequest[j] = $root.Authentication.EnterpriseUserAddAliasRequest.toObject(message.enterpriseUserAddAliasRequest[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this EnterpriseUserAddAliasRequestV2 to JSON.
         * @function toJSON
         * @memberof Authentication.EnterpriseUserAddAliasRequestV2
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EnterpriseUserAddAliasRequestV2.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EnterpriseUserAddAliasRequestV2
         * @function getTypeUrl
         * @memberof Authentication.EnterpriseUserAddAliasRequestV2
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EnterpriseUserAddAliasRequestV2.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.EnterpriseUserAddAliasRequestV2";
        };

        return EnterpriseUserAddAliasRequestV2;
    })();

    Authentication.EnterpriseUserAddAliasStatus = (function() {

        /**
         * Properties of an EnterpriseUserAddAliasStatus.
         * @memberof Authentication
         * @interface IEnterpriseUserAddAliasStatus
         * @property {number|null} [enterpriseUserId] EnterpriseUserAddAliasStatus enterpriseUserId
         * @property {string|null} [status] EnterpriseUserAddAliasStatus status
         */

        /**
         * Constructs a new EnterpriseUserAddAliasStatus.
         * @memberof Authentication
         * @classdesc Represents an EnterpriseUserAddAliasStatus.
         * @implements IEnterpriseUserAddAliasStatus
         * @constructor
         * @param {Authentication.IEnterpriseUserAddAliasStatus=} [properties] Properties to set
         */
        function EnterpriseUserAddAliasStatus(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EnterpriseUserAddAliasStatus enterpriseUserId.
         * @member {number} enterpriseUserId
         * @memberof Authentication.EnterpriseUserAddAliasStatus
         * @instance
         */
        EnterpriseUserAddAliasStatus.prototype.enterpriseUserId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EnterpriseUserAddAliasStatus status.
         * @member {string} status
         * @memberof Authentication.EnterpriseUserAddAliasStatus
         * @instance
         */
        EnterpriseUserAddAliasStatus.prototype.status = "";

        /**
         * Creates a new EnterpriseUserAddAliasStatus instance using the specified properties.
         * @function create
         * @memberof Authentication.EnterpriseUserAddAliasStatus
         * @static
         * @param {Authentication.IEnterpriseUserAddAliasStatus=} [properties] Properties to set
         * @returns {Authentication.EnterpriseUserAddAliasStatus} EnterpriseUserAddAliasStatus instance
         */
        EnterpriseUserAddAliasStatus.create = function create(properties) {
            return new EnterpriseUserAddAliasStatus(properties);
        };

        /**
         * Encodes the specified EnterpriseUserAddAliasStatus message. Does not implicitly {@link Authentication.EnterpriseUserAddAliasStatus.verify|verify} messages.
         * @function encode
         * @memberof Authentication.EnterpriseUserAddAliasStatus
         * @static
         * @param {Authentication.IEnterpriseUserAddAliasStatus} message EnterpriseUserAddAliasStatus message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterpriseUserAddAliasStatus.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.enterpriseUserId);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.status);
            return writer;
        };

        /**
         * Decodes an EnterpriseUserAddAliasStatus message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.EnterpriseUserAddAliasStatus
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.EnterpriseUserAddAliasStatus} EnterpriseUserAddAliasStatus
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterpriseUserAddAliasStatus.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.EnterpriseUserAddAliasStatus();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.enterpriseUserId = reader.int64();
                        break;
                    }
                case 2: {
                        message.status = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an EnterpriseUserAddAliasStatus message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.EnterpriseUserAddAliasStatus
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.EnterpriseUserAddAliasStatus} EnterpriseUserAddAliasStatus
         */
        EnterpriseUserAddAliasStatus.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.EnterpriseUserAddAliasStatus)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.EnterpriseUserAddAliasStatus: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.EnterpriseUserAddAliasStatus();
            if (object.enterpriseUserId != null)
                if ($util.Long)
                    message.enterpriseUserId = $util.Long.fromValue(object.enterpriseUserId, false);
                else if (typeof object.enterpriseUserId === "string")
                    message.enterpriseUserId = parseInt(object.enterpriseUserId, 10);
                else if (typeof object.enterpriseUserId === "number")
                    message.enterpriseUserId = object.enterpriseUserId;
                else if (typeof object.enterpriseUserId === "object")
                    message.enterpriseUserId = new $util.LongBits(object.enterpriseUserId.low >>> 0, object.enterpriseUserId.high >>> 0).toNumber();
            if (object.status != null)
                message.status = String(object.status);
            return message;
        };

        /**
         * Creates a plain object from an EnterpriseUserAddAliasStatus message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.EnterpriseUserAddAliasStatus
         * @static
         * @param {Authentication.EnterpriseUserAddAliasStatus} message EnterpriseUserAddAliasStatus
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EnterpriseUserAddAliasStatus.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.enterpriseUserId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.enterpriseUserId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.status = "";
            }
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.enterpriseUserId = typeof message.enterpriseUserId === "number" ? BigInt(message.enterpriseUserId) : $util.Long.fromBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0, false).toBigInt();
                else if (typeof message.enterpriseUserId === "number")
                    object.enterpriseUserId = options.longs === String ? String(message.enterpriseUserId) : message.enterpriseUserId;
                else
                    object.enterpriseUserId = options.longs === String ? $util.Long.prototype.toString.call(message.enterpriseUserId) : options.longs === Number ? new $util.LongBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0).toNumber() : message.enterpriseUserId;
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                object.status = message.status;
            return object;
        };

        /**
         * Converts this EnterpriseUserAddAliasStatus to JSON.
         * @function toJSON
         * @memberof Authentication.EnterpriseUserAddAliasStatus
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EnterpriseUserAddAliasStatus.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EnterpriseUserAddAliasStatus
         * @function getTypeUrl
         * @memberof Authentication.EnterpriseUserAddAliasStatus
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EnterpriseUserAddAliasStatus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.EnterpriseUserAddAliasStatus";
        };

        return EnterpriseUserAddAliasStatus;
    })();

    Authentication.EnterpriseUserAddAliasResponse = (function() {

        /**
         * Properties of an EnterpriseUserAddAliasResponse.
         * @memberof Authentication
         * @interface IEnterpriseUserAddAliasResponse
         * @property {Array.<Authentication.IEnterpriseUserAddAliasStatus>|null} [status] EnterpriseUserAddAliasResponse status
         */

        /**
         * Constructs a new EnterpriseUserAddAliasResponse.
         * @memberof Authentication
         * @classdesc Represents an EnterpriseUserAddAliasResponse.
         * @implements IEnterpriseUserAddAliasResponse
         * @constructor
         * @param {Authentication.IEnterpriseUserAddAliasResponse=} [properties] Properties to set
         */
        function EnterpriseUserAddAliasResponse(properties) {
            this.status = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EnterpriseUserAddAliasResponse status.
         * @member {Array.<Authentication.IEnterpriseUserAddAliasStatus>} status
         * @memberof Authentication.EnterpriseUserAddAliasResponse
         * @instance
         */
        EnterpriseUserAddAliasResponse.prototype.status = $util.emptyArray;

        /**
         * Creates a new EnterpriseUserAddAliasResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.EnterpriseUserAddAliasResponse
         * @static
         * @param {Authentication.IEnterpriseUserAddAliasResponse=} [properties] Properties to set
         * @returns {Authentication.EnterpriseUserAddAliasResponse} EnterpriseUserAddAliasResponse instance
         */
        EnterpriseUserAddAliasResponse.create = function create(properties) {
            return new EnterpriseUserAddAliasResponse(properties);
        };

        /**
         * Encodes the specified EnterpriseUserAddAliasResponse message. Does not implicitly {@link Authentication.EnterpriseUserAddAliasResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.EnterpriseUserAddAliasResponse
         * @static
         * @param {Authentication.IEnterpriseUserAddAliasResponse} message EnterpriseUserAddAliasResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterpriseUserAddAliasResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.status != null && message.status.length)
                for (let i = 0; i < message.status.length; ++i)
                    $root.Authentication.EnterpriseUserAddAliasStatus.encode(message.status[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes an EnterpriseUserAddAliasResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.EnterpriseUserAddAliasResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.EnterpriseUserAddAliasResponse} EnterpriseUserAddAliasResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterpriseUserAddAliasResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.EnterpriseUserAddAliasResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.status && message.status.length))
                            message.status = [];
                        message.status.push($root.Authentication.EnterpriseUserAddAliasStatus.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an EnterpriseUserAddAliasResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.EnterpriseUserAddAliasResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.EnterpriseUserAddAliasResponse} EnterpriseUserAddAliasResponse
         */
        EnterpriseUserAddAliasResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.EnterpriseUserAddAliasResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.EnterpriseUserAddAliasResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.EnterpriseUserAddAliasResponse();
            if (object.status) {
                if (!Array.isArray(object.status))
                    throw TypeError(".Authentication.EnterpriseUserAddAliasResponse.status: array expected");
                message.status = [];
                for (let i = 0; i < object.status.length; ++i) {
                    if (!$util.isObject(object.status[i]))
                        throw TypeError(".Authentication.EnterpriseUserAddAliasResponse.status: object expected");
                    message.status[i] = $root.Authentication.EnterpriseUserAddAliasStatus.fromObject(object.status[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an EnterpriseUserAddAliasResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.EnterpriseUserAddAliasResponse
         * @static
         * @param {Authentication.EnterpriseUserAddAliasResponse} message EnterpriseUserAddAliasResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EnterpriseUserAddAliasResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.status = [];
            if (message.status && message.status.length) {
                object.status = [];
                for (let j = 0; j < message.status.length; ++j)
                    object.status[j] = $root.Authentication.EnterpriseUserAddAliasStatus.toObject(message.status[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this EnterpriseUserAddAliasResponse to JSON.
         * @function toJSON
         * @memberof Authentication.EnterpriseUserAddAliasResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EnterpriseUserAddAliasResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EnterpriseUserAddAliasResponse
         * @function getTypeUrl
         * @memberof Authentication.EnterpriseUserAddAliasResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EnterpriseUserAddAliasResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.EnterpriseUserAddAliasResponse";
        };

        return EnterpriseUserAddAliasResponse;
    })();

    Authentication.Device = (function() {

        /**
         * Properties of a Device.
         * @memberof Authentication
         * @interface IDevice
         * @property {Uint8Array|null} [encryptedDeviceToken] Device encryptedDeviceToken
         */

        /**
         * Constructs a new Device.
         * @memberof Authentication
         * @classdesc Represents a Device.
         * @implements IDevice
         * @constructor
         * @param {Authentication.IDevice=} [properties] Properties to set
         */
        function Device(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Device encryptedDeviceToken.
         * @member {Uint8Array} encryptedDeviceToken
         * @memberof Authentication.Device
         * @instance
         */
        Device.prototype.encryptedDeviceToken = $util.newBuffer([]);

        /**
         * Creates a new Device instance using the specified properties.
         * @function create
         * @memberof Authentication.Device
         * @static
         * @param {Authentication.IDevice=} [properties] Properties to set
         * @returns {Authentication.Device} Device instance
         */
        Device.create = function create(properties) {
            return new Device(properties);
        };

        /**
         * Encodes the specified Device message. Does not implicitly {@link Authentication.Device.verify|verify} messages.
         * @function encode
         * @memberof Authentication.Device
         * @static
         * @param {Authentication.IDevice} message Device message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Device.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedDeviceToken);
            return writer;
        };

        /**
         * Decodes a Device message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.Device
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.Device} Device
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Device.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.Device();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedDeviceToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a Device message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.Device
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.Device} Device
         */
        Device.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.Device)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.Device: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.Device();
            if (object.encryptedDeviceToken != null)
                if (typeof object.encryptedDeviceToken === "string")
                    $util.base64.decode(object.encryptedDeviceToken, message.encryptedDeviceToken = $util.newBuffer($util.base64.length(object.encryptedDeviceToken)), 0);
                else if (object.encryptedDeviceToken.length >= 0)
                    message.encryptedDeviceToken = object.encryptedDeviceToken;
            return message;
        };

        /**
         * Creates a plain object from a Device message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.Device
         * @static
         * @param {Authentication.Device} message Device
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Device.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === String)
                    object.encryptedDeviceToken = "";
                else {
                    object.encryptedDeviceToken = [];
                    if (options.bytes !== Array)
                        object.encryptedDeviceToken = $util.newBuffer(object.encryptedDeviceToken);
                }
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                object.encryptedDeviceToken = options.bytes === String ? $util.base64.encode(message.encryptedDeviceToken, 0, message.encryptedDeviceToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDeviceToken) : message.encryptedDeviceToken;
            return object;
        };

        /**
         * Converts this Device to JSON.
         * @function toJSON
         * @memberof Authentication.Device
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Device.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Device
         * @function getTypeUrl
         * @memberof Authentication.Device
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Device.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.Device";
        };

        return Device;
    })();

    Authentication.RegisterDeviceDataKeyRequest = (function() {

        /**
         * Properties of a RegisterDeviceDataKeyRequest.
         * @memberof Authentication
         * @interface IRegisterDeviceDataKeyRequest
         * @property {Uint8Array|null} [encryptedDeviceToken] RegisterDeviceDataKeyRequest encryptedDeviceToken
         * @property {Uint8Array|null} [encryptedDeviceDataKey] RegisterDeviceDataKeyRequest encryptedDeviceDataKey
         */

        /**
         * Constructs a new RegisterDeviceDataKeyRequest.
         * @memberof Authentication
         * @classdesc Represents a RegisterDeviceDataKeyRequest.
         * @implements IRegisterDeviceDataKeyRequest
         * @constructor
         * @param {Authentication.IRegisterDeviceDataKeyRequest=} [properties] Properties to set
         */
        function RegisterDeviceDataKeyRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RegisterDeviceDataKeyRequest encryptedDeviceToken.
         * @member {Uint8Array} encryptedDeviceToken
         * @memberof Authentication.RegisterDeviceDataKeyRequest
         * @instance
         */
        RegisterDeviceDataKeyRequest.prototype.encryptedDeviceToken = $util.newBuffer([]);

        /**
         * RegisterDeviceDataKeyRequest encryptedDeviceDataKey.
         * @member {Uint8Array} encryptedDeviceDataKey
         * @memberof Authentication.RegisterDeviceDataKeyRequest
         * @instance
         */
        RegisterDeviceDataKeyRequest.prototype.encryptedDeviceDataKey = $util.newBuffer([]);

        /**
         * Creates a new RegisterDeviceDataKeyRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.RegisterDeviceDataKeyRequest
         * @static
         * @param {Authentication.IRegisterDeviceDataKeyRequest=} [properties] Properties to set
         * @returns {Authentication.RegisterDeviceDataKeyRequest} RegisterDeviceDataKeyRequest instance
         */
        RegisterDeviceDataKeyRequest.create = function create(properties) {
            return new RegisterDeviceDataKeyRequest(properties);
        };

        /**
         * Encodes the specified RegisterDeviceDataKeyRequest message. Does not implicitly {@link Authentication.RegisterDeviceDataKeyRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.RegisterDeviceDataKeyRequest
         * @static
         * @param {Authentication.IRegisterDeviceDataKeyRequest} message RegisterDeviceDataKeyRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RegisterDeviceDataKeyRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedDeviceToken);
            if (message.encryptedDeviceDataKey != null && Object.hasOwnProperty.call(message, "encryptedDeviceDataKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encryptedDeviceDataKey);
            return writer;
        };

        /**
         * Decodes a RegisterDeviceDataKeyRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.RegisterDeviceDataKeyRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.RegisterDeviceDataKeyRequest} RegisterDeviceDataKeyRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RegisterDeviceDataKeyRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.RegisterDeviceDataKeyRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedDeviceToken = reader.bytes();
                        break;
                    }
                case 2: {
                        message.encryptedDeviceDataKey = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a RegisterDeviceDataKeyRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.RegisterDeviceDataKeyRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.RegisterDeviceDataKeyRequest} RegisterDeviceDataKeyRequest
         */
        RegisterDeviceDataKeyRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.RegisterDeviceDataKeyRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.RegisterDeviceDataKeyRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.RegisterDeviceDataKeyRequest();
            if (object.encryptedDeviceToken != null)
                if (typeof object.encryptedDeviceToken === "string")
                    $util.base64.decode(object.encryptedDeviceToken, message.encryptedDeviceToken = $util.newBuffer($util.base64.length(object.encryptedDeviceToken)), 0);
                else if (object.encryptedDeviceToken.length >= 0)
                    message.encryptedDeviceToken = object.encryptedDeviceToken;
            if (object.encryptedDeviceDataKey != null)
                if (typeof object.encryptedDeviceDataKey === "string")
                    $util.base64.decode(object.encryptedDeviceDataKey, message.encryptedDeviceDataKey = $util.newBuffer($util.base64.length(object.encryptedDeviceDataKey)), 0);
                else if (object.encryptedDeviceDataKey.length >= 0)
                    message.encryptedDeviceDataKey = object.encryptedDeviceDataKey;
            return message;
        };

        /**
         * Creates a plain object from a RegisterDeviceDataKeyRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.RegisterDeviceDataKeyRequest
         * @static
         * @param {Authentication.RegisterDeviceDataKeyRequest} message RegisterDeviceDataKeyRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RegisterDeviceDataKeyRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.encryptedDeviceToken = "";
                else {
                    object.encryptedDeviceToken = [];
                    if (options.bytes !== Array)
                        object.encryptedDeviceToken = $util.newBuffer(object.encryptedDeviceToken);
                }
                if (options.bytes === String)
                    object.encryptedDeviceDataKey = "";
                else {
                    object.encryptedDeviceDataKey = [];
                    if (options.bytes !== Array)
                        object.encryptedDeviceDataKey = $util.newBuffer(object.encryptedDeviceDataKey);
                }
            }
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                object.encryptedDeviceToken = options.bytes === String ? $util.base64.encode(message.encryptedDeviceToken, 0, message.encryptedDeviceToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDeviceToken) : message.encryptedDeviceToken;
            if (message.encryptedDeviceDataKey != null && Object.hasOwnProperty.call(message, "encryptedDeviceDataKey"))
                object.encryptedDeviceDataKey = options.bytes === String ? $util.base64.encode(message.encryptedDeviceDataKey, 0, message.encryptedDeviceDataKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDeviceDataKey) : message.encryptedDeviceDataKey;
            return object;
        };

        /**
         * Converts this RegisterDeviceDataKeyRequest to JSON.
         * @function toJSON
         * @memberof Authentication.RegisterDeviceDataKeyRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RegisterDeviceDataKeyRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RegisterDeviceDataKeyRequest
         * @function getTypeUrl
         * @memberof Authentication.RegisterDeviceDataKeyRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RegisterDeviceDataKeyRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.RegisterDeviceDataKeyRequest";
        };

        return RegisterDeviceDataKeyRequest;
    })();

    Authentication.ValidateCreateUserVerificationCodeRequest = (function() {

        /**
         * Properties of a ValidateCreateUserVerificationCodeRequest.
         * @memberof Authentication
         * @interface IValidateCreateUserVerificationCodeRequest
         * @property {string|null} [username] ValidateCreateUserVerificationCodeRequest username
         * @property {string|null} [clientVersion] ValidateCreateUserVerificationCodeRequest clientVersion
         * @property {string|null} [verificationCode] ValidateCreateUserVerificationCodeRequest verificationCode
         */

        /**
         * Constructs a new ValidateCreateUserVerificationCodeRequest.
         * @memberof Authentication
         * @classdesc Represents a ValidateCreateUserVerificationCodeRequest.
         * @implements IValidateCreateUserVerificationCodeRequest
         * @constructor
         * @param {Authentication.IValidateCreateUserVerificationCodeRequest=} [properties] Properties to set
         */
        function ValidateCreateUserVerificationCodeRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ValidateCreateUserVerificationCodeRequest username.
         * @member {string} username
         * @memberof Authentication.ValidateCreateUserVerificationCodeRequest
         * @instance
         */
        ValidateCreateUserVerificationCodeRequest.prototype.username = "";

        /**
         * ValidateCreateUserVerificationCodeRequest clientVersion.
         * @member {string} clientVersion
         * @memberof Authentication.ValidateCreateUserVerificationCodeRequest
         * @instance
         */
        ValidateCreateUserVerificationCodeRequest.prototype.clientVersion = "";

        /**
         * ValidateCreateUserVerificationCodeRequest verificationCode.
         * @member {string} verificationCode
         * @memberof Authentication.ValidateCreateUserVerificationCodeRequest
         * @instance
         */
        ValidateCreateUserVerificationCodeRequest.prototype.verificationCode = "";

        /**
         * Creates a new ValidateCreateUserVerificationCodeRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.ValidateCreateUserVerificationCodeRequest
         * @static
         * @param {Authentication.IValidateCreateUserVerificationCodeRequest=} [properties] Properties to set
         * @returns {Authentication.ValidateCreateUserVerificationCodeRequest} ValidateCreateUserVerificationCodeRequest instance
         */
        ValidateCreateUserVerificationCodeRequest.create = function create(properties) {
            return new ValidateCreateUserVerificationCodeRequest(properties);
        };

        /**
         * Encodes the specified ValidateCreateUserVerificationCodeRequest message. Does not implicitly {@link Authentication.ValidateCreateUserVerificationCodeRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ValidateCreateUserVerificationCodeRequest
         * @static
         * @param {Authentication.IValidateCreateUserVerificationCodeRequest} message ValidateCreateUserVerificationCodeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ValidateCreateUserVerificationCodeRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.username);
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.clientVersion);
            if (message.verificationCode != null && Object.hasOwnProperty.call(message, "verificationCode"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.verificationCode);
            return writer;
        };

        /**
         * Decodes a ValidateCreateUserVerificationCodeRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ValidateCreateUserVerificationCodeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ValidateCreateUserVerificationCodeRequest} ValidateCreateUserVerificationCodeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ValidateCreateUserVerificationCodeRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ValidateCreateUserVerificationCodeRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.username = reader.string();
                        break;
                    }
                case 2: {
                        message.clientVersion = reader.string();
                        break;
                    }
                case 3: {
                        message.verificationCode = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a ValidateCreateUserVerificationCodeRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ValidateCreateUserVerificationCodeRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ValidateCreateUserVerificationCodeRequest} ValidateCreateUserVerificationCodeRequest
         */
        ValidateCreateUserVerificationCodeRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ValidateCreateUserVerificationCodeRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ValidateCreateUserVerificationCodeRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ValidateCreateUserVerificationCodeRequest();
            if (object.username != null)
                message.username = String(object.username);
            if (object.clientVersion != null)
                message.clientVersion = String(object.clientVersion);
            if (object.verificationCode != null)
                message.verificationCode = String(object.verificationCode);
            return message;
        };

        /**
         * Creates a plain object from a ValidateCreateUserVerificationCodeRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ValidateCreateUserVerificationCodeRequest
         * @static
         * @param {Authentication.ValidateCreateUserVerificationCodeRequest} message ValidateCreateUserVerificationCodeRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ValidateCreateUserVerificationCodeRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.username = "";
                object.clientVersion = "";
                object.verificationCode = "";
            }
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                object.clientVersion = message.clientVersion;
            if (message.verificationCode != null && Object.hasOwnProperty.call(message, "verificationCode"))
                object.verificationCode = message.verificationCode;
            return object;
        };

        /**
         * Converts this ValidateCreateUserVerificationCodeRequest to JSON.
         * @function toJSON
         * @memberof Authentication.ValidateCreateUserVerificationCodeRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ValidateCreateUserVerificationCodeRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ValidateCreateUserVerificationCodeRequest
         * @function getTypeUrl
         * @memberof Authentication.ValidateCreateUserVerificationCodeRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ValidateCreateUserVerificationCodeRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ValidateCreateUserVerificationCodeRequest";
        };

        return ValidateCreateUserVerificationCodeRequest;
    })();

    Authentication.ValidateDeviceVerificationCodeRequest = (function() {

        /**
         * Properties of a ValidateDeviceVerificationCodeRequest.
         * @memberof Authentication
         * @interface IValidateDeviceVerificationCodeRequest
         * @property {string|null} [username] ValidateDeviceVerificationCodeRequest username
         * @property {string|null} [clientVersion] ValidateDeviceVerificationCodeRequest clientVersion
         * @property {string|null} [verificationCode] ValidateDeviceVerificationCodeRequest verificationCode
         * @property {Uint8Array|null} [messageSessionUid] ValidateDeviceVerificationCodeRequest messageSessionUid
         * @property {Uint8Array|null} [encryptedDeviceToken] ValidateDeviceVerificationCodeRequest encryptedDeviceToken
         */

        /**
         * Constructs a new ValidateDeviceVerificationCodeRequest.
         * @memberof Authentication
         * @classdesc Represents a ValidateDeviceVerificationCodeRequest.
         * @implements IValidateDeviceVerificationCodeRequest
         * @constructor
         * @param {Authentication.IValidateDeviceVerificationCodeRequest=} [properties] Properties to set
         */
        function ValidateDeviceVerificationCodeRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ValidateDeviceVerificationCodeRequest username.
         * @member {string} username
         * @memberof Authentication.ValidateDeviceVerificationCodeRequest
         * @instance
         */
        ValidateDeviceVerificationCodeRequest.prototype.username = "";

        /**
         * ValidateDeviceVerificationCodeRequest clientVersion.
         * @member {string} clientVersion
         * @memberof Authentication.ValidateDeviceVerificationCodeRequest
         * @instance
         */
        ValidateDeviceVerificationCodeRequest.prototype.clientVersion = "";

        /**
         * ValidateDeviceVerificationCodeRequest verificationCode.
         * @member {string} verificationCode
         * @memberof Authentication.ValidateDeviceVerificationCodeRequest
         * @instance
         */
        ValidateDeviceVerificationCodeRequest.prototype.verificationCode = "";

        /**
         * ValidateDeviceVerificationCodeRequest messageSessionUid.
         * @member {Uint8Array} messageSessionUid
         * @memberof Authentication.ValidateDeviceVerificationCodeRequest
         * @instance
         */
        ValidateDeviceVerificationCodeRequest.prototype.messageSessionUid = $util.newBuffer([]);

        /**
         * ValidateDeviceVerificationCodeRequest encryptedDeviceToken.
         * @member {Uint8Array} encryptedDeviceToken
         * @memberof Authentication.ValidateDeviceVerificationCodeRequest
         * @instance
         */
        ValidateDeviceVerificationCodeRequest.prototype.encryptedDeviceToken = $util.newBuffer([]);

        /**
         * Creates a new ValidateDeviceVerificationCodeRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.ValidateDeviceVerificationCodeRequest
         * @static
         * @param {Authentication.IValidateDeviceVerificationCodeRequest=} [properties] Properties to set
         * @returns {Authentication.ValidateDeviceVerificationCodeRequest} ValidateDeviceVerificationCodeRequest instance
         */
        ValidateDeviceVerificationCodeRequest.create = function create(properties) {
            return new ValidateDeviceVerificationCodeRequest(properties);
        };

        /**
         * Encodes the specified ValidateDeviceVerificationCodeRequest message. Does not implicitly {@link Authentication.ValidateDeviceVerificationCodeRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ValidateDeviceVerificationCodeRequest
         * @static
         * @param {Authentication.IValidateDeviceVerificationCodeRequest} message ValidateDeviceVerificationCodeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ValidateDeviceVerificationCodeRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.username);
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.clientVersion);
            if (message.verificationCode != null && Object.hasOwnProperty.call(message, "verificationCode"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.verificationCode);
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.messageSessionUid);
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.encryptedDeviceToken);
            return writer;
        };

        /**
         * Decodes a ValidateDeviceVerificationCodeRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ValidateDeviceVerificationCodeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ValidateDeviceVerificationCodeRequest} ValidateDeviceVerificationCodeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ValidateDeviceVerificationCodeRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ValidateDeviceVerificationCodeRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.username = reader.string();
                        break;
                    }
                case 2: {
                        message.clientVersion = reader.string();
                        break;
                    }
                case 3: {
                        message.verificationCode = reader.string();
                        break;
                    }
                case 4: {
                        message.messageSessionUid = reader.bytes();
                        break;
                    }
                case 5: {
                        message.encryptedDeviceToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a ValidateDeviceVerificationCodeRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ValidateDeviceVerificationCodeRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ValidateDeviceVerificationCodeRequest} ValidateDeviceVerificationCodeRequest
         */
        ValidateDeviceVerificationCodeRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ValidateDeviceVerificationCodeRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ValidateDeviceVerificationCodeRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ValidateDeviceVerificationCodeRequest();
            if (object.username != null)
                message.username = String(object.username);
            if (object.clientVersion != null)
                message.clientVersion = String(object.clientVersion);
            if (object.verificationCode != null)
                message.verificationCode = String(object.verificationCode);
            if (object.messageSessionUid != null)
                if (typeof object.messageSessionUid === "string")
                    $util.base64.decode(object.messageSessionUid, message.messageSessionUid = $util.newBuffer($util.base64.length(object.messageSessionUid)), 0);
                else if (object.messageSessionUid.length >= 0)
                    message.messageSessionUid = object.messageSessionUid;
            if (object.encryptedDeviceToken != null)
                if (typeof object.encryptedDeviceToken === "string")
                    $util.base64.decode(object.encryptedDeviceToken, message.encryptedDeviceToken = $util.newBuffer($util.base64.length(object.encryptedDeviceToken)), 0);
                else if (object.encryptedDeviceToken.length >= 0)
                    message.encryptedDeviceToken = object.encryptedDeviceToken;
            return message;
        };

        /**
         * Creates a plain object from a ValidateDeviceVerificationCodeRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ValidateDeviceVerificationCodeRequest
         * @static
         * @param {Authentication.ValidateDeviceVerificationCodeRequest} message ValidateDeviceVerificationCodeRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ValidateDeviceVerificationCodeRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.username = "";
                object.clientVersion = "";
                object.verificationCode = "";
                if (options.bytes === String)
                    object.messageSessionUid = "";
                else {
                    object.messageSessionUid = [];
                    if (options.bytes !== Array)
                        object.messageSessionUid = $util.newBuffer(object.messageSessionUid);
                }
                if (options.bytes === String)
                    object.encryptedDeviceToken = "";
                else {
                    object.encryptedDeviceToken = [];
                    if (options.bytes !== Array)
                        object.encryptedDeviceToken = $util.newBuffer(object.encryptedDeviceToken);
                }
            }
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                object.clientVersion = message.clientVersion;
            if (message.verificationCode != null && Object.hasOwnProperty.call(message, "verificationCode"))
                object.verificationCode = message.verificationCode;
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                object.messageSessionUid = options.bytes === String ? $util.base64.encode(message.messageSessionUid, 0, message.messageSessionUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.messageSessionUid) : message.messageSessionUid;
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                object.encryptedDeviceToken = options.bytes === String ? $util.base64.encode(message.encryptedDeviceToken, 0, message.encryptedDeviceToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDeviceToken) : message.encryptedDeviceToken;
            return object;
        };

        /**
         * Converts this ValidateDeviceVerificationCodeRequest to JSON.
         * @function toJSON
         * @memberof Authentication.ValidateDeviceVerificationCodeRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ValidateDeviceVerificationCodeRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ValidateDeviceVerificationCodeRequest
         * @function getTypeUrl
         * @memberof Authentication.ValidateDeviceVerificationCodeRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ValidateDeviceVerificationCodeRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ValidateDeviceVerificationCodeRequest";
        };

        return ValidateDeviceVerificationCodeRequest;
    })();

    Authentication.SendSessionMessageRequest = (function() {

        /**
         * Properties of a SendSessionMessageRequest.
         * @memberof Authentication
         * @interface ISendSessionMessageRequest
         * @property {Uint8Array|null} [messageSessionUid] SendSessionMessageRequest messageSessionUid
         * @property {string|null} [command] SendSessionMessageRequest command
         * @property {string|null} [username] SendSessionMessageRequest username
         */

        /**
         * Constructs a new SendSessionMessageRequest.
         * @memberof Authentication
         * @classdesc Represents a SendSessionMessageRequest.
         * @implements ISendSessionMessageRequest
         * @constructor
         * @param {Authentication.ISendSessionMessageRequest=} [properties] Properties to set
         */
        function SendSessionMessageRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SendSessionMessageRequest messageSessionUid.
         * @member {Uint8Array} messageSessionUid
         * @memberof Authentication.SendSessionMessageRequest
         * @instance
         */
        SendSessionMessageRequest.prototype.messageSessionUid = $util.newBuffer([]);

        /**
         * SendSessionMessageRequest command.
         * @member {string} command
         * @memberof Authentication.SendSessionMessageRequest
         * @instance
         */
        SendSessionMessageRequest.prototype.command = "";

        /**
         * SendSessionMessageRequest username.
         * @member {string} username
         * @memberof Authentication.SendSessionMessageRequest
         * @instance
         */
        SendSessionMessageRequest.prototype.username = "";

        /**
         * Creates a new SendSessionMessageRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.SendSessionMessageRequest
         * @static
         * @param {Authentication.ISendSessionMessageRequest=} [properties] Properties to set
         * @returns {Authentication.SendSessionMessageRequest} SendSessionMessageRequest instance
         */
        SendSessionMessageRequest.create = function create(properties) {
            return new SendSessionMessageRequest(properties);
        };

        /**
         * Encodes the specified SendSessionMessageRequest message. Does not implicitly {@link Authentication.SendSessionMessageRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SendSessionMessageRequest
         * @static
         * @param {Authentication.ISendSessionMessageRequest} message SendSessionMessageRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendSessionMessageRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.messageSessionUid);
            if (message.command != null && Object.hasOwnProperty.call(message, "command"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.command);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.username);
            return writer;
        };

        /**
         * Decodes a SendSessionMessageRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SendSessionMessageRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SendSessionMessageRequest} SendSessionMessageRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendSessionMessageRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SendSessionMessageRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.messageSessionUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.command = reader.string();
                        break;
                    }
                case 3: {
                        message.username = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SendSessionMessageRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SendSessionMessageRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SendSessionMessageRequest} SendSessionMessageRequest
         */
        SendSessionMessageRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SendSessionMessageRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SendSessionMessageRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SendSessionMessageRequest();
            if (object.messageSessionUid != null)
                if (typeof object.messageSessionUid === "string")
                    $util.base64.decode(object.messageSessionUid, message.messageSessionUid = $util.newBuffer($util.base64.length(object.messageSessionUid)), 0);
                else if (object.messageSessionUid.length >= 0)
                    message.messageSessionUid = object.messageSessionUid;
            if (object.command != null)
                message.command = String(object.command);
            if (object.username != null)
                message.username = String(object.username);
            return message;
        };

        /**
         * Creates a plain object from a SendSessionMessageRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SendSessionMessageRequest
         * @static
         * @param {Authentication.SendSessionMessageRequest} message SendSessionMessageRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SendSessionMessageRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.messageSessionUid = "";
                else {
                    object.messageSessionUid = [];
                    if (options.bytes !== Array)
                        object.messageSessionUid = $util.newBuffer(object.messageSessionUid);
                }
                object.command = "";
                object.username = "";
            }
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                object.messageSessionUid = options.bytes === String ? $util.base64.encode(message.messageSessionUid, 0, message.messageSessionUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.messageSessionUid) : message.messageSessionUid;
            if (message.command != null && Object.hasOwnProperty.call(message, "command"))
                object.command = message.command;
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            return object;
        };

        /**
         * Converts this SendSessionMessageRequest to JSON.
         * @function toJSON
         * @memberof Authentication.SendSessionMessageRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SendSessionMessageRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SendSessionMessageRequest
         * @function getTypeUrl
         * @memberof Authentication.SendSessionMessageRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SendSessionMessageRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SendSessionMessageRequest";
        };

        return SendSessionMessageRequest;
    })();

    Authentication.GlobalUserAccount = (function() {

        /**
         * Properties of a GlobalUserAccount.
         * @memberof Authentication
         * @interface IGlobalUserAccount
         * @property {string|null} [username] GlobalUserAccount username
         * @property {Uint8Array|null} [accountUid] GlobalUserAccount accountUid
         * @property {string|null} [regionName] GlobalUserAccount regionName
         */

        /**
         * Constructs a new GlobalUserAccount.
         * @memberof Authentication
         * @classdesc Represents a GlobalUserAccount.
         * @implements IGlobalUserAccount
         * @constructor
         * @param {Authentication.IGlobalUserAccount=} [properties] Properties to set
         */
        function GlobalUserAccount(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GlobalUserAccount username.
         * @member {string} username
         * @memberof Authentication.GlobalUserAccount
         * @instance
         */
        GlobalUserAccount.prototype.username = "";

        /**
         * GlobalUserAccount accountUid.
         * @member {Uint8Array} accountUid
         * @memberof Authentication.GlobalUserAccount
         * @instance
         */
        GlobalUserAccount.prototype.accountUid = $util.newBuffer([]);

        /**
         * GlobalUserAccount regionName.
         * @member {string} regionName
         * @memberof Authentication.GlobalUserAccount
         * @instance
         */
        GlobalUserAccount.prototype.regionName = "";

        /**
         * Creates a new GlobalUserAccount instance using the specified properties.
         * @function create
         * @memberof Authentication.GlobalUserAccount
         * @static
         * @param {Authentication.IGlobalUserAccount=} [properties] Properties to set
         * @returns {Authentication.GlobalUserAccount} GlobalUserAccount instance
         */
        GlobalUserAccount.create = function create(properties) {
            return new GlobalUserAccount(properties);
        };

        /**
         * Encodes the specified GlobalUserAccount message. Does not implicitly {@link Authentication.GlobalUserAccount.verify|verify} messages.
         * @function encode
         * @memberof Authentication.GlobalUserAccount
         * @static
         * @param {Authentication.IGlobalUserAccount} message GlobalUserAccount message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GlobalUserAccount.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.username);
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.accountUid);
            if (message.regionName != null && Object.hasOwnProperty.call(message, "regionName"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.regionName);
            return writer;
        };

        /**
         * Decodes a GlobalUserAccount message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.GlobalUserAccount
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.GlobalUserAccount} GlobalUserAccount
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GlobalUserAccount.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.GlobalUserAccount();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.username = reader.string();
                        break;
                    }
                case 2: {
                        message.accountUid = reader.bytes();
                        break;
                    }
                case 3: {
                        message.regionName = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a GlobalUserAccount message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.GlobalUserAccount
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.GlobalUserAccount} GlobalUserAccount
         */
        GlobalUserAccount.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.GlobalUserAccount)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.GlobalUserAccount: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.GlobalUserAccount();
            if (object.username != null)
                message.username = String(object.username);
            if (object.accountUid != null)
                if (typeof object.accountUid === "string")
                    $util.base64.decode(object.accountUid, message.accountUid = $util.newBuffer($util.base64.length(object.accountUid)), 0);
                else if (object.accountUid.length >= 0)
                    message.accountUid = object.accountUid;
            if (object.regionName != null)
                message.regionName = String(object.regionName);
            return message;
        };

        /**
         * Creates a plain object from a GlobalUserAccount message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.GlobalUserAccount
         * @static
         * @param {Authentication.GlobalUserAccount} message GlobalUserAccount
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GlobalUserAccount.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.username = "";
                if (options.bytes === String)
                    object.accountUid = "";
                else {
                    object.accountUid = [];
                    if (options.bytes !== Array)
                        object.accountUid = $util.newBuffer(object.accountUid);
                }
                object.regionName = "";
            }
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                object.accountUid = options.bytes === String ? $util.base64.encode(message.accountUid, 0, message.accountUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.accountUid) : message.accountUid;
            if (message.regionName != null && Object.hasOwnProperty.call(message, "regionName"))
                object.regionName = message.regionName;
            return object;
        };

        /**
         * Converts this GlobalUserAccount to JSON.
         * @function toJSON
         * @memberof Authentication.GlobalUserAccount
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GlobalUserAccount.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GlobalUserAccount
         * @function getTypeUrl
         * @memberof Authentication.GlobalUserAccount
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GlobalUserAccount.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.GlobalUserAccount";
        };

        return GlobalUserAccount;
    })();

    Authentication.AccountUsername = (function() {

        /**
         * Properties of an AccountUsername.
         * @memberof Authentication
         * @interface IAccountUsername
         * @property {string|null} [username] AccountUsername username
         * @property {string|null} [dateActive] AccountUsername dateActive
         */

        /**
         * Constructs a new AccountUsername.
         * @memberof Authentication
         * @classdesc Represents an AccountUsername.
         * @implements IAccountUsername
         * @constructor
         * @param {Authentication.IAccountUsername=} [properties] Properties to set
         */
        function AccountUsername(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AccountUsername username.
         * @member {string} username
         * @memberof Authentication.AccountUsername
         * @instance
         */
        AccountUsername.prototype.username = "";

        /**
         * AccountUsername dateActive.
         * @member {string} dateActive
         * @memberof Authentication.AccountUsername
         * @instance
         */
        AccountUsername.prototype.dateActive = "";

        /**
         * Creates a new AccountUsername instance using the specified properties.
         * @function create
         * @memberof Authentication.AccountUsername
         * @static
         * @param {Authentication.IAccountUsername=} [properties] Properties to set
         * @returns {Authentication.AccountUsername} AccountUsername instance
         */
        AccountUsername.create = function create(properties) {
            return new AccountUsername(properties);
        };

        /**
         * Encodes the specified AccountUsername message. Does not implicitly {@link Authentication.AccountUsername.verify|verify} messages.
         * @function encode
         * @memberof Authentication.AccountUsername
         * @static
         * @param {Authentication.IAccountUsername} message AccountUsername message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountUsername.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.username);
            if (message.dateActive != null && Object.hasOwnProperty.call(message, "dateActive"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.dateActive);
            return writer;
        };

        /**
         * Decodes an AccountUsername message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.AccountUsername
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.AccountUsername} AccountUsername
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountUsername.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.AccountUsername();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.username = reader.string();
                        break;
                    }
                case 2: {
                        message.dateActive = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an AccountUsername message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.AccountUsername
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.AccountUsername} AccountUsername
         */
        AccountUsername.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.AccountUsername)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.AccountUsername: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.AccountUsername();
            if (object.username != null)
                message.username = String(object.username);
            if (object.dateActive != null)
                message.dateActive = String(object.dateActive);
            return message;
        };

        /**
         * Creates a plain object from an AccountUsername message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.AccountUsername
         * @static
         * @param {Authentication.AccountUsername} message AccountUsername
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AccountUsername.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.username = "";
                object.dateActive = "";
            }
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.dateActive != null && Object.hasOwnProperty.call(message, "dateActive"))
                object.dateActive = message.dateActive;
            return object;
        };

        /**
         * Converts this AccountUsername to JSON.
         * @function toJSON
         * @memberof Authentication.AccountUsername
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AccountUsername.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AccountUsername
         * @function getTypeUrl
         * @memberof Authentication.AccountUsername
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AccountUsername.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.AccountUsername";
        };

        return AccountUsername;
    })();

    Authentication.SsoServiceProviderRequest = (function() {

        /**
         * Properties of a SsoServiceProviderRequest.
         * @memberof Authentication
         * @interface ISsoServiceProviderRequest
         * @property {string|null} [name] SsoServiceProviderRequest name
         * @property {string|null} [clientVersion] SsoServiceProviderRequest clientVersion
         * @property {string|null} [locale] SsoServiceProviderRequest locale
         */

        /**
         * Constructs a new SsoServiceProviderRequest.
         * @memberof Authentication
         * @classdesc This is the input to the get_sso_service_provider command.
         * @implements ISsoServiceProviderRequest
         * @constructor
         * @param {Authentication.ISsoServiceProviderRequest=} [properties] Properties to set
         */
        function SsoServiceProviderRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SsoServiceProviderRequest name.
         * @member {string} name
         * @memberof Authentication.SsoServiceProviderRequest
         * @instance
         */
        SsoServiceProviderRequest.prototype.name = "";

        /**
         * SsoServiceProviderRequest clientVersion.
         * @member {string} clientVersion
         * @memberof Authentication.SsoServiceProviderRequest
         * @instance
         */
        SsoServiceProviderRequest.prototype.clientVersion = "";

        /**
         * SsoServiceProviderRequest locale.
         * @member {string} locale
         * @memberof Authentication.SsoServiceProviderRequest
         * @instance
         */
        SsoServiceProviderRequest.prototype.locale = "";

        /**
         * Creates a new SsoServiceProviderRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.SsoServiceProviderRequest
         * @static
         * @param {Authentication.ISsoServiceProviderRequest=} [properties] Properties to set
         * @returns {Authentication.SsoServiceProviderRequest} SsoServiceProviderRequest instance
         */
        SsoServiceProviderRequest.create = function create(properties) {
            return new SsoServiceProviderRequest(properties);
        };

        /**
         * Encodes the specified SsoServiceProviderRequest message. Does not implicitly {@link Authentication.SsoServiceProviderRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SsoServiceProviderRequest
         * @static
         * @param {Authentication.ISsoServiceProviderRequest} message SsoServiceProviderRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SsoServiceProviderRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.clientVersion);
            if (message.locale != null && Object.hasOwnProperty.call(message, "locale"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.locale);
            return writer;
        };

        /**
         * Decodes a SsoServiceProviderRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SsoServiceProviderRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SsoServiceProviderRequest} SsoServiceProviderRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SsoServiceProviderRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SsoServiceProviderRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.name = reader.string();
                        break;
                    }
                case 2: {
                        message.clientVersion = reader.string();
                        break;
                    }
                case 3: {
                        message.locale = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SsoServiceProviderRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SsoServiceProviderRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SsoServiceProviderRequest} SsoServiceProviderRequest
         */
        SsoServiceProviderRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SsoServiceProviderRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SsoServiceProviderRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SsoServiceProviderRequest();
            if (object.name != null)
                message.name = String(object.name);
            if (object.clientVersion != null)
                message.clientVersion = String(object.clientVersion);
            if (object.locale != null)
                message.locale = String(object.locale);
            return message;
        };

        /**
         * Creates a plain object from a SsoServiceProviderRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SsoServiceProviderRequest
         * @static
         * @param {Authentication.SsoServiceProviderRequest} message SsoServiceProviderRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SsoServiceProviderRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.name = "";
                object.clientVersion = "";
                object.locale = "";
            }
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                object.clientVersion = message.clientVersion;
            if (message.locale != null && Object.hasOwnProperty.call(message, "locale"))
                object.locale = message.locale;
            return object;
        };

        /**
         * Converts this SsoServiceProviderRequest to JSON.
         * @function toJSON
         * @memberof Authentication.SsoServiceProviderRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SsoServiceProviderRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SsoServiceProviderRequest
         * @function getTypeUrl
         * @memberof Authentication.SsoServiceProviderRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SsoServiceProviderRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SsoServiceProviderRequest";
        };

        return SsoServiceProviderRequest;
    })();

    Authentication.SsoServiceProviderResponse = (function() {

        /**
         * Properties of a SsoServiceProviderResponse.
         * @memberof Authentication
         * @interface ISsoServiceProviderResponse
         * @property {string|null} [name] SsoServiceProviderResponse name
         * @property {string|null} [spUrl] SsoServiceProviderResponse spUrl
         * @property {boolean|null} [isCloud] SsoServiceProviderResponse isCloud
         * @property {string|null} [clientVersion] SsoServiceProviderResponse clientVersion
         */

        /**
         * Constructs a new SsoServiceProviderResponse.
         * @memberof Authentication
         * @classdesc This is the response from the get_sso_service_provider command.
         * @implements ISsoServiceProviderResponse
         * @constructor
         * @param {Authentication.ISsoServiceProviderResponse=} [properties] Properties to set
         */
        function SsoServiceProviderResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SsoServiceProviderResponse name.
         * @member {string} name
         * @memberof Authentication.SsoServiceProviderResponse
         * @instance
         */
        SsoServiceProviderResponse.prototype.name = "";

        /**
         * SsoServiceProviderResponse spUrl.
         * @member {string} spUrl
         * @memberof Authentication.SsoServiceProviderResponse
         * @instance
         */
        SsoServiceProviderResponse.prototype.spUrl = "";

        /**
         * SsoServiceProviderResponse isCloud.
         * @member {boolean} isCloud
         * @memberof Authentication.SsoServiceProviderResponse
         * @instance
         */
        SsoServiceProviderResponse.prototype.isCloud = false;

        /**
         * SsoServiceProviderResponse clientVersion.
         * @member {string} clientVersion
         * @memberof Authentication.SsoServiceProviderResponse
         * @instance
         */
        SsoServiceProviderResponse.prototype.clientVersion = "";

        /**
         * Creates a new SsoServiceProviderResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.SsoServiceProviderResponse
         * @static
         * @param {Authentication.ISsoServiceProviderResponse=} [properties] Properties to set
         * @returns {Authentication.SsoServiceProviderResponse} SsoServiceProviderResponse instance
         */
        SsoServiceProviderResponse.create = function create(properties) {
            return new SsoServiceProviderResponse(properties);
        };

        /**
         * Encodes the specified SsoServiceProviderResponse message. Does not implicitly {@link Authentication.SsoServiceProviderResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SsoServiceProviderResponse
         * @static
         * @param {Authentication.ISsoServiceProviderResponse} message SsoServiceProviderResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SsoServiceProviderResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.spUrl != null && Object.hasOwnProperty.call(message, "spUrl"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.spUrl);
            if (message.isCloud != null && Object.hasOwnProperty.call(message, "isCloud"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.isCloud);
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.clientVersion);
            return writer;
        };

        /**
         * Decodes a SsoServiceProviderResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SsoServiceProviderResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SsoServiceProviderResponse} SsoServiceProviderResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SsoServiceProviderResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SsoServiceProviderResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.name = reader.string();
                        break;
                    }
                case 2: {
                        message.spUrl = reader.string();
                        break;
                    }
                case 3: {
                        message.isCloud = reader.bool();
                        break;
                    }
                case 4: {
                        message.clientVersion = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SsoServiceProviderResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SsoServiceProviderResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SsoServiceProviderResponse} SsoServiceProviderResponse
         */
        SsoServiceProviderResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SsoServiceProviderResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SsoServiceProviderResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SsoServiceProviderResponse();
            if (object.name != null)
                message.name = String(object.name);
            if (object.spUrl != null)
                message.spUrl = String(object.spUrl);
            if (object.isCloud != null)
                message.isCloud = Boolean(object.isCloud);
            if (object.clientVersion != null)
                message.clientVersion = String(object.clientVersion);
            return message;
        };

        /**
         * Creates a plain object from a SsoServiceProviderResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SsoServiceProviderResponse
         * @static
         * @param {Authentication.SsoServiceProviderResponse} message SsoServiceProviderResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SsoServiceProviderResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.name = "";
                object.spUrl = "";
                object.isCloud = false;
                object.clientVersion = "";
            }
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.spUrl != null && Object.hasOwnProperty.call(message, "spUrl"))
                object.spUrl = message.spUrl;
            if (message.isCloud != null && Object.hasOwnProperty.call(message, "isCloud"))
                object.isCloud = message.isCloud;
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                object.clientVersion = message.clientVersion;
            return object;
        };

        /**
         * Converts this SsoServiceProviderResponse to JSON.
         * @function toJSON
         * @memberof Authentication.SsoServiceProviderResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SsoServiceProviderResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SsoServiceProviderResponse
         * @function getTypeUrl
         * @memberof Authentication.SsoServiceProviderResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SsoServiceProviderResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SsoServiceProviderResponse";
        };

        return SsoServiceProviderResponse;
    })();

    Authentication.UserSettingRequest = (function() {

        /**
         * Properties of a UserSettingRequest.
         * @memberof Authentication
         * @interface IUserSettingRequest
         * @property {string|null} [setting] UserSettingRequest setting
         * @property {string|null} [value] UserSettingRequest value
         */

        /**
         * Constructs a new UserSettingRequest.
         * @memberof Authentication
         * @classdesc Represents a UserSettingRequest.
         * @implements IUserSettingRequest
         * @constructor
         * @param {Authentication.IUserSettingRequest=} [properties] Properties to set
         */
        function UserSettingRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UserSettingRequest setting.
         * @member {string} setting
         * @memberof Authentication.UserSettingRequest
         * @instance
         */
        UserSettingRequest.prototype.setting = "";

        /**
         * UserSettingRequest value.
         * @member {string} value
         * @memberof Authentication.UserSettingRequest
         * @instance
         */
        UserSettingRequest.prototype.value = "";

        /**
         * Creates a new UserSettingRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.UserSettingRequest
         * @static
         * @param {Authentication.IUserSettingRequest=} [properties] Properties to set
         * @returns {Authentication.UserSettingRequest} UserSettingRequest instance
         */
        UserSettingRequest.create = function create(properties) {
            return new UserSettingRequest(properties);
        };

        /**
         * Encodes the specified UserSettingRequest message. Does not implicitly {@link Authentication.UserSettingRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.UserSettingRequest
         * @static
         * @param {Authentication.IUserSettingRequest} message UserSettingRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserSettingRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.setting != null && Object.hasOwnProperty.call(message, "setting"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.setting);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.value);
            return writer;
        };

        /**
         * Decodes a UserSettingRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.UserSettingRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.UserSettingRequest} UserSettingRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserSettingRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.UserSettingRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.setting = reader.string();
                        break;
                    }
                case 2: {
                        message.value = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a UserSettingRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.UserSettingRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.UserSettingRequest} UserSettingRequest
         */
        UserSettingRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.UserSettingRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.UserSettingRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.UserSettingRequest();
            if (object.setting != null)
                message.setting = String(object.setting);
            if (object.value != null)
                message.value = String(object.value);
            return message;
        };

        /**
         * Creates a plain object from a UserSettingRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.UserSettingRequest
         * @static
         * @param {Authentication.UserSettingRequest} message UserSettingRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UserSettingRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.setting = "";
                object.value = "";
            }
            if (message.setting != null && Object.hasOwnProperty.call(message, "setting"))
                object.setting = message.setting;
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                object.value = message.value;
            return object;
        };

        /**
         * Converts this UserSettingRequest to JSON.
         * @function toJSON
         * @memberof Authentication.UserSettingRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UserSettingRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UserSettingRequest
         * @function getTypeUrl
         * @memberof Authentication.UserSettingRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UserSettingRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.UserSettingRequest";
        };

        return UserSettingRequest;
    })();

    /**
     * AlternateAuthenticationType enum.
     * @name Authentication.AlternateAuthenticationType
     * @enum {number}
     * @property {number} ALTERNATE_MASTER_PASSWORD=0 ALTERNATE_MASTER_PASSWORD value
     * @property {number} BIOMETRIC=1 BIOMETRIC value
     * @property {number} ACCOUNT_RECOVER=2 ACCOUNT_RECOVER value
     */
    Authentication.AlternateAuthenticationType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "ALTERNATE_MASTER_PASSWORD"] = 0;
        values[valuesById[1] = "BIOMETRIC"] = 1;
        values[valuesById[2] = "ACCOUNT_RECOVER"] = 2;
        return values;
    })();

    /**
     * ThrottleType enum.
     * @name Authentication.ThrottleType
     * @enum {number}
     * @property {number} PASSWORD_RETRY_THROTTLE=0 PASSWORD_RETRY_THROTTLE value
     * @property {number} PASSWORD_RETRY_LEGACY_THROTTLE=1 PASSWORD_RETRY_LEGACY_THROTTLE value
     * @property {number} TWO_FA_THROTTLE=2 TWO_FA_THROTTLE value
     * @property {number} TWO_FA_LEGACY_THROTTLE=3 TWO_FA_LEGACY_THROTTLE value
     * @property {number} QA_RETRY_THROTTLE=4 QA_RETRY_THROTTLE value
     * @property {number} ACCOUNT_RECOVER_THROTTLE=5 ACCOUNT_RECOVER_THROTTLE value
     * @property {number} VALIDATE_DEVICE_VERIFICATION_CODE_THROTTLE=6 VALIDATE_DEVICE_VERIFICATION_CODE_THROTTLE value
     * @property {number} VALIDATE_CREATE_USER_VERIFICATION_CODE_THROTTLE=7 VALIDATE_CREATE_USER_VERIFICATION_CODE_THROTTLE value
     */
    Authentication.ThrottleType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "PASSWORD_RETRY_THROTTLE"] = 0;
        values[valuesById[1] = "PASSWORD_RETRY_LEGACY_THROTTLE"] = 1;
        values[valuesById[2] = "TWO_FA_THROTTLE"] = 2;
        values[valuesById[3] = "TWO_FA_LEGACY_THROTTLE"] = 3;
        values[valuesById[4] = "QA_RETRY_THROTTLE"] = 4;
        values[valuesById[5] = "ACCOUNT_RECOVER_THROTTLE"] = 5;
        values[valuesById[6] = "VALIDATE_DEVICE_VERIFICATION_CODE_THROTTLE"] = 6;
        values[valuesById[7] = "VALIDATE_CREATE_USER_VERIFICATION_CODE_THROTTLE"] = 7;
        return values;
    })();

    Authentication.ThrottleState = (function() {

        /**
         * Properties of a ThrottleState.
         * @memberof Authentication
         * @interface IThrottleState
         * @property {Authentication.ThrottleType|null} [type] ThrottleState type
         * @property {string|null} [key] ThrottleState key
         * @property {string|null} [value] ThrottleState value
         * @property {boolean|null} [state] ThrottleState state
         */

        /**
         * Constructs a new ThrottleState.
         * @memberof Authentication
         * @classdesc Represents a ThrottleState.
         * @implements IThrottleState
         * @constructor
         * @param {Authentication.IThrottleState=} [properties] Properties to set
         */
        function ThrottleState(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ThrottleState type.
         * @member {Authentication.ThrottleType} type
         * @memberof Authentication.ThrottleState
         * @instance
         */
        ThrottleState.prototype.type = 0;

        /**
         * ThrottleState key.
         * @member {string} key
         * @memberof Authentication.ThrottleState
         * @instance
         */
        ThrottleState.prototype.key = "";

        /**
         * ThrottleState value.
         * @member {string} value
         * @memberof Authentication.ThrottleState
         * @instance
         */
        ThrottleState.prototype.value = "";

        /**
         * ThrottleState state.
         * @member {boolean} state
         * @memberof Authentication.ThrottleState
         * @instance
         */
        ThrottleState.prototype.state = false;

        /**
         * Creates a new ThrottleState instance using the specified properties.
         * @function create
         * @memberof Authentication.ThrottleState
         * @static
         * @param {Authentication.IThrottleState=} [properties] Properties to set
         * @returns {Authentication.ThrottleState} ThrottleState instance
         */
        ThrottleState.create = function create(properties) {
            return new ThrottleState(properties);
        };

        /**
         * Encodes the specified ThrottleState message. Does not implicitly {@link Authentication.ThrottleState.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ThrottleState
         * @static
         * @param {Authentication.IThrottleState} message ThrottleState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThrottleState.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.key);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.value);
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.state);
            return writer;
        };

        /**
         * Decodes a ThrottleState message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ThrottleState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ThrottleState} ThrottleState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThrottleState.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ThrottleState();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.type = reader.int32();
                        break;
                    }
                case 2: {
                        message.key = reader.string();
                        break;
                    }
                case 3: {
                        message.value = reader.string();
                        break;
                    }
                case 4: {
                        message.state = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a ThrottleState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ThrottleState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ThrottleState} ThrottleState
         */
        ThrottleState.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ThrottleState)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ThrottleState: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ThrottleState();
            switch (object.type) {
            default:
                if (typeof object.type === "number") {
                    message.type = object.type;
                    break;
                }
                break;
            case "PASSWORD_RETRY_THROTTLE":
            case 0:
                message.type = 0;
                break;
            case "PASSWORD_RETRY_LEGACY_THROTTLE":
            case 1:
                message.type = 1;
                break;
            case "TWO_FA_THROTTLE":
            case 2:
                message.type = 2;
                break;
            case "TWO_FA_LEGACY_THROTTLE":
            case 3:
                message.type = 3;
                break;
            case "QA_RETRY_THROTTLE":
            case 4:
                message.type = 4;
                break;
            case "ACCOUNT_RECOVER_THROTTLE":
            case 5:
                message.type = 5;
                break;
            case "VALIDATE_DEVICE_VERIFICATION_CODE_THROTTLE":
            case 6:
                message.type = 6;
                break;
            case "VALIDATE_CREATE_USER_VERIFICATION_CODE_THROTTLE":
            case 7:
                message.type = 7;
                break;
            }
            if (object.key != null)
                message.key = String(object.key);
            if (object.value != null)
                message.value = String(object.value);
            if (object.state != null)
                message.state = Boolean(object.state);
            return message;
        };

        /**
         * Creates a plain object from a ThrottleState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ThrottleState
         * @static
         * @param {Authentication.ThrottleState} message ThrottleState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ThrottleState.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.type = options.enums === String ? "PASSWORD_RETRY_THROTTLE" : 0;
                object.key = "";
                object.value = "";
                object.state = false;
            }
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                object.type = options.enums === String ? $root.Authentication.ThrottleType[message.type] === undefined ? message.type : $root.Authentication.ThrottleType[message.type] : message.type;
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                object.key = message.key;
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                object.value = message.value;
            if (message.state != null && Object.hasOwnProperty.call(message, "state"))
                object.state = message.state;
            return object;
        };

        /**
         * Converts this ThrottleState to JSON.
         * @function toJSON
         * @memberof Authentication.ThrottleState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ThrottleState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ThrottleState
         * @function getTypeUrl
         * @memberof Authentication.ThrottleState
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ThrottleState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ThrottleState";
        };

        return ThrottleState;
    })();

    Authentication.ThrottleState2 = (function() {

        /**
         * Properties of a ThrottleState2.
         * @memberof Authentication
         * @interface IThrottleState2
         * @property {string|null} [key] ThrottleState2 key
         * @property {string|null} [keyDescription] ThrottleState2 keyDescription
         * @property {string|null} [value] ThrottleState2 value
         * @property {string|null} [valueDescription] ThrottleState2 valueDescription
         * @property {string|null} [identifier] ThrottleState2 identifier
         * @property {boolean|null} [locked] ThrottleState2 locked
         * @property {boolean|null} [includedInAllClear] ThrottleState2 includedInAllClear
         * @property {number|null} [expireSeconds] ThrottleState2 expireSeconds
         */

        /**
         * Constructs a new ThrottleState2.
         * @memberof Authentication
         * @classdesc Represents a ThrottleState2.
         * @implements IThrottleState2
         * @constructor
         * @param {Authentication.IThrottleState2=} [properties] Properties to set
         */
        function ThrottleState2(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ThrottleState2 key.
         * @member {string} key
         * @memberof Authentication.ThrottleState2
         * @instance
         */
        ThrottleState2.prototype.key = "";

        /**
         * ThrottleState2 keyDescription.
         * @member {string} keyDescription
         * @memberof Authentication.ThrottleState2
         * @instance
         */
        ThrottleState2.prototype.keyDescription = "";

        /**
         * ThrottleState2 value.
         * @member {string} value
         * @memberof Authentication.ThrottleState2
         * @instance
         */
        ThrottleState2.prototype.value = "";

        /**
         * ThrottleState2 valueDescription.
         * @member {string} valueDescription
         * @memberof Authentication.ThrottleState2
         * @instance
         */
        ThrottleState2.prototype.valueDescription = "";

        /**
         * ThrottleState2 identifier.
         * @member {string} identifier
         * @memberof Authentication.ThrottleState2
         * @instance
         */
        ThrottleState2.prototype.identifier = "";

        /**
         * ThrottleState2 locked.
         * @member {boolean} locked
         * @memberof Authentication.ThrottleState2
         * @instance
         */
        ThrottleState2.prototype.locked = false;

        /**
         * ThrottleState2 includedInAllClear.
         * @member {boolean} includedInAllClear
         * @memberof Authentication.ThrottleState2
         * @instance
         */
        ThrottleState2.prototype.includedInAllClear = false;

        /**
         * ThrottleState2 expireSeconds.
         * @member {number} expireSeconds
         * @memberof Authentication.ThrottleState2
         * @instance
         */
        ThrottleState2.prototype.expireSeconds = 0;

        /**
         * Creates a new ThrottleState2 instance using the specified properties.
         * @function create
         * @memberof Authentication.ThrottleState2
         * @static
         * @param {Authentication.IThrottleState2=} [properties] Properties to set
         * @returns {Authentication.ThrottleState2} ThrottleState2 instance
         */
        ThrottleState2.create = function create(properties) {
            return new ThrottleState2(properties);
        };

        /**
         * Encodes the specified ThrottleState2 message. Does not implicitly {@link Authentication.ThrottleState2.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ThrottleState2
         * @static
         * @param {Authentication.IThrottleState2} message ThrottleState2 message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ThrottleState2.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.key);
            if (message.keyDescription != null && Object.hasOwnProperty.call(message, "keyDescription"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.keyDescription);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.value);
            if (message.valueDescription != null && Object.hasOwnProperty.call(message, "valueDescription"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.valueDescription);
            if (message.identifier != null && Object.hasOwnProperty.call(message, "identifier"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.identifier);
            if (message.locked != null && Object.hasOwnProperty.call(message, "locked"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.locked);
            if (message.includedInAllClear != null && Object.hasOwnProperty.call(message, "includedInAllClear"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.includedInAllClear);
            if (message.expireSeconds != null && Object.hasOwnProperty.call(message, "expireSeconds"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.expireSeconds);
            return writer;
        };

        /**
         * Decodes a ThrottleState2 message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ThrottleState2
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ThrottleState2} ThrottleState2
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ThrottleState2.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ThrottleState2();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.key = reader.string();
                        break;
                    }
                case 2: {
                        message.keyDescription = reader.string();
                        break;
                    }
                case 3: {
                        message.value = reader.string();
                        break;
                    }
                case 4: {
                        message.valueDescription = reader.string();
                        break;
                    }
                case 5: {
                        message.identifier = reader.string();
                        break;
                    }
                case 6: {
                        message.locked = reader.bool();
                        break;
                    }
                case 7: {
                        message.includedInAllClear = reader.bool();
                        break;
                    }
                case 8: {
                        message.expireSeconds = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a ThrottleState2 message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ThrottleState2
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ThrottleState2} ThrottleState2
         */
        ThrottleState2.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ThrottleState2)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ThrottleState2: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ThrottleState2();
            if (object.key != null)
                message.key = String(object.key);
            if (object.keyDescription != null)
                message.keyDescription = String(object.keyDescription);
            if (object.value != null)
                message.value = String(object.value);
            if (object.valueDescription != null)
                message.valueDescription = String(object.valueDescription);
            if (object.identifier != null)
                message.identifier = String(object.identifier);
            if (object.locked != null)
                message.locked = Boolean(object.locked);
            if (object.includedInAllClear != null)
                message.includedInAllClear = Boolean(object.includedInAllClear);
            if (object.expireSeconds != null)
                message.expireSeconds = object.expireSeconds | 0;
            return message;
        };

        /**
         * Creates a plain object from a ThrottleState2 message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ThrottleState2
         * @static
         * @param {Authentication.ThrottleState2} message ThrottleState2
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ThrottleState2.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.key = "";
                object.keyDescription = "";
                object.value = "";
                object.valueDescription = "";
                object.identifier = "";
                object.locked = false;
                object.includedInAllClear = false;
                object.expireSeconds = 0;
            }
            if (message.key != null && Object.hasOwnProperty.call(message, "key"))
                object.key = message.key;
            if (message.keyDescription != null && Object.hasOwnProperty.call(message, "keyDescription"))
                object.keyDescription = message.keyDescription;
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                object.value = message.value;
            if (message.valueDescription != null && Object.hasOwnProperty.call(message, "valueDescription"))
                object.valueDescription = message.valueDescription;
            if (message.identifier != null && Object.hasOwnProperty.call(message, "identifier"))
                object.identifier = message.identifier;
            if (message.locked != null && Object.hasOwnProperty.call(message, "locked"))
                object.locked = message.locked;
            if (message.includedInAllClear != null && Object.hasOwnProperty.call(message, "includedInAllClear"))
                object.includedInAllClear = message.includedInAllClear;
            if (message.expireSeconds != null && Object.hasOwnProperty.call(message, "expireSeconds"))
                object.expireSeconds = message.expireSeconds;
            return object;
        };

        /**
         * Converts this ThrottleState2 to JSON.
         * @function toJSON
         * @memberof Authentication.ThrottleState2
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ThrottleState2.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ThrottleState2
         * @function getTypeUrl
         * @memberof Authentication.ThrottleState2
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ThrottleState2.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ThrottleState2";
        };

        return ThrottleState2;
    })();

    Authentication.DeviceInformation = (function() {

        /**
         * Properties of a DeviceInformation.
         * @memberof Authentication
         * @interface IDeviceInformation
         * @property {number|null} [deviceId] DeviceInformation deviceId
         * @property {string|null} [deviceName] DeviceInformation deviceName
         * @property {string|null} [clientVersion] DeviceInformation clientVersion
         * @property {number|null} [lastLogin] DeviceInformation lastLogin
         * @property {Authentication.DeviceStatus|null} [deviceStatus] DeviceInformation deviceStatus
         */

        /**
         * Constructs a new DeviceInformation.
         * @memberof Authentication
         * @classdesc Represents a DeviceInformation.
         * @implements IDeviceInformation
         * @constructor
         * @param {Authentication.IDeviceInformation=} [properties] Properties to set
         */
        function DeviceInformation(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeviceInformation deviceId.
         * @member {number} deviceId
         * @memberof Authentication.DeviceInformation
         * @instance
         */
        DeviceInformation.prototype.deviceId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * DeviceInformation deviceName.
         * @member {string} deviceName
         * @memberof Authentication.DeviceInformation
         * @instance
         */
        DeviceInformation.prototype.deviceName = "";

        /**
         * DeviceInformation clientVersion.
         * @member {string} clientVersion
         * @memberof Authentication.DeviceInformation
         * @instance
         */
        DeviceInformation.prototype.clientVersion = "";

        /**
         * DeviceInformation lastLogin.
         * @member {number} lastLogin
         * @memberof Authentication.DeviceInformation
         * @instance
         */
        DeviceInformation.prototype.lastLogin = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * DeviceInformation deviceStatus.
         * @member {Authentication.DeviceStatus} deviceStatus
         * @memberof Authentication.DeviceInformation
         * @instance
         */
        DeviceInformation.prototype.deviceStatus = 0;

        /**
         * Creates a new DeviceInformation instance using the specified properties.
         * @function create
         * @memberof Authentication.DeviceInformation
         * @static
         * @param {Authentication.IDeviceInformation=} [properties] Properties to set
         * @returns {Authentication.DeviceInformation} DeviceInformation instance
         */
        DeviceInformation.create = function create(properties) {
            return new DeviceInformation(properties);
        };

        /**
         * Encodes the specified DeviceInformation message. Does not implicitly {@link Authentication.DeviceInformation.verify|verify} messages.
         * @function encode
         * @memberof Authentication.DeviceInformation
         * @static
         * @param {Authentication.IDeviceInformation} message DeviceInformation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceInformation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.deviceId);
            if (message.deviceName != null && Object.hasOwnProperty.call(message, "deviceName"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.deviceName);
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.clientVersion);
            if (message.lastLogin != null && Object.hasOwnProperty.call(message, "lastLogin"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.lastLogin);
            if (message.deviceStatus != null && Object.hasOwnProperty.call(message, "deviceStatus"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.deviceStatus);
            return writer;
        };

        /**
         * Decodes a DeviceInformation message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.DeviceInformation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.DeviceInformation} DeviceInformation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceInformation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.DeviceInformation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.deviceId = reader.int64();
                        break;
                    }
                case 2: {
                        message.deviceName = reader.string();
                        break;
                    }
                case 3: {
                        message.clientVersion = reader.string();
                        break;
                    }
                case 4: {
                        message.lastLogin = reader.int64();
                        break;
                    }
                case 5: {
                        message.deviceStatus = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a DeviceInformation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.DeviceInformation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.DeviceInformation} DeviceInformation
         */
        DeviceInformation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.DeviceInformation)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.DeviceInformation: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.DeviceInformation();
            if (object.deviceId != null)
                if ($util.Long)
                    message.deviceId = $util.Long.fromValue(object.deviceId, false);
                else if (typeof object.deviceId === "string")
                    message.deviceId = parseInt(object.deviceId, 10);
                else if (typeof object.deviceId === "number")
                    message.deviceId = object.deviceId;
                else if (typeof object.deviceId === "object")
                    message.deviceId = new $util.LongBits(object.deviceId.low >>> 0, object.deviceId.high >>> 0).toNumber();
            if (object.deviceName != null)
                message.deviceName = String(object.deviceName);
            if (object.clientVersion != null)
                message.clientVersion = String(object.clientVersion);
            if (object.lastLogin != null)
                if ($util.Long)
                    message.lastLogin = $util.Long.fromValue(object.lastLogin, false);
                else if (typeof object.lastLogin === "string")
                    message.lastLogin = parseInt(object.lastLogin, 10);
                else if (typeof object.lastLogin === "number")
                    message.lastLogin = object.lastLogin;
                else if (typeof object.lastLogin === "object")
                    message.lastLogin = new $util.LongBits(object.lastLogin.low >>> 0, object.lastLogin.high >>> 0).toNumber();
            switch (object.deviceStatus) {
            default:
                if (typeof object.deviceStatus === "number") {
                    message.deviceStatus = object.deviceStatus;
                    break;
                }
                break;
            case "DEVICE_NEEDS_APPROVAL":
            case 0:
                message.deviceStatus = 0;
                break;
            case "DEVICE_OK":
            case 1:
                message.deviceStatus = 1;
                break;
            case "DEVICE_DISABLED_BY_USER":
            case 2:
                message.deviceStatus = 2;
                break;
            case "DEVICE_LOCKED_BY_ADMIN":
            case 3:
                message.deviceStatus = 3;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a DeviceInformation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.DeviceInformation
         * @static
         * @param {Authentication.DeviceInformation} message DeviceInformation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeviceInformation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.deviceId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.deviceId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.deviceName = "";
                object.clientVersion = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastLogin = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastLogin = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.deviceStatus = options.enums === String ? "DEVICE_NEEDS_APPROVAL" : 0;
            }
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.deviceId = typeof message.deviceId === "number" ? BigInt(message.deviceId) : $util.Long.fromBits(message.deviceId.low >>> 0, message.deviceId.high >>> 0, false).toBigInt();
                else if (typeof message.deviceId === "number")
                    object.deviceId = options.longs === String ? String(message.deviceId) : message.deviceId;
                else
                    object.deviceId = options.longs === String ? $util.Long.prototype.toString.call(message.deviceId) : options.longs === Number ? new $util.LongBits(message.deviceId.low >>> 0, message.deviceId.high >>> 0).toNumber() : message.deviceId;
            if (message.deviceName != null && Object.hasOwnProperty.call(message, "deviceName"))
                object.deviceName = message.deviceName;
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                object.clientVersion = message.clientVersion;
            if (message.lastLogin != null && Object.hasOwnProperty.call(message, "lastLogin"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastLogin = typeof message.lastLogin === "number" ? BigInt(message.lastLogin) : $util.Long.fromBits(message.lastLogin.low >>> 0, message.lastLogin.high >>> 0, false).toBigInt();
                else if (typeof message.lastLogin === "number")
                    object.lastLogin = options.longs === String ? String(message.lastLogin) : message.lastLogin;
                else
                    object.lastLogin = options.longs === String ? $util.Long.prototype.toString.call(message.lastLogin) : options.longs === Number ? new $util.LongBits(message.lastLogin.low >>> 0, message.lastLogin.high >>> 0).toNumber() : message.lastLogin;
            if (message.deviceStatus != null && Object.hasOwnProperty.call(message, "deviceStatus"))
                object.deviceStatus = options.enums === String ? $root.Authentication.DeviceStatus[message.deviceStatus] === undefined ? message.deviceStatus : $root.Authentication.DeviceStatus[message.deviceStatus] : message.deviceStatus;
            return object;
        };

        /**
         * Converts this DeviceInformation to JSON.
         * @function toJSON
         * @memberof Authentication.DeviceInformation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeviceInformation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeviceInformation
         * @function getTypeUrl
         * @memberof Authentication.DeviceInformation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeviceInformation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.DeviceInformation";
        };

        return DeviceInformation;
    })();

    Authentication.UserSetting = (function() {

        /**
         * Properties of a UserSetting.
         * @memberof Authentication
         * @interface IUserSetting
         * @property {string|null} [name] UserSetting name
         * @property {boolean|null} [value] UserSetting value
         */

        /**
         * Constructs a new UserSetting.
         * @memberof Authentication
         * @classdesc Represents a UserSetting.
         * @implements IUserSetting
         * @constructor
         * @param {Authentication.IUserSetting=} [properties] Properties to set
         */
        function UserSetting(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UserSetting name.
         * @member {string} name
         * @memberof Authentication.UserSetting
         * @instance
         */
        UserSetting.prototype.name = "";

        /**
         * UserSetting value.
         * @member {boolean} value
         * @memberof Authentication.UserSetting
         * @instance
         */
        UserSetting.prototype.value = false;

        /**
         * Creates a new UserSetting instance using the specified properties.
         * @function create
         * @memberof Authentication.UserSetting
         * @static
         * @param {Authentication.IUserSetting=} [properties] Properties to set
         * @returns {Authentication.UserSetting} UserSetting instance
         */
        UserSetting.create = function create(properties) {
            return new UserSetting(properties);
        };

        /**
         * Encodes the specified UserSetting message. Does not implicitly {@link Authentication.UserSetting.verify|verify} messages.
         * @function encode
         * @memberof Authentication.UserSetting
         * @static
         * @param {Authentication.IUserSetting} message UserSetting message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserSetting.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.value);
            return writer;
        };

        /**
         * Decodes a UserSetting message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.UserSetting
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.UserSetting} UserSetting
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserSetting.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.UserSetting();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.name = reader.string();
                        break;
                    }
                case 2: {
                        message.value = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a UserSetting message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.UserSetting
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.UserSetting} UserSetting
         */
        UserSetting.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.UserSetting)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.UserSetting: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.UserSetting();
            if (object.name != null)
                message.name = String(object.name);
            if (object.value != null)
                message.value = Boolean(object.value);
            return message;
        };

        /**
         * Creates a plain object from a UserSetting message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.UserSetting
         * @static
         * @param {Authentication.UserSetting} message UserSetting
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UserSetting.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.name = "";
                object.value = false;
            }
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                object.value = message.value;
            return object;
        };

        /**
         * Converts this UserSetting to JSON.
         * @function toJSON
         * @memberof Authentication.UserSetting
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UserSetting.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UserSetting
         * @function getTypeUrl
         * @memberof Authentication.UserSetting
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UserSetting.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.UserSetting";
        };

        return UserSetting;
    })();

    Authentication.UserDataKeyRequest = (function() {

        /**
         * Properties of a UserDataKeyRequest.
         * @memberof Authentication
         * @interface IUserDataKeyRequest
         * @property {Array.<number>|null} [enterpriseUserId] UserDataKeyRequest enterpriseUserId
         */

        /**
         * Constructs a new UserDataKeyRequest.
         * @memberof Authentication
         * @classdesc Represents a UserDataKeyRequest.
         * @implements IUserDataKeyRequest
         * @constructor
         * @param {Authentication.IUserDataKeyRequest=} [properties] Properties to set
         */
        function UserDataKeyRequest(properties) {
            this.enterpriseUserId = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UserDataKeyRequest enterpriseUserId.
         * @member {Array.<number>} enterpriseUserId
         * @memberof Authentication.UserDataKeyRequest
         * @instance
         */
        UserDataKeyRequest.prototype.enterpriseUserId = $util.emptyArray;

        /**
         * Creates a new UserDataKeyRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.UserDataKeyRequest
         * @static
         * @param {Authentication.IUserDataKeyRequest=} [properties] Properties to set
         * @returns {Authentication.UserDataKeyRequest} UserDataKeyRequest instance
         */
        UserDataKeyRequest.create = function create(properties) {
            return new UserDataKeyRequest(properties);
        };

        /**
         * Encodes the specified UserDataKeyRequest message. Does not implicitly {@link Authentication.UserDataKeyRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.UserDataKeyRequest
         * @static
         * @param {Authentication.IUserDataKeyRequest} message UserDataKeyRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserDataKeyRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.enterpriseUserId != null && message.enterpriseUserId.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.enterpriseUserId.length; ++i)
                    writer.int64(message.enterpriseUserId[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Decodes a UserDataKeyRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.UserDataKeyRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.UserDataKeyRequest} UserDataKeyRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserDataKeyRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.UserDataKeyRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.enterpriseUserId && message.enterpriseUserId.length))
                            message.enterpriseUserId = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.enterpriseUserId.push(reader.int64());
                        } else
                            message.enterpriseUserId.push(reader.int64());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a UserDataKeyRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.UserDataKeyRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.UserDataKeyRequest} UserDataKeyRequest
         */
        UserDataKeyRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.UserDataKeyRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.UserDataKeyRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.UserDataKeyRequest();
            if (object.enterpriseUserId) {
                if (!Array.isArray(object.enterpriseUserId))
                    throw TypeError(".Authentication.UserDataKeyRequest.enterpriseUserId: array expected");
                message.enterpriseUserId = [];
                for (let i = 0; i < object.enterpriseUserId.length; ++i)
                    if ($util.Long)
                        message.enterpriseUserId[i] = $util.Long.fromValue(object.enterpriseUserId[i], false);
                    else if (typeof object.enterpriseUserId[i] === "string")
                        message.enterpriseUserId[i] = parseInt(object.enterpriseUserId[i], 10);
                    else if (typeof object.enterpriseUserId[i] === "number")
                        message.enterpriseUserId[i] = object.enterpriseUserId[i];
                    else if (typeof object.enterpriseUserId[i] === "object")
                        message.enterpriseUserId[i] = new $util.LongBits(object.enterpriseUserId[i].low >>> 0, object.enterpriseUserId[i].high >>> 0).toNumber();
            }
            return message;
        };

        /**
         * Creates a plain object from a UserDataKeyRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.UserDataKeyRequest
         * @static
         * @param {Authentication.UserDataKeyRequest} message UserDataKeyRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UserDataKeyRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.enterpriseUserId = [];
            if (message.enterpriseUserId && message.enterpriseUserId.length) {
                object.enterpriseUserId = [];
                for (let j = 0; j < message.enterpriseUserId.length; ++j)
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.enterpriseUserId[j] = typeof message.enterpriseUserId[j] === "number" ? BigInt(message.enterpriseUserId[j]) : $util.Long.fromBits(message.enterpriseUserId[j].low >>> 0, message.enterpriseUserId[j].high >>> 0, false).toBigInt();
                    else if (typeof message.enterpriseUserId[j] === "number")
                        object.enterpriseUserId[j] = options.longs === String ? String(message.enterpriseUserId[j]) : message.enterpriseUserId[j];
                    else
                        object.enterpriseUserId[j] = options.longs === String ? $util.Long.prototype.toString.call(message.enterpriseUserId[j]) : options.longs === Number ? new $util.LongBits(message.enterpriseUserId[j].low >>> 0, message.enterpriseUserId[j].high >>> 0).toNumber() : message.enterpriseUserId[j];
            }
            return object;
        };

        /**
         * Converts this UserDataKeyRequest to JSON.
         * @function toJSON
         * @memberof Authentication.UserDataKeyRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UserDataKeyRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UserDataKeyRequest
         * @function getTypeUrl
         * @memberof Authentication.UserDataKeyRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UserDataKeyRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.UserDataKeyRequest";
        };

        return UserDataKeyRequest;
    })();

    Authentication.UserDataKeyByNodeRequest = (function() {

        /**
         * Properties of a UserDataKeyByNodeRequest.
         * @memberof Authentication
         * @interface IUserDataKeyByNodeRequest
         * @property {Array.<number>|null} [nodeIds] UserDataKeyByNodeRequest nodeIds
         */

        /**
         * Constructs a new UserDataKeyByNodeRequest.
         * @memberof Authentication
         * @classdesc Represents a UserDataKeyByNodeRequest.
         * @implements IUserDataKeyByNodeRequest
         * @constructor
         * @param {Authentication.IUserDataKeyByNodeRequest=} [properties] Properties to set
         */
        function UserDataKeyByNodeRequest(properties) {
            this.nodeIds = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UserDataKeyByNodeRequest nodeIds.
         * @member {Array.<number>} nodeIds
         * @memberof Authentication.UserDataKeyByNodeRequest
         * @instance
         */
        UserDataKeyByNodeRequest.prototype.nodeIds = $util.emptyArray;

        /**
         * Creates a new UserDataKeyByNodeRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.UserDataKeyByNodeRequest
         * @static
         * @param {Authentication.IUserDataKeyByNodeRequest=} [properties] Properties to set
         * @returns {Authentication.UserDataKeyByNodeRequest} UserDataKeyByNodeRequest instance
         */
        UserDataKeyByNodeRequest.create = function create(properties) {
            return new UserDataKeyByNodeRequest(properties);
        };

        /**
         * Encodes the specified UserDataKeyByNodeRequest message. Does not implicitly {@link Authentication.UserDataKeyByNodeRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.UserDataKeyByNodeRequest
         * @static
         * @param {Authentication.IUserDataKeyByNodeRequest} message UserDataKeyByNodeRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserDataKeyByNodeRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.nodeIds != null && message.nodeIds.length) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork();
                for (let i = 0; i < message.nodeIds.length; ++i)
                    writer.int64(message.nodeIds[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Decodes a UserDataKeyByNodeRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.UserDataKeyByNodeRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.UserDataKeyByNodeRequest} UserDataKeyByNodeRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserDataKeyByNodeRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.UserDataKeyByNodeRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.nodeIds && message.nodeIds.length))
                            message.nodeIds = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.nodeIds.push(reader.int64());
                        } else
                            message.nodeIds.push(reader.int64());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a UserDataKeyByNodeRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.UserDataKeyByNodeRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.UserDataKeyByNodeRequest} UserDataKeyByNodeRequest
         */
        UserDataKeyByNodeRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.UserDataKeyByNodeRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.UserDataKeyByNodeRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.UserDataKeyByNodeRequest();
            if (object.nodeIds) {
                if (!Array.isArray(object.nodeIds))
                    throw TypeError(".Authentication.UserDataKeyByNodeRequest.nodeIds: array expected");
                message.nodeIds = [];
                for (let i = 0; i < object.nodeIds.length; ++i)
                    if ($util.Long)
                        message.nodeIds[i] = $util.Long.fromValue(object.nodeIds[i], false);
                    else if (typeof object.nodeIds[i] === "string")
                        message.nodeIds[i] = parseInt(object.nodeIds[i], 10);
                    else if (typeof object.nodeIds[i] === "number")
                        message.nodeIds[i] = object.nodeIds[i];
                    else if (typeof object.nodeIds[i] === "object")
                        message.nodeIds[i] = new $util.LongBits(object.nodeIds[i].low >>> 0, object.nodeIds[i].high >>> 0).toNumber();
            }
            return message;
        };

        /**
         * Creates a plain object from a UserDataKeyByNodeRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.UserDataKeyByNodeRequest
         * @static
         * @param {Authentication.UserDataKeyByNodeRequest} message UserDataKeyByNodeRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UserDataKeyByNodeRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.nodeIds = [];
            if (message.nodeIds && message.nodeIds.length) {
                object.nodeIds = [];
                for (let j = 0; j < message.nodeIds.length; ++j)
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.nodeIds[j] = typeof message.nodeIds[j] === "number" ? BigInt(message.nodeIds[j]) : $util.Long.fromBits(message.nodeIds[j].low >>> 0, message.nodeIds[j].high >>> 0, false).toBigInt();
                    else if (typeof message.nodeIds[j] === "number")
                        object.nodeIds[j] = options.longs === String ? String(message.nodeIds[j]) : message.nodeIds[j];
                    else
                        object.nodeIds[j] = options.longs === String ? $util.Long.prototype.toString.call(message.nodeIds[j]) : options.longs === Number ? new $util.LongBits(message.nodeIds[j].low >>> 0, message.nodeIds[j].high >>> 0).toNumber() : message.nodeIds[j];
            }
            return object;
        };

        /**
         * Converts this UserDataKeyByNodeRequest to JSON.
         * @function toJSON
         * @memberof Authentication.UserDataKeyByNodeRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UserDataKeyByNodeRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UserDataKeyByNodeRequest
         * @function getTypeUrl
         * @memberof Authentication.UserDataKeyByNodeRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UserDataKeyByNodeRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.UserDataKeyByNodeRequest";
        };

        return UserDataKeyByNodeRequest;
    })();

    Authentication.EnterpriseUserIdDataKeyPair = (function() {

        /**
         * Properties of an EnterpriseUserIdDataKeyPair.
         * @memberof Authentication
         * @interface IEnterpriseUserIdDataKeyPair
         * @property {number|null} [enterpriseUserId] EnterpriseUserIdDataKeyPair enterpriseUserId
         * @property {Uint8Array|null} [encryptedDataKey] EnterpriseUserIdDataKeyPair encryptedDataKey
         * @property {Enterprise.EncryptedKeyType|null} [keyType] EnterpriseUserIdDataKeyPair keyType
         */

        /**
         * Constructs a new EnterpriseUserIdDataKeyPair.
         * @memberof Authentication
         * @classdesc Represents an EnterpriseUserIdDataKeyPair.
         * @implements IEnterpriseUserIdDataKeyPair
         * @constructor
         * @param {Authentication.IEnterpriseUserIdDataKeyPair=} [properties] Properties to set
         */
        function EnterpriseUserIdDataKeyPair(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EnterpriseUserIdDataKeyPair enterpriseUserId.
         * @member {number} enterpriseUserId
         * @memberof Authentication.EnterpriseUserIdDataKeyPair
         * @instance
         */
        EnterpriseUserIdDataKeyPair.prototype.enterpriseUserId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EnterpriseUserIdDataKeyPair encryptedDataKey.
         * @member {Uint8Array} encryptedDataKey
         * @memberof Authentication.EnterpriseUserIdDataKeyPair
         * @instance
         */
        EnterpriseUserIdDataKeyPair.prototype.encryptedDataKey = $util.newBuffer([]);

        /**
         * EnterpriseUserIdDataKeyPair keyType.
         * @member {Enterprise.EncryptedKeyType} keyType
         * @memberof Authentication.EnterpriseUserIdDataKeyPair
         * @instance
         */
        EnterpriseUserIdDataKeyPair.prototype.keyType = 0;

        /**
         * Creates a new EnterpriseUserIdDataKeyPair instance using the specified properties.
         * @function create
         * @memberof Authentication.EnterpriseUserIdDataKeyPair
         * @static
         * @param {Authentication.IEnterpriseUserIdDataKeyPair=} [properties] Properties to set
         * @returns {Authentication.EnterpriseUserIdDataKeyPair} EnterpriseUserIdDataKeyPair instance
         */
        EnterpriseUserIdDataKeyPair.create = function create(properties) {
            return new EnterpriseUserIdDataKeyPair(properties);
        };

        /**
         * Encodes the specified EnterpriseUserIdDataKeyPair message. Does not implicitly {@link Authentication.EnterpriseUserIdDataKeyPair.verify|verify} messages.
         * @function encode
         * @memberof Authentication.EnterpriseUserIdDataKeyPair
         * @static
         * @param {Authentication.IEnterpriseUserIdDataKeyPair} message EnterpriseUserIdDataKeyPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterpriseUserIdDataKeyPair.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.enterpriseUserId);
            if (message.encryptedDataKey != null && Object.hasOwnProperty.call(message, "encryptedDataKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encryptedDataKey);
            if (message.keyType != null && Object.hasOwnProperty.call(message, "keyType"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.keyType);
            return writer;
        };

        /**
         * Decodes an EnterpriseUserIdDataKeyPair message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.EnterpriseUserIdDataKeyPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.EnterpriseUserIdDataKeyPair} EnterpriseUserIdDataKeyPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterpriseUserIdDataKeyPair.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.EnterpriseUserIdDataKeyPair();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.enterpriseUserId = reader.int64();
                        break;
                    }
                case 2: {
                        message.encryptedDataKey = reader.bytes();
                        break;
                    }
                case 3: {
                        message.keyType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an EnterpriseUserIdDataKeyPair message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.EnterpriseUserIdDataKeyPair
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.EnterpriseUserIdDataKeyPair} EnterpriseUserIdDataKeyPair
         */
        EnterpriseUserIdDataKeyPair.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.EnterpriseUserIdDataKeyPair)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.EnterpriseUserIdDataKeyPair: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.EnterpriseUserIdDataKeyPair();
            if (object.enterpriseUserId != null)
                if ($util.Long)
                    message.enterpriseUserId = $util.Long.fromValue(object.enterpriseUserId, false);
                else if (typeof object.enterpriseUserId === "string")
                    message.enterpriseUserId = parseInt(object.enterpriseUserId, 10);
                else if (typeof object.enterpriseUserId === "number")
                    message.enterpriseUserId = object.enterpriseUserId;
                else if (typeof object.enterpriseUserId === "object")
                    message.enterpriseUserId = new $util.LongBits(object.enterpriseUserId.low >>> 0, object.enterpriseUserId.high >>> 0).toNumber();
            if (object.encryptedDataKey != null)
                if (typeof object.encryptedDataKey === "string")
                    $util.base64.decode(object.encryptedDataKey, message.encryptedDataKey = $util.newBuffer($util.base64.length(object.encryptedDataKey)), 0);
                else if (object.encryptedDataKey.length >= 0)
                    message.encryptedDataKey = object.encryptedDataKey;
            switch (object.keyType) {
            default:
                if (typeof object.keyType === "number") {
                    message.keyType = object.keyType;
                    break;
                }
                break;
            case "KT_NO_KEY":
            case 0:
                message.keyType = 0;
                break;
            case "KT_ENCRYPTED_BY_DATA_KEY":
            case 1:
                message.keyType = 1;
                break;
            case "KT_ENCRYPTED_BY_PUBLIC_KEY":
            case 2:
                message.keyType = 2;
                break;
            case "KT_ENCRYPTED_BY_DATA_KEY_GCM":
            case 3:
                message.keyType = 3;
                break;
            case "KT_ENCRYPTED_BY_PUBLIC_KEY_ECC":
            case 4:
                message.keyType = 4;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from an EnterpriseUserIdDataKeyPair message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.EnterpriseUserIdDataKeyPair
         * @static
         * @param {Authentication.EnterpriseUserIdDataKeyPair} message EnterpriseUserIdDataKeyPair
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EnterpriseUserIdDataKeyPair.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.enterpriseUserId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.enterpriseUserId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.encryptedDataKey = "";
                else {
                    object.encryptedDataKey = [];
                    if (options.bytes !== Array)
                        object.encryptedDataKey = $util.newBuffer(object.encryptedDataKey);
                }
                object.keyType = options.enums === String ? "KT_NO_KEY" : 0;
            }
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.enterpriseUserId = typeof message.enterpriseUserId === "number" ? BigInt(message.enterpriseUserId) : $util.Long.fromBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0, false).toBigInt();
                else if (typeof message.enterpriseUserId === "number")
                    object.enterpriseUserId = options.longs === String ? String(message.enterpriseUserId) : message.enterpriseUserId;
                else
                    object.enterpriseUserId = options.longs === String ? $util.Long.prototype.toString.call(message.enterpriseUserId) : options.longs === Number ? new $util.LongBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0).toNumber() : message.enterpriseUserId;
            if (message.encryptedDataKey != null && Object.hasOwnProperty.call(message, "encryptedDataKey"))
                object.encryptedDataKey = options.bytes === String ? $util.base64.encode(message.encryptedDataKey, 0, message.encryptedDataKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDataKey) : message.encryptedDataKey;
            if (message.keyType != null && Object.hasOwnProperty.call(message, "keyType"))
                object.keyType = options.enums === String ? $root.Enterprise.EncryptedKeyType[message.keyType] === undefined ? message.keyType : $root.Enterprise.EncryptedKeyType[message.keyType] : message.keyType;
            return object;
        };

        /**
         * Converts this EnterpriseUserIdDataKeyPair to JSON.
         * @function toJSON
         * @memberof Authentication.EnterpriseUserIdDataKeyPair
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EnterpriseUserIdDataKeyPair.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EnterpriseUserIdDataKeyPair
         * @function getTypeUrl
         * @memberof Authentication.EnterpriseUserIdDataKeyPair
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EnterpriseUserIdDataKeyPair.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.EnterpriseUserIdDataKeyPair";
        };

        return EnterpriseUserIdDataKeyPair;
    })();

    Authentication.UserDataKey = (function() {

        /**
         * Properties of a UserDataKey.
         * @memberof Authentication
         * @interface IUserDataKey
         * @property {number|null} [roleId] UserDataKey roleId
         * @property {Uint8Array|null} [roleKey] UserDataKey roleKey
         * @property {string|null} [privateKey] UserDataKey privateKey
         * @property {Array.<Authentication.IEnterpriseUserIdDataKeyPair>|null} [enterpriseUserIdDataKeyPairs] UserDataKey enterpriseUserIdDataKeyPairs
         */

        /**
         * Constructs a new UserDataKey.
         * @memberof Authentication
         * @classdesc Represents a UserDataKey.
         * @implements IUserDataKey
         * @constructor
         * @param {Authentication.IUserDataKey=} [properties] Properties to set
         */
        function UserDataKey(properties) {
            this.enterpriseUserIdDataKeyPairs = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UserDataKey roleId.
         * @member {number} roleId
         * @memberof Authentication.UserDataKey
         * @instance
         */
        UserDataKey.prototype.roleId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * UserDataKey roleKey.
         * @member {Uint8Array} roleKey
         * @memberof Authentication.UserDataKey
         * @instance
         */
        UserDataKey.prototype.roleKey = $util.newBuffer([]);

        /**
         * UserDataKey privateKey.
         * @member {string} privateKey
         * @memberof Authentication.UserDataKey
         * @instance
         */
        UserDataKey.prototype.privateKey = "";

        /**
         * UserDataKey enterpriseUserIdDataKeyPairs.
         * @member {Array.<Authentication.IEnterpriseUserIdDataKeyPair>} enterpriseUserIdDataKeyPairs
         * @memberof Authentication.UserDataKey
         * @instance
         */
        UserDataKey.prototype.enterpriseUserIdDataKeyPairs = $util.emptyArray;

        /**
         * Creates a new UserDataKey instance using the specified properties.
         * @function create
         * @memberof Authentication.UserDataKey
         * @static
         * @param {Authentication.IUserDataKey=} [properties] Properties to set
         * @returns {Authentication.UserDataKey} UserDataKey instance
         */
        UserDataKey.create = function create(properties) {
            return new UserDataKey(properties);
        };

        /**
         * Encodes the specified UserDataKey message. Does not implicitly {@link Authentication.UserDataKey.verify|verify} messages.
         * @function encode
         * @memberof Authentication.UserDataKey
         * @static
         * @param {Authentication.IUserDataKey} message UserDataKey message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserDataKey.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.roleId != null && Object.hasOwnProperty.call(message, "roleId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.roleId);
            if (message.roleKey != null && Object.hasOwnProperty.call(message, "roleKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.roleKey);
            if (message.privateKey != null && Object.hasOwnProperty.call(message, "privateKey"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.privateKey);
            if (message.enterpriseUserIdDataKeyPairs != null && message.enterpriseUserIdDataKeyPairs.length)
                for (let i = 0; i < message.enterpriseUserIdDataKeyPairs.length; ++i)
                    $root.Authentication.EnterpriseUserIdDataKeyPair.encode(message.enterpriseUserIdDataKeyPairs[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes a UserDataKey message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.UserDataKey
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.UserDataKey} UserDataKey
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserDataKey.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.UserDataKey();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.roleId = reader.int64();
                        break;
                    }
                case 2: {
                        message.roleKey = reader.bytes();
                        break;
                    }
                case 3: {
                        message.privateKey = reader.string();
                        break;
                    }
                case 4: {
                        if (!(message.enterpriseUserIdDataKeyPairs && message.enterpriseUserIdDataKeyPairs.length))
                            message.enterpriseUserIdDataKeyPairs = [];
                        message.enterpriseUserIdDataKeyPairs.push($root.Authentication.EnterpriseUserIdDataKeyPair.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a UserDataKey message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.UserDataKey
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.UserDataKey} UserDataKey
         */
        UserDataKey.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.UserDataKey)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.UserDataKey: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.UserDataKey();
            if (object.roleId != null)
                if ($util.Long)
                    message.roleId = $util.Long.fromValue(object.roleId, false);
                else if (typeof object.roleId === "string")
                    message.roleId = parseInt(object.roleId, 10);
                else if (typeof object.roleId === "number")
                    message.roleId = object.roleId;
                else if (typeof object.roleId === "object")
                    message.roleId = new $util.LongBits(object.roleId.low >>> 0, object.roleId.high >>> 0).toNumber();
            if (object.roleKey != null)
                if (typeof object.roleKey === "string")
                    $util.base64.decode(object.roleKey, message.roleKey = $util.newBuffer($util.base64.length(object.roleKey)), 0);
                else if (object.roleKey.length >= 0)
                    message.roleKey = object.roleKey;
            if (object.privateKey != null)
                message.privateKey = String(object.privateKey);
            if (object.enterpriseUserIdDataKeyPairs) {
                if (!Array.isArray(object.enterpriseUserIdDataKeyPairs))
                    throw TypeError(".Authentication.UserDataKey.enterpriseUserIdDataKeyPairs: array expected");
                message.enterpriseUserIdDataKeyPairs = [];
                for (let i = 0; i < object.enterpriseUserIdDataKeyPairs.length; ++i) {
                    if (!$util.isObject(object.enterpriseUserIdDataKeyPairs[i]))
                        throw TypeError(".Authentication.UserDataKey.enterpriseUserIdDataKeyPairs: object expected");
                    message.enterpriseUserIdDataKeyPairs[i] = $root.Authentication.EnterpriseUserIdDataKeyPair.fromObject(object.enterpriseUserIdDataKeyPairs[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a UserDataKey message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.UserDataKey
         * @static
         * @param {Authentication.UserDataKey} message UserDataKey
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UserDataKey.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.enterpriseUserIdDataKeyPairs = [];
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.roleId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.roleId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.roleKey = "";
                else {
                    object.roleKey = [];
                    if (options.bytes !== Array)
                        object.roleKey = $util.newBuffer(object.roleKey);
                }
                object.privateKey = "";
            }
            if (message.roleId != null && Object.hasOwnProperty.call(message, "roleId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.roleId = typeof message.roleId === "number" ? BigInt(message.roleId) : $util.Long.fromBits(message.roleId.low >>> 0, message.roleId.high >>> 0, false).toBigInt();
                else if (typeof message.roleId === "number")
                    object.roleId = options.longs === String ? String(message.roleId) : message.roleId;
                else
                    object.roleId = options.longs === String ? $util.Long.prototype.toString.call(message.roleId) : options.longs === Number ? new $util.LongBits(message.roleId.low >>> 0, message.roleId.high >>> 0).toNumber() : message.roleId;
            if (message.roleKey != null && Object.hasOwnProperty.call(message, "roleKey"))
                object.roleKey = options.bytes === String ? $util.base64.encode(message.roleKey, 0, message.roleKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.roleKey) : message.roleKey;
            if (message.privateKey != null && Object.hasOwnProperty.call(message, "privateKey"))
                object.privateKey = message.privateKey;
            if (message.enterpriseUserIdDataKeyPairs && message.enterpriseUserIdDataKeyPairs.length) {
                object.enterpriseUserIdDataKeyPairs = [];
                for (let j = 0; j < message.enterpriseUserIdDataKeyPairs.length; ++j)
                    object.enterpriseUserIdDataKeyPairs[j] = $root.Authentication.EnterpriseUserIdDataKeyPair.toObject(message.enterpriseUserIdDataKeyPairs[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this UserDataKey to JSON.
         * @function toJSON
         * @memberof Authentication.UserDataKey
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UserDataKey.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UserDataKey
         * @function getTypeUrl
         * @memberof Authentication.UserDataKey
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UserDataKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.UserDataKey";
        };

        return UserDataKey;
    })();

    Authentication.UserDataKeyResponse = (function() {

        /**
         * Properties of a UserDataKeyResponse.
         * @memberof Authentication
         * @interface IUserDataKeyResponse
         * @property {Array.<Authentication.IUserDataKey>|null} [userDataKeys] UserDataKeyResponse userDataKeys
         * @property {Array.<number>|null} [accessDenied] UserDataKeyResponse accessDenied
         * @property {Array.<number>|null} [noEncryptedDataKey] UserDataKeyResponse noEncryptedDataKey
         */

        /**
         * Constructs a new UserDataKeyResponse.
         * @memberof Authentication
         * @classdesc Represents a UserDataKeyResponse.
         * @implements IUserDataKeyResponse
         * @constructor
         * @param {Authentication.IUserDataKeyResponse=} [properties] Properties to set
         */
        function UserDataKeyResponse(properties) {
            this.userDataKeys = [];
            this.accessDenied = [];
            this.noEncryptedDataKey = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UserDataKeyResponse userDataKeys.
         * @member {Array.<Authentication.IUserDataKey>} userDataKeys
         * @memberof Authentication.UserDataKeyResponse
         * @instance
         */
        UserDataKeyResponse.prototype.userDataKeys = $util.emptyArray;

        /**
         * UserDataKeyResponse accessDenied.
         * @member {Array.<number>} accessDenied
         * @memberof Authentication.UserDataKeyResponse
         * @instance
         */
        UserDataKeyResponse.prototype.accessDenied = $util.emptyArray;

        /**
         * UserDataKeyResponse noEncryptedDataKey.
         * @member {Array.<number>} noEncryptedDataKey
         * @memberof Authentication.UserDataKeyResponse
         * @instance
         */
        UserDataKeyResponse.prototype.noEncryptedDataKey = $util.emptyArray;

        /**
         * Creates a new UserDataKeyResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.UserDataKeyResponse
         * @static
         * @param {Authentication.IUserDataKeyResponse=} [properties] Properties to set
         * @returns {Authentication.UserDataKeyResponse} UserDataKeyResponse instance
         */
        UserDataKeyResponse.create = function create(properties) {
            return new UserDataKeyResponse(properties);
        };

        /**
         * Encodes the specified UserDataKeyResponse message. Does not implicitly {@link Authentication.UserDataKeyResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.UserDataKeyResponse
         * @static
         * @param {Authentication.IUserDataKeyResponse} message UserDataKeyResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserDataKeyResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.userDataKeys != null && message.userDataKeys.length)
                for (let i = 0; i < message.userDataKeys.length; ++i)
                    $root.Authentication.UserDataKey.encode(message.userDataKeys[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.accessDenied != null && message.accessDenied.length) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork();
                for (let i = 0; i < message.accessDenied.length; ++i)
                    writer.int64(message.accessDenied[i]);
                writer.ldelim();
            }
            if (message.noEncryptedDataKey != null && message.noEncryptedDataKey.length) {
                writer.uint32(/* id 3, wireType 2 =*/26).fork();
                for (let i = 0; i < message.noEncryptedDataKey.length; ++i)
                    writer.int64(message.noEncryptedDataKey[i]);
                writer.ldelim();
            }
            return writer;
        };

        /**
         * Decodes a UserDataKeyResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.UserDataKeyResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.UserDataKeyResponse} UserDataKeyResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserDataKeyResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.UserDataKeyResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.userDataKeys && message.userDataKeys.length))
                            message.userDataKeys = [];
                        message.userDataKeys.push($root.Authentication.UserDataKey.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 2: {
                        if (!(message.accessDenied && message.accessDenied.length))
                            message.accessDenied = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.accessDenied.push(reader.int64());
                        } else
                            message.accessDenied.push(reader.int64());
                        break;
                    }
                case 3: {
                        if (!(message.noEncryptedDataKey && message.noEncryptedDataKey.length))
                            message.noEncryptedDataKey = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.noEncryptedDataKey.push(reader.int64());
                        } else
                            message.noEncryptedDataKey.push(reader.int64());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a UserDataKeyResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.UserDataKeyResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.UserDataKeyResponse} UserDataKeyResponse
         */
        UserDataKeyResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.UserDataKeyResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.UserDataKeyResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.UserDataKeyResponse();
            if (object.userDataKeys) {
                if (!Array.isArray(object.userDataKeys))
                    throw TypeError(".Authentication.UserDataKeyResponse.userDataKeys: array expected");
                message.userDataKeys = [];
                for (let i = 0; i < object.userDataKeys.length; ++i) {
                    if (!$util.isObject(object.userDataKeys[i]))
                        throw TypeError(".Authentication.UserDataKeyResponse.userDataKeys: object expected");
                    message.userDataKeys[i] = $root.Authentication.UserDataKey.fromObject(object.userDataKeys[i], long + 1);
                }
            }
            if (object.accessDenied) {
                if (!Array.isArray(object.accessDenied))
                    throw TypeError(".Authentication.UserDataKeyResponse.accessDenied: array expected");
                message.accessDenied = [];
                for (let i = 0; i < object.accessDenied.length; ++i)
                    if ($util.Long)
                        message.accessDenied[i] = $util.Long.fromValue(object.accessDenied[i], false);
                    else if (typeof object.accessDenied[i] === "string")
                        message.accessDenied[i] = parseInt(object.accessDenied[i], 10);
                    else if (typeof object.accessDenied[i] === "number")
                        message.accessDenied[i] = object.accessDenied[i];
                    else if (typeof object.accessDenied[i] === "object")
                        message.accessDenied[i] = new $util.LongBits(object.accessDenied[i].low >>> 0, object.accessDenied[i].high >>> 0).toNumber();
            }
            if (object.noEncryptedDataKey) {
                if (!Array.isArray(object.noEncryptedDataKey))
                    throw TypeError(".Authentication.UserDataKeyResponse.noEncryptedDataKey: array expected");
                message.noEncryptedDataKey = [];
                for (let i = 0; i < object.noEncryptedDataKey.length; ++i)
                    if ($util.Long)
                        message.noEncryptedDataKey[i] = $util.Long.fromValue(object.noEncryptedDataKey[i], false);
                    else if (typeof object.noEncryptedDataKey[i] === "string")
                        message.noEncryptedDataKey[i] = parseInt(object.noEncryptedDataKey[i], 10);
                    else if (typeof object.noEncryptedDataKey[i] === "number")
                        message.noEncryptedDataKey[i] = object.noEncryptedDataKey[i];
                    else if (typeof object.noEncryptedDataKey[i] === "object")
                        message.noEncryptedDataKey[i] = new $util.LongBits(object.noEncryptedDataKey[i].low >>> 0, object.noEncryptedDataKey[i].high >>> 0).toNumber();
            }
            return message;
        };

        /**
         * Creates a plain object from a UserDataKeyResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.UserDataKeyResponse
         * @static
         * @param {Authentication.UserDataKeyResponse} message UserDataKeyResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UserDataKeyResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.userDataKeys = [];
                object.accessDenied = [];
                object.noEncryptedDataKey = [];
            }
            if (message.userDataKeys && message.userDataKeys.length) {
                object.userDataKeys = [];
                for (let j = 0; j < message.userDataKeys.length; ++j)
                    object.userDataKeys[j] = $root.Authentication.UserDataKey.toObject(message.userDataKeys[j], options, q + 1);
            }
            if (message.accessDenied && message.accessDenied.length) {
                object.accessDenied = [];
                for (let j = 0; j < message.accessDenied.length; ++j)
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.accessDenied[j] = typeof message.accessDenied[j] === "number" ? BigInt(message.accessDenied[j]) : $util.Long.fromBits(message.accessDenied[j].low >>> 0, message.accessDenied[j].high >>> 0, false).toBigInt();
                    else if (typeof message.accessDenied[j] === "number")
                        object.accessDenied[j] = options.longs === String ? String(message.accessDenied[j]) : message.accessDenied[j];
                    else
                        object.accessDenied[j] = options.longs === String ? $util.Long.prototype.toString.call(message.accessDenied[j]) : options.longs === Number ? new $util.LongBits(message.accessDenied[j].low >>> 0, message.accessDenied[j].high >>> 0).toNumber() : message.accessDenied[j];
            }
            if (message.noEncryptedDataKey && message.noEncryptedDataKey.length) {
                object.noEncryptedDataKey = [];
                for (let j = 0; j < message.noEncryptedDataKey.length; ++j)
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.noEncryptedDataKey[j] = typeof message.noEncryptedDataKey[j] === "number" ? BigInt(message.noEncryptedDataKey[j]) : $util.Long.fromBits(message.noEncryptedDataKey[j].low >>> 0, message.noEncryptedDataKey[j].high >>> 0, false).toBigInt();
                    else if (typeof message.noEncryptedDataKey[j] === "number")
                        object.noEncryptedDataKey[j] = options.longs === String ? String(message.noEncryptedDataKey[j]) : message.noEncryptedDataKey[j];
                    else
                        object.noEncryptedDataKey[j] = options.longs === String ? $util.Long.prototype.toString.call(message.noEncryptedDataKey[j]) : options.longs === Number ? new $util.LongBits(message.noEncryptedDataKey[j].low >>> 0, message.noEncryptedDataKey[j].high >>> 0).toNumber() : message.noEncryptedDataKey[j];
            }
            return object;
        };

        /**
         * Converts this UserDataKeyResponse to JSON.
         * @function toJSON
         * @memberof Authentication.UserDataKeyResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UserDataKeyResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UserDataKeyResponse
         * @function getTypeUrl
         * @memberof Authentication.UserDataKeyResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UserDataKeyResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.UserDataKeyResponse";
        };

        return UserDataKeyResponse;
    })();

    Authentication.MasterPasswordRecoveryVerificationRequest = (function() {

        /**
         * Properties of a MasterPasswordRecoveryVerificationRequest.
         * @memberof Authentication
         * @interface IMasterPasswordRecoveryVerificationRequest
         * @property {Uint8Array|null} [encryptedLoginToken] MasterPasswordRecoveryVerificationRequest encryptedLoginToken
         */

        /**
         * Constructs a new MasterPasswordRecoveryVerificationRequest.
         * @memberof Authentication
         * @classdesc Represents a MasterPasswordRecoveryVerificationRequest.
         * @implements IMasterPasswordRecoveryVerificationRequest
         * @constructor
         * @param {Authentication.IMasterPasswordRecoveryVerificationRequest=} [properties] Properties to set
         */
        function MasterPasswordRecoveryVerificationRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MasterPasswordRecoveryVerificationRequest encryptedLoginToken.
         * @member {Uint8Array} encryptedLoginToken
         * @memberof Authentication.MasterPasswordRecoveryVerificationRequest
         * @instance
         */
        MasterPasswordRecoveryVerificationRequest.prototype.encryptedLoginToken = $util.newBuffer([]);

        /**
         * Creates a new MasterPasswordRecoveryVerificationRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.MasterPasswordRecoveryVerificationRequest
         * @static
         * @param {Authentication.IMasterPasswordRecoveryVerificationRequest=} [properties] Properties to set
         * @returns {Authentication.MasterPasswordRecoveryVerificationRequest} MasterPasswordRecoveryVerificationRequest instance
         */
        MasterPasswordRecoveryVerificationRequest.create = function create(properties) {
            return new MasterPasswordRecoveryVerificationRequest(properties);
        };

        /**
         * Encodes the specified MasterPasswordRecoveryVerificationRequest message. Does not implicitly {@link Authentication.MasterPasswordRecoveryVerificationRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.MasterPasswordRecoveryVerificationRequest
         * @static
         * @param {Authentication.IMasterPasswordRecoveryVerificationRequest} message MasterPasswordRecoveryVerificationRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MasterPasswordRecoveryVerificationRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedLoginToken);
            return writer;
        };

        /**
         * Decodes a MasterPasswordRecoveryVerificationRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.MasterPasswordRecoveryVerificationRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.MasterPasswordRecoveryVerificationRequest} MasterPasswordRecoveryVerificationRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MasterPasswordRecoveryVerificationRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.MasterPasswordRecoveryVerificationRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedLoginToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a MasterPasswordRecoveryVerificationRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.MasterPasswordRecoveryVerificationRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.MasterPasswordRecoveryVerificationRequest} MasterPasswordRecoveryVerificationRequest
         */
        MasterPasswordRecoveryVerificationRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.MasterPasswordRecoveryVerificationRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.MasterPasswordRecoveryVerificationRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.MasterPasswordRecoveryVerificationRequest();
            if (object.encryptedLoginToken != null)
                if (typeof object.encryptedLoginToken === "string")
                    $util.base64.decode(object.encryptedLoginToken, message.encryptedLoginToken = $util.newBuffer($util.base64.length(object.encryptedLoginToken)), 0);
                else if (object.encryptedLoginToken.length >= 0)
                    message.encryptedLoginToken = object.encryptedLoginToken;
            return message;
        };

        /**
         * Creates a plain object from a MasterPasswordRecoveryVerificationRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.MasterPasswordRecoveryVerificationRequest
         * @static
         * @param {Authentication.MasterPasswordRecoveryVerificationRequest} message MasterPasswordRecoveryVerificationRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MasterPasswordRecoveryVerificationRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === String)
                    object.encryptedLoginToken = "";
                else {
                    object.encryptedLoginToken = [];
                    if (options.bytes !== Array)
                        object.encryptedLoginToken = $util.newBuffer(object.encryptedLoginToken);
                }
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                object.encryptedLoginToken = options.bytes === String ? $util.base64.encode(message.encryptedLoginToken, 0, message.encryptedLoginToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedLoginToken) : message.encryptedLoginToken;
            return object;
        };

        /**
         * Converts this MasterPasswordRecoveryVerificationRequest to JSON.
         * @function toJSON
         * @memberof Authentication.MasterPasswordRecoveryVerificationRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MasterPasswordRecoveryVerificationRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MasterPasswordRecoveryVerificationRequest
         * @function getTypeUrl
         * @memberof Authentication.MasterPasswordRecoveryVerificationRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MasterPasswordRecoveryVerificationRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.MasterPasswordRecoveryVerificationRequest";
        };

        return MasterPasswordRecoveryVerificationRequest;
    })();

    Authentication.GetSecurityQuestionV3Request = (function() {

        /**
         * Properties of a GetSecurityQuestionV3Request.
         * @memberof Authentication
         * @interface IGetSecurityQuestionV3Request
         * @property {Uint8Array|null} [encryptedLoginToken] GetSecurityQuestionV3Request encryptedLoginToken
         * @property {string|null} [verificationCode] GetSecurityQuestionV3Request verificationCode
         */

        /**
         * Constructs a new GetSecurityQuestionV3Request.
         * @memberof Authentication
         * @classdesc Represents a GetSecurityQuestionV3Request.
         * @implements IGetSecurityQuestionV3Request
         * @constructor
         * @param {Authentication.IGetSecurityQuestionV3Request=} [properties] Properties to set
         */
        function GetSecurityQuestionV3Request(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetSecurityQuestionV3Request encryptedLoginToken.
         * @member {Uint8Array} encryptedLoginToken
         * @memberof Authentication.GetSecurityQuestionV3Request
         * @instance
         */
        GetSecurityQuestionV3Request.prototype.encryptedLoginToken = $util.newBuffer([]);

        /**
         * GetSecurityQuestionV3Request verificationCode.
         * @member {string} verificationCode
         * @memberof Authentication.GetSecurityQuestionV3Request
         * @instance
         */
        GetSecurityQuestionV3Request.prototype.verificationCode = "";

        /**
         * Creates a new GetSecurityQuestionV3Request instance using the specified properties.
         * @function create
         * @memberof Authentication.GetSecurityQuestionV3Request
         * @static
         * @param {Authentication.IGetSecurityQuestionV3Request=} [properties] Properties to set
         * @returns {Authentication.GetSecurityQuestionV3Request} GetSecurityQuestionV3Request instance
         */
        GetSecurityQuestionV3Request.create = function create(properties) {
            return new GetSecurityQuestionV3Request(properties);
        };

        /**
         * Encodes the specified GetSecurityQuestionV3Request message. Does not implicitly {@link Authentication.GetSecurityQuestionV3Request.verify|verify} messages.
         * @function encode
         * @memberof Authentication.GetSecurityQuestionV3Request
         * @static
         * @param {Authentication.IGetSecurityQuestionV3Request} message GetSecurityQuestionV3Request message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetSecurityQuestionV3Request.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedLoginToken);
            if (message.verificationCode != null && Object.hasOwnProperty.call(message, "verificationCode"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.verificationCode);
            return writer;
        };

        /**
         * Decodes a GetSecurityQuestionV3Request message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.GetSecurityQuestionV3Request
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.GetSecurityQuestionV3Request} GetSecurityQuestionV3Request
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetSecurityQuestionV3Request.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.GetSecurityQuestionV3Request();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedLoginToken = reader.bytes();
                        break;
                    }
                case 2: {
                        message.verificationCode = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a GetSecurityQuestionV3Request message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.GetSecurityQuestionV3Request
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.GetSecurityQuestionV3Request} GetSecurityQuestionV3Request
         */
        GetSecurityQuestionV3Request.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.GetSecurityQuestionV3Request)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.GetSecurityQuestionV3Request: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.GetSecurityQuestionV3Request();
            if (object.encryptedLoginToken != null)
                if (typeof object.encryptedLoginToken === "string")
                    $util.base64.decode(object.encryptedLoginToken, message.encryptedLoginToken = $util.newBuffer($util.base64.length(object.encryptedLoginToken)), 0);
                else if (object.encryptedLoginToken.length >= 0)
                    message.encryptedLoginToken = object.encryptedLoginToken;
            if (object.verificationCode != null)
                message.verificationCode = String(object.verificationCode);
            return message;
        };

        /**
         * Creates a plain object from a GetSecurityQuestionV3Request message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.GetSecurityQuestionV3Request
         * @static
         * @param {Authentication.GetSecurityQuestionV3Request} message GetSecurityQuestionV3Request
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetSecurityQuestionV3Request.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.encryptedLoginToken = "";
                else {
                    object.encryptedLoginToken = [];
                    if (options.bytes !== Array)
                        object.encryptedLoginToken = $util.newBuffer(object.encryptedLoginToken);
                }
                object.verificationCode = "";
            }
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                object.encryptedLoginToken = options.bytes === String ? $util.base64.encode(message.encryptedLoginToken, 0, message.encryptedLoginToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedLoginToken) : message.encryptedLoginToken;
            if (message.verificationCode != null && Object.hasOwnProperty.call(message, "verificationCode"))
                object.verificationCode = message.verificationCode;
            return object;
        };

        /**
         * Converts this GetSecurityQuestionV3Request to JSON.
         * @function toJSON
         * @memberof Authentication.GetSecurityQuestionV3Request
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetSecurityQuestionV3Request.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetSecurityQuestionV3Request
         * @function getTypeUrl
         * @memberof Authentication.GetSecurityQuestionV3Request
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetSecurityQuestionV3Request.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.GetSecurityQuestionV3Request";
        };

        return GetSecurityQuestionV3Request;
    })();

    Authentication.GetSecurityQuestionV3Response = (function() {

        /**
         * Properties of a GetSecurityQuestionV3Response.
         * @memberof Authentication
         * @interface IGetSecurityQuestionV3Response
         * @property {string|null} [securityQuestion] GetSecurityQuestionV3Response securityQuestion
         * @property {number|null} [backupKeyDate] GetSecurityQuestionV3Response backupKeyDate
         * @property {Uint8Array|null} [salt] GetSecurityQuestionV3Response salt
         * @property {number|null} [iterations] GetSecurityQuestionV3Response iterations
         */

        /**
         * Constructs a new GetSecurityQuestionV3Response.
         * @memberof Authentication
         * @classdesc Represents a GetSecurityQuestionV3Response.
         * @implements IGetSecurityQuestionV3Response
         * @constructor
         * @param {Authentication.IGetSecurityQuestionV3Response=} [properties] Properties to set
         */
        function GetSecurityQuestionV3Response(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetSecurityQuestionV3Response securityQuestion.
         * @member {string} securityQuestion
         * @memberof Authentication.GetSecurityQuestionV3Response
         * @instance
         */
        GetSecurityQuestionV3Response.prototype.securityQuestion = "";

        /**
         * GetSecurityQuestionV3Response backupKeyDate.
         * @member {number} backupKeyDate
         * @memberof Authentication.GetSecurityQuestionV3Response
         * @instance
         */
        GetSecurityQuestionV3Response.prototype.backupKeyDate = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GetSecurityQuestionV3Response salt.
         * @member {Uint8Array} salt
         * @memberof Authentication.GetSecurityQuestionV3Response
         * @instance
         */
        GetSecurityQuestionV3Response.prototype.salt = $util.newBuffer([]);

        /**
         * GetSecurityQuestionV3Response iterations.
         * @member {number} iterations
         * @memberof Authentication.GetSecurityQuestionV3Response
         * @instance
         */
        GetSecurityQuestionV3Response.prototype.iterations = 0;

        /**
         * Creates a new GetSecurityQuestionV3Response instance using the specified properties.
         * @function create
         * @memberof Authentication.GetSecurityQuestionV3Response
         * @static
         * @param {Authentication.IGetSecurityQuestionV3Response=} [properties] Properties to set
         * @returns {Authentication.GetSecurityQuestionV3Response} GetSecurityQuestionV3Response instance
         */
        GetSecurityQuestionV3Response.create = function create(properties) {
            return new GetSecurityQuestionV3Response(properties);
        };

        /**
         * Encodes the specified GetSecurityQuestionV3Response message. Does not implicitly {@link Authentication.GetSecurityQuestionV3Response.verify|verify} messages.
         * @function encode
         * @memberof Authentication.GetSecurityQuestionV3Response
         * @static
         * @param {Authentication.IGetSecurityQuestionV3Response} message GetSecurityQuestionV3Response message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetSecurityQuestionV3Response.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.securityQuestion != null && Object.hasOwnProperty.call(message, "securityQuestion"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.securityQuestion);
            if (message.backupKeyDate != null && Object.hasOwnProperty.call(message, "backupKeyDate"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.backupKeyDate);
            if (message.salt != null && Object.hasOwnProperty.call(message, "salt"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.salt);
            if (message.iterations != null && Object.hasOwnProperty.call(message, "iterations"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.iterations);
            return writer;
        };

        /**
         * Decodes a GetSecurityQuestionV3Response message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.GetSecurityQuestionV3Response
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.GetSecurityQuestionV3Response} GetSecurityQuestionV3Response
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetSecurityQuestionV3Response.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.GetSecurityQuestionV3Response();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.securityQuestion = reader.string();
                        break;
                    }
                case 2: {
                        message.backupKeyDate = reader.int64();
                        break;
                    }
                case 3: {
                        message.salt = reader.bytes();
                        break;
                    }
                case 4: {
                        message.iterations = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a GetSecurityQuestionV3Response message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.GetSecurityQuestionV3Response
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.GetSecurityQuestionV3Response} GetSecurityQuestionV3Response
         */
        GetSecurityQuestionV3Response.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.GetSecurityQuestionV3Response)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.GetSecurityQuestionV3Response: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.GetSecurityQuestionV3Response();
            if (object.securityQuestion != null)
                message.securityQuestion = String(object.securityQuestion);
            if (object.backupKeyDate != null)
                if ($util.Long)
                    message.backupKeyDate = $util.Long.fromValue(object.backupKeyDate, false);
                else if (typeof object.backupKeyDate === "string")
                    message.backupKeyDate = parseInt(object.backupKeyDate, 10);
                else if (typeof object.backupKeyDate === "number")
                    message.backupKeyDate = object.backupKeyDate;
                else if (typeof object.backupKeyDate === "object")
                    message.backupKeyDate = new $util.LongBits(object.backupKeyDate.low >>> 0, object.backupKeyDate.high >>> 0).toNumber();
            if (object.salt != null)
                if (typeof object.salt === "string")
                    $util.base64.decode(object.salt, message.salt = $util.newBuffer($util.base64.length(object.salt)), 0);
                else if (object.salt.length >= 0)
                    message.salt = object.salt;
            if (object.iterations != null)
                message.iterations = object.iterations | 0;
            return message;
        };

        /**
         * Creates a plain object from a GetSecurityQuestionV3Response message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.GetSecurityQuestionV3Response
         * @static
         * @param {Authentication.GetSecurityQuestionV3Response} message GetSecurityQuestionV3Response
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetSecurityQuestionV3Response.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.securityQuestion = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.backupKeyDate = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.backupKeyDate = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.salt = "";
                else {
                    object.salt = [];
                    if (options.bytes !== Array)
                        object.salt = $util.newBuffer(object.salt);
                }
                object.iterations = 0;
            }
            if (message.securityQuestion != null && Object.hasOwnProperty.call(message, "securityQuestion"))
                object.securityQuestion = message.securityQuestion;
            if (message.backupKeyDate != null && Object.hasOwnProperty.call(message, "backupKeyDate"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.backupKeyDate = typeof message.backupKeyDate === "number" ? BigInt(message.backupKeyDate) : $util.Long.fromBits(message.backupKeyDate.low >>> 0, message.backupKeyDate.high >>> 0, false).toBigInt();
                else if (typeof message.backupKeyDate === "number")
                    object.backupKeyDate = options.longs === String ? String(message.backupKeyDate) : message.backupKeyDate;
                else
                    object.backupKeyDate = options.longs === String ? $util.Long.prototype.toString.call(message.backupKeyDate) : options.longs === Number ? new $util.LongBits(message.backupKeyDate.low >>> 0, message.backupKeyDate.high >>> 0).toNumber() : message.backupKeyDate;
            if (message.salt != null && Object.hasOwnProperty.call(message, "salt"))
                object.salt = options.bytes === String ? $util.base64.encode(message.salt, 0, message.salt.length) : options.bytes === Array ? Array.prototype.slice.call(message.salt) : message.salt;
            if (message.iterations != null && Object.hasOwnProperty.call(message, "iterations"))
                object.iterations = message.iterations;
            return object;
        };

        /**
         * Converts this GetSecurityQuestionV3Response to JSON.
         * @function toJSON
         * @memberof Authentication.GetSecurityQuestionV3Response
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetSecurityQuestionV3Response.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetSecurityQuestionV3Response
         * @function getTypeUrl
         * @memberof Authentication.GetSecurityQuestionV3Response
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetSecurityQuestionV3Response.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.GetSecurityQuestionV3Response";
        };

        return GetSecurityQuestionV3Response;
    })();

    Authentication.GetDataKeyBackupV3Request = (function() {

        /**
         * Properties of a GetDataKeyBackupV3Request.
         * @memberof Authentication
         * @interface IGetDataKeyBackupV3Request
         * @property {Uint8Array|null} [encryptedLoginToken] GetDataKeyBackupV3Request encryptedLoginToken
         * @property {string|null} [verificationCode] GetDataKeyBackupV3Request verificationCode
         * @property {Uint8Array|null} [securityAnswerHash] GetDataKeyBackupV3Request securityAnswerHash
         */

        /**
         * Constructs a new GetDataKeyBackupV3Request.
         * @memberof Authentication
         * @classdesc Represents a GetDataKeyBackupV3Request.
         * @implements IGetDataKeyBackupV3Request
         * @constructor
         * @param {Authentication.IGetDataKeyBackupV3Request=} [properties] Properties to set
         */
        function GetDataKeyBackupV3Request(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetDataKeyBackupV3Request encryptedLoginToken.
         * @member {Uint8Array} encryptedLoginToken
         * @memberof Authentication.GetDataKeyBackupV3Request
         * @instance
         */
        GetDataKeyBackupV3Request.prototype.encryptedLoginToken = $util.newBuffer([]);

        /**
         * GetDataKeyBackupV3Request verificationCode.
         * @member {string} verificationCode
         * @memberof Authentication.GetDataKeyBackupV3Request
         * @instance
         */
        GetDataKeyBackupV3Request.prototype.verificationCode = "";

        /**
         * GetDataKeyBackupV3Request securityAnswerHash.
         * @member {Uint8Array} securityAnswerHash
         * @memberof Authentication.GetDataKeyBackupV3Request
         * @instance
         */
        GetDataKeyBackupV3Request.prototype.securityAnswerHash = $util.newBuffer([]);

        /**
         * Creates a new GetDataKeyBackupV3Request instance using the specified properties.
         * @function create
         * @memberof Authentication.GetDataKeyBackupV3Request
         * @static
         * @param {Authentication.IGetDataKeyBackupV3Request=} [properties] Properties to set
         * @returns {Authentication.GetDataKeyBackupV3Request} GetDataKeyBackupV3Request instance
         */
        GetDataKeyBackupV3Request.create = function create(properties) {
            return new GetDataKeyBackupV3Request(properties);
        };

        /**
         * Encodes the specified GetDataKeyBackupV3Request message. Does not implicitly {@link Authentication.GetDataKeyBackupV3Request.verify|verify} messages.
         * @function encode
         * @memberof Authentication.GetDataKeyBackupV3Request
         * @static
         * @param {Authentication.IGetDataKeyBackupV3Request} message GetDataKeyBackupV3Request message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetDataKeyBackupV3Request.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedLoginToken);
            if (message.verificationCode != null && Object.hasOwnProperty.call(message, "verificationCode"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.verificationCode);
            if (message.securityAnswerHash != null && Object.hasOwnProperty.call(message, "securityAnswerHash"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.securityAnswerHash);
            return writer;
        };

        /**
         * Decodes a GetDataKeyBackupV3Request message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.GetDataKeyBackupV3Request
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.GetDataKeyBackupV3Request} GetDataKeyBackupV3Request
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetDataKeyBackupV3Request.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.GetDataKeyBackupV3Request();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedLoginToken = reader.bytes();
                        break;
                    }
                case 2: {
                        message.verificationCode = reader.string();
                        break;
                    }
                case 3: {
                        message.securityAnswerHash = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a GetDataKeyBackupV3Request message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.GetDataKeyBackupV3Request
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.GetDataKeyBackupV3Request} GetDataKeyBackupV3Request
         */
        GetDataKeyBackupV3Request.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.GetDataKeyBackupV3Request)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.GetDataKeyBackupV3Request: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.GetDataKeyBackupV3Request();
            if (object.encryptedLoginToken != null)
                if (typeof object.encryptedLoginToken === "string")
                    $util.base64.decode(object.encryptedLoginToken, message.encryptedLoginToken = $util.newBuffer($util.base64.length(object.encryptedLoginToken)), 0);
                else if (object.encryptedLoginToken.length >= 0)
                    message.encryptedLoginToken = object.encryptedLoginToken;
            if (object.verificationCode != null)
                message.verificationCode = String(object.verificationCode);
            if (object.securityAnswerHash != null)
                if (typeof object.securityAnswerHash === "string")
                    $util.base64.decode(object.securityAnswerHash, message.securityAnswerHash = $util.newBuffer($util.base64.length(object.securityAnswerHash)), 0);
                else if (object.securityAnswerHash.length >= 0)
                    message.securityAnswerHash = object.securityAnswerHash;
            return message;
        };

        /**
         * Creates a plain object from a GetDataKeyBackupV3Request message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.GetDataKeyBackupV3Request
         * @static
         * @param {Authentication.GetDataKeyBackupV3Request} message GetDataKeyBackupV3Request
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetDataKeyBackupV3Request.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.encryptedLoginToken = "";
                else {
                    object.encryptedLoginToken = [];
                    if (options.bytes !== Array)
                        object.encryptedLoginToken = $util.newBuffer(object.encryptedLoginToken);
                }
                object.verificationCode = "";
                if (options.bytes === String)
                    object.securityAnswerHash = "";
                else {
                    object.securityAnswerHash = [];
                    if (options.bytes !== Array)
                        object.securityAnswerHash = $util.newBuffer(object.securityAnswerHash);
                }
            }
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                object.encryptedLoginToken = options.bytes === String ? $util.base64.encode(message.encryptedLoginToken, 0, message.encryptedLoginToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedLoginToken) : message.encryptedLoginToken;
            if (message.verificationCode != null && Object.hasOwnProperty.call(message, "verificationCode"))
                object.verificationCode = message.verificationCode;
            if (message.securityAnswerHash != null && Object.hasOwnProperty.call(message, "securityAnswerHash"))
                object.securityAnswerHash = options.bytes === String ? $util.base64.encode(message.securityAnswerHash, 0, message.securityAnswerHash.length) : options.bytes === Array ? Array.prototype.slice.call(message.securityAnswerHash) : message.securityAnswerHash;
            return object;
        };

        /**
         * Converts this GetDataKeyBackupV3Request to JSON.
         * @function toJSON
         * @memberof Authentication.GetDataKeyBackupV3Request
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetDataKeyBackupV3Request.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetDataKeyBackupV3Request
         * @function getTypeUrl
         * @memberof Authentication.GetDataKeyBackupV3Request
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetDataKeyBackupV3Request.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.GetDataKeyBackupV3Request";
        };

        return GetDataKeyBackupV3Request;
    })();

    Authentication.PasswordRules = (function() {

        /**
         * Properties of a PasswordRules.
         * @memberof Authentication
         * @interface IPasswordRules
         * @property {string|null} [ruleType] PasswordRules ruleType
         * @property {boolean|null} [match] PasswordRules match
         * @property {string|null} [pattern] PasswordRules pattern
         * @property {string|null} [description] PasswordRules description
         * @property {number|null} [minimum] PasswordRules minimum
         * @property {string|null} [value] PasswordRules value
         */

        /**
         * Constructs a new PasswordRules.
         * @memberof Authentication
         * @classdesc Represents a PasswordRules.
         * @implements IPasswordRules
         * @constructor
         * @param {Authentication.IPasswordRules=} [properties] Properties to set
         */
        function PasswordRules(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PasswordRules ruleType.
         * @member {string} ruleType
         * @memberof Authentication.PasswordRules
         * @instance
         */
        PasswordRules.prototype.ruleType = "";

        /**
         * PasswordRules match.
         * @member {boolean} match
         * @memberof Authentication.PasswordRules
         * @instance
         */
        PasswordRules.prototype.match = false;

        /**
         * PasswordRules pattern.
         * @member {string} pattern
         * @memberof Authentication.PasswordRules
         * @instance
         */
        PasswordRules.prototype.pattern = "";

        /**
         * PasswordRules description.
         * @member {string} description
         * @memberof Authentication.PasswordRules
         * @instance
         */
        PasswordRules.prototype.description = "";

        /**
         * PasswordRules minimum.
         * @member {number} minimum
         * @memberof Authentication.PasswordRules
         * @instance
         */
        PasswordRules.prototype.minimum = 0;

        /**
         * PasswordRules value.
         * @member {string} value
         * @memberof Authentication.PasswordRules
         * @instance
         */
        PasswordRules.prototype.value = "";

        /**
         * Creates a new PasswordRules instance using the specified properties.
         * @function create
         * @memberof Authentication.PasswordRules
         * @static
         * @param {Authentication.IPasswordRules=} [properties] Properties to set
         * @returns {Authentication.PasswordRules} PasswordRules instance
         */
        PasswordRules.create = function create(properties) {
            return new PasswordRules(properties);
        };

        /**
         * Encodes the specified PasswordRules message. Does not implicitly {@link Authentication.PasswordRules.verify|verify} messages.
         * @function encode
         * @memberof Authentication.PasswordRules
         * @static
         * @param {Authentication.IPasswordRules} message PasswordRules message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PasswordRules.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ruleType != null && Object.hasOwnProperty.call(message, "ruleType"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.ruleType);
            if (message.match != null && Object.hasOwnProperty.call(message, "match"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.match);
            if (message.pattern != null && Object.hasOwnProperty.call(message, "pattern"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.pattern);
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.description);
            if (message.minimum != null && Object.hasOwnProperty.call(message, "minimum"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.minimum);
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.value);
            return writer;
        };

        /**
         * Decodes a PasswordRules message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.PasswordRules
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.PasswordRules} PasswordRules
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PasswordRules.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.PasswordRules();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ruleType = reader.string();
                        break;
                    }
                case 2: {
                        message.match = reader.bool();
                        break;
                    }
                case 3: {
                        message.pattern = reader.string();
                        break;
                    }
                case 4: {
                        message.description = reader.string();
                        break;
                    }
                case 5: {
                        message.minimum = reader.int32();
                        break;
                    }
                case 6: {
                        message.value = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a PasswordRules message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.PasswordRules
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.PasswordRules} PasswordRules
         */
        PasswordRules.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.PasswordRules)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.PasswordRules: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.PasswordRules();
            if (object.ruleType != null)
                message.ruleType = String(object.ruleType);
            if (object.match != null)
                message.match = Boolean(object.match);
            if (object.pattern != null)
                message.pattern = String(object.pattern);
            if (object.description != null)
                message.description = String(object.description);
            if (object.minimum != null)
                message.minimum = object.minimum | 0;
            if (object.value != null)
                message.value = String(object.value);
            return message;
        };

        /**
         * Creates a plain object from a PasswordRules message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.PasswordRules
         * @static
         * @param {Authentication.PasswordRules} message PasswordRules
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PasswordRules.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.ruleType = "";
                object.match = false;
                object.pattern = "";
                object.description = "";
                object.minimum = 0;
                object.value = "";
            }
            if (message.ruleType != null && Object.hasOwnProperty.call(message, "ruleType"))
                object.ruleType = message.ruleType;
            if (message.match != null && Object.hasOwnProperty.call(message, "match"))
                object.match = message.match;
            if (message.pattern != null && Object.hasOwnProperty.call(message, "pattern"))
                object.pattern = message.pattern;
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                object.description = message.description;
            if (message.minimum != null && Object.hasOwnProperty.call(message, "minimum"))
                object.minimum = message.minimum;
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                object.value = message.value;
            return object;
        };

        /**
         * Converts this PasswordRules to JSON.
         * @function toJSON
         * @memberof Authentication.PasswordRules
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PasswordRules.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PasswordRules
         * @function getTypeUrl
         * @memberof Authentication.PasswordRules
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PasswordRules.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.PasswordRules";
        };

        return PasswordRules;
    })();

    Authentication.GetDataKeyBackupV3Response = (function() {

        /**
         * Properties of a GetDataKeyBackupV3Response.
         * @memberof Authentication
         * @interface IGetDataKeyBackupV3Response
         * @property {Uint8Array|null} [dataKeyBackup] GetDataKeyBackupV3Response dataKeyBackup
         * @property {number|null} [dataKeyBackupDate] GetDataKeyBackupV3Response dataKeyBackupDate
         * @property {Uint8Array|null} [publicKey] GetDataKeyBackupV3Response publicKey
         * @property {Uint8Array|null} [encryptedPrivateKey] GetDataKeyBackupV3Response encryptedPrivateKey
         * @property {Uint8Array|null} [clientKey] GetDataKeyBackupV3Response clientKey
         * @property {Uint8Array|null} [encryptedSessionToken] GetDataKeyBackupV3Response encryptedSessionToken
         * @property {Array.<Authentication.IPasswordRules>|null} [passwordRules] GetDataKeyBackupV3Response passwordRules
         * @property {string|null} [passwordRulesIntro] GetDataKeyBackupV3Response passwordRulesIntro
         * @property {number|null} [minimumPbkdf2Iterations] GetDataKeyBackupV3Response minimumPbkdf2Iterations
         * @property {Enterprise.KeyType|null} [keyType] GetDataKeyBackupV3Response keyType
         */

        /**
         * Constructs a new GetDataKeyBackupV3Response.
         * @memberof Authentication
         * @classdesc Represents a GetDataKeyBackupV3Response.
         * @implements IGetDataKeyBackupV3Response
         * @constructor
         * @param {Authentication.IGetDataKeyBackupV3Response=} [properties] Properties to set
         */
        function GetDataKeyBackupV3Response(properties) {
            this.passwordRules = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetDataKeyBackupV3Response dataKeyBackup.
         * @member {Uint8Array} dataKeyBackup
         * @memberof Authentication.GetDataKeyBackupV3Response
         * @instance
         */
        GetDataKeyBackupV3Response.prototype.dataKeyBackup = $util.newBuffer([]);

        /**
         * GetDataKeyBackupV3Response dataKeyBackupDate.
         * @member {number} dataKeyBackupDate
         * @memberof Authentication.GetDataKeyBackupV3Response
         * @instance
         */
        GetDataKeyBackupV3Response.prototype.dataKeyBackupDate = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * GetDataKeyBackupV3Response publicKey.
         * @member {Uint8Array} publicKey
         * @memberof Authentication.GetDataKeyBackupV3Response
         * @instance
         */
        GetDataKeyBackupV3Response.prototype.publicKey = $util.newBuffer([]);

        /**
         * GetDataKeyBackupV3Response encryptedPrivateKey.
         * @member {Uint8Array} encryptedPrivateKey
         * @memberof Authentication.GetDataKeyBackupV3Response
         * @instance
         */
        GetDataKeyBackupV3Response.prototype.encryptedPrivateKey = $util.newBuffer([]);

        /**
         * GetDataKeyBackupV3Response clientKey.
         * @member {Uint8Array} clientKey
         * @memberof Authentication.GetDataKeyBackupV3Response
         * @instance
         */
        GetDataKeyBackupV3Response.prototype.clientKey = $util.newBuffer([]);

        /**
         * GetDataKeyBackupV3Response encryptedSessionToken.
         * @member {Uint8Array} encryptedSessionToken
         * @memberof Authentication.GetDataKeyBackupV3Response
         * @instance
         */
        GetDataKeyBackupV3Response.prototype.encryptedSessionToken = $util.newBuffer([]);

        /**
         * GetDataKeyBackupV3Response passwordRules.
         * @member {Array.<Authentication.IPasswordRules>} passwordRules
         * @memberof Authentication.GetDataKeyBackupV3Response
         * @instance
         */
        GetDataKeyBackupV3Response.prototype.passwordRules = $util.emptyArray;

        /**
         * GetDataKeyBackupV3Response passwordRulesIntro.
         * @member {string} passwordRulesIntro
         * @memberof Authentication.GetDataKeyBackupV3Response
         * @instance
         */
        GetDataKeyBackupV3Response.prototype.passwordRulesIntro = "";

        /**
         * GetDataKeyBackupV3Response minimumPbkdf2Iterations.
         * @member {number} minimumPbkdf2Iterations
         * @memberof Authentication.GetDataKeyBackupV3Response
         * @instance
         */
        GetDataKeyBackupV3Response.prototype.minimumPbkdf2Iterations = 0;

        /**
         * GetDataKeyBackupV3Response keyType.
         * @member {Enterprise.KeyType} keyType
         * @memberof Authentication.GetDataKeyBackupV3Response
         * @instance
         */
        GetDataKeyBackupV3Response.prototype.keyType = 0;

        /**
         * Creates a new GetDataKeyBackupV3Response instance using the specified properties.
         * @function create
         * @memberof Authentication.GetDataKeyBackupV3Response
         * @static
         * @param {Authentication.IGetDataKeyBackupV3Response=} [properties] Properties to set
         * @returns {Authentication.GetDataKeyBackupV3Response} GetDataKeyBackupV3Response instance
         */
        GetDataKeyBackupV3Response.create = function create(properties) {
            return new GetDataKeyBackupV3Response(properties);
        };

        /**
         * Encodes the specified GetDataKeyBackupV3Response message. Does not implicitly {@link Authentication.GetDataKeyBackupV3Response.verify|verify} messages.
         * @function encode
         * @memberof Authentication.GetDataKeyBackupV3Response
         * @static
         * @param {Authentication.IGetDataKeyBackupV3Response} message GetDataKeyBackupV3Response message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetDataKeyBackupV3Response.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.dataKeyBackup != null && Object.hasOwnProperty.call(message, "dataKeyBackup"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.dataKeyBackup);
            if (message.dataKeyBackupDate != null && Object.hasOwnProperty.call(message, "dataKeyBackupDate"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.dataKeyBackupDate);
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.publicKey);
            if (message.encryptedPrivateKey != null && Object.hasOwnProperty.call(message, "encryptedPrivateKey"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.encryptedPrivateKey);
            if (message.clientKey != null && Object.hasOwnProperty.call(message, "clientKey"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.clientKey);
            if (message.encryptedSessionToken != null && Object.hasOwnProperty.call(message, "encryptedSessionToken"))
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.encryptedSessionToken);
            if (message.passwordRules != null && message.passwordRules.length)
                for (let i = 0; i < message.passwordRules.length; ++i)
                    $root.Authentication.PasswordRules.encode(message.passwordRules[i], writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            if (message.passwordRulesIntro != null && Object.hasOwnProperty.call(message, "passwordRulesIntro"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.passwordRulesIntro);
            if (message.minimumPbkdf2Iterations != null && Object.hasOwnProperty.call(message, "minimumPbkdf2Iterations"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.minimumPbkdf2Iterations);
            if (message.keyType != null && Object.hasOwnProperty.call(message, "keyType"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.keyType);
            return writer;
        };

        /**
         * Decodes a GetDataKeyBackupV3Response message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.GetDataKeyBackupV3Response
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.GetDataKeyBackupV3Response} GetDataKeyBackupV3Response
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetDataKeyBackupV3Response.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.GetDataKeyBackupV3Response();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.dataKeyBackup = reader.bytes();
                        break;
                    }
                case 2: {
                        message.dataKeyBackupDate = reader.int64();
                        break;
                    }
                case 3: {
                        message.publicKey = reader.bytes();
                        break;
                    }
                case 4: {
                        message.encryptedPrivateKey = reader.bytes();
                        break;
                    }
                case 5: {
                        message.clientKey = reader.bytes();
                        break;
                    }
                case 6: {
                        message.encryptedSessionToken = reader.bytes();
                        break;
                    }
                case 7: {
                        if (!(message.passwordRules && message.passwordRules.length))
                            message.passwordRules = [];
                        message.passwordRules.push($root.Authentication.PasswordRules.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 8: {
                        message.passwordRulesIntro = reader.string();
                        break;
                    }
                case 9: {
                        message.minimumPbkdf2Iterations = reader.int32();
                        break;
                    }
                case 10: {
                        message.keyType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a GetDataKeyBackupV3Response message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.GetDataKeyBackupV3Response
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.GetDataKeyBackupV3Response} GetDataKeyBackupV3Response
         */
        GetDataKeyBackupV3Response.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.GetDataKeyBackupV3Response)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.GetDataKeyBackupV3Response: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.GetDataKeyBackupV3Response();
            if (object.dataKeyBackup != null)
                if (typeof object.dataKeyBackup === "string")
                    $util.base64.decode(object.dataKeyBackup, message.dataKeyBackup = $util.newBuffer($util.base64.length(object.dataKeyBackup)), 0);
                else if (object.dataKeyBackup.length >= 0)
                    message.dataKeyBackup = object.dataKeyBackup;
            if (object.dataKeyBackupDate != null)
                if ($util.Long)
                    message.dataKeyBackupDate = $util.Long.fromValue(object.dataKeyBackupDate, false);
                else if (typeof object.dataKeyBackupDate === "string")
                    message.dataKeyBackupDate = parseInt(object.dataKeyBackupDate, 10);
                else if (typeof object.dataKeyBackupDate === "number")
                    message.dataKeyBackupDate = object.dataKeyBackupDate;
                else if (typeof object.dataKeyBackupDate === "object")
                    message.dataKeyBackupDate = new $util.LongBits(object.dataKeyBackupDate.low >>> 0, object.dataKeyBackupDate.high >>> 0).toNumber();
            if (object.publicKey != null)
                if (typeof object.publicKey === "string")
                    $util.base64.decode(object.publicKey, message.publicKey = $util.newBuffer($util.base64.length(object.publicKey)), 0);
                else if (object.publicKey.length >= 0)
                    message.publicKey = object.publicKey;
            if (object.encryptedPrivateKey != null)
                if (typeof object.encryptedPrivateKey === "string")
                    $util.base64.decode(object.encryptedPrivateKey, message.encryptedPrivateKey = $util.newBuffer($util.base64.length(object.encryptedPrivateKey)), 0);
                else if (object.encryptedPrivateKey.length >= 0)
                    message.encryptedPrivateKey = object.encryptedPrivateKey;
            if (object.clientKey != null)
                if (typeof object.clientKey === "string")
                    $util.base64.decode(object.clientKey, message.clientKey = $util.newBuffer($util.base64.length(object.clientKey)), 0);
                else if (object.clientKey.length >= 0)
                    message.clientKey = object.clientKey;
            if (object.encryptedSessionToken != null)
                if (typeof object.encryptedSessionToken === "string")
                    $util.base64.decode(object.encryptedSessionToken, message.encryptedSessionToken = $util.newBuffer($util.base64.length(object.encryptedSessionToken)), 0);
                else if (object.encryptedSessionToken.length >= 0)
                    message.encryptedSessionToken = object.encryptedSessionToken;
            if (object.passwordRules) {
                if (!Array.isArray(object.passwordRules))
                    throw TypeError(".Authentication.GetDataKeyBackupV3Response.passwordRules: array expected");
                message.passwordRules = [];
                for (let i = 0; i < object.passwordRules.length; ++i) {
                    if (!$util.isObject(object.passwordRules[i]))
                        throw TypeError(".Authentication.GetDataKeyBackupV3Response.passwordRules: object expected");
                    message.passwordRules[i] = $root.Authentication.PasswordRules.fromObject(object.passwordRules[i], long + 1);
                }
            }
            if (object.passwordRulesIntro != null)
                message.passwordRulesIntro = String(object.passwordRulesIntro);
            if (object.minimumPbkdf2Iterations != null)
                message.minimumPbkdf2Iterations = object.minimumPbkdf2Iterations | 0;
            switch (object.keyType) {
            default:
                if (typeof object.keyType === "number") {
                    message.keyType = object.keyType;
                    break;
                }
                break;
            case "RSA":
            case 0:
                message.keyType = 0;
                break;
            case "ECC":
            case 1:
                message.keyType = 1;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a GetDataKeyBackupV3Response message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.GetDataKeyBackupV3Response
         * @static
         * @param {Authentication.GetDataKeyBackupV3Response} message GetDataKeyBackupV3Response
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetDataKeyBackupV3Response.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.passwordRules = [];
            if (options.defaults) {
                if (options.bytes === String)
                    object.dataKeyBackup = "";
                else {
                    object.dataKeyBackup = [];
                    if (options.bytes !== Array)
                        object.dataKeyBackup = $util.newBuffer(object.dataKeyBackup);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.dataKeyBackupDate = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.dataKeyBackupDate = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.publicKey = "";
                else {
                    object.publicKey = [];
                    if (options.bytes !== Array)
                        object.publicKey = $util.newBuffer(object.publicKey);
                }
                if (options.bytes === String)
                    object.encryptedPrivateKey = "";
                else {
                    object.encryptedPrivateKey = [];
                    if (options.bytes !== Array)
                        object.encryptedPrivateKey = $util.newBuffer(object.encryptedPrivateKey);
                }
                if (options.bytes === String)
                    object.clientKey = "";
                else {
                    object.clientKey = [];
                    if (options.bytes !== Array)
                        object.clientKey = $util.newBuffer(object.clientKey);
                }
                if (options.bytes === String)
                    object.encryptedSessionToken = "";
                else {
                    object.encryptedSessionToken = [];
                    if (options.bytes !== Array)
                        object.encryptedSessionToken = $util.newBuffer(object.encryptedSessionToken);
                }
                object.passwordRulesIntro = "";
                object.minimumPbkdf2Iterations = 0;
                object.keyType = options.enums === String ? "RSA" : 0;
            }
            if (message.dataKeyBackup != null && Object.hasOwnProperty.call(message, "dataKeyBackup"))
                object.dataKeyBackup = options.bytes === String ? $util.base64.encode(message.dataKeyBackup, 0, message.dataKeyBackup.length) : options.bytes === Array ? Array.prototype.slice.call(message.dataKeyBackup) : message.dataKeyBackup;
            if (message.dataKeyBackupDate != null && Object.hasOwnProperty.call(message, "dataKeyBackupDate"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.dataKeyBackupDate = typeof message.dataKeyBackupDate === "number" ? BigInt(message.dataKeyBackupDate) : $util.Long.fromBits(message.dataKeyBackupDate.low >>> 0, message.dataKeyBackupDate.high >>> 0, false).toBigInt();
                else if (typeof message.dataKeyBackupDate === "number")
                    object.dataKeyBackupDate = options.longs === String ? String(message.dataKeyBackupDate) : message.dataKeyBackupDate;
                else
                    object.dataKeyBackupDate = options.longs === String ? $util.Long.prototype.toString.call(message.dataKeyBackupDate) : options.longs === Number ? new $util.LongBits(message.dataKeyBackupDate.low >>> 0, message.dataKeyBackupDate.high >>> 0).toNumber() : message.dataKeyBackupDate;
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                object.publicKey = options.bytes === String ? $util.base64.encode(message.publicKey, 0, message.publicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.publicKey) : message.publicKey;
            if (message.encryptedPrivateKey != null && Object.hasOwnProperty.call(message, "encryptedPrivateKey"))
                object.encryptedPrivateKey = options.bytes === String ? $util.base64.encode(message.encryptedPrivateKey, 0, message.encryptedPrivateKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedPrivateKey) : message.encryptedPrivateKey;
            if (message.clientKey != null && Object.hasOwnProperty.call(message, "clientKey"))
                object.clientKey = options.bytes === String ? $util.base64.encode(message.clientKey, 0, message.clientKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.clientKey) : message.clientKey;
            if (message.encryptedSessionToken != null && Object.hasOwnProperty.call(message, "encryptedSessionToken"))
                object.encryptedSessionToken = options.bytes === String ? $util.base64.encode(message.encryptedSessionToken, 0, message.encryptedSessionToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedSessionToken) : message.encryptedSessionToken;
            if (message.passwordRules && message.passwordRules.length) {
                object.passwordRules = [];
                for (let j = 0; j < message.passwordRules.length; ++j)
                    object.passwordRules[j] = $root.Authentication.PasswordRules.toObject(message.passwordRules[j], options, q + 1);
            }
            if (message.passwordRulesIntro != null && Object.hasOwnProperty.call(message, "passwordRulesIntro"))
                object.passwordRulesIntro = message.passwordRulesIntro;
            if (message.minimumPbkdf2Iterations != null && Object.hasOwnProperty.call(message, "minimumPbkdf2Iterations"))
                object.minimumPbkdf2Iterations = message.minimumPbkdf2Iterations;
            if (message.keyType != null && Object.hasOwnProperty.call(message, "keyType"))
                object.keyType = options.enums === String ? $root.Enterprise.KeyType[message.keyType] === undefined ? message.keyType : $root.Enterprise.KeyType[message.keyType] : message.keyType;
            return object;
        };

        /**
         * Converts this GetDataKeyBackupV3Response to JSON.
         * @function toJSON
         * @memberof Authentication.GetDataKeyBackupV3Response
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetDataKeyBackupV3Response.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetDataKeyBackupV3Response
         * @function getTypeUrl
         * @memberof Authentication.GetDataKeyBackupV3Response
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetDataKeyBackupV3Response.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.GetDataKeyBackupV3Response";
        };

        return GetDataKeyBackupV3Response;
    })();

    Authentication.GetPublicKeysRequest = (function() {

        /**
         * Properties of a GetPublicKeysRequest.
         * @memberof Authentication
         * @interface IGetPublicKeysRequest
         * @property {Array.<string>|null} [usernames] GetPublicKeysRequest usernames
         */

        /**
         * Constructs a new GetPublicKeysRequest.
         * @memberof Authentication
         * @classdesc Represents a GetPublicKeysRequest.
         * @implements IGetPublicKeysRequest
         * @constructor
         * @param {Authentication.IGetPublicKeysRequest=} [properties] Properties to set
         */
        function GetPublicKeysRequest(properties) {
            this.usernames = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetPublicKeysRequest usernames.
         * @member {Array.<string>} usernames
         * @memberof Authentication.GetPublicKeysRequest
         * @instance
         */
        GetPublicKeysRequest.prototype.usernames = $util.emptyArray;

        /**
         * Creates a new GetPublicKeysRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.GetPublicKeysRequest
         * @static
         * @param {Authentication.IGetPublicKeysRequest=} [properties] Properties to set
         * @returns {Authentication.GetPublicKeysRequest} GetPublicKeysRequest instance
         */
        GetPublicKeysRequest.create = function create(properties) {
            return new GetPublicKeysRequest(properties);
        };

        /**
         * Encodes the specified GetPublicKeysRequest message. Does not implicitly {@link Authentication.GetPublicKeysRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.GetPublicKeysRequest
         * @static
         * @param {Authentication.IGetPublicKeysRequest} message GetPublicKeysRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetPublicKeysRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.usernames != null && message.usernames.length)
                for (let i = 0; i < message.usernames.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.usernames[i]);
            return writer;
        };

        /**
         * Decodes a GetPublicKeysRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.GetPublicKeysRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.GetPublicKeysRequest} GetPublicKeysRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetPublicKeysRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.GetPublicKeysRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.usernames && message.usernames.length))
                            message.usernames = [];
                        message.usernames.push(reader.string());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a GetPublicKeysRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.GetPublicKeysRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.GetPublicKeysRequest} GetPublicKeysRequest
         */
        GetPublicKeysRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.GetPublicKeysRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.GetPublicKeysRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.GetPublicKeysRequest();
            if (object.usernames) {
                if (!Array.isArray(object.usernames))
                    throw TypeError(".Authentication.GetPublicKeysRequest.usernames: array expected");
                message.usernames = [];
                for (let i = 0; i < object.usernames.length; ++i)
                    message.usernames[i] = String(object.usernames[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a GetPublicKeysRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.GetPublicKeysRequest
         * @static
         * @param {Authentication.GetPublicKeysRequest} message GetPublicKeysRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetPublicKeysRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.usernames = [];
            if (message.usernames && message.usernames.length) {
                object.usernames = [];
                for (let j = 0; j < message.usernames.length; ++j)
                    object.usernames[j] = message.usernames[j];
            }
            return object;
        };

        /**
         * Converts this GetPublicKeysRequest to JSON.
         * @function toJSON
         * @memberof Authentication.GetPublicKeysRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetPublicKeysRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetPublicKeysRequest
         * @function getTypeUrl
         * @memberof Authentication.GetPublicKeysRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetPublicKeysRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.GetPublicKeysRequest";
        };

        return GetPublicKeysRequest;
    })();

    Authentication.PublicKeyResponse = (function() {

        /**
         * Properties of a PublicKeyResponse.
         * @memberof Authentication
         * @interface IPublicKeyResponse
         * @property {string|null} [username] PublicKeyResponse username
         * @property {Uint8Array|null} [publicKey] PublicKeyResponse publicKey
         * @property {Uint8Array|null} [publicEccKey] PublicKeyResponse publicEccKey
         * @property {string|null} [message] PublicKeyResponse message
         * @property {string|null} [errorCode] PublicKeyResponse errorCode
         * @property {Uint8Array|null} [accountUid] PublicKeyResponse accountUid
         */

        /**
         * Constructs a new PublicKeyResponse.
         * @memberof Authentication
         * @classdesc Represents a PublicKeyResponse.
         * @implements IPublicKeyResponse
         * @constructor
         * @param {Authentication.IPublicKeyResponse=} [properties] Properties to set
         */
        function PublicKeyResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PublicKeyResponse username.
         * @member {string} username
         * @memberof Authentication.PublicKeyResponse
         * @instance
         */
        PublicKeyResponse.prototype.username = "";

        /**
         * PublicKeyResponse publicKey.
         * @member {Uint8Array} publicKey
         * @memberof Authentication.PublicKeyResponse
         * @instance
         */
        PublicKeyResponse.prototype.publicKey = $util.newBuffer([]);

        /**
         * PublicKeyResponse publicEccKey.
         * @member {Uint8Array} publicEccKey
         * @memberof Authentication.PublicKeyResponse
         * @instance
         */
        PublicKeyResponse.prototype.publicEccKey = $util.newBuffer([]);

        /**
         * PublicKeyResponse message.
         * @member {string} message
         * @memberof Authentication.PublicKeyResponse
         * @instance
         */
        PublicKeyResponse.prototype.message = "";

        /**
         * PublicKeyResponse errorCode.
         * @member {string} errorCode
         * @memberof Authentication.PublicKeyResponse
         * @instance
         */
        PublicKeyResponse.prototype.errorCode = "";

        /**
         * PublicKeyResponse accountUid.
         * @member {Uint8Array} accountUid
         * @memberof Authentication.PublicKeyResponse
         * @instance
         */
        PublicKeyResponse.prototype.accountUid = $util.newBuffer([]);

        /**
         * Creates a new PublicKeyResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.PublicKeyResponse
         * @static
         * @param {Authentication.IPublicKeyResponse=} [properties] Properties to set
         * @returns {Authentication.PublicKeyResponse} PublicKeyResponse instance
         */
        PublicKeyResponse.create = function create(properties) {
            return new PublicKeyResponse(properties);
        };

        /**
         * Encodes the specified PublicKeyResponse message. Does not implicitly {@link Authentication.PublicKeyResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.PublicKeyResponse
         * @static
         * @param {Authentication.IPublicKeyResponse} message PublicKeyResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PublicKeyResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.username);
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.publicKey);
            if (message.publicEccKey != null && Object.hasOwnProperty.call(message, "publicEccKey"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.publicEccKey);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.message);
            if (message.errorCode != null && Object.hasOwnProperty.call(message, "errorCode"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.errorCode);
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.accountUid);
            return writer;
        };

        /**
         * Decodes a PublicKeyResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.PublicKeyResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.PublicKeyResponse} PublicKeyResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PublicKeyResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.PublicKeyResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.username = reader.string();
                        break;
                    }
                case 2: {
                        message.publicKey = reader.bytes();
                        break;
                    }
                case 3: {
                        message.publicEccKey = reader.bytes();
                        break;
                    }
                case 4: {
                        message.message = reader.string();
                        break;
                    }
                case 5: {
                        message.errorCode = reader.string();
                        break;
                    }
                case 6: {
                        message.accountUid = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a PublicKeyResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.PublicKeyResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.PublicKeyResponse} PublicKeyResponse
         */
        PublicKeyResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.PublicKeyResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.PublicKeyResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.PublicKeyResponse();
            if (object.username != null)
                message.username = String(object.username);
            if (object.publicKey != null)
                if (typeof object.publicKey === "string")
                    $util.base64.decode(object.publicKey, message.publicKey = $util.newBuffer($util.base64.length(object.publicKey)), 0);
                else if (object.publicKey.length >= 0)
                    message.publicKey = object.publicKey;
            if (object.publicEccKey != null)
                if (typeof object.publicEccKey === "string")
                    $util.base64.decode(object.publicEccKey, message.publicEccKey = $util.newBuffer($util.base64.length(object.publicEccKey)), 0);
                else if (object.publicEccKey.length >= 0)
                    message.publicEccKey = object.publicEccKey;
            if (object.message != null)
                message.message = String(object.message);
            if (object.errorCode != null)
                message.errorCode = String(object.errorCode);
            if (object.accountUid != null)
                if (typeof object.accountUid === "string")
                    $util.base64.decode(object.accountUid, message.accountUid = $util.newBuffer($util.base64.length(object.accountUid)), 0);
                else if (object.accountUid.length >= 0)
                    message.accountUid = object.accountUid;
            return message;
        };

        /**
         * Creates a plain object from a PublicKeyResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.PublicKeyResponse
         * @static
         * @param {Authentication.PublicKeyResponse} message PublicKeyResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PublicKeyResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.username = "";
                if (options.bytes === String)
                    object.publicKey = "";
                else {
                    object.publicKey = [];
                    if (options.bytes !== Array)
                        object.publicKey = $util.newBuffer(object.publicKey);
                }
                if (options.bytes === String)
                    object.publicEccKey = "";
                else {
                    object.publicEccKey = [];
                    if (options.bytes !== Array)
                        object.publicEccKey = $util.newBuffer(object.publicEccKey);
                }
                object.message = "";
                object.errorCode = "";
                if (options.bytes === String)
                    object.accountUid = "";
                else {
                    object.accountUid = [];
                    if (options.bytes !== Array)
                        object.accountUid = $util.newBuffer(object.accountUid);
                }
            }
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                object.publicKey = options.bytes === String ? $util.base64.encode(message.publicKey, 0, message.publicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.publicKey) : message.publicKey;
            if (message.publicEccKey != null && Object.hasOwnProperty.call(message, "publicEccKey"))
                object.publicEccKey = options.bytes === String ? $util.base64.encode(message.publicEccKey, 0, message.publicEccKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.publicEccKey) : message.publicEccKey;
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                object.message = message.message;
            if (message.errorCode != null && Object.hasOwnProperty.call(message, "errorCode"))
                object.errorCode = message.errorCode;
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                object.accountUid = options.bytes === String ? $util.base64.encode(message.accountUid, 0, message.accountUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.accountUid) : message.accountUid;
            return object;
        };

        /**
         * Converts this PublicKeyResponse to JSON.
         * @function toJSON
         * @memberof Authentication.PublicKeyResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PublicKeyResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PublicKeyResponse
         * @function getTypeUrl
         * @memberof Authentication.PublicKeyResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PublicKeyResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.PublicKeyResponse";
        };

        return PublicKeyResponse;
    })();

    Authentication.GetPublicKeysResponse = (function() {

        /**
         * Properties of a GetPublicKeysResponse.
         * @memberof Authentication
         * @interface IGetPublicKeysResponse
         * @property {Array.<Authentication.IPublicKeyResponse>|null} [keyResponses] GetPublicKeysResponse keyResponses
         */

        /**
         * Constructs a new GetPublicKeysResponse.
         * @memberof Authentication
         * @classdesc Represents a GetPublicKeysResponse.
         * @implements IGetPublicKeysResponse
         * @constructor
         * @param {Authentication.IGetPublicKeysResponse=} [properties] Properties to set
         */
        function GetPublicKeysResponse(properties) {
            this.keyResponses = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetPublicKeysResponse keyResponses.
         * @member {Array.<Authentication.IPublicKeyResponse>} keyResponses
         * @memberof Authentication.GetPublicKeysResponse
         * @instance
         */
        GetPublicKeysResponse.prototype.keyResponses = $util.emptyArray;

        /**
         * Creates a new GetPublicKeysResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.GetPublicKeysResponse
         * @static
         * @param {Authentication.IGetPublicKeysResponse=} [properties] Properties to set
         * @returns {Authentication.GetPublicKeysResponse} GetPublicKeysResponse instance
         */
        GetPublicKeysResponse.create = function create(properties) {
            return new GetPublicKeysResponse(properties);
        };

        /**
         * Encodes the specified GetPublicKeysResponse message. Does not implicitly {@link Authentication.GetPublicKeysResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.GetPublicKeysResponse
         * @static
         * @param {Authentication.IGetPublicKeysResponse} message GetPublicKeysResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetPublicKeysResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.keyResponses != null && message.keyResponses.length)
                for (let i = 0; i < message.keyResponses.length; ++i)
                    $root.Authentication.PublicKeyResponse.encode(message.keyResponses[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes a GetPublicKeysResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.GetPublicKeysResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.GetPublicKeysResponse} GetPublicKeysResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetPublicKeysResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.GetPublicKeysResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.keyResponses && message.keyResponses.length))
                            message.keyResponses = [];
                        message.keyResponses.push($root.Authentication.PublicKeyResponse.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a GetPublicKeysResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.GetPublicKeysResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.GetPublicKeysResponse} GetPublicKeysResponse
         */
        GetPublicKeysResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.GetPublicKeysResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.GetPublicKeysResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.GetPublicKeysResponse();
            if (object.keyResponses) {
                if (!Array.isArray(object.keyResponses))
                    throw TypeError(".Authentication.GetPublicKeysResponse.keyResponses: array expected");
                message.keyResponses = [];
                for (let i = 0; i < object.keyResponses.length; ++i) {
                    if (!$util.isObject(object.keyResponses[i]))
                        throw TypeError(".Authentication.GetPublicKeysResponse.keyResponses: object expected");
                    message.keyResponses[i] = $root.Authentication.PublicKeyResponse.fromObject(object.keyResponses[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GetPublicKeysResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.GetPublicKeysResponse
         * @static
         * @param {Authentication.GetPublicKeysResponse} message GetPublicKeysResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetPublicKeysResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.keyResponses = [];
            if (message.keyResponses && message.keyResponses.length) {
                object.keyResponses = [];
                for (let j = 0; j < message.keyResponses.length; ++j)
                    object.keyResponses[j] = $root.Authentication.PublicKeyResponse.toObject(message.keyResponses[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this GetPublicKeysResponse to JSON.
         * @function toJSON
         * @memberof Authentication.GetPublicKeysResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetPublicKeysResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetPublicKeysResponse
         * @function getTypeUrl
         * @memberof Authentication.GetPublicKeysResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetPublicKeysResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.GetPublicKeysResponse";
        };

        return GetPublicKeysResponse;
    })();

    Authentication.SetEccKeyPairRequest = (function() {

        /**
         * Properties of a SetEccKeyPairRequest.
         * @memberof Authentication
         * @interface ISetEccKeyPairRequest
         * @property {Uint8Array|null} [publicKey] SetEccKeyPairRequest publicKey
         * @property {Uint8Array|null} [encryptedPrivateKey] SetEccKeyPairRequest encryptedPrivateKey
         */

        /**
         * Constructs a new SetEccKeyPairRequest.
         * @memberof Authentication
         * @classdesc Represents a SetEccKeyPairRequest.
         * @implements ISetEccKeyPairRequest
         * @constructor
         * @param {Authentication.ISetEccKeyPairRequest=} [properties] Properties to set
         */
        function SetEccKeyPairRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SetEccKeyPairRequest publicKey.
         * @member {Uint8Array} publicKey
         * @memberof Authentication.SetEccKeyPairRequest
         * @instance
         */
        SetEccKeyPairRequest.prototype.publicKey = $util.newBuffer([]);

        /**
         * SetEccKeyPairRequest encryptedPrivateKey.
         * @member {Uint8Array} encryptedPrivateKey
         * @memberof Authentication.SetEccKeyPairRequest
         * @instance
         */
        SetEccKeyPairRequest.prototype.encryptedPrivateKey = $util.newBuffer([]);

        /**
         * Creates a new SetEccKeyPairRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.SetEccKeyPairRequest
         * @static
         * @param {Authentication.ISetEccKeyPairRequest=} [properties] Properties to set
         * @returns {Authentication.SetEccKeyPairRequest} SetEccKeyPairRequest instance
         */
        SetEccKeyPairRequest.create = function create(properties) {
            return new SetEccKeyPairRequest(properties);
        };

        /**
         * Encodes the specified SetEccKeyPairRequest message. Does not implicitly {@link Authentication.SetEccKeyPairRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SetEccKeyPairRequest
         * @static
         * @param {Authentication.ISetEccKeyPairRequest} message SetEccKeyPairRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetEccKeyPairRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.publicKey);
            if (message.encryptedPrivateKey != null && Object.hasOwnProperty.call(message, "encryptedPrivateKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encryptedPrivateKey);
            return writer;
        };

        /**
         * Decodes a SetEccKeyPairRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SetEccKeyPairRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SetEccKeyPairRequest} SetEccKeyPairRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetEccKeyPairRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SetEccKeyPairRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.publicKey = reader.bytes();
                        break;
                    }
                case 2: {
                        message.encryptedPrivateKey = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SetEccKeyPairRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SetEccKeyPairRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SetEccKeyPairRequest} SetEccKeyPairRequest
         */
        SetEccKeyPairRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SetEccKeyPairRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SetEccKeyPairRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SetEccKeyPairRequest();
            if (object.publicKey != null)
                if (typeof object.publicKey === "string")
                    $util.base64.decode(object.publicKey, message.publicKey = $util.newBuffer($util.base64.length(object.publicKey)), 0);
                else if (object.publicKey.length >= 0)
                    message.publicKey = object.publicKey;
            if (object.encryptedPrivateKey != null)
                if (typeof object.encryptedPrivateKey === "string")
                    $util.base64.decode(object.encryptedPrivateKey, message.encryptedPrivateKey = $util.newBuffer($util.base64.length(object.encryptedPrivateKey)), 0);
                else if (object.encryptedPrivateKey.length >= 0)
                    message.encryptedPrivateKey = object.encryptedPrivateKey;
            return message;
        };

        /**
         * Creates a plain object from a SetEccKeyPairRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SetEccKeyPairRequest
         * @static
         * @param {Authentication.SetEccKeyPairRequest} message SetEccKeyPairRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SetEccKeyPairRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.publicKey = "";
                else {
                    object.publicKey = [];
                    if (options.bytes !== Array)
                        object.publicKey = $util.newBuffer(object.publicKey);
                }
                if (options.bytes === String)
                    object.encryptedPrivateKey = "";
                else {
                    object.encryptedPrivateKey = [];
                    if (options.bytes !== Array)
                        object.encryptedPrivateKey = $util.newBuffer(object.encryptedPrivateKey);
                }
            }
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                object.publicKey = options.bytes === String ? $util.base64.encode(message.publicKey, 0, message.publicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.publicKey) : message.publicKey;
            if (message.encryptedPrivateKey != null && Object.hasOwnProperty.call(message, "encryptedPrivateKey"))
                object.encryptedPrivateKey = options.bytes === String ? $util.base64.encode(message.encryptedPrivateKey, 0, message.encryptedPrivateKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedPrivateKey) : message.encryptedPrivateKey;
            return object;
        };

        /**
         * Converts this SetEccKeyPairRequest to JSON.
         * @function toJSON
         * @memberof Authentication.SetEccKeyPairRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SetEccKeyPairRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SetEccKeyPairRequest
         * @function getTypeUrl
         * @memberof Authentication.SetEccKeyPairRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SetEccKeyPairRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SetEccKeyPairRequest";
        };

        return SetEccKeyPairRequest;
    })();

    Authentication.SetEccKeyPairsRequest = (function() {

        /**
         * Properties of a SetEccKeyPairsRequest.
         * @memberof Authentication
         * @interface ISetEccKeyPairsRequest
         * @property {Array.<Authentication.ITeamEccKeyPair>|null} [teamKeys] SetEccKeyPairsRequest teamKeys
         */

        /**
         * Constructs a new SetEccKeyPairsRequest.
         * @memberof Authentication
         * @classdesc Represents a SetEccKeyPairsRequest.
         * @implements ISetEccKeyPairsRequest
         * @constructor
         * @param {Authentication.ISetEccKeyPairsRequest=} [properties] Properties to set
         */
        function SetEccKeyPairsRequest(properties) {
            this.teamKeys = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SetEccKeyPairsRequest teamKeys.
         * @member {Array.<Authentication.ITeamEccKeyPair>} teamKeys
         * @memberof Authentication.SetEccKeyPairsRequest
         * @instance
         */
        SetEccKeyPairsRequest.prototype.teamKeys = $util.emptyArray;

        /**
         * Creates a new SetEccKeyPairsRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.SetEccKeyPairsRequest
         * @static
         * @param {Authentication.ISetEccKeyPairsRequest=} [properties] Properties to set
         * @returns {Authentication.SetEccKeyPairsRequest} SetEccKeyPairsRequest instance
         */
        SetEccKeyPairsRequest.create = function create(properties) {
            return new SetEccKeyPairsRequest(properties);
        };

        /**
         * Encodes the specified SetEccKeyPairsRequest message. Does not implicitly {@link Authentication.SetEccKeyPairsRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SetEccKeyPairsRequest
         * @static
         * @param {Authentication.ISetEccKeyPairsRequest} message SetEccKeyPairsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetEccKeyPairsRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.teamKeys != null && message.teamKeys.length)
                for (let i = 0; i < message.teamKeys.length; ++i)
                    $root.Authentication.TeamEccKeyPair.encode(message.teamKeys[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes a SetEccKeyPairsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SetEccKeyPairsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SetEccKeyPairsRequest} SetEccKeyPairsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetEccKeyPairsRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SetEccKeyPairsRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.teamKeys && message.teamKeys.length))
                            message.teamKeys = [];
                        message.teamKeys.push($root.Authentication.TeamEccKeyPair.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SetEccKeyPairsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SetEccKeyPairsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SetEccKeyPairsRequest} SetEccKeyPairsRequest
         */
        SetEccKeyPairsRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SetEccKeyPairsRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SetEccKeyPairsRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SetEccKeyPairsRequest();
            if (object.teamKeys) {
                if (!Array.isArray(object.teamKeys))
                    throw TypeError(".Authentication.SetEccKeyPairsRequest.teamKeys: array expected");
                message.teamKeys = [];
                for (let i = 0; i < object.teamKeys.length; ++i) {
                    if (!$util.isObject(object.teamKeys[i]))
                        throw TypeError(".Authentication.SetEccKeyPairsRequest.teamKeys: object expected");
                    message.teamKeys[i] = $root.Authentication.TeamEccKeyPair.fromObject(object.teamKeys[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SetEccKeyPairsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SetEccKeyPairsRequest
         * @static
         * @param {Authentication.SetEccKeyPairsRequest} message SetEccKeyPairsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SetEccKeyPairsRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.teamKeys = [];
            if (message.teamKeys && message.teamKeys.length) {
                object.teamKeys = [];
                for (let j = 0; j < message.teamKeys.length; ++j)
                    object.teamKeys[j] = $root.Authentication.TeamEccKeyPair.toObject(message.teamKeys[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this SetEccKeyPairsRequest to JSON.
         * @function toJSON
         * @memberof Authentication.SetEccKeyPairsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SetEccKeyPairsRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SetEccKeyPairsRequest
         * @function getTypeUrl
         * @memberof Authentication.SetEccKeyPairsRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SetEccKeyPairsRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SetEccKeyPairsRequest";
        };

        return SetEccKeyPairsRequest;
    })();

    Authentication.SetEccKeyPairsResponse = (function() {

        /**
         * Properties of a SetEccKeyPairsResponse.
         * @memberof Authentication
         * @interface ISetEccKeyPairsResponse
         * @property {Array.<Authentication.ITeamEccKeyPairResponse>|null} [teamKeys] SetEccKeyPairsResponse teamKeys
         */

        /**
         * Constructs a new SetEccKeyPairsResponse.
         * @memberof Authentication
         * @classdesc Represents a SetEccKeyPairsResponse.
         * @implements ISetEccKeyPairsResponse
         * @constructor
         * @param {Authentication.ISetEccKeyPairsResponse=} [properties] Properties to set
         */
        function SetEccKeyPairsResponse(properties) {
            this.teamKeys = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SetEccKeyPairsResponse teamKeys.
         * @member {Array.<Authentication.ITeamEccKeyPairResponse>} teamKeys
         * @memberof Authentication.SetEccKeyPairsResponse
         * @instance
         */
        SetEccKeyPairsResponse.prototype.teamKeys = $util.emptyArray;

        /**
         * Creates a new SetEccKeyPairsResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.SetEccKeyPairsResponse
         * @static
         * @param {Authentication.ISetEccKeyPairsResponse=} [properties] Properties to set
         * @returns {Authentication.SetEccKeyPairsResponse} SetEccKeyPairsResponse instance
         */
        SetEccKeyPairsResponse.create = function create(properties) {
            return new SetEccKeyPairsResponse(properties);
        };

        /**
         * Encodes the specified SetEccKeyPairsResponse message. Does not implicitly {@link Authentication.SetEccKeyPairsResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SetEccKeyPairsResponse
         * @static
         * @param {Authentication.ISetEccKeyPairsResponse} message SetEccKeyPairsResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SetEccKeyPairsResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.teamKeys != null && message.teamKeys.length)
                for (let i = 0; i < message.teamKeys.length; ++i)
                    $root.Authentication.TeamEccKeyPairResponse.encode(message.teamKeys[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes a SetEccKeyPairsResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SetEccKeyPairsResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SetEccKeyPairsResponse} SetEccKeyPairsResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SetEccKeyPairsResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SetEccKeyPairsResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.teamKeys && message.teamKeys.length))
                            message.teamKeys = [];
                        message.teamKeys.push($root.Authentication.TeamEccKeyPairResponse.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SetEccKeyPairsResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SetEccKeyPairsResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SetEccKeyPairsResponse} SetEccKeyPairsResponse
         */
        SetEccKeyPairsResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SetEccKeyPairsResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SetEccKeyPairsResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SetEccKeyPairsResponse();
            if (object.teamKeys) {
                if (!Array.isArray(object.teamKeys))
                    throw TypeError(".Authentication.SetEccKeyPairsResponse.teamKeys: array expected");
                message.teamKeys = [];
                for (let i = 0; i < object.teamKeys.length; ++i) {
                    if (!$util.isObject(object.teamKeys[i]))
                        throw TypeError(".Authentication.SetEccKeyPairsResponse.teamKeys: object expected");
                    message.teamKeys[i] = $root.Authentication.TeamEccKeyPairResponse.fromObject(object.teamKeys[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a SetEccKeyPairsResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SetEccKeyPairsResponse
         * @static
         * @param {Authentication.SetEccKeyPairsResponse} message SetEccKeyPairsResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SetEccKeyPairsResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.teamKeys = [];
            if (message.teamKeys && message.teamKeys.length) {
                object.teamKeys = [];
                for (let j = 0; j < message.teamKeys.length; ++j)
                    object.teamKeys[j] = $root.Authentication.TeamEccKeyPairResponse.toObject(message.teamKeys[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this SetEccKeyPairsResponse to JSON.
         * @function toJSON
         * @memberof Authentication.SetEccKeyPairsResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SetEccKeyPairsResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SetEccKeyPairsResponse
         * @function getTypeUrl
         * @memberof Authentication.SetEccKeyPairsResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SetEccKeyPairsResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SetEccKeyPairsResponse";
        };

        return SetEccKeyPairsResponse;
    })();

    Authentication.TeamEccKeyPair = (function() {

        /**
         * Properties of a TeamEccKeyPair.
         * @memberof Authentication
         * @interface ITeamEccKeyPair
         * @property {Uint8Array|null} [teamUid] TeamEccKeyPair teamUid
         * @property {Uint8Array|null} [publicKey] TeamEccKeyPair publicKey
         * @property {Uint8Array|null} [encryptedPrivateKey] TeamEccKeyPair encryptedPrivateKey
         */

        /**
         * Constructs a new TeamEccKeyPair.
         * @memberof Authentication
         * @classdesc Represents a TeamEccKeyPair.
         * @implements ITeamEccKeyPair
         * @constructor
         * @param {Authentication.ITeamEccKeyPair=} [properties] Properties to set
         */
        function TeamEccKeyPair(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TeamEccKeyPair teamUid.
         * @member {Uint8Array} teamUid
         * @memberof Authentication.TeamEccKeyPair
         * @instance
         */
        TeamEccKeyPair.prototype.teamUid = $util.newBuffer([]);

        /**
         * TeamEccKeyPair publicKey.
         * @member {Uint8Array} publicKey
         * @memberof Authentication.TeamEccKeyPair
         * @instance
         */
        TeamEccKeyPair.prototype.publicKey = $util.newBuffer([]);

        /**
         * TeamEccKeyPair encryptedPrivateKey.
         * @member {Uint8Array} encryptedPrivateKey
         * @memberof Authentication.TeamEccKeyPair
         * @instance
         */
        TeamEccKeyPair.prototype.encryptedPrivateKey = $util.newBuffer([]);

        /**
         * Creates a new TeamEccKeyPair instance using the specified properties.
         * @function create
         * @memberof Authentication.TeamEccKeyPair
         * @static
         * @param {Authentication.ITeamEccKeyPair=} [properties] Properties to set
         * @returns {Authentication.TeamEccKeyPair} TeamEccKeyPair instance
         */
        TeamEccKeyPair.create = function create(properties) {
            return new TeamEccKeyPair(properties);
        };

        /**
         * Encodes the specified TeamEccKeyPair message. Does not implicitly {@link Authentication.TeamEccKeyPair.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TeamEccKeyPair
         * @static
         * @param {Authentication.ITeamEccKeyPair} message TeamEccKeyPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TeamEccKeyPair.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.teamUid != null && Object.hasOwnProperty.call(message, "teamUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.teamUid);
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.publicKey);
            if (message.encryptedPrivateKey != null && Object.hasOwnProperty.call(message, "encryptedPrivateKey"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.encryptedPrivateKey);
            return writer;
        };

        /**
         * Decodes a TeamEccKeyPair message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TeamEccKeyPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TeamEccKeyPair} TeamEccKeyPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TeamEccKeyPair.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TeamEccKeyPair();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.teamUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.publicKey = reader.bytes();
                        break;
                    }
                case 3: {
                        message.encryptedPrivateKey = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TeamEccKeyPair message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TeamEccKeyPair
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TeamEccKeyPair} TeamEccKeyPair
         */
        TeamEccKeyPair.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TeamEccKeyPair)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TeamEccKeyPair: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TeamEccKeyPair();
            if (object.teamUid != null)
                if (typeof object.teamUid === "string")
                    $util.base64.decode(object.teamUid, message.teamUid = $util.newBuffer($util.base64.length(object.teamUid)), 0);
                else if (object.teamUid.length >= 0)
                    message.teamUid = object.teamUid;
            if (object.publicKey != null)
                if (typeof object.publicKey === "string")
                    $util.base64.decode(object.publicKey, message.publicKey = $util.newBuffer($util.base64.length(object.publicKey)), 0);
                else if (object.publicKey.length >= 0)
                    message.publicKey = object.publicKey;
            if (object.encryptedPrivateKey != null)
                if (typeof object.encryptedPrivateKey === "string")
                    $util.base64.decode(object.encryptedPrivateKey, message.encryptedPrivateKey = $util.newBuffer($util.base64.length(object.encryptedPrivateKey)), 0);
                else if (object.encryptedPrivateKey.length >= 0)
                    message.encryptedPrivateKey = object.encryptedPrivateKey;
            return message;
        };

        /**
         * Creates a plain object from a TeamEccKeyPair message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TeamEccKeyPair
         * @static
         * @param {Authentication.TeamEccKeyPair} message TeamEccKeyPair
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TeamEccKeyPair.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.teamUid = "";
                else {
                    object.teamUid = [];
                    if (options.bytes !== Array)
                        object.teamUid = $util.newBuffer(object.teamUid);
                }
                if (options.bytes === String)
                    object.publicKey = "";
                else {
                    object.publicKey = [];
                    if (options.bytes !== Array)
                        object.publicKey = $util.newBuffer(object.publicKey);
                }
                if (options.bytes === String)
                    object.encryptedPrivateKey = "";
                else {
                    object.encryptedPrivateKey = [];
                    if (options.bytes !== Array)
                        object.encryptedPrivateKey = $util.newBuffer(object.encryptedPrivateKey);
                }
            }
            if (message.teamUid != null && Object.hasOwnProperty.call(message, "teamUid"))
                object.teamUid = options.bytes === String ? $util.base64.encode(message.teamUid, 0, message.teamUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.teamUid) : message.teamUid;
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                object.publicKey = options.bytes === String ? $util.base64.encode(message.publicKey, 0, message.publicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.publicKey) : message.publicKey;
            if (message.encryptedPrivateKey != null && Object.hasOwnProperty.call(message, "encryptedPrivateKey"))
                object.encryptedPrivateKey = options.bytes === String ? $util.base64.encode(message.encryptedPrivateKey, 0, message.encryptedPrivateKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedPrivateKey) : message.encryptedPrivateKey;
            return object;
        };

        /**
         * Converts this TeamEccKeyPair to JSON.
         * @function toJSON
         * @memberof Authentication.TeamEccKeyPair
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TeamEccKeyPair.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TeamEccKeyPair
         * @function getTypeUrl
         * @memberof Authentication.TeamEccKeyPair
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TeamEccKeyPair.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TeamEccKeyPair";
        };

        return TeamEccKeyPair;
    })();

    Authentication.TeamEccKeyPairResponse = (function() {

        /**
         * Properties of a TeamEccKeyPairResponse.
         * @memberof Authentication
         * @interface ITeamEccKeyPairResponse
         * @property {Uint8Array|null} [teamUid] TeamEccKeyPairResponse teamUid
         * @property {Authentication.GenericStatus|null} [status] TeamEccKeyPairResponse status
         */

        /**
         * Constructs a new TeamEccKeyPairResponse.
         * @memberof Authentication
         * @classdesc Represents a TeamEccKeyPairResponse.
         * @implements ITeamEccKeyPairResponse
         * @constructor
         * @param {Authentication.ITeamEccKeyPairResponse=} [properties] Properties to set
         */
        function TeamEccKeyPairResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TeamEccKeyPairResponse teamUid.
         * @member {Uint8Array} teamUid
         * @memberof Authentication.TeamEccKeyPairResponse
         * @instance
         */
        TeamEccKeyPairResponse.prototype.teamUid = $util.newBuffer([]);

        /**
         * TeamEccKeyPairResponse status.
         * @member {Authentication.GenericStatus} status
         * @memberof Authentication.TeamEccKeyPairResponse
         * @instance
         */
        TeamEccKeyPairResponse.prototype.status = 0;

        /**
         * Creates a new TeamEccKeyPairResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.TeamEccKeyPairResponse
         * @static
         * @param {Authentication.ITeamEccKeyPairResponse=} [properties] Properties to set
         * @returns {Authentication.TeamEccKeyPairResponse} TeamEccKeyPairResponse instance
         */
        TeamEccKeyPairResponse.create = function create(properties) {
            return new TeamEccKeyPairResponse(properties);
        };

        /**
         * Encodes the specified TeamEccKeyPairResponse message. Does not implicitly {@link Authentication.TeamEccKeyPairResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TeamEccKeyPairResponse
         * @static
         * @param {Authentication.ITeamEccKeyPairResponse} message TeamEccKeyPairResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TeamEccKeyPairResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.teamUid != null && Object.hasOwnProperty.call(message, "teamUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.teamUid);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.status);
            return writer;
        };

        /**
         * Decodes a TeamEccKeyPairResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TeamEccKeyPairResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TeamEccKeyPairResponse} TeamEccKeyPairResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TeamEccKeyPairResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TeamEccKeyPairResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.teamUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.status = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TeamEccKeyPairResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TeamEccKeyPairResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TeamEccKeyPairResponse} TeamEccKeyPairResponse
         */
        TeamEccKeyPairResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TeamEccKeyPairResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TeamEccKeyPairResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TeamEccKeyPairResponse();
            if (object.teamUid != null)
                if (typeof object.teamUid === "string")
                    $util.base64.decode(object.teamUid, message.teamUid = $util.newBuffer($util.base64.length(object.teamUid)), 0);
                else if (object.teamUid.length >= 0)
                    message.teamUid = object.teamUid;
            switch (object.status) {
            default:
                if (typeof object.status === "number") {
                    message.status = object.status;
                    break;
                }
                break;
            case "SUCCESS":
            case 0:
                message.status = 0;
                break;
            case "INVALID_OBJECT":
            case 1:
                message.status = 1;
                break;
            case "ALREADY_EXISTS":
            case 2:
                message.status = 2;
                break;
            case "ACCESS_DENIED":
            case 3:
                message.status = 3;
                break;
            case "LICENSE_SEAT_EXCEEDED":
            case 4:
                message.status = 4;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a TeamEccKeyPairResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TeamEccKeyPairResponse
         * @static
         * @param {Authentication.TeamEccKeyPairResponse} message TeamEccKeyPairResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TeamEccKeyPairResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.teamUid = "";
                else {
                    object.teamUid = [];
                    if (options.bytes !== Array)
                        object.teamUid = $util.newBuffer(object.teamUid);
                }
                object.status = options.enums === String ? "SUCCESS" : 0;
            }
            if (message.teamUid != null && Object.hasOwnProperty.call(message, "teamUid"))
                object.teamUid = options.bytes === String ? $util.base64.encode(message.teamUid, 0, message.teamUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.teamUid) : message.teamUid;
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                object.status = options.enums === String ? $root.Authentication.GenericStatus[message.status] === undefined ? message.status : $root.Authentication.GenericStatus[message.status] : message.status;
            return object;
        };

        /**
         * Converts this TeamEccKeyPairResponse to JSON.
         * @function toJSON
         * @memberof Authentication.TeamEccKeyPairResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TeamEccKeyPairResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TeamEccKeyPairResponse
         * @function getTypeUrl
         * @memberof Authentication.TeamEccKeyPairResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TeamEccKeyPairResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TeamEccKeyPairResponse";
        };

        return TeamEccKeyPairResponse;
    })();

    Authentication.GetKsmPublicKeysRequest = (function() {

        /**
         * Properties of a GetKsmPublicKeysRequest.
         * @memberof Authentication
         * @interface IGetKsmPublicKeysRequest
         * @property {Array.<Uint8Array>|null} [clientIds] GetKsmPublicKeysRequest clientIds
         * @property {Array.<Uint8Array>|null} [controllerUids] GetKsmPublicKeysRequest controllerUids
         */

        /**
         * Constructs a new GetKsmPublicKeysRequest.
         * @memberof Authentication
         * @classdesc Represents a GetKsmPublicKeysRequest.
         * @implements IGetKsmPublicKeysRequest
         * @constructor
         * @param {Authentication.IGetKsmPublicKeysRequest=} [properties] Properties to set
         */
        function GetKsmPublicKeysRequest(properties) {
            this.clientIds = [];
            this.controllerUids = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetKsmPublicKeysRequest clientIds.
         * @member {Array.<Uint8Array>} clientIds
         * @memberof Authentication.GetKsmPublicKeysRequest
         * @instance
         */
        GetKsmPublicKeysRequest.prototype.clientIds = $util.emptyArray;

        /**
         * GetKsmPublicKeysRequest controllerUids.
         * @member {Array.<Uint8Array>} controllerUids
         * @memberof Authentication.GetKsmPublicKeysRequest
         * @instance
         */
        GetKsmPublicKeysRequest.prototype.controllerUids = $util.emptyArray;

        /**
         * Creates a new GetKsmPublicKeysRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.GetKsmPublicKeysRequest
         * @static
         * @param {Authentication.IGetKsmPublicKeysRequest=} [properties] Properties to set
         * @returns {Authentication.GetKsmPublicKeysRequest} GetKsmPublicKeysRequest instance
         */
        GetKsmPublicKeysRequest.create = function create(properties) {
            return new GetKsmPublicKeysRequest(properties);
        };

        /**
         * Encodes the specified GetKsmPublicKeysRequest message. Does not implicitly {@link Authentication.GetKsmPublicKeysRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.GetKsmPublicKeysRequest
         * @static
         * @param {Authentication.IGetKsmPublicKeysRequest} message GetKsmPublicKeysRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetKsmPublicKeysRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.clientIds != null && message.clientIds.length)
                for (let i = 0; i < message.clientIds.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.clientIds[i]);
            if (message.controllerUids != null && message.controllerUids.length)
                for (let i = 0; i < message.controllerUids.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.controllerUids[i]);
            return writer;
        };

        /**
         * Decodes a GetKsmPublicKeysRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.GetKsmPublicKeysRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.GetKsmPublicKeysRequest} GetKsmPublicKeysRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetKsmPublicKeysRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.GetKsmPublicKeysRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.clientIds && message.clientIds.length))
                            message.clientIds = [];
                        message.clientIds.push(reader.bytes());
                        break;
                    }
                case 2: {
                        if (!(message.controllerUids && message.controllerUids.length))
                            message.controllerUids = [];
                        message.controllerUids.push(reader.bytes());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a GetKsmPublicKeysRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.GetKsmPublicKeysRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.GetKsmPublicKeysRequest} GetKsmPublicKeysRequest
         */
        GetKsmPublicKeysRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.GetKsmPublicKeysRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.GetKsmPublicKeysRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.GetKsmPublicKeysRequest();
            if (object.clientIds) {
                if (!Array.isArray(object.clientIds))
                    throw TypeError(".Authentication.GetKsmPublicKeysRequest.clientIds: array expected");
                message.clientIds = [];
                for (let i = 0; i < object.clientIds.length; ++i)
                    if (typeof object.clientIds[i] === "string")
                        $util.base64.decode(object.clientIds[i], message.clientIds[i] = $util.newBuffer($util.base64.length(object.clientIds[i])), 0);
                    else if (object.clientIds[i].length >= 0)
                        message.clientIds[i] = object.clientIds[i];
            }
            if (object.controllerUids) {
                if (!Array.isArray(object.controllerUids))
                    throw TypeError(".Authentication.GetKsmPublicKeysRequest.controllerUids: array expected");
                message.controllerUids = [];
                for (let i = 0; i < object.controllerUids.length; ++i)
                    if (typeof object.controllerUids[i] === "string")
                        $util.base64.decode(object.controllerUids[i], message.controllerUids[i] = $util.newBuffer($util.base64.length(object.controllerUids[i])), 0);
                    else if (object.controllerUids[i].length >= 0)
                        message.controllerUids[i] = object.controllerUids[i];
            }
            return message;
        };

        /**
         * Creates a plain object from a GetKsmPublicKeysRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.GetKsmPublicKeysRequest
         * @static
         * @param {Authentication.GetKsmPublicKeysRequest} message GetKsmPublicKeysRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetKsmPublicKeysRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.clientIds = [];
                object.controllerUids = [];
            }
            if (message.clientIds && message.clientIds.length) {
                object.clientIds = [];
                for (let j = 0; j < message.clientIds.length; ++j)
                    object.clientIds[j] = options.bytes === String ? $util.base64.encode(message.clientIds[j], 0, message.clientIds[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.clientIds[j]) : message.clientIds[j];
            }
            if (message.controllerUids && message.controllerUids.length) {
                object.controllerUids = [];
                for (let j = 0; j < message.controllerUids.length; ++j)
                    object.controllerUids[j] = options.bytes === String ? $util.base64.encode(message.controllerUids[j], 0, message.controllerUids[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.controllerUids[j]) : message.controllerUids[j];
            }
            return object;
        };

        /**
         * Converts this GetKsmPublicKeysRequest to JSON.
         * @function toJSON
         * @memberof Authentication.GetKsmPublicKeysRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetKsmPublicKeysRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetKsmPublicKeysRequest
         * @function getTypeUrl
         * @memberof Authentication.GetKsmPublicKeysRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetKsmPublicKeysRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.GetKsmPublicKeysRequest";
        };

        return GetKsmPublicKeysRequest;
    })();

    Authentication.DevicePublicKeyResponse = (function() {

        /**
         * Properties of a DevicePublicKeyResponse.
         * @memberof Authentication
         * @interface IDevicePublicKeyResponse
         * @property {Uint8Array|null} [clientId] DevicePublicKeyResponse clientId
         * @property {Uint8Array|null} [publicKey] DevicePublicKeyResponse publicKey
         * @property {Uint8Array|null} [controllerUid] DevicePublicKeyResponse controllerUid
         */

        /**
         * Constructs a new DevicePublicKeyResponse.
         * @memberof Authentication
         * @classdesc Represents a DevicePublicKeyResponse.
         * @implements IDevicePublicKeyResponse
         * @constructor
         * @param {Authentication.IDevicePublicKeyResponse=} [properties] Properties to set
         */
        function DevicePublicKeyResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DevicePublicKeyResponse clientId.
         * @member {Uint8Array} clientId
         * @memberof Authentication.DevicePublicKeyResponse
         * @instance
         */
        DevicePublicKeyResponse.prototype.clientId = $util.newBuffer([]);

        /**
         * DevicePublicKeyResponse publicKey.
         * @member {Uint8Array} publicKey
         * @memberof Authentication.DevicePublicKeyResponse
         * @instance
         */
        DevicePublicKeyResponse.prototype.publicKey = $util.newBuffer([]);

        /**
         * DevicePublicKeyResponse controllerUid.
         * @member {Uint8Array} controllerUid
         * @memberof Authentication.DevicePublicKeyResponse
         * @instance
         */
        DevicePublicKeyResponse.prototype.controllerUid = $util.newBuffer([]);

        /**
         * Creates a new DevicePublicKeyResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.DevicePublicKeyResponse
         * @static
         * @param {Authentication.IDevicePublicKeyResponse=} [properties] Properties to set
         * @returns {Authentication.DevicePublicKeyResponse} DevicePublicKeyResponse instance
         */
        DevicePublicKeyResponse.create = function create(properties) {
            return new DevicePublicKeyResponse(properties);
        };

        /**
         * Encodes the specified DevicePublicKeyResponse message. Does not implicitly {@link Authentication.DevicePublicKeyResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.DevicePublicKeyResponse
         * @static
         * @param {Authentication.IDevicePublicKeyResponse} message DevicePublicKeyResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DevicePublicKeyResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.clientId != null && Object.hasOwnProperty.call(message, "clientId"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.clientId);
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.publicKey);
            if (message.controllerUid != null && Object.hasOwnProperty.call(message, "controllerUid"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.controllerUid);
            return writer;
        };

        /**
         * Decodes a DevicePublicKeyResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.DevicePublicKeyResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.DevicePublicKeyResponse} DevicePublicKeyResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DevicePublicKeyResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.DevicePublicKeyResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.clientId = reader.bytes();
                        break;
                    }
                case 2: {
                        message.publicKey = reader.bytes();
                        break;
                    }
                case 3: {
                        message.controllerUid = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a DevicePublicKeyResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.DevicePublicKeyResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.DevicePublicKeyResponse} DevicePublicKeyResponse
         */
        DevicePublicKeyResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.DevicePublicKeyResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.DevicePublicKeyResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.DevicePublicKeyResponse();
            if (object.clientId != null)
                if (typeof object.clientId === "string")
                    $util.base64.decode(object.clientId, message.clientId = $util.newBuffer($util.base64.length(object.clientId)), 0);
                else if (object.clientId.length >= 0)
                    message.clientId = object.clientId;
            if (object.publicKey != null)
                if (typeof object.publicKey === "string")
                    $util.base64.decode(object.publicKey, message.publicKey = $util.newBuffer($util.base64.length(object.publicKey)), 0);
                else if (object.publicKey.length >= 0)
                    message.publicKey = object.publicKey;
            if (object.controllerUid != null)
                if (typeof object.controllerUid === "string")
                    $util.base64.decode(object.controllerUid, message.controllerUid = $util.newBuffer($util.base64.length(object.controllerUid)), 0);
                else if (object.controllerUid.length >= 0)
                    message.controllerUid = object.controllerUid;
            return message;
        };

        /**
         * Creates a plain object from a DevicePublicKeyResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.DevicePublicKeyResponse
         * @static
         * @param {Authentication.DevicePublicKeyResponse} message DevicePublicKeyResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DevicePublicKeyResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.clientId = "";
                else {
                    object.clientId = [];
                    if (options.bytes !== Array)
                        object.clientId = $util.newBuffer(object.clientId);
                }
                if (options.bytes === String)
                    object.publicKey = "";
                else {
                    object.publicKey = [];
                    if (options.bytes !== Array)
                        object.publicKey = $util.newBuffer(object.publicKey);
                }
                if (options.bytes === String)
                    object.controllerUid = "";
                else {
                    object.controllerUid = [];
                    if (options.bytes !== Array)
                        object.controllerUid = $util.newBuffer(object.controllerUid);
                }
            }
            if (message.clientId != null && Object.hasOwnProperty.call(message, "clientId"))
                object.clientId = options.bytes === String ? $util.base64.encode(message.clientId, 0, message.clientId.length) : options.bytes === Array ? Array.prototype.slice.call(message.clientId) : message.clientId;
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                object.publicKey = options.bytes === String ? $util.base64.encode(message.publicKey, 0, message.publicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.publicKey) : message.publicKey;
            if (message.controllerUid != null && Object.hasOwnProperty.call(message, "controllerUid"))
                object.controllerUid = options.bytes === String ? $util.base64.encode(message.controllerUid, 0, message.controllerUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.controllerUid) : message.controllerUid;
            return object;
        };

        /**
         * Converts this DevicePublicKeyResponse to JSON.
         * @function toJSON
         * @memberof Authentication.DevicePublicKeyResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DevicePublicKeyResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DevicePublicKeyResponse
         * @function getTypeUrl
         * @memberof Authentication.DevicePublicKeyResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DevicePublicKeyResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.DevicePublicKeyResponse";
        };

        return DevicePublicKeyResponse;
    })();

    Authentication.GetKsmPublicKeysResponse = (function() {

        /**
         * Properties of a GetKsmPublicKeysResponse.
         * @memberof Authentication
         * @interface IGetKsmPublicKeysResponse
         * @property {Array.<Authentication.IDevicePublicKeyResponse>|null} [keyResponses] GetKsmPublicKeysResponse keyResponses
         */

        /**
         * Constructs a new GetKsmPublicKeysResponse.
         * @memberof Authentication
         * @classdesc Represents a GetKsmPublicKeysResponse.
         * @implements IGetKsmPublicKeysResponse
         * @constructor
         * @param {Authentication.IGetKsmPublicKeysResponse=} [properties] Properties to set
         */
        function GetKsmPublicKeysResponse(properties) {
            this.keyResponses = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetKsmPublicKeysResponse keyResponses.
         * @member {Array.<Authentication.IDevicePublicKeyResponse>} keyResponses
         * @memberof Authentication.GetKsmPublicKeysResponse
         * @instance
         */
        GetKsmPublicKeysResponse.prototype.keyResponses = $util.emptyArray;

        /**
         * Creates a new GetKsmPublicKeysResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.GetKsmPublicKeysResponse
         * @static
         * @param {Authentication.IGetKsmPublicKeysResponse=} [properties] Properties to set
         * @returns {Authentication.GetKsmPublicKeysResponse} GetKsmPublicKeysResponse instance
         */
        GetKsmPublicKeysResponse.create = function create(properties) {
            return new GetKsmPublicKeysResponse(properties);
        };

        /**
         * Encodes the specified GetKsmPublicKeysResponse message. Does not implicitly {@link Authentication.GetKsmPublicKeysResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.GetKsmPublicKeysResponse
         * @static
         * @param {Authentication.IGetKsmPublicKeysResponse} message GetKsmPublicKeysResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetKsmPublicKeysResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.keyResponses != null && message.keyResponses.length)
                for (let i = 0; i < message.keyResponses.length; ++i)
                    $root.Authentication.DevicePublicKeyResponse.encode(message.keyResponses[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes a GetKsmPublicKeysResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.GetKsmPublicKeysResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.GetKsmPublicKeysResponse} GetKsmPublicKeysResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetKsmPublicKeysResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.GetKsmPublicKeysResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.keyResponses && message.keyResponses.length))
                            message.keyResponses = [];
                        message.keyResponses.push($root.Authentication.DevicePublicKeyResponse.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a GetKsmPublicKeysResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.GetKsmPublicKeysResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.GetKsmPublicKeysResponse} GetKsmPublicKeysResponse
         */
        GetKsmPublicKeysResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.GetKsmPublicKeysResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.GetKsmPublicKeysResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.GetKsmPublicKeysResponse();
            if (object.keyResponses) {
                if (!Array.isArray(object.keyResponses))
                    throw TypeError(".Authentication.GetKsmPublicKeysResponse.keyResponses: array expected");
                message.keyResponses = [];
                for (let i = 0; i < object.keyResponses.length; ++i) {
                    if (!$util.isObject(object.keyResponses[i]))
                        throw TypeError(".Authentication.GetKsmPublicKeysResponse.keyResponses: object expected");
                    message.keyResponses[i] = $root.Authentication.DevicePublicKeyResponse.fromObject(object.keyResponses[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GetKsmPublicKeysResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.GetKsmPublicKeysResponse
         * @static
         * @param {Authentication.GetKsmPublicKeysResponse} message GetKsmPublicKeysResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetKsmPublicKeysResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.keyResponses = [];
            if (message.keyResponses && message.keyResponses.length) {
                object.keyResponses = [];
                for (let j = 0; j < message.keyResponses.length; ++j)
                    object.keyResponses[j] = $root.Authentication.DevicePublicKeyResponse.toObject(message.keyResponses[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this GetKsmPublicKeysResponse to JSON.
         * @function toJSON
         * @memberof Authentication.GetKsmPublicKeysResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetKsmPublicKeysResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetKsmPublicKeysResponse
         * @function getTypeUrl
         * @memberof Authentication.GetKsmPublicKeysResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetKsmPublicKeysResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.GetKsmPublicKeysResponse";
        };

        return GetKsmPublicKeysResponse;
    })();

    /**
     * The names and IDs in this class correspond to the Kinfo database entries and to the Region class in Keeperpap.
     * @name Authentication.Region
     * @enum {number}
     * @property {number} UNKNOWN=0 UNKNOWN value
     * @property {number} eu=1 eu value
     * @property {number} us=2 us value
     * @property {number} usgov=3 usgov value
     * @property {number} au=4 au value
     * @property {number} jp=5 jp value
     * @property {number} ca=6 ca value
     */
    Authentication.Region = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "UNKNOWN"] = 0;
        values[valuesById[1] = "eu"] = 1;
        values[valuesById[2] = "us"] = 2;
        values[valuesById[3] = "usgov"] = 3;
        values[valuesById[4] = "au"] = 4;
        values[valuesById[5] = "jp"] = 5;
        values[valuesById[6] = "ca"] = 6;
        return values;
    })();

    /**
     * ApplicationShareType enum.
     * @name Authentication.ApplicationShareType
     * @enum {number}
     * @property {number} SHARE_TYPE_RECORD=0 SHARE_TYPE_RECORD value
     * @property {number} SHARE_TYPE_FOLDER=1 SHARE_TYPE_FOLDER value
     */
    Authentication.ApplicationShareType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "SHARE_TYPE_RECORD"] = 0;
        values[valuesById[1] = "SHARE_TYPE_FOLDER"] = 1;
        return values;
    })();

    Authentication.AddAppSharesRequest = (function() {

        /**
         * Properties of an AddAppSharesRequest.
         * @memberof Authentication
         * @interface IAddAppSharesRequest
         * @property {Uint8Array|null} [appRecordUid] AddAppSharesRequest appRecordUid
         * @property {Array.<Authentication.IAppShareAdd>|null} [shares] AddAppSharesRequest shares
         */

        /**
         * Constructs a new AddAppSharesRequest.
         * @memberof Authentication
         * @classdesc Represents an AddAppSharesRequest.
         * @implements IAddAppSharesRequest
         * @constructor
         * @param {Authentication.IAddAppSharesRequest=} [properties] Properties to set
         */
        function AddAppSharesRequest(properties) {
            this.shares = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AddAppSharesRequest appRecordUid.
         * @member {Uint8Array} appRecordUid
         * @memberof Authentication.AddAppSharesRequest
         * @instance
         */
        AddAppSharesRequest.prototype.appRecordUid = $util.newBuffer([]);

        /**
         * AddAppSharesRequest shares.
         * @member {Array.<Authentication.IAppShareAdd>} shares
         * @memberof Authentication.AddAppSharesRequest
         * @instance
         */
        AddAppSharesRequest.prototype.shares = $util.emptyArray;

        /**
         * Creates a new AddAppSharesRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.AddAppSharesRequest
         * @static
         * @param {Authentication.IAddAppSharesRequest=} [properties] Properties to set
         * @returns {Authentication.AddAppSharesRequest} AddAppSharesRequest instance
         */
        AddAppSharesRequest.create = function create(properties) {
            return new AddAppSharesRequest(properties);
        };

        /**
         * Encodes the specified AddAppSharesRequest message. Does not implicitly {@link Authentication.AddAppSharesRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.AddAppSharesRequest
         * @static
         * @param {Authentication.IAddAppSharesRequest} message AddAppSharesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddAppSharesRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.appRecordUid != null && Object.hasOwnProperty.call(message, "appRecordUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.appRecordUid);
            if (message.shares != null && message.shares.length)
                for (let i = 0; i < message.shares.length; ++i)
                    $root.Authentication.AppShareAdd.encode(message.shares[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes an AddAppSharesRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.AddAppSharesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.AddAppSharesRequest} AddAppSharesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddAppSharesRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.AddAppSharesRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.appRecordUid = reader.bytes();
                        break;
                    }
                case 2: {
                        if (!(message.shares && message.shares.length))
                            message.shares = [];
                        message.shares.push($root.Authentication.AppShareAdd.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an AddAppSharesRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.AddAppSharesRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.AddAppSharesRequest} AddAppSharesRequest
         */
        AddAppSharesRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.AddAppSharesRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.AddAppSharesRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.AddAppSharesRequest();
            if (object.appRecordUid != null)
                if (typeof object.appRecordUid === "string")
                    $util.base64.decode(object.appRecordUid, message.appRecordUid = $util.newBuffer($util.base64.length(object.appRecordUid)), 0);
                else if (object.appRecordUid.length >= 0)
                    message.appRecordUid = object.appRecordUid;
            if (object.shares) {
                if (!Array.isArray(object.shares))
                    throw TypeError(".Authentication.AddAppSharesRequest.shares: array expected");
                message.shares = [];
                for (let i = 0; i < object.shares.length; ++i) {
                    if (!$util.isObject(object.shares[i]))
                        throw TypeError(".Authentication.AddAppSharesRequest.shares: object expected");
                    message.shares[i] = $root.Authentication.AppShareAdd.fromObject(object.shares[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an AddAppSharesRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.AddAppSharesRequest
         * @static
         * @param {Authentication.AddAppSharesRequest} message AddAppSharesRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AddAppSharesRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.shares = [];
            if (options.defaults)
                if (options.bytes === String)
                    object.appRecordUid = "";
                else {
                    object.appRecordUid = [];
                    if (options.bytes !== Array)
                        object.appRecordUid = $util.newBuffer(object.appRecordUid);
                }
            if (message.appRecordUid != null && Object.hasOwnProperty.call(message, "appRecordUid"))
                object.appRecordUid = options.bytes === String ? $util.base64.encode(message.appRecordUid, 0, message.appRecordUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.appRecordUid) : message.appRecordUid;
            if (message.shares && message.shares.length) {
                object.shares = [];
                for (let j = 0; j < message.shares.length; ++j)
                    object.shares[j] = $root.Authentication.AppShareAdd.toObject(message.shares[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this AddAppSharesRequest to JSON.
         * @function toJSON
         * @memberof Authentication.AddAppSharesRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AddAppSharesRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AddAppSharesRequest
         * @function getTypeUrl
         * @memberof Authentication.AddAppSharesRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AddAppSharesRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.AddAppSharesRequest";
        };

        return AddAppSharesRequest;
    })();

    Authentication.RemoveAppSharesRequest = (function() {

        /**
         * Properties of a RemoveAppSharesRequest.
         * @memberof Authentication
         * @interface IRemoveAppSharesRequest
         * @property {Uint8Array|null} [appRecordUid] RemoveAppSharesRequest appRecordUid
         * @property {Array.<Uint8Array>|null} [shares] RemoveAppSharesRequest shares
         */

        /**
         * Constructs a new RemoveAppSharesRequest.
         * @memberof Authentication
         * @classdesc Represents a RemoveAppSharesRequest.
         * @implements IRemoveAppSharesRequest
         * @constructor
         * @param {Authentication.IRemoveAppSharesRequest=} [properties] Properties to set
         */
        function RemoveAppSharesRequest(properties) {
            this.shares = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RemoveAppSharesRequest appRecordUid.
         * @member {Uint8Array} appRecordUid
         * @memberof Authentication.RemoveAppSharesRequest
         * @instance
         */
        RemoveAppSharesRequest.prototype.appRecordUid = $util.newBuffer([]);

        /**
         * RemoveAppSharesRequest shares.
         * @member {Array.<Uint8Array>} shares
         * @memberof Authentication.RemoveAppSharesRequest
         * @instance
         */
        RemoveAppSharesRequest.prototype.shares = $util.emptyArray;

        /**
         * Creates a new RemoveAppSharesRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.RemoveAppSharesRequest
         * @static
         * @param {Authentication.IRemoveAppSharesRequest=} [properties] Properties to set
         * @returns {Authentication.RemoveAppSharesRequest} RemoveAppSharesRequest instance
         */
        RemoveAppSharesRequest.create = function create(properties) {
            return new RemoveAppSharesRequest(properties);
        };

        /**
         * Encodes the specified RemoveAppSharesRequest message. Does not implicitly {@link Authentication.RemoveAppSharesRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.RemoveAppSharesRequest
         * @static
         * @param {Authentication.IRemoveAppSharesRequest} message RemoveAppSharesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RemoveAppSharesRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.appRecordUid != null && Object.hasOwnProperty.call(message, "appRecordUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.appRecordUid);
            if (message.shares != null && message.shares.length)
                for (let i = 0; i < message.shares.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.shares[i]);
            return writer;
        };

        /**
         * Decodes a RemoveAppSharesRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.RemoveAppSharesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.RemoveAppSharesRequest} RemoveAppSharesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RemoveAppSharesRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.RemoveAppSharesRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.appRecordUid = reader.bytes();
                        break;
                    }
                case 2: {
                        if (!(message.shares && message.shares.length))
                            message.shares = [];
                        message.shares.push(reader.bytes());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a RemoveAppSharesRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.RemoveAppSharesRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.RemoveAppSharesRequest} RemoveAppSharesRequest
         */
        RemoveAppSharesRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.RemoveAppSharesRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.RemoveAppSharesRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.RemoveAppSharesRequest();
            if (object.appRecordUid != null)
                if (typeof object.appRecordUid === "string")
                    $util.base64.decode(object.appRecordUid, message.appRecordUid = $util.newBuffer($util.base64.length(object.appRecordUid)), 0);
                else if (object.appRecordUid.length >= 0)
                    message.appRecordUid = object.appRecordUid;
            if (object.shares) {
                if (!Array.isArray(object.shares))
                    throw TypeError(".Authentication.RemoveAppSharesRequest.shares: array expected");
                message.shares = [];
                for (let i = 0; i < object.shares.length; ++i)
                    if (typeof object.shares[i] === "string")
                        $util.base64.decode(object.shares[i], message.shares[i] = $util.newBuffer($util.base64.length(object.shares[i])), 0);
                    else if (object.shares[i].length >= 0)
                        message.shares[i] = object.shares[i];
            }
            return message;
        };

        /**
         * Creates a plain object from a RemoveAppSharesRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.RemoveAppSharesRequest
         * @static
         * @param {Authentication.RemoveAppSharesRequest} message RemoveAppSharesRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RemoveAppSharesRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.shares = [];
            if (options.defaults)
                if (options.bytes === String)
                    object.appRecordUid = "";
                else {
                    object.appRecordUid = [];
                    if (options.bytes !== Array)
                        object.appRecordUid = $util.newBuffer(object.appRecordUid);
                }
            if (message.appRecordUid != null && Object.hasOwnProperty.call(message, "appRecordUid"))
                object.appRecordUid = options.bytes === String ? $util.base64.encode(message.appRecordUid, 0, message.appRecordUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.appRecordUid) : message.appRecordUid;
            if (message.shares && message.shares.length) {
                object.shares = [];
                for (let j = 0; j < message.shares.length; ++j)
                    object.shares[j] = options.bytes === String ? $util.base64.encode(message.shares[j], 0, message.shares[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.shares[j]) : message.shares[j];
            }
            return object;
        };

        /**
         * Converts this RemoveAppSharesRequest to JSON.
         * @function toJSON
         * @memberof Authentication.RemoveAppSharesRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RemoveAppSharesRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RemoveAppSharesRequest
         * @function getTypeUrl
         * @memberof Authentication.RemoveAppSharesRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RemoveAppSharesRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.RemoveAppSharesRequest";
        };

        return RemoveAppSharesRequest;
    })();

    Authentication.AppShareAdd = (function() {

        /**
         * Properties of an AppShareAdd.
         * @memberof Authentication
         * @interface IAppShareAdd
         * @property {Uint8Array|null} [secretUid] AppShareAdd secretUid
         * @property {Authentication.ApplicationShareType|null} [shareType] AppShareAdd shareType
         * @property {Uint8Array|null} [encryptedSecretKey] AppShareAdd encryptedSecretKey
         * @property {boolean|null} [editable] AppShareAdd editable
         */

        /**
         * Constructs a new AppShareAdd.
         * @memberof Authentication
         * @classdesc Represents an AppShareAdd.
         * @implements IAppShareAdd
         * @constructor
         * @param {Authentication.IAppShareAdd=} [properties] Properties to set
         */
        function AppShareAdd(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AppShareAdd secretUid.
         * @member {Uint8Array} secretUid
         * @memberof Authentication.AppShareAdd
         * @instance
         */
        AppShareAdd.prototype.secretUid = $util.newBuffer([]);

        /**
         * AppShareAdd shareType.
         * @member {Authentication.ApplicationShareType} shareType
         * @memberof Authentication.AppShareAdd
         * @instance
         */
        AppShareAdd.prototype.shareType = 0;

        /**
         * AppShareAdd encryptedSecretKey.
         * @member {Uint8Array} encryptedSecretKey
         * @memberof Authentication.AppShareAdd
         * @instance
         */
        AppShareAdd.prototype.encryptedSecretKey = $util.newBuffer([]);

        /**
         * AppShareAdd editable.
         * @member {boolean} editable
         * @memberof Authentication.AppShareAdd
         * @instance
         */
        AppShareAdd.prototype.editable = false;

        /**
         * Creates a new AppShareAdd instance using the specified properties.
         * @function create
         * @memberof Authentication.AppShareAdd
         * @static
         * @param {Authentication.IAppShareAdd=} [properties] Properties to set
         * @returns {Authentication.AppShareAdd} AppShareAdd instance
         */
        AppShareAdd.create = function create(properties) {
            return new AppShareAdd(properties);
        };

        /**
         * Encodes the specified AppShareAdd message. Does not implicitly {@link Authentication.AppShareAdd.verify|verify} messages.
         * @function encode
         * @memberof Authentication.AppShareAdd
         * @static
         * @param {Authentication.IAppShareAdd} message AppShareAdd message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AppShareAdd.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.secretUid != null && Object.hasOwnProperty.call(message, "secretUid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.secretUid);
            if (message.shareType != null && Object.hasOwnProperty.call(message, "shareType"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.shareType);
            if (message.encryptedSecretKey != null && Object.hasOwnProperty.call(message, "encryptedSecretKey"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.encryptedSecretKey);
            if (message.editable != null && Object.hasOwnProperty.call(message, "editable"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.editable);
            return writer;
        };

        /**
         * Decodes an AppShareAdd message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.AppShareAdd
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.AppShareAdd} AppShareAdd
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AppShareAdd.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.AppShareAdd();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 2: {
                        message.secretUid = reader.bytes();
                        break;
                    }
                case 3: {
                        message.shareType = reader.int32();
                        break;
                    }
                case 4: {
                        message.encryptedSecretKey = reader.bytes();
                        break;
                    }
                case 5: {
                        message.editable = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an AppShareAdd message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.AppShareAdd
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.AppShareAdd} AppShareAdd
         */
        AppShareAdd.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.AppShareAdd)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.AppShareAdd: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.AppShareAdd();
            if (object.secretUid != null)
                if (typeof object.secretUid === "string")
                    $util.base64.decode(object.secretUid, message.secretUid = $util.newBuffer($util.base64.length(object.secretUid)), 0);
                else if (object.secretUid.length >= 0)
                    message.secretUid = object.secretUid;
            switch (object.shareType) {
            default:
                if (typeof object.shareType === "number") {
                    message.shareType = object.shareType;
                    break;
                }
                break;
            case "SHARE_TYPE_RECORD":
            case 0:
                message.shareType = 0;
                break;
            case "SHARE_TYPE_FOLDER":
            case 1:
                message.shareType = 1;
                break;
            }
            if (object.encryptedSecretKey != null)
                if (typeof object.encryptedSecretKey === "string")
                    $util.base64.decode(object.encryptedSecretKey, message.encryptedSecretKey = $util.newBuffer($util.base64.length(object.encryptedSecretKey)), 0);
                else if (object.encryptedSecretKey.length >= 0)
                    message.encryptedSecretKey = object.encryptedSecretKey;
            if (object.editable != null)
                message.editable = Boolean(object.editable);
            return message;
        };

        /**
         * Creates a plain object from an AppShareAdd message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.AppShareAdd
         * @static
         * @param {Authentication.AppShareAdd} message AppShareAdd
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AppShareAdd.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.secretUid = "";
                else {
                    object.secretUid = [];
                    if (options.bytes !== Array)
                        object.secretUid = $util.newBuffer(object.secretUid);
                }
                object.shareType = options.enums === String ? "SHARE_TYPE_RECORD" : 0;
                if (options.bytes === String)
                    object.encryptedSecretKey = "";
                else {
                    object.encryptedSecretKey = [];
                    if (options.bytes !== Array)
                        object.encryptedSecretKey = $util.newBuffer(object.encryptedSecretKey);
                }
                object.editable = false;
            }
            if (message.secretUid != null && Object.hasOwnProperty.call(message, "secretUid"))
                object.secretUid = options.bytes === String ? $util.base64.encode(message.secretUid, 0, message.secretUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.secretUid) : message.secretUid;
            if (message.shareType != null && Object.hasOwnProperty.call(message, "shareType"))
                object.shareType = options.enums === String ? $root.Authentication.ApplicationShareType[message.shareType] === undefined ? message.shareType : $root.Authentication.ApplicationShareType[message.shareType] : message.shareType;
            if (message.encryptedSecretKey != null && Object.hasOwnProperty.call(message, "encryptedSecretKey"))
                object.encryptedSecretKey = options.bytes === String ? $util.base64.encode(message.encryptedSecretKey, 0, message.encryptedSecretKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedSecretKey) : message.encryptedSecretKey;
            if (message.editable != null && Object.hasOwnProperty.call(message, "editable"))
                object.editable = message.editable;
            return object;
        };

        /**
         * Converts this AppShareAdd to JSON.
         * @function toJSON
         * @memberof Authentication.AppShareAdd
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AppShareAdd.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AppShareAdd
         * @function getTypeUrl
         * @memberof Authentication.AppShareAdd
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AppShareAdd.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.AppShareAdd";
        };

        return AppShareAdd;
    })();

    Authentication.AppShare = (function() {

        /**
         * Properties of an AppShare.
         * @memberof Authentication
         * @interface IAppShare
         * @property {Uint8Array|null} [secretUid] AppShare secretUid
         * @property {Authentication.ApplicationShareType|null} [shareType] AppShare shareType
         * @property {boolean|null} [editable] AppShare editable
         * @property {number|null} [createdOn] AppShare createdOn
         * @property {Uint8Array|null} [data] AppShare data
         */

        /**
         * Constructs a new AppShare.
         * @memberof Authentication
         * @classdesc Represents an AppShare.
         * @implements IAppShare
         * @constructor
         * @param {Authentication.IAppShare=} [properties] Properties to set
         */
        function AppShare(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AppShare secretUid.
         * @member {Uint8Array} secretUid
         * @memberof Authentication.AppShare
         * @instance
         */
        AppShare.prototype.secretUid = $util.newBuffer([]);

        /**
         * AppShare shareType.
         * @member {Authentication.ApplicationShareType} shareType
         * @memberof Authentication.AppShare
         * @instance
         */
        AppShare.prototype.shareType = 0;

        /**
         * AppShare editable.
         * @member {boolean} editable
         * @memberof Authentication.AppShare
         * @instance
         */
        AppShare.prototype.editable = false;

        /**
         * AppShare createdOn.
         * @member {number} createdOn
         * @memberof Authentication.AppShare
         * @instance
         */
        AppShare.prototype.createdOn = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AppShare data.
         * @member {Uint8Array} data
         * @memberof Authentication.AppShare
         * @instance
         */
        AppShare.prototype.data = $util.newBuffer([]);

        /**
         * Creates a new AppShare instance using the specified properties.
         * @function create
         * @memberof Authentication.AppShare
         * @static
         * @param {Authentication.IAppShare=} [properties] Properties to set
         * @returns {Authentication.AppShare} AppShare instance
         */
        AppShare.create = function create(properties) {
            return new AppShare(properties);
        };

        /**
         * Encodes the specified AppShare message. Does not implicitly {@link Authentication.AppShare.verify|verify} messages.
         * @function encode
         * @memberof Authentication.AppShare
         * @static
         * @param {Authentication.IAppShare} message AppShare message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AppShare.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.secretUid != null && Object.hasOwnProperty.call(message, "secretUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.secretUid);
            if (message.shareType != null && Object.hasOwnProperty.call(message, "shareType"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.shareType);
            if (message.editable != null && Object.hasOwnProperty.call(message, "editable"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.editable);
            if (message.createdOn != null && Object.hasOwnProperty.call(message, "createdOn"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.createdOn);
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.data);
            return writer;
        };

        /**
         * Decodes an AppShare message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.AppShare
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.AppShare} AppShare
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AppShare.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.AppShare();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.secretUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.shareType = reader.int32();
                        break;
                    }
                case 3: {
                        message.editable = reader.bool();
                        break;
                    }
                case 4: {
                        message.createdOn = reader.int64();
                        break;
                    }
                case 5: {
                        message.data = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an AppShare message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.AppShare
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.AppShare} AppShare
         */
        AppShare.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.AppShare)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.AppShare: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.AppShare();
            if (object.secretUid != null)
                if (typeof object.secretUid === "string")
                    $util.base64.decode(object.secretUid, message.secretUid = $util.newBuffer($util.base64.length(object.secretUid)), 0);
                else if (object.secretUid.length >= 0)
                    message.secretUid = object.secretUid;
            switch (object.shareType) {
            default:
                if (typeof object.shareType === "number") {
                    message.shareType = object.shareType;
                    break;
                }
                break;
            case "SHARE_TYPE_RECORD":
            case 0:
                message.shareType = 0;
                break;
            case "SHARE_TYPE_FOLDER":
            case 1:
                message.shareType = 1;
                break;
            }
            if (object.editable != null)
                message.editable = Boolean(object.editable);
            if (object.createdOn != null)
                if ($util.Long)
                    message.createdOn = $util.Long.fromValue(object.createdOn, false);
                else if (typeof object.createdOn === "string")
                    message.createdOn = parseInt(object.createdOn, 10);
                else if (typeof object.createdOn === "number")
                    message.createdOn = object.createdOn;
                else if (typeof object.createdOn === "object")
                    message.createdOn = new $util.LongBits(object.createdOn.low >>> 0, object.createdOn.high >>> 0).toNumber();
            if (object.data != null)
                if (typeof object.data === "string")
                    $util.base64.decode(object.data, message.data = $util.newBuffer($util.base64.length(object.data)), 0);
                else if (object.data.length >= 0)
                    message.data = object.data;
            return message;
        };

        /**
         * Creates a plain object from an AppShare message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.AppShare
         * @static
         * @param {Authentication.AppShare} message AppShare
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AppShare.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.secretUid = "";
                else {
                    object.secretUid = [];
                    if (options.bytes !== Array)
                        object.secretUid = $util.newBuffer(object.secretUid);
                }
                object.shareType = options.enums === String ? "SHARE_TYPE_RECORD" : 0;
                object.editable = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.createdOn = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.createdOn = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.data = "";
                else {
                    object.data = [];
                    if (options.bytes !== Array)
                        object.data = $util.newBuffer(object.data);
                }
            }
            if (message.secretUid != null && Object.hasOwnProperty.call(message, "secretUid"))
                object.secretUid = options.bytes === String ? $util.base64.encode(message.secretUid, 0, message.secretUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.secretUid) : message.secretUid;
            if (message.shareType != null && Object.hasOwnProperty.call(message, "shareType"))
                object.shareType = options.enums === String ? $root.Authentication.ApplicationShareType[message.shareType] === undefined ? message.shareType : $root.Authentication.ApplicationShareType[message.shareType] : message.shareType;
            if (message.editable != null && Object.hasOwnProperty.call(message, "editable"))
                object.editable = message.editable;
            if (message.createdOn != null && Object.hasOwnProperty.call(message, "createdOn"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.createdOn = typeof message.createdOn === "number" ? BigInt(message.createdOn) : $util.Long.fromBits(message.createdOn.low >>> 0, message.createdOn.high >>> 0, false).toBigInt();
                else if (typeof message.createdOn === "number")
                    object.createdOn = options.longs === String ? String(message.createdOn) : message.createdOn;
                else
                    object.createdOn = options.longs === String ? $util.Long.prototype.toString.call(message.createdOn) : options.longs === Number ? new $util.LongBits(message.createdOn.low >>> 0, message.createdOn.high >>> 0).toNumber() : message.createdOn;
            if (message.data != null && Object.hasOwnProperty.call(message, "data"))
                object.data = options.bytes === String ? $util.base64.encode(message.data, 0, message.data.length) : options.bytes === Array ? Array.prototype.slice.call(message.data) : message.data;
            return object;
        };

        /**
         * Converts this AppShare to JSON.
         * @function toJSON
         * @memberof Authentication.AppShare
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AppShare.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AppShare
         * @function getTypeUrl
         * @memberof Authentication.AppShare
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AppShare.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.AppShare";
        };

        return AppShare;
    })();

    Authentication.AddAppClientRequest = (function() {

        /**
         * Properties of an AddAppClientRequest.
         * @memberof Authentication
         * @interface IAddAppClientRequest
         * @property {Uint8Array|null} [appRecordUid] AddAppClientRequest appRecordUid
         * @property {Uint8Array|null} [encryptedAppKey] AddAppClientRequest encryptedAppKey
         * @property {Uint8Array|null} [clientId] AddAppClientRequest clientId
         * @property {boolean|null} [lockIp] AddAppClientRequest lockIp
         * @property {number|null} [firstAccessExpireOn] AddAppClientRequest firstAccessExpireOn
         * @property {number|null} [accessExpireOn] AddAppClientRequest accessExpireOn
         * @property {string|null} [id] AddAppClientRequest id
         * @property {Enterprise.AppClientType|null} [appClientType] AddAppClientRequest appClientType
         */

        /**
         * Constructs a new AddAppClientRequest.
         * @memberof Authentication
         * @classdesc Represents an AddAppClientRequest.
         * @implements IAddAppClientRequest
         * @constructor
         * @param {Authentication.IAddAppClientRequest=} [properties] Properties to set
         */
        function AddAppClientRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AddAppClientRequest appRecordUid.
         * @member {Uint8Array} appRecordUid
         * @memberof Authentication.AddAppClientRequest
         * @instance
         */
        AddAppClientRequest.prototype.appRecordUid = $util.newBuffer([]);

        /**
         * AddAppClientRequest encryptedAppKey.
         * @member {Uint8Array} encryptedAppKey
         * @memberof Authentication.AddAppClientRequest
         * @instance
         */
        AddAppClientRequest.prototype.encryptedAppKey = $util.newBuffer([]);

        /**
         * AddAppClientRequest clientId.
         * @member {Uint8Array} clientId
         * @memberof Authentication.AddAppClientRequest
         * @instance
         */
        AddAppClientRequest.prototype.clientId = $util.newBuffer([]);

        /**
         * AddAppClientRequest lockIp.
         * @member {boolean} lockIp
         * @memberof Authentication.AddAppClientRequest
         * @instance
         */
        AddAppClientRequest.prototype.lockIp = false;

        /**
         * AddAppClientRequest firstAccessExpireOn.
         * @member {number} firstAccessExpireOn
         * @memberof Authentication.AddAppClientRequest
         * @instance
         */
        AddAppClientRequest.prototype.firstAccessExpireOn = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AddAppClientRequest accessExpireOn.
         * @member {number} accessExpireOn
         * @memberof Authentication.AddAppClientRequest
         * @instance
         */
        AddAppClientRequest.prototype.accessExpireOn = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AddAppClientRequest id.
         * @member {string} id
         * @memberof Authentication.AddAppClientRequest
         * @instance
         */
        AddAppClientRequest.prototype.id = "";

        /**
         * AddAppClientRequest appClientType.
         * @member {Enterprise.AppClientType} appClientType
         * @memberof Authentication.AddAppClientRequest
         * @instance
         */
        AddAppClientRequest.prototype.appClientType = 0;

        /**
         * Creates a new AddAppClientRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.AddAppClientRequest
         * @static
         * @param {Authentication.IAddAppClientRequest=} [properties] Properties to set
         * @returns {Authentication.AddAppClientRequest} AddAppClientRequest instance
         */
        AddAppClientRequest.create = function create(properties) {
            return new AddAppClientRequest(properties);
        };

        /**
         * Encodes the specified AddAppClientRequest message. Does not implicitly {@link Authentication.AddAppClientRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.AddAppClientRequest
         * @static
         * @param {Authentication.IAddAppClientRequest} message AddAppClientRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddAppClientRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.appRecordUid != null && Object.hasOwnProperty.call(message, "appRecordUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.appRecordUid);
            if (message.encryptedAppKey != null && Object.hasOwnProperty.call(message, "encryptedAppKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encryptedAppKey);
            if (message.clientId != null && Object.hasOwnProperty.call(message, "clientId"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.clientId);
            if (message.lockIp != null && Object.hasOwnProperty.call(message, "lockIp"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.lockIp);
            if (message.firstAccessExpireOn != null && Object.hasOwnProperty.call(message, "firstAccessExpireOn"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.firstAccessExpireOn);
            if (message.accessExpireOn != null && Object.hasOwnProperty.call(message, "accessExpireOn"))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.accessExpireOn);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.id);
            if (message.appClientType != null && Object.hasOwnProperty.call(message, "appClientType"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.appClientType);
            return writer;
        };

        /**
         * Decodes an AddAppClientRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.AddAppClientRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.AddAppClientRequest} AddAppClientRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddAppClientRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.AddAppClientRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.appRecordUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.encryptedAppKey = reader.bytes();
                        break;
                    }
                case 3: {
                        message.clientId = reader.bytes();
                        break;
                    }
                case 4: {
                        message.lockIp = reader.bool();
                        break;
                    }
                case 5: {
                        message.firstAccessExpireOn = reader.int64();
                        break;
                    }
                case 6: {
                        message.accessExpireOn = reader.int64();
                        break;
                    }
                case 7: {
                        message.id = reader.string();
                        break;
                    }
                case 8: {
                        message.appClientType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an AddAppClientRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.AddAppClientRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.AddAppClientRequest} AddAppClientRequest
         */
        AddAppClientRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.AddAppClientRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.AddAppClientRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.AddAppClientRequest();
            if (object.appRecordUid != null)
                if (typeof object.appRecordUid === "string")
                    $util.base64.decode(object.appRecordUid, message.appRecordUid = $util.newBuffer($util.base64.length(object.appRecordUid)), 0);
                else if (object.appRecordUid.length >= 0)
                    message.appRecordUid = object.appRecordUid;
            if (object.encryptedAppKey != null)
                if (typeof object.encryptedAppKey === "string")
                    $util.base64.decode(object.encryptedAppKey, message.encryptedAppKey = $util.newBuffer($util.base64.length(object.encryptedAppKey)), 0);
                else if (object.encryptedAppKey.length >= 0)
                    message.encryptedAppKey = object.encryptedAppKey;
            if (object.clientId != null)
                if (typeof object.clientId === "string")
                    $util.base64.decode(object.clientId, message.clientId = $util.newBuffer($util.base64.length(object.clientId)), 0);
                else if (object.clientId.length >= 0)
                    message.clientId = object.clientId;
            if (object.lockIp != null)
                message.lockIp = Boolean(object.lockIp);
            if (object.firstAccessExpireOn != null)
                if ($util.Long)
                    message.firstAccessExpireOn = $util.Long.fromValue(object.firstAccessExpireOn, false);
                else if (typeof object.firstAccessExpireOn === "string")
                    message.firstAccessExpireOn = parseInt(object.firstAccessExpireOn, 10);
                else if (typeof object.firstAccessExpireOn === "number")
                    message.firstAccessExpireOn = object.firstAccessExpireOn;
                else if (typeof object.firstAccessExpireOn === "object")
                    message.firstAccessExpireOn = new $util.LongBits(object.firstAccessExpireOn.low >>> 0, object.firstAccessExpireOn.high >>> 0).toNumber();
            if (object.accessExpireOn != null)
                if ($util.Long)
                    message.accessExpireOn = $util.Long.fromValue(object.accessExpireOn, false);
                else if (typeof object.accessExpireOn === "string")
                    message.accessExpireOn = parseInt(object.accessExpireOn, 10);
                else if (typeof object.accessExpireOn === "number")
                    message.accessExpireOn = object.accessExpireOn;
                else if (typeof object.accessExpireOn === "object")
                    message.accessExpireOn = new $util.LongBits(object.accessExpireOn.low >>> 0, object.accessExpireOn.high >>> 0).toNumber();
            if (object.id != null)
                message.id = String(object.id);
            switch (object.appClientType) {
            default:
                if (typeof object.appClientType === "number") {
                    message.appClientType = object.appClientType;
                    break;
                }
                break;
            case "NOT_USED":
            case 0:
                message.appClientType = 0;
                break;
            case "GENERAL":
            case 1:
                message.appClientType = 1;
                break;
            case "DISCOVERY_AND_ROTATION_CONTROLLER":
            case 2:
                message.appClientType = 2;
                break;
            case "KCM_CONTROLLER":
            case 3:
                message.appClientType = 3;
                break;
            case "SELF_DESTRUCT":
            case 4:
                message.appClientType = 4;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from an AddAppClientRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.AddAppClientRequest
         * @static
         * @param {Authentication.AddAppClientRequest} message AddAppClientRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AddAppClientRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.appRecordUid = "";
                else {
                    object.appRecordUid = [];
                    if (options.bytes !== Array)
                        object.appRecordUid = $util.newBuffer(object.appRecordUid);
                }
                if (options.bytes === String)
                    object.encryptedAppKey = "";
                else {
                    object.encryptedAppKey = [];
                    if (options.bytes !== Array)
                        object.encryptedAppKey = $util.newBuffer(object.encryptedAppKey);
                }
                if (options.bytes === String)
                    object.clientId = "";
                else {
                    object.clientId = [];
                    if (options.bytes !== Array)
                        object.clientId = $util.newBuffer(object.clientId);
                }
                object.lockIp = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.firstAccessExpireOn = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.firstAccessExpireOn = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.accessExpireOn = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.accessExpireOn = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.id = "";
                object.appClientType = options.enums === String ? "NOT_USED" : 0;
            }
            if (message.appRecordUid != null && Object.hasOwnProperty.call(message, "appRecordUid"))
                object.appRecordUid = options.bytes === String ? $util.base64.encode(message.appRecordUid, 0, message.appRecordUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.appRecordUid) : message.appRecordUid;
            if (message.encryptedAppKey != null && Object.hasOwnProperty.call(message, "encryptedAppKey"))
                object.encryptedAppKey = options.bytes === String ? $util.base64.encode(message.encryptedAppKey, 0, message.encryptedAppKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedAppKey) : message.encryptedAppKey;
            if (message.clientId != null && Object.hasOwnProperty.call(message, "clientId"))
                object.clientId = options.bytes === String ? $util.base64.encode(message.clientId, 0, message.clientId.length) : options.bytes === Array ? Array.prototype.slice.call(message.clientId) : message.clientId;
            if (message.lockIp != null && Object.hasOwnProperty.call(message, "lockIp"))
                object.lockIp = message.lockIp;
            if (message.firstAccessExpireOn != null && Object.hasOwnProperty.call(message, "firstAccessExpireOn"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.firstAccessExpireOn = typeof message.firstAccessExpireOn === "number" ? BigInt(message.firstAccessExpireOn) : $util.Long.fromBits(message.firstAccessExpireOn.low >>> 0, message.firstAccessExpireOn.high >>> 0, false).toBigInt();
                else if (typeof message.firstAccessExpireOn === "number")
                    object.firstAccessExpireOn = options.longs === String ? String(message.firstAccessExpireOn) : message.firstAccessExpireOn;
                else
                    object.firstAccessExpireOn = options.longs === String ? $util.Long.prototype.toString.call(message.firstAccessExpireOn) : options.longs === Number ? new $util.LongBits(message.firstAccessExpireOn.low >>> 0, message.firstAccessExpireOn.high >>> 0).toNumber() : message.firstAccessExpireOn;
            if (message.accessExpireOn != null && Object.hasOwnProperty.call(message, "accessExpireOn"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.accessExpireOn = typeof message.accessExpireOn === "number" ? BigInt(message.accessExpireOn) : $util.Long.fromBits(message.accessExpireOn.low >>> 0, message.accessExpireOn.high >>> 0, false).toBigInt();
                else if (typeof message.accessExpireOn === "number")
                    object.accessExpireOn = options.longs === String ? String(message.accessExpireOn) : message.accessExpireOn;
                else
                    object.accessExpireOn = options.longs === String ? $util.Long.prototype.toString.call(message.accessExpireOn) : options.longs === Number ? new $util.LongBits(message.accessExpireOn.low >>> 0, message.accessExpireOn.high >>> 0).toNumber() : message.accessExpireOn;
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.appClientType != null && Object.hasOwnProperty.call(message, "appClientType"))
                object.appClientType = options.enums === String ? $root.Enterprise.AppClientType[message.appClientType] === undefined ? message.appClientType : $root.Enterprise.AppClientType[message.appClientType] : message.appClientType;
            return object;
        };

        /**
         * Converts this AddAppClientRequest to JSON.
         * @function toJSON
         * @memberof Authentication.AddAppClientRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AddAppClientRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AddAppClientRequest
         * @function getTypeUrl
         * @memberof Authentication.AddAppClientRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AddAppClientRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.AddAppClientRequest";
        };

        return AddAppClientRequest;
    })();

    Authentication.RemoveAppClientsRequest = (function() {

        /**
         * Properties of a RemoveAppClientsRequest.
         * @memberof Authentication
         * @interface IRemoveAppClientsRequest
         * @property {Uint8Array|null} [appRecordUid] RemoveAppClientsRequest appRecordUid
         * @property {Array.<Uint8Array>|null} [clients] RemoveAppClientsRequest clients
         */

        /**
         * Constructs a new RemoveAppClientsRequest.
         * @memberof Authentication
         * @classdesc Represents a RemoveAppClientsRequest.
         * @implements IRemoveAppClientsRequest
         * @constructor
         * @param {Authentication.IRemoveAppClientsRequest=} [properties] Properties to set
         */
        function RemoveAppClientsRequest(properties) {
            this.clients = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RemoveAppClientsRequest appRecordUid.
         * @member {Uint8Array} appRecordUid
         * @memberof Authentication.RemoveAppClientsRequest
         * @instance
         */
        RemoveAppClientsRequest.prototype.appRecordUid = $util.newBuffer([]);

        /**
         * RemoveAppClientsRequest clients.
         * @member {Array.<Uint8Array>} clients
         * @memberof Authentication.RemoveAppClientsRequest
         * @instance
         */
        RemoveAppClientsRequest.prototype.clients = $util.emptyArray;

        /**
         * Creates a new RemoveAppClientsRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.RemoveAppClientsRequest
         * @static
         * @param {Authentication.IRemoveAppClientsRequest=} [properties] Properties to set
         * @returns {Authentication.RemoveAppClientsRequest} RemoveAppClientsRequest instance
         */
        RemoveAppClientsRequest.create = function create(properties) {
            return new RemoveAppClientsRequest(properties);
        };

        /**
         * Encodes the specified RemoveAppClientsRequest message. Does not implicitly {@link Authentication.RemoveAppClientsRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.RemoveAppClientsRequest
         * @static
         * @param {Authentication.IRemoveAppClientsRequest} message RemoveAppClientsRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RemoveAppClientsRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.appRecordUid != null && Object.hasOwnProperty.call(message, "appRecordUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.appRecordUid);
            if (message.clients != null && message.clients.length)
                for (let i = 0; i < message.clients.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.clients[i]);
            return writer;
        };

        /**
         * Decodes a RemoveAppClientsRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.RemoveAppClientsRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.RemoveAppClientsRequest} RemoveAppClientsRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RemoveAppClientsRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.RemoveAppClientsRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.appRecordUid = reader.bytes();
                        break;
                    }
                case 2: {
                        if (!(message.clients && message.clients.length))
                            message.clients = [];
                        message.clients.push(reader.bytes());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a RemoveAppClientsRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.RemoveAppClientsRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.RemoveAppClientsRequest} RemoveAppClientsRequest
         */
        RemoveAppClientsRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.RemoveAppClientsRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.RemoveAppClientsRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.RemoveAppClientsRequest();
            if (object.appRecordUid != null)
                if (typeof object.appRecordUid === "string")
                    $util.base64.decode(object.appRecordUid, message.appRecordUid = $util.newBuffer($util.base64.length(object.appRecordUid)), 0);
                else if (object.appRecordUid.length >= 0)
                    message.appRecordUid = object.appRecordUid;
            if (object.clients) {
                if (!Array.isArray(object.clients))
                    throw TypeError(".Authentication.RemoveAppClientsRequest.clients: array expected");
                message.clients = [];
                for (let i = 0; i < object.clients.length; ++i)
                    if (typeof object.clients[i] === "string")
                        $util.base64.decode(object.clients[i], message.clients[i] = $util.newBuffer($util.base64.length(object.clients[i])), 0);
                    else if (object.clients[i].length >= 0)
                        message.clients[i] = object.clients[i];
            }
            return message;
        };

        /**
         * Creates a plain object from a RemoveAppClientsRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.RemoveAppClientsRequest
         * @static
         * @param {Authentication.RemoveAppClientsRequest} message RemoveAppClientsRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RemoveAppClientsRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.clients = [];
            if (options.defaults)
                if (options.bytes === String)
                    object.appRecordUid = "";
                else {
                    object.appRecordUid = [];
                    if (options.bytes !== Array)
                        object.appRecordUid = $util.newBuffer(object.appRecordUid);
                }
            if (message.appRecordUid != null && Object.hasOwnProperty.call(message, "appRecordUid"))
                object.appRecordUid = options.bytes === String ? $util.base64.encode(message.appRecordUid, 0, message.appRecordUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.appRecordUid) : message.appRecordUid;
            if (message.clients && message.clients.length) {
                object.clients = [];
                for (let j = 0; j < message.clients.length; ++j)
                    object.clients[j] = options.bytes === String ? $util.base64.encode(message.clients[j], 0, message.clients[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.clients[j]) : message.clients[j];
            }
            return object;
        };

        /**
         * Converts this RemoveAppClientsRequest to JSON.
         * @function toJSON
         * @memberof Authentication.RemoveAppClientsRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RemoveAppClientsRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RemoveAppClientsRequest
         * @function getTypeUrl
         * @memberof Authentication.RemoveAppClientsRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RemoveAppClientsRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.RemoveAppClientsRequest";
        };

        return RemoveAppClientsRequest;
    })();

    Authentication.AddExternalShareRequest = (function() {

        /**
         * Properties of an AddExternalShareRequest.
         * @memberof Authentication
         * @interface IAddExternalShareRequest
         * @property {Uint8Array|null} [recordUid] AddExternalShareRequest recordUid
         * @property {Uint8Array|null} [encryptedRecordKey] AddExternalShareRequest encryptedRecordKey
         * @property {Uint8Array|null} [clientId] AddExternalShareRequest clientId
         * @property {number|null} [accessExpireOn] AddExternalShareRequest accessExpireOn
         * @property {string|null} [id] AddExternalShareRequest id
         * @property {boolean|null} [isSelfDestruct] AddExternalShareRequest isSelfDestruct
         * @property {boolean|null} [isEditable] AddExternalShareRequest isEditable
         */

        /**
         * Constructs a new AddExternalShareRequest.
         * @memberof Authentication
         * @classdesc Represents an AddExternalShareRequest.
         * @implements IAddExternalShareRequest
         * @constructor
         * @param {Authentication.IAddExternalShareRequest=} [properties] Properties to set
         */
        function AddExternalShareRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AddExternalShareRequest recordUid.
         * @member {Uint8Array} recordUid
         * @memberof Authentication.AddExternalShareRequest
         * @instance
         */
        AddExternalShareRequest.prototype.recordUid = $util.newBuffer([]);

        /**
         * AddExternalShareRequest encryptedRecordKey.
         * @member {Uint8Array} encryptedRecordKey
         * @memberof Authentication.AddExternalShareRequest
         * @instance
         */
        AddExternalShareRequest.prototype.encryptedRecordKey = $util.newBuffer([]);

        /**
         * AddExternalShareRequest clientId.
         * @member {Uint8Array} clientId
         * @memberof Authentication.AddExternalShareRequest
         * @instance
         */
        AddExternalShareRequest.prototype.clientId = $util.newBuffer([]);

        /**
         * AddExternalShareRequest accessExpireOn.
         * @member {number} accessExpireOn
         * @memberof Authentication.AddExternalShareRequest
         * @instance
         */
        AddExternalShareRequest.prototype.accessExpireOn = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AddExternalShareRequest id.
         * @member {string} id
         * @memberof Authentication.AddExternalShareRequest
         * @instance
         */
        AddExternalShareRequest.prototype.id = "";

        /**
         * AddExternalShareRequest isSelfDestruct.
         * @member {boolean} isSelfDestruct
         * @memberof Authentication.AddExternalShareRequest
         * @instance
         */
        AddExternalShareRequest.prototype.isSelfDestruct = false;

        /**
         * AddExternalShareRequest isEditable.
         * @member {boolean} isEditable
         * @memberof Authentication.AddExternalShareRequest
         * @instance
         */
        AddExternalShareRequest.prototype.isEditable = false;

        /**
         * Creates a new AddExternalShareRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.AddExternalShareRequest
         * @static
         * @param {Authentication.IAddExternalShareRequest=} [properties] Properties to set
         * @returns {Authentication.AddExternalShareRequest} AddExternalShareRequest instance
         */
        AddExternalShareRequest.create = function create(properties) {
            return new AddExternalShareRequest(properties);
        };

        /**
         * Encodes the specified AddExternalShareRequest message. Does not implicitly {@link Authentication.AddExternalShareRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.AddExternalShareRequest
         * @static
         * @param {Authentication.IAddExternalShareRequest} message AddExternalShareRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AddExternalShareRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.recordUid);
            if (message.encryptedRecordKey != null && Object.hasOwnProperty.call(message, "encryptedRecordKey"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encryptedRecordKey);
            if (message.clientId != null && Object.hasOwnProperty.call(message, "clientId"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.clientId);
            if (message.accessExpireOn != null && Object.hasOwnProperty.call(message, "accessExpireOn"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.accessExpireOn);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.id);
            if (message.isSelfDestruct != null && Object.hasOwnProperty.call(message, "isSelfDestruct"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.isSelfDestruct);
            if (message.isEditable != null && Object.hasOwnProperty.call(message, "isEditable"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.isEditable);
            return writer;
        };

        /**
         * Decodes an AddExternalShareRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.AddExternalShareRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.AddExternalShareRequest} AddExternalShareRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AddExternalShareRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.AddExternalShareRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.recordUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.encryptedRecordKey = reader.bytes();
                        break;
                    }
                case 3: {
                        message.clientId = reader.bytes();
                        break;
                    }
                case 4: {
                        message.accessExpireOn = reader.int64();
                        break;
                    }
                case 5: {
                        message.id = reader.string();
                        break;
                    }
                case 6: {
                        message.isSelfDestruct = reader.bool();
                        break;
                    }
                case 7: {
                        message.isEditable = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an AddExternalShareRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.AddExternalShareRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.AddExternalShareRequest} AddExternalShareRequest
         */
        AddExternalShareRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.AddExternalShareRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.AddExternalShareRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.AddExternalShareRequest();
            if (object.recordUid != null)
                if (typeof object.recordUid === "string")
                    $util.base64.decode(object.recordUid, message.recordUid = $util.newBuffer($util.base64.length(object.recordUid)), 0);
                else if (object.recordUid.length >= 0)
                    message.recordUid = object.recordUid;
            if (object.encryptedRecordKey != null)
                if (typeof object.encryptedRecordKey === "string")
                    $util.base64.decode(object.encryptedRecordKey, message.encryptedRecordKey = $util.newBuffer($util.base64.length(object.encryptedRecordKey)), 0);
                else if (object.encryptedRecordKey.length >= 0)
                    message.encryptedRecordKey = object.encryptedRecordKey;
            if (object.clientId != null)
                if (typeof object.clientId === "string")
                    $util.base64.decode(object.clientId, message.clientId = $util.newBuffer($util.base64.length(object.clientId)), 0);
                else if (object.clientId.length >= 0)
                    message.clientId = object.clientId;
            if (object.accessExpireOn != null)
                if ($util.Long)
                    message.accessExpireOn = $util.Long.fromValue(object.accessExpireOn, false);
                else if (typeof object.accessExpireOn === "string")
                    message.accessExpireOn = parseInt(object.accessExpireOn, 10);
                else if (typeof object.accessExpireOn === "number")
                    message.accessExpireOn = object.accessExpireOn;
                else if (typeof object.accessExpireOn === "object")
                    message.accessExpireOn = new $util.LongBits(object.accessExpireOn.low >>> 0, object.accessExpireOn.high >>> 0).toNumber();
            if (object.id != null)
                message.id = String(object.id);
            if (object.isSelfDestruct != null)
                message.isSelfDestruct = Boolean(object.isSelfDestruct);
            if (object.isEditable != null)
                message.isEditable = Boolean(object.isEditable);
            return message;
        };

        /**
         * Creates a plain object from an AddExternalShareRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.AddExternalShareRequest
         * @static
         * @param {Authentication.AddExternalShareRequest} message AddExternalShareRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AddExternalShareRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.recordUid = "";
                else {
                    object.recordUid = [];
                    if (options.bytes !== Array)
                        object.recordUid = $util.newBuffer(object.recordUid);
                }
                if (options.bytes === String)
                    object.encryptedRecordKey = "";
                else {
                    object.encryptedRecordKey = [];
                    if (options.bytes !== Array)
                        object.encryptedRecordKey = $util.newBuffer(object.encryptedRecordKey);
                }
                if (options.bytes === String)
                    object.clientId = "";
                else {
                    object.clientId = [];
                    if (options.bytes !== Array)
                        object.clientId = $util.newBuffer(object.clientId);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.accessExpireOn = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.accessExpireOn = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.id = "";
                object.isSelfDestruct = false;
                object.isEditable = false;
            }
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                object.recordUid = options.bytes === String ? $util.base64.encode(message.recordUid, 0, message.recordUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.recordUid) : message.recordUid;
            if (message.encryptedRecordKey != null && Object.hasOwnProperty.call(message, "encryptedRecordKey"))
                object.encryptedRecordKey = options.bytes === String ? $util.base64.encode(message.encryptedRecordKey, 0, message.encryptedRecordKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedRecordKey) : message.encryptedRecordKey;
            if (message.clientId != null && Object.hasOwnProperty.call(message, "clientId"))
                object.clientId = options.bytes === String ? $util.base64.encode(message.clientId, 0, message.clientId.length) : options.bytes === Array ? Array.prototype.slice.call(message.clientId) : message.clientId;
            if (message.accessExpireOn != null && Object.hasOwnProperty.call(message, "accessExpireOn"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.accessExpireOn = typeof message.accessExpireOn === "number" ? BigInt(message.accessExpireOn) : $util.Long.fromBits(message.accessExpireOn.low >>> 0, message.accessExpireOn.high >>> 0, false).toBigInt();
                else if (typeof message.accessExpireOn === "number")
                    object.accessExpireOn = options.longs === String ? String(message.accessExpireOn) : message.accessExpireOn;
                else
                    object.accessExpireOn = options.longs === String ? $util.Long.prototype.toString.call(message.accessExpireOn) : options.longs === Number ? new $util.LongBits(message.accessExpireOn.low >>> 0, message.accessExpireOn.high >>> 0).toNumber() : message.accessExpireOn;
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.isSelfDestruct != null && Object.hasOwnProperty.call(message, "isSelfDestruct"))
                object.isSelfDestruct = message.isSelfDestruct;
            if (message.isEditable != null && Object.hasOwnProperty.call(message, "isEditable"))
                object.isEditable = message.isEditable;
            return object;
        };

        /**
         * Converts this AddExternalShareRequest to JSON.
         * @function toJSON
         * @memberof Authentication.AddExternalShareRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AddExternalShareRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AddExternalShareRequest
         * @function getTypeUrl
         * @memberof Authentication.AddExternalShareRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AddExternalShareRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.AddExternalShareRequest";
        };

        return AddExternalShareRequest;
    })();

    Authentication.AppClient = (function() {

        /**
         * Properties of an AppClient.
         * @memberof Authentication
         * @interface IAppClient
         * @property {string|null} [id] AppClient id
         * @property {Uint8Array|null} [clientId] AppClient clientId
         * @property {number|null} [createdOn] AppClient createdOn
         * @property {number|null} [firstAccess] AppClient firstAccess
         * @property {number|null} [lastAccess] AppClient lastAccess
         * @property {Uint8Array|null} [publicKey] AppClient publicKey
         * @property {boolean|null} [lockIp] AppClient lockIp
         * @property {string|null} [ipAddress] AppClient ipAddress
         * @property {number|null} [firstAccessExpireOn] AppClient firstAccessExpireOn
         * @property {number|null} [accessExpireOn] AppClient accessExpireOn
         * @property {Enterprise.AppClientType|null} [appClientType] AppClient appClientType
         * @property {boolean|null} [canEdit] AppClient canEdit
         */

        /**
         * Constructs a new AppClient.
         * @memberof Authentication
         * @classdesc Represents an AppClient.
         * @implements IAppClient
         * @constructor
         * @param {Authentication.IAppClient=} [properties] Properties to set
         */
        function AppClient(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AppClient id.
         * @member {string} id
         * @memberof Authentication.AppClient
         * @instance
         */
        AppClient.prototype.id = "";

        /**
         * AppClient clientId.
         * @member {Uint8Array} clientId
         * @memberof Authentication.AppClient
         * @instance
         */
        AppClient.prototype.clientId = $util.newBuffer([]);

        /**
         * AppClient createdOn.
         * @member {number} createdOn
         * @memberof Authentication.AppClient
         * @instance
         */
        AppClient.prototype.createdOn = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AppClient firstAccess.
         * @member {number} firstAccess
         * @memberof Authentication.AppClient
         * @instance
         */
        AppClient.prototype.firstAccess = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AppClient lastAccess.
         * @member {number} lastAccess
         * @memberof Authentication.AppClient
         * @instance
         */
        AppClient.prototype.lastAccess = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AppClient publicKey.
         * @member {Uint8Array} publicKey
         * @memberof Authentication.AppClient
         * @instance
         */
        AppClient.prototype.publicKey = $util.newBuffer([]);

        /**
         * AppClient lockIp.
         * @member {boolean} lockIp
         * @memberof Authentication.AppClient
         * @instance
         */
        AppClient.prototype.lockIp = false;

        /**
         * AppClient ipAddress.
         * @member {string} ipAddress
         * @memberof Authentication.AppClient
         * @instance
         */
        AppClient.prototype.ipAddress = "";

        /**
         * AppClient firstAccessExpireOn.
         * @member {number} firstAccessExpireOn
         * @memberof Authentication.AppClient
         * @instance
         */
        AppClient.prototype.firstAccessExpireOn = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AppClient accessExpireOn.
         * @member {number} accessExpireOn
         * @memberof Authentication.AppClient
         * @instance
         */
        AppClient.prototype.accessExpireOn = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AppClient appClientType.
         * @member {Enterprise.AppClientType} appClientType
         * @memberof Authentication.AppClient
         * @instance
         */
        AppClient.prototype.appClientType = 0;

        /**
         * AppClient canEdit.
         * @member {boolean} canEdit
         * @memberof Authentication.AppClient
         * @instance
         */
        AppClient.prototype.canEdit = false;

        /**
         * Creates a new AppClient instance using the specified properties.
         * @function create
         * @memberof Authentication.AppClient
         * @static
         * @param {Authentication.IAppClient=} [properties] Properties to set
         * @returns {Authentication.AppClient} AppClient instance
         */
        AppClient.create = function create(properties) {
            return new AppClient(properties);
        };

        /**
         * Encodes the specified AppClient message. Does not implicitly {@link Authentication.AppClient.verify|verify} messages.
         * @function encode
         * @memberof Authentication.AppClient
         * @static
         * @param {Authentication.IAppClient} message AppClient message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AppClient.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
            if (message.clientId != null && Object.hasOwnProperty.call(message, "clientId"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.clientId);
            if (message.createdOn != null && Object.hasOwnProperty.call(message, "createdOn"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.createdOn);
            if (message.firstAccess != null && Object.hasOwnProperty.call(message, "firstAccess"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.firstAccess);
            if (message.lastAccess != null && Object.hasOwnProperty.call(message, "lastAccess"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.lastAccess);
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.publicKey);
            if (message.lockIp != null && Object.hasOwnProperty.call(message, "lockIp"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.lockIp);
            if (message.ipAddress != null && Object.hasOwnProperty.call(message, "ipAddress"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.ipAddress);
            if (message.firstAccessExpireOn != null && Object.hasOwnProperty.call(message, "firstAccessExpireOn"))
                writer.uint32(/* id 9, wireType 0 =*/72).int64(message.firstAccessExpireOn);
            if (message.accessExpireOn != null && Object.hasOwnProperty.call(message, "accessExpireOn"))
                writer.uint32(/* id 10, wireType 0 =*/80).int64(message.accessExpireOn);
            if (message.appClientType != null && Object.hasOwnProperty.call(message, "appClientType"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.appClientType);
            if (message.canEdit != null && Object.hasOwnProperty.call(message, "canEdit"))
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.canEdit);
            return writer;
        };

        /**
         * Decodes an AppClient message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.AppClient
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.AppClient} AppClient
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AppClient.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.AppClient();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.string();
                        break;
                    }
                case 2: {
                        message.clientId = reader.bytes();
                        break;
                    }
                case 3: {
                        message.createdOn = reader.int64();
                        break;
                    }
                case 4: {
                        message.firstAccess = reader.int64();
                        break;
                    }
                case 5: {
                        message.lastAccess = reader.int64();
                        break;
                    }
                case 6: {
                        message.publicKey = reader.bytes();
                        break;
                    }
                case 7: {
                        message.lockIp = reader.bool();
                        break;
                    }
                case 8: {
                        message.ipAddress = reader.string();
                        break;
                    }
                case 9: {
                        message.firstAccessExpireOn = reader.int64();
                        break;
                    }
                case 10: {
                        message.accessExpireOn = reader.int64();
                        break;
                    }
                case 11: {
                        message.appClientType = reader.int32();
                        break;
                    }
                case 12: {
                        message.canEdit = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an AppClient message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.AppClient
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.AppClient} AppClient
         */
        AppClient.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.AppClient)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.AppClient: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.AppClient();
            if (object.id != null)
                message.id = String(object.id);
            if (object.clientId != null)
                if (typeof object.clientId === "string")
                    $util.base64.decode(object.clientId, message.clientId = $util.newBuffer($util.base64.length(object.clientId)), 0);
                else if (object.clientId.length >= 0)
                    message.clientId = object.clientId;
            if (object.createdOn != null)
                if ($util.Long)
                    message.createdOn = $util.Long.fromValue(object.createdOn, false);
                else if (typeof object.createdOn === "string")
                    message.createdOn = parseInt(object.createdOn, 10);
                else if (typeof object.createdOn === "number")
                    message.createdOn = object.createdOn;
                else if (typeof object.createdOn === "object")
                    message.createdOn = new $util.LongBits(object.createdOn.low >>> 0, object.createdOn.high >>> 0).toNumber();
            if (object.firstAccess != null)
                if ($util.Long)
                    message.firstAccess = $util.Long.fromValue(object.firstAccess, false);
                else if (typeof object.firstAccess === "string")
                    message.firstAccess = parseInt(object.firstAccess, 10);
                else if (typeof object.firstAccess === "number")
                    message.firstAccess = object.firstAccess;
                else if (typeof object.firstAccess === "object")
                    message.firstAccess = new $util.LongBits(object.firstAccess.low >>> 0, object.firstAccess.high >>> 0).toNumber();
            if (object.lastAccess != null)
                if ($util.Long)
                    message.lastAccess = $util.Long.fromValue(object.lastAccess, false);
                else if (typeof object.lastAccess === "string")
                    message.lastAccess = parseInt(object.lastAccess, 10);
                else if (typeof object.lastAccess === "number")
                    message.lastAccess = object.lastAccess;
                else if (typeof object.lastAccess === "object")
                    message.lastAccess = new $util.LongBits(object.lastAccess.low >>> 0, object.lastAccess.high >>> 0).toNumber();
            if (object.publicKey != null)
                if (typeof object.publicKey === "string")
                    $util.base64.decode(object.publicKey, message.publicKey = $util.newBuffer($util.base64.length(object.publicKey)), 0);
                else if (object.publicKey.length >= 0)
                    message.publicKey = object.publicKey;
            if (object.lockIp != null)
                message.lockIp = Boolean(object.lockIp);
            if (object.ipAddress != null)
                message.ipAddress = String(object.ipAddress);
            if (object.firstAccessExpireOn != null)
                if ($util.Long)
                    message.firstAccessExpireOn = $util.Long.fromValue(object.firstAccessExpireOn, false);
                else if (typeof object.firstAccessExpireOn === "string")
                    message.firstAccessExpireOn = parseInt(object.firstAccessExpireOn, 10);
                else if (typeof object.firstAccessExpireOn === "number")
                    message.firstAccessExpireOn = object.firstAccessExpireOn;
                else if (typeof object.firstAccessExpireOn === "object")
                    message.firstAccessExpireOn = new $util.LongBits(object.firstAccessExpireOn.low >>> 0, object.firstAccessExpireOn.high >>> 0).toNumber();
            if (object.accessExpireOn != null)
                if ($util.Long)
                    message.accessExpireOn = $util.Long.fromValue(object.accessExpireOn, false);
                else if (typeof object.accessExpireOn === "string")
                    message.accessExpireOn = parseInt(object.accessExpireOn, 10);
                else if (typeof object.accessExpireOn === "number")
                    message.accessExpireOn = object.accessExpireOn;
                else if (typeof object.accessExpireOn === "object")
                    message.accessExpireOn = new $util.LongBits(object.accessExpireOn.low >>> 0, object.accessExpireOn.high >>> 0).toNumber();
            switch (object.appClientType) {
            default:
                if (typeof object.appClientType === "number") {
                    message.appClientType = object.appClientType;
                    break;
                }
                break;
            case "NOT_USED":
            case 0:
                message.appClientType = 0;
                break;
            case "GENERAL":
            case 1:
                message.appClientType = 1;
                break;
            case "DISCOVERY_AND_ROTATION_CONTROLLER":
            case 2:
                message.appClientType = 2;
                break;
            case "KCM_CONTROLLER":
            case 3:
                message.appClientType = 3;
                break;
            case "SELF_DESTRUCT":
            case 4:
                message.appClientType = 4;
                break;
            }
            if (object.canEdit != null)
                message.canEdit = Boolean(object.canEdit);
            return message;
        };

        /**
         * Creates a plain object from an AppClient message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.AppClient
         * @static
         * @param {Authentication.AppClient} message AppClient
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AppClient.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id = "";
                if (options.bytes === String)
                    object.clientId = "";
                else {
                    object.clientId = [];
                    if (options.bytes !== Array)
                        object.clientId = $util.newBuffer(object.clientId);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.createdOn = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.createdOn = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.firstAccess = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.firstAccess = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastAccess = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastAccess = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.publicKey = "";
                else {
                    object.publicKey = [];
                    if (options.bytes !== Array)
                        object.publicKey = $util.newBuffer(object.publicKey);
                }
                object.lockIp = false;
                object.ipAddress = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.firstAccessExpireOn = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.firstAccessExpireOn = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.accessExpireOn = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.accessExpireOn = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.appClientType = options.enums === String ? "NOT_USED" : 0;
                object.canEdit = false;
            }
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            if (message.clientId != null && Object.hasOwnProperty.call(message, "clientId"))
                object.clientId = options.bytes === String ? $util.base64.encode(message.clientId, 0, message.clientId.length) : options.bytes === Array ? Array.prototype.slice.call(message.clientId) : message.clientId;
            if (message.createdOn != null && Object.hasOwnProperty.call(message, "createdOn"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.createdOn = typeof message.createdOn === "number" ? BigInt(message.createdOn) : $util.Long.fromBits(message.createdOn.low >>> 0, message.createdOn.high >>> 0, false).toBigInt();
                else if (typeof message.createdOn === "number")
                    object.createdOn = options.longs === String ? String(message.createdOn) : message.createdOn;
                else
                    object.createdOn = options.longs === String ? $util.Long.prototype.toString.call(message.createdOn) : options.longs === Number ? new $util.LongBits(message.createdOn.low >>> 0, message.createdOn.high >>> 0).toNumber() : message.createdOn;
            if (message.firstAccess != null && Object.hasOwnProperty.call(message, "firstAccess"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.firstAccess = typeof message.firstAccess === "number" ? BigInt(message.firstAccess) : $util.Long.fromBits(message.firstAccess.low >>> 0, message.firstAccess.high >>> 0, false).toBigInt();
                else if (typeof message.firstAccess === "number")
                    object.firstAccess = options.longs === String ? String(message.firstAccess) : message.firstAccess;
                else
                    object.firstAccess = options.longs === String ? $util.Long.prototype.toString.call(message.firstAccess) : options.longs === Number ? new $util.LongBits(message.firstAccess.low >>> 0, message.firstAccess.high >>> 0).toNumber() : message.firstAccess;
            if (message.lastAccess != null && Object.hasOwnProperty.call(message, "lastAccess"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastAccess = typeof message.lastAccess === "number" ? BigInt(message.lastAccess) : $util.Long.fromBits(message.lastAccess.low >>> 0, message.lastAccess.high >>> 0, false).toBigInt();
                else if (typeof message.lastAccess === "number")
                    object.lastAccess = options.longs === String ? String(message.lastAccess) : message.lastAccess;
                else
                    object.lastAccess = options.longs === String ? $util.Long.prototype.toString.call(message.lastAccess) : options.longs === Number ? new $util.LongBits(message.lastAccess.low >>> 0, message.lastAccess.high >>> 0).toNumber() : message.lastAccess;
            if (message.publicKey != null && Object.hasOwnProperty.call(message, "publicKey"))
                object.publicKey = options.bytes === String ? $util.base64.encode(message.publicKey, 0, message.publicKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.publicKey) : message.publicKey;
            if (message.lockIp != null && Object.hasOwnProperty.call(message, "lockIp"))
                object.lockIp = message.lockIp;
            if (message.ipAddress != null && Object.hasOwnProperty.call(message, "ipAddress"))
                object.ipAddress = message.ipAddress;
            if (message.firstAccessExpireOn != null && Object.hasOwnProperty.call(message, "firstAccessExpireOn"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.firstAccessExpireOn = typeof message.firstAccessExpireOn === "number" ? BigInt(message.firstAccessExpireOn) : $util.Long.fromBits(message.firstAccessExpireOn.low >>> 0, message.firstAccessExpireOn.high >>> 0, false).toBigInt();
                else if (typeof message.firstAccessExpireOn === "number")
                    object.firstAccessExpireOn = options.longs === String ? String(message.firstAccessExpireOn) : message.firstAccessExpireOn;
                else
                    object.firstAccessExpireOn = options.longs === String ? $util.Long.prototype.toString.call(message.firstAccessExpireOn) : options.longs === Number ? new $util.LongBits(message.firstAccessExpireOn.low >>> 0, message.firstAccessExpireOn.high >>> 0).toNumber() : message.firstAccessExpireOn;
            if (message.accessExpireOn != null && Object.hasOwnProperty.call(message, "accessExpireOn"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.accessExpireOn = typeof message.accessExpireOn === "number" ? BigInt(message.accessExpireOn) : $util.Long.fromBits(message.accessExpireOn.low >>> 0, message.accessExpireOn.high >>> 0, false).toBigInt();
                else if (typeof message.accessExpireOn === "number")
                    object.accessExpireOn = options.longs === String ? String(message.accessExpireOn) : message.accessExpireOn;
                else
                    object.accessExpireOn = options.longs === String ? $util.Long.prototype.toString.call(message.accessExpireOn) : options.longs === Number ? new $util.LongBits(message.accessExpireOn.low >>> 0, message.accessExpireOn.high >>> 0).toNumber() : message.accessExpireOn;
            if (message.appClientType != null && Object.hasOwnProperty.call(message, "appClientType"))
                object.appClientType = options.enums === String ? $root.Enterprise.AppClientType[message.appClientType] === undefined ? message.appClientType : $root.Enterprise.AppClientType[message.appClientType] : message.appClientType;
            if (message.canEdit != null && Object.hasOwnProperty.call(message, "canEdit"))
                object.canEdit = message.canEdit;
            return object;
        };

        /**
         * Converts this AppClient to JSON.
         * @function toJSON
         * @memberof Authentication.AppClient
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AppClient.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AppClient
         * @function getTypeUrl
         * @memberof Authentication.AppClient
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AppClient.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.AppClient";
        };

        return AppClient;
    })();

    Authentication.GetAppInfoRequest = (function() {

        /**
         * Properties of a GetAppInfoRequest.
         * @memberof Authentication
         * @interface IGetAppInfoRequest
         * @property {Array.<Uint8Array>|null} [appRecordUid] GetAppInfoRequest appRecordUid
         */

        /**
         * Constructs a new GetAppInfoRequest.
         * @memberof Authentication
         * @classdesc Represents a GetAppInfoRequest.
         * @implements IGetAppInfoRequest
         * @constructor
         * @param {Authentication.IGetAppInfoRequest=} [properties] Properties to set
         */
        function GetAppInfoRequest(properties) {
            this.appRecordUid = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetAppInfoRequest appRecordUid.
         * @member {Array.<Uint8Array>} appRecordUid
         * @memberof Authentication.GetAppInfoRequest
         * @instance
         */
        GetAppInfoRequest.prototype.appRecordUid = $util.emptyArray;

        /**
         * Creates a new GetAppInfoRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.GetAppInfoRequest
         * @static
         * @param {Authentication.IGetAppInfoRequest=} [properties] Properties to set
         * @returns {Authentication.GetAppInfoRequest} GetAppInfoRequest instance
         */
        GetAppInfoRequest.create = function create(properties) {
            return new GetAppInfoRequest(properties);
        };

        /**
         * Encodes the specified GetAppInfoRequest message. Does not implicitly {@link Authentication.GetAppInfoRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.GetAppInfoRequest
         * @static
         * @param {Authentication.IGetAppInfoRequest} message GetAppInfoRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetAppInfoRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.appRecordUid != null && message.appRecordUid.length)
                for (let i = 0; i < message.appRecordUid.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.appRecordUid[i]);
            return writer;
        };

        /**
         * Decodes a GetAppInfoRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.GetAppInfoRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.GetAppInfoRequest} GetAppInfoRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetAppInfoRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.GetAppInfoRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.appRecordUid && message.appRecordUid.length))
                            message.appRecordUid = [];
                        message.appRecordUid.push(reader.bytes());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a GetAppInfoRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.GetAppInfoRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.GetAppInfoRequest} GetAppInfoRequest
         */
        GetAppInfoRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.GetAppInfoRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.GetAppInfoRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.GetAppInfoRequest();
            if (object.appRecordUid) {
                if (!Array.isArray(object.appRecordUid))
                    throw TypeError(".Authentication.GetAppInfoRequest.appRecordUid: array expected");
                message.appRecordUid = [];
                for (let i = 0; i < object.appRecordUid.length; ++i)
                    if (typeof object.appRecordUid[i] === "string")
                        $util.base64.decode(object.appRecordUid[i], message.appRecordUid[i] = $util.newBuffer($util.base64.length(object.appRecordUid[i])), 0);
                    else if (object.appRecordUid[i].length >= 0)
                        message.appRecordUid[i] = object.appRecordUid[i];
            }
            return message;
        };

        /**
         * Creates a plain object from a GetAppInfoRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.GetAppInfoRequest
         * @static
         * @param {Authentication.GetAppInfoRequest} message GetAppInfoRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetAppInfoRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.appRecordUid = [];
            if (message.appRecordUid && message.appRecordUid.length) {
                object.appRecordUid = [];
                for (let j = 0; j < message.appRecordUid.length; ++j)
                    object.appRecordUid[j] = options.bytes === String ? $util.base64.encode(message.appRecordUid[j], 0, message.appRecordUid[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.appRecordUid[j]) : message.appRecordUid[j];
            }
            return object;
        };

        /**
         * Converts this GetAppInfoRequest to JSON.
         * @function toJSON
         * @memberof Authentication.GetAppInfoRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetAppInfoRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetAppInfoRequest
         * @function getTypeUrl
         * @memberof Authentication.GetAppInfoRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetAppInfoRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.GetAppInfoRequest";
        };

        return GetAppInfoRequest;
    })();

    Authentication.AppInfo = (function() {

        /**
         * Properties of an AppInfo.
         * @memberof Authentication
         * @interface IAppInfo
         * @property {Uint8Array|null} [appRecordUid] AppInfo appRecordUid
         * @property {Array.<Authentication.IAppShare>|null} [shares] AppInfo shares
         * @property {Array.<Authentication.IAppClient>|null} [clients] AppInfo clients
         * @property {boolean|null} [isExternalShare] AppInfo isExternalShare
         */

        /**
         * Constructs a new AppInfo.
         * @memberof Authentication
         * @classdesc Represents an AppInfo.
         * @implements IAppInfo
         * @constructor
         * @param {Authentication.IAppInfo=} [properties] Properties to set
         */
        function AppInfo(properties) {
            this.shares = [];
            this.clients = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AppInfo appRecordUid.
         * @member {Uint8Array} appRecordUid
         * @memberof Authentication.AppInfo
         * @instance
         */
        AppInfo.prototype.appRecordUid = $util.newBuffer([]);

        /**
         * AppInfo shares.
         * @member {Array.<Authentication.IAppShare>} shares
         * @memberof Authentication.AppInfo
         * @instance
         */
        AppInfo.prototype.shares = $util.emptyArray;

        /**
         * AppInfo clients.
         * @member {Array.<Authentication.IAppClient>} clients
         * @memberof Authentication.AppInfo
         * @instance
         */
        AppInfo.prototype.clients = $util.emptyArray;

        /**
         * AppInfo isExternalShare.
         * @member {boolean} isExternalShare
         * @memberof Authentication.AppInfo
         * @instance
         */
        AppInfo.prototype.isExternalShare = false;

        /**
         * Creates a new AppInfo instance using the specified properties.
         * @function create
         * @memberof Authentication.AppInfo
         * @static
         * @param {Authentication.IAppInfo=} [properties] Properties to set
         * @returns {Authentication.AppInfo} AppInfo instance
         */
        AppInfo.create = function create(properties) {
            return new AppInfo(properties);
        };

        /**
         * Encodes the specified AppInfo message. Does not implicitly {@link Authentication.AppInfo.verify|verify} messages.
         * @function encode
         * @memberof Authentication.AppInfo
         * @static
         * @param {Authentication.IAppInfo} message AppInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AppInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.appRecordUid != null && Object.hasOwnProperty.call(message, "appRecordUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.appRecordUid);
            if (message.shares != null && message.shares.length)
                for (let i = 0; i < message.shares.length; ++i)
                    $root.Authentication.AppShare.encode(message.shares[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.clients != null && message.clients.length)
                for (let i = 0; i < message.clients.length; ++i)
                    $root.Authentication.AppClient.encode(message.clients[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.isExternalShare != null && Object.hasOwnProperty.call(message, "isExternalShare"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.isExternalShare);
            return writer;
        };

        /**
         * Decodes an AppInfo message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.AppInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.AppInfo} AppInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AppInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.AppInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.appRecordUid = reader.bytes();
                        break;
                    }
                case 2: {
                        if (!(message.shares && message.shares.length))
                            message.shares = [];
                        message.shares.push($root.Authentication.AppShare.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 3: {
                        if (!(message.clients && message.clients.length))
                            message.clients = [];
                        message.clients.push($root.Authentication.AppClient.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 4: {
                        message.isExternalShare = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an AppInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.AppInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.AppInfo} AppInfo
         */
        AppInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.AppInfo)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.AppInfo: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.AppInfo();
            if (object.appRecordUid != null)
                if (typeof object.appRecordUid === "string")
                    $util.base64.decode(object.appRecordUid, message.appRecordUid = $util.newBuffer($util.base64.length(object.appRecordUid)), 0);
                else if (object.appRecordUid.length >= 0)
                    message.appRecordUid = object.appRecordUid;
            if (object.shares) {
                if (!Array.isArray(object.shares))
                    throw TypeError(".Authentication.AppInfo.shares: array expected");
                message.shares = [];
                for (let i = 0; i < object.shares.length; ++i) {
                    if (!$util.isObject(object.shares[i]))
                        throw TypeError(".Authentication.AppInfo.shares: object expected");
                    message.shares[i] = $root.Authentication.AppShare.fromObject(object.shares[i], long + 1);
                }
            }
            if (object.clients) {
                if (!Array.isArray(object.clients))
                    throw TypeError(".Authentication.AppInfo.clients: array expected");
                message.clients = [];
                for (let i = 0; i < object.clients.length; ++i) {
                    if (!$util.isObject(object.clients[i]))
                        throw TypeError(".Authentication.AppInfo.clients: object expected");
                    message.clients[i] = $root.Authentication.AppClient.fromObject(object.clients[i], long + 1);
                }
            }
            if (object.isExternalShare != null)
                message.isExternalShare = Boolean(object.isExternalShare);
            return message;
        };

        /**
         * Creates a plain object from an AppInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.AppInfo
         * @static
         * @param {Authentication.AppInfo} message AppInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AppInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.shares = [];
                object.clients = [];
            }
            if (options.defaults) {
                if (options.bytes === String)
                    object.appRecordUid = "";
                else {
                    object.appRecordUid = [];
                    if (options.bytes !== Array)
                        object.appRecordUid = $util.newBuffer(object.appRecordUid);
                }
                object.isExternalShare = false;
            }
            if (message.appRecordUid != null && Object.hasOwnProperty.call(message, "appRecordUid"))
                object.appRecordUid = options.bytes === String ? $util.base64.encode(message.appRecordUid, 0, message.appRecordUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.appRecordUid) : message.appRecordUid;
            if (message.shares && message.shares.length) {
                object.shares = [];
                for (let j = 0; j < message.shares.length; ++j)
                    object.shares[j] = $root.Authentication.AppShare.toObject(message.shares[j], options, q + 1);
            }
            if (message.clients && message.clients.length) {
                object.clients = [];
                for (let j = 0; j < message.clients.length; ++j)
                    object.clients[j] = $root.Authentication.AppClient.toObject(message.clients[j], options, q + 1);
            }
            if (message.isExternalShare != null && Object.hasOwnProperty.call(message, "isExternalShare"))
                object.isExternalShare = message.isExternalShare;
            return object;
        };

        /**
         * Converts this AppInfo to JSON.
         * @function toJSON
         * @memberof Authentication.AppInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AppInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AppInfo
         * @function getTypeUrl
         * @memberof Authentication.AppInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AppInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.AppInfo";
        };

        return AppInfo;
    })();

    Authentication.GetAppInfoResponse = (function() {

        /**
         * Properties of a GetAppInfoResponse.
         * @memberof Authentication
         * @interface IGetAppInfoResponse
         * @property {Array.<Authentication.IAppInfo>|null} [appInfo] GetAppInfoResponse appInfo
         */

        /**
         * Constructs a new GetAppInfoResponse.
         * @memberof Authentication
         * @classdesc Represents a GetAppInfoResponse.
         * @implements IGetAppInfoResponse
         * @constructor
         * @param {Authentication.IGetAppInfoResponse=} [properties] Properties to set
         */
        function GetAppInfoResponse(properties) {
            this.appInfo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetAppInfoResponse appInfo.
         * @member {Array.<Authentication.IAppInfo>} appInfo
         * @memberof Authentication.GetAppInfoResponse
         * @instance
         */
        GetAppInfoResponse.prototype.appInfo = $util.emptyArray;

        /**
         * Creates a new GetAppInfoResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.GetAppInfoResponse
         * @static
         * @param {Authentication.IGetAppInfoResponse=} [properties] Properties to set
         * @returns {Authentication.GetAppInfoResponse} GetAppInfoResponse instance
         */
        GetAppInfoResponse.create = function create(properties) {
            return new GetAppInfoResponse(properties);
        };

        /**
         * Encodes the specified GetAppInfoResponse message. Does not implicitly {@link Authentication.GetAppInfoResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.GetAppInfoResponse
         * @static
         * @param {Authentication.IGetAppInfoResponse} message GetAppInfoResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetAppInfoResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.appInfo != null && message.appInfo.length)
                for (let i = 0; i < message.appInfo.length; ++i)
                    $root.Authentication.AppInfo.encode(message.appInfo[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes a GetAppInfoResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.GetAppInfoResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.GetAppInfoResponse} GetAppInfoResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetAppInfoResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.GetAppInfoResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.appInfo && message.appInfo.length))
                            message.appInfo = [];
                        message.appInfo.push($root.Authentication.AppInfo.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a GetAppInfoResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.GetAppInfoResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.GetAppInfoResponse} GetAppInfoResponse
         */
        GetAppInfoResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.GetAppInfoResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.GetAppInfoResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.GetAppInfoResponse();
            if (object.appInfo) {
                if (!Array.isArray(object.appInfo))
                    throw TypeError(".Authentication.GetAppInfoResponse.appInfo: array expected");
                message.appInfo = [];
                for (let i = 0; i < object.appInfo.length; ++i) {
                    if (!$util.isObject(object.appInfo[i]))
                        throw TypeError(".Authentication.GetAppInfoResponse.appInfo: object expected");
                    message.appInfo[i] = $root.Authentication.AppInfo.fromObject(object.appInfo[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GetAppInfoResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.GetAppInfoResponse
         * @static
         * @param {Authentication.GetAppInfoResponse} message GetAppInfoResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetAppInfoResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.appInfo = [];
            if (message.appInfo && message.appInfo.length) {
                object.appInfo = [];
                for (let j = 0; j < message.appInfo.length; ++j)
                    object.appInfo[j] = $root.Authentication.AppInfo.toObject(message.appInfo[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this GetAppInfoResponse to JSON.
         * @function toJSON
         * @memberof Authentication.GetAppInfoResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetAppInfoResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetAppInfoResponse
         * @function getTypeUrl
         * @memberof Authentication.GetAppInfoResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetAppInfoResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.GetAppInfoResponse";
        };

        return GetAppInfoResponse;
    })();

    Authentication.ApplicationSummary = (function() {

        /**
         * Properties of an ApplicationSummary.
         * @memberof Authentication
         * @interface IApplicationSummary
         * @property {Uint8Array|null} [appRecordUid] ApplicationSummary appRecordUid
         * @property {number|null} [lastAccess] ApplicationSummary lastAccess
         * @property {number|null} [recordShares] ApplicationSummary recordShares
         * @property {number|null} [folderShares] ApplicationSummary folderShares
         * @property {number|null} [folderRecords] ApplicationSummary folderRecords
         * @property {number|null} [clientCount] ApplicationSummary clientCount
         * @property {number|null} [expiredClientCount] ApplicationSummary expiredClientCount
         * @property {string|null} [username] ApplicationSummary username
         * @property {Uint8Array|null} [appData] ApplicationSummary appData
         */

        /**
         * Constructs a new ApplicationSummary.
         * @memberof Authentication
         * @classdesc Represents an ApplicationSummary.
         * @implements IApplicationSummary
         * @constructor
         * @param {Authentication.IApplicationSummary=} [properties] Properties to set
         */
        function ApplicationSummary(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ApplicationSummary appRecordUid.
         * @member {Uint8Array} appRecordUid
         * @memberof Authentication.ApplicationSummary
         * @instance
         */
        ApplicationSummary.prototype.appRecordUid = $util.newBuffer([]);

        /**
         * ApplicationSummary lastAccess.
         * @member {number} lastAccess
         * @memberof Authentication.ApplicationSummary
         * @instance
         */
        ApplicationSummary.prototype.lastAccess = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ApplicationSummary recordShares.
         * @member {number} recordShares
         * @memberof Authentication.ApplicationSummary
         * @instance
         */
        ApplicationSummary.prototype.recordShares = 0;

        /**
         * ApplicationSummary folderShares.
         * @member {number} folderShares
         * @memberof Authentication.ApplicationSummary
         * @instance
         */
        ApplicationSummary.prototype.folderShares = 0;

        /**
         * ApplicationSummary folderRecords.
         * @member {number} folderRecords
         * @memberof Authentication.ApplicationSummary
         * @instance
         */
        ApplicationSummary.prototype.folderRecords = 0;

        /**
         * ApplicationSummary clientCount.
         * @member {number} clientCount
         * @memberof Authentication.ApplicationSummary
         * @instance
         */
        ApplicationSummary.prototype.clientCount = 0;

        /**
         * ApplicationSummary expiredClientCount.
         * @member {number} expiredClientCount
         * @memberof Authentication.ApplicationSummary
         * @instance
         */
        ApplicationSummary.prototype.expiredClientCount = 0;

        /**
         * ApplicationSummary username.
         * @member {string} username
         * @memberof Authentication.ApplicationSummary
         * @instance
         */
        ApplicationSummary.prototype.username = "";

        /**
         * ApplicationSummary appData.
         * @member {Uint8Array} appData
         * @memberof Authentication.ApplicationSummary
         * @instance
         */
        ApplicationSummary.prototype.appData = $util.newBuffer([]);

        /**
         * Creates a new ApplicationSummary instance using the specified properties.
         * @function create
         * @memberof Authentication.ApplicationSummary
         * @static
         * @param {Authentication.IApplicationSummary=} [properties] Properties to set
         * @returns {Authentication.ApplicationSummary} ApplicationSummary instance
         */
        ApplicationSummary.create = function create(properties) {
            return new ApplicationSummary(properties);
        };

        /**
         * Encodes the specified ApplicationSummary message. Does not implicitly {@link Authentication.ApplicationSummary.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ApplicationSummary
         * @static
         * @param {Authentication.IApplicationSummary} message ApplicationSummary message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ApplicationSummary.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.appRecordUid != null && Object.hasOwnProperty.call(message, "appRecordUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.appRecordUid);
            if (message.lastAccess != null && Object.hasOwnProperty.call(message, "lastAccess"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.lastAccess);
            if (message.recordShares != null && Object.hasOwnProperty.call(message, "recordShares"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.recordShares);
            if (message.folderShares != null && Object.hasOwnProperty.call(message, "folderShares"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.folderShares);
            if (message.folderRecords != null && Object.hasOwnProperty.call(message, "folderRecords"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.folderRecords);
            if (message.clientCount != null && Object.hasOwnProperty.call(message, "clientCount"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.clientCount);
            if (message.expiredClientCount != null && Object.hasOwnProperty.call(message, "expiredClientCount"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.expiredClientCount);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.username);
            if (message.appData != null && Object.hasOwnProperty.call(message, "appData"))
                writer.uint32(/* id 9, wireType 2 =*/74).bytes(message.appData);
            return writer;
        };

        /**
         * Decodes an ApplicationSummary message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ApplicationSummary
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ApplicationSummary} ApplicationSummary
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ApplicationSummary.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ApplicationSummary();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.appRecordUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.lastAccess = reader.int64();
                        break;
                    }
                case 3: {
                        message.recordShares = reader.int32();
                        break;
                    }
                case 4: {
                        message.folderShares = reader.int32();
                        break;
                    }
                case 5: {
                        message.folderRecords = reader.int32();
                        break;
                    }
                case 6: {
                        message.clientCount = reader.int32();
                        break;
                    }
                case 7: {
                        message.expiredClientCount = reader.int32();
                        break;
                    }
                case 8: {
                        message.username = reader.string();
                        break;
                    }
                case 9: {
                        message.appData = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an ApplicationSummary message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ApplicationSummary
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ApplicationSummary} ApplicationSummary
         */
        ApplicationSummary.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ApplicationSummary)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ApplicationSummary: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ApplicationSummary();
            if (object.appRecordUid != null)
                if (typeof object.appRecordUid === "string")
                    $util.base64.decode(object.appRecordUid, message.appRecordUid = $util.newBuffer($util.base64.length(object.appRecordUid)), 0);
                else if (object.appRecordUid.length >= 0)
                    message.appRecordUid = object.appRecordUid;
            if (object.lastAccess != null)
                if ($util.Long)
                    message.lastAccess = $util.Long.fromValue(object.lastAccess, false);
                else if (typeof object.lastAccess === "string")
                    message.lastAccess = parseInt(object.lastAccess, 10);
                else if (typeof object.lastAccess === "number")
                    message.lastAccess = object.lastAccess;
                else if (typeof object.lastAccess === "object")
                    message.lastAccess = new $util.LongBits(object.lastAccess.low >>> 0, object.lastAccess.high >>> 0).toNumber();
            if (object.recordShares != null)
                message.recordShares = object.recordShares | 0;
            if (object.folderShares != null)
                message.folderShares = object.folderShares | 0;
            if (object.folderRecords != null)
                message.folderRecords = object.folderRecords | 0;
            if (object.clientCount != null)
                message.clientCount = object.clientCount | 0;
            if (object.expiredClientCount != null)
                message.expiredClientCount = object.expiredClientCount | 0;
            if (object.username != null)
                message.username = String(object.username);
            if (object.appData != null)
                if (typeof object.appData === "string")
                    $util.base64.decode(object.appData, message.appData = $util.newBuffer($util.base64.length(object.appData)), 0);
                else if (object.appData.length >= 0)
                    message.appData = object.appData;
            return message;
        };

        /**
         * Creates a plain object from an ApplicationSummary message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ApplicationSummary
         * @static
         * @param {Authentication.ApplicationSummary} message ApplicationSummary
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ApplicationSummary.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.appRecordUid = "";
                else {
                    object.appRecordUid = [];
                    if (options.bytes !== Array)
                        object.appRecordUid = $util.newBuffer(object.appRecordUid);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastAccess = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastAccess = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.recordShares = 0;
                object.folderShares = 0;
                object.folderRecords = 0;
                object.clientCount = 0;
                object.expiredClientCount = 0;
                object.username = "";
                if (options.bytes === String)
                    object.appData = "";
                else {
                    object.appData = [];
                    if (options.bytes !== Array)
                        object.appData = $util.newBuffer(object.appData);
                }
            }
            if (message.appRecordUid != null && Object.hasOwnProperty.call(message, "appRecordUid"))
                object.appRecordUid = options.bytes === String ? $util.base64.encode(message.appRecordUid, 0, message.appRecordUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.appRecordUid) : message.appRecordUid;
            if (message.lastAccess != null && Object.hasOwnProperty.call(message, "lastAccess"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastAccess = typeof message.lastAccess === "number" ? BigInt(message.lastAccess) : $util.Long.fromBits(message.lastAccess.low >>> 0, message.lastAccess.high >>> 0, false).toBigInt();
                else if (typeof message.lastAccess === "number")
                    object.lastAccess = options.longs === String ? String(message.lastAccess) : message.lastAccess;
                else
                    object.lastAccess = options.longs === String ? $util.Long.prototype.toString.call(message.lastAccess) : options.longs === Number ? new $util.LongBits(message.lastAccess.low >>> 0, message.lastAccess.high >>> 0).toNumber() : message.lastAccess;
            if (message.recordShares != null && Object.hasOwnProperty.call(message, "recordShares"))
                object.recordShares = message.recordShares;
            if (message.folderShares != null && Object.hasOwnProperty.call(message, "folderShares"))
                object.folderShares = message.folderShares;
            if (message.folderRecords != null && Object.hasOwnProperty.call(message, "folderRecords"))
                object.folderRecords = message.folderRecords;
            if (message.clientCount != null && Object.hasOwnProperty.call(message, "clientCount"))
                object.clientCount = message.clientCount;
            if (message.expiredClientCount != null && Object.hasOwnProperty.call(message, "expiredClientCount"))
                object.expiredClientCount = message.expiredClientCount;
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.appData != null && Object.hasOwnProperty.call(message, "appData"))
                object.appData = options.bytes === String ? $util.base64.encode(message.appData, 0, message.appData.length) : options.bytes === Array ? Array.prototype.slice.call(message.appData) : message.appData;
            return object;
        };

        /**
         * Converts this ApplicationSummary to JSON.
         * @function toJSON
         * @memberof Authentication.ApplicationSummary
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ApplicationSummary.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ApplicationSummary
         * @function getTypeUrl
         * @memberof Authentication.ApplicationSummary
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ApplicationSummary.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ApplicationSummary";
        };

        return ApplicationSummary;
    })();

    Authentication.GetApplicationsSummaryResponse = (function() {

        /**
         * Properties of a GetApplicationsSummaryResponse.
         * @memberof Authentication
         * @interface IGetApplicationsSummaryResponse
         * @property {Array.<Authentication.IApplicationSummary>|null} [applicationSummary] GetApplicationsSummaryResponse applicationSummary
         */

        /**
         * Constructs a new GetApplicationsSummaryResponse.
         * @memberof Authentication
         * @classdesc Represents a GetApplicationsSummaryResponse.
         * @implements IGetApplicationsSummaryResponse
         * @constructor
         * @param {Authentication.IGetApplicationsSummaryResponse=} [properties] Properties to set
         */
        function GetApplicationsSummaryResponse(properties) {
            this.applicationSummary = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetApplicationsSummaryResponse applicationSummary.
         * @member {Array.<Authentication.IApplicationSummary>} applicationSummary
         * @memberof Authentication.GetApplicationsSummaryResponse
         * @instance
         */
        GetApplicationsSummaryResponse.prototype.applicationSummary = $util.emptyArray;

        /**
         * Creates a new GetApplicationsSummaryResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.GetApplicationsSummaryResponse
         * @static
         * @param {Authentication.IGetApplicationsSummaryResponse=} [properties] Properties to set
         * @returns {Authentication.GetApplicationsSummaryResponse} GetApplicationsSummaryResponse instance
         */
        GetApplicationsSummaryResponse.create = function create(properties) {
            return new GetApplicationsSummaryResponse(properties);
        };

        /**
         * Encodes the specified GetApplicationsSummaryResponse message. Does not implicitly {@link Authentication.GetApplicationsSummaryResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.GetApplicationsSummaryResponse
         * @static
         * @param {Authentication.IGetApplicationsSummaryResponse} message GetApplicationsSummaryResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetApplicationsSummaryResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.applicationSummary != null && message.applicationSummary.length)
                for (let i = 0; i < message.applicationSummary.length; ++i)
                    $root.Authentication.ApplicationSummary.encode(message.applicationSummary[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes a GetApplicationsSummaryResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.GetApplicationsSummaryResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.GetApplicationsSummaryResponse} GetApplicationsSummaryResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetApplicationsSummaryResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.GetApplicationsSummaryResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.applicationSummary && message.applicationSummary.length))
                            message.applicationSummary = [];
                        message.applicationSummary.push($root.Authentication.ApplicationSummary.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a GetApplicationsSummaryResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.GetApplicationsSummaryResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.GetApplicationsSummaryResponse} GetApplicationsSummaryResponse
         */
        GetApplicationsSummaryResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.GetApplicationsSummaryResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.GetApplicationsSummaryResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.GetApplicationsSummaryResponse();
            if (object.applicationSummary) {
                if (!Array.isArray(object.applicationSummary))
                    throw TypeError(".Authentication.GetApplicationsSummaryResponse.applicationSummary: array expected");
                message.applicationSummary = [];
                for (let i = 0; i < object.applicationSummary.length; ++i) {
                    if (!$util.isObject(object.applicationSummary[i]))
                        throw TypeError(".Authentication.GetApplicationsSummaryResponse.applicationSummary: object expected");
                    message.applicationSummary[i] = $root.Authentication.ApplicationSummary.fromObject(object.applicationSummary[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GetApplicationsSummaryResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.GetApplicationsSummaryResponse
         * @static
         * @param {Authentication.GetApplicationsSummaryResponse} message GetApplicationsSummaryResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetApplicationsSummaryResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.applicationSummary = [];
            if (message.applicationSummary && message.applicationSummary.length) {
                object.applicationSummary = [];
                for (let j = 0; j < message.applicationSummary.length; ++j)
                    object.applicationSummary[j] = $root.Authentication.ApplicationSummary.toObject(message.applicationSummary[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this GetApplicationsSummaryResponse to JSON.
         * @function toJSON
         * @memberof Authentication.GetApplicationsSummaryResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetApplicationsSummaryResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetApplicationsSummaryResponse
         * @function getTypeUrl
         * @memberof Authentication.GetApplicationsSummaryResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetApplicationsSummaryResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.GetApplicationsSummaryResponse";
        };

        return GetApplicationsSummaryResponse;
    })();

    Authentication.GetVerificationTokenRequest = (function() {

        /**
         * Properties of a GetVerificationTokenRequest.
         * @memberof Authentication
         * @interface IGetVerificationTokenRequest
         * @property {string|null} [username] GetVerificationTokenRequest username
         */

        /**
         * Constructs a new GetVerificationTokenRequest.
         * @memberof Authentication
         * @classdesc Represents a GetVerificationTokenRequest.
         * @implements IGetVerificationTokenRequest
         * @constructor
         * @param {Authentication.IGetVerificationTokenRequest=} [properties] Properties to set
         */
        function GetVerificationTokenRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetVerificationTokenRequest username.
         * @member {string} username
         * @memberof Authentication.GetVerificationTokenRequest
         * @instance
         */
        GetVerificationTokenRequest.prototype.username = "";

        /**
         * Creates a new GetVerificationTokenRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.GetVerificationTokenRequest
         * @static
         * @param {Authentication.IGetVerificationTokenRequest=} [properties] Properties to set
         * @returns {Authentication.GetVerificationTokenRequest} GetVerificationTokenRequest instance
         */
        GetVerificationTokenRequest.create = function create(properties) {
            return new GetVerificationTokenRequest(properties);
        };

        /**
         * Encodes the specified GetVerificationTokenRequest message. Does not implicitly {@link Authentication.GetVerificationTokenRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.GetVerificationTokenRequest
         * @static
         * @param {Authentication.IGetVerificationTokenRequest} message GetVerificationTokenRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetVerificationTokenRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.username);
            return writer;
        };

        /**
         * Decodes a GetVerificationTokenRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.GetVerificationTokenRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.GetVerificationTokenRequest} GetVerificationTokenRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetVerificationTokenRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.GetVerificationTokenRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.username = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a GetVerificationTokenRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.GetVerificationTokenRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.GetVerificationTokenRequest} GetVerificationTokenRequest
         */
        GetVerificationTokenRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.GetVerificationTokenRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.GetVerificationTokenRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.GetVerificationTokenRequest();
            if (object.username != null)
                message.username = String(object.username);
            return message;
        };

        /**
         * Creates a plain object from a GetVerificationTokenRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.GetVerificationTokenRequest
         * @static
         * @param {Authentication.GetVerificationTokenRequest} message GetVerificationTokenRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetVerificationTokenRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.username = "";
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            return object;
        };

        /**
         * Converts this GetVerificationTokenRequest to JSON.
         * @function toJSON
         * @memberof Authentication.GetVerificationTokenRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetVerificationTokenRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetVerificationTokenRequest
         * @function getTypeUrl
         * @memberof Authentication.GetVerificationTokenRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetVerificationTokenRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.GetVerificationTokenRequest";
        };

        return GetVerificationTokenRequest;
    })();

    Authentication.GetVerificationTokenResponse = (function() {

        /**
         * Properties of a GetVerificationTokenResponse.
         * @memberof Authentication
         * @interface IGetVerificationTokenResponse
         * @property {Uint8Array|null} [encryptedVerificationToken] GetVerificationTokenResponse encryptedVerificationToken
         */

        /**
         * Constructs a new GetVerificationTokenResponse.
         * @memberof Authentication
         * @classdesc Represents a GetVerificationTokenResponse.
         * @implements IGetVerificationTokenResponse
         * @constructor
         * @param {Authentication.IGetVerificationTokenResponse=} [properties] Properties to set
         */
        function GetVerificationTokenResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetVerificationTokenResponse encryptedVerificationToken.
         * @member {Uint8Array} encryptedVerificationToken
         * @memberof Authentication.GetVerificationTokenResponse
         * @instance
         */
        GetVerificationTokenResponse.prototype.encryptedVerificationToken = $util.newBuffer([]);

        /**
         * Creates a new GetVerificationTokenResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.GetVerificationTokenResponse
         * @static
         * @param {Authentication.IGetVerificationTokenResponse=} [properties] Properties to set
         * @returns {Authentication.GetVerificationTokenResponse} GetVerificationTokenResponse instance
         */
        GetVerificationTokenResponse.create = function create(properties) {
            return new GetVerificationTokenResponse(properties);
        };

        /**
         * Encodes the specified GetVerificationTokenResponse message. Does not implicitly {@link Authentication.GetVerificationTokenResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.GetVerificationTokenResponse
         * @static
         * @param {Authentication.IGetVerificationTokenResponse} message GetVerificationTokenResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetVerificationTokenResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedVerificationToken != null && Object.hasOwnProperty.call(message, "encryptedVerificationToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedVerificationToken);
            return writer;
        };

        /**
         * Decodes a GetVerificationTokenResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.GetVerificationTokenResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.GetVerificationTokenResponse} GetVerificationTokenResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetVerificationTokenResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.GetVerificationTokenResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedVerificationToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a GetVerificationTokenResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.GetVerificationTokenResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.GetVerificationTokenResponse} GetVerificationTokenResponse
         */
        GetVerificationTokenResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.GetVerificationTokenResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.GetVerificationTokenResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.GetVerificationTokenResponse();
            if (object.encryptedVerificationToken != null)
                if (typeof object.encryptedVerificationToken === "string")
                    $util.base64.decode(object.encryptedVerificationToken, message.encryptedVerificationToken = $util.newBuffer($util.base64.length(object.encryptedVerificationToken)), 0);
                else if (object.encryptedVerificationToken.length >= 0)
                    message.encryptedVerificationToken = object.encryptedVerificationToken;
            return message;
        };

        /**
         * Creates a plain object from a GetVerificationTokenResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.GetVerificationTokenResponse
         * @static
         * @param {Authentication.GetVerificationTokenResponse} message GetVerificationTokenResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetVerificationTokenResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === String)
                    object.encryptedVerificationToken = "";
                else {
                    object.encryptedVerificationToken = [];
                    if (options.bytes !== Array)
                        object.encryptedVerificationToken = $util.newBuffer(object.encryptedVerificationToken);
                }
            if (message.encryptedVerificationToken != null && Object.hasOwnProperty.call(message, "encryptedVerificationToken"))
                object.encryptedVerificationToken = options.bytes === String ? $util.base64.encode(message.encryptedVerificationToken, 0, message.encryptedVerificationToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedVerificationToken) : message.encryptedVerificationToken;
            return object;
        };

        /**
         * Converts this GetVerificationTokenResponse to JSON.
         * @function toJSON
         * @memberof Authentication.GetVerificationTokenResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetVerificationTokenResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetVerificationTokenResponse
         * @function getTypeUrl
         * @memberof Authentication.GetVerificationTokenResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetVerificationTokenResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.GetVerificationTokenResponse";
        };

        return GetVerificationTokenResponse;
    })();

    Authentication.SendShareInviteRequest = (function() {

        /**
         * Properties of a SendShareInviteRequest.
         * @memberof Authentication
         * @interface ISendShareInviteRequest
         * @property {string|null} [email] SendShareInviteRequest email
         */

        /**
         * Constructs a new SendShareInviteRequest.
         * @memberof Authentication
         * @classdesc Represents a SendShareInviteRequest.
         * @implements ISendShareInviteRequest
         * @constructor
         * @param {Authentication.ISendShareInviteRequest=} [properties] Properties to set
         */
        function SendShareInviteRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SendShareInviteRequest email.
         * @member {string} email
         * @memberof Authentication.SendShareInviteRequest
         * @instance
         */
        SendShareInviteRequest.prototype.email = "";

        /**
         * Creates a new SendShareInviteRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.SendShareInviteRequest
         * @static
         * @param {Authentication.ISendShareInviteRequest=} [properties] Properties to set
         * @returns {Authentication.SendShareInviteRequest} SendShareInviteRequest instance
         */
        SendShareInviteRequest.create = function create(properties) {
            return new SendShareInviteRequest(properties);
        };

        /**
         * Encodes the specified SendShareInviteRequest message. Does not implicitly {@link Authentication.SendShareInviteRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.SendShareInviteRequest
         * @static
         * @param {Authentication.ISendShareInviteRequest} message SendShareInviteRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SendShareInviteRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.email);
            return writer;
        };

        /**
         * Decodes a SendShareInviteRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.SendShareInviteRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.SendShareInviteRequest} SendShareInviteRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SendShareInviteRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.SendShareInviteRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.email = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a SendShareInviteRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.SendShareInviteRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.SendShareInviteRequest} SendShareInviteRequest
         */
        SendShareInviteRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.SendShareInviteRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.SendShareInviteRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.SendShareInviteRequest();
            if (object.email != null)
                message.email = String(object.email);
            return message;
        };

        /**
         * Creates a plain object from a SendShareInviteRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.SendShareInviteRequest
         * @static
         * @param {Authentication.SendShareInviteRequest} message SendShareInviteRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SendShareInviteRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.email = "";
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                object.email = message.email;
            return object;
        };

        /**
         * Converts this SendShareInviteRequest to JSON.
         * @function toJSON
         * @memberof Authentication.SendShareInviteRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SendShareInviteRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SendShareInviteRequest
         * @function getTypeUrl
         * @memberof Authentication.SendShareInviteRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SendShareInviteRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.SendShareInviteRequest";
        };

        return SendShareInviteRequest;
    })();

    /**
     * TimeLimitedAccessType enum.
     * @name Authentication.TimeLimitedAccessType
     * @enum {number}
     * @property {number} INVALID_TIME_LIMITED_ACCESS_TYPE=0 INVALID_TIME_LIMITED_ACCESS_TYPE value
     * @property {number} USER_ACCESS_TO_RECORD=1 USER_ACCESS_TO_RECORD value
     * @property {number} USER_OR_TEAM_ACCESS_TO_SHAREDFOLDER=2 USER_OR_TEAM_ACCESS_TO_SHAREDFOLDER value
     * @property {number} RECORD_ACCESS_TO_SHAREDFOLDER=3 RECORD_ACCESS_TO_SHAREDFOLDER value
     */
    Authentication.TimeLimitedAccessType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "INVALID_TIME_LIMITED_ACCESS_TYPE"] = 0;
        values[valuesById[1] = "USER_ACCESS_TO_RECORD"] = 1;
        values[valuesById[2] = "USER_OR_TEAM_ACCESS_TO_SHAREDFOLDER"] = 2;
        values[valuesById[3] = "RECORD_ACCESS_TO_SHAREDFOLDER"] = 3;
        return values;
    })();

    Authentication.TimeLimitedAccessRequest = (function() {

        /**
         * Properties of a TimeLimitedAccessRequest.
         * @memberof Authentication
         * @interface ITimeLimitedAccessRequest
         * @property {Array.<Uint8Array>|null} [accountUid] TimeLimitedAccessRequest accountUid
         * @property {Array.<Uint8Array>|null} [teamUid] TimeLimitedAccessRequest teamUid
         * @property {Array.<Uint8Array>|null} [recordUid] TimeLimitedAccessRequest recordUid
         * @property {Uint8Array|null} [sharedObjectUid] TimeLimitedAccessRequest sharedObjectUid
         * @property {Authentication.TimeLimitedAccessType|null} [timeLimitedAccessType] TimeLimitedAccessRequest timeLimitedAccessType
         * @property {number|null} [expiration] TimeLimitedAccessRequest expiration
         */

        /**
         * Constructs a new TimeLimitedAccessRequest.
         * @memberof Authentication
         * @classdesc Represents a TimeLimitedAccessRequest.
         * @implements ITimeLimitedAccessRequest
         * @constructor
         * @param {Authentication.ITimeLimitedAccessRequest=} [properties] Properties to set
         */
        function TimeLimitedAccessRequest(properties) {
            this.accountUid = [];
            this.teamUid = [];
            this.recordUid = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TimeLimitedAccessRequest accountUid.
         * @member {Array.<Uint8Array>} accountUid
         * @memberof Authentication.TimeLimitedAccessRequest
         * @instance
         */
        TimeLimitedAccessRequest.prototype.accountUid = $util.emptyArray;

        /**
         * TimeLimitedAccessRequest teamUid.
         * @member {Array.<Uint8Array>} teamUid
         * @memberof Authentication.TimeLimitedAccessRequest
         * @instance
         */
        TimeLimitedAccessRequest.prototype.teamUid = $util.emptyArray;

        /**
         * TimeLimitedAccessRequest recordUid.
         * @member {Array.<Uint8Array>} recordUid
         * @memberof Authentication.TimeLimitedAccessRequest
         * @instance
         */
        TimeLimitedAccessRequest.prototype.recordUid = $util.emptyArray;

        /**
         * TimeLimitedAccessRequest sharedObjectUid.
         * @member {Uint8Array} sharedObjectUid
         * @memberof Authentication.TimeLimitedAccessRequest
         * @instance
         */
        TimeLimitedAccessRequest.prototype.sharedObjectUid = $util.newBuffer([]);

        /**
         * TimeLimitedAccessRequest timeLimitedAccessType.
         * @member {Authentication.TimeLimitedAccessType} timeLimitedAccessType
         * @memberof Authentication.TimeLimitedAccessRequest
         * @instance
         */
        TimeLimitedAccessRequest.prototype.timeLimitedAccessType = 0;

        /**
         * TimeLimitedAccessRequest expiration.
         * @member {number} expiration
         * @memberof Authentication.TimeLimitedAccessRequest
         * @instance
         */
        TimeLimitedAccessRequest.prototype.expiration = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new TimeLimitedAccessRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.TimeLimitedAccessRequest
         * @static
         * @param {Authentication.ITimeLimitedAccessRequest=} [properties] Properties to set
         * @returns {Authentication.TimeLimitedAccessRequest} TimeLimitedAccessRequest instance
         */
        TimeLimitedAccessRequest.create = function create(properties) {
            return new TimeLimitedAccessRequest(properties);
        };

        /**
         * Encodes the specified TimeLimitedAccessRequest message. Does not implicitly {@link Authentication.TimeLimitedAccessRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TimeLimitedAccessRequest
         * @static
         * @param {Authentication.ITimeLimitedAccessRequest} message TimeLimitedAccessRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TimeLimitedAccessRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.accountUid != null && message.accountUid.length)
                for (let i = 0; i < message.accountUid.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.accountUid[i]);
            if (message.teamUid != null && message.teamUid.length)
                for (let i = 0; i < message.teamUid.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.teamUid[i]);
            if (message.recordUid != null && message.recordUid.length)
                for (let i = 0; i < message.recordUid.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.recordUid[i]);
            if (message.sharedObjectUid != null && Object.hasOwnProperty.call(message, "sharedObjectUid"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.sharedObjectUid);
            if (message.timeLimitedAccessType != null && Object.hasOwnProperty.call(message, "timeLimitedAccessType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.timeLimitedAccessType);
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.expiration);
            return writer;
        };

        /**
         * Decodes a TimeLimitedAccessRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TimeLimitedAccessRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TimeLimitedAccessRequest} TimeLimitedAccessRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TimeLimitedAccessRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TimeLimitedAccessRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.accountUid && message.accountUid.length))
                            message.accountUid = [];
                        message.accountUid.push(reader.bytes());
                        break;
                    }
                case 2: {
                        if (!(message.teamUid && message.teamUid.length))
                            message.teamUid = [];
                        message.teamUid.push(reader.bytes());
                        break;
                    }
                case 3: {
                        if (!(message.recordUid && message.recordUid.length))
                            message.recordUid = [];
                        message.recordUid.push(reader.bytes());
                        break;
                    }
                case 4: {
                        message.sharedObjectUid = reader.bytes();
                        break;
                    }
                case 5: {
                        message.timeLimitedAccessType = reader.int32();
                        break;
                    }
                case 6: {
                        message.expiration = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TimeLimitedAccessRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TimeLimitedAccessRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TimeLimitedAccessRequest} TimeLimitedAccessRequest
         */
        TimeLimitedAccessRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TimeLimitedAccessRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TimeLimitedAccessRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TimeLimitedAccessRequest();
            if (object.accountUid) {
                if (!Array.isArray(object.accountUid))
                    throw TypeError(".Authentication.TimeLimitedAccessRequest.accountUid: array expected");
                message.accountUid = [];
                for (let i = 0; i < object.accountUid.length; ++i)
                    if (typeof object.accountUid[i] === "string")
                        $util.base64.decode(object.accountUid[i], message.accountUid[i] = $util.newBuffer($util.base64.length(object.accountUid[i])), 0);
                    else if (object.accountUid[i].length >= 0)
                        message.accountUid[i] = object.accountUid[i];
            }
            if (object.teamUid) {
                if (!Array.isArray(object.teamUid))
                    throw TypeError(".Authentication.TimeLimitedAccessRequest.teamUid: array expected");
                message.teamUid = [];
                for (let i = 0; i < object.teamUid.length; ++i)
                    if (typeof object.teamUid[i] === "string")
                        $util.base64.decode(object.teamUid[i], message.teamUid[i] = $util.newBuffer($util.base64.length(object.teamUid[i])), 0);
                    else if (object.teamUid[i].length >= 0)
                        message.teamUid[i] = object.teamUid[i];
            }
            if (object.recordUid) {
                if (!Array.isArray(object.recordUid))
                    throw TypeError(".Authentication.TimeLimitedAccessRequest.recordUid: array expected");
                message.recordUid = [];
                for (let i = 0; i < object.recordUid.length; ++i)
                    if (typeof object.recordUid[i] === "string")
                        $util.base64.decode(object.recordUid[i], message.recordUid[i] = $util.newBuffer($util.base64.length(object.recordUid[i])), 0);
                    else if (object.recordUid[i].length >= 0)
                        message.recordUid[i] = object.recordUid[i];
            }
            if (object.sharedObjectUid != null)
                if (typeof object.sharedObjectUid === "string")
                    $util.base64.decode(object.sharedObjectUid, message.sharedObjectUid = $util.newBuffer($util.base64.length(object.sharedObjectUid)), 0);
                else if (object.sharedObjectUid.length >= 0)
                    message.sharedObjectUid = object.sharedObjectUid;
            switch (object.timeLimitedAccessType) {
            default:
                if (typeof object.timeLimitedAccessType === "number") {
                    message.timeLimitedAccessType = object.timeLimitedAccessType;
                    break;
                }
                break;
            case "INVALID_TIME_LIMITED_ACCESS_TYPE":
            case 0:
                message.timeLimitedAccessType = 0;
                break;
            case "USER_ACCESS_TO_RECORD":
            case 1:
                message.timeLimitedAccessType = 1;
                break;
            case "USER_OR_TEAM_ACCESS_TO_SHAREDFOLDER":
            case 2:
                message.timeLimitedAccessType = 2;
                break;
            case "RECORD_ACCESS_TO_SHAREDFOLDER":
            case 3:
                message.timeLimitedAccessType = 3;
                break;
            }
            if (object.expiration != null)
                if ($util.Long)
                    message.expiration = $util.Long.fromValue(object.expiration, false);
                else if (typeof object.expiration === "string")
                    message.expiration = parseInt(object.expiration, 10);
                else if (typeof object.expiration === "number")
                    message.expiration = object.expiration;
                else if (typeof object.expiration === "object")
                    message.expiration = new $util.LongBits(object.expiration.low >>> 0, object.expiration.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a TimeLimitedAccessRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TimeLimitedAccessRequest
         * @static
         * @param {Authentication.TimeLimitedAccessRequest} message TimeLimitedAccessRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TimeLimitedAccessRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.accountUid = [];
                object.teamUid = [];
                object.recordUid = [];
            }
            if (options.defaults) {
                if (options.bytes === String)
                    object.sharedObjectUid = "";
                else {
                    object.sharedObjectUid = [];
                    if (options.bytes !== Array)
                        object.sharedObjectUid = $util.newBuffer(object.sharedObjectUid);
                }
                object.timeLimitedAccessType = options.enums === String ? "INVALID_TIME_LIMITED_ACCESS_TYPE" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expiration = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.expiration = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.accountUid && message.accountUid.length) {
                object.accountUid = [];
                for (let j = 0; j < message.accountUid.length; ++j)
                    object.accountUid[j] = options.bytes === String ? $util.base64.encode(message.accountUid[j], 0, message.accountUid[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.accountUid[j]) : message.accountUid[j];
            }
            if (message.teamUid && message.teamUid.length) {
                object.teamUid = [];
                for (let j = 0; j < message.teamUid.length; ++j)
                    object.teamUid[j] = options.bytes === String ? $util.base64.encode(message.teamUid[j], 0, message.teamUid[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.teamUid[j]) : message.teamUid[j];
            }
            if (message.recordUid && message.recordUid.length) {
                object.recordUid = [];
                for (let j = 0; j < message.recordUid.length; ++j)
                    object.recordUid[j] = options.bytes === String ? $util.base64.encode(message.recordUid[j], 0, message.recordUid[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.recordUid[j]) : message.recordUid[j];
            }
            if (message.sharedObjectUid != null && Object.hasOwnProperty.call(message, "sharedObjectUid"))
                object.sharedObjectUid = options.bytes === String ? $util.base64.encode(message.sharedObjectUid, 0, message.sharedObjectUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.sharedObjectUid) : message.sharedObjectUid;
            if (message.timeLimitedAccessType != null && Object.hasOwnProperty.call(message, "timeLimitedAccessType"))
                object.timeLimitedAccessType = options.enums === String ? $root.Authentication.TimeLimitedAccessType[message.timeLimitedAccessType] === undefined ? message.timeLimitedAccessType : $root.Authentication.TimeLimitedAccessType[message.timeLimitedAccessType] : message.timeLimitedAccessType;
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.expiration = typeof message.expiration === "number" ? BigInt(message.expiration) : $util.Long.fromBits(message.expiration.low >>> 0, message.expiration.high >>> 0, false).toBigInt();
                else if (typeof message.expiration === "number")
                    object.expiration = options.longs === String ? String(message.expiration) : message.expiration;
                else
                    object.expiration = options.longs === String ? $util.Long.prototype.toString.call(message.expiration) : options.longs === Number ? new $util.LongBits(message.expiration.low >>> 0, message.expiration.high >>> 0).toNumber() : message.expiration;
            return object;
        };

        /**
         * Converts this TimeLimitedAccessRequest to JSON.
         * @function toJSON
         * @memberof Authentication.TimeLimitedAccessRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TimeLimitedAccessRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TimeLimitedAccessRequest
         * @function getTypeUrl
         * @memberof Authentication.TimeLimitedAccessRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TimeLimitedAccessRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TimeLimitedAccessRequest";
        };

        return TimeLimitedAccessRequest;
    })();

    Authentication.TimeLimitedAccessStatus = (function() {

        /**
         * Properties of a TimeLimitedAccessStatus.
         * @memberof Authentication
         * @interface ITimeLimitedAccessStatus
         * @property {Uint8Array|null} [uid] TimeLimitedAccessStatus uid
         * @property {string|null} [message] TimeLimitedAccessStatus message
         */

        /**
         * Constructs a new TimeLimitedAccessStatus.
         * @memberof Authentication
         * @classdesc Represents a TimeLimitedAccessStatus.
         * @implements ITimeLimitedAccessStatus
         * @constructor
         * @param {Authentication.ITimeLimitedAccessStatus=} [properties] Properties to set
         */
        function TimeLimitedAccessStatus(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TimeLimitedAccessStatus uid.
         * @member {Uint8Array} uid
         * @memberof Authentication.TimeLimitedAccessStatus
         * @instance
         */
        TimeLimitedAccessStatus.prototype.uid = $util.newBuffer([]);

        /**
         * TimeLimitedAccessStatus message.
         * @member {string} message
         * @memberof Authentication.TimeLimitedAccessStatus
         * @instance
         */
        TimeLimitedAccessStatus.prototype.message = "";

        /**
         * Creates a new TimeLimitedAccessStatus instance using the specified properties.
         * @function create
         * @memberof Authentication.TimeLimitedAccessStatus
         * @static
         * @param {Authentication.ITimeLimitedAccessStatus=} [properties] Properties to set
         * @returns {Authentication.TimeLimitedAccessStatus} TimeLimitedAccessStatus instance
         */
        TimeLimitedAccessStatus.create = function create(properties) {
            return new TimeLimitedAccessStatus(properties);
        };

        /**
         * Encodes the specified TimeLimitedAccessStatus message. Does not implicitly {@link Authentication.TimeLimitedAccessStatus.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TimeLimitedAccessStatus
         * @static
         * @param {Authentication.ITimeLimitedAccessStatus} message TimeLimitedAccessStatus message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TimeLimitedAccessStatus.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.uid);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.message);
            return writer;
        };

        /**
         * Decodes a TimeLimitedAccessStatus message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TimeLimitedAccessStatus
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TimeLimitedAccessStatus} TimeLimitedAccessStatus
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TimeLimitedAccessStatus.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TimeLimitedAccessStatus();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.message = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TimeLimitedAccessStatus message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TimeLimitedAccessStatus
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TimeLimitedAccessStatus} TimeLimitedAccessStatus
         */
        TimeLimitedAccessStatus.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TimeLimitedAccessStatus)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TimeLimitedAccessStatus: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TimeLimitedAccessStatus();
            if (object.uid != null)
                if (typeof object.uid === "string")
                    $util.base64.decode(object.uid, message.uid = $util.newBuffer($util.base64.length(object.uid)), 0);
                else if (object.uid.length >= 0)
                    message.uid = object.uid;
            if (object.message != null)
                message.message = String(object.message);
            return message;
        };

        /**
         * Creates a plain object from a TimeLimitedAccessStatus message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TimeLimitedAccessStatus
         * @static
         * @param {Authentication.TimeLimitedAccessStatus} message TimeLimitedAccessStatus
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TimeLimitedAccessStatus.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.uid = "";
                else {
                    object.uid = [];
                    if (options.bytes !== Array)
                        object.uid = $util.newBuffer(object.uid);
                }
                object.message = "";
            }
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                object.uid = options.bytes === String ? $util.base64.encode(message.uid, 0, message.uid.length) : options.bytes === Array ? Array.prototype.slice.call(message.uid) : message.uid;
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                object.message = message.message;
            return object;
        };

        /**
         * Converts this TimeLimitedAccessStatus to JSON.
         * @function toJSON
         * @memberof Authentication.TimeLimitedAccessStatus
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TimeLimitedAccessStatus.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TimeLimitedAccessStatus
         * @function getTypeUrl
         * @memberof Authentication.TimeLimitedAccessStatus
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TimeLimitedAccessStatus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TimeLimitedAccessStatus";
        };

        return TimeLimitedAccessStatus;
    })();

    Authentication.TimeLimitedAccessResponse = (function() {

        /**
         * Properties of a TimeLimitedAccessResponse.
         * @memberof Authentication
         * @interface ITimeLimitedAccessResponse
         * @property {number|null} [revision] TimeLimitedAccessResponse revision
         * @property {Array.<Authentication.ITimeLimitedAccessStatus>|null} [userAccessStatus] TimeLimitedAccessResponse userAccessStatus
         * @property {Array.<Authentication.ITimeLimitedAccessStatus>|null} [teamAccessStatus] TimeLimitedAccessResponse teamAccessStatus
         * @property {Array.<Authentication.ITimeLimitedAccessStatus>|null} [recordAccessStatus] TimeLimitedAccessResponse recordAccessStatus
         */

        /**
         * Constructs a new TimeLimitedAccessResponse.
         * @memberof Authentication
         * @classdesc Represents a TimeLimitedAccessResponse.
         * @implements ITimeLimitedAccessResponse
         * @constructor
         * @param {Authentication.ITimeLimitedAccessResponse=} [properties] Properties to set
         */
        function TimeLimitedAccessResponse(properties) {
            this.userAccessStatus = [];
            this.teamAccessStatus = [];
            this.recordAccessStatus = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TimeLimitedAccessResponse revision.
         * @member {number} revision
         * @memberof Authentication.TimeLimitedAccessResponse
         * @instance
         */
        TimeLimitedAccessResponse.prototype.revision = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * TimeLimitedAccessResponse userAccessStatus.
         * @member {Array.<Authentication.ITimeLimitedAccessStatus>} userAccessStatus
         * @memberof Authentication.TimeLimitedAccessResponse
         * @instance
         */
        TimeLimitedAccessResponse.prototype.userAccessStatus = $util.emptyArray;

        /**
         * TimeLimitedAccessResponse teamAccessStatus.
         * @member {Array.<Authentication.ITimeLimitedAccessStatus>} teamAccessStatus
         * @memberof Authentication.TimeLimitedAccessResponse
         * @instance
         */
        TimeLimitedAccessResponse.prototype.teamAccessStatus = $util.emptyArray;

        /**
         * TimeLimitedAccessResponse recordAccessStatus.
         * @member {Array.<Authentication.ITimeLimitedAccessStatus>} recordAccessStatus
         * @memberof Authentication.TimeLimitedAccessResponse
         * @instance
         */
        TimeLimitedAccessResponse.prototype.recordAccessStatus = $util.emptyArray;

        /**
         * Creates a new TimeLimitedAccessResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.TimeLimitedAccessResponse
         * @static
         * @param {Authentication.ITimeLimitedAccessResponse=} [properties] Properties to set
         * @returns {Authentication.TimeLimitedAccessResponse} TimeLimitedAccessResponse instance
         */
        TimeLimitedAccessResponse.create = function create(properties) {
            return new TimeLimitedAccessResponse(properties);
        };

        /**
         * Encodes the specified TimeLimitedAccessResponse message. Does not implicitly {@link Authentication.TimeLimitedAccessResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TimeLimitedAccessResponse
         * @static
         * @param {Authentication.ITimeLimitedAccessResponse} message TimeLimitedAccessResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TimeLimitedAccessResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.revision);
            if (message.userAccessStatus != null && message.userAccessStatus.length)
                for (let i = 0; i < message.userAccessStatus.length; ++i)
                    $root.Authentication.TimeLimitedAccessStatus.encode(message.userAccessStatus[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.teamAccessStatus != null && message.teamAccessStatus.length)
                for (let i = 0; i < message.teamAccessStatus.length; ++i)
                    $root.Authentication.TimeLimitedAccessStatus.encode(message.teamAccessStatus[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.recordAccessStatus != null && message.recordAccessStatus.length)
                for (let i = 0; i < message.recordAccessStatus.length; ++i)
                    $root.Authentication.TimeLimitedAccessStatus.encode(message.recordAccessStatus[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes a TimeLimitedAccessResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TimeLimitedAccessResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TimeLimitedAccessResponse} TimeLimitedAccessResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TimeLimitedAccessResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TimeLimitedAccessResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.revision = reader.int64();
                        break;
                    }
                case 2: {
                        if (!(message.userAccessStatus && message.userAccessStatus.length))
                            message.userAccessStatus = [];
                        message.userAccessStatus.push($root.Authentication.TimeLimitedAccessStatus.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 3: {
                        if (!(message.teamAccessStatus && message.teamAccessStatus.length))
                            message.teamAccessStatus = [];
                        message.teamAccessStatus.push($root.Authentication.TimeLimitedAccessStatus.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 4: {
                        if (!(message.recordAccessStatus && message.recordAccessStatus.length))
                            message.recordAccessStatus = [];
                        message.recordAccessStatus.push($root.Authentication.TimeLimitedAccessStatus.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TimeLimitedAccessResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TimeLimitedAccessResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TimeLimitedAccessResponse} TimeLimitedAccessResponse
         */
        TimeLimitedAccessResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TimeLimitedAccessResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TimeLimitedAccessResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TimeLimitedAccessResponse();
            if (object.revision != null)
                if ($util.Long)
                    message.revision = $util.Long.fromValue(object.revision, false);
                else if (typeof object.revision === "string")
                    message.revision = parseInt(object.revision, 10);
                else if (typeof object.revision === "number")
                    message.revision = object.revision;
                else if (typeof object.revision === "object")
                    message.revision = new $util.LongBits(object.revision.low >>> 0, object.revision.high >>> 0).toNumber();
            if (object.userAccessStatus) {
                if (!Array.isArray(object.userAccessStatus))
                    throw TypeError(".Authentication.TimeLimitedAccessResponse.userAccessStatus: array expected");
                message.userAccessStatus = [];
                for (let i = 0; i < object.userAccessStatus.length; ++i) {
                    if (!$util.isObject(object.userAccessStatus[i]))
                        throw TypeError(".Authentication.TimeLimitedAccessResponse.userAccessStatus: object expected");
                    message.userAccessStatus[i] = $root.Authentication.TimeLimitedAccessStatus.fromObject(object.userAccessStatus[i], long + 1);
                }
            }
            if (object.teamAccessStatus) {
                if (!Array.isArray(object.teamAccessStatus))
                    throw TypeError(".Authentication.TimeLimitedAccessResponse.teamAccessStatus: array expected");
                message.teamAccessStatus = [];
                for (let i = 0; i < object.teamAccessStatus.length; ++i) {
                    if (!$util.isObject(object.teamAccessStatus[i]))
                        throw TypeError(".Authentication.TimeLimitedAccessResponse.teamAccessStatus: object expected");
                    message.teamAccessStatus[i] = $root.Authentication.TimeLimitedAccessStatus.fromObject(object.teamAccessStatus[i], long + 1);
                }
            }
            if (object.recordAccessStatus) {
                if (!Array.isArray(object.recordAccessStatus))
                    throw TypeError(".Authentication.TimeLimitedAccessResponse.recordAccessStatus: array expected");
                message.recordAccessStatus = [];
                for (let i = 0; i < object.recordAccessStatus.length; ++i) {
                    if (!$util.isObject(object.recordAccessStatus[i]))
                        throw TypeError(".Authentication.TimeLimitedAccessResponse.recordAccessStatus: object expected");
                    message.recordAccessStatus[i] = $root.Authentication.TimeLimitedAccessStatus.fromObject(object.recordAccessStatus[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TimeLimitedAccessResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TimeLimitedAccessResponse
         * @static
         * @param {Authentication.TimeLimitedAccessResponse} message TimeLimitedAccessResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TimeLimitedAccessResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.userAccessStatus = [];
                object.teamAccessStatus = [];
                object.recordAccessStatus = [];
            }
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.revision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.revision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            if (message.revision != null && Object.hasOwnProperty.call(message, "revision"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.revision = typeof message.revision === "number" ? BigInt(message.revision) : $util.Long.fromBits(message.revision.low >>> 0, message.revision.high >>> 0, false).toBigInt();
                else if (typeof message.revision === "number")
                    object.revision = options.longs === String ? String(message.revision) : message.revision;
                else
                    object.revision = options.longs === String ? $util.Long.prototype.toString.call(message.revision) : options.longs === Number ? new $util.LongBits(message.revision.low >>> 0, message.revision.high >>> 0).toNumber() : message.revision;
            if (message.userAccessStatus && message.userAccessStatus.length) {
                object.userAccessStatus = [];
                for (let j = 0; j < message.userAccessStatus.length; ++j)
                    object.userAccessStatus[j] = $root.Authentication.TimeLimitedAccessStatus.toObject(message.userAccessStatus[j], options, q + 1);
            }
            if (message.teamAccessStatus && message.teamAccessStatus.length) {
                object.teamAccessStatus = [];
                for (let j = 0; j < message.teamAccessStatus.length; ++j)
                    object.teamAccessStatus[j] = $root.Authentication.TimeLimitedAccessStatus.toObject(message.teamAccessStatus[j], options, q + 1);
            }
            if (message.recordAccessStatus && message.recordAccessStatus.length) {
                object.recordAccessStatus = [];
                for (let j = 0; j < message.recordAccessStatus.length; ++j)
                    object.recordAccessStatus[j] = $root.Authentication.TimeLimitedAccessStatus.toObject(message.recordAccessStatus[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this TimeLimitedAccessResponse to JSON.
         * @function toJSON
         * @memberof Authentication.TimeLimitedAccessResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TimeLimitedAccessResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TimeLimitedAccessResponse
         * @function getTypeUrl
         * @memberof Authentication.TimeLimitedAccessResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TimeLimitedAccessResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TimeLimitedAccessResponse";
        };

        return TimeLimitedAccessResponse;
    })();

    Authentication.RequestDownloadRequest = (function() {

        /**
         * Properties of a RequestDownloadRequest.
         * @memberof Authentication
         * @interface IRequestDownloadRequest
         * @property {Array.<string>|null} [fileNames] RequestDownloadRequest fileNames
         */

        /**
         * Constructs a new RequestDownloadRequest.
         * @memberof Authentication
         * @classdesc Represents a RequestDownloadRequest.
         * @implements IRequestDownloadRequest
         * @constructor
         * @param {Authentication.IRequestDownloadRequest=} [properties] Properties to set
         */
        function RequestDownloadRequest(properties) {
            this.fileNames = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RequestDownloadRequest fileNames.
         * @member {Array.<string>} fileNames
         * @memberof Authentication.RequestDownloadRequest
         * @instance
         */
        RequestDownloadRequest.prototype.fileNames = $util.emptyArray;

        /**
         * Creates a new RequestDownloadRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.RequestDownloadRequest
         * @static
         * @param {Authentication.IRequestDownloadRequest=} [properties] Properties to set
         * @returns {Authentication.RequestDownloadRequest} RequestDownloadRequest instance
         */
        RequestDownloadRequest.create = function create(properties) {
            return new RequestDownloadRequest(properties);
        };

        /**
         * Encodes the specified RequestDownloadRequest message. Does not implicitly {@link Authentication.RequestDownloadRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.RequestDownloadRequest
         * @static
         * @param {Authentication.IRequestDownloadRequest} message RequestDownloadRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RequestDownloadRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.fileNames != null && message.fileNames.length)
                for (let i = 0; i < message.fileNames.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.fileNames[i]);
            return writer;
        };

        /**
         * Decodes a RequestDownloadRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.RequestDownloadRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.RequestDownloadRequest} RequestDownloadRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RequestDownloadRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.RequestDownloadRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.fileNames && message.fileNames.length))
                            message.fileNames = [];
                        message.fileNames.push(reader.string());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a RequestDownloadRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.RequestDownloadRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.RequestDownloadRequest} RequestDownloadRequest
         */
        RequestDownloadRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.RequestDownloadRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.RequestDownloadRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.RequestDownloadRequest();
            if (object.fileNames) {
                if (!Array.isArray(object.fileNames))
                    throw TypeError(".Authentication.RequestDownloadRequest.fileNames: array expected");
                message.fileNames = [];
                for (let i = 0; i < object.fileNames.length; ++i)
                    message.fileNames[i] = String(object.fileNames[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a RequestDownloadRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.RequestDownloadRequest
         * @static
         * @param {Authentication.RequestDownloadRequest} message RequestDownloadRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RequestDownloadRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.fileNames = [];
            if (message.fileNames && message.fileNames.length) {
                object.fileNames = [];
                for (let j = 0; j < message.fileNames.length; ++j)
                    object.fileNames[j] = message.fileNames[j];
            }
            return object;
        };

        /**
         * Converts this RequestDownloadRequest to JSON.
         * @function toJSON
         * @memberof Authentication.RequestDownloadRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RequestDownloadRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RequestDownloadRequest
         * @function getTypeUrl
         * @memberof Authentication.RequestDownloadRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RequestDownloadRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.RequestDownloadRequest";
        };

        return RequestDownloadRequest;
    })();

    Authentication.RequestDownloadResponse = (function() {

        /**
         * Properties of a RequestDownloadResponse.
         * @memberof Authentication
         * @interface IRequestDownloadResponse
         * @property {string|null} [result] RequestDownloadResponse result
         * @property {string|null} [message] RequestDownloadResponse message
         * @property {Array.<Authentication.IDownload>|null} [downloads] RequestDownloadResponse downloads
         */

        /**
         * Constructs a new RequestDownloadResponse.
         * @memberof Authentication
         * @classdesc Represents a RequestDownloadResponse.
         * @implements IRequestDownloadResponse
         * @constructor
         * @param {Authentication.IRequestDownloadResponse=} [properties] Properties to set
         */
        function RequestDownloadResponse(properties) {
            this.downloads = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RequestDownloadResponse result.
         * @member {string} result
         * @memberof Authentication.RequestDownloadResponse
         * @instance
         */
        RequestDownloadResponse.prototype.result = "";

        /**
         * RequestDownloadResponse message.
         * @member {string} message
         * @memberof Authentication.RequestDownloadResponse
         * @instance
         */
        RequestDownloadResponse.prototype.message = "";

        /**
         * RequestDownloadResponse downloads.
         * @member {Array.<Authentication.IDownload>} downloads
         * @memberof Authentication.RequestDownloadResponse
         * @instance
         */
        RequestDownloadResponse.prototype.downloads = $util.emptyArray;

        /**
         * Creates a new RequestDownloadResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.RequestDownloadResponse
         * @static
         * @param {Authentication.IRequestDownloadResponse=} [properties] Properties to set
         * @returns {Authentication.RequestDownloadResponse} RequestDownloadResponse instance
         */
        RequestDownloadResponse.create = function create(properties) {
            return new RequestDownloadResponse(properties);
        };

        /**
         * Encodes the specified RequestDownloadResponse message. Does not implicitly {@link Authentication.RequestDownloadResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.RequestDownloadResponse
         * @static
         * @param {Authentication.IRequestDownloadResponse} message RequestDownloadResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RequestDownloadResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.result);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.message);
            if (message.downloads != null && message.downloads.length)
                for (let i = 0; i < message.downloads.length; ++i)
                    $root.Authentication.Download.encode(message.downloads[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes a RequestDownloadResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.RequestDownloadResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.RequestDownloadResponse} RequestDownloadResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RequestDownloadResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.RequestDownloadResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.result = reader.string();
                        break;
                    }
                case 2: {
                        message.message = reader.string();
                        break;
                    }
                case 3: {
                        if (!(message.downloads && message.downloads.length))
                            message.downloads = [];
                        message.downloads.push($root.Authentication.Download.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a RequestDownloadResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.RequestDownloadResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.RequestDownloadResponse} RequestDownloadResponse
         */
        RequestDownloadResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.RequestDownloadResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.RequestDownloadResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.RequestDownloadResponse();
            if (object.result != null)
                message.result = String(object.result);
            if (object.message != null)
                message.message = String(object.message);
            if (object.downloads) {
                if (!Array.isArray(object.downloads))
                    throw TypeError(".Authentication.RequestDownloadResponse.downloads: array expected");
                message.downloads = [];
                for (let i = 0; i < object.downloads.length; ++i) {
                    if (!$util.isObject(object.downloads[i]))
                        throw TypeError(".Authentication.RequestDownloadResponse.downloads: object expected");
                    message.downloads[i] = $root.Authentication.Download.fromObject(object.downloads[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a RequestDownloadResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.RequestDownloadResponse
         * @static
         * @param {Authentication.RequestDownloadResponse} message RequestDownloadResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RequestDownloadResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.downloads = [];
            if (options.defaults) {
                object.result = "";
                object.message = "";
            }
            if (message.result != null && Object.hasOwnProperty.call(message, "result"))
                object.result = message.result;
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                object.message = message.message;
            if (message.downloads && message.downloads.length) {
                object.downloads = [];
                for (let j = 0; j < message.downloads.length; ++j)
                    object.downloads[j] = $root.Authentication.Download.toObject(message.downloads[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this RequestDownloadResponse to JSON.
         * @function toJSON
         * @memberof Authentication.RequestDownloadResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RequestDownloadResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RequestDownloadResponse
         * @function getTypeUrl
         * @memberof Authentication.RequestDownloadResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RequestDownloadResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.RequestDownloadResponse";
        };

        return RequestDownloadResponse;
    })();

    Authentication.Download = (function() {

        /**
         * Properties of a Download.
         * @memberof Authentication
         * @interface IDownload
         * @property {string|null} [fileName] Download fileName
         * @property {string|null} [url] Download url
         * @property {number|null} [successStatusCode] Download successStatusCode
         */

        /**
         * Constructs a new Download.
         * @memberof Authentication
         * @classdesc Represents a Download.
         * @implements IDownload
         * @constructor
         * @param {Authentication.IDownload=} [properties] Properties to set
         */
        function Download(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Download fileName.
         * @member {string} fileName
         * @memberof Authentication.Download
         * @instance
         */
        Download.prototype.fileName = "";

        /**
         * Download url.
         * @member {string} url
         * @memberof Authentication.Download
         * @instance
         */
        Download.prototype.url = "";

        /**
         * Download successStatusCode.
         * @member {number} successStatusCode
         * @memberof Authentication.Download
         * @instance
         */
        Download.prototype.successStatusCode = 0;

        /**
         * Creates a new Download instance using the specified properties.
         * @function create
         * @memberof Authentication.Download
         * @static
         * @param {Authentication.IDownload=} [properties] Properties to set
         * @returns {Authentication.Download} Download instance
         */
        Download.create = function create(properties) {
            return new Download(properties);
        };

        /**
         * Encodes the specified Download message. Does not implicitly {@link Authentication.Download.verify|verify} messages.
         * @function encode
         * @memberof Authentication.Download
         * @static
         * @param {Authentication.IDownload} message Download message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Download.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.fileName);
            if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.url);
            if (message.successStatusCode != null && Object.hasOwnProperty.call(message, "successStatusCode"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.successStatusCode);
            return writer;
        };

        /**
         * Decodes a Download message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.Download
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.Download} Download
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Download.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.Download();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.fileName = reader.string();
                        break;
                    }
                case 2: {
                        message.url = reader.string();
                        break;
                    }
                case 3: {
                        message.successStatusCode = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a Download message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.Download
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.Download} Download
         */
        Download.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.Download)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.Download: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.Download();
            if (object.fileName != null)
                message.fileName = String(object.fileName);
            if (object.url != null)
                message.url = String(object.url);
            if (object.successStatusCode != null)
                message.successStatusCode = object.successStatusCode | 0;
            return message;
        };

        /**
         * Creates a plain object from a Download message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.Download
         * @static
         * @param {Authentication.Download} message Download
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Download.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.fileName = "";
                object.url = "";
                object.successStatusCode = 0;
            }
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                object.fileName = message.fileName;
            if (message.url != null && Object.hasOwnProperty.call(message, "url"))
                object.url = message.url;
            if (message.successStatusCode != null && Object.hasOwnProperty.call(message, "successStatusCode"))
                object.successStatusCode = message.successStatusCode;
            return object;
        };

        /**
         * Converts this Download to JSON.
         * @function toJSON
         * @memberof Authentication.Download
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Download.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Download
         * @function getTypeUrl
         * @memberof Authentication.Download
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Download.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.Download";
        };

        return Download;
    })();

    Authentication.DeleteUserRequest = (function() {

        /**
         * Properties of a DeleteUserRequest.
         * @memberof Authentication
         * @interface IDeleteUserRequest
         * @property {string|null} [reason] DeleteUserRequest reason
         */

        /**
         * Constructs a new DeleteUserRequest.
         * @memberof Authentication
         * @classdesc Represents a DeleteUserRequest.
         * @implements IDeleteUserRequest
         * @constructor
         * @param {Authentication.IDeleteUserRequest=} [properties] Properties to set
         */
        function DeleteUserRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeleteUserRequest reason.
         * @member {string} reason
         * @memberof Authentication.DeleteUserRequest
         * @instance
         */
        DeleteUserRequest.prototype.reason = "";

        /**
         * Creates a new DeleteUserRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.DeleteUserRequest
         * @static
         * @param {Authentication.IDeleteUserRequest=} [properties] Properties to set
         * @returns {Authentication.DeleteUserRequest} DeleteUserRequest instance
         */
        DeleteUserRequest.create = function create(properties) {
            return new DeleteUserRequest(properties);
        };

        /**
         * Encodes the specified DeleteUserRequest message. Does not implicitly {@link Authentication.DeleteUserRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.DeleteUserRequest
         * @static
         * @param {Authentication.IDeleteUserRequest} message DeleteUserRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeleteUserRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.reason != null && Object.hasOwnProperty.call(message, "reason"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.reason);
            return writer;
        };

        /**
         * Decodes a DeleteUserRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.DeleteUserRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.DeleteUserRequest} DeleteUserRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeleteUserRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.DeleteUserRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.reason = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a DeleteUserRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.DeleteUserRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.DeleteUserRequest} DeleteUserRequest
         */
        DeleteUserRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.DeleteUserRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.DeleteUserRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.DeleteUserRequest();
            if (object.reason != null)
                message.reason = String(object.reason);
            return message;
        };

        /**
         * Creates a plain object from a DeleteUserRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.DeleteUserRequest
         * @static
         * @param {Authentication.DeleteUserRequest} message DeleteUserRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeleteUserRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.reason = "";
            if (message.reason != null && Object.hasOwnProperty.call(message, "reason"))
                object.reason = message.reason;
            return object;
        };

        /**
         * Converts this DeleteUserRequest to JSON.
         * @function toJSON
         * @memberof Authentication.DeleteUserRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeleteUserRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeleteUserRequest
         * @function getTypeUrl
         * @memberof Authentication.DeleteUserRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeleteUserRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.DeleteUserRequest";
        };

        return DeleteUserRequest;
    })();

    Authentication.ChangeMasterPasswordRequest = (function() {

        /**
         * Properties of a ChangeMasterPasswordRequest.
         * @memberof Authentication
         * @interface IChangeMasterPasswordRequest
         * @property {Uint8Array|null} [authVerifier] ChangeMasterPasswordRequest authVerifier
         * @property {Uint8Array|null} [encryptionParams] ChangeMasterPasswordRequest encryptionParams
         * @property {boolean|null} [fromServiceProvider] ChangeMasterPasswordRequest fromServiceProvider
         * @property {boolean|null} [iterationsChange] ChangeMasterPasswordRequest iterationsChange
         */

        /**
         * Constructs a new ChangeMasterPasswordRequest.
         * @memberof Authentication
         * @classdesc Represents a ChangeMasterPasswordRequest.
         * @implements IChangeMasterPasswordRequest
         * @constructor
         * @param {Authentication.IChangeMasterPasswordRequest=} [properties] Properties to set
         */
        function ChangeMasterPasswordRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChangeMasterPasswordRequest authVerifier.
         * @member {Uint8Array} authVerifier
         * @memberof Authentication.ChangeMasterPasswordRequest
         * @instance
         */
        ChangeMasterPasswordRequest.prototype.authVerifier = $util.newBuffer([]);

        /**
         * ChangeMasterPasswordRequest encryptionParams.
         * @member {Uint8Array} encryptionParams
         * @memberof Authentication.ChangeMasterPasswordRequest
         * @instance
         */
        ChangeMasterPasswordRequest.prototype.encryptionParams = $util.newBuffer([]);

        /**
         * ChangeMasterPasswordRequest fromServiceProvider.
         * @member {boolean} fromServiceProvider
         * @memberof Authentication.ChangeMasterPasswordRequest
         * @instance
         */
        ChangeMasterPasswordRequest.prototype.fromServiceProvider = false;

        /**
         * ChangeMasterPasswordRequest iterationsChange.
         * @member {boolean} iterationsChange
         * @memberof Authentication.ChangeMasterPasswordRequest
         * @instance
         */
        ChangeMasterPasswordRequest.prototype.iterationsChange = false;

        /**
         * Creates a new ChangeMasterPasswordRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.ChangeMasterPasswordRequest
         * @static
         * @param {Authentication.IChangeMasterPasswordRequest=} [properties] Properties to set
         * @returns {Authentication.ChangeMasterPasswordRequest} ChangeMasterPasswordRequest instance
         */
        ChangeMasterPasswordRequest.create = function create(properties) {
            return new ChangeMasterPasswordRequest(properties);
        };

        /**
         * Encodes the specified ChangeMasterPasswordRequest message. Does not implicitly {@link Authentication.ChangeMasterPasswordRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ChangeMasterPasswordRequest
         * @static
         * @param {Authentication.IChangeMasterPasswordRequest} message ChangeMasterPasswordRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangeMasterPasswordRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.authVerifier != null && Object.hasOwnProperty.call(message, "authVerifier"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.authVerifier);
            if (message.encryptionParams != null && Object.hasOwnProperty.call(message, "encryptionParams"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encryptionParams);
            if (message.fromServiceProvider != null && Object.hasOwnProperty.call(message, "fromServiceProvider"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.fromServiceProvider);
            if (message.iterationsChange != null && Object.hasOwnProperty.call(message, "iterationsChange"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.iterationsChange);
            return writer;
        };

        /**
         * Decodes a ChangeMasterPasswordRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ChangeMasterPasswordRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ChangeMasterPasswordRequest} ChangeMasterPasswordRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangeMasterPasswordRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ChangeMasterPasswordRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.authVerifier = reader.bytes();
                        break;
                    }
                case 2: {
                        message.encryptionParams = reader.bytes();
                        break;
                    }
                case 3: {
                        message.fromServiceProvider = reader.bool();
                        break;
                    }
                case 4: {
                        message.iterationsChange = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a ChangeMasterPasswordRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ChangeMasterPasswordRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ChangeMasterPasswordRequest} ChangeMasterPasswordRequest
         */
        ChangeMasterPasswordRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ChangeMasterPasswordRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ChangeMasterPasswordRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ChangeMasterPasswordRequest();
            if (object.authVerifier != null)
                if (typeof object.authVerifier === "string")
                    $util.base64.decode(object.authVerifier, message.authVerifier = $util.newBuffer($util.base64.length(object.authVerifier)), 0);
                else if (object.authVerifier.length >= 0)
                    message.authVerifier = object.authVerifier;
            if (object.encryptionParams != null)
                if (typeof object.encryptionParams === "string")
                    $util.base64.decode(object.encryptionParams, message.encryptionParams = $util.newBuffer($util.base64.length(object.encryptionParams)), 0);
                else if (object.encryptionParams.length >= 0)
                    message.encryptionParams = object.encryptionParams;
            if (object.fromServiceProvider != null)
                message.fromServiceProvider = Boolean(object.fromServiceProvider);
            if (object.iterationsChange != null)
                message.iterationsChange = Boolean(object.iterationsChange);
            return message;
        };

        /**
         * Creates a plain object from a ChangeMasterPasswordRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ChangeMasterPasswordRequest
         * @static
         * @param {Authentication.ChangeMasterPasswordRequest} message ChangeMasterPasswordRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChangeMasterPasswordRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.authVerifier = "";
                else {
                    object.authVerifier = [];
                    if (options.bytes !== Array)
                        object.authVerifier = $util.newBuffer(object.authVerifier);
                }
                if (options.bytes === String)
                    object.encryptionParams = "";
                else {
                    object.encryptionParams = [];
                    if (options.bytes !== Array)
                        object.encryptionParams = $util.newBuffer(object.encryptionParams);
                }
                object.fromServiceProvider = false;
                object.iterationsChange = false;
            }
            if (message.authVerifier != null && Object.hasOwnProperty.call(message, "authVerifier"))
                object.authVerifier = options.bytes === String ? $util.base64.encode(message.authVerifier, 0, message.authVerifier.length) : options.bytes === Array ? Array.prototype.slice.call(message.authVerifier) : message.authVerifier;
            if (message.encryptionParams != null && Object.hasOwnProperty.call(message, "encryptionParams"))
                object.encryptionParams = options.bytes === String ? $util.base64.encode(message.encryptionParams, 0, message.encryptionParams.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptionParams) : message.encryptionParams;
            if (message.fromServiceProvider != null && Object.hasOwnProperty.call(message, "fromServiceProvider"))
                object.fromServiceProvider = message.fromServiceProvider;
            if (message.iterationsChange != null && Object.hasOwnProperty.call(message, "iterationsChange"))
                object.iterationsChange = message.iterationsChange;
            return object;
        };

        /**
         * Converts this ChangeMasterPasswordRequest to JSON.
         * @function toJSON
         * @memberof Authentication.ChangeMasterPasswordRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChangeMasterPasswordRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChangeMasterPasswordRequest
         * @function getTypeUrl
         * @memberof Authentication.ChangeMasterPasswordRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChangeMasterPasswordRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ChangeMasterPasswordRequest";
        };

        return ChangeMasterPasswordRequest;
    })();

    Authentication.ChangeMasterPasswordResponse = (function() {

        /**
         * Properties of a ChangeMasterPasswordResponse.
         * @memberof Authentication
         * @interface IChangeMasterPasswordResponse
         * @property {Uint8Array|null} [encryptedSessionToken] ChangeMasterPasswordResponse encryptedSessionToken
         */

        /**
         * Constructs a new ChangeMasterPasswordResponse.
         * @memberof Authentication
         * @classdesc Represents a ChangeMasterPasswordResponse.
         * @implements IChangeMasterPasswordResponse
         * @constructor
         * @param {Authentication.IChangeMasterPasswordResponse=} [properties] Properties to set
         */
        function ChangeMasterPasswordResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChangeMasterPasswordResponse encryptedSessionToken.
         * @member {Uint8Array} encryptedSessionToken
         * @memberof Authentication.ChangeMasterPasswordResponse
         * @instance
         */
        ChangeMasterPasswordResponse.prototype.encryptedSessionToken = $util.newBuffer([]);

        /**
         * Creates a new ChangeMasterPasswordResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.ChangeMasterPasswordResponse
         * @static
         * @param {Authentication.IChangeMasterPasswordResponse=} [properties] Properties to set
         * @returns {Authentication.ChangeMasterPasswordResponse} ChangeMasterPasswordResponse instance
         */
        ChangeMasterPasswordResponse.create = function create(properties) {
            return new ChangeMasterPasswordResponse(properties);
        };

        /**
         * Encodes the specified ChangeMasterPasswordResponse message. Does not implicitly {@link Authentication.ChangeMasterPasswordResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.ChangeMasterPasswordResponse
         * @static
         * @param {Authentication.IChangeMasterPasswordResponse} message ChangeMasterPasswordResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangeMasterPasswordResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.encryptedSessionToken != null && Object.hasOwnProperty.call(message, "encryptedSessionToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.encryptedSessionToken);
            return writer;
        };

        /**
         * Decodes a ChangeMasterPasswordResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.ChangeMasterPasswordResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.ChangeMasterPasswordResponse} ChangeMasterPasswordResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangeMasterPasswordResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.ChangeMasterPasswordResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.encryptedSessionToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a ChangeMasterPasswordResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.ChangeMasterPasswordResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.ChangeMasterPasswordResponse} ChangeMasterPasswordResponse
         */
        ChangeMasterPasswordResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.ChangeMasterPasswordResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.ChangeMasterPasswordResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.ChangeMasterPasswordResponse();
            if (object.encryptedSessionToken != null)
                if (typeof object.encryptedSessionToken === "string")
                    $util.base64.decode(object.encryptedSessionToken, message.encryptedSessionToken = $util.newBuffer($util.base64.length(object.encryptedSessionToken)), 0);
                else if (object.encryptedSessionToken.length >= 0)
                    message.encryptedSessionToken = object.encryptedSessionToken;
            return message;
        };

        /**
         * Creates a plain object from a ChangeMasterPasswordResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.ChangeMasterPasswordResponse
         * @static
         * @param {Authentication.ChangeMasterPasswordResponse} message ChangeMasterPasswordResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChangeMasterPasswordResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === String)
                    object.encryptedSessionToken = "";
                else {
                    object.encryptedSessionToken = [];
                    if (options.bytes !== Array)
                        object.encryptedSessionToken = $util.newBuffer(object.encryptedSessionToken);
                }
            if (message.encryptedSessionToken != null && Object.hasOwnProperty.call(message, "encryptedSessionToken"))
                object.encryptedSessionToken = options.bytes === String ? $util.base64.encode(message.encryptedSessionToken, 0, message.encryptedSessionToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedSessionToken) : message.encryptedSessionToken;
            return object;
        };

        /**
         * Converts this ChangeMasterPasswordResponse to JSON.
         * @function toJSON
         * @memberof Authentication.ChangeMasterPasswordResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChangeMasterPasswordResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChangeMasterPasswordResponse
         * @function getTypeUrl
         * @memberof Authentication.ChangeMasterPasswordResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChangeMasterPasswordResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.ChangeMasterPasswordResponse";
        };

        return ChangeMasterPasswordResponse;
    })();

    Authentication.AccountRecoverySetupRequest = (function() {

        /**
         * Properties of an AccountRecoverySetupRequest.
         * @memberof Authentication
         * @interface IAccountRecoverySetupRequest
         * @property {Uint8Array|null} [recoveryEncryptedDataKey] AccountRecoverySetupRequest recoveryEncryptedDataKey
         * @property {Uint8Array|null} [recoveryAuthHash] AccountRecoverySetupRequest recoveryAuthHash
         */

        /**
         * Constructs a new AccountRecoverySetupRequest.
         * @memberof Authentication
         * @classdesc Represents an AccountRecoverySetupRequest.
         * @implements IAccountRecoverySetupRequest
         * @constructor
         * @param {Authentication.IAccountRecoverySetupRequest=} [properties] Properties to set
         */
        function AccountRecoverySetupRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AccountRecoverySetupRequest recoveryEncryptedDataKey.
         * @member {Uint8Array} recoveryEncryptedDataKey
         * @memberof Authentication.AccountRecoverySetupRequest
         * @instance
         */
        AccountRecoverySetupRequest.prototype.recoveryEncryptedDataKey = $util.newBuffer([]);

        /**
         * AccountRecoverySetupRequest recoveryAuthHash.
         * @member {Uint8Array} recoveryAuthHash
         * @memberof Authentication.AccountRecoverySetupRequest
         * @instance
         */
        AccountRecoverySetupRequest.prototype.recoveryAuthHash = $util.newBuffer([]);

        /**
         * Creates a new AccountRecoverySetupRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.AccountRecoverySetupRequest
         * @static
         * @param {Authentication.IAccountRecoverySetupRequest=} [properties] Properties to set
         * @returns {Authentication.AccountRecoverySetupRequest} AccountRecoverySetupRequest instance
         */
        AccountRecoverySetupRequest.create = function create(properties) {
            return new AccountRecoverySetupRequest(properties);
        };

        /**
         * Encodes the specified AccountRecoverySetupRequest message. Does not implicitly {@link Authentication.AccountRecoverySetupRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.AccountRecoverySetupRequest
         * @static
         * @param {Authentication.IAccountRecoverySetupRequest} message AccountRecoverySetupRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountRecoverySetupRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.recoveryEncryptedDataKey != null && Object.hasOwnProperty.call(message, "recoveryEncryptedDataKey"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.recoveryEncryptedDataKey);
            if (message.recoveryAuthHash != null && Object.hasOwnProperty.call(message, "recoveryAuthHash"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.recoveryAuthHash);
            return writer;
        };

        /**
         * Decodes an AccountRecoverySetupRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.AccountRecoverySetupRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.AccountRecoverySetupRequest} AccountRecoverySetupRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountRecoverySetupRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.AccountRecoverySetupRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.recoveryEncryptedDataKey = reader.bytes();
                        break;
                    }
                case 2: {
                        message.recoveryAuthHash = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an AccountRecoverySetupRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.AccountRecoverySetupRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.AccountRecoverySetupRequest} AccountRecoverySetupRequest
         */
        AccountRecoverySetupRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.AccountRecoverySetupRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.AccountRecoverySetupRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.AccountRecoverySetupRequest();
            if (object.recoveryEncryptedDataKey != null)
                if (typeof object.recoveryEncryptedDataKey === "string")
                    $util.base64.decode(object.recoveryEncryptedDataKey, message.recoveryEncryptedDataKey = $util.newBuffer($util.base64.length(object.recoveryEncryptedDataKey)), 0);
                else if (object.recoveryEncryptedDataKey.length >= 0)
                    message.recoveryEncryptedDataKey = object.recoveryEncryptedDataKey;
            if (object.recoveryAuthHash != null)
                if (typeof object.recoveryAuthHash === "string")
                    $util.base64.decode(object.recoveryAuthHash, message.recoveryAuthHash = $util.newBuffer($util.base64.length(object.recoveryAuthHash)), 0);
                else if (object.recoveryAuthHash.length >= 0)
                    message.recoveryAuthHash = object.recoveryAuthHash;
            return message;
        };

        /**
         * Creates a plain object from an AccountRecoverySetupRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.AccountRecoverySetupRequest
         * @static
         * @param {Authentication.AccountRecoverySetupRequest} message AccountRecoverySetupRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AccountRecoverySetupRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.recoveryEncryptedDataKey = "";
                else {
                    object.recoveryEncryptedDataKey = [];
                    if (options.bytes !== Array)
                        object.recoveryEncryptedDataKey = $util.newBuffer(object.recoveryEncryptedDataKey);
                }
                if (options.bytes === String)
                    object.recoveryAuthHash = "";
                else {
                    object.recoveryAuthHash = [];
                    if (options.bytes !== Array)
                        object.recoveryAuthHash = $util.newBuffer(object.recoveryAuthHash);
                }
            }
            if (message.recoveryEncryptedDataKey != null && Object.hasOwnProperty.call(message, "recoveryEncryptedDataKey"))
                object.recoveryEncryptedDataKey = options.bytes === String ? $util.base64.encode(message.recoveryEncryptedDataKey, 0, message.recoveryEncryptedDataKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.recoveryEncryptedDataKey) : message.recoveryEncryptedDataKey;
            if (message.recoveryAuthHash != null && Object.hasOwnProperty.call(message, "recoveryAuthHash"))
                object.recoveryAuthHash = options.bytes === String ? $util.base64.encode(message.recoveryAuthHash, 0, message.recoveryAuthHash.length) : options.bytes === Array ? Array.prototype.slice.call(message.recoveryAuthHash) : message.recoveryAuthHash;
            return object;
        };

        /**
         * Converts this AccountRecoverySetupRequest to JSON.
         * @function toJSON
         * @memberof Authentication.AccountRecoverySetupRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AccountRecoverySetupRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AccountRecoverySetupRequest
         * @function getTypeUrl
         * @memberof Authentication.AccountRecoverySetupRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AccountRecoverySetupRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.AccountRecoverySetupRequest";
        };

        return AccountRecoverySetupRequest;
    })();

    /**
     * BackupKeyType enum.
     * @name Authentication.BackupKeyType
     * @enum {number}
     * @property {number} BKT_SEC_ANSWER=0 BKT_SEC_ANSWER value
     * @property {number} BKT_PASSPHRASE_HASH=1 BKT_PASSPHRASE_HASH value
     */
    Authentication.BackupKeyType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "BKT_SEC_ANSWER"] = 0;
        values[valuesById[1] = "BKT_PASSPHRASE_HASH"] = 1;
        return values;
    })();

    Authentication.AccountRecoveryVerifyCodeResponse = (function() {

        /**
         * Properties of an AccountRecoveryVerifyCodeResponse.
         * @memberof Authentication
         * @interface IAccountRecoveryVerifyCodeResponse
         * @property {Authentication.BackupKeyType|null} [backupKeyType] AccountRecoveryVerifyCodeResponse backupKeyType
         * @property {number|null} [backupKeyDate] AccountRecoveryVerifyCodeResponse backupKeyDate
         * @property {string|null} [securityQuestion] AccountRecoveryVerifyCodeResponse securityQuestion
         * @property {Uint8Array|null} [salt] AccountRecoveryVerifyCodeResponse salt
         * @property {number|null} [iterations] AccountRecoveryVerifyCodeResponse iterations
         */

        /**
         * Constructs a new AccountRecoveryVerifyCodeResponse.
         * @memberof Authentication
         * @classdesc Represents an AccountRecoveryVerifyCodeResponse.
         * @implements IAccountRecoveryVerifyCodeResponse
         * @constructor
         * @param {Authentication.IAccountRecoveryVerifyCodeResponse=} [properties] Properties to set
         */
        function AccountRecoveryVerifyCodeResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AccountRecoveryVerifyCodeResponse backupKeyType.
         * @member {Authentication.BackupKeyType} backupKeyType
         * @memberof Authentication.AccountRecoveryVerifyCodeResponse
         * @instance
         */
        AccountRecoveryVerifyCodeResponse.prototype.backupKeyType = 0;

        /**
         * AccountRecoveryVerifyCodeResponse backupKeyDate.
         * @member {number} backupKeyDate
         * @memberof Authentication.AccountRecoveryVerifyCodeResponse
         * @instance
         */
        AccountRecoveryVerifyCodeResponse.prototype.backupKeyDate = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AccountRecoveryVerifyCodeResponse securityQuestion.
         * @member {string} securityQuestion
         * @memberof Authentication.AccountRecoveryVerifyCodeResponse
         * @instance
         */
        AccountRecoveryVerifyCodeResponse.prototype.securityQuestion = "";

        /**
         * AccountRecoveryVerifyCodeResponse salt.
         * @member {Uint8Array} salt
         * @memberof Authentication.AccountRecoveryVerifyCodeResponse
         * @instance
         */
        AccountRecoveryVerifyCodeResponse.prototype.salt = $util.newBuffer([]);

        /**
         * AccountRecoveryVerifyCodeResponse iterations.
         * @member {number} iterations
         * @memberof Authentication.AccountRecoveryVerifyCodeResponse
         * @instance
         */
        AccountRecoveryVerifyCodeResponse.prototype.iterations = 0;

        /**
         * Creates a new AccountRecoveryVerifyCodeResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.AccountRecoveryVerifyCodeResponse
         * @static
         * @param {Authentication.IAccountRecoveryVerifyCodeResponse=} [properties] Properties to set
         * @returns {Authentication.AccountRecoveryVerifyCodeResponse} AccountRecoveryVerifyCodeResponse instance
         */
        AccountRecoveryVerifyCodeResponse.create = function create(properties) {
            return new AccountRecoveryVerifyCodeResponse(properties);
        };

        /**
         * Encodes the specified AccountRecoveryVerifyCodeResponse message. Does not implicitly {@link Authentication.AccountRecoveryVerifyCodeResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.AccountRecoveryVerifyCodeResponse
         * @static
         * @param {Authentication.IAccountRecoveryVerifyCodeResponse} message AccountRecoveryVerifyCodeResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AccountRecoveryVerifyCodeResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.backupKeyType != null && Object.hasOwnProperty.call(message, "backupKeyType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.backupKeyType);
            if (message.backupKeyDate != null && Object.hasOwnProperty.call(message, "backupKeyDate"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.backupKeyDate);
            if (message.securityQuestion != null && Object.hasOwnProperty.call(message, "securityQuestion"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.securityQuestion);
            if (message.salt != null && Object.hasOwnProperty.call(message, "salt"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.salt);
            if (message.iterations != null && Object.hasOwnProperty.call(message, "iterations"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.iterations);
            return writer;
        };

        /**
         * Decodes an AccountRecoveryVerifyCodeResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.AccountRecoveryVerifyCodeResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.AccountRecoveryVerifyCodeResponse} AccountRecoveryVerifyCodeResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AccountRecoveryVerifyCodeResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.AccountRecoveryVerifyCodeResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.backupKeyType = reader.int32();
                        break;
                    }
                case 2: {
                        message.backupKeyDate = reader.int64();
                        break;
                    }
                case 3: {
                        message.securityQuestion = reader.string();
                        break;
                    }
                case 4: {
                        message.salt = reader.bytes();
                        break;
                    }
                case 5: {
                        message.iterations = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an AccountRecoveryVerifyCodeResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.AccountRecoveryVerifyCodeResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.AccountRecoveryVerifyCodeResponse} AccountRecoveryVerifyCodeResponse
         */
        AccountRecoveryVerifyCodeResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.AccountRecoveryVerifyCodeResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.AccountRecoveryVerifyCodeResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.AccountRecoveryVerifyCodeResponse();
            switch (object.backupKeyType) {
            default:
                if (typeof object.backupKeyType === "number") {
                    message.backupKeyType = object.backupKeyType;
                    break;
                }
                break;
            case "BKT_SEC_ANSWER":
            case 0:
                message.backupKeyType = 0;
                break;
            case "BKT_PASSPHRASE_HASH":
            case 1:
                message.backupKeyType = 1;
                break;
            }
            if (object.backupKeyDate != null)
                if ($util.Long)
                    message.backupKeyDate = $util.Long.fromValue(object.backupKeyDate, false);
                else if (typeof object.backupKeyDate === "string")
                    message.backupKeyDate = parseInt(object.backupKeyDate, 10);
                else if (typeof object.backupKeyDate === "number")
                    message.backupKeyDate = object.backupKeyDate;
                else if (typeof object.backupKeyDate === "object")
                    message.backupKeyDate = new $util.LongBits(object.backupKeyDate.low >>> 0, object.backupKeyDate.high >>> 0).toNumber();
            if (object.securityQuestion != null)
                message.securityQuestion = String(object.securityQuestion);
            if (object.salt != null)
                if (typeof object.salt === "string")
                    $util.base64.decode(object.salt, message.salt = $util.newBuffer($util.base64.length(object.salt)), 0);
                else if (object.salt.length >= 0)
                    message.salt = object.salt;
            if (object.iterations != null)
                message.iterations = object.iterations | 0;
            return message;
        };

        /**
         * Creates a plain object from an AccountRecoveryVerifyCodeResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.AccountRecoveryVerifyCodeResponse
         * @static
         * @param {Authentication.AccountRecoveryVerifyCodeResponse} message AccountRecoveryVerifyCodeResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AccountRecoveryVerifyCodeResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.backupKeyType = options.enums === String ? "BKT_SEC_ANSWER" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.backupKeyDate = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.backupKeyDate = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.securityQuestion = "";
                if (options.bytes === String)
                    object.salt = "";
                else {
                    object.salt = [];
                    if (options.bytes !== Array)
                        object.salt = $util.newBuffer(object.salt);
                }
                object.iterations = 0;
            }
            if (message.backupKeyType != null && Object.hasOwnProperty.call(message, "backupKeyType"))
                object.backupKeyType = options.enums === String ? $root.Authentication.BackupKeyType[message.backupKeyType] === undefined ? message.backupKeyType : $root.Authentication.BackupKeyType[message.backupKeyType] : message.backupKeyType;
            if (message.backupKeyDate != null && Object.hasOwnProperty.call(message, "backupKeyDate"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.backupKeyDate = typeof message.backupKeyDate === "number" ? BigInt(message.backupKeyDate) : $util.Long.fromBits(message.backupKeyDate.low >>> 0, message.backupKeyDate.high >>> 0, false).toBigInt();
                else if (typeof message.backupKeyDate === "number")
                    object.backupKeyDate = options.longs === String ? String(message.backupKeyDate) : message.backupKeyDate;
                else
                    object.backupKeyDate = options.longs === String ? $util.Long.prototype.toString.call(message.backupKeyDate) : options.longs === Number ? new $util.LongBits(message.backupKeyDate.low >>> 0, message.backupKeyDate.high >>> 0).toNumber() : message.backupKeyDate;
            if (message.securityQuestion != null && Object.hasOwnProperty.call(message, "securityQuestion"))
                object.securityQuestion = message.securityQuestion;
            if (message.salt != null && Object.hasOwnProperty.call(message, "salt"))
                object.salt = options.bytes === String ? $util.base64.encode(message.salt, 0, message.salt.length) : options.bytes === Array ? Array.prototype.slice.call(message.salt) : message.salt;
            if (message.iterations != null && Object.hasOwnProperty.call(message, "iterations"))
                object.iterations = message.iterations;
            return object;
        };

        /**
         * Converts this AccountRecoveryVerifyCodeResponse to JSON.
         * @function toJSON
         * @memberof Authentication.AccountRecoveryVerifyCodeResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AccountRecoveryVerifyCodeResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AccountRecoveryVerifyCodeResponse
         * @function getTypeUrl
         * @memberof Authentication.AccountRecoveryVerifyCodeResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AccountRecoveryVerifyCodeResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.AccountRecoveryVerifyCodeResponse";
        };

        return AccountRecoveryVerifyCodeResponse;
    })();

    Authentication.EmergencyAccessLoginRequest = (function() {

        /**
         * Properties of an EmergencyAccessLoginRequest.
         * @memberof Authentication
         * @interface IEmergencyAccessLoginRequest
         * @property {string|null} [owner] EmergencyAccessLoginRequest owner
         */

        /**
         * Constructs a new EmergencyAccessLoginRequest.
         * @memberof Authentication
         * @classdesc Represents an EmergencyAccessLoginRequest.
         * @implements IEmergencyAccessLoginRequest
         * @constructor
         * @param {Authentication.IEmergencyAccessLoginRequest=} [properties] Properties to set
         */
        function EmergencyAccessLoginRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EmergencyAccessLoginRequest owner.
         * @member {string} owner
         * @memberof Authentication.EmergencyAccessLoginRequest
         * @instance
         */
        EmergencyAccessLoginRequest.prototype.owner = "";

        /**
         * Creates a new EmergencyAccessLoginRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.EmergencyAccessLoginRequest
         * @static
         * @param {Authentication.IEmergencyAccessLoginRequest=} [properties] Properties to set
         * @returns {Authentication.EmergencyAccessLoginRequest} EmergencyAccessLoginRequest instance
         */
        EmergencyAccessLoginRequest.create = function create(properties) {
            return new EmergencyAccessLoginRequest(properties);
        };

        /**
         * Encodes the specified EmergencyAccessLoginRequest message. Does not implicitly {@link Authentication.EmergencyAccessLoginRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.EmergencyAccessLoginRequest
         * @static
         * @param {Authentication.IEmergencyAccessLoginRequest} message EmergencyAccessLoginRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EmergencyAccessLoginRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.owner != null && Object.hasOwnProperty.call(message, "owner"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.owner);
            return writer;
        };

        /**
         * Decodes an EmergencyAccessLoginRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.EmergencyAccessLoginRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.EmergencyAccessLoginRequest} EmergencyAccessLoginRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EmergencyAccessLoginRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.EmergencyAccessLoginRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.owner = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an EmergencyAccessLoginRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.EmergencyAccessLoginRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.EmergencyAccessLoginRequest} EmergencyAccessLoginRequest
         */
        EmergencyAccessLoginRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.EmergencyAccessLoginRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.EmergencyAccessLoginRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.EmergencyAccessLoginRequest();
            if (object.owner != null)
                message.owner = String(object.owner);
            return message;
        };

        /**
         * Creates a plain object from an EmergencyAccessLoginRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.EmergencyAccessLoginRequest
         * @static
         * @param {Authentication.EmergencyAccessLoginRequest} message EmergencyAccessLoginRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EmergencyAccessLoginRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.owner = "";
            if (message.owner != null && Object.hasOwnProperty.call(message, "owner"))
                object.owner = message.owner;
            return object;
        };

        /**
         * Converts this EmergencyAccessLoginRequest to JSON.
         * @function toJSON
         * @memberof Authentication.EmergencyAccessLoginRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EmergencyAccessLoginRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EmergencyAccessLoginRequest
         * @function getTypeUrl
         * @memberof Authentication.EmergencyAccessLoginRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EmergencyAccessLoginRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.EmergencyAccessLoginRequest";
        };

        return EmergencyAccessLoginRequest;
    })();

    Authentication.EmergencyAccessLoginResponse = (function() {

        /**
         * Properties of an EmergencyAccessLoginResponse.
         * @memberof Authentication
         * @interface IEmergencyAccessLoginResponse
         * @property {Uint8Array|null} [sessionToken] EmergencyAccessLoginResponse sessionToken
         * @property {Enterprise.ITypedKey|null} [dataKey] EmergencyAccessLoginResponse dataKey
         * @property {Enterprise.ITypedKey|null} [rsaPrivateKey] EmergencyAccessLoginResponse rsaPrivateKey
         * @property {Enterprise.ITypedKey|null} [eccPrivateKey] EmergencyAccessLoginResponse eccPrivateKey
         */

        /**
         * Constructs a new EmergencyAccessLoginResponse.
         * @memberof Authentication
         * @classdesc Represents an EmergencyAccessLoginResponse.
         * @implements IEmergencyAccessLoginResponse
         * @constructor
         * @param {Authentication.IEmergencyAccessLoginResponse=} [properties] Properties to set
         */
        function EmergencyAccessLoginResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EmergencyAccessLoginResponse sessionToken.
         * @member {Uint8Array} sessionToken
         * @memberof Authentication.EmergencyAccessLoginResponse
         * @instance
         */
        EmergencyAccessLoginResponse.prototype.sessionToken = $util.newBuffer([]);

        /**
         * EmergencyAccessLoginResponse dataKey.
         * @member {Enterprise.ITypedKey|null|undefined} dataKey
         * @memberof Authentication.EmergencyAccessLoginResponse
         * @instance
         */
        EmergencyAccessLoginResponse.prototype.dataKey = null;

        /**
         * EmergencyAccessLoginResponse rsaPrivateKey.
         * @member {Enterprise.ITypedKey|null|undefined} rsaPrivateKey
         * @memberof Authentication.EmergencyAccessLoginResponse
         * @instance
         */
        EmergencyAccessLoginResponse.prototype.rsaPrivateKey = null;

        /**
         * EmergencyAccessLoginResponse eccPrivateKey.
         * @member {Enterprise.ITypedKey|null|undefined} eccPrivateKey
         * @memberof Authentication.EmergencyAccessLoginResponse
         * @instance
         */
        EmergencyAccessLoginResponse.prototype.eccPrivateKey = null;

        /**
         * Creates a new EmergencyAccessLoginResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.EmergencyAccessLoginResponse
         * @static
         * @param {Authentication.IEmergencyAccessLoginResponse=} [properties] Properties to set
         * @returns {Authentication.EmergencyAccessLoginResponse} EmergencyAccessLoginResponse instance
         */
        EmergencyAccessLoginResponse.create = function create(properties) {
            return new EmergencyAccessLoginResponse(properties);
        };

        /**
         * Encodes the specified EmergencyAccessLoginResponse message. Does not implicitly {@link Authentication.EmergencyAccessLoginResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.EmergencyAccessLoginResponse
         * @static
         * @param {Authentication.IEmergencyAccessLoginResponse} message EmergencyAccessLoginResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EmergencyAccessLoginResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.sessionToken != null && Object.hasOwnProperty.call(message, "sessionToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.sessionToken);
            if (message.dataKey != null && Object.hasOwnProperty.call(message, "dataKey"))
                $root.Enterprise.TypedKey.encode(message.dataKey, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.rsaPrivateKey != null && Object.hasOwnProperty.call(message, "rsaPrivateKey"))
                $root.Enterprise.TypedKey.encode(message.rsaPrivateKey, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.eccPrivateKey != null && Object.hasOwnProperty.call(message, "eccPrivateKey"))
                $root.Enterprise.TypedKey.encode(message.eccPrivateKey, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes an EmergencyAccessLoginResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.EmergencyAccessLoginResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.EmergencyAccessLoginResponse} EmergencyAccessLoginResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EmergencyAccessLoginResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.EmergencyAccessLoginResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.sessionToken = reader.bytes();
                        break;
                    }
                case 2: {
                        message.dataKey = $root.Enterprise.TypedKey.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 3: {
                        message.rsaPrivateKey = $root.Enterprise.TypedKey.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 4: {
                        message.eccPrivateKey = $root.Enterprise.TypedKey.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an EmergencyAccessLoginResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.EmergencyAccessLoginResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.EmergencyAccessLoginResponse} EmergencyAccessLoginResponse
         */
        EmergencyAccessLoginResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.EmergencyAccessLoginResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.EmergencyAccessLoginResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.EmergencyAccessLoginResponse();
            if (object.sessionToken != null)
                if (typeof object.sessionToken === "string")
                    $util.base64.decode(object.sessionToken, message.sessionToken = $util.newBuffer($util.base64.length(object.sessionToken)), 0);
                else if (object.sessionToken.length >= 0)
                    message.sessionToken = object.sessionToken;
            if (object.dataKey != null) {
                if (!$util.isObject(object.dataKey))
                    throw TypeError(".Authentication.EmergencyAccessLoginResponse.dataKey: object expected");
                message.dataKey = $root.Enterprise.TypedKey.fromObject(object.dataKey, long + 1);
            }
            if (object.rsaPrivateKey != null) {
                if (!$util.isObject(object.rsaPrivateKey))
                    throw TypeError(".Authentication.EmergencyAccessLoginResponse.rsaPrivateKey: object expected");
                message.rsaPrivateKey = $root.Enterprise.TypedKey.fromObject(object.rsaPrivateKey, long + 1);
            }
            if (object.eccPrivateKey != null) {
                if (!$util.isObject(object.eccPrivateKey))
                    throw TypeError(".Authentication.EmergencyAccessLoginResponse.eccPrivateKey: object expected");
                message.eccPrivateKey = $root.Enterprise.TypedKey.fromObject(object.eccPrivateKey, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an EmergencyAccessLoginResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.EmergencyAccessLoginResponse
         * @static
         * @param {Authentication.EmergencyAccessLoginResponse} message EmergencyAccessLoginResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EmergencyAccessLoginResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.sessionToken = "";
                else {
                    object.sessionToken = [];
                    if (options.bytes !== Array)
                        object.sessionToken = $util.newBuffer(object.sessionToken);
                }
                object.dataKey = null;
                object.rsaPrivateKey = null;
                object.eccPrivateKey = null;
            }
            if (message.sessionToken != null && Object.hasOwnProperty.call(message, "sessionToken"))
                object.sessionToken = options.bytes === String ? $util.base64.encode(message.sessionToken, 0, message.sessionToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.sessionToken) : message.sessionToken;
            if (message.dataKey != null && Object.hasOwnProperty.call(message, "dataKey"))
                object.dataKey = $root.Enterprise.TypedKey.toObject(message.dataKey, options, q + 1);
            if (message.rsaPrivateKey != null && Object.hasOwnProperty.call(message, "rsaPrivateKey"))
                object.rsaPrivateKey = $root.Enterprise.TypedKey.toObject(message.rsaPrivateKey, options, q + 1);
            if (message.eccPrivateKey != null && Object.hasOwnProperty.call(message, "eccPrivateKey"))
                object.eccPrivateKey = $root.Enterprise.TypedKey.toObject(message.eccPrivateKey, options, q + 1);
            return object;
        };

        /**
         * Converts this EmergencyAccessLoginResponse to JSON.
         * @function toJSON
         * @memberof Authentication.EmergencyAccessLoginResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EmergencyAccessLoginResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EmergencyAccessLoginResponse
         * @function getTypeUrl
         * @memberof Authentication.EmergencyAccessLoginResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EmergencyAccessLoginResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.EmergencyAccessLoginResponse";
        };

        return EmergencyAccessLoginResponse;
    })();

    /**
     * GenericStatus enum.
     * @name Authentication.GenericStatus
     * @enum {number}
     * @property {number} SUCCESS=0 SUCCESS value
     * @property {number} INVALID_OBJECT=1 INVALID_OBJECT value
     * @property {number} ALREADY_EXISTS=2 ALREADY_EXISTS value
     * @property {number} ACCESS_DENIED=3 ACCESS_DENIED value
     * @property {number} LICENSE_SEAT_EXCEEDED=4 LICENSE_SEAT_EXCEEDED value
     */
    Authentication.GenericStatus = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "SUCCESS"] = 0;
        values[valuesById[1] = "INVALID_OBJECT"] = 1;
        values[valuesById[2] = "ALREADY_EXISTS"] = 2;
        values[valuesById[3] = "ACCESS_DENIED"] = 3;
        values[valuesById[4] = "LICENSE_SEAT_EXCEEDED"] = 4;
        return values;
    })();

    Authentication.UserTeamKey = (function() {

        /**
         * Properties of a UserTeamKey.
         * @memberof Authentication
         * @interface IUserTeamKey
         * @property {Uint8Array|null} [teamUid] UserTeamKey teamUid
         * @property {string|null} [username] UserTeamKey username
         * @property {number|null} [enterpriseUserId] UserTeamKey enterpriseUserId
         * @property {Uint8Array|null} [encryptedTeamKeyRSA] UserTeamKey encryptedTeamKeyRSA
         * @property {Uint8Array|null} [encryptedTeamKeyEC] UserTeamKey encryptedTeamKeyEC
         * @property {Authentication.GenericStatus|null} [status] UserTeamKey status
         */

        /**
         * Constructs a new UserTeamKey.
         * @memberof Authentication
         * @classdesc Represents a UserTeamKey.
         * @implements IUserTeamKey
         * @constructor
         * @param {Authentication.IUserTeamKey=} [properties] Properties to set
         */
        function UserTeamKey(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UserTeamKey teamUid.
         * @member {Uint8Array} teamUid
         * @memberof Authentication.UserTeamKey
         * @instance
         */
        UserTeamKey.prototype.teamUid = $util.newBuffer([]);

        /**
         * UserTeamKey username.
         * @member {string} username
         * @memberof Authentication.UserTeamKey
         * @instance
         */
        UserTeamKey.prototype.username = "";

        /**
         * UserTeamKey enterpriseUserId.
         * @member {number} enterpriseUserId
         * @memberof Authentication.UserTeamKey
         * @instance
         */
        UserTeamKey.prototype.enterpriseUserId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * UserTeamKey encryptedTeamKeyRSA.
         * @member {Uint8Array} encryptedTeamKeyRSA
         * @memberof Authentication.UserTeamKey
         * @instance
         */
        UserTeamKey.prototype.encryptedTeamKeyRSA = $util.newBuffer([]);

        /**
         * UserTeamKey encryptedTeamKeyEC.
         * @member {Uint8Array} encryptedTeamKeyEC
         * @memberof Authentication.UserTeamKey
         * @instance
         */
        UserTeamKey.prototype.encryptedTeamKeyEC = $util.newBuffer([]);

        /**
         * UserTeamKey status.
         * @member {Authentication.GenericStatus} status
         * @memberof Authentication.UserTeamKey
         * @instance
         */
        UserTeamKey.prototype.status = 0;

        /**
         * Creates a new UserTeamKey instance using the specified properties.
         * @function create
         * @memberof Authentication.UserTeamKey
         * @static
         * @param {Authentication.IUserTeamKey=} [properties] Properties to set
         * @returns {Authentication.UserTeamKey} UserTeamKey instance
         */
        UserTeamKey.create = function create(properties) {
            return new UserTeamKey(properties);
        };

        /**
         * Encodes the specified UserTeamKey message. Does not implicitly {@link Authentication.UserTeamKey.verify|verify} messages.
         * @function encode
         * @memberof Authentication.UserTeamKey
         * @static
         * @param {Authentication.IUserTeamKey} message UserTeamKey message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserTeamKey.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.teamUid != null && Object.hasOwnProperty.call(message, "teamUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.teamUid);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.username);
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.enterpriseUserId);
            if (message.encryptedTeamKeyRSA != null && Object.hasOwnProperty.call(message, "encryptedTeamKeyRSA"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.encryptedTeamKeyRSA);
            if (message.encryptedTeamKeyEC != null && Object.hasOwnProperty.call(message, "encryptedTeamKeyEC"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.encryptedTeamKeyEC);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.status);
            return writer;
        };

        /**
         * Decodes a UserTeamKey message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.UserTeamKey
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.UserTeamKey} UserTeamKey
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserTeamKey.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.UserTeamKey();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.teamUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.username = reader.string();
                        break;
                    }
                case 3: {
                        message.enterpriseUserId = reader.int64();
                        break;
                    }
                case 4: {
                        message.encryptedTeamKeyRSA = reader.bytes();
                        break;
                    }
                case 5: {
                        message.encryptedTeamKeyEC = reader.bytes();
                        break;
                    }
                case 6: {
                        message.status = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a UserTeamKey message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.UserTeamKey
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.UserTeamKey} UserTeamKey
         */
        UserTeamKey.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.UserTeamKey)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.UserTeamKey: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.UserTeamKey();
            if (object.teamUid != null)
                if (typeof object.teamUid === "string")
                    $util.base64.decode(object.teamUid, message.teamUid = $util.newBuffer($util.base64.length(object.teamUid)), 0);
                else if (object.teamUid.length >= 0)
                    message.teamUid = object.teamUid;
            if (object.username != null)
                message.username = String(object.username);
            if (object.enterpriseUserId != null)
                if ($util.Long)
                    message.enterpriseUserId = $util.Long.fromValue(object.enterpriseUserId, false);
                else if (typeof object.enterpriseUserId === "string")
                    message.enterpriseUserId = parseInt(object.enterpriseUserId, 10);
                else if (typeof object.enterpriseUserId === "number")
                    message.enterpriseUserId = object.enterpriseUserId;
                else if (typeof object.enterpriseUserId === "object")
                    message.enterpriseUserId = new $util.LongBits(object.enterpriseUserId.low >>> 0, object.enterpriseUserId.high >>> 0).toNumber();
            if (object.encryptedTeamKeyRSA != null)
                if (typeof object.encryptedTeamKeyRSA === "string")
                    $util.base64.decode(object.encryptedTeamKeyRSA, message.encryptedTeamKeyRSA = $util.newBuffer($util.base64.length(object.encryptedTeamKeyRSA)), 0);
                else if (object.encryptedTeamKeyRSA.length >= 0)
                    message.encryptedTeamKeyRSA = object.encryptedTeamKeyRSA;
            if (object.encryptedTeamKeyEC != null)
                if (typeof object.encryptedTeamKeyEC === "string")
                    $util.base64.decode(object.encryptedTeamKeyEC, message.encryptedTeamKeyEC = $util.newBuffer($util.base64.length(object.encryptedTeamKeyEC)), 0);
                else if (object.encryptedTeamKeyEC.length >= 0)
                    message.encryptedTeamKeyEC = object.encryptedTeamKeyEC;
            switch (object.status) {
            default:
                if (typeof object.status === "number") {
                    message.status = object.status;
                    break;
                }
                break;
            case "SUCCESS":
            case 0:
                message.status = 0;
                break;
            case "INVALID_OBJECT":
            case 1:
                message.status = 1;
                break;
            case "ALREADY_EXISTS":
            case 2:
                message.status = 2;
                break;
            case "ACCESS_DENIED":
            case 3:
                message.status = 3;
                break;
            case "LICENSE_SEAT_EXCEEDED":
            case 4:
                message.status = 4;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a UserTeamKey message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.UserTeamKey
         * @static
         * @param {Authentication.UserTeamKey} message UserTeamKey
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UserTeamKey.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.teamUid = "";
                else {
                    object.teamUid = [];
                    if (options.bytes !== Array)
                        object.teamUid = $util.newBuffer(object.teamUid);
                }
                object.username = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.enterpriseUserId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.enterpriseUserId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.encryptedTeamKeyRSA = "";
                else {
                    object.encryptedTeamKeyRSA = [];
                    if (options.bytes !== Array)
                        object.encryptedTeamKeyRSA = $util.newBuffer(object.encryptedTeamKeyRSA);
                }
                if (options.bytes === String)
                    object.encryptedTeamKeyEC = "";
                else {
                    object.encryptedTeamKeyEC = [];
                    if (options.bytes !== Array)
                        object.encryptedTeamKeyEC = $util.newBuffer(object.encryptedTeamKeyEC);
                }
                object.status = options.enums === String ? "SUCCESS" : 0;
            }
            if (message.teamUid != null && Object.hasOwnProperty.call(message, "teamUid"))
                object.teamUid = options.bytes === String ? $util.base64.encode(message.teamUid, 0, message.teamUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.teamUid) : message.teamUid;
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.enterpriseUserId = typeof message.enterpriseUserId === "number" ? BigInt(message.enterpriseUserId) : $util.Long.fromBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0, false).toBigInt();
                else if (typeof message.enterpriseUserId === "number")
                    object.enterpriseUserId = options.longs === String ? String(message.enterpriseUserId) : message.enterpriseUserId;
                else
                    object.enterpriseUserId = options.longs === String ? $util.Long.prototype.toString.call(message.enterpriseUserId) : options.longs === Number ? new $util.LongBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0).toNumber() : message.enterpriseUserId;
            if (message.encryptedTeamKeyRSA != null && Object.hasOwnProperty.call(message, "encryptedTeamKeyRSA"))
                object.encryptedTeamKeyRSA = options.bytes === String ? $util.base64.encode(message.encryptedTeamKeyRSA, 0, message.encryptedTeamKeyRSA.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedTeamKeyRSA) : message.encryptedTeamKeyRSA;
            if (message.encryptedTeamKeyEC != null && Object.hasOwnProperty.call(message, "encryptedTeamKeyEC"))
                object.encryptedTeamKeyEC = options.bytes === String ? $util.base64.encode(message.encryptedTeamKeyEC, 0, message.encryptedTeamKeyEC.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedTeamKeyEC) : message.encryptedTeamKeyEC;
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                object.status = options.enums === String ? $root.Authentication.GenericStatus[message.status] === undefined ? message.status : $root.Authentication.GenericStatus[message.status] : message.status;
            return object;
        };

        /**
         * Converts this UserTeamKey to JSON.
         * @function toJSON
         * @memberof Authentication.UserTeamKey
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UserTeamKey.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UserTeamKey
         * @function getTypeUrl
         * @memberof Authentication.UserTeamKey
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UserTeamKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.UserTeamKey";
        };

        return UserTeamKey;
    })();

    Authentication.GenericRequestResponse = (function() {

        /**
         * Properties of a GenericRequestResponse.
         * @memberof Authentication
         * @interface IGenericRequestResponse
         * @property {Array.<Uint8Array>|null} [request] GenericRequestResponse request
         */

        /**
         * Constructs a new GenericRequestResponse.
         * @memberof Authentication
         * @classdesc Represents a GenericRequestResponse.
         * @implements IGenericRequestResponse
         * @constructor
         * @param {Authentication.IGenericRequestResponse=} [properties] Properties to set
         */
        function GenericRequestResponse(properties) {
            this.request = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GenericRequestResponse request.
         * @member {Array.<Uint8Array>} request
         * @memberof Authentication.GenericRequestResponse
         * @instance
         */
        GenericRequestResponse.prototype.request = $util.emptyArray;

        /**
         * Creates a new GenericRequestResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.GenericRequestResponse
         * @static
         * @param {Authentication.IGenericRequestResponse=} [properties] Properties to set
         * @returns {Authentication.GenericRequestResponse} GenericRequestResponse instance
         */
        GenericRequestResponse.create = function create(properties) {
            return new GenericRequestResponse(properties);
        };

        /**
         * Encodes the specified GenericRequestResponse message. Does not implicitly {@link Authentication.GenericRequestResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.GenericRequestResponse
         * @static
         * @param {Authentication.IGenericRequestResponse} message GenericRequestResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GenericRequestResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.request != null && message.request.length)
                for (let i = 0; i < message.request.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.request[i]);
            return writer;
        };

        /**
         * Decodes a GenericRequestResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.GenericRequestResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.GenericRequestResponse} GenericRequestResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GenericRequestResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.GenericRequestResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.request && message.request.length))
                            message.request = [];
                        message.request.push(reader.bytes());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a GenericRequestResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.GenericRequestResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.GenericRequestResponse} GenericRequestResponse
         */
        GenericRequestResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.GenericRequestResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.GenericRequestResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.GenericRequestResponse();
            if (object.request) {
                if (!Array.isArray(object.request))
                    throw TypeError(".Authentication.GenericRequestResponse.request: array expected");
                message.request = [];
                for (let i = 0; i < object.request.length; ++i)
                    if (typeof object.request[i] === "string")
                        $util.base64.decode(object.request[i], message.request[i] = $util.newBuffer($util.base64.length(object.request[i])), 0);
                    else if (object.request[i].length >= 0)
                        message.request[i] = object.request[i];
            }
            return message;
        };

        /**
         * Creates a plain object from a GenericRequestResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.GenericRequestResponse
         * @static
         * @param {Authentication.GenericRequestResponse} message GenericRequestResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GenericRequestResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.request = [];
            if (message.request && message.request.length) {
                object.request = [];
                for (let j = 0; j < message.request.length; ++j)
                    object.request[j] = options.bytes === String ? $util.base64.encode(message.request[j], 0, message.request[j].length) : options.bytes === Array ? Array.prototype.slice.call(message.request[j]) : message.request[j];
            }
            return object;
        };

        /**
         * Converts this GenericRequestResponse to JSON.
         * @function toJSON
         * @memberof Authentication.GenericRequestResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GenericRequestResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GenericRequestResponse
         * @function getTypeUrl
         * @memberof Authentication.GenericRequestResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GenericRequestResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.GenericRequestResponse";
        };

        return GenericRequestResponse;
    })();

    /**
     * AuthenticatorAttachment enum.
     * @name Authentication.AuthenticatorAttachment
     * @enum {number}
     * @property {number} CROSS_PLATFORM=0 CROSS_PLATFORM value
     * @property {number} PLATFORM=1 PLATFORM value
     * @property {number} ALL_SUPPORTED=2 ALL_SUPPORTED value
     */
    Authentication.AuthenticatorAttachment = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "CROSS_PLATFORM"] = 0;
        values[valuesById[1] = "PLATFORM"] = 1;
        values[valuesById[2] = "ALL_SUPPORTED"] = 2;
        return values;
    })();

    Authentication.PasskeyRegistrationRequest = (function() {

        /**
         * Properties of a PasskeyRegistrationRequest.
         * @memberof Authentication
         * @interface IPasskeyRegistrationRequest
         * @property {Authentication.AuthenticatorAttachment|null} [authenticatorAttachment] PasskeyRegistrationRequest authenticatorAttachment
         */

        /**
         * Constructs a new PasskeyRegistrationRequest.
         * @memberof Authentication
         * @classdesc Represents a PasskeyRegistrationRequest.
         * @implements IPasskeyRegistrationRequest
         * @constructor
         * @param {Authentication.IPasskeyRegistrationRequest=} [properties] Properties to set
         */
        function PasskeyRegistrationRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PasskeyRegistrationRequest authenticatorAttachment.
         * @member {Authentication.AuthenticatorAttachment} authenticatorAttachment
         * @memberof Authentication.PasskeyRegistrationRequest
         * @instance
         */
        PasskeyRegistrationRequest.prototype.authenticatorAttachment = 0;

        /**
         * Creates a new PasskeyRegistrationRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.PasskeyRegistrationRequest
         * @static
         * @param {Authentication.IPasskeyRegistrationRequest=} [properties] Properties to set
         * @returns {Authentication.PasskeyRegistrationRequest} PasskeyRegistrationRequest instance
         */
        PasskeyRegistrationRequest.create = function create(properties) {
            return new PasskeyRegistrationRequest(properties);
        };

        /**
         * Encodes the specified PasskeyRegistrationRequest message. Does not implicitly {@link Authentication.PasskeyRegistrationRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.PasskeyRegistrationRequest
         * @static
         * @param {Authentication.IPasskeyRegistrationRequest} message PasskeyRegistrationRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PasskeyRegistrationRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.authenticatorAttachment != null && Object.hasOwnProperty.call(message, "authenticatorAttachment"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.authenticatorAttachment);
            return writer;
        };

        /**
         * Decodes a PasskeyRegistrationRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.PasskeyRegistrationRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.PasskeyRegistrationRequest} PasskeyRegistrationRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PasskeyRegistrationRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.PasskeyRegistrationRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.authenticatorAttachment = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a PasskeyRegistrationRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.PasskeyRegistrationRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.PasskeyRegistrationRequest} PasskeyRegistrationRequest
         */
        PasskeyRegistrationRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.PasskeyRegistrationRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.PasskeyRegistrationRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.PasskeyRegistrationRequest();
            switch (object.authenticatorAttachment) {
            default:
                if (typeof object.authenticatorAttachment === "number") {
                    message.authenticatorAttachment = object.authenticatorAttachment;
                    break;
                }
                break;
            case "CROSS_PLATFORM":
            case 0:
                message.authenticatorAttachment = 0;
                break;
            case "PLATFORM":
            case 1:
                message.authenticatorAttachment = 1;
                break;
            case "ALL_SUPPORTED":
            case 2:
                message.authenticatorAttachment = 2;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a PasskeyRegistrationRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.PasskeyRegistrationRequest
         * @static
         * @param {Authentication.PasskeyRegistrationRequest} message PasskeyRegistrationRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PasskeyRegistrationRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.authenticatorAttachment = options.enums === String ? "CROSS_PLATFORM" : 0;
            if (message.authenticatorAttachment != null && Object.hasOwnProperty.call(message, "authenticatorAttachment"))
                object.authenticatorAttachment = options.enums === String ? $root.Authentication.AuthenticatorAttachment[message.authenticatorAttachment] === undefined ? message.authenticatorAttachment : $root.Authentication.AuthenticatorAttachment[message.authenticatorAttachment] : message.authenticatorAttachment;
            return object;
        };

        /**
         * Converts this PasskeyRegistrationRequest to JSON.
         * @function toJSON
         * @memberof Authentication.PasskeyRegistrationRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PasskeyRegistrationRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PasskeyRegistrationRequest
         * @function getTypeUrl
         * @memberof Authentication.PasskeyRegistrationRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PasskeyRegistrationRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.PasskeyRegistrationRequest";
        };

        return PasskeyRegistrationRequest;
    })();

    Authentication.PasskeyRegistrationResponse = (function() {

        /**
         * Properties of a PasskeyRegistrationResponse.
         * @memberof Authentication
         * @interface IPasskeyRegistrationResponse
         * @property {Uint8Array|null} [challengeToken] PasskeyRegistrationResponse challengeToken
         * @property {string|null} [pkCreationOptions] PasskeyRegistrationResponse pkCreationOptions
         */

        /**
         * Constructs a new PasskeyRegistrationResponse.
         * @memberof Authentication
         * @classdesc Represents a PasskeyRegistrationResponse.
         * @implements IPasskeyRegistrationResponse
         * @constructor
         * @param {Authentication.IPasskeyRegistrationResponse=} [properties] Properties to set
         */
        function PasskeyRegistrationResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PasskeyRegistrationResponse challengeToken.
         * @member {Uint8Array} challengeToken
         * @memberof Authentication.PasskeyRegistrationResponse
         * @instance
         */
        PasskeyRegistrationResponse.prototype.challengeToken = $util.newBuffer([]);

        /**
         * PasskeyRegistrationResponse pkCreationOptions.
         * @member {string} pkCreationOptions
         * @memberof Authentication.PasskeyRegistrationResponse
         * @instance
         */
        PasskeyRegistrationResponse.prototype.pkCreationOptions = "";

        /**
         * Creates a new PasskeyRegistrationResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.PasskeyRegistrationResponse
         * @static
         * @param {Authentication.IPasskeyRegistrationResponse=} [properties] Properties to set
         * @returns {Authentication.PasskeyRegistrationResponse} PasskeyRegistrationResponse instance
         */
        PasskeyRegistrationResponse.create = function create(properties) {
            return new PasskeyRegistrationResponse(properties);
        };

        /**
         * Encodes the specified PasskeyRegistrationResponse message. Does not implicitly {@link Authentication.PasskeyRegistrationResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.PasskeyRegistrationResponse
         * @static
         * @param {Authentication.IPasskeyRegistrationResponse} message PasskeyRegistrationResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PasskeyRegistrationResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.challengeToken != null && Object.hasOwnProperty.call(message, "challengeToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.challengeToken);
            if (message.pkCreationOptions != null && Object.hasOwnProperty.call(message, "pkCreationOptions"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.pkCreationOptions);
            return writer;
        };

        /**
         * Decodes a PasskeyRegistrationResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.PasskeyRegistrationResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.PasskeyRegistrationResponse} PasskeyRegistrationResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PasskeyRegistrationResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.PasskeyRegistrationResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.challengeToken = reader.bytes();
                        break;
                    }
                case 2: {
                        message.pkCreationOptions = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a PasskeyRegistrationResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.PasskeyRegistrationResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.PasskeyRegistrationResponse} PasskeyRegistrationResponse
         */
        PasskeyRegistrationResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.PasskeyRegistrationResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.PasskeyRegistrationResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.PasskeyRegistrationResponse();
            if (object.challengeToken != null)
                if (typeof object.challengeToken === "string")
                    $util.base64.decode(object.challengeToken, message.challengeToken = $util.newBuffer($util.base64.length(object.challengeToken)), 0);
                else if (object.challengeToken.length >= 0)
                    message.challengeToken = object.challengeToken;
            if (object.pkCreationOptions != null)
                message.pkCreationOptions = String(object.pkCreationOptions);
            return message;
        };

        /**
         * Creates a plain object from a PasskeyRegistrationResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.PasskeyRegistrationResponse
         * @static
         * @param {Authentication.PasskeyRegistrationResponse} message PasskeyRegistrationResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PasskeyRegistrationResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.challengeToken = "";
                else {
                    object.challengeToken = [];
                    if (options.bytes !== Array)
                        object.challengeToken = $util.newBuffer(object.challengeToken);
                }
                object.pkCreationOptions = "";
            }
            if (message.challengeToken != null && Object.hasOwnProperty.call(message, "challengeToken"))
                object.challengeToken = options.bytes === String ? $util.base64.encode(message.challengeToken, 0, message.challengeToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.challengeToken) : message.challengeToken;
            if (message.pkCreationOptions != null && Object.hasOwnProperty.call(message, "pkCreationOptions"))
                object.pkCreationOptions = message.pkCreationOptions;
            return object;
        };

        /**
         * Converts this PasskeyRegistrationResponse to JSON.
         * @function toJSON
         * @memberof Authentication.PasskeyRegistrationResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PasskeyRegistrationResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PasskeyRegistrationResponse
         * @function getTypeUrl
         * @memberof Authentication.PasskeyRegistrationResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PasskeyRegistrationResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.PasskeyRegistrationResponse";
        };

        return PasskeyRegistrationResponse;
    })();

    Authentication.PasskeyRegistrationFinalization = (function() {

        /**
         * Properties of a PasskeyRegistrationFinalization.
         * @memberof Authentication
         * @interface IPasskeyRegistrationFinalization
         * @property {Uint8Array|null} [challengeToken] PasskeyRegistrationFinalization challengeToken
         * @property {string|null} [authenticatorResponse] PasskeyRegistrationFinalization authenticatorResponse
         * @property {string|null} [friendlyName] PasskeyRegistrationFinalization friendlyName
         */

        /**
         * Constructs a new PasskeyRegistrationFinalization.
         * @memberof Authentication
         * @classdesc Represents a PasskeyRegistrationFinalization.
         * @implements IPasskeyRegistrationFinalization
         * @constructor
         * @param {Authentication.IPasskeyRegistrationFinalization=} [properties] Properties to set
         */
        function PasskeyRegistrationFinalization(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PasskeyRegistrationFinalization challengeToken.
         * @member {Uint8Array} challengeToken
         * @memberof Authentication.PasskeyRegistrationFinalization
         * @instance
         */
        PasskeyRegistrationFinalization.prototype.challengeToken = $util.newBuffer([]);

        /**
         * PasskeyRegistrationFinalization authenticatorResponse.
         * @member {string} authenticatorResponse
         * @memberof Authentication.PasskeyRegistrationFinalization
         * @instance
         */
        PasskeyRegistrationFinalization.prototype.authenticatorResponse = "";

        /**
         * PasskeyRegistrationFinalization friendlyName.
         * @member {string|null|undefined} friendlyName
         * @memberof Authentication.PasskeyRegistrationFinalization
         * @instance
         */
        PasskeyRegistrationFinalization.prototype.friendlyName = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PasskeyRegistrationFinalization.prototype, "_friendlyName", {
            get: $util.oneOfGetter($oneOfFields = ["friendlyName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PasskeyRegistrationFinalization instance using the specified properties.
         * @function create
         * @memberof Authentication.PasskeyRegistrationFinalization
         * @static
         * @param {Authentication.IPasskeyRegistrationFinalization=} [properties] Properties to set
         * @returns {Authentication.PasskeyRegistrationFinalization} PasskeyRegistrationFinalization instance
         */
        PasskeyRegistrationFinalization.create = function create(properties) {
            return new PasskeyRegistrationFinalization(properties);
        };

        /**
         * Encodes the specified PasskeyRegistrationFinalization message. Does not implicitly {@link Authentication.PasskeyRegistrationFinalization.verify|verify} messages.
         * @function encode
         * @memberof Authentication.PasskeyRegistrationFinalization
         * @static
         * @param {Authentication.IPasskeyRegistrationFinalization} message PasskeyRegistrationFinalization message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PasskeyRegistrationFinalization.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.challengeToken != null && Object.hasOwnProperty.call(message, "challengeToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.challengeToken);
            if (message.authenticatorResponse != null && Object.hasOwnProperty.call(message, "authenticatorResponse"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.authenticatorResponse);
            if (message.friendlyName != null && Object.hasOwnProperty.call(message, "friendlyName"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.friendlyName);
            return writer;
        };

        /**
         * Decodes a PasskeyRegistrationFinalization message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.PasskeyRegistrationFinalization
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.PasskeyRegistrationFinalization} PasskeyRegistrationFinalization
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PasskeyRegistrationFinalization.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.PasskeyRegistrationFinalization();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.challengeToken = reader.bytes();
                        break;
                    }
                case 2: {
                        message.authenticatorResponse = reader.string();
                        break;
                    }
                case 3: {
                        message.friendlyName = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a PasskeyRegistrationFinalization message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.PasskeyRegistrationFinalization
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.PasskeyRegistrationFinalization} PasskeyRegistrationFinalization
         */
        PasskeyRegistrationFinalization.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.PasskeyRegistrationFinalization)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.PasskeyRegistrationFinalization: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.PasskeyRegistrationFinalization();
            if (object.challengeToken != null)
                if (typeof object.challengeToken === "string")
                    $util.base64.decode(object.challengeToken, message.challengeToken = $util.newBuffer($util.base64.length(object.challengeToken)), 0);
                else if (object.challengeToken.length >= 0)
                    message.challengeToken = object.challengeToken;
            if (object.authenticatorResponse != null)
                message.authenticatorResponse = String(object.authenticatorResponse);
            if (object.friendlyName != null)
                message.friendlyName = String(object.friendlyName);
            return message;
        };

        /**
         * Creates a plain object from a PasskeyRegistrationFinalization message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.PasskeyRegistrationFinalization
         * @static
         * @param {Authentication.PasskeyRegistrationFinalization} message PasskeyRegistrationFinalization
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PasskeyRegistrationFinalization.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.challengeToken = "";
                else {
                    object.challengeToken = [];
                    if (options.bytes !== Array)
                        object.challengeToken = $util.newBuffer(object.challengeToken);
                }
                object.authenticatorResponse = "";
            }
            if (message.challengeToken != null && Object.hasOwnProperty.call(message, "challengeToken"))
                object.challengeToken = options.bytes === String ? $util.base64.encode(message.challengeToken, 0, message.challengeToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.challengeToken) : message.challengeToken;
            if (message.authenticatorResponse != null && Object.hasOwnProperty.call(message, "authenticatorResponse"))
                object.authenticatorResponse = message.authenticatorResponse;
            if (message.friendlyName != null && Object.hasOwnProperty.call(message, "friendlyName")) {
                object.friendlyName = message.friendlyName;
                if (options.oneofs)
                    object._friendlyName = "friendlyName";
            }
            return object;
        };

        /**
         * Converts this PasskeyRegistrationFinalization to JSON.
         * @function toJSON
         * @memberof Authentication.PasskeyRegistrationFinalization
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PasskeyRegistrationFinalization.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PasskeyRegistrationFinalization
         * @function getTypeUrl
         * @memberof Authentication.PasskeyRegistrationFinalization
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PasskeyRegistrationFinalization.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.PasskeyRegistrationFinalization";
        };

        return PasskeyRegistrationFinalization;
    })();

    /**
     * PasskeyPurpose enum.
     * @name Authentication.PasskeyPurpose
     * @enum {number}
     * @property {number} PK_LOGIN=0 PK_LOGIN value
     * @property {number} PK_REAUTH=1 PK_REAUTH value
     */
    Authentication.PasskeyPurpose = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "PK_LOGIN"] = 0;
        values[valuesById[1] = "PK_REAUTH"] = 1;
        return values;
    })();

    Authentication.PasskeyAuthenticationRequest = (function() {

        /**
         * Properties of a PasskeyAuthenticationRequest.
         * @memberof Authentication
         * @interface IPasskeyAuthenticationRequest
         * @property {Authentication.AuthenticatorAttachment|null} [authenticatorAttachment] PasskeyAuthenticationRequest authenticatorAttachment
         * @property {Authentication.PasskeyPurpose|null} [passkeyPurpose] PasskeyAuthenticationRequest passkeyPurpose
         * @property {string|null} [clientVersion] PasskeyAuthenticationRequest clientVersion
         * @property {Uint8Array|null} [encryptedDeviceToken] PasskeyAuthenticationRequest encryptedDeviceToken
         * @property {string|null} [username] PasskeyAuthenticationRequest username
         * @property {Uint8Array|null} [encryptedLoginToken] PasskeyAuthenticationRequest encryptedLoginToken
         */

        /**
         * Constructs a new PasskeyAuthenticationRequest.
         * @memberof Authentication
         * @classdesc Represents a PasskeyAuthenticationRequest.
         * @implements IPasskeyAuthenticationRequest
         * @constructor
         * @param {Authentication.IPasskeyAuthenticationRequest=} [properties] Properties to set
         */
        function PasskeyAuthenticationRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PasskeyAuthenticationRequest authenticatorAttachment.
         * @member {Authentication.AuthenticatorAttachment} authenticatorAttachment
         * @memberof Authentication.PasskeyAuthenticationRequest
         * @instance
         */
        PasskeyAuthenticationRequest.prototype.authenticatorAttachment = 0;

        /**
         * PasskeyAuthenticationRequest passkeyPurpose.
         * @member {Authentication.PasskeyPurpose} passkeyPurpose
         * @memberof Authentication.PasskeyAuthenticationRequest
         * @instance
         */
        PasskeyAuthenticationRequest.prototype.passkeyPurpose = 0;

        /**
         * PasskeyAuthenticationRequest clientVersion.
         * @member {string} clientVersion
         * @memberof Authentication.PasskeyAuthenticationRequest
         * @instance
         */
        PasskeyAuthenticationRequest.prototype.clientVersion = "";

        /**
         * PasskeyAuthenticationRequest encryptedDeviceToken.
         * @member {Uint8Array} encryptedDeviceToken
         * @memberof Authentication.PasskeyAuthenticationRequest
         * @instance
         */
        PasskeyAuthenticationRequest.prototype.encryptedDeviceToken = $util.newBuffer([]);

        /**
         * PasskeyAuthenticationRequest username.
         * @member {string|null|undefined} username
         * @memberof Authentication.PasskeyAuthenticationRequest
         * @instance
         */
        PasskeyAuthenticationRequest.prototype.username = null;

        /**
         * PasskeyAuthenticationRequest encryptedLoginToken.
         * @member {Uint8Array|null|undefined} encryptedLoginToken
         * @memberof Authentication.PasskeyAuthenticationRequest
         * @instance
         */
        PasskeyAuthenticationRequest.prototype.encryptedLoginToken = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PasskeyAuthenticationRequest.prototype, "_username", {
            get: $util.oneOfGetter($oneOfFields = ["username"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PasskeyAuthenticationRequest.prototype, "_encryptedLoginToken", {
            get: $util.oneOfGetter($oneOfFields = ["encryptedLoginToken"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PasskeyAuthenticationRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.PasskeyAuthenticationRequest
         * @static
         * @param {Authentication.IPasskeyAuthenticationRequest=} [properties] Properties to set
         * @returns {Authentication.PasskeyAuthenticationRequest} PasskeyAuthenticationRequest instance
         */
        PasskeyAuthenticationRequest.create = function create(properties) {
            return new PasskeyAuthenticationRequest(properties);
        };

        /**
         * Encodes the specified PasskeyAuthenticationRequest message. Does not implicitly {@link Authentication.PasskeyAuthenticationRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.PasskeyAuthenticationRequest
         * @static
         * @param {Authentication.IPasskeyAuthenticationRequest} message PasskeyAuthenticationRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PasskeyAuthenticationRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.authenticatorAttachment != null && Object.hasOwnProperty.call(message, "authenticatorAttachment"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.authenticatorAttachment);
            if (message.passkeyPurpose != null && Object.hasOwnProperty.call(message, "passkeyPurpose"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.passkeyPurpose);
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.clientVersion);
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.encryptedDeviceToken);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.username);
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.encryptedLoginToken);
            return writer;
        };

        /**
         * Decodes a PasskeyAuthenticationRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.PasskeyAuthenticationRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.PasskeyAuthenticationRequest} PasskeyAuthenticationRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PasskeyAuthenticationRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.PasskeyAuthenticationRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.authenticatorAttachment = reader.int32();
                        break;
                    }
                case 2: {
                        message.passkeyPurpose = reader.int32();
                        break;
                    }
                case 3: {
                        message.clientVersion = reader.string();
                        break;
                    }
                case 4: {
                        message.encryptedDeviceToken = reader.bytes();
                        break;
                    }
                case 5: {
                        message.username = reader.string();
                        break;
                    }
                case 6: {
                        message.encryptedLoginToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a PasskeyAuthenticationRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.PasskeyAuthenticationRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.PasskeyAuthenticationRequest} PasskeyAuthenticationRequest
         */
        PasskeyAuthenticationRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.PasskeyAuthenticationRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.PasskeyAuthenticationRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.PasskeyAuthenticationRequest();
            switch (object.authenticatorAttachment) {
            default:
                if (typeof object.authenticatorAttachment === "number") {
                    message.authenticatorAttachment = object.authenticatorAttachment;
                    break;
                }
                break;
            case "CROSS_PLATFORM":
            case 0:
                message.authenticatorAttachment = 0;
                break;
            case "PLATFORM":
            case 1:
                message.authenticatorAttachment = 1;
                break;
            case "ALL_SUPPORTED":
            case 2:
                message.authenticatorAttachment = 2;
                break;
            }
            switch (object.passkeyPurpose) {
            default:
                if (typeof object.passkeyPurpose === "number") {
                    message.passkeyPurpose = object.passkeyPurpose;
                    break;
                }
                break;
            case "PK_LOGIN":
            case 0:
                message.passkeyPurpose = 0;
                break;
            case "PK_REAUTH":
            case 1:
                message.passkeyPurpose = 1;
                break;
            }
            if (object.clientVersion != null)
                message.clientVersion = String(object.clientVersion);
            if (object.encryptedDeviceToken != null)
                if (typeof object.encryptedDeviceToken === "string")
                    $util.base64.decode(object.encryptedDeviceToken, message.encryptedDeviceToken = $util.newBuffer($util.base64.length(object.encryptedDeviceToken)), 0);
                else if (object.encryptedDeviceToken.length >= 0)
                    message.encryptedDeviceToken = object.encryptedDeviceToken;
            if (object.username != null)
                message.username = String(object.username);
            if (object.encryptedLoginToken != null)
                if (typeof object.encryptedLoginToken === "string")
                    $util.base64.decode(object.encryptedLoginToken, message.encryptedLoginToken = $util.newBuffer($util.base64.length(object.encryptedLoginToken)), 0);
                else if (object.encryptedLoginToken.length >= 0)
                    message.encryptedLoginToken = object.encryptedLoginToken;
            return message;
        };

        /**
         * Creates a plain object from a PasskeyAuthenticationRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.PasskeyAuthenticationRequest
         * @static
         * @param {Authentication.PasskeyAuthenticationRequest} message PasskeyAuthenticationRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PasskeyAuthenticationRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.authenticatorAttachment = options.enums === String ? "CROSS_PLATFORM" : 0;
                object.passkeyPurpose = options.enums === String ? "PK_LOGIN" : 0;
                object.clientVersion = "";
                if (options.bytes === String)
                    object.encryptedDeviceToken = "";
                else {
                    object.encryptedDeviceToken = [];
                    if (options.bytes !== Array)
                        object.encryptedDeviceToken = $util.newBuffer(object.encryptedDeviceToken);
                }
            }
            if (message.authenticatorAttachment != null && Object.hasOwnProperty.call(message, "authenticatorAttachment"))
                object.authenticatorAttachment = options.enums === String ? $root.Authentication.AuthenticatorAttachment[message.authenticatorAttachment] === undefined ? message.authenticatorAttachment : $root.Authentication.AuthenticatorAttachment[message.authenticatorAttachment] : message.authenticatorAttachment;
            if (message.passkeyPurpose != null && Object.hasOwnProperty.call(message, "passkeyPurpose"))
                object.passkeyPurpose = options.enums === String ? $root.Authentication.PasskeyPurpose[message.passkeyPurpose] === undefined ? message.passkeyPurpose : $root.Authentication.PasskeyPurpose[message.passkeyPurpose] : message.passkeyPurpose;
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                object.clientVersion = message.clientVersion;
            if (message.encryptedDeviceToken != null && Object.hasOwnProperty.call(message, "encryptedDeviceToken"))
                object.encryptedDeviceToken = options.bytes === String ? $util.base64.encode(message.encryptedDeviceToken, 0, message.encryptedDeviceToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedDeviceToken) : message.encryptedDeviceToken;
            if (message.username != null && Object.hasOwnProperty.call(message, "username")) {
                object.username = message.username;
                if (options.oneofs)
                    object._username = "username";
            }
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken")) {
                object.encryptedLoginToken = options.bytes === String ? $util.base64.encode(message.encryptedLoginToken, 0, message.encryptedLoginToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedLoginToken) : message.encryptedLoginToken;
                if (options.oneofs)
                    object._encryptedLoginToken = "encryptedLoginToken";
            }
            return object;
        };

        /**
         * Converts this PasskeyAuthenticationRequest to JSON.
         * @function toJSON
         * @memberof Authentication.PasskeyAuthenticationRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PasskeyAuthenticationRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PasskeyAuthenticationRequest
         * @function getTypeUrl
         * @memberof Authentication.PasskeyAuthenticationRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PasskeyAuthenticationRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.PasskeyAuthenticationRequest";
        };

        return PasskeyAuthenticationRequest;
    })();

    Authentication.PasskeyAuthenticationResponse = (function() {

        /**
         * Properties of a PasskeyAuthenticationResponse.
         * @memberof Authentication
         * @interface IPasskeyAuthenticationResponse
         * @property {string|null} [pkRequestOptions] PasskeyAuthenticationResponse pkRequestOptions
         * @property {Uint8Array|null} [challengeToken] PasskeyAuthenticationResponse challengeToken
         * @property {Uint8Array|null} [encryptedLoginToken] PasskeyAuthenticationResponse encryptedLoginToken
         */

        /**
         * Constructs a new PasskeyAuthenticationResponse.
         * @memberof Authentication
         * @classdesc Represents a PasskeyAuthenticationResponse.
         * @implements IPasskeyAuthenticationResponse
         * @constructor
         * @param {Authentication.IPasskeyAuthenticationResponse=} [properties] Properties to set
         */
        function PasskeyAuthenticationResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PasskeyAuthenticationResponse pkRequestOptions.
         * @member {string} pkRequestOptions
         * @memberof Authentication.PasskeyAuthenticationResponse
         * @instance
         */
        PasskeyAuthenticationResponse.prototype.pkRequestOptions = "";

        /**
         * PasskeyAuthenticationResponse challengeToken.
         * @member {Uint8Array} challengeToken
         * @memberof Authentication.PasskeyAuthenticationResponse
         * @instance
         */
        PasskeyAuthenticationResponse.prototype.challengeToken = $util.newBuffer([]);

        /**
         * PasskeyAuthenticationResponse encryptedLoginToken.
         * @member {Uint8Array|null|undefined} encryptedLoginToken
         * @memberof Authentication.PasskeyAuthenticationResponse
         * @instance
         */
        PasskeyAuthenticationResponse.prototype.encryptedLoginToken = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PasskeyAuthenticationResponse.prototype, "_encryptedLoginToken", {
            get: $util.oneOfGetter($oneOfFields = ["encryptedLoginToken"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PasskeyAuthenticationResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.PasskeyAuthenticationResponse
         * @static
         * @param {Authentication.IPasskeyAuthenticationResponse=} [properties] Properties to set
         * @returns {Authentication.PasskeyAuthenticationResponse} PasskeyAuthenticationResponse instance
         */
        PasskeyAuthenticationResponse.create = function create(properties) {
            return new PasskeyAuthenticationResponse(properties);
        };

        /**
         * Encodes the specified PasskeyAuthenticationResponse message. Does not implicitly {@link Authentication.PasskeyAuthenticationResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.PasskeyAuthenticationResponse
         * @static
         * @param {Authentication.IPasskeyAuthenticationResponse} message PasskeyAuthenticationResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PasskeyAuthenticationResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.pkRequestOptions != null && Object.hasOwnProperty.call(message, "pkRequestOptions"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.pkRequestOptions);
            if (message.challengeToken != null && Object.hasOwnProperty.call(message, "challengeToken"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.challengeToken);
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.encryptedLoginToken);
            return writer;
        };

        /**
         * Decodes a PasskeyAuthenticationResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.PasskeyAuthenticationResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.PasskeyAuthenticationResponse} PasskeyAuthenticationResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PasskeyAuthenticationResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.PasskeyAuthenticationResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.pkRequestOptions = reader.string();
                        break;
                    }
                case 2: {
                        message.challengeToken = reader.bytes();
                        break;
                    }
                case 3: {
                        message.encryptedLoginToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a PasskeyAuthenticationResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.PasskeyAuthenticationResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.PasskeyAuthenticationResponse} PasskeyAuthenticationResponse
         */
        PasskeyAuthenticationResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.PasskeyAuthenticationResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.PasskeyAuthenticationResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.PasskeyAuthenticationResponse();
            if (object.pkRequestOptions != null)
                message.pkRequestOptions = String(object.pkRequestOptions);
            if (object.challengeToken != null)
                if (typeof object.challengeToken === "string")
                    $util.base64.decode(object.challengeToken, message.challengeToken = $util.newBuffer($util.base64.length(object.challengeToken)), 0);
                else if (object.challengeToken.length >= 0)
                    message.challengeToken = object.challengeToken;
            if (object.encryptedLoginToken != null)
                if (typeof object.encryptedLoginToken === "string")
                    $util.base64.decode(object.encryptedLoginToken, message.encryptedLoginToken = $util.newBuffer($util.base64.length(object.encryptedLoginToken)), 0);
                else if (object.encryptedLoginToken.length >= 0)
                    message.encryptedLoginToken = object.encryptedLoginToken;
            return message;
        };

        /**
         * Creates a plain object from a PasskeyAuthenticationResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.PasskeyAuthenticationResponse
         * @static
         * @param {Authentication.PasskeyAuthenticationResponse} message PasskeyAuthenticationResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PasskeyAuthenticationResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.pkRequestOptions = "";
                if (options.bytes === String)
                    object.challengeToken = "";
                else {
                    object.challengeToken = [];
                    if (options.bytes !== Array)
                        object.challengeToken = $util.newBuffer(object.challengeToken);
                }
            }
            if (message.pkRequestOptions != null && Object.hasOwnProperty.call(message, "pkRequestOptions"))
                object.pkRequestOptions = message.pkRequestOptions;
            if (message.challengeToken != null && Object.hasOwnProperty.call(message, "challengeToken"))
                object.challengeToken = options.bytes === String ? $util.base64.encode(message.challengeToken, 0, message.challengeToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.challengeToken) : message.challengeToken;
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken")) {
                object.encryptedLoginToken = options.bytes === String ? $util.base64.encode(message.encryptedLoginToken, 0, message.encryptedLoginToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedLoginToken) : message.encryptedLoginToken;
                if (options.oneofs)
                    object._encryptedLoginToken = "encryptedLoginToken";
            }
            return object;
        };

        /**
         * Converts this PasskeyAuthenticationResponse to JSON.
         * @function toJSON
         * @memberof Authentication.PasskeyAuthenticationResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PasskeyAuthenticationResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PasskeyAuthenticationResponse
         * @function getTypeUrl
         * @memberof Authentication.PasskeyAuthenticationResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PasskeyAuthenticationResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.PasskeyAuthenticationResponse";
        };

        return PasskeyAuthenticationResponse;
    })();

    Authentication.PasskeyValidationRequest = (function() {

        /**
         * Properties of a PasskeyValidationRequest.
         * @memberof Authentication
         * @interface IPasskeyValidationRequest
         * @property {Uint8Array|null} [challengeToken] PasskeyValidationRequest challengeToken
         * @property {Uint8Array|null} [assertionResponse] PasskeyValidationRequest assertionResponse
         * @property {Authentication.PasskeyPurpose|null} [passkeyPurpose] PasskeyValidationRequest passkeyPurpose
         * @property {Uint8Array|null} [encryptedLoginToken] PasskeyValidationRequest encryptedLoginToken
         */

        /**
         * Constructs a new PasskeyValidationRequest.
         * @memberof Authentication
         * @classdesc Represents a PasskeyValidationRequest.
         * @implements IPasskeyValidationRequest
         * @constructor
         * @param {Authentication.IPasskeyValidationRequest=} [properties] Properties to set
         */
        function PasskeyValidationRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PasskeyValidationRequest challengeToken.
         * @member {Uint8Array} challengeToken
         * @memberof Authentication.PasskeyValidationRequest
         * @instance
         */
        PasskeyValidationRequest.prototype.challengeToken = $util.newBuffer([]);

        /**
         * PasskeyValidationRequest assertionResponse.
         * @member {Uint8Array} assertionResponse
         * @memberof Authentication.PasskeyValidationRequest
         * @instance
         */
        PasskeyValidationRequest.prototype.assertionResponse = $util.newBuffer([]);

        /**
         * PasskeyValidationRequest passkeyPurpose.
         * @member {Authentication.PasskeyPurpose} passkeyPurpose
         * @memberof Authentication.PasskeyValidationRequest
         * @instance
         */
        PasskeyValidationRequest.prototype.passkeyPurpose = 0;

        /**
         * PasskeyValidationRequest encryptedLoginToken.
         * @member {Uint8Array|null|undefined} encryptedLoginToken
         * @memberof Authentication.PasskeyValidationRequest
         * @instance
         */
        PasskeyValidationRequest.prototype.encryptedLoginToken = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PasskeyValidationRequest.prototype, "_encryptedLoginToken", {
            get: $util.oneOfGetter($oneOfFields = ["encryptedLoginToken"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new PasskeyValidationRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.PasskeyValidationRequest
         * @static
         * @param {Authentication.IPasskeyValidationRequest=} [properties] Properties to set
         * @returns {Authentication.PasskeyValidationRequest} PasskeyValidationRequest instance
         */
        PasskeyValidationRequest.create = function create(properties) {
            return new PasskeyValidationRequest(properties);
        };

        /**
         * Encodes the specified PasskeyValidationRequest message. Does not implicitly {@link Authentication.PasskeyValidationRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.PasskeyValidationRequest
         * @static
         * @param {Authentication.IPasskeyValidationRequest} message PasskeyValidationRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PasskeyValidationRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.challengeToken != null && Object.hasOwnProperty.call(message, "challengeToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.challengeToken);
            if (message.assertionResponse != null && Object.hasOwnProperty.call(message, "assertionResponse"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.assertionResponse);
            if (message.passkeyPurpose != null && Object.hasOwnProperty.call(message, "passkeyPurpose"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.passkeyPurpose);
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.encryptedLoginToken);
            return writer;
        };

        /**
         * Decodes a PasskeyValidationRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.PasskeyValidationRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.PasskeyValidationRequest} PasskeyValidationRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PasskeyValidationRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.PasskeyValidationRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.challengeToken = reader.bytes();
                        break;
                    }
                case 2: {
                        message.assertionResponse = reader.bytes();
                        break;
                    }
                case 3: {
                        message.passkeyPurpose = reader.int32();
                        break;
                    }
                case 4: {
                        message.encryptedLoginToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a PasskeyValidationRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.PasskeyValidationRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.PasskeyValidationRequest} PasskeyValidationRequest
         */
        PasskeyValidationRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.PasskeyValidationRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.PasskeyValidationRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.PasskeyValidationRequest();
            if (object.challengeToken != null)
                if (typeof object.challengeToken === "string")
                    $util.base64.decode(object.challengeToken, message.challengeToken = $util.newBuffer($util.base64.length(object.challengeToken)), 0);
                else if (object.challengeToken.length >= 0)
                    message.challengeToken = object.challengeToken;
            if (object.assertionResponse != null)
                if (typeof object.assertionResponse === "string")
                    $util.base64.decode(object.assertionResponse, message.assertionResponse = $util.newBuffer($util.base64.length(object.assertionResponse)), 0);
                else if (object.assertionResponse.length >= 0)
                    message.assertionResponse = object.assertionResponse;
            switch (object.passkeyPurpose) {
            default:
                if (typeof object.passkeyPurpose === "number") {
                    message.passkeyPurpose = object.passkeyPurpose;
                    break;
                }
                break;
            case "PK_LOGIN":
            case 0:
                message.passkeyPurpose = 0;
                break;
            case "PK_REAUTH":
            case 1:
                message.passkeyPurpose = 1;
                break;
            }
            if (object.encryptedLoginToken != null)
                if (typeof object.encryptedLoginToken === "string")
                    $util.base64.decode(object.encryptedLoginToken, message.encryptedLoginToken = $util.newBuffer($util.base64.length(object.encryptedLoginToken)), 0);
                else if (object.encryptedLoginToken.length >= 0)
                    message.encryptedLoginToken = object.encryptedLoginToken;
            return message;
        };

        /**
         * Creates a plain object from a PasskeyValidationRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.PasskeyValidationRequest
         * @static
         * @param {Authentication.PasskeyValidationRequest} message PasskeyValidationRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PasskeyValidationRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.challengeToken = "";
                else {
                    object.challengeToken = [];
                    if (options.bytes !== Array)
                        object.challengeToken = $util.newBuffer(object.challengeToken);
                }
                if (options.bytes === String)
                    object.assertionResponse = "";
                else {
                    object.assertionResponse = [];
                    if (options.bytes !== Array)
                        object.assertionResponse = $util.newBuffer(object.assertionResponse);
                }
                object.passkeyPurpose = options.enums === String ? "PK_LOGIN" : 0;
            }
            if (message.challengeToken != null && Object.hasOwnProperty.call(message, "challengeToken"))
                object.challengeToken = options.bytes === String ? $util.base64.encode(message.challengeToken, 0, message.challengeToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.challengeToken) : message.challengeToken;
            if (message.assertionResponse != null && Object.hasOwnProperty.call(message, "assertionResponse"))
                object.assertionResponse = options.bytes === String ? $util.base64.encode(message.assertionResponse, 0, message.assertionResponse.length) : options.bytes === Array ? Array.prototype.slice.call(message.assertionResponse) : message.assertionResponse;
            if (message.passkeyPurpose != null && Object.hasOwnProperty.call(message, "passkeyPurpose"))
                object.passkeyPurpose = options.enums === String ? $root.Authentication.PasskeyPurpose[message.passkeyPurpose] === undefined ? message.passkeyPurpose : $root.Authentication.PasskeyPurpose[message.passkeyPurpose] : message.passkeyPurpose;
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken")) {
                object.encryptedLoginToken = options.bytes === String ? $util.base64.encode(message.encryptedLoginToken, 0, message.encryptedLoginToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedLoginToken) : message.encryptedLoginToken;
                if (options.oneofs)
                    object._encryptedLoginToken = "encryptedLoginToken";
            }
            return object;
        };

        /**
         * Converts this PasskeyValidationRequest to JSON.
         * @function toJSON
         * @memberof Authentication.PasskeyValidationRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PasskeyValidationRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PasskeyValidationRequest
         * @function getTypeUrl
         * @memberof Authentication.PasskeyValidationRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PasskeyValidationRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.PasskeyValidationRequest";
        };

        return PasskeyValidationRequest;
    })();

    Authentication.PasskeyValidationResponse = (function() {

        /**
         * Properties of a PasskeyValidationResponse.
         * @memberof Authentication
         * @interface IPasskeyValidationResponse
         * @property {boolean|null} [isValid] PasskeyValidationResponse isValid
         * @property {Uint8Array|null} [encryptedLoginToken] PasskeyValidationResponse encryptedLoginToken
         */

        /**
         * Constructs a new PasskeyValidationResponse.
         * @memberof Authentication
         * @classdesc Represents a PasskeyValidationResponse.
         * @implements IPasskeyValidationResponse
         * @constructor
         * @param {Authentication.IPasskeyValidationResponse=} [properties] Properties to set
         */
        function PasskeyValidationResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PasskeyValidationResponse isValid.
         * @member {boolean} isValid
         * @memberof Authentication.PasskeyValidationResponse
         * @instance
         */
        PasskeyValidationResponse.prototype.isValid = false;

        /**
         * PasskeyValidationResponse encryptedLoginToken.
         * @member {Uint8Array} encryptedLoginToken
         * @memberof Authentication.PasskeyValidationResponse
         * @instance
         */
        PasskeyValidationResponse.prototype.encryptedLoginToken = $util.newBuffer([]);

        /**
         * Creates a new PasskeyValidationResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.PasskeyValidationResponse
         * @static
         * @param {Authentication.IPasskeyValidationResponse=} [properties] Properties to set
         * @returns {Authentication.PasskeyValidationResponse} PasskeyValidationResponse instance
         */
        PasskeyValidationResponse.create = function create(properties) {
            return new PasskeyValidationResponse(properties);
        };

        /**
         * Encodes the specified PasskeyValidationResponse message. Does not implicitly {@link Authentication.PasskeyValidationResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.PasskeyValidationResponse
         * @static
         * @param {Authentication.IPasskeyValidationResponse} message PasskeyValidationResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PasskeyValidationResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.isValid != null && Object.hasOwnProperty.call(message, "isValid"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.isValid);
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encryptedLoginToken);
            return writer;
        };

        /**
         * Decodes a PasskeyValidationResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.PasskeyValidationResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.PasskeyValidationResponse} PasskeyValidationResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PasskeyValidationResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.PasskeyValidationResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.isValid = reader.bool();
                        break;
                    }
                case 2: {
                        message.encryptedLoginToken = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a PasskeyValidationResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.PasskeyValidationResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.PasskeyValidationResponse} PasskeyValidationResponse
         */
        PasskeyValidationResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.PasskeyValidationResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.PasskeyValidationResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.PasskeyValidationResponse();
            if (object.isValid != null)
                message.isValid = Boolean(object.isValid);
            if (object.encryptedLoginToken != null)
                if (typeof object.encryptedLoginToken === "string")
                    $util.base64.decode(object.encryptedLoginToken, message.encryptedLoginToken = $util.newBuffer($util.base64.length(object.encryptedLoginToken)), 0);
                else if (object.encryptedLoginToken.length >= 0)
                    message.encryptedLoginToken = object.encryptedLoginToken;
            return message;
        };

        /**
         * Creates a plain object from a PasskeyValidationResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.PasskeyValidationResponse
         * @static
         * @param {Authentication.PasskeyValidationResponse} message PasskeyValidationResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PasskeyValidationResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.isValid = false;
                if (options.bytes === String)
                    object.encryptedLoginToken = "";
                else {
                    object.encryptedLoginToken = [];
                    if (options.bytes !== Array)
                        object.encryptedLoginToken = $util.newBuffer(object.encryptedLoginToken);
                }
            }
            if (message.isValid != null && Object.hasOwnProperty.call(message, "isValid"))
                object.isValid = message.isValid;
            if (message.encryptedLoginToken != null && Object.hasOwnProperty.call(message, "encryptedLoginToken"))
                object.encryptedLoginToken = options.bytes === String ? $util.base64.encode(message.encryptedLoginToken, 0, message.encryptedLoginToken.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedLoginToken) : message.encryptedLoginToken;
            return object;
        };

        /**
         * Converts this PasskeyValidationResponse to JSON.
         * @function toJSON
         * @memberof Authentication.PasskeyValidationResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PasskeyValidationResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PasskeyValidationResponse
         * @function getTypeUrl
         * @memberof Authentication.PasskeyValidationResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PasskeyValidationResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.PasskeyValidationResponse";
        };

        return PasskeyValidationResponse;
    })();

    Authentication.UpdatePasskeyRequest = (function() {

        /**
         * Properties of an UpdatePasskeyRequest.
         * @memberof Authentication
         * @interface IUpdatePasskeyRequest
         * @property {number|null} [userId] UpdatePasskeyRequest userId
         * @property {Uint8Array|null} [credentialId] UpdatePasskeyRequest credentialId
         * @property {string|null} [friendlyName] UpdatePasskeyRequest friendlyName
         */

        /**
         * Constructs a new UpdatePasskeyRequest.
         * @memberof Authentication
         * @classdesc Represents an UpdatePasskeyRequest.
         * @implements IUpdatePasskeyRequest
         * @constructor
         * @param {Authentication.IUpdatePasskeyRequest=} [properties] Properties to set
         */
        function UpdatePasskeyRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UpdatePasskeyRequest userId.
         * @member {number} userId
         * @memberof Authentication.UpdatePasskeyRequest
         * @instance
         */
        UpdatePasskeyRequest.prototype.userId = 0;

        /**
         * UpdatePasskeyRequest credentialId.
         * @member {Uint8Array} credentialId
         * @memberof Authentication.UpdatePasskeyRequest
         * @instance
         */
        UpdatePasskeyRequest.prototype.credentialId = $util.newBuffer([]);

        /**
         * UpdatePasskeyRequest friendlyName.
         * @member {string|null|undefined} friendlyName
         * @memberof Authentication.UpdatePasskeyRequest
         * @instance
         */
        UpdatePasskeyRequest.prototype.friendlyName = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(UpdatePasskeyRequest.prototype, "_friendlyName", {
            get: $util.oneOfGetter($oneOfFields = ["friendlyName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new UpdatePasskeyRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.UpdatePasskeyRequest
         * @static
         * @param {Authentication.IUpdatePasskeyRequest=} [properties] Properties to set
         * @returns {Authentication.UpdatePasskeyRequest} UpdatePasskeyRequest instance
         */
        UpdatePasskeyRequest.create = function create(properties) {
            return new UpdatePasskeyRequest(properties);
        };

        /**
         * Encodes the specified UpdatePasskeyRequest message. Does not implicitly {@link Authentication.UpdatePasskeyRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.UpdatePasskeyRequest
         * @static
         * @param {Authentication.IUpdatePasskeyRequest} message UpdatePasskeyRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UpdatePasskeyRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.userId);
            if (message.credentialId != null && Object.hasOwnProperty.call(message, "credentialId"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.credentialId);
            if (message.friendlyName != null && Object.hasOwnProperty.call(message, "friendlyName"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.friendlyName);
            return writer;
        };

        /**
         * Decodes an UpdatePasskeyRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.UpdatePasskeyRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.UpdatePasskeyRequest} UpdatePasskeyRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UpdatePasskeyRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.UpdatePasskeyRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.userId = reader.int32();
                        break;
                    }
                case 2: {
                        message.credentialId = reader.bytes();
                        break;
                    }
                case 3: {
                        message.friendlyName = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates an UpdatePasskeyRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.UpdatePasskeyRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.UpdatePasskeyRequest} UpdatePasskeyRequest
         */
        UpdatePasskeyRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.UpdatePasskeyRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.UpdatePasskeyRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.UpdatePasskeyRequest();
            if (object.userId != null)
                message.userId = object.userId | 0;
            if (object.credentialId != null)
                if (typeof object.credentialId === "string")
                    $util.base64.decode(object.credentialId, message.credentialId = $util.newBuffer($util.base64.length(object.credentialId)), 0);
                else if (object.credentialId.length >= 0)
                    message.credentialId = object.credentialId;
            if (object.friendlyName != null)
                message.friendlyName = String(object.friendlyName);
            return message;
        };

        /**
         * Creates a plain object from an UpdatePasskeyRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.UpdatePasskeyRequest
         * @static
         * @param {Authentication.UpdatePasskeyRequest} message UpdatePasskeyRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UpdatePasskeyRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.userId = 0;
                if (options.bytes === String)
                    object.credentialId = "";
                else {
                    object.credentialId = [];
                    if (options.bytes !== Array)
                        object.credentialId = $util.newBuffer(object.credentialId);
                }
            }
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                object.userId = message.userId;
            if (message.credentialId != null && Object.hasOwnProperty.call(message, "credentialId"))
                object.credentialId = options.bytes === String ? $util.base64.encode(message.credentialId, 0, message.credentialId.length) : options.bytes === Array ? Array.prototype.slice.call(message.credentialId) : message.credentialId;
            if (message.friendlyName != null && Object.hasOwnProperty.call(message, "friendlyName")) {
                object.friendlyName = message.friendlyName;
                if (options.oneofs)
                    object._friendlyName = "friendlyName";
            }
            return object;
        };

        /**
         * Converts this UpdatePasskeyRequest to JSON.
         * @function toJSON
         * @memberof Authentication.UpdatePasskeyRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UpdatePasskeyRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UpdatePasskeyRequest
         * @function getTypeUrl
         * @memberof Authentication.UpdatePasskeyRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UpdatePasskeyRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.UpdatePasskeyRequest";
        };

        return UpdatePasskeyRequest;
    })();

    Authentication.PasskeyListRequest = (function() {

        /**
         * Properties of a PasskeyListRequest.
         * @memberof Authentication
         * @interface IPasskeyListRequest
         * @property {boolean|null} [includeDisabled] PasskeyListRequest includeDisabled
         */

        /**
         * Constructs a new PasskeyListRequest.
         * @memberof Authentication
         * @classdesc Represents a PasskeyListRequest.
         * @implements IPasskeyListRequest
         * @constructor
         * @param {Authentication.IPasskeyListRequest=} [properties] Properties to set
         */
        function PasskeyListRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PasskeyListRequest includeDisabled.
         * @member {boolean} includeDisabled
         * @memberof Authentication.PasskeyListRequest
         * @instance
         */
        PasskeyListRequest.prototype.includeDisabled = false;

        /**
         * Creates a new PasskeyListRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.PasskeyListRequest
         * @static
         * @param {Authentication.IPasskeyListRequest=} [properties] Properties to set
         * @returns {Authentication.PasskeyListRequest} PasskeyListRequest instance
         */
        PasskeyListRequest.create = function create(properties) {
            return new PasskeyListRequest(properties);
        };

        /**
         * Encodes the specified PasskeyListRequest message. Does not implicitly {@link Authentication.PasskeyListRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.PasskeyListRequest
         * @static
         * @param {Authentication.IPasskeyListRequest} message PasskeyListRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PasskeyListRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.includeDisabled != null && Object.hasOwnProperty.call(message, "includeDisabled"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.includeDisabled);
            return writer;
        };

        /**
         * Decodes a PasskeyListRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.PasskeyListRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.PasskeyListRequest} PasskeyListRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PasskeyListRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.PasskeyListRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.includeDisabled = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a PasskeyListRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.PasskeyListRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.PasskeyListRequest} PasskeyListRequest
         */
        PasskeyListRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.PasskeyListRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.PasskeyListRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.PasskeyListRequest();
            if (object.includeDisabled != null)
                message.includeDisabled = Boolean(object.includeDisabled);
            return message;
        };

        /**
         * Creates a plain object from a PasskeyListRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.PasskeyListRequest
         * @static
         * @param {Authentication.PasskeyListRequest} message PasskeyListRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PasskeyListRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.includeDisabled = false;
            if (message.includeDisabled != null && Object.hasOwnProperty.call(message, "includeDisabled"))
                object.includeDisabled = message.includeDisabled;
            return object;
        };

        /**
         * Converts this PasskeyListRequest to JSON.
         * @function toJSON
         * @memberof Authentication.PasskeyListRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PasskeyListRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PasskeyListRequest
         * @function getTypeUrl
         * @memberof Authentication.PasskeyListRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PasskeyListRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.PasskeyListRequest";
        };

        return PasskeyListRequest;
    })();

    Authentication.PasskeyInfo = (function() {

        /**
         * Properties of a PasskeyInfo.
         * @memberof Authentication
         * @interface IPasskeyInfo
         * @property {number|null} [userId] PasskeyInfo userId
         * @property {Uint8Array|null} [credentialId] PasskeyInfo credentialId
         * @property {string|null} [friendlyName] PasskeyInfo friendlyName
         * @property {string|null} [AAGUID] PasskeyInfo AAGUID
         * @property {number|null} [createdAtMillis] PasskeyInfo createdAtMillis
         * @property {number|null} [lastUsedMillis] PasskeyInfo lastUsedMillis
         * @property {number|null} [disabledAtMillis] PasskeyInfo disabledAtMillis
         */

        /**
         * Constructs a new PasskeyInfo.
         * @memberof Authentication
         * @classdesc Represents a PasskeyInfo.
         * @implements IPasskeyInfo
         * @constructor
         * @param {Authentication.IPasskeyInfo=} [properties] Properties to set
         */
        function PasskeyInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PasskeyInfo userId.
         * @member {number} userId
         * @memberof Authentication.PasskeyInfo
         * @instance
         */
        PasskeyInfo.prototype.userId = 0;

        /**
         * PasskeyInfo credentialId.
         * @member {Uint8Array} credentialId
         * @memberof Authentication.PasskeyInfo
         * @instance
         */
        PasskeyInfo.prototype.credentialId = $util.newBuffer([]);

        /**
         * PasskeyInfo friendlyName.
         * @member {string} friendlyName
         * @memberof Authentication.PasskeyInfo
         * @instance
         */
        PasskeyInfo.prototype.friendlyName = "";

        /**
         * PasskeyInfo AAGUID.
         * @member {string} AAGUID
         * @memberof Authentication.PasskeyInfo
         * @instance
         */
        PasskeyInfo.prototype.AAGUID = "";

        /**
         * PasskeyInfo createdAtMillis.
         * @member {number} createdAtMillis
         * @memberof Authentication.PasskeyInfo
         * @instance
         */
        PasskeyInfo.prototype.createdAtMillis = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PasskeyInfo lastUsedMillis.
         * @member {number} lastUsedMillis
         * @memberof Authentication.PasskeyInfo
         * @instance
         */
        PasskeyInfo.prototype.lastUsedMillis = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PasskeyInfo disabledAtMillis.
         * @member {number} disabledAtMillis
         * @memberof Authentication.PasskeyInfo
         * @instance
         */
        PasskeyInfo.prototype.disabledAtMillis = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new PasskeyInfo instance using the specified properties.
         * @function create
         * @memberof Authentication.PasskeyInfo
         * @static
         * @param {Authentication.IPasskeyInfo=} [properties] Properties to set
         * @returns {Authentication.PasskeyInfo} PasskeyInfo instance
         */
        PasskeyInfo.create = function create(properties) {
            return new PasskeyInfo(properties);
        };

        /**
         * Encodes the specified PasskeyInfo message. Does not implicitly {@link Authentication.PasskeyInfo.verify|verify} messages.
         * @function encode
         * @memberof Authentication.PasskeyInfo
         * @static
         * @param {Authentication.IPasskeyInfo} message PasskeyInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PasskeyInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.userId);
            if (message.credentialId != null && Object.hasOwnProperty.call(message, "credentialId"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.credentialId);
            if (message.friendlyName != null && Object.hasOwnProperty.call(message, "friendlyName"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.friendlyName);
            if (message.AAGUID != null && Object.hasOwnProperty.call(message, "AAGUID"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.AAGUID);
            if (message.createdAtMillis != null && Object.hasOwnProperty.call(message, "createdAtMillis"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.createdAtMillis);
            if (message.lastUsedMillis != null && Object.hasOwnProperty.call(message, "lastUsedMillis"))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.lastUsedMillis);
            if (message.disabledAtMillis != null && Object.hasOwnProperty.call(message, "disabledAtMillis"))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.disabledAtMillis);
            return writer;
        };

        /**
         * Decodes a PasskeyInfo message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.PasskeyInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.PasskeyInfo} PasskeyInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PasskeyInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.PasskeyInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.userId = reader.int32();
                        break;
                    }
                case 2: {
                        message.credentialId = reader.bytes();
                        break;
                    }
                case 3: {
                        message.friendlyName = reader.string();
                        break;
                    }
                case 4: {
                        message.AAGUID = reader.string();
                        break;
                    }
                case 5: {
                        message.createdAtMillis = reader.int64();
                        break;
                    }
                case 6: {
                        message.lastUsedMillis = reader.int64();
                        break;
                    }
                case 7: {
                        message.disabledAtMillis = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a PasskeyInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.PasskeyInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.PasskeyInfo} PasskeyInfo
         */
        PasskeyInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.PasskeyInfo)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.PasskeyInfo: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.PasskeyInfo();
            if (object.userId != null)
                message.userId = object.userId | 0;
            if (object.credentialId != null)
                if (typeof object.credentialId === "string")
                    $util.base64.decode(object.credentialId, message.credentialId = $util.newBuffer($util.base64.length(object.credentialId)), 0);
                else if (object.credentialId.length >= 0)
                    message.credentialId = object.credentialId;
            if (object.friendlyName != null)
                message.friendlyName = String(object.friendlyName);
            if (object.AAGUID != null)
                message.AAGUID = String(object.AAGUID);
            if (object.createdAtMillis != null)
                if ($util.Long)
                    message.createdAtMillis = $util.Long.fromValue(object.createdAtMillis, false);
                else if (typeof object.createdAtMillis === "string")
                    message.createdAtMillis = parseInt(object.createdAtMillis, 10);
                else if (typeof object.createdAtMillis === "number")
                    message.createdAtMillis = object.createdAtMillis;
                else if (typeof object.createdAtMillis === "object")
                    message.createdAtMillis = new $util.LongBits(object.createdAtMillis.low >>> 0, object.createdAtMillis.high >>> 0).toNumber();
            if (object.lastUsedMillis != null)
                if ($util.Long)
                    message.lastUsedMillis = $util.Long.fromValue(object.lastUsedMillis, false);
                else if (typeof object.lastUsedMillis === "string")
                    message.lastUsedMillis = parseInt(object.lastUsedMillis, 10);
                else if (typeof object.lastUsedMillis === "number")
                    message.lastUsedMillis = object.lastUsedMillis;
                else if (typeof object.lastUsedMillis === "object")
                    message.lastUsedMillis = new $util.LongBits(object.lastUsedMillis.low >>> 0, object.lastUsedMillis.high >>> 0).toNumber();
            if (object.disabledAtMillis != null)
                if ($util.Long)
                    message.disabledAtMillis = $util.Long.fromValue(object.disabledAtMillis, false);
                else if (typeof object.disabledAtMillis === "string")
                    message.disabledAtMillis = parseInt(object.disabledAtMillis, 10);
                else if (typeof object.disabledAtMillis === "number")
                    message.disabledAtMillis = object.disabledAtMillis;
                else if (typeof object.disabledAtMillis === "object")
                    message.disabledAtMillis = new $util.LongBits(object.disabledAtMillis.low >>> 0, object.disabledAtMillis.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a PasskeyInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.PasskeyInfo
         * @static
         * @param {Authentication.PasskeyInfo} message PasskeyInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PasskeyInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.userId = 0;
                if (options.bytes === String)
                    object.credentialId = "";
                else {
                    object.credentialId = [];
                    if (options.bytes !== Array)
                        object.credentialId = $util.newBuffer(object.credentialId);
                }
                object.friendlyName = "";
                object.AAGUID = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.createdAtMillis = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.createdAtMillis = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastUsedMillis = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastUsedMillis = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.disabledAtMillis = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.disabledAtMillis = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                object.userId = message.userId;
            if (message.credentialId != null && Object.hasOwnProperty.call(message, "credentialId"))
                object.credentialId = options.bytes === String ? $util.base64.encode(message.credentialId, 0, message.credentialId.length) : options.bytes === Array ? Array.prototype.slice.call(message.credentialId) : message.credentialId;
            if (message.friendlyName != null && Object.hasOwnProperty.call(message, "friendlyName"))
                object.friendlyName = message.friendlyName;
            if (message.AAGUID != null && Object.hasOwnProperty.call(message, "AAGUID"))
                object.AAGUID = message.AAGUID;
            if (message.createdAtMillis != null && Object.hasOwnProperty.call(message, "createdAtMillis"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.createdAtMillis = typeof message.createdAtMillis === "number" ? BigInt(message.createdAtMillis) : $util.Long.fromBits(message.createdAtMillis.low >>> 0, message.createdAtMillis.high >>> 0, false).toBigInt();
                else if (typeof message.createdAtMillis === "number")
                    object.createdAtMillis = options.longs === String ? String(message.createdAtMillis) : message.createdAtMillis;
                else
                    object.createdAtMillis = options.longs === String ? $util.Long.prototype.toString.call(message.createdAtMillis) : options.longs === Number ? new $util.LongBits(message.createdAtMillis.low >>> 0, message.createdAtMillis.high >>> 0).toNumber() : message.createdAtMillis;
            if (message.lastUsedMillis != null && Object.hasOwnProperty.call(message, "lastUsedMillis"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastUsedMillis = typeof message.lastUsedMillis === "number" ? BigInt(message.lastUsedMillis) : $util.Long.fromBits(message.lastUsedMillis.low >>> 0, message.lastUsedMillis.high >>> 0, false).toBigInt();
                else if (typeof message.lastUsedMillis === "number")
                    object.lastUsedMillis = options.longs === String ? String(message.lastUsedMillis) : message.lastUsedMillis;
                else
                    object.lastUsedMillis = options.longs === String ? $util.Long.prototype.toString.call(message.lastUsedMillis) : options.longs === Number ? new $util.LongBits(message.lastUsedMillis.low >>> 0, message.lastUsedMillis.high >>> 0).toNumber() : message.lastUsedMillis;
            if (message.disabledAtMillis != null && Object.hasOwnProperty.call(message, "disabledAtMillis"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.disabledAtMillis = typeof message.disabledAtMillis === "number" ? BigInt(message.disabledAtMillis) : $util.Long.fromBits(message.disabledAtMillis.low >>> 0, message.disabledAtMillis.high >>> 0, false).toBigInt();
                else if (typeof message.disabledAtMillis === "number")
                    object.disabledAtMillis = options.longs === String ? String(message.disabledAtMillis) : message.disabledAtMillis;
                else
                    object.disabledAtMillis = options.longs === String ? $util.Long.prototype.toString.call(message.disabledAtMillis) : options.longs === Number ? new $util.LongBits(message.disabledAtMillis.low >>> 0, message.disabledAtMillis.high >>> 0).toNumber() : message.disabledAtMillis;
            return object;
        };

        /**
         * Converts this PasskeyInfo to JSON.
         * @function toJSON
         * @memberof Authentication.PasskeyInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PasskeyInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PasskeyInfo
         * @function getTypeUrl
         * @memberof Authentication.PasskeyInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PasskeyInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.PasskeyInfo";
        };

        return PasskeyInfo;
    })();

    Authentication.PasskeyListResponse = (function() {

        /**
         * Properties of a PasskeyListResponse.
         * @memberof Authentication
         * @interface IPasskeyListResponse
         * @property {Array.<Authentication.IPasskeyInfo>|null} [passkeyInfo] PasskeyListResponse passkeyInfo
         */

        /**
         * Constructs a new PasskeyListResponse.
         * @memberof Authentication
         * @classdesc Represents a PasskeyListResponse.
         * @implements IPasskeyListResponse
         * @constructor
         * @param {Authentication.IPasskeyListResponse=} [properties] Properties to set
         */
        function PasskeyListResponse(properties) {
            this.passkeyInfo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PasskeyListResponse passkeyInfo.
         * @member {Array.<Authentication.IPasskeyInfo>} passkeyInfo
         * @memberof Authentication.PasskeyListResponse
         * @instance
         */
        PasskeyListResponse.prototype.passkeyInfo = $util.emptyArray;

        /**
         * Creates a new PasskeyListResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.PasskeyListResponse
         * @static
         * @param {Authentication.IPasskeyListResponse=} [properties] Properties to set
         * @returns {Authentication.PasskeyListResponse} PasskeyListResponse instance
         */
        PasskeyListResponse.create = function create(properties) {
            return new PasskeyListResponse(properties);
        };

        /**
         * Encodes the specified PasskeyListResponse message. Does not implicitly {@link Authentication.PasskeyListResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.PasskeyListResponse
         * @static
         * @param {Authentication.IPasskeyListResponse} message PasskeyListResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PasskeyListResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.passkeyInfo != null && message.passkeyInfo.length)
                for (let i = 0; i < message.passkeyInfo.length; ++i)
                    $root.Authentication.PasskeyInfo.encode(message.passkeyInfo[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes a PasskeyListResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.PasskeyListResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.PasskeyListResponse} PasskeyListResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PasskeyListResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.PasskeyListResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.passkeyInfo && message.passkeyInfo.length))
                            message.passkeyInfo = [];
                        message.passkeyInfo.push($root.Authentication.PasskeyInfo.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a PasskeyListResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.PasskeyListResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.PasskeyListResponse} PasskeyListResponse
         */
        PasskeyListResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.PasskeyListResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.PasskeyListResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.PasskeyListResponse();
            if (object.passkeyInfo) {
                if (!Array.isArray(object.passkeyInfo))
                    throw TypeError(".Authentication.PasskeyListResponse.passkeyInfo: array expected");
                message.passkeyInfo = [];
                for (let i = 0; i < object.passkeyInfo.length; ++i) {
                    if (!$util.isObject(object.passkeyInfo[i]))
                        throw TypeError(".Authentication.PasskeyListResponse.passkeyInfo: object expected");
                    message.passkeyInfo[i] = $root.Authentication.PasskeyInfo.fromObject(object.passkeyInfo[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a PasskeyListResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.PasskeyListResponse
         * @static
         * @param {Authentication.PasskeyListResponse} message PasskeyListResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PasskeyListResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.passkeyInfo = [];
            if (message.passkeyInfo && message.passkeyInfo.length) {
                object.passkeyInfo = [];
                for (let j = 0; j < message.passkeyInfo.length; ++j)
                    object.passkeyInfo[j] = $root.Authentication.PasskeyInfo.toObject(message.passkeyInfo[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this PasskeyListResponse to JSON.
         * @function toJSON
         * @memberof Authentication.PasskeyListResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PasskeyListResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PasskeyListResponse
         * @function getTypeUrl
         * @memberof Authentication.PasskeyListResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PasskeyListResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.PasskeyListResponse";
        };

        return PasskeyListResponse;
    })();

    /**
     * ClientFormFactor enum.
     * @name Authentication.ClientFormFactor
     * @enum {number}
     * @property {number} FF_EMPTY=0 FF_EMPTY value
     * @property {number} FF_PHONE=1 FF_PHONE value
     * @property {number} FF_TABLET=2 FF_TABLET value
     * @property {number} FF_WATCH=3 FF_WATCH value
     */
    Authentication.ClientFormFactor = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "FF_EMPTY"] = 0;
        values[valuesById[1] = "FF_PHONE"] = 1;
        values[valuesById[2] = "FF_TABLET"] = 2;
        values[valuesById[3] = "FF_WATCH"] = 3;
        return values;
    })();

    Authentication.TranslationInfo = (function() {

        /**
         * Properties of a TranslationInfo.
         * @memberof Authentication
         * @interface ITranslationInfo
         * @property {string|null} [translationKey] TranslationInfo translationKey
         * @property {string|null} [translationValue] TranslationInfo translationValue
         */

        /**
         * Constructs a new TranslationInfo.
         * @memberof Authentication
         * @classdesc Represents a TranslationInfo.
         * @implements ITranslationInfo
         * @constructor
         * @param {Authentication.ITranslationInfo=} [properties] Properties to set
         */
        function TranslationInfo(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TranslationInfo translationKey.
         * @member {string} translationKey
         * @memberof Authentication.TranslationInfo
         * @instance
         */
        TranslationInfo.prototype.translationKey = "";

        /**
         * TranslationInfo translationValue.
         * @member {string} translationValue
         * @memberof Authentication.TranslationInfo
         * @instance
         */
        TranslationInfo.prototype.translationValue = "";

        /**
         * Creates a new TranslationInfo instance using the specified properties.
         * @function create
         * @memberof Authentication.TranslationInfo
         * @static
         * @param {Authentication.ITranslationInfo=} [properties] Properties to set
         * @returns {Authentication.TranslationInfo} TranslationInfo instance
         */
        TranslationInfo.create = function create(properties) {
            return new TranslationInfo(properties);
        };

        /**
         * Encodes the specified TranslationInfo message. Does not implicitly {@link Authentication.TranslationInfo.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TranslationInfo
         * @static
         * @param {Authentication.ITranslationInfo} message TranslationInfo message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TranslationInfo.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.translationKey != null && Object.hasOwnProperty.call(message, "translationKey"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.translationKey);
            if (message.translationValue != null && Object.hasOwnProperty.call(message, "translationValue"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.translationValue);
            return writer;
        };

        /**
         * Decodes a TranslationInfo message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TranslationInfo
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TranslationInfo} TranslationInfo
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TranslationInfo.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TranslationInfo();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.translationKey = reader.string();
                        break;
                    }
                case 2: {
                        message.translationValue = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TranslationInfo message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TranslationInfo
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TranslationInfo} TranslationInfo
         */
        TranslationInfo.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TranslationInfo)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TranslationInfo: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TranslationInfo();
            if (object.translationKey != null)
                message.translationKey = String(object.translationKey);
            if (object.translationValue != null)
                message.translationValue = String(object.translationValue);
            return message;
        };

        /**
         * Creates a plain object from a TranslationInfo message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TranslationInfo
         * @static
         * @param {Authentication.TranslationInfo} message TranslationInfo
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TranslationInfo.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.translationKey = "";
                object.translationValue = "";
            }
            if (message.translationKey != null && Object.hasOwnProperty.call(message, "translationKey"))
                object.translationKey = message.translationKey;
            if (message.translationValue != null && Object.hasOwnProperty.call(message, "translationValue"))
                object.translationValue = message.translationValue;
            return object;
        };

        /**
         * Converts this TranslationInfo to JSON.
         * @function toJSON
         * @memberof Authentication.TranslationInfo
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TranslationInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TranslationInfo
         * @function getTypeUrl
         * @memberof Authentication.TranslationInfo
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TranslationInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TranslationInfo";
        };

        return TranslationInfo;
    })();

    Authentication.TranslationRequest = (function() {

        /**
         * Properties of a TranslationRequest.
         * @memberof Authentication
         * @interface ITranslationRequest
         * @property {Array.<string>|null} [translationKey] TranslationRequest translationKey
         */

        /**
         * Constructs a new TranslationRequest.
         * @memberof Authentication
         * @classdesc Represents a TranslationRequest.
         * @implements ITranslationRequest
         * @constructor
         * @param {Authentication.ITranslationRequest=} [properties] Properties to set
         */
        function TranslationRequest(properties) {
            this.translationKey = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TranslationRequest translationKey.
         * @member {Array.<string>} translationKey
         * @memberof Authentication.TranslationRequest
         * @instance
         */
        TranslationRequest.prototype.translationKey = $util.emptyArray;

        /**
         * Creates a new TranslationRequest instance using the specified properties.
         * @function create
         * @memberof Authentication.TranslationRequest
         * @static
         * @param {Authentication.ITranslationRequest=} [properties] Properties to set
         * @returns {Authentication.TranslationRequest} TranslationRequest instance
         */
        TranslationRequest.create = function create(properties) {
            return new TranslationRequest(properties);
        };

        /**
         * Encodes the specified TranslationRequest message. Does not implicitly {@link Authentication.TranslationRequest.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TranslationRequest
         * @static
         * @param {Authentication.ITranslationRequest} message TranslationRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TranslationRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.translationKey != null && message.translationKey.length)
                for (let i = 0; i < message.translationKey.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.translationKey[i]);
            return writer;
        };

        /**
         * Decodes a TranslationRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TranslationRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TranslationRequest} TranslationRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TranslationRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TranslationRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.translationKey && message.translationKey.length))
                            message.translationKey = [];
                        message.translationKey.push(reader.string());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TranslationRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TranslationRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TranslationRequest} TranslationRequest
         */
        TranslationRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TranslationRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TranslationRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TranslationRequest();
            if (object.translationKey) {
                if (!Array.isArray(object.translationKey))
                    throw TypeError(".Authentication.TranslationRequest.translationKey: array expected");
                message.translationKey = [];
                for (let i = 0; i < object.translationKey.length; ++i)
                    message.translationKey[i] = String(object.translationKey[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a TranslationRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TranslationRequest
         * @static
         * @param {Authentication.TranslationRequest} message TranslationRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TranslationRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.translationKey = [];
            if (message.translationKey && message.translationKey.length) {
                object.translationKey = [];
                for (let j = 0; j < message.translationKey.length; ++j)
                    object.translationKey[j] = message.translationKey[j];
            }
            return object;
        };

        /**
         * Converts this TranslationRequest to JSON.
         * @function toJSON
         * @memberof Authentication.TranslationRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TranslationRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TranslationRequest
         * @function getTypeUrl
         * @memberof Authentication.TranslationRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TranslationRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TranslationRequest";
        };

        return TranslationRequest;
    })();

    Authentication.TranslationResponse = (function() {

        /**
         * Properties of a TranslationResponse.
         * @memberof Authentication
         * @interface ITranslationResponse
         * @property {Array.<Authentication.ITranslationInfo>|null} [translationInfo] TranslationResponse translationInfo
         */

        /**
         * Constructs a new TranslationResponse.
         * @memberof Authentication
         * @classdesc Represents a TranslationResponse.
         * @implements ITranslationResponse
         * @constructor
         * @param {Authentication.ITranslationResponse=} [properties] Properties to set
         */
        function TranslationResponse(properties) {
            this.translationInfo = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TranslationResponse translationInfo.
         * @member {Array.<Authentication.ITranslationInfo>} translationInfo
         * @memberof Authentication.TranslationResponse
         * @instance
         */
        TranslationResponse.prototype.translationInfo = $util.emptyArray;

        /**
         * Creates a new TranslationResponse instance using the specified properties.
         * @function create
         * @memberof Authentication.TranslationResponse
         * @static
         * @param {Authentication.ITranslationResponse=} [properties] Properties to set
         * @returns {Authentication.TranslationResponse} TranslationResponse instance
         */
        TranslationResponse.create = function create(properties) {
            return new TranslationResponse(properties);
        };

        /**
         * Encodes the specified TranslationResponse message. Does not implicitly {@link Authentication.TranslationResponse.verify|verify} messages.
         * @function encode
         * @memberof Authentication.TranslationResponse
         * @static
         * @param {Authentication.ITranslationResponse} message TranslationResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TranslationResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.translationInfo != null && message.translationInfo.length)
                for (let i = 0; i < message.translationInfo.length; ++i)
                    $root.Authentication.TranslationInfo.encode(message.translationInfo[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Decodes a TranslationResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Authentication.TranslationResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Authentication.TranslationResponse} TranslationResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TranslationResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Authentication.TranslationResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.translationInfo && message.translationInfo.length))
                            message.translationInfo = [];
                        message.translationInfo.push($root.Authentication.TranslationInfo.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Creates a TranslationResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Authentication.TranslationResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Authentication.TranslationResponse} TranslationResponse
         */
        TranslationResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Authentication.TranslationResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Authentication.TranslationResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Authentication.TranslationResponse();
            if (object.translationInfo) {
                if (!Array.isArray(object.translationInfo))
                    throw TypeError(".Authentication.TranslationResponse.translationInfo: array expected");
                message.translationInfo = [];
                for (let i = 0; i < object.translationInfo.length; ++i) {
                    if (!$util.isObject(object.translationInfo[i]))
                        throw TypeError(".Authentication.TranslationResponse.translationInfo: object expected");
                    message.translationInfo[i] = $root.Authentication.TranslationInfo.fromObject(object.translationInfo[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a TranslationResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Authentication.TranslationResponse
         * @static
         * @param {Authentication.TranslationResponse} message TranslationResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TranslationResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.translationInfo = [];
            if (message.translationInfo && message.translationInfo.length) {
                object.translationInfo = [];
                for (let j = 0; j < message.translationInfo.length; ++j)
                    object.translationInfo[j] = $root.Authentication.TranslationInfo.toObject(message.translationInfo[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this TranslationResponse to JSON.
         * @function toJSON
         * @memberof Authentication.TranslationResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TranslationResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TranslationResponse
         * @function getTypeUrl
         * @memberof Authentication.TranslationResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TranslationResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Authentication.TranslationResponse";
        };

        return TranslationResponse;
    })();

    return Authentication;
})();
