/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars*/
import { $protobuf, $Reader, $Writer, $util, $root } from './root.js';

export const Tokens = $root.Tokens = (() => {

    /**
     * Namespace Tokens.
     * @exports Tokens
     * @namespace
     */
    const Tokens = {};

    Tokens.BreachWatchUpdateRequest = (function() {

        /**
         * Properties of a BreachWatchUpdateRequest.
         * @memberof Tokens
         * @interface IBreachWatchUpdateRequest
         * @property {Array.<Tokens.IBreachWatchRecordRequest>|null} [breachWatchRecordRequest] BreachWatchUpdateRequest breachWatchRecordRequest
         * @property {Uint8Array|null} [encryptedData] BreachWatchUpdateRequest encryptedData
         */

        /**
         * Constructs a new BreachWatchUpdateRequest.
         * @memberof Tokens
         * @classdesc Represents a BreachWatchUpdateRequest.
         * @implements IBreachWatchUpdateRequest
         * @constructor
         * @param {Tokens.IBreachWatchUpdateRequest=} [properties] Properties to set
         */
        function BreachWatchUpdateRequest(properties) {
            this.breachWatchRecordRequest = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BreachWatchUpdateRequest breachWatchRecordRequest.
         * @member {Array.<Tokens.IBreachWatchRecordRequest>} breachWatchRecordRequest
         * @memberof Tokens.BreachWatchUpdateRequest
         * @instance
         */
        BreachWatchUpdateRequest.prototype.breachWatchRecordRequest = $util.emptyArray;

        /**
         * BreachWatchUpdateRequest encryptedData.
         * @member {Uint8Array} encryptedData
         * @memberof Tokens.BreachWatchUpdateRequest
         * @instance
         */
        BreachWatchUpdateRequest.prototype.encryptedData = $util.newBuffer([]);

        /**
         * Creates a new BreachWatchUpdateRequest instance using the specified properties.
         * @function create
         * @memberof Tokens.BreachWatchUpdateRequest
         * @static
         * @param {Tokens.IBreachWatchUpdateRequest=} [properties] Properties to set
         * @returns {Tokens.BreachWatchUpdateRequest} BreachWatchUpdateRequest instance
         */
        BreachWatchUpdateRequest.create = function create(properties) {
            return new BreachWatchUpdateRequest(properties);
        };

        /**
         * Encodes the specified BreachWatchUpdateRequest message. Does not implicitly {@link Tokens.BreachWatchUpdateRequest.verify|verify} messages.
         * @function encode
         * @memberof Tokens.BreachWatchUpdateRequest
         * @static
         * @param {Tokens.IBreachWatchUpdateRequest} message BreachWatchUpdateRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BreachWatchUpdateRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.breachWatchRecordRequest != null && message.breachWatchRecordRequest.length)
                for (let i = 0; i < message.breachWatchRecordRequest.length; ++i)
                    $root.Tokens.BreachWatchRecordRequest.encode(message.breachWatchRecordRequest[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.encryptedData != null && Object.hasOwnProperty.call(message, "encryptedData"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encryptedData);
            return writer;
        };

        /**
         * Encodes the specified BreachWatchUpdateRequest message, length delimited. Does not implicitly {@link Tokens.BreachWatchUpdateRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.BreachWatchUpdateRequest
         * @static
         * @param {Tokens.IBreachWatchUpdateRequest} message BreachWatchUpdateRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BreachWatchUpdateRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BreachWatchUpdateRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.BreachWatchUpdateRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.BreachWatchUpdateRequest} BreachWatchUpdateRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BreachWatchUpdateRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.BreachWatchUpdateRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.breachWatchRecordRequest && message.breachWatchRecordRequest.length))
                            message.breachWatchRecordRequest = [];
                        message.breachWatchRecordRequest.push($root.Tokens.BreachWatchRecordRequest.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 2: {
                        message.encryptedData = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BreachWatchUpdateRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.BreachWatchUpdateRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.BreachWatchUpdateRequest} BreachWatchUpdateRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BreachWatchUpdateRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BreachWatchUpdateRequest message.
         * @function verify
         * @memberof Tokens.BreachWatchUpdateRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BreachWatchUpdateRequest.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.breachWatchRecordRequest != null && Object.hasOwnProperty.call(message, "breachWatchRecordRequest")) {
                if (!Array.isArray(message.breachWatchRecordRequest))
                    return "breachWatchRecordRequest: array expected";
                for (let i = 0; i < message.breachWatchRecordRequest.length; ++i) {
                    let error = $root.Tokens.BreachWatchRecordRequest.verify(message.breachWatchRecordRequest[i], long + 1);
                    if (error)
                        return "breachWatchRecordRequest." + error;
                }
            }
            if (message.encryptedData != null && Object.hasOwnProperty.call(message, "encryptedData"))
                if (!(message.encryptedData && typeof message.encryptedData.length === "number" || $util.isString(message.encryptedData)))
                    return "encryptedData: buffer expected";
            return null;
        };

        /**
         * Creates a BreachWatchUpdateRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.BreachWatchUpdateRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.BreachWatchUpdateRequest} BreachWatchUpdateRequest
         */
        BreachWatchUpdateRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.BreachWatchUpdateRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.BreachWatchUpdateRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.BreachWatchUpdateRequest();
            if (object.breachWatchRecordRequest) {
                if (!Array.isArray(object.breachWatchRecordRequest))
                    throw TypeError(".Tokens.BreachWatchUpdateRequest.breachWatchRecordRequest: array expected");
                message.breachWatchRecordRequest = [];
                for (let i = 0; i < object.breachWatchRecordRequest.length; ++i) {
                    if (!$util.isObject(object.breachWatchRecordRequest[i]))
                        throw TypeError(".Tokens.BreachWatchUpdateRequest.breachWatchRecordRequest: object expected");
                    message.breachWatchRecordRequest[i] = $root.Tokens.BreachWatchRecordRequest.fromObject(object.breachWatchRecordRequest[i], long + 1);
                }
            }
            if (object.encryptedData != null)
                if (typeof object.encryptedData === "string")
                    $util.base64.decode(object.encryptedData, message.encryptedData = $util.newBuffer($util.base64.length(object.encryptedData)), 0);
                else if (object.encryptedData.length >= 0)
                    message.encryptedData = object.encryptedData;
            return message;
        };

        /**
         * Creates a plain object from a BreachWatchUpdateRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.BreachWatchUpdateRequest
         * @static
         * @param {Tokens.BreachWatchUpdateRequest} message BreachWatchUpdateRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BreachWatchUpdateRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.breachWatchRecordRequest = [];
            if (options.defaults)
                if (options.bytes === String)
                    object.encryptedData = "";
                else {
                    object.encryptedData = [];
                    if (options.bytes !== Array)
                        object.encryptedData = $util.newBuffer(object.encryptedData);
                }
            if (message.breachWatchRecordRequest && message.breachWatchRecordRequest.length) {
                object.breachWatchRecordRequest = [];
                for (let j = 0; j < message.breachWatchRecordRequest.length; ++j)
                    object.breachWatchRecordRequest[j] = $root.Tokens.BreachWatchRecordRequest.toObject(message.breachWatchRecordRequest[j], options, q + 1);
            }
            if (message.encryptedData != null && Object.hasOwnProperty.call(message, "encryptedData"))
                object.encryptedData = options.bytes === String ? $util.base64.encode(message.encryptedData, 0, message.encryptedData.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedData) : message.encryptedData;
            return object;
        };

        /**
         * Converts this BreachWatchUpdateRequest to JSON.
         * @function toJSON
         * @memberof Tokens.BreachWatchUpdateRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BreachWatchUpdateRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BreachWatchUpdateRequest
         * @function getTypeUrl
         * @memberof Tokens.BreachWatchUpdateRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BreachWatchUpdateRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.BreachWatchUpdateRequest";
        };

        return BreachWatchUpdateRequest;
    })();

    Tokens.BreachWatchRecordRequest = (function() {

        /**
         * Properties of a BreachWatchRecordRequest.
         * @memberof Tokens
         * @interface IBreachWatchRecordRequest
         * @property {Uint8Array|null} [recordUid] BreachWatchRecordRequest recordUid
         * @property {Uint8Array|null} [encryptedData] BreachWatchRecordRequest encryptedData
         * @property {Tokens.BreachWatchInfoType|null} [breachWatchInfoType] BreachWatchRecordRequest breachWatchInfoType
         * @property {boolean|null} [updateUserWhoScanned] BreachWatchRecordRequest updateUserWhoScanned
         */

        /**
         * Constructs a new BreachWatchRecordRequest.
         * @memberof Tokens
         * @classdesc Represents a BreachWatchRecordRequest.
         * @implements IBreachWatchRecordRequest
         * @constructor
         * @param {Tokens.IBreachWatchRecordRequest=} [properties] Properties to set
         */
        function BreachWatchRecordRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BreachWatchRecordRequest recordUid.
         * @member {Uint8Array} recordUid
         * @memberof Tokens.BreachWatchRecordRequest
         * @instance
         */
        BreachWatchRecordRequest.prototype.recordUid = $util.newBuffer([]);

        /**
         * BreachWatchRecordRequest encryptedData.
         * @member {Uint8Array} encryptedData
         * @memberof Tokens.BreachWatchRecordRequest
         * @instance
         */
        BreachWatchRecordRequest.prototype.encryptedData = $util.newBuffer([]);

        /**
         * BreachWatchRecordRequest breachWatchInfoType.
         * @member {Tokens.BreachWatchInfoType} breachWatchInfoType
         * @memberof Tokens.BreachWatchRecordRequest
         * @instance
         */
        BreachWatchRecordRequest.prototype.breachWatchInfoType = 0;

        /**
         * BreachWatchRecordRequest updateUserWhoScanned.
         * @member {boolean} updateUserWhoScanned
         * @memberof Tokens.BreachWatchRecordRequest
         * @instance
         */
        BreachWatchRecordRequest.prototype.updateUserWhoScanned = false;

        /**
         * Creates a new BreachWatchRecordRequest instance using the specified properties.
         * @function create
         * @memberof Tokens.BreachWatchRecordRequest
         * @static
         * @param {Tokens.IBreachWatchRecordRequest=} [properties] Properties to set
         * @returns {Tokens.BreachWatchRecordRequest} BreachWatchRecordRequest instance
         */
        BreachWatchRecordRequest.create = function create(properties) {
            return new BreachWatchRecordRequest(properties);
        };

        /**
         * Encodes the specified BreachWatchRecordRequest message. Does not implicitly {@link Tokens.BreachWatchRecordRequest.verify|verify} messages.
         * @function encode
         * @memberof Tokens.BreachWatchRecordRequest
         * @static
         * @param {Tokens.IBreachWatchRecordRequest} message BreachWatchRecordRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BreachWatchRecordRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.recordUid);
            if (message.encryptedData != null && Object.hasOwnProperty.call(message, "encryptedData"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.encryptedData);
            if (message.breachWatchInfoType != null && Object.hasOwnProperty.call(message, "breachWatchInfoType"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.breachWatchInfoType);
            if (message.updateUserWhoScanned != null && Object.hasOwnProperty.call(message, "updateUserWhoScanned"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.updateUserWhoScanned);
            return writer;
        };

        /**
         * Encodes the specified BreachWatchRecordRequest message, length delimited. Does not implicitly {@link Tokens.BreachWatchRecordRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.BreachWatchRecordRequest
         * @static
         * @param {Tokens.IBreachWatchRecordRequest} message BreachWatchRecordRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BreachWatchRecordRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BreachWatchRecordRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.BreachWatchRecordRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.BreachWatchRecordRequest} BreachWatchRecordRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BreachWatchRecordRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.BreachWatchRecordRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.recordUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.encryptedData = reader.bytes();
                        break;
                    }
                case 3: {
                        message.breachWatchInfoType = reader.int32();
                        break;
                    }
                case 4: {
                        message.updateUserWhoScanned = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BreachWatchRecordRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.BreachWatchRecordRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.BreachWatchRecordRequest} BreachWatchRecordRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BreachWatchRecordRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BreachWatchRecordRequest message.
         * @function verify
         * @memberof Tokens.BreachWatchRecordRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BreachWatchRecordRequest.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                if (!(message.recordUid && typeof message.recordUid.length === "number" || $util.isString(message.recordUid)))
                    return "recordUid: buffer expected";
            if (message.encryptedData != null && Object.hasOwnProperty.call(message, "encryptedData"))
                if (!(message.encryptedData && typeof message.encryptedData.length === "number" || $util.isString(message.encryptedData)))
                    return "encryptedData: buffer expected";
            if (message.breachWatchInfoType != null && Object.hasOwnProperty.call(message, "breachWatchInfoType"))
                switch (message.breachWatchInfoType) {
                default:
                    return "breachWatchInfoType: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.updateUserWhoScanned != null && Object.hasOwnProperty.call(message, "updateUserWhoScanned"))
                if (typeof message.updateUserWhoScanned !== "boolean")
                    return "updateUserWhoScanned: boolean expected";
            return null;
        };

        /**
         * Creates a BreachWatchRecordRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.BreachWatchRecordRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.BreachWatchRecordRequest} BreachWatchRecordRequest
         */
        BreachWatchRecordRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.BreachWatchRecordRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.BreachWatchRecordRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.BreachWatchRecordRequest();
            if (object.recordUid != null)
                if (typeof object.recordUid === "string")
                    $util.base64.decode(object.recordUid, message.recordUid = $util.newBuffer($util.base64.length(object.recordUid)), 0);
                else if (object.recordUid.length >= 0)
                    message.recordUid = object.recordUid;
            if (object.encryptedData != null)
                if (typeof object.encryptedData === "string")
                    $util.base64.decode(object.encryptedData, message.encryptedData = $util.newBuffer($util.base64.length(object.encryptedData)), 0);
                else if (object.encryptedData.length >= 0)
                    message.encryptedData = object.encryptedData;
            switch (object.breachWatchInfoType) {
            default:
                if (typeof object.breachWatchInfoType === "number") {
                    message.breachWatchInfoType = object.breachWatchInfoType;
                    break;
                }
                break;
            case "RECORD":
            case 0:
                message.breachWatchInfoType = 0;
                break;
            case "ALTERNATE_PASSWORD":
            case 1:
                message.breachWatchInfoType = 1;
                break;
            }
            if (object.updateUserWhoScanned != null)
                message.updateUserWhoScanned = Boolean(object.updateUserWhoScanned);
            return message;
        };

        /**
         * Creates a plain object from a BreachWatchRecordRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.BreachWatchRecordRequest
         * @static
         * @param {Tokens.BreachWatchRecordRequest} message BreachWatchRecordRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BreachWatchRecordRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.recordUid = "";
                else {
                    object.recordUid = [];
                    if (options.bytes !== Array)
                        object.recordUid = $util.newBuffer(object.recordUid);
                }
                if (options.bytes === String)
                    object.encryptedData = "";
                else {
                    object.encryptedData = [];
                    if (options.bytes !== Array)
                        object.encryptedData = $util.newBuffer(object.encryptedData);
                }
                object.breachWatchInfoType = options.enums === String ? "RECORD" : 0;
                object.updateUserWhoScanned = false;
            }
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                object.recordUid = options.bytes === String ? $util.base64.encode(message.recordUid, 0, message.recordUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.recordUid) : message.recordUid;
            if (message.encryptedData != null && Object.hasOwnProperty.call(message, "encryptedData"))
                object.encryptedData = options.bytes === String ? $util.base64.encode(message.encryptedData, 0, message.encryptedData.length) : options.bytes === Array ? Array.prototype.slice.call(message.encryptedData) : message.encryptedData;
            if (message.breachWatchInfoType != null && Object.hasOwnProperty.call(message, "breachWatchInfoType"))
                object.breachWatchInfoType = options.enums === String ? $root.Tokens.BreachWatchInfoType[message.breachWatchInfoType] === undefined ? message.breachWatchInfoType : $root.Tokens.BreachWatchInfoType[message.breachWatchInfoType] : message.breachWatchInfoType;
            if (message.updateUserWhoScanned != null && Object.hasOwnProperty.call(message, "updateUserWhoScanned"))
                object.updateUserWhoScanned = message.updateUserWhoScanned;
            return object;
        };

        /**
         * Converts this BreachWatchRecordRequest to JSON.
         * @function toJSON
         * @memberof Tokens.BreachWatchRecordRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BreachWatchRecordRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BreachWatchRecordRequest
         * @function getTypeUrl
         * @memberof Tokens.BreachWatchRecordRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BreachWatchRecordRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.BreachWatchRecordRequest";
        };

        return BreachWatchRecordRequest;
    })();

    /**
     * BreachWatchInfoType enum.
     * @name Tokens.BreachWatchInfoType
     * @enum {number}
     * @property {number} RECORD=0 RECORD value
     * @property {number} ALTERNATE_PASSWORD=1 ALTERNATE_PASSWORD value
     */
    Tokens.BreachWatchInfoType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "RECORD"] = 0;
        values[valuesById[1] = "ALTERNATE_PASSWORD"] = 1;
        return values;
    })();

    Tokens.BreachWatchData = (function() {

        /**
         * Properties of a BreachWatchData.
         * @memberof Tokens
         * @interface IBreachWatchData
         * @property {Array.<Tokens.IBWPassword>|null} [passwords] BreachWatchData passwords
         * @property {Array.<Tokens.IBWPassword>|null} [emails] BreachWatchData emails
         * @property {Array.<Tokens.IBWPassword>|null} [domains] BreachWatchData domains
         */

        /**
         * Constructs a new BreachWatchData.
         * @memberof Tokens
         * @classdesc Represents a BreachWatchData.
         * @implements IBreachWatchData
         * @constructor
         * @param {Tokens.IBreachWatchData=} [properties] Properties to set
         */
        function BreachWatchData(properties) {
            this.passwords = [];
            this.emails = [];
            this.domains = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BreachWatchData passwords.
         * @member {Array.<Tokens.IBWPassword>} passwords
         * @memberof Tokens.BreachWatchData
         * @instance
         */
        BreachWatchData.prototype.passwords = $util.emptyArray;

        /**
         * BreachWatchData emails.
         * @member {Array.<Tokens.IBWPassword>} emails
         * @memberof Tokens.BreachWatchData
         * @instance
         */
        BreachWatchData.prototype.emails = $util.emptyArray;

        /**
         * BreachWatchData domains.
         * @member {Array.<Tokens.IBWPassword>} domains
         * @memberof Tokens.BreachWatchData
         * @instance
         */
        BreachWatchData.prototype.domains = $util.emptyArray;

        /**
         * Creates a new BreachWatchData instance using the specified properties.
         * @function create
         * @memberof Tokens.BreachWatchData
         * @static
         * @param {Tokens.IBreachWatchData=} [properties] Properties to set
         * @returns {Tokens.BreachWatchData} BreachWatchData instance
         */
        BreachWatchData.create = function create(properties) {
            return new BreachWatchData(properties);
        };

        /**
         * Encodes the specified BreachWatchData message. Does not implicitly {@link Tokens.BreachWatchData.verify|verify} messages.
         * @function encode
         * @memberof Tokens.BreachWatchData
         * @static
         * @param {Tokens.IBreachWatchData} message BreachWatchData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BreachWatchData.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.passwords != null && message.passwords.length)
                for (let i = 0; i < message.passwords.length; ++i)
                    $root.Tokens.BWPassword.encode(message.passwords[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.emails != null && message.emails.length)
                for (let i = 0; i < message.emails.length; ++i)
                    $root.Tokens.BWPassword.encode(message.emails[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.domains != null && message.domains.length)
                for (let i = 0; i < message.domains.length; ++i)
                    $root.Tokens.BWPassword.encode(message.domains[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified BreachWatchData message, length delimited. Does not implicitly {@link Tokens.BreachWatchData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.BreachWatchData
         * @static
         * @param {Tokens.IBreachWatchData} message BreachWatchData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BreachWatchData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BreachWatchData message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.BreachWatchData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.BreachWatchData} BreachWatchData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BreachWatchData.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.BreachWatchData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.passwords && message.passwords.length))
                            message.passwords = [];
                        message.passwords.push($root.Tokens.BWPassword.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 2: {
                        if (!(message.emails && message.emails.length))
                            message.emails = [];
                        message.emails.push($root.Tokens.BWPassword.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 3: {
                        if (!(message.domains && message.domains.length))
                            message.domains = [];
                        message.domains.push($root.Tokens.BWPassword.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BreachWatchData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.BreachWatchData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.BreachWatchData} BreachWatchData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BreachWatchData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BreachWatchData message.
         * @function verify
         * @memberof Tokens.BreachWatchData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BreachWatchData.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.passwords != null && Object.hasOwnProperty.call(message, "passwords")) {
                if (!Array.isArray(message.passwords))
                    return "passwords: array expected";
                for (let i = 0; i < message.passwords.length; ++i) {
                    let error = $root.Tokens.BWPassword.verify(message.passwords[i], long + 1);
                    if (error)
                        return "passwords." + error;
                }
            }
            if (message.emails != null && Object.hasOwnProperty.call(message, "emails")) {
                if (!Array.isArray(message.emails))
                    return "emails: array expected";
                for (let i = 0; i < message.emails.length; ++i) {
                    let error = $root.Tokens.BWPassword.verify(message.emails[i], long + 1);
                    if (error)
                        return "emails." + error;
                }
            }
            if (message.domains != null && Object.hasOwnProperty.call(message, "domains")) {
                if (!Array.isArray(message.domains))
                    return "domains: array expected";
                for (let i = 0; i < message.domains.length; ++i) {
                    let error = $root.Tokens.BWPassword.verify(message.domains[i], long + 1);
                    if (error)
                        return "domains." + error;
                }
            }
            return null;
        };

        /**
         * Creates a BreachWatchData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.BreachWatchData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.BreachWatchData} BreachWatchData
         */
        BreachWatchData.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.BreachWatchData)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.BreachWatchData: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.BreachWatchData();
            if (object.passwords) {
                if (!Array.isArray(object.passwords))
                    throw TypeError(".Tokens.BreachWatchData.passwords: array expected");
                message.passwords = [];
                for (let i = 0; i < object.passwords.length; ++i) {
                    if (!$util.isObject(object.passwords[i]))
                        throw TypeError(".Tokens.BreachWatchData.passwords: object expected");
                    message.passwords[i] = $root.Tokens.BWPassword.fromObject(object.passwords[i], long + 1);
                }
            }
            if (object.emails) {
                if (!Array.isArray(object.emails))
                    throw TypeError(".Tokens.BreachWatchData.emails: array expected");
                message.emails = [];
                for (let i = 0; i < object.emails.length; ++i) {
                    if (!$util.isObject(object.emails[i]))
                        throw TypeError(".Tokens.BreachWatchData.emails: object expected");
                    message.emails[i] = $root.Tokens.BWPassword.fromObject(object.emails[i], long + 1);
                }
            }
            if (object.domains) {
                if (!Array.isArray(object.domains))
                    throw TypeError(".Tokens.BreachWatchData.domains: array expected");
                message.domains = [];
                for (let i = 0; i < object.domains.length; ++i) {
                    if (!$util.isObject(object.domains[i]))
                        throw TypeError(".Tokens.BreachWatchData.domains: object expected");
                    message.domains[i] = $root.Tokens.BWPassword.fromObject(object.domains[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a BreachWatchData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.BreachWatchData
         * @static
         * @param {Tokens.BreachWatchData} message BreachWatchData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BreachWatchData.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.passwords = [];
                object.emails = [];
                object.domains = [];
            }
            if (message.passwords && message.passwords.length) {
                object.passwords = [];
                for (let j = 0; j < message.passwords.length; ++j)
                    object.passwords[j] = $root.Tokens.BWPassword.toObject(message.passwords[j], options, q + 1);
            }
            if (message.emails && message.emails.length) {
                object.emails = [];
                for (let j = 0; j < message.emails.length; ++j)
                    object.emails[j] = $root.Tokens.BWPassword.toObject(message.emails[j], options, q + 1);
            }
            if (message.domains && message.domains.length) {
                object.domains = [];
                for (let j = 0; j < message.domains.length; ++j)
                    object.domains[j] = $root.Tokens.BWPassword.toObject(message.domains[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this BreachWatchData to JSON.
         * @function toJSON
         * @memberof Tokens.BreachWatchData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BreachWatchData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BreachWatchData
         * @function getTypeUrl
         * @memberof Tokens.BreachWatchData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BreachWatchData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.BreachWatchData";
        };

        return BreachWatchData;
    })();

    Tokens.BWPassword = (function() {

        /**
         * Properties of a BWPassword.
         * @memberof Tokens
         * @interface IBWPassword
         * @property {string|null} [value] BWPassword value
         * @property {number|null} [resolved] BWPassword resolved
         * @property {Tokens.BWStatus|null} [status] BWPassword status
         * @property {Uint8Array|null} [euid] BWPassword euid
         */

        /**
         * Constructs a new BWPassword.
         * @memberof Tokens
         * @classdesc Represents a BWPassword.
         * @implements IBWPassword
         * @constructor
         * @param {Tokens.IBWPassword=} [properties] Properties to set
         */
        function BWPassword(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BWPassword value.
         * @member {string} value
         * @memberof Tokens.BWPassword
         * @instance
         */
        BWPassword.prototype.value = "";

        /**
         * BWPassword resolved.
         * @member {number} resolved
         * @memberof Tokens.BWPassword
         * @instance
         */
        BWPassword.prototype.resolved = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        /**
         * BWPassword status.
         * @member {Tokens.BWStatus} status
         * @memberof Tokens.BWPassword
         * @instance
         */
        BWPassword.prototype.status = 0;

        /**
         * BWPassword euid.
         * @member {Uint8Array} euid
         * @memberof Tokens.BWPassword
         * @instance
         */
        BWPassword.prototype.euid = $util.newBuffer([]);

        /**
         * Creates a new BWPassword instance using the specified properties.
         * @function create
         * @memberof Tokens.BWPassword
         * @static
         * @param {Tokens.IBWPassword=} [properties] Properties to set
         * @returns {Tokens.BWPassword} BWPassword instance
         */
        BWPassword.create = function create(properties) {
            return new BWPassword(properties);
        };

        /**
         * Encodes the specified BWPassword message. Does not implicitly {@link Tokens.BWPassword.verify|verify} messages.
         * @function encode
         * @memberof Tokens.BWPassword
         * @static
         * @param {Tokens.IBWPassword} message BWPassword message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BWPassword.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.value);
            if (message.resolved != null && Object.hasOwnProperty.call(message, "resolved"))
                writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.resolved);
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.status);
            if (message.euid != null && Object.hasOwnProperty.call(message, "euid"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.euid);
            return writer;
        };

        /**
         * Encodes the specified BWPassword message, length delimited. Does not implicitly {@link Tokens.BWPassword.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.BWPassword
         * @static
         * @param {Tokens.IBWPassword} message BWPassword message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BWPassword.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BWPassword message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.BWPassword
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.BWPassword} BWPassword
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BWPassword.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.BWPassword();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.value = reader.string();
                        break;
                    }
                case 2: {
                        message.resolved = reader.uint64();
                        break;
                    }
                case 3: {
                        message.status = reader.int32();
                        break;
                    }
                case 4: {
                        message.euid = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BWPassword message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.BWPassword
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.BWPassword} BWPassword
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BWPassword.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BWPassword message.
         * @function verify
         * @memberof Tokens.BWPassword
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BWPassword.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                if (!$util.isString(message.value))
                    return "value: string expected";
            if (message.resolved != null && Object.hasOwnProperty.call(message, "resolved"))
                if (!$util.isInteger(message.resolved) && !(message.resolved && $util.isInteger(message.resolved.low) && $util.isInteger(message.resolved.high)))
                    return "resolved: integer|Long expected";
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                switch (message.status) {
                default:
                    return "status: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                    break;
                }
            if (message.euid != null && Object.hasOwnProperty.call(message, "euid"))
                if (!(message.euid && typeof message.euid.length === "number" || $util.isString(message.euid)))
                    return "euid: buffer expected";
            return null;
        };

        /**
         * Creates a BWPassword message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.BWPassword
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.BWPassword} BWPassword
         */
        BWPassword.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.BWPassword)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.BWPassword: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.BWPassword();
            if (object.value != null)
                message.value = String(object.value);
            if (object.resolved != null)
                if ($util.Long)
                    message.resolved = $util.Long.fromValue(object.resolved, true);
                else if (typeof object.resolved === "string")
                    message.resolved = parseInt(object.resolved, 10);
                else if (typeof object.resolved === "number")
                    message.resolved = object.resolved;
                else if (typeof object.resolved === "object")
                    message.resolved = new $util.LongBits(object.resolved.low >>> 0, object.resolved.high >>> 0).toNumber(true);
            switch (object.status) {
            default:
                if (typeof object.status === "number") {
                    message.status = object.status;
                    break;
                }
                break;
            case "GOOD":
            case 0:
                message.status = 0;
                break;
            case "CHANGED":
            case 1:
                message.status = 1;
                break;
            case "WEAK":
            case 2:
                message.status = 2;
                break;
            case "BREACHED":
            case 3:
                message.status = 3;
                break;
            case "IGNORE":
            case 4:
                message.status = 4;
                break;
            }
            if (object.euid != null)
                if (typeof object.euid === "string")
                    $util.base64.decode(object.euid, message.euid = $util.newBuffer($util.base64.length(object.euid)), 0);
                else if (object.euid.length >= 0)
                    message.euid = object.euid;
            return message;
        };

        /**
         * Creates a plain object from a BWPassword message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.BWPassword
         * @static
         * @param {Tokens.BWPassword} message BWPassword
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BWPassword.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.value = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, true);
                    object.resolved = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.resolved = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.status = options.enums === String ? "GOOD" : 0;
                if (options.bytes === String)
                    object.euid = "";
                else {
                    object.euid = [];
                    if (options.bytes !== Array)
                        object.euid = $util.newBuffer(object.euid);
                }
            }
            if (message.value != null && Object.hasOwnProperty.call(message, "value"))
                object.value = message.value;
            if (message.resolved != null && Object.hasOwnProperty.call(message, "resolved"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.resolved = typeof message.resolved === "number" ? BigInt(message.resolved) : $util.Long.fromBits(message.resolved.low >>> 0, message.resolved.high >>> 0, true).toBigInt();
                else if (typeof message.resolved === "number")
                    object.resolved = options.longs === String ? String(message.resolved) : message.resolved;
                else
                    object.resolved = options.longs === String ? $util.Long.prototype.toString.call(message.resolved) : options.longs === Number ? new $util.LongBits(message.resolved.low >>> 0, message.resolved.high >>> 0).toNumber(true) : message.resolved;
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                object.status = options.enums === String ? $root.Tokens.BWStatus[message.status] === undefined ? message.status : $root.Tokens.BWStatus[message.status] : message.status;
            if (message.euid != null && Object.hasOwnProperty.call(message, "euid"))
                object.euid = options.bytes === String ? $util.base64.encode(message.euid, 0, message.euid.length) : options.bytes === Array ? Array.prototype.slice.call(message.euid) : message.euid;
            return object;
        };

        /**
         * Converts this BWPassword to JSON.
         * @function toJSON
         * @memberof Tokens.BWPassword
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BWPassword.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BWPassword
         * @function getTypeUrl
         * @memberof Tokens.BWPassword
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BWPassword.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.BWPassword";
        };

        return BWPassword;
    })();

    /**
     * BWStatus enum.
     * @name Tokens.BWStatus
     * @enum {number}
     * @property {number} GOOD=0 GOOD value
     * @property {number} CHANGED=1 CHANGED value
     * @property {number} WEAK=2 WEAK value
     * @property {number} BREACHED=3 BREACHED value
     * @property {number} IGNORE=4 IGNORE value
     */
    Tokens.BWStatus = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "GOOD"] = 0;
        values[valuesById[1] = "CHANGED"] = 1;
        values[valuesById[2] = "WEAK"] = 2;
        values[valuesById[3] = "BREACHED"] = 3;
        values[valuesById[4] = "IGNORE"] = 4;
        return values;
    })();

    Tokens.DeviceToken = (function() {

        /**
         * Properties of a DeviceToken.
         * @memberof Tokens
         * @interface IDeviceToken
         * @property {number|null} [deviceId] DeviceToken deviceId
         * @property {number|null} [region] DeviceToken region
         */

        /**
         * Constructs a new DeviceToken.
         * @memberof Tokens
         * @classdesc Represents a DeviceToken.
         * @implements IDeviceToken
         * @constructor
         * @param {Tokens.IDeviceToken=} [properties] Properties to set
         */
        function DeviceToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeviceToken deviceId.
         * @member {number} deviceId
         * @memberof Tokens.DeviceToken
         * @instance
         */
        DeviceToken.prototype.deviceId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * DeviceToken region.
         * @member {number} region
         * @memberof Tokens.DeviceToken
         * @instance
         */
        DeviceToken.prototype.region = 0;

        /**
         * Creates a new DeviceToken instance using the specified properties.
         * @function create
         * @memberof Tokens.DeviceToken
         * @static
         * @param {Tokens.IDeviceToken=} [properties] Properties to set
         * @returns {Tokens.DeviceToken} DeviceToken instance
         */
        DeviceToken.create = function create(properties) {
            return new DeviceToken(properties);
        };

        /**
         * Encodes the specified DeviceToken message. Does not implicitly {@link Tokens.DeviceToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.DeviceToken
         * @static
         * @param {Tokens.IDeviceToken} message DeviceToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.deviceId);
            if (message.region != null && Object.hasOwnProperty.call(message, "region"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.region);
            return writer;
        };

        /**
         * Encodes the specified DeviceToken message, length delimited. Does not implicitly {@link Tokens.DeviceToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.DeviceToken
         * @static
         * @param {Tokens.IDeviceToken} message DeviceToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DeviceToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.DeviceToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.DeviceToken} DeviceToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.DeviceToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.deviceId = reader.int64();
                        break;
                    }
                case 2: {
                        message.region = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a DeviceToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.DeviceToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.DeviceToken} DeviceToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeviceToken message.
         * @function verify
         * @memberof Tokens.DeviceToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeviceToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                if (!$util.isInteger(message.deviceId) && !(message.deviceId && $util.isInteger(message.deviceId.low) && $util.isInteger(message.deviceId.high)))
                    return "deviceId: integer|Long expected";
            if (message.region != null && Object.hasOwnProperty.call(message, "region"))
                if (!$util.isInteger(message.region))
                    return "region: integer expected";
            return null;
        };

        /**
         * Creates a DeviceToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.DeviceToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.DeviceToken} DeviceToken
         */
        DeviceToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.DeviceToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.DeviceToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.DeviceToken();
            if (object.deviceId != null)
                if ($util.Long)
                    message.deviceId = $util.Long.fromValue(object.deviceId, false);
                else if (typeof object.deviceId === "string")
                    message.deviceId = parseInt(object.deviceId, 10);
                else if (typeof object.deviceId === "number")
                    message.deviceId = object.deviceId;
                else if (typeof object.deviceId === "object")
                    message.deviceId = new $util.LongBits(object.deviceId.low >>> 0, object.deviceId.high >>> 0).toNumber();
            if (object.region != null)
                message.region = object.region | 0;
            return message;
        };

        /**
         * Creates a plain object from a DeviceToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.DeviceToken
         * @static
         * @param {Tokens.DeviceToken} message DeviceToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeviceToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.deviceId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.deviceId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.region = 0;
            }
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.deviceId = typeof message.deviceId === "number" ? BigInt(message.deviceId) : $util.Long.fromBits(message.deviceId.low >>> 0, message.deviceId.high >>> 0, false).toBigInt();
                else if (typeof message.deviceId === "number")
                    object.deviceId = options.longs === String ? String(message.deviceId) : message.deviceId;
                else
                    object.deviceId = options.longs === String ? $util.Long.prototype.toString.call(message.deviceId) : options.longs === Number ? new $util.LongBits(message.deviceId.low >>> 0, message.deviceId.high >>> 0).toNumber() : message.deviceId;
            if (message.region != null && Object.hasOwnProperty.call(message, "region"))
                object.region = message.region;
            return object;
        };

        /**
         * Converts this DeviceToken to JSON.
         * @function toJSON
         * @memberof Tokens.DeviceToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeviceToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeviceToken
         * @function getTypeUrl
         * @memberof Tokens.DeviceToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeviceToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.DeviceToken";
        };

        return DeviceToken;
    })();

    Tokens.SSOLoginToken = (function() {

        /**
         * Properties of a SSOLoginToken.
         * @memberof Tokens
         * @interface ISSOLoginToken
         * @property {number|null} [ssoServiceProviderId] SSOLoginToken ssoServiceProviderId
         * @property {string|null} [username] SSOLoginToken username
         * @property {string|null} [alias] SSOLoginToken alias
         * @property {string|null} [displayname] SSOLoginToken displayname
         */

        /**
         * Constructs a new SSOLoginToken.
         * @memberof Tokens
         * @classdesc Represents a SSOLoginToken.
         * @implements ISSOLoginToken
         * @constructor
         * @param {Tokens.ISSOLoginToken=} [properties] Properties to set
         */
        function SSOLoginToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SSOLoginToken ssoServiceProviderId.
         * @member {number} ssoServiceProviderId
         * @memberof Tokens.SSOLoginToken
         * @instance
         */
        SSOLoginToken.prototype.ssoServiceProviderId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SSOLoginToken username.
         * @member {string} username
         * @memberof Tokens.SSOLoginToken
         * @instance
         */
        SSOLoginToken.prototype.username = "";

        /**
         * SSOLoginToken alias.
         * @member {string} alias
         * @memberof Tokens.SSOLoginToken
         * @instance
         */
        SSOLoginToken.prototype.alias = "";

        /**
         * SSOLoginToken displayname.
         * @member {string} displayname
         * @memberof Tokens.SSOLoginToken
         * @instance
         */
        SSOLoginToken.prototype.displayname = "";

        /**
         * Creates a new SSOLoginToken instance using the specified properties.
         * @function create
         * @memberof Tokens.SSOLoginToken
         * @static
         * @param {Tokens.ISSOLoginToken=} [properties] Properties to set
         * @returns {Tokens.SSOLoginToken} SSOLoginToken instance
         */
        SSOLoginToken.create = function create(properties) {
            return new SSOLoginToken(properties);
        };

        /**
         * Encodes the specified SSOLoginToken message. Does not implicitly {@link Tokens.SSOLoginToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.SSOLoginToken
         * @static
         * @param {Tokens.ISSOLoginToken} message SSOLoginToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSOLoginToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ssoServiceProviderId != null && Object.hasOwnProperty.call(message, "ssoServiceProviderId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.ssoServiceProviderId);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.username);
            if (message.alias != null && Object.hasOwnProperty.call(message, "alias"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.alias);
            if (message.displayname != null && Object.hasOwnProperty.call(message, "displayname"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.displayname);
            return writer;
        };

        /**
         * Encodes the specified SSOLoginToken message, length delimited. Does not implicitly {@link Tokens.SSOLoginToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.SSOLoginToken
         * @static
         * @param {Tokens.ISSOLoginToken} message SSOLoginToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SSOLoginToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SSOLoginToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.SSOLoginToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.SSOLoginToken} SSOLoginToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSOLoginToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.SSOLoginToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ssoServiceProviderId = reader.int64();
                        break;
                    }
                case 2: {
                        message.username = reader.string();
                        break;
                    }
                case 3: {
                        message.alias = reader.string();
                        break;
                    }
                case 4: {
                        message.displayname = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SSOLoginToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.SSOLoginToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.SSOLoginToken} SSOLoginToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SSOLoginToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SSOLoginToken message.
         * @function verify
         * @memberof Tokens.SSOLoginToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SSOLoginToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ssoServiceProviderId != null && Object.hasOwnProperty.call(message, "ssoServiceProviderId"))
                if (!$util.isInteger(message.ssoServiceProviderId) && !(message.ssoServiceProviderId && $util.isInteger(message.ssoServiceProviderId.low) && $util.isInteger(message.ssoServiceProviderId.high)))
                    return "ssoServiceProviderId: integer|Long expected";
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                if (!$util.isString(message.username))
                    return "username: string expected";
            if (message.alias != null && Object.hasOwnProperty.call(message, "alias"))
                if (!$util.isString(message.alias))
                    return "alias: string expected";
            if (message.displayname != null && Object.hasOwnProperty.call(message, "displayname"))
                if (!$util.isString(message.displayname))
                    return "displayname: string expected";
            return null;
        };

        /**
         * Creates a SSOLoginToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.SSOLoginToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.SSOLoginToken} SSOLoginToken
         */
        SSOLoginToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.SSOLoginToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.SSOLoginToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.SSOLoginToken();
            if (object.ssoServiceProviderId != null)
                if ($util.Long)
                    message.ssoServiceProviderId = $util.Long.fromValue(object.ssoServiceProviderId, false);
                else if (typeof object.ssoServiceProviderId === "string")
                    message.ssoServiceProviderId = parseInt(object.ssoServiceProviderId, 10);
                else if (typeof object.ssoServiceProviderId === "number")
                    message.ssoServiceProviderId = object.ssoServiceProviderId;
                else if (typeof object.ssoServiceProviderId === "object")
                    message.ssoServiceProviderId = new $util.LongBits(object.ssoServiceProviderId.low >>> 0, object.ssoServiceProviderId.high >>> 0).toNumber();
            if (object.username != null)
                message.username = String(object.username);
            if (object.alias != null)
                message.alias = String(object.alias);
            if (object.displayname != null)
                message.displayname = String(object.displayname);
            return message;
        };

        /**
         * Creates a plain object from a SSOLoginToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.SSOLoginToken
         * @static
         * @param {Tokens.SSOLoginToken} message SSOLoginToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SSOLoginToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.ssoServiceProviderId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.ssoServiceProviderId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.username = "";
                object.alias = "";
                object.displayname = "";
            }
            if (message.ssoServiceProviderId != null && Object.hasOwnProperty.call(message, "ssoServiceProviderId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.ssoServiceProviderId = typeof message.ssoServiceProviderId === "number" ? BigInt(message.ssoServiceProviderId) : $util.Long.fromBits(message.ssoServiceProviderId.low >>> 0, message.ssoServiceProviderId.high >>> 0, false).toBigInt();
                else if (typeof message.ssoServiceProviderId === "number")
                    object.ssoServiceProviderId = options.longs === String ? String(message.ssoServiceProviderId) : message.ssoServiceProviderId;
                else
                    object.ssoServiceProviderId = options.longs === String ? $util.Long.prototype.toString.call(message.ssoServiceProviderId) : options.longs === Number ? new $util.LongBits(message.ssoServiceProviderId.low >>> 0, message.ssoServiceProviderId.high >>> 0).toNumber() : message.ssoServiceProviderId;
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.alias != null && Object.hasOwnProperty.call(message, "alias"))
                object.alias = message.alias;
            if (message.displayname != null && Object.hasOwnProperty.call(message, "displayname"))
                object.displayname = message.displayname;
            return object;
        };

        /**
         * Converts this SSOLoginToken to JSON.
         * @function toJSON
         * @memberof Tokens.SSOLoginToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SSOLoginToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SSOLoginToken
         * @function getTypeUrl
         * @memberof Tokens.SSOLoginToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SSOLoginToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.SSOLoginToken";
        };

        return SSOLoginToken;
    })();

    Tokens.PasskeyLoginToken = (function() {

        /**
         * Properties of a PasskeyLoginToken.
         * @memberof Tokens
         * @interface IPasskeyLoginToken
         * @property {number|null} [passkeyVerificationMS] PasskeyLoginToken passkeyVerificationMS
         * @property {Uint8Array|null} [challenge] PasskeyLoginToken challenge
         */

        /**
         * Constructs a new PasskeyLoginToken.
         * @memberof Tokens
         * @classdesc Represents a PasskeyLoginToken.
         * @implements IPasskeyLoginToken
         * @constructor
         * @param {Tokens.IPasskeyLoginToken=} [properties] Properties to set
         */
        function PasskeyLoginToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * PasskeyLoginToken passkeyVerificationMS.
         * @member {number} passkeyVerificationMS
         * @memberof Tokens.PasskeyLoginToken
         * @instance
         */
        PasskeyLoginToken.prototype.passkeyVerificationMS = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * PasskeyLoginToken challenge.
         * @member {Uint8Array} challenge
         * @memberof Tokens.PasskeyLoginToken
         * @instance
         */
        PasskeyLoginToken.prototype.challenge = $util.newBuffer([]);

        /**
         * Creates a new PasskeyLoginToken instance using the specified properties.
         * @function create
         * @memberof Tokens.PasskeyLoginToken
         * @static
         * @param {Tokens.IPasskeyLoginToken=} [properties] Properties to set
         * @returns {Tokens.PasskeyLoginToken} PasskeyLoginToken instance
         */
        PasskeyLoginToken.create = function create(properties) {
            return new PasskeyLoginToken(properties);
        };

        /**
         * Encodes the specified PasskeyLoginToken message. Does not implicitly {@link Tokens.PasskeyLoginToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.PasskeyLoginToken
         * @static
         * @param {Tokens.IPasskeyLoginToken} message PasskeyLoginToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PasskeyLoginToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.passkeyVerificationMS != null && Object.hasOwnProperty.call(message, "passkeyVerificationMS"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.passkeyVerificationMS);
            if (message.challenge != null && Object.hasOwnProperty.call(message, "challenge"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.challenge);
            return writer;
        };

        /**
         * Encodes the specified PasskeyLoginToken message, length delimited. Does not implicitly {@link Tokens.PasskeyLoginToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.PasskeyLoginToken
         * @static
         * @param {Tokens.IPasskeyLoginToken} message PasskeyLoginToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        PasskeyLoginToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a PasskeyLoginToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.PasskeyLoginToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.PasskeyLoginToken} PasskeyLoginToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PasskeyLoginToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.PasskeyLoginToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.passkeyVerificationMS = reader.int64();
                        break;
                    }
                case 2: {
                        message.challenge = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a PasskeyLoginToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.PasskeyLoginToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.PasskeyLoginToken} PasskeyLoginToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        PasskeyLoginToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a PasskeyLoginToken message.
         * @function verify
         * @memberof Tokens.PasskeyLoginToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        PasskeyLoginToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.passkeyVerificationMS != null && Object.hasOwnProperty.call(message, "passkeyVerificationMS"))
                if (!$util.isInteger(message.passkeyVerificationMS) && !(message.passkeyVerificationMS && $util.isInteger(message.passkeyVerificationMS.low) && $util.isInteger(message.passkeyVerificationMS.high)))
                    return "passkeyVerificationMS: integer|Long expected";
            if (message.challenge != null && Object.hasOwnProperty.call(message, "challenge"))
                if (!(message.challenge && typeof message.challenge.length === "number" || $util.isString(message.challenge)))
                    return "challenge: buffer expected";
            return null;
        };

        /**
         * Creates a PasskeyLoginToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.PasskeyLoginToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.PasskeyLoginToken} PasskeyLoginToken
         */
        PasskeyLoginToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.PasskeyLoginToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.PasskeyLoginToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.PasskeyLoginToken();
            if (object.passkeyVerificationMS != null)
                if ($util.Long)
                    message.passkeyVerificationMS = $util.Long.fromValue(object.passkeyVerificationMS, false);
                else if (typeof object.passkeyVerificationMS === "string")
                    message.passkeyVerificationMS = parseInt(object.passkeyVerificationMS, 10);
                else if (typeof object.passkeyVerificationMS === "number")
                    message.passkeyVerificationMS = object.passkeyVerificationMS;
                else if (typeof object.passkeyVerificationMS === "object")
                    message.passkeyVerificationMS = new $util.LongBits(object.passkeyVerificationMS.low >>> 0, object.passkeyVerificationMS.high >>> 0).toNumber();
            if (object.challenge != null)
                if (typeof object.challenge === "string")
                    $util.base64.decode(object.challenge, message.challenge = $util.newBuffer($util.base64.length(object.challenge)), 0);
                else if (object.challenge.length >= 0)
                    message.challenge = object.challenge;
            return message;
        };

        /**
         * Creates a plain object from a PasskeyLoginToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.PasskeyLoginToken
         * @static
         * @param {Tokens.PasskeyLoginToken} message PasskeyLoginToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        PasskeyLoginToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.passkeyVerificationMS = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.passkeyVerificationMS = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.challenge = "";
                else {
                    object.challenge = [];
                    if (options.bytes !== Array)
                        object.challenge = $util.newBuffer(object.challenge);
                }
            }
            if (message.passkeyVerificationMS != null && Object.hasOwnProperty.call(message, "passkeyVerificationMS"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.passkeyVerificationMS = typeof message.passkeyVerificationMS === "number" ? BigInt(message.passkeyVerificationMS) : $util.Long.fromBits(message.passkeyVerificationMS.low >>> 0, message.passkeyVerificationMS.high >>> 0, false).toBigInt();
                else if (typeof message.passkeyVerificationMS === "number")
                    object.passkeyVerificationMS = options.longs === String ? String(message.passkeyVerificationMS) : message.passkeyVerificationMS;
                else
                    object.passkeyVerificationMS = options.longs === String ? $util.Long.prototype.toString.call(message.passkeyVerificationMS) : options.longs === Number ? new $util.LongBits(message.passkeyVerificationMS.low >>> 0, message.passkeyVerificationMS.high >>> 0).toNumber() : message.passkeyVerificationMS;
            if (message.challenge != null && Object.hasOwnProperty.call(message, "challenge"))
                object.challenge = options.bytes === String ? $util.base64.encode(message.challenge, 0, message.challenge.length) : options.bytes === Array ? Array.prototype.slice.call(message.challenge) : message.challenge;
            return object;
        };

        /**
         * Converts this PasskeyLoginToken to JSON.
         * @function toJSON
         * @memberof Tokens.PasskeyLoginToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        PasskeyLoginToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for PasskeyLoginToken
         * @function getTypeUrl
         * @memberof Tokens.PasskeyLoginToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        PasskeyLoginToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.PasskeyLoginToken";
        };

        return PasskeyLoginToken;
    })();

    Tokens.LoginToken = (function() {

        /**
         * Properties of a LoginToken.
         * @memberof Tokens
         * @interface ILoginToken
         * @property {number|null} [loginSessionId] LoginToken loginSessionId
         * @property {number|null} [deviceId] LoginToken deviceId
         * @property {Uint8Array|null} [accountUid] LoginToken accountUid
         * @property {Uint8Array|null} [messageSessionUid] LoginToken messageSessionUid
         * @property {Authentication.LoginState|null} [loginState] LoginToken loginState
         * @property {Authentication.LoginMethod|null} [loginMethod] LoginToken loginMethod
         * @property {number|null} [creation] LoginToken creation
         * @property {number|null} [userId] LoginToken userId
         * @property {number|null} [enterpriseUserId] LoginToken enterpriseUserId
         * @property {number|null} [clientVersionId] LoginToken clientVersionId
         * @property {Authentication.SupportedLanguage|null} [supportedLanguage] LoginToken supportedLanguage
         * @property {Tokens.ISSOLoginToken|null} [ssoLoginToken] LoginToken ssoLoginToken
         * @property {string|null} [username] LoginToken username
         * @property {boolean|null} [relogin] LoginToken relogin
         * @property {Authentication.LoginType|null} [loginType] LoginToken loginType
         * @property {number|null} [fromUserId] LoginToken fromUserId
         * @property {Tokens.IPasskeyLoginToken|null} [passkeyLoginToken] LoginToken passkeyLoginToken
         */

        /**
         * Constructs a new LoginToken.
         * @memberof Tokens
         * @classdesc Represents a LoginToken.
         * @implements ILoginToken
         * @constructor
         * @param {Tokens.ILoginToken=} [properties] Properties to set
         */
        function LoginToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LoginToken loginSessionId.
         * @member {number} loginSessionId
         * @memberof Tokens.LoginToken
         * @instance
         */
        LoginToken.prototype.loginSessionId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LoginToken deviceId.
         * @member {number} deviceId
         * @memberof Tokens.LoginToken
         * @instance
         */
        LoginToken.prototype.deviceId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LoginToken accountUid.
         * @member {Uint8Array} accountUid
         * @memberof Tokens.LoginToken
         * @instance
         */
        LoginToken.prototype.accountUid = $util.newBuffer([]);

        /**
         * LoginToken messageSessionUid.
         * @member {Uint8Array} messageSessionUid
         * @memberof Tokens.LoginToken
         * @instance
         */
        LoginToken.prototype.messageSessionUid = $util.newBuffer([]);

        /**
         * LoginToken loginState.
         * @member {Authentication.LoginState} loginState
         * @memberof Tokens.LoginToken
         * @instance
         */
        LoginToken.prototype.loginState = 0;

        /**
         * LoginToken loginMethod.
         * @member {Authentication.LoginMethod} loginMethod
         * @memberof Tokens.LoginToken
         * @instance
         */
        LoginToken.prototype.loginMethod = 0;

        /**
         * LoginToken creation.
         * @member {number} creation
         * @memberof Tokens.LoginToken
         * @instance
         */
        LoginToken.prototype.creation = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LoginToken userId.
         * @member {number} userId
         * @memberof Tokens.LoginToken
         * @instance
         */
        LoginToken.prototype.userId = 0;

        /**
         * LoginToken enterpriseUserId.
         * @member {number} enterpriseUserId
         * @memberof Tokens.LoginToken
         * @instance
         */
        LoginToken.prototype.enterpriseUserId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LoginToken clientVersionId.
         * @member {number} clientVersionId
         * @memberof Tokens.LoginToken
         * @instance
         */
        LoginToken.prototype.clientVersionId = 0;

        /**
         * LoginToken supportedLanguage.
         * @member {Authentication.SupportedLanguage} supportedLanguage
         * @memberof Tokens.LoginToken
         * @instance
         */
        LoginToken.prototype.supportedLanguage = 0;

        /**
         * LoginToken ssoLoginToken.
         * @member {Tokens.ISSOLoginToken|null|undefined} ssoLoginToken
         * @memberof Tokens.LoginToken
         * @instance
         */
        LoginToken.prototype.ssoLoginToken = null;

        /**
         * LoginToken username.
         * @member {string} username
         * @memberof Tokens.LoginToken
         * @instance
         */
        LoginToken.prototype.username = "";

        /**
         * LoginToken relogin.
         * @member {boolean} relogin
         * @memberof Tokens.LoginToken
         * @instance
         */
        LoginToken.prototype.relogin = false;

        /**
         * LoginToken loginType.
         * @member {Authentication.LoginType} loginType
         * @memberof Tokens.LoginToken
         * @instance
         */
        LoginToken.prototype.loginType = 0;

        /**
         * LoginToken fromUserId.
         * @member {number} fromUserId
         * @memberof Tokens.LoginToken
         * @instance
         */
        LoginToken.prototype.fromUserId = 0;

        /**
         * LoginToken passkeyLoginToken.
         * @member {Tokens.IPasskeyLoginToken|null|undefined} passkeyLoginToken
         * @memberof Tokens.LoginToken
         * @instance
         */
        LoginToken.prototype.passkeyLoginToken = null;

        /**
         * Creates a new LoginToken instance using the specified properties.
         * @function create
         * @memberof Tokens.LoginToken
         * @static
         * @param {Tokens.ILoginToken=} [properties] Properties to set
         * @returns {Tokens.LoginToken} LoginToken instance
         */
        LoginToken.create = function create(properties) {
            return new LoginToken(properties);
        };

        /**
         * Encodes the specified LoginToken message. Does not implicitly {@link Tokens.LoginToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.LoginToken
         * @static
         * @param {Tokens.ILoginToken} message LoginToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LoginToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.loginSessionId != null && Object.hasOwnProperty.call(message, "loginSessionId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.loginSessionId);
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.deviceId);
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.accountUid);
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.messageSessionUid);
            if (message.loginState != null && Object.hasOwnProperty.call(message, "loginState"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.loginState);
            if (message.loginMethod != null && Object.hasOwnProperty.call(message, "loginMethod"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.loginMethod);
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.creation);
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.userId);
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                writer.uint32(/* id 9, wireType 0 =*/72).int64(message.enterpriseUserId);
            if (message.clientVersionId != null && Object.hasOwnProperty.call(message, "clientVersionId"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.clientVersionId);
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                writer.uint32(/* id 11, wireType 0 =*/88).int32(message.supportedLanguage);
            if (message.ssoLoginToken != null && Object.hasOwnProperty.call(message, "ssoLoginToken"))
                $root.Tokens.SSOLoginToken.encode(message.ssoLoginToken, writer.uint32(/* id 12, wireType 2 =*/98).fork(), q + 1).ldelim();
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 13, wireType 2 =*/106).string(message.username);
            if (message.relogin != null && Object.hasOwnProperty.call(message, "relogin"))
                writer.uint32(/* id 14, wireType 0 =*/112).bool(message.relogin);
            if (message.loginType != null && Object.hasOwnProperty.call(message, "loginType"))
                writer.uint32(/* id 15, wireType 0 =*/120).int32(message.loginType);
            if (message.fromUserId != null && Object.hasOwnProperty.call(message, "fromUserId"))
                writer.uint32(/* id 16, wireType 0 =*/128).int32(message.fromUserId);
            if (message.passkeyLoginToken != null && Object.hasOwnProperty.call(message, "passkeyLoginToken"))
                $root.Tokens.PasskeyLoginToken.encode(message.passkeyLoginToken, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified LoginToken message, length delimited. Does not implicitly {@link Tokens.LoginToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.LoginToken
         * @static
         * @param {Tokens.ILoginToken} message LoginToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LoginToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LoginToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.LoginToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.LoginToken} LoginToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LoginToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.LoginToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.loginSessionId = reader.int64();
                        break;
                    }
                case 2: {
                        message.deviceId = reader.int64();
                        break;
                    }
                case 3: {
                        message.accountUid = reader.bytes();
                        break;
                    }
                case 4: {
                        message.messageSessionUid = reader.bytes();
                        break;
                    }
                case 5: {
                        message.loginState = reader.int32();
                        break;
                    }
                case 6: {
                        message.loginMethod = reader.int32();
                        break;
                    }
                case 7: {
                        message.creation = reader.int64();
                        break;
                    }
                case 8: {
                        message.userId = reader.int32();
                        break;
                    }
                case 9: {
                        message.enterpriseUserId = reader.int64();
                        break;
                    }
                case 10: {
                        message.clientVersionId = reader.int32();
                        break;
                    }
                case 11: {
                        message.supportedLanguage = reader.int32();
                        break;
                    }
                case 12: {
                        message.ssoLoginToken = $root.Tokens.SSOLoginToken.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 13: {
                        message.username = reader.string();
                        break;
                    }
                case 14: {
                        message.relogin = reader.bool();
                        break;
                    }
                case 15: {
                        message.loginType = reader.int32();
                        break;
                    }
                case 16: {
                        message.fromUserId = reader.int32();
                        break;
                    }
                case 17: {
                        message.passkeyLoginToken = $root.Tokens.PasskeyLoginToken.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LoginToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.LoginToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.LoginToken} LoginToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LoginToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LoginToken message.
         * @function verify
         * @memberof Tokens.LoginToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LoginToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.loginSessionId != null && Object.hasOwnProperty.call(message, "loginSessionId"))
                if (!$util.isInteger(message.loginSessionId) && !(message.loginSessionId && $util.isInteger(message.loginSessionId.low) && $util.isInteger(message.loginSessionId.high)))
                    return "loginSessionId: integer|Long expected";
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                if (!$util.isInteger(message.deviceId) && !(message.deviceId && $util.isInteger(message.deviceId.low) && $util.isInteger(message.deviceId.high)))
                    return "deviceId: integer|Long expected";
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                if (!(message.accountUid && typeof message.accountUid.length === "number" || $util.isString(message.accountUid)))
                    return "accountUid: buffer expected";
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                if (!(message.messageSessionUid && typeof message.messageSessionUid.length === "number" || $util.isString(message.messageSessionUid)))
                    return "messageSessionUid: buffer expected";
            if (message.loginState != null && Object.hasOwnProperty.call(message, "loginState"))
                switch (message.loginState) {
                default:
                    return "loginState: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                case 12:
                case 13:
                case 14:
                case 15:
                case 16:
                case 17:
                case 18:
                case 19:
                case 20:
                case 21:
                case 22:
                case 99:
                    break;
                }
            if (message.loginMethod != null && Object.hasOwnProperty.call(message, "loginMethod"))
                switch (message.loginMethod) {
                default:
                    return "loginMethod: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                    break;
                }
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                if (!$util.isInteger(message.creation) && !(message.creation && $util.isInteger(message.creation.low) && $util.isInteger(message.creation.high)))
                    return "creation: integer|Long expected";
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                if (!$util.isInteger(message.userId))
                    return "userId: integer expected";
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                if (!$util.isInteger(message.enterpriseUserId) && !(message.enterpriseUserId && $util.isInteger(message.enterpriseUserId.low) && $util.isInteger(message.enterpriseUserId.high)))
                    return "enterpriseUserId: integer|Long expected";
            if (message.clientVersionId != null && Object.hasOwnProperty.call(message, "clientVersionId"))
                if (!$util.isInteger(message.clientVersionId))
                    return "clientVersionId: integer expected";
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                switch (message.supportedLanguage) {
                default:
                    return "supportedLanguage: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                case 11:
                case 12:
                case 13:
                case 14:
                case 15:
                case 16:
                case 17:
                case 18:
                case 19:
                case 20:
                case 21:
                case 22:
                    break;
                }
            if (message.ssoLoginToken != null && Object.hasOwnProperty.call(message, "ssoLoginToken")) {
                let error = $root.Tokens.SSOLoginToken.verify(message.ssoLoginToken, long + 1);
                if (error)
                    return "ssoLoginToken." + error;
            }
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                if (!$util.isString(message.username))
                    return "username: string expected";
            if (message.relogin != null && Object.hasOwnProperty.call(message, "relogin"))
                if (typeof message.relogin !== "boolean")
                    return "relogin: boolean expected";
            if (message.loginType != null && Object.hasOwnProperty.call(message, "loginType"))
                switch (message.loginType) {
                default:
                    return "loginType: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                    break;
                }
            if (message.fromUserId != null && Object.hasOwnProperty.call(message, "fromUserId"))
                if (!$util.isInteger(message.fromUserId))
                    return "fromUserId: integer expected";
            if (message.passkeyLoginToken != null && Object.hasOwnProperty.call(message, "passkeyLoginToken")) {
                let error = $root.Tokens.PasskeyLoginToken.verify(message.passkeyLoginToken, long + 1);
                if (error)
                    return "passkeyLoginToken." + error;
            }
            return null;
        };

        /**
         * Creates a LoginToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.LoginToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.LoginToken} LoginToken
         */
        LoginToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.LoginToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.LoginToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.LoginToken();
            if (object.loginSessionId != null)
                if ($util.Long)
                    message.loginSessionId = $util.Long.fromValue(object.loginSessionId, false);
                else if (typeof object.loginSessionId === "string")
                    message.loginSessionId = parseInt(object.loginSessionId, 10);
                else if (typeof object.loginSessionId === "number")
                    message.loginSessionId = object.loginSessionId;
                else if (typeof object.loginSessionId === "object")
                    message.loginSessionId = new $util.LongBits(object.loginSessionId.low >>> 0, object.loginSessionId.high >>> 0).toNumber();
            if (object.deviceId != null)
                if ($util.Long)
                    message.deviceId = $util.Long.fromValue(object.deviceId, false);
                else if (typeof object.deviceId === "string")
                    message.deviceId = parseInt(object.deviceId, 10);
                else if (typeof object.deviceId === "number")
                    message.deviceId = object.deviceId;
                else if (typeof object.deviceId === "object")
                    message.deviceId = new $util.LongBits(object.deviceId.low >>> 0, object.deviceId.high >>> 0).toNumber();
            if (object.accountUid != null)
                if (typeof object.accountUid === "string")
                    $util.base64.decode(object.accountUid, message.accountUid = $util.newBuffer($util.base64.length(object.accountUid)), 0);
                else if (object.accountUid.length >= 0)
                    message.accountUid = object.accountUid;
            if (object.messageSessionUid != null)
                if (typeof object.messageSessionUid === "string")
                    $util.base64.decode(object.messageSessionUid, message.messageSessionUid = $util.newBuffer($util.base64.length(object.messageSessionUid)), 0);
                else if (object.messageSessionUid.length >= 0)
                    message.messageSessionUid = object.messageSessionUid;
            switch (object.loginState) {
            default:
                if (typeof object.loginState === "number") {
                    message.loginState = object.loginState;
                    break;
                }
                break;
            case "INVALID_LOGINSTATE":
            case 0:
                message.loginState = 0;
                break;
            case "LOGGED_OUT":
            case 1:
                message.loginState = 1;
                break;
            case "DEVICE_APPROVAL_REQUIRED":
            case 2:
                message.loginState = 2;
                break;
            case "DEVICE_LOCKED":
            case 3:
                message.loginState = 3;
                break;
            case "ACCOUNT_LOCKED":
            case 4:
                message.loginState = 4;
                break;
            case "DEVICE_ACCOUNT_LOCKED":
            case 5:
                message.loginState = 5;
                break;
            case "UPGRADE":
            case 6:
                message.loginState = 6;
                break;
            case "LICENSE_EXPIRED":
            case 7:
                message.loginState = 7;
                break;
            case "REGION_REDIRECT":
            case 8:
                message.loginState = 8;
                break;
            case "REDIRECT_CLOUD_SSO":
            case 9:
                message.loginState = 9;
                break;
            case "REDIRECT_ONSITE_SSO":
            case 10:
                message.loginState = 10;
                break;
            case "REQUIRES_2FA":
            case 12:
                message.loginState = 12;
                break;
            case "REQUIRES_AUTH_HASH":
            case 13:
                message.loginState = 13;
                break;
            case "REQUIRES_USERNAME":
            case 14:
                message.loginState = 14;
                break;
            case "AFTER_CLOUD_SSO_LOGIN":
            case 15:
                message.loginState = 15;
                break;
            case "REQUIRES_ACCOUNT_CREATION":
            case 16:
                message.loginState = 16;
                break;
            case "REQUIRES_DEVICE_ENCRYPTED_DATA_KEY":
            case 17:
                message.loginState = 17;
                break;
            case "LOGIN_TOKEN_EXPIRED":
            case 18:
                message.loginState = 18;
                break;
            case "PASSKEY_INITIATE_CHALLENGE":
            case 19:
                message.loginState = 19;
                break;
            case "PASSKEY_AUTH_REQUIRED":
            case 20:
                message.loginState = 20;
                break;
            case "PASSKEY_VERIFY_AUTHENTICATION":
            case 21:
                message.loginState = 21;
                break;
            case "AFTER_PASSKEY_LOGIN":
            case 22:
                message.loginState = 22;
                break;
            case "LOGGED_IN":
            case 99:
                message.loginState = 99;
                break;
            }
            switch (object.loginMethod) {
            default:
                if (typeof object.loginMethod === "number") {
                    message.loginMethod = object.loginMethod;
                    break;
                }
                break;
            case "INVALID_LOGINMETHOD":
            case 0:
                message.loginMethod = 0;
                break;
            case "EXISTING_ACCOUNT":
            case 1:
                message.loginMethod = 1;
                break;
            case "SSO_DOMAIN":
            case 2:
                message.loginMethod = 2;
                break;
            case "AFTER_SSO":
            case 3:
                message.loginMethod = 3;
                break;
            case "NEW_ACCOUNT":
            case 4:
                message.loginMethod = 4;
                break;
            }
            if (object.creation != null)
                if ($util.Long)
                    message.creation = $util.Long.fromValue(object.creation, false);
                else if (typeof object.creation === "string")
                    message.creation = parseInt(object.creation, 10);
                else if (typeof object.creation === "number")
                    message.creation = object.creation;
                else if (typeof object.creation === "object")
                    message.creation = new $util.LongBits(object.creation.low >>> 0, object.creation.high >>> 0).toNumber();
            if (object.userId != null)
                message.userId = object.userId | 0;
            if (object.enterpriseUserId != null)
                if ($util.Long)
                    message.enterpriseUserId = $util.Long.fromValue(object.enterpriseUserId, false);
                else if (typeof object.enterpriseUserId === "string")
                    message.enterpriseUserId = parseInt(object.enterpriseUserId, 10);
                else if (typeof object.enterpriseUserId === "number")
                    message.enterpriseUserId = object.enterpriseUserId;
                else if (typeof object.enterpriseUserId === "object")
                    message.enterpriseUserId = new $util.LongBits(object.enterpriseUserId.low >>> 0, object.enterpriseUserId.high >>> 0).toNumber();
            if (object.clientVersionId != null)
                message.clientVersionId = object.clientVersionId | 0;
            switch (object.supportedLanguage) {
            default:
                if (typeof object.supportedLanguage === "number") {
                    message.supportedLanguage = object.supportedLanguage;
                    break;
                }
                break;
            case "ENGLISH":
            case 0:
                message.supportedLanguage = 0;
                break;
            case "ARABIC":
            case 1:
                message.supportedLanguage = 1;
                break;
            case "BRITISH":
            case 2:
                message.supportedLanguage = 2;
                break;
            case "CHINESE":
            case 3:
                message.supportedLanguage = 3;
                break;
            case "CHINESE_HONG_KONG":
            case 4:
                message.supportedLanguage = 4;
                break;
            case "CHINESE_TAIWAN":
            case 5:
                message.supportedLanguage = 5;
                break;
            case "DUTCH":
            case 6:
                message.supportedLanguage = 6;
                break;
            case "FRENCH":
            case 7:
                message.supportedLanguage = 7;
                break;
            case "GERMAN":
            case 8:
                message.supportedLanguage = 8;
                break;
            case "GREEK":
            case 9:
                message.supportedLanguage = 9;
                break;
            case "HEBREW":
            case 10:
                message.supportedLanguage = 10;
                break;
            case "ITALIAN":
            case 11:
                message.supportedLanguage = 11;
                break;
            case "JAPANESE":
            case 12:
                message.supportedLanguage = 12;
                break;
            case "KOREAN":
            case 13:
                message.supportedLanguage = 13;
                break;
            case "POLISH":
            case 14:
                message.supportedLanguage = 14;
                break;
            case "PORTUGUESE":
            case 15:
                message.supportedLanguage = 15;
                break;
            case "PORTUGUESE_BRAZIL":
            case 16:
                message.supportedLanguage = 16;
                break;
            case "ROMANIAN":
            case 17:
                message.supportedLanguage = 17;
                break;
            case "RUSSIAN":
            case 18:
                message.supportedLanguage = 18;
                break;
            case "SLOVAK":
            case 19:
                message.supportedLanguage = 19;
                break;
            case "SPANISH":
            case 20:
                message.supportedLanguage = 20;
                break;
            case "FINNISH":
            case 21:
                message.supportedLanguage = 21;
                break;
            case "SWEDISH":
            case 22:
                message.supportedLanguage = 22;
                break;
            }
            if (object.ssoLoginToken != null) {
                if (!$util.isObject(object.ssoLoginToken))
                    throw TypeError(".Tokens.LoginToken.ssoLoginToken: object expected");
                message.ssoLoginToken = $root.Tokens.SSOLoginToken.fromObject(object.ssoLoginToken, long + 1);
            }
            if (object.username != null)
                message.username = String(object.username);
            if (object.relogin != null)
                message.relogin = Boolean(object.relogin);
            switch (object.loginType) {
            default:
                if (typeof object.loginType === "number") {
                    message.loginType = object.loginType;
                    break;
                }
                break;
            case "NORMAL":
            case 0:
                message.loginType = 0;
                break;
            case "SSO":
            case 1:
                message.loginType = 1;
                break;
            case "BIO":
            case 2:
                message.loginType = 2;
                break;
            case "ALTERNATE":
            case 3:
                message.loginType = 3;
                break;
            case "OFFLINE":
            case 4:
                message.loginType = 4;
                break;
            case "FORGOT_PASSWORD":
            case 5:
                message.loginType = 5;
                break;
            case "PASSKEY_BIO":
            case 6:
                message.loginType = 6;
                break;
            }
            if (object.fromUserId != null)
                message.fromUserId = object.fromUserId | 0;
            if (object.passkeyLoginToken != null) {
                if (!$util.isObject(object.passkeyLoginToken))
                    throw TypeError(".Tokens.LoginToken.passkeyLoginToken: object expected");
                message.passkeyLoginToken = $root.Tokens.PasskeyLoginToken.fromObject(object.passkeyLoginToken, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a LoginToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.LoginToken
         * @static
         * @param {Tokens.LoginToken} message LoginToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LoginToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.loginSessionId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.loginSessionId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.deviceId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.deviceId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.accountUid = "";
                else {
                    object.accountUid = [];
                    if (options.bytes !== Array)
                        object.accountUid = $util.newBuffer(object.accountUid);
                }
                if (options.bytes === String)
                    object.messageSessionUid = "";
                else {
                    object.messageSessionUid = [];
                    if (options.bytes !== Array)
                        object.messageSessionUid = $util.newBuffer(object.messageSessionUid);
                }
                object.loginState = options.enums === String ? "INVALID_LOGINSTATE" : 0;
                object.loginMethod = options.enums === String ? "INVALID_LOGINMETHOD" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.creation = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.creation = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.userId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.enterpriseUserId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.enterpriseUserId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.clientVersionId = 0;
                object.supportedLanguage = options.enums === String ? "ENGLISH" : 0;
                object.ssoLoginToken = null;
                object.username = "";
                object.relogin = false;
                object.loginType = options.enums === String ? "NORMAL" : 0;
                object.fromUserId = 0;
                object.passkeyLoginToken = null;
            }
            if (message.loginSessionId != null && Object.hasOwnProperty.call(message, "loginSessionId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.loginSessionId = typeof message.loginSessionId === "number" ? BigInt(message.loginSessionId) : $util.Long.fromBits(message.loginSessionId.low >>> 0, message.loginSessionId.high >>> 0, false).toBigInt();
                else if (typeof message.loginSessionId === "number")
                    object.loginSessionId = options.longs === String ? String(message.loginSessionId) : message.loginSessionId;
                else
                    object.loginSessionId = options.longs === String ? $util.Long.prototype.toString.call(message.loginSessionId) : options.longs === Number ? new $util.LongBits(message.loginSessionId.low >>> 0, message.loginSessionId.high >>> 0).toNumber() : message.loginSessionId;
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.deviceId = typeof message.deviceId === "number" ? BigInt(message.deviceId) : $util.Long.fromBits(message.deviceId.low >>> 0, message.deviceId.high >>> 0, false).toBigInt();
                else if (typeof message.deviceId === "number")
                    object.deviceId = options.longs === String ? String(message.deviceId) : message.deviceId;
                else
                    object.deviceId = options.longs === String ? $util.Long.prototype.toString.call(message.deviceId) : options.longs === Number ? new $util.LongBits(message.deviceId.low >>> 0, message.deviceId.high >>> 0).toNumber() : message.deviceId;
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                object.accountUid = options.bytes === String ? $util.base64.encode(message.accountUid, 0, message.accountUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.accountUid) : message.accountUid;
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                object.messageSessionUid = options.bytes === String ? $util.base64.encode(message.messageSessionUid, 0, message.messageSessionUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.messageSessionUid) : message.messageSessionUid;
            if (message.loginState != null && Object.hasOwnProperty.call(message, "loginState"))
                object.loginState = options.enums === String ? $root.Authentication.LoginState[message.loginState] === undefined ? message.loginState : $root.Authentication.LoginState[message.loginState] : message.loginState;
            if (message.loginMethod != null && Object.hasOwnProperty.call(message, "loginMethod"))
                object.loginMethod = options.enums === String ? $root.Authentication.LoginMethod[message.loginMethod] === undefined ? message.loginMethod : $root.Authentication.LoginMethod[message.loginMethod] : message.loginMethod;
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.creation = typeof message.creation === "number" ? BigInt(message.creation) : $util.Long.fromBits(message.creation.low >>> 0, message.creation.high >>> 0, false).toBigInt();
                else if (typeof message.creation === "number")
                    object.creation = options.longs === String ? String(message.creation) : message.creation;
                else
                    object.creation = options.longs === String ? $util.Long.prototype.toString.call(message.creation) : options.longs === Number ? new $util.LongBits(message.creation.low >>> 0, message.creation.high >>> 0).toNumber() : message.creation;
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                object.userId = message.userId;
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.enterpriseUserId = typeof message.enterpriseUserId === "number" ? BigInt(message.enterpriseUserId) : $util.Long.fromBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0, false).toBigInt();
                else if (typeof message.enterpriseUserId === "number")
                    object.enterpriseUserId = options.longs === String ? String(message.enterpriseUserId) : message.enterpriseUserId;
                else
                    object.enterpriseUserId = options.longs === String ? $util.Long.prototype.toString.call(message.enterpriseUserId) : options.longs === Number ? new $util.LongBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0).toNumber() : message.enterpriseUserId;
            if (message.clientVersionId != null && Object.hasOwnProperty.call(message, "clientVersionId"))
                object.clientVersionId = message.clientVersionId;
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                object.supportedLanguage = options.enums === String ? $root.Authentication.SupportedLanguage[message.supportedLanguage] === undefined ? message.supportedLanguage : $root.Authentication.SupportedLanguage[message.supportedLanguage] : message.supportedLanguage;
            if (message.ssoLoginToken != null && Object.hasOwnProperty.call(message, "ssoLoginToken"))
                object.ssoLoginToken = $root.Tokens.SSOLoginToken.toObject(message.ssoLoginToken, options, q + 1);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.relogin != null && Object.hasOwnProperty.call(message, "relogin"))
                object.relogin = message.relogin;
            if (message.loginType != null && Object.hasOwnProperty.call(message, "loginType"))
                object.loginType = options.enums === String ? $root.Authentication.LoginType[message.loginType] === undefined ? message.loginType : $root.Authentication.LoginType[message.loginType] : message.loginType;
            if (message.fromUserId != null && Object.hasOwnProperty.call(message, "fromUserId"))
                object.fromUserId = message.fromUserId;
            if (message.passkeyLoginToken != null && Object.hasOwnProperty.call(message, "passkeyLoginToken"))
                object.passkeyLoginToken = $root.Tokens.PasskeyLoginToken.toObject(message.passkeyLoginToken, options, q + 1);
            return object;
        };

        /**
         * Converts this LoginToken to JSON.
         * @function toJSON
         * @memberof Tokens.LoginToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LoginToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LoginToken
         * @function getTypeUrl
         * @memberof Tokens.LoginToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LoginToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.LoginToken";
        };

        return LoginToken;
    })();

    Tokens.DeviceApprovalToken = (function() {

        /**
         * Properties of a DeviceApprovalToken.
         * @memberof Tokens
         * @interface IDeviceApprovalToken
         * @property {number|null} [creation] DeviceApprovalToken creation
         * @property {number|null} [expiration] DeviceApprovalToken expiration
         * @property {Tokens.IDeviceToken|null} [deviceToken] DeviceApprovalToken deviceToken
         * @property {Authentication.SupportedLanguage|null} [supportedLanguage] DeviceApprovalToken supportedLanguage
         */

        /**
         * Constructs a new DeviceApprovalToken.
         * @memberof Tokens
         * @classdesc Represents a DeviceApprovalToken.
         * @implements IDeviceApprovalToken
         * @constructor
         * @param {Tokens.IDeviceApprovalToken=} [properties] Properties to set
         */
        function DeviceApprovalToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeviceApprovalToken creation.
         * @member {number} creation
         * @memberof Tokens.DeviceApprovalToken
         * @instance
         */
        DeviceApprovalToken.prototype.creation = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * DeviceApprovalToken expiration.
         * @member {number} expiration
         * @memberof Tokens.DeviceApprovalToken
         * @instance
         */
        DeviceApprovalToken.prototype.expiration = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * DeviceApprovalToken deviceToken.
         * @member {Tokens.IDeviceToken|null|undefined} deviceToken
         * @memberof Tokens.DeviceApprovalToken
         * @instance
         */
        DeviceApprovalToken.prototype.deviceToken = null;

        /**
         * DeviceApprovalToken supportedLanguage.
         * @member {Authentication.SupportedLanguage} supportedLanguage
         * @memberof Tokens.DeviceApprovalToken
         * @instance
         */
        DeviceApprovalToken.prototype.supportedLanguage = 0;

        /**
         * Creates a new DeviceApprovalToken instance using the specified properties.
         * @function create
         * @memberof Tokens.DeviceApprovalToken
         * @static
         * @param {Tokens.IDeviceApprovalToken=} [properties] Properties to set
         * @returns {Tokens.DeviceApprovalToken} DeviceApprovalToken instance
         */
        DeviceApprovalToken.create = function create(properties) {
            return new DeviceApprovalToken(properties);
        };

        /**
         * Encodes the specified DeviceApprovalToken message. Does not implicitly {@link Tokens.DeviceApprovalToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.DeviceApprovalToken
         * @static
         * @param {Tokens.IDeviceApprovalToken} message DeviceApprovalToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceApprovalToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.creation);
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.expiration);
            if (message.deviceToken != null && Object.hasOwnProperty.call(message, "deviceToken"))
                $root.Tokens.DeviceToken.encode(message.deviceToken, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.supportedLanguage);
            return writer;
        };

        /**
         * Encodes the specified DeviceApprovalToken message, length delimited. Does not implicitly {@link Tokens.DeviceApprovalToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.DeviceApprovalToken
         * @static
         * @param {Tokens.IDeviceApprovalToken} message DeviceApprovalToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceApprovalToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DeviceApprovalToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.DeviceApprovalToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.DeviceApprovalToken} DeviceApprovalToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceApprovalToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.DeviceApprovalToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.creation = reader.int64();
                        break;
                    }
                case 2: {
                        message.expiration = reader.int64();
                        break;
                    }
                case 3: {
                        message.deviceToken = $root.Tokens.DeviceToken.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 4: {
                        message.supportedLanguage = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a DeviceApprovalToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.DeviceApprovalToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.DeviceApprovalToken} DeviceApprovalToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceApprovalToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeviceApprovalToken message.
         * @function verify
         * @memberof Tokens.DeviceApprovalToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeviceApprovalToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                if (!$util.isInteger(message.creation) && !(message.creation && $util.isInteger(message.creation.low) && $util.isInteger(message.creation.high)))
                    return "creation: integer|Long expected";
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                if (!$util.isInteger(message.expiration) && !(message.expiration && $util.isInteger(message.expiration.low) && $util.isInteger(message.expiration.high)))
                    return "expiration: integer|Long expected";
            if (message.deviceToken != null && Object.hasOwnProperty.call(message, "deviceToken")) {
                let error = $root.Tokens.DeviceToken.verify(message.deviceToken, long + 1);
                if (error)
                    return "deviceToken." + error;
            }
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                switch (message.supportedLanguage) {
                default:
                    return "supportedLanguage: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                case 11:
                case 12:
                case 13:
                case 14:
                case 15:
                case 16:
                case 17:
                case 18:
                case 19:
                case 20:
                case 21:
                case 22:
                    break;
                }
            return null;
        };

        /**
         * Creates a DeviceApprovalToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.DeviceApprovalToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.DeviceApprovalToken} DeviceApprovalToken
         */
        DeviceApprovalToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.DeviceApprovalToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.DeviceApprovalToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.DeviceApprovalToken();
            if (object.creation != null)
                if ($util.Long)
                    message.creation = $util.Long.fromValue(object.creation, false);
                else if (typeof object.creation === "string")
                    message.creation = parseInt(object.creation, 10);
                else if (typeof object.creation === "number")
                    message.creation = object.creation;
                else if (typeof object.creation === "object")
                    message.creation = new $util.LongBits(object.creation.low >>> 0, object.creation.high >>> 0).toNumber();
            if (object.expiration != null)
                if ($util.Long)
                    message.expiration = $util.Long.fromValue(object.expiration, false);
                else if (typeof object.expiration === "string")
                    message.expiration = parseInt(object.expiration, 10);
                else if (typeof object.expiration === "number")
                    message.expiration = object.expiration;
                else if (typeof object.expiration === "object")
                    message.expiration = new $util.LongBits(object.expiration.low >>> 0, object.expiration.high >>> 0).toNumber();
            if (object.deviceToken != null) {
                if (!$util.isObject(object.deviceToken))
                    throw TypeError(".Tokens.DeviceApprovalToken.deviceToken: object expected");
                message.deviceToken = $root.Tokens.DeviceToken.fromObject(object.deviceToken, long + 1);
            }
            switch (object.supportedLanguage) {
            default:
                if (typeof object.supportedLanguage === "number") {
                    message.supportedLanguage = object.supportedLanguage;
                    break;
                }
                break;
            case "ENGLISH":
            case 0:
                message.supportedLanguage = 0;
                break;
            case "ARABIC":
            case 1:
                message.supportedLanguage = 1;
                break;
            case "BRITISH":
            case 2:
                message.supportedLanguage = 2;
                break;
            case "CHINESE":
            case 3:
                message.supportedLanguage = 3;
                break;
            case "CHINESE_HONG_KONG":
            case 4:
                message.supportedLanguage = 4;
                break;
            case "CHINESE_TAIWAN":
            case 5:
                message.supportedLanguage = 5;
                break;
            case "DUTCH":
            case 6:
                message.supportedLanguage = 6;
                break;
            case "FRENCH":
            case 7:
                message.supportedLanguage = 7;
                break;
            case "GERMAN":
            case 8:
                message.supportedLanguage = 8;
                break;
            case "GREEK":
            case 9:
                message.supportedLanguage = 9;
                break;
            case "HEBREW":
            case 10:
                message.supportedLanguage = 10;
                break;
            case "ITALIAN":
            case 11:
                message.supportedLanguage = 11;
                break;
            case "JAPANESE":
            case 12:
                message.supportedLanguage = 12;
                break;
            case "KOREAN":
            case 13:
                message.supportedLanguage = 13;
                break;
            case "POLISH":
            case 14:
                message.supportedLanguage = 14;
                break;
            case "PORTUGUESE":
            case 15:
                message.supportedLanguage = 15;
                break;
            case "PORTUGUESE_BRAZIL":
            case 16:
                message.supportedLanguage = 16;
                break;
            case "ROMANIAN":
            case 17:
                message.supportedLanguage = 17;
                break;
            case "RUSSIAN":
            case 18:
                message.supportedLanguage = 18;
                break;
            case "SLOVAK":
            case 19:
                message.supportedLanguage = 19;
                break;
            case "SPANISH":
            case 20:
                message.supportedLanguage = 20;
                break;
            case "FINNISH":
            case 21:
                message.supportedLanguage = 21;
                break;
            case "SWEDISH":
            case 22:
                message.supportedLanguage = 22;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a DeviceApprovalToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.DeviceApprovalToken
         * @static
         * @param {Tokens.DeviceApprovalToken} message DeviceApprovalToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeviceApprovalToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.creation = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.creation = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expiration = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.expiration = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.deviceToken = null;
                object.supportedLanguage = options.enums === String ? "ENGLISH" : 0;
            }
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.creation = typeof message.creation === "number" ? BigInt(message.creation) : $util.Long.fromBits(message.creation.low >>> 0, message.creation.high >>> 0, false).toBigInt();
                else if (typeof message.creation === "number")
                    object.creation = options.longs === String ? String(message.creation) : message.creation;
                else
                    object.creation = options.longs === String ? $util.Long.prototype.toString.call(message.creation) : options.longs === Number ? new $util.LongBits(message.creation.low >>> 0, message.creation.high >>> 0).toNumber() : message.creation;
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.expiration = typeof message.expiration === "number" ? BigInt(message.expiration) : $util.Long.fromBits(message.expiration.low >>> 0, message.expiration.high >>> 0, false).toBigInt();
                else if (typeof message.expiration === "number")
                    object.expiration = options.longs === String ? String(message.expiration) : message.expiration;
                else
                    object.expiration = options.longs === String ? $util.Long.prototype.toString.call(message.expiration) : options.longs === Number ? new $util.LongBits(message.expiration.low >>> 0, message.expiration.high >>> 0).toNumber() : message.expiration;
            if (message.deviceToken != null && Object.hasOwnProperty.call(message, "deviceToken"))
                object.deviceToken = $root.Tokens.DeviceToken.toObject(message.deviceToken, options, q + 1);
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                object.supportedLanguage = options.enums === String ? $root.Authentication.SupportedLanguage[message.supportedLanguage] === undefined ? message.supportedLanguage : $root.Authentication.SupportedLanguage[message.supportedLanguage] : message.supportedLanguage;
            return object;
        };

        /**
         * Converts this DeviceApprovalToken to JSON.
         * @function toJSON
         * @memberof Tokens.DeviceApprovalToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeviceApprovalToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeviceApprovalToken
         * @function getTypeUrl
         * @memberof Tokens.DeviceApprovalToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeviceApprovalToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.DeviceApprovalToken";
        };

        return DeviceApprovalToken;
    })();

    Tokens.TwoFactorToken = (function() {

        /**
         * Properties of a TwoFactorToken.
         * @memberof Tokens
         * @interface ITwoFactorToken
         * @property {number|null} [creation] TwoFactorToken creation
         * @property {number|null} [expiration] TwoFactorToken expiration
         * @property {Tokens.IDeviceToken|null} [deviceToken] TwoFactorToken deviceToken
         * @property {string|null} [deviceIp] TwoFactorToken deviceIp
         */

        /**
         * Constructs a new TwoFactorToken.
         * @memberof Tokens
         * @classdesc Represents a TwoFactorToken.
         * @implements ITwoFactorToken
         * @constructor
         * @param {Tokens.ITwoFactorToken=} [properties] Properties to set
         */
        function TwoFactorToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TwoFactorToken creation.
         * @member {number} creation
         * @memberof Tokens.TwoFactorToken
         * @instance
         */
        TwoFactorToken.prototype.creation = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * TwoFactorToken expiration.
         * @member {number} expiration
         * @memberof Tokens.TwoFactorToken
         * @instance
         */
        TwoFactorToken.prototype.expiration = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * TwoFactorToken deviceToken.
         * @member {Tokens.IDeviceToken|null|undefined} deviceToken
         * @memberof Tokens.TwoFactorToken
         * @instance
         */
        TwoFactorToken.prototype.deviceToken = null;

        /**
         * TwoFactorToken deviceIp.
         * @member {string} deviceIp
         * @memberof Tokens.TwoFactorToken
         * @instance
         */
        TwoFactorToken.prototype.deviceIp = "";

        /**
         * Creates a new TwoFactorToken instance using the specified properties.
         * @function create
         * @memberof Tokens.TwoFactorToken
         * @static
         * @param {Tokens.ITwoFactorToken=} [properties] Properties to set
         * @returns {Tokens.TwoFactorToken} TwoFactorToken instance
         */
        TwoFactorToken.create = function create(properties) {
            return new TwoFactorToken(properties);
        };

        /**
         * Encodes the specified TwoFactorToken message. Does not implicitly {@link Tokens.TwoFactorToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.TwoFactorToken
         * @static
         * @param {Tokens.ITwoFactorToken} message TwoFactorToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TwoFactorToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.creation);
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.expiration);
            if (message.deviceToken != null && Object.hasOwnProperty.call(message, "deviceToken"))
                $root.Tokens.DeviceToken.encode(message.deviceToken, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.deviceIp != null && Object.hasOwnProperty.call(message, "deviceIp"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.deviceIp);
            return writer;
        };

        /**
         * Encodes the specified TwoFactorToken message, length delimited. Does not implicitly {@link Tokens.TwoFactorToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.TwoFactorToken
         * @static
         * @param {Tokens.ITwoFactorToken} message TwoFactorToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TwoFactorToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TwoFactorToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.TwoFactorToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.TwoFactorToken} TwoFactorToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TwoFactorToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.TwoFactorToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.creation = reader.int64();
                        break;
                    }
                case 2: {
                        message.expiration = reader.int64();
                        break;
                    }
                case 3: {
                        message.deviceToken = $root.Tokens.DeviceToken.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 4: {
                        message.deviceIp = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TwoFactorToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.TwoFactorToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.TwoFactorToken} TwoFactorToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TwoFactorToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TwoFactorToken message.
         * @function verify
         * @memberof Tokens.TwoFactorToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TwoFactorToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                if (!$util.isInteger(message.creation) && !(message.creation && $util.isInteger(message.creation.low) && $util.isInteger(message.creation.high)))
                    return "creation: integer|Long expected";
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                if (!$util.isInteger(message.expiration) && !(message.expiration && $util.isInteger(message.expiration.low) && $util.isInteger(message.expiration.high)))
                    return "expiration: integer|Long expected";
            if (message.deviceToken != null && Object.hasOwnProperty.call(message, "deviceToken")) {
                let error = $root.Tokens.DeviceToken.verify(message.deviceToken, long + 1);
                if (error)
                    return "deviceToken." + error;
            }
            if (message.deviceIp != null && Object.hasOwnProperty.call(message, "deviceIp"))
                if (!$util.isString(message.deviceIp))
                    return "deviceIp: string expected";
            return null;
        };

        /**
         * Creates a TwoFactorToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.TwoFactorToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.TwoFactorToken} TwoFactorToken
         */
        TwoFactorToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.TwoFactorToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.TwoFactorToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.TwoFactorToken();
            if (object.creation != null)
                if ($util.Long)
                    message.creation = $util.Long.fromValue(object.creation, false);
                else if (typeof object.creation === "string")
                    message.creation = parseInt(object.creation, 10);
                else if (typeof object.creation === "number")
                    message.creation = object.creation;
                else if (typeof object.creation === "object")
                    message.creation = new $util.LongBits(object.creation.low >>> 0, object.creation.high >>> 0).toNumber();
            if (object.expiration != null)
                if ($util.Long)
                    message.expiration = $util.Long.fromValue(object.expiration, false);
                else if (typeof object.expiration === "string")
                    message.expiration = parseInt(object.expiration, 10);
                else if (typeof object.expiration === "number")
                    message.expiration = object.expiration;
                else if (typeof object.expiration === "object")
                    message.expiration = new $util.LongBits(object.expiration.low >>> 0, object.expiration.high >>> 0).toNumber();
            if (object.deviceToken != null) {
                if (!$util.isObject(object.deviceToken))
                    throw TypeError(".Tokens.TwoFactorToken.deviceToken: object expected");
                message.deviceToken = $root.Tokens.DeviceToken.fromObject(object.deviceToken, long + 1);
            }
            if (object.deviceIp != null)
                message.deviceIp = String(object.deviceIp);
            return message;
        };

        /**
         * Creates a plain object from a TwoFactorToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.TwoFactorToken
         * @static
         * @param {Tokens.TwoFactorToken} message TwoFactorToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TwoFactorToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.creation = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.creation = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expiration = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.expiration = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.deviceToken = null;
                object.deviceIp = "";
            }
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.creation = typeof message.creation === "number" ? BigInt(message.creation) : $util.Long.fromBits(message.creation.low >>> 0, message.creation.high >>> 0, false).toBigInt();
                else if (typeof message.creation === "number")
                    object.creation = options.longs === String ? String(message.creation) : message.creation;
                else
                    object.creation = options.longs === String ? $util.Long.prototype.toString.call(message.creation) : options.longs === Number ? new $util.LongBits(message.creation.low >>> 0, message.creation.high >>> 0).toNumber() : message.creation;
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.expiration = typeof message.expiration === "number" ? BigInt(message.expiration) : $util.Long.fromBits(message.expiration.low >>> 0, message.expiration.high >>> 0, false).toBigInt();
                else if (typeof message.expiration === "number")
                    object.expiration = options.longs === String ? String(message.expiration) : message.expiration;
                else
                    object.expiration = options.longs === String ? $util.Long.prototype.toString.call(message.expiration) : options.longs === Number ? new $util.LongBits(message.expiration.low >>> 0, message.expiration.high >>> 0).toNumber() : message.expiration;
            if (message.deviceToken != null && Object.hasOwnProperty.call(message, "deviceToken"))
                object.deviceToken = $root.Tokens.DeviceToken.toObject(message.deviceToken, options, q + 1);
            if (message.deviceIp != null && Object.hasOwnProperty.call(message, "deviceIp"))
                object.deviceIp = message.deviceIp;
            return object;
        };

        /**
         * Converts this TwoFactorToken to JSON.
         * @function toJSON
         * @memberof Tokens.TwoFactorToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TwoFactorToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TwoFactorToken
         * @function getTypeUrl
         * @memberof Tokens.TwoFactorToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TwoFactorToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.TwoFactorToken";
        };

        return TwoFactorToken;
    })();

    Tokens.BreachWatchToken = (function() {

        /**
         * Properties of a BreachWatchToken.
         * @memberof Tokens
         * @interface IBreachWatchToken
         * @property {number|null} [userId] BreachWatchToken userId
         * @property {Uint8Array|null} [domainUid] BreachWatchToken domainUid
         * @property {Uint8Array|null} [emailUid] BreachWatchToken emailUid
         * @property {Uint8Array|null} [passwordUid] BreachWatchToken passwordUid
         */

        /**
         * Constructs a new BreachWatchToken.
         * @memberof Tokens
         * @classdesc Represents a BreachWatchToken.
         * @implements IBreachWatchToken
         * @constructor
         * @param {Tokens.IBreachWatchToken=} [properties] Properties to set
         */
        function BreachWatchToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BreachWatchToken userId.
         * @member {number} userId
         * @memberof Tokens.BreachWatchToken
         * @instance
         */
        BreachWatchToken.prototype.userId = 0;

        /**
         * BreachWatchToken domainUid.
         * @member {Uint8Array} domainUid
         * @memberof Tokens.BreachWatchToken
         * @instance
         */
        BreachWatchToken.prototype.domainUid = $util.newBuffer([]);

        /**
         * BreachWatchToken emailUid.
         * @member {Uint8Array} emailUid
         * @memberof Tokens.BreachWatchToken
         * @instance
         */
        BreachWatchToken.prototype.emailUid = $util.newBuffer([]);

        /**
         * BreachWatchToken passwordUid.
         * @member {Uint8Array} passwordUid
         * @memberof Tokens.BreachWatchToken
         * @instance
         */
        BreachWatchToken.prototype.passwordUid = $util.newBuffer([]);

        /**
         * Creates a new BreachWatchToken instance using the specified properties.
         * @function create
         * @memberof Tokens.BreachWatchToken
         * @static
         * @param {Tokens.IBreachWatchToken=} [properties] Properties to set
         * @returns {Tokens.BreachWatchToken} BreachWatchToken instance
         */
        BreachWatchToken.create = function create(properties) {
            return new BreachWatchToken(properties);
        };

        /**
         * Encodes the specified BreachWatchToken message. Does not implicitly {@link Tokens.BreachWatchToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.BreachWatchToken
         * @static
         * @param {Tokens.IBreachWatchToken} message BreachWatchToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BreachWatchToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.userId);
            if (message.domainUid != null && Object.hasOwnProperty.call(message, "domainUid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.domainUid);
            if (message.emailUid != null && Object.hasOwnProperty.call(message, "emailUid"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.emailUid);
            if (message.passwordUid != null && Object.hasOwnProperty.call(message, "passwordUid"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.passwordUid);
            return writer;
        };

        /**
         * Encodes the specified BreachWatchToken message, length delimited. Does not implicitly {@link Tokens.BreachWatchToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.BreachWatchToken
         * @static
         * @param {Tokens.IBreachWatchToken} message BreachWatchToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BreachWatchToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BreachWatchToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.BreachWatchToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.BreachWatchToken} BreachWatchToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BreachWatchToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.BreachWatchToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.userId = reader.int32();
                        break;
                    }
                case 2: {
                        message.domainUid = reader.bytes();
                        break;
                    }
                case 3: {
                        message.emailUid = reader.bytes();
                        break;
                    }
                case 4: {
                        message.passwordUid = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BreachWatchToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.BreachWatchToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.BreachWatchToken} BreachWatchToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BreachWatchToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BreachWatchToken message.
         * @function verify
         * @memberof Tokens.BreachWatchToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BreachWatchToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                if (!$util.isInteger(message.userId))
                    return "userId: integer expected";
            if (message.domainUid != null && Object.hasOwnProperty.call(message, "domainUid"))
                if (!(message.domainUid && typeof message.domainUid.length === "number" || $util.isString(message.domainUid)))
                    return "domainUid: buffer expected";
            if (message.emailUid != null && Object.hasOwnProperty.call(message, "emailUid"))
                if (!(message.emailUid && typeof message.emailUid.length === "number" || $util.isString(message.emailUid)))
                    return "emailUid: buffer expected";
            if (message.passwordUid != null && Object.hasOwnProperty.call(message, "passwordUid"))
                if (!(message.passwordUid && typeof message.passwordUid.length === "number" || $util.isString(message.passwordUid)))
                    return "passwordUid: buffer expected";
            return null;
        };

        /**
         * Creates a BreachWatchToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.BreachWatchToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.BreachWatchToken} BreachWatchToken
         */
        BreachWatchToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.BreachWatchToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.BreachWatchToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.BreachWatchToken();
            if (object.userId != null)
                message.userId = object.userId | 0;
            if (object.domainUid != null)
                if (typeof object.domainUid === "string")
                    $util.base64.decode(object.domainUid, message.domainUid = $util.newBuffer($util.base64.length(object.domainUid)), 0);
                else if (object.domainUid.length >= 0)
                    message.domainUid = object.domainUid;
            if (object.emailUid != null)
                if (typeof object.emailUid === "string")
                    $util.base64.decode(object.emailUid, message.emailUid = $util.newBuffer($util.base64.length(object.emailUid)), 0);
                else if (object.emailUid.length >= 0)
                    message.emailUid = object.emailUid;
            if (object.passwordUid != null)
                if (typeof object.passwordUid === "string")
                    $util.base64.decode(object.passwordUid, message.passwordUid = $util.newBuffer($util.base64.length(object.passwordUid)), 0);
                else if (object.passwordUid.length >= 0)
                    message.passwordUid = object.passwordUid;
            return message;
        };

        /**
         * Creates a plain object from a BreachWatchToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.BreachWatchToken
         * @static
         * @param {Tokens.BreachWatchToken} message BreachWatchToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BreachWatchToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.userId = 0;
                if (options.bytes === String)
                    object.domainUid = "";
                else {
                    object.domainUid = [];
                    if (options.bytes !== Array)
                        object.domainUid = $util.newBuffer(object.domainUid);
                }
                if (options.bytes === String)
                    object.emailUid = "";
                else {
                    object.emailUid = [];
                    if (options.bytes !== Array)
                        object.emailUid = $util.newBuffer(object.emailUid);
                }
                if (options.bytes === String)
                    object.passwordUid = "";
                else {
                    object.passwordUid = [];
                    if (options.bytes !== Array)
                        object.passwordUid = $util.newBuffer(object.passwordUid);
                }
            }
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                object.userId = message.userId;
            if (message.domainUid != null && Object.hasOwnProperty.call(message, "domainUid"))
                object.domainUid = options.bytes === String ? $util.base64.encode(message.domainUid, 0, message.domainUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.domainUid) : message.domainUid;
            if (message.emailUid != null && Object.hasOwnProperty.call(message, "emailUid"))
                object.emailUid = options.bytes === String ? $util.base64.encode(message.emailUid, 0, message.emailUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.emailUid) : message.emailUid;
            if (message.passwordUid != null && Object.hasOwnProperty.call(message, "passwordUid"))
                object.passwordUid = options.bytes === String ? $util.base64.encode(message.passwordUid, 0, message.passwordUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.passwordUid) : message.passwordUid;
            return object;
        };

        /**
         * Converts this BreachWatchToken to JSON.
         * @function toJSON
         * @memberof Tokens.BreachWatchToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BreachWatchToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BreachWatchToken
         * @function getTypeUrl
         * @memberof Tokens.BreachWatchToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BreachWatchToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.BreachWatchToken";
        };

        return BreachWatchToken;
    })();

    /**
     * AnonymizedTokenType enum.
     * @name Tokens.AnonymizedTokenType
     * @enum {number}
     * @property {number} DAT=0 DAT value
     * @property {number} EAT=1 EAT value
     * @property {number} PAT=2 PAT value
     */
    Tokens.AnonymizedTokenType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "DAT"] = 0;
        values[valuesById[1] = "EAT"] = 1;
        values[valuesById[2] = "PAT"] = 2;
        return values;
    })();

    Tokens.AnonymizedToken = (function() {

        /**
         * Properties of an AnonymizedToken.
         * @memberof Tokens
         * @interface IAnonymizedToken
         * @property {number|null} [created] AnonymizedToken created
         * @property {number|null} [expiration] AnonymizedToken expiration
         * @property {Uint8Array|null} [uid] AnonymizedToken uid
         * @property {Tokens.AnonymizedTokenType|null} [anonymizedTokenType] AnonymizedToken anonymizedTokenType
         */

        /**
         * Constructs a new AnonymizedToken.
         * @memberof Tokens
         * @classdesc Represents an AnonymizedToken.
         * @implements IAnonymizedToken
         * @constructor
         * @param {Tokens.IAnonymizedToken=} [properties] Properties to set
         */
        function AnonymizedToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * AnonymizedToken created.
         * @member {number} created
         * @memberof Tokens.AnonymizedToken
         * @instance
         */
        AnonymizedToken.prototype.created = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AnonymizedToken expiration.
         * @member {number} expiration
         * @memberof Tokens.AnonymizedToken
         * @instance
         */
        AnonymizedToken.prototype.expiration = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * AnonymizedToken uid.
         * @member {Uint8Array} uid
         * @memberof Tokens.AnonymizedToken
         * @instance
         */
        AnonymizedToken.prototype.uid = $util.newBuffer([]);

        /**
         * AnonymizedToken anonymizedTokenType.
         * @member {Tokens.AnonymizedTokenType} anonymizedTokenType
         * @memberof Tokens.AnonymizedToken
         * @instance
         */
        AnonymizedToken.prototype.anonymizedTokenType = 0;

        /**
         * Creates a new AnonymizedToken instance using the specified properties.
         * @function create
         * @memberof Tokens.AnonymizedToken
         * @static
         * @param {Tokens.IAnonymizedToken=} [properties] Properties to set
         * @returns {Tokens.AnonymizedToken} AnonymizedToken instance
         */
        AnonymizedToken.create = function create(properties) {
            return new AnonymizedToken(properties);
        };

        /**
         * Encodes the specified AnonymizedToken message. Does not implicitly {@link Tokens.AnonymizedToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.AnonymizedToken
         * @static
         * @param {Tokens.IAnonymizedToken} message AnonymizedToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AnonymizedToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.created != null && Object.hasOwnProperty.call(message, "created"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.created);
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.expiration);
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.uid);
            if (message.anonymizedTokenType != null && Object.hasOwnProperty.call(message, "anonymizedTokenType"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.anonymizedTokenType);
            return writer;
        };

        /**
         * Encodes the specified AnonymizedToken message, length delimited. Does not implicitly {@link Tokens.AnonymizedToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.AnonymizedToken
         * @static
         * @param {Tokens.IAnonymizedToken} message AnonymizedToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        AnonymizedToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an AnonymizedToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.AnonymizedToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.AnonymizedToken} AnonymizedToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AnonymizedToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.AnonymizedToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.created = reader.int64();
                        break;
                    }
                case 2: {
                        message.expiration = reader.int64();
                        break;
                    }
                case 3: {
                        message.uid = reader.bytes();
                        break;
                    }
                case 4: {
                        message.anonymizedTokenType = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an AnonymizedToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.AnonymizedToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.AnonymizedToken} AnonymizedToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        AnonymizedToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an AnonymizedToken message.
         * @function verify
         * @memberof Tokens.AnonymizedToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        AnonymizedToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.created != null && Object.hasOwnProperty.call(message, "created"))
                if (!$util.isInteger(message.created) && !(message.created && $util.isInteger(message.created.low) && $util.isInteger(message.created.high)))
                    return "created: integer|Long expected";
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                if (!$util.isInteger(message.expiration) && !(message.expiration && $util.isInteger(message.expiration.low) && $util.isInteger(message.expiration.high)))
                    return "expiration: integer|Long expected";
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                if (!(message.uid && typeof message.uid.length === "number" || $util.isString(message.uid)))
                    return "uid: buffer expected";
            if (message.anonymizedTokenType != null && Object.hasOwnProperty.call(message, "anonymizedTokenType"))
                switch (message.anonymizedTokenType) {
                default:
                    return "anonymizedTokenType: enum value expected";
                case 0:
                case 1:
                case 2:
                    break;
                }
            return null;
        };

        /**
         * Creates an AnonymizedToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.AnonymizedToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.AnonymizedToken} AnonymizedToken
         */
        AnonymizedToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.AnonymizedToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.AnonymizedToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.AnonymizedToken();
            if (object.created != null)
                if ($util.Long)
                    message.created = $util.Long.fromValue(object.created, false);
                else if (typeof object.created === "string")
                    message.created = parseInt(object.created, 10);
                else if (typeof object.created === "number")
                    message.created = object.created;
                else if (typeof object.created === "object")
                    message.created = new $util.LongBits(object.created.low >>> 0, object.created.high >>> 0).toNumber();
            if (object.expiration != null)
                if ($util.Long)
                    message.expiration = $util.Long.fromValue(object.expiration, false);
                else if (typeof object.expiration === "string")
                    message.expiration = parseInt(object.expiration, 10);
                else if (typeof object.expiration === "number")
                    message.expiration = object.expiration;
                else if (typeof object.expiration === "object")
                    message.expiration = new $util.LongBits(object.expiration.low >>> 0, object.expiration.high >>> 0).toNumber();
            if (object.uid != null)
                if (typeof object.uid === "string")
                    $util.base64.decode(object.uid, message.uid = $util.newBuffer($util.base64.length(object.uid)), 0);
                else if (object.uid.length >= 0)
                    message.uid = object.uid;
            switch (object.anonymizedTokenType) {
            default:
                if (typeof object.anonymizedTokenType === "number") {
                    message.anonymizedTokenType = object.anonymizedTokenType;
                    break;
                }
                break;
            case "DAT":
            case 0:
                message.anonymizedTokenType = 0;
                break;
            case "EAT":
            case 1:
                message.anonymizedTokenType = 1;
                break;
            case "PAT":
            case 2:
                message.anonymizedTokenType = 2;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from an AnonymizedToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.AnonymizedToken
         * @static
         * @param {Tokens.AnonymizedToken} message AnonymizedToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        AnonymizedToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.created = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.created = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expiration = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.expiration = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.uid = "";
                else {
                    object.uid = [];
                    if (options.bytes !== Array)
                        object.uid = $util.newBuffer(object.uid);
                }
                object.anonymizedTokenType = options.enums === String ? "DAT" : 0;
            }
            if (message.created != null && Object.hasOwnProperty.call(message, "created"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.created = typeof message.created === "number" ? BigInt(message.created) : $util.Long.fromBits(message.created.low >>> 0, message.created.high >>> 0, false).toBigInt();
                else if (typeof message.created === "number")
                    object.created = options.longs === String ? String(message.created) : message.created;
                else
                    object.created = options.longs === String ? $util.Long.prototype.toString.call(message.created) : options.longs === Number ? new $util.LongBits(message.created.low >>> 0, message.created.high >>> 0).toNumber() : message.created;
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.expiration = typeof message.expiration === "number" ? BigInt(message.expiration) : $util.Long.fromBits(message.expiration.low >>> 0, message.expiration.high >>> 0, false).toBigInt();
                else if (typeof message.expiration === "number")
                    object.expiration = options.longs === String ? String(message.expiration) : message.expiration;
                else
                    object.expiration = options.longs === String ? $util.Long.prototype.toString.call(message.expiration) : options.longs === Number ? new $util.LongBits(message.expiration.low >>> 0, message.expiration.high >>> 0).toNumber() : message.expiration;
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                object.uid = options.bytes === String ? $util.base64.encode(message.uid, 0, message.uid.length) : options.bytes === Array ? Array.prototype.slice.call(message.uid) : message.uid;
            if (message.anonymizedTokenType != null && Object.hasOwnProperty.call(message, "anonymizedTokenType"))
                object.anonymizedTokenType = options.enums === String ? $root.Tokens.AnonymizedTokenType[message.anonymizedTokenType] === undefined ? message.anonymizedTokenType : $root.Tokens.AnonymizedTokenType[message.anonymizedTokenType] : message.anonymizedTokenType;
            return object;
        };

        /**
         * Converts this AnonymizedToken to JSON.
         * @function toJSON
         * @memberof Tokens.AnonymizedToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        AnonymizedToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for AnonymizedToken
         * @function getTypeUrl
         * @memberof Tokens.AnonymizedToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        AnonymizedToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.AnonymizedToken";
        };

        return AnonymizedToken;
    })();

    Tokens.LicenseToken = (function() {

        /**
         * Properties of a LicenseToken.
         * @memberof Tokens
         * @interface ILicenseToken
         * @property {number|null} [expiration] LicenseToken expiration
         * @property {Authentication.LicenseStatus|null} [licenseStatus] LicenseToken licenseStatus
         */

        /**
         * Constructs a new LicenseToken.
         * @memberof Tokens
         * @classdesc Represents a LicenseToken.
         * @implements ILicenseToken
         * @constructor
         * @param {Tokens.ILicenseToken=} [properties] Properties to set
         */
        function LicenseToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LicenseToken expiration.
         * @member {number} expiration
         * @memberof Tokens.LicenseToken
         * @instance
         */
        LicenseToken.prototype.expiration = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LicenseToken licenseStatus.
         * @member {Authentication.LicenseStatus} licenseStatus
         * @memberof Tokens.LicenseToken
         * @instance
         */
        LicenseToken.prototype.licenseStatus = 0;

        /**
         * Creates a new LicenseToken instance using the specified properties.
         * @function create
         * @memberof Tokens.LicenseToken
         * @static
         * @param {Tokens.ILicenseToken=} [properties] Properties to set
         * @returns {Tokens.LicenseToken} LicenseToken instance
         */
        LicenseToken.create = function create(properties) {
            return new LicenseToken(properties);
        };

        /**
         * Encodes the specified LicenseToken message. Does not implicitly {@link Tokens.LicenseToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.LicenseToken
         * @static
         * @param {Tokens.ILicenseToken} message LicenseToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LicenseToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.expiration);
            if (message.licenseStatus != null && Object.hasOwnProperty.call(message, "licenseStatus"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.licenseStatus);
            return writer;
        };

        /**
         * Encodes the specified LicenseToken message, length delimited. Does not implicitly {@link Tokens.LicenseToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.LicenseToken
         * @static
         * @param {Tokens.ILicenseToken} message LicenseToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LicenseToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LicenseToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.LicenseToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.LicenseToken} LicenseToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LicenseToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.LicenseToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 2: {
                        message.expiration = reader.int64();
                        break;
                    }
                case 3: {
                        message.licenseStatus = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LicenseToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.LicenseToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.LicenseToken} LicenseToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LicenseToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LicenseToken message.
         * @function verify
         * @memberof Tokens.LicenseToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LicenseToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                if (!$util.isInteger(message.expiration) && !(message.expiration && $util.isInteger(message.expiration.low) && $util.isInteger(message.expiration.high)))
                    return "expiration: integer|Long expected";
            if (message.licenseStatus != null && Object.hasOwnProperty.call(message, "licenseStatus"))
                switch (message.licenseStatus) {
                default:
                    return "licenseStatus: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                    break;
                }
            return null;
        };

        /**
         * Creates a LicenseToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.LicenseToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.LicenseToken} LicenseToken
         */
        LicenseToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.LicenseToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.LicenseToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.LicenseToken();
            if (object.expiration != null)
                if ($util.Long)
                    message.expiration = $util.Long.fromValue(object.expiration, false);
                else if (typeof object.expiration === "string")
                    message.expiration = parseInt(object.expiration, 10);
                else if (typeof object.expiration === "number")
                    message.expiration = object.expiration;
                else if (typeof object.expiration === "object")
                    message.expiration = new $util.LongBits(object.expiration.low >>> 0, object.expiration.high >>> 0).toNumber();
            switch (object.licenseStatus) {
            default:
                if (typeof object.licenseStatus === "number") {
                    message.licenseStatus = object.licenseStatus;
                    break;
                }
                break;
            case "OTHER":
            case 0:
                message.licenseStatus = 0;
                break;
            case "ACTIVE":
            case 1:
                message.licenseStatus = 1;
                break;
            case "EXPIRED":
            case 2:
                message.licenseStatus = 2;
                break;
            case "DISABLED":
            case 3:
                message.licenseStatus = 3;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a LicenseToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.LicenseToken
         * @static
         * @param {Tokens.LicenseToken} message LicenseToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LicenseToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expiration = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.expiration = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.licenseStatus = options.enums === String ? "OTHER" : 0;
            }
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.expiration = typeof message.expiration === "number" ? BigInt(message.expiration) : $util.Long.fromBits(message.expiration.low >>> 0, message.expiration.high >>> 0, false).toBigInt();
                else if (typeof message.expiration === "number")
                    object.expiration = options.longs === String ? String(message.expiration) : message.expiration;
                else
                    object.expiration = options.longs === String ? $util.Long.prototype.toString.call(message.expiration) : options.longs === Number ? new $util.LongBits(message.expiration.low >>> 0, message.expiration.high >>> 0).toNumber() : message.expiration;
            if (message.licenseStatus != null && Object.hasOwnProperty.call(message, "licenseStatus"))
                object.licenseStatus = options.enums === String ? $root.Authentication.LicenseStatus[message.licenseStatus] === undefined ? message.licenseStatus : $root.Authentication.LicenseStatus[message.licenseStatus] : message.licenseStatus;
            return object;
        };

        /**
         * Converts this LicenseToken to JSON.
         * @function toJSON
         * @memberof Tokens.LicenseToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LicenseToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LicenseToken
         * @function getTypeUrl
         * @memberof Tokens.LicenseToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LicenseToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.LicenseToken";
        };

        return LicenseToken;
    })();

    /**
     * AccessType enum.
     * @name Tokens.AccessType
     * @enum {number}
     * @property {number} VAULT_RW=0 VAULT_RW value
     * @property {number} VAULT_RO=1 VAULT_RO value
     * @property {number} ADMIN=2 ADMIN value
     */
    Tokens.AccessType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "VAULT_RW"] = 0;
        values[valuesById[1] = "VAULT_RO"] = 1;
        values[valuesById[2] = "ADMIN"] = 2;
        return values;
    })();

    Tokens.SessionToken = (function() {

        /**
         * Properties of a SessionToken.
         * @memberof Tokens
         * @interface ISessionToken
         * @property {number|null} [created] SessionToken created
         * @property {number|null} [expiration] SessionToken expiration
         * @property {string|null} [du1] SessionToken du1
         * @property {Uint8Array|null} [messageSessionUid] SessionToken messageSessionUid
         * @property {Tokens.IDeviceToken|null} [du8] SessionToken du8
         * @property {number|null} [fromUserId] SessionToken fromUserId
         * @property {number|null} [forUserId] SessionToken forUserId
         * @property {number|null} [enterpriseUserId] SessionToken enterpriseUserId
         * @property {Tokens.ILicenseToken|null} [du2] SessionToken du2
         * @property {Tokens.ILicenseToken|null} [du3] SessionToken du3
         * @property {Tokens.ILicenseToken|null} [du4] SessionToken du4
         * @property {Tokens.ILicenseToken|null} [du5] SessionToken du5
         * @property {Authentication.AccountType|null} [du6] SessionToken du6
         * @property {number|null} [clientVersionId] SessionToken clientVersionId
         * @property {Authentication.SessionTokenType|null} [sessionTokenType] SessionToken sessionTokenType
         * @property {number|null} [mcEnterpriseId] SessionToken mcEnterpriseId
         * @property {boolean|null} [du7] SessionToken du7
         * @property {number|null} [deviceId] SessionToken deviceId
         * @property {number|null} [loginSessionId] SessionToken loginSessionId
         * @property {number|null} [clientTypeId] SessionToken clientTypeId
         * @property {number|null} [ipAddress] SessionToken ipAddress
         * @property {number|null} [latitude] SessionToken latitude
         * @property {number|null} [longitude] SessionToken longitude
         */

        /**
         * Constructs a new SessionToken.
         * @memberof Tokens
         * @classdesc Represents a SessionToken.
         * @implements ISessionToken
         * @constructor
         * @param {Tokens.ISessionToken=} [properties] Properties to set
         */
        function SessionToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SessionToken created.
         * @member {number} created
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.created = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SessionToken expiration.
         * @member {number} expiration
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.expiration = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SessionToken du1.
         * @member {string} du1
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.du1 = "";

        /**
         * SessionToken messageSessionUid.
         * @member {Uint8Array} messageSessionUid
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.messageSessionUid = $util.newBuffer([]);

        /**
         * SessionToken du8.
         * @member {Tokens.IDeviceToken|null|undefined} du8
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.du8 = null;

        /**
         * SessionToken fromUserId.
         * @member {number} fromUserId
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.fromUserId = 0;

        /**
         * SessionToken forUserId.
         * @member {number} forUserId
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.forUserId = 0;

        /**
         * SessionToken enterpriseUserId.
         * @member {number} enterpriseUserId
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.enterpriseUserId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SessionToken du2.
         * @member {Tokens.ILicenseToken|null|undefined} du2
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.du2 = null;

        /**
         * SessionToken du3.
         * @member {Tokens.ILicenseToken|null|undefined} du3
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.du3 = null;

        /**
         * SessionToken du4.
         * @member {Tokens.ILicenseToken|null|undefined} du4
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.du4 = null;

        /**
         * SessionToken du5.
         * @member {Tokens.ILicenseToken|null|undefined} du5
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.du5 = null;

        /**
         * SessionToken du6.
         * @member {Authentication.AccountType} du6
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.du6 = 0;

        /**
         * SessionToken clientVersionId.
         * @member {number} clientVersionId
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.clientVersionId = 0;

        /**
         * SessionToken sessionTokenType.
         * @member {Authentication.SessionTokenType} sessionTokenType
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.sessionTokenType = 0;

        /**
         * SessionToken mcEnterpriseId.
         * @member {number} mcEnterpriseId
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.mcEnterpriseId = 0;

        /**
         * SessionToken du7.
         * @member {boolean} du7
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.du7 = false;

        /**
         * SessionToken deviceId.
         * @member {number} deviceId
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.deviceId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SessionToken loginSessionId.
         * @member {number} loginSessionId
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.loginSessionId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SessionToken clientTypeId.
         * @member {number} clientTypeId
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.clientTypeId = 0;

        /**
         * SessionToken ipAddress.
         * @member {number} ipAddress
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.ipAddress = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SessionToken latitude.
         * @member {number} latitude
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.latitude = 0;

        /**
         * SessionToken longitude.
         * @member {number} longitude
         * @memberof Tokens.SessionToken
         * @instance
         */
        SessionToken.prototype.longitude = 0;

        /**
         * Creates a new SessionToken instance using the specified properties.
         * @function create
         * @memberof Tokens.SessionToken
         * @static
         * @param {Tokens.ISessionToken=} [properties] Properties to set
         * @returns {Tokens.SessionToken} SessionToken instance
         */
        SessionToken.create = function create(properties) {
            return new SessionToken(properties);
        };

        /**
         * Encodes the specified SessionToken message. Does not implicitly {@link Tokens.SessionToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.SessionToken
         * @static
         * @param {Tokens.ISessionToken} message SessionToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SessionToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.created != null && Object.hasOwnProperty.call(message, "created"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.created);
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.expiration);
            if (message.du1 != null && Object.hasOwnProperty.call(message, "du1"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.du1);
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.messageSessionUid);
            if (message.du8 != null && Object.hasOwnProperty.call(message, "du8"))
                $root.Tokens.DeviceToken.encode(message.du8, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            if (message.fromUserId != null && Object.hasOwnProperty.call(message, "fromUserId"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.fromUserId);
            if (message.forUserId != null && Object.hasOwnProperty.call(message, "forUserId"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.forUserId);
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.enterpriseUserId);
            if (message.du2 != null && Object.hasOwnProperty.call(message, "du2"))
                $root.Tokens.LicenseToken.encode(message.du2, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
            if (message.du3 != null && Object.hasOwnProperty.call(message, "du3"))
                $root.Tokens.LicenseToken.encode(message.du3, writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
            if (message.du4 != null && Object.hasOwnProperty.call(message, "du4"))
                $root.Tokens.LicenseToken.encode(message.du4, writer.uint32(/* id 11, wireType 2 =*/90).fork(), q + 1).ldelim();
            if (message.du6 != null && Object.hasOwnProperty.call(message, "du6"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.du6);
            if (message.clientVersionId != null && Object.hasOwnProperty.call(message, "clientVersionId"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.clientVersionId);
            if (message.sessionTokenType != null && Object.hasOwnProperty.call(message, "sessionTokenType"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.sessionTokenType);
            if (message.du5 != null && Object.hasOwnProperty.call(message, "du5"))
                $root.Tokens.LicenseToken.encode(message.du5, writer.uint32(/* id 15, wireType 2 =*/122).fork(), q + 1).ldelim();
            if (message.mcEnterpriseId != null && Object.hasOwnProperty.call(message, "mcEnterpriseId"))
                writer.uint32(/* id 16, wireType 0 =*/128).int32(message.mcEnterpriseId);
            if (message.du7 != null && Object.hasOwnProperty.call(message, "du7"))
                writer.uint32(/* id 17, wireType 0 =*/136).bool(message.du7);
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                writer.uint32(/* id 18, wireType 0 =*/144).int64(message.deviceId);
            if (message.loginSessionId != null && Object.hasOwnProperty.call(message, "loginSessionId"))
                writer.uint32(/* id 19, wireType 0 =*/152).int64(message.loginSessionId);
            if (message.clientTypeId != null && Object.hasOwnProperty.call(message, "clientTypeId"))
                writer.uint32(/* id 20, wireType 0 =*/160).int32(message.clientTypeId);
            if (message.ipAddress != null && Object.hasOwnProperty.call(message, "ipAddress"))
                writer.uint32(/* id 21, wireType 0 =*/168).int64(message.ipAddress);
            if (message.latitude != null && Object.hasOwnProperty.call(message, "latitude"))
                writer.uint32(/* id 22, wireType 1 =*/177).double(message.latitude);
            if (message.longitude != null && Object.hasOwnProperty.call(message, "longitude"))
                writer.uint32(/* id 23, wireType 1 =*/185).double(message.longitude);
            return writer;
        };

        /**
         * Encodes the specified SessionToken message, length delimited. Does not implicitly {@link Tokens.SessionToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.SessionToken
         * @static
         * @param {Tokens.ISessionToken} message SessionToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SessionToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SessionToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.SessionToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.SessionToken} SessionToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SessionToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.SessionToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.created = reader.int64();
                        break;
                    }
                case 2: {
                        message.expiration = reader.int64();
                        break;
                    }
                case 3: {
                        message.du1 = reader.string();
                        break;
                    }
                case 4: {
                        message.messageSessionUid = reader.bytes();
                        break;
                    }
                case 5: {
                        message.du8 = $root.Tokens.DeviceToken.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 6: {
                        message.fromUserId = reader.int32();
                        break;
                    }
                case 7: {
                        message.forUserId = reader.int32();
                        break;
                    }
                case 8: {
                        message.enterpriseUserId = reader.int64();
                        break;
                    }
                case 9: {
                        message.du2 = $root.Tokens.LicenseToken.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 10: {
                        message.du3 = $root.Tokens.LicenseToken.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 11: {
                        message.du4 = $root.Tokens.LicenseToken.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 15: {
                        message.du5 = $root.Tokens.LicenseToken.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 12: {
                        message.du6 = reader.int32();
                        break;
                    }
                case 13: {
                        message.clientVersionId = reader.int32();
                        break;
                    }
                case 14: {
                        message.sessionTokenType = reader.int32();
                        break;
                    }
                case 16: {
                        message.mcEnterpriseId = reader.int32();
                        break;
                    }
                case 17: {
                        message.du7 = reader.bool();
                        break;
                    }
                case 18: {
                        message.deviceId = reader.int64();
                        break;
                    }
                case 19: {
                        message.loginSessionId = reader.int64();
                        break;
                    }
                case 20: {
                        message.clientTypeId = reader.int32();
                        break;
                    }
                case 21: {
                        message.ipAddress = reader.int64();
                        break;
                    }
                case 22: {
                        message.latitude = reader.double();
                        break;
                    }
                case 23: {
                        message.longitude = reader.double();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SessionToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.SessionToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.SessionToken} SessionToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SessionToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SessionToken message.
         * @function verify
         * @memberof Tokens.SessionToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SessionToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.created != null && Object.hasOwnProperty.call(message, "created"))
                if (!$util.isInteger(message.created) && !(message.created && $util.isInteger(message.created.low) && $util.isInteger(message.created.high)))
                    return "created: integer|Long expected";
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                if (!$util.isInteger(message.expiration) && !(message.expiration && $util.isInteger(message.expiration.low) && $util.isInteger(message.expiration.high)))
                    return "expiration: integer|Long expected";
            if (message.du1 != null && Object.hasOwnProperty.call(message, "du1"))
                if (!$util.isString(message.du1))
                    return "du1: string expected";
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                if (!(message.messageSessionUid && typeof message.messageSessionUid.length === "number" || $util.isString(message.messageSessionUid)))
                    return "messageSessionUid: buffer expected";
            if (message.du8 != null && Object.hasOwnProperty.call(message, "du8")) {
                let error = $root.Tokens.DeviceToken.verify(message.du8, long + 1);
                if (error)
                    return "du8." + error;
            }
            if (message.fromUserId != null && Object.hasOwnProperty.call(message, "fromUserId"))
                if (!$util.isInteger(message.fromUserId))
                    return "fromUserId: integer expected";
            if (message.forUserId != null && Object.hasOwnProperty.call(message, "forUserId"))
                if (!$util.isInteger(message.forUserId))
                    return "forUserId: integer expected";
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                if (!$util.isInteger(message.enterpriseUserId) && !(message.enterpriseUserId && $util.isInteger(message.enterpriseUserId.low) && $util.isInteger(message.enterpriseUserId.high)))
                    return "enterpriseUserId: integer|Long expected";
            if (message.du2 != null && Object.hasOwnProperty.call(message, "du2")) {
                let error = $root.Tokens.LicenseToken.verify(message.du2, long + 1);
                if (error)
                    return "du2." + error;
            }
            if (message.du3 != null && Object.hasOwnProperty.call(message, "du3")) {
                let error = $root.Tokens.LicenseToken.verify(message.du3, long + 1);
                if (error)
                    return "du3." + error;
            }
            if (message.du4 != null && Object.hasOwnProperty.call(message, "du4")) {
                let error = $root.Tokens.LicenseToken.verify(message.du4, long + 1);
                if (error)
                    return "du4." + error;
            }
            if (message.du5 != null && Object.hasOwnProperty.call(message, "du5")) {
                let error = $root.Tokens.LicenseToken.verify(message.du5, long + 1);
                if (error)
                    return "du5." + error;
            }
            if (message.du6 != null && Object.hasOwnProperty.call(message, "du6"))
                switch (message.du6) {
                default:
                    return "du6: enum value expected";
                case 0:
                case 1:
                case 2:
                    break;
                }
            if (message.clientVersionId != null && Object.hasOwnProperty.call(message, "clientVersionId"))
                if (!$util.isInteger(message.clientVersionId))
                    return "clientVersionId: integer expected";
            if (message.sessionTokenType != null && Object.hasOwnProperty.call(message, "sessionTokenType"))
                switch (message.sessionTokenType) {
                default:
                    return "sessionTokenType: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                case 11:
                    break;
                }
            if (message.mcEnterpriseId != null && Object.hasOwnProperty.call(message, "mcEnterpriseId"))
                if (!$util.isInteger(message.mcEnterpriseId))
                    return "mcEnterpriseId: integer expected";
            if (message.du7 != null && Object.hasOwnProperty.call(message, "du7"))
                if (typeof message.du7 !== "boolean")
                    return "du7: boolean expected";
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                if (!$util.isInteger(message.deviceId) && !(message.deviceId && $util.isInteger(message.deviceId.low) && $util.isInteger(message.deviceId.high)))
                    return "deviceId: integer|Long expected";
            if (message.loginSessionId != null && Object.hasOwnProperty.call(message, "loginSessionId"))
                if (!$util.isInteger(message.loginSessionId) && !(message.loginSessionId && $util.isInteger(message.loginSessionId.low) && $util.isInteger(message.loginSessionId.high)))
                    return "loginSessionId: integer|Long expected";
            if (message.clientTypeId != null && Object.hasOwnProperty.call(message, "clientTypeId"))
                if (!$util.isInteger(message.clientTypeId))
                    return "clientTypeId: integer expected";
            if (message.ipAddress != null && Object.hasOwnProperty.call(message, "ipAddress"))
                if (!$util.isInteger(message.ipAddress) && !(message.ipAddress && $util.isInteger(message.ipAddress.low) && $util.isInteger(message.ipAddress.high)))
                    return "ipAddress: integer|Long expected";
            if (message.latitude != null && Object.hasOwnProperty.call(message, "latitude"))
                if (typeof message.latitude !== "number")
                    return "latitude: number expected";
            if (message.longitude != null && Object.hasOwnProperty.call(message, "longitude"))
                if (typeof message.longitude !== "number")
                    return "longitude: number expected";
            return null;
        };

        /**
         * Creates a SessionToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.SessionToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.SessionToken} SessionToken
         */
        SessionToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.SessionToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.SessionToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.SessionToken();
            if (object.created != null)
                if ($util.Long)
                    message.created = $util.Long.fromValue(object.created, false);
                else if (typeof object.created === "string")
                    message.created = parseInt(object.created, 10);
                else if (typeof object.created === "number")
                    message.created = object.created;
                else if (typeof object.created === "object")
                    message.created = new $util.LongBits(object.created.low >>> 0, object.created.high >>> 0).toNumber();
            if (object.expiration != null)
                if ($util.Long)
                    message.expiration = $util.Long.fromValue(object.expiration, false);
                else if (typeof object.expiration === "string")
                    message.expiration = parseInt(object.expiration, 10);
                else if (typeof object.expiration === "number")
                    message.expiration = object.expiration;
                else if (typeof object.expiration === "object")
                    message.expiration = new $util.LongBits(object.expiration.low >>> 0, object.expiration.high >>> 0).toNumber();
            if (object.du1 != null)
                message.du1 = String(object.du1);
            if (object.messageSessionUid != null)
                if (typeof object.messageSessionUid === "string")
                    $util.base64.decode(object.messageSessionUid, message.messageSessionUid = $util.newBuffer($util.base64.length(object.messageSessionUid)), 0);
                else if (object.messageSessionUid.length >= 0)
                    message.messageSessionUid = object.messageSessionUid;
            if (object.du8 != null) {
                if (!$util.isObject(object.du8))
                    throw TypeError(".Tokens.SessionToken.du8: object expected");
                message.du8 = $root.Tokens.DeviceToken.fromObject(object.du8, long + 1);
            }
            if (object.fromUserId != null)
                message.fromUserId = object.fromUserId | 0;
            if (object.forUserId != null)
                message.forUserId = object.forUserId | 0;
            if (object.enterpriseUserId != null)
                if ($util.Long)
                    message.enterpriseUserId = $util.Long.fromValue(object.enterpriseUserId, false);
                else if (typeof object.enterpriseUserId === "string")
                    message.enterpriseUserId = parseInt(object.enterpriseUserId, 10);
                else if (typeof object.enterpriseUserId === "number")
                    message.enterpriseUserId = object.enterpriseUserId;
                else if (typeof object.enterpriseUserId === "object")
                    message.enterpriseUserId = new $util.LongBits(object.enterpriseUserId.low >>> 0, object.enterpriseUserId.high >>> 0).toNumber();
            if (object.du2 != null) {
                if (!$util.isObject(object.du2))
                    throw TypeError(".Tokens.SessionToken.du2: object expected");
                message.du2 = $root.Tokens.LicenseToken.fromObject(object.du2, long + 1);
            }
            if (object.du3 != null) {
                if (!$util.isObject(object.du3))
                    throw TypeError(".Tokens.SessionToken.du3: object expected");
                message.du3 = $root.Tokens.LicenseToken.fromObject(object.du3, long + 1);
            }
            if (object.du4 != null) {
                if (!$util.isObject(object.du4))
                    throw TypeError(".Tokens.SessionToken.du4: object expected");
                message.du4 = $root.Tokens.LicenseToken.fromObject(object.du4, long + 1);
            }
            if (object.du5 != null) {
                if (!$util.isObject(object.du5))
                    throw TypeError(".Tokens.SessionToken.du5: object expected");
                message.du5 = $root.Tokens.LicenseToken.fromObject(object.du5, long + 1);
            }
            switch (object.du6) {
            default:
                if (typeof object.du6 === "number") {
                    message.du6 = object.du6;
                    break;
                }
                break;
            case "CONSUMER":
            case 0:
                message.du6 = 0;
                break;
            case "FAMILY":
            case 1:
                message.du6 = 1;
                break;
            case "ENTERPRISE":
            case 2:
                message.du6 = 2;
                break;
            }
            if (object.clientVersionId != null)
                message.clientVersionId = object.clientVersionId | 0;
            switch (object.sessionTokenType) {
            default:
                if (typeof object.sessionTokenType === "number") {
                    message.sessionTokenType = object.sessionTokenType;
                    break;
                }
                break;
            case "NO_RESTRICTION":
            case 0:
                message.sessionTokenType = 0;
                break;
            case "ACCOUNT_RECOVERY":
            case 1:
                message.sessionTokenType = 1;
                break;
            case "SHARE_ACCOUNT":
            case 2:
                message.sessionTokenType = 2;
                break;
            case "PURCHASE":
            case 3:
                message.sessionTokenType = 3;
                break;
            case "RESTRICT":
            case 4:
                message.sessionTokenType = 4;
                break;
            case "ACCEPT_INVITE":
            case 5:
                message.sessionTokenType = 5;
                break;
            case "SUPPORT_SERVER":
            case 6:
                message.sessionTokenType = 6;
                break;
            case "ENTERPRISE_CREATION":
            case 7:
                message.sessionTokenType = 7;
                break;
            case "EXPIRED_BUT_ALLOWED_TO_SYNC":
            case 8:
                message.sessionTokenType = 8;
                break;
            case "ACCEPT_FAMILY_INVITE":
            case 9:
                message.sessionTokenType = 9;
                break;
            case "ENTERPRISE_CREATION_PURCHASED":
            case 10:
                message.sessionTokenType = 10;
                break;
            case "EMERGENCY_ACCESS":
            case 11:
                message.sessionTokenType = 11;
                break;
            }
            if (object.mcEnterpriseId != null)
                message.mcEnterpriseId = object.mcEnterpriseId | 0;
            if (object.du7 != null)
                message.du7 = Boolean(object.du7);
            if (object.deviceId != null)
                if ($util.Long)
                    message.deviceId = $util.Long.fromValue(object.deviceId, false);
                else if (typeof object.deviceId === "string")
                    message.deviceId = parseInt(object.deviceId, 10);
                else if (typeof object.deviceId === "number")
                    message.deviceId = object.deviceId;
                else if (typeof object.deviceId === "object")
                    message.deviceId = new $util.LongBits(object.deviceId.low >>> 0, object.deviceId.high >>> 0).toNumber();
            if (object.loginSessionId != null)
                if ($util.Long)
                    message.loginSessionId = $util.Long.fromValue(object.loginSessionId, false);
                else if (typeof object.loginSessionId === "string")
                    message.loginSessionId = parseInt(object.loginSessionId, 10);
                else if (typeof object.loginSessionId === "number")
                    message.loginSessionId = object.loginSessionId;
                else if (typeof object.loginSessionId === "object")
                    message.loginSessionId = new $util.LongBits(object.loginSessionId.low >>> 0, object.loginSessionId.high >>> 0).toNumber();
            if (object.clientTypeId != null)
                message.clientTypeId = object.clientTypeId | 0;
            if (object.ipAddress != null)
                if ($util.Long)
                    message.ipAddress = $util.Long.fromValue(object.ipAddress, false);
                else if (typeof object.ipAddress === "string")
                    message.ipAddress = parseInt(object.ipAddress, 10);
                else if (typeof object.ipAddress === "number")
                    message.ipAddress = object.ipAddress;
                else if (typeof object.ipAddress === "object")
                    message.ipAddress = new $util.LongBits(object.ipAddress.low >>> 0, object.ipAddress.high >>> 0).toNumber();
            if (object.latitude != null)
                message.latitude = Number(object.latitude);
            if (object.longitude != null)
                message.longitude = Number(object.longitude);
            return message;
        };

        /**
         * Creates a plain object from a SessionToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.SessionToken
         * @static
         * @param {Tokens.SessionToken} message SessionToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SessionToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.created = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.created = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expiration = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.expiration = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.du1 = "";
                if (options.bytes === String)
                    object.messageSessionUid = "";
                else {
                    object.messageSessionUid = [];
                    if (options.bytes !== Array)
                        object.messageSessionUid = $util.newBuffer(object.messageSessionUid);
                }
                object.du8 = null;
                object.fromUserId = 0;
                object.forUserId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.enterpriseUserId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.enterpriseUserId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.du2 = null;
                object.du3 = null;
                object.du4 = null;
                object.du6 = options.enums === String ? "CONSUMER" : 0;
                object.clientVersionId = 0;
                object.sessionTokenType = options.enums === String ? "NO_RESTRICTION" : 0;
                object.du5 = null;
                object.mcEnterpriseId = 0;
                object.du7 = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.deviceId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.deviceId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.loginSessionId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.loginSessionId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.clientTypeId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.ipAddress = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.ipAddress = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.latitude = 0;
                object.longitude = 0;
            }
            if (message.created != null && Object.hasOwnProperty.call(message, "created"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.created = typeof message.created === "number" ? BigInt(message.created) : $util.Long.fromBits(message.created.low >>> 0, message.created.high >>> 0, false).toBigInt();
                else if (typeof message.created === "number")
                    object.created = options.longs === String ? String(message.created) : message.created;
                else
                    object.created = options.longs === String ? $util.Long.prototype.toString.call(message.created) : options.longs === Number ? new $util.LongBits(message.created.low >>> 0, message.created.high >>> 0).toNumber() : message.created;
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.expiration = typeof message.expiration === "number" ? BigInt(message.expiration) : $util.Long.fromBits(message.expiration.low >>> 0, message.expiration.high >>> 0, false).toBigInt();
                else if (typeof message.expiration === "number")
                    object.expiration = options.longs === String ? String(message.expiration) : message.expiration;
                else
                    object.expiration = options.longs === String ? $util.Long.prototype.toString.call(message.expiration) : options.longs === Number ? new $util.LongBits(message.expiration.low >>> 0, message.expiration.high >>> 0).toNumber() : message.expiration;
            if (message.du1 != null && Object.hasOwnProperty.call(message, "du1"))
                object.du1 = message.du1;
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                object.messageSessionUid = options.bytes === String ? $util.base64.encode(message.messageSessionUid, 0, message.messageSessionUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.messageSessionUid) : message.messageSessionUid;
            if (message.du8 != null && Object.hasOwnProperty.call(message, "du8"))
                object.du8 = $root.Tokens.DeviceToken.toObject(message.du8, options, q + 1);
            if (message.fromUserId != null && Object.hasOwnProperty.call(message, "fromUserId"))
                object.fromUserId = message.fromUserId;
            if (message.forUserId != null && Object.hasOwnProperty.call(message, "forUserId"))
                object.forUserId = message.forUserId;
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.enterpriseUserId = typeof message.enterpriseUserId === "number" ? BigInt(message.enterpriseUserId) : $util.Long.fromBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0, false).toBigInt();
                else if (typeof message.enterpriseUserId === "number")
                    object.enterpriseUserId = options.longs === String ? String(message.enterpriseUserId) : message.enterpriseUserId;
                else
                    object.enterpriseUserId = options.longs === String ? $util.Long.prototype.toString.call(message.enterpriseUserId) : options.longs === Number ? new $util.LongBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0).toNumber() : message.enterpriseUserId;
            if (message.du2 != null && Object.hasOwnProperty.call(message, "du2"))
                object.du2 = $root.Tokens.LicenseToken.toObject(message.du2, options, q + 1);
            if (message.du3 != null && Object.hasOwnProperty.call(message, "du3"))
                object.du3 = $root.Tokens.LicenseToken.toObject(message.du3, options, q + 1);
            if (message.du4 != null && Object.hasOwnProperty.call(message, "du4"))
                object.du4 = $root.Tokens.LicenseToken.toObject(message.du4, options, q + 1);
            if (message.du6 != null && Object.hasOwnProperty.call(message, "du6"))
                object.du6 = options.enums === String ? $root.Authentication.AccountType[message.du6] === undefined ? message.du6 : $root.Authentication.AccountType[message.du6] : message.du6;
            if (message.clientVersionId != null && Object.hasOwnProperty.call(message, "clientVersionId"))
                object.clientVersionId = message.clientVersionId;
            if (message.sessionTokenType != null && Object.hasOwnProperty.call(message, "sessionTokenType"))
                object.sessionTokenType = options.enums === String ? $root.Authentication.SessionTokenType[message.sessionTokenType] === undefined ? message.sessionTokenType : $root.Authentication.SessionTokenType[message.sessionTokenType] : message.sessionTokenType;
            if (message.du5 != null && Object.hasOwnProperty.call(message, "du5"))
                object.du5 = $root.Tokens.LicenseToken.toObject(message.du5, options, q + 1);
            if (message.mcEnterpriseId != null && Object.hasOwnProperty.call(message, "mcEnterpriseId"))
                object.mcEnterpriseId = message.mcEnterpriseId;
            if (message.du7 != null && Object.hasOwnProperty.call(message, "du7"))
                object.du7 = message.du7;
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.deviceId = typeof message.deviceId === "number" ? BigInt(message.deviceId) : $util.Long.fromBits(message.deviceId.low >>> 0, message.deviceId.high >>> 0, false).toBigInt();
                else if (typeof message.deviceId === "number")
                    object.deviceId = options.longs === String ? String(message.deviceId) : message.deviceId;
                else
                    object.deviceId = options.longs === String ? $util.Long.prototype.toString.call(message.deviceId) : options.longs === Number ? new $util.LongBits(message.deviceId.low >>> 0, message.deviceId.high >>> 0).toNumber() : message.deviceId;
            if (message.loginSessionId != null && Object.hasOwnProperty.call(message, "loginSessionId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.loginSessionId = typeof message.loginSessionId === "number" ? BigInt(message.loginSessionId) : $util.Long.fromBits(message.loginSessionId.low >>> 0, message.loginSessionId.high >>> 0, false).toBigInt();
                else if (typeof message.loginSessionId === "number")
                    object.loginSessionId = options.longs === String ? String(message.loginSessionId) : message.loginSessionId;
                else
                    object.loginSessionId = options.longs === String ? $util.Long.prototype.toString.call(message.loginSessionId) : options.longs === Number ? new $util.LongBits(message.loginSessionId.low >>> 0, message.loginSessionId.high >>> 0).toNumber() : message.loginSessionId;
            if (message.clientTypeId != null && Object.hasOwnProperty.call(message, "clientTypeId"))
                object.clientTypeId = message.clientTypeId;
            if (message.ipAddress != null && Object.hasOwnProperty.call(message, "ipAddress"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.ipAddress = typeof message.ipAddress === "number" ? BigInt(message.ipAddress) : $util.Long.fromBits(message.ipAddress.low >>> 0, message.ipAddress.high >>> 0, false).toBigInt();
                else if (typeof message.ipAddress === "number")
                    object.ipAddress = options.longs === String ? String(message.ipAddress) : message.ipAddress;
                else
                    object.ipAddress = options.longs === String ? $util.Long.prototype.toString.call(message.ipAddress) : options.longs === Number ? new $util.LongBits(message.ipAddress.low >>> 0, message.ipAddress.high >>> 0).toNumber() : message.ipAddress;
            if (message.latitude != null && Object.hasOwnProperty.call(message, "latitude"))
                object.latitude = options.json && !isFinite(message.latitude) ? String(message.latitude) : message.latitude;
            if (message.longitude != null && Object.hasOwnProperty.call(message, "longitude"))
                object.longitude = options.json && !isFinite(message.longitude) ? String(message.longitude) : message.longitude;
            return object;
        };

        /**
         * Converts this SessionToken to JSON.
         * @function toJSON
         * @memberof Tokens.SessionToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SessionToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SessionToken
         * @function getTypeUrl
         * @memberof Tokens.SessionToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SessionToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.SessionToken";
        };

        return SessionToken;
    })();

    Tokens.UserLicenseStatus = (function() {

        /**
         * Properties of a UserLicenseStatus.
         * @memberof Tokens
         * @interface IUserLicenseStatus
         * @property {Authentication.LicenseType|null} [licenseType] UserLicenseStatus licenseType
         * @property {number|null} [expiration] UserLicenseStatus expiration
         */

        /**
         * Constructs a new UserLicenseStatus.
         * @memberof Tokens
         * @classdesc Represents a UserLicenseStatus.
         * @implements IUserLicenseStatus
         * @constructor
         * @param {Tokens.IUserLicenseStatus=} [properties] Properties to set
         */
        function UserLicenseStatus(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UserLicenseStatus licenseType.
         * @member {Authentication.LicenseType} licenseType
         * @memberof Tokens.UserLicenseStatus
         * @instance
         */
        UserLicenseStatus.prototype.licenseType = 0;

        /**
         * UserLicenseStatus expiration.
         * @member {number} expiration
         * @memberof Tokens.UserLicenseStatus
         * @instance
         */
        UserLicenseStatus.prototype.expiration = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new UserLicenseStatus instance using the specified properties.
         * @function create
         * @memberof Tokens.UserLicenseStatus
         * @static
         * @param {Tokens.IUserLicenseStatus=} [properties] Properties to set
         * @returns {Tokens.UserLicenseStatus} UserLicenseStatus instance
         */
        UserLicenseStatus.create = function create(properties) {
            return new UserLicenseStatus(properties);
        };

        /**
         * Encodes the specified UserLicenseStatus message. Does not implicitly {@link Tokens.UserLicenseStatus.verify|verify} messages.
         * @function encode
         * @memberof Tokens.UserLicenseStatus
         * @static
         * @param {Tokens.IUserLicenseStatus} message UserLicenseStatus message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserLicenseStatus.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.licenseType != null && Object.hasOwnProperty.call(message, "licenseType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.licenseType);
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.expiration);
            return writer;
        };

        /**
         * Encodes the specified UserLicenseStatus message, length delimited. Does not implicitly {@link Tokens.UserLicenseStatus.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.UserLicenseStatus
         * @static
         * @param {Tokens.IUserLicenseStatus} message UserLicenseStatus message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserLicenseStatus.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a UserLicenseStatus message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.UserLicenseStatus
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.UserLicenseStatus} UserLicenseStatus
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserLicenseStatus.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.UserLicenseStatus();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.licenseType = reader.int32();
                        break;
                    }
                case 2: {
                        message.expiration = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a UserLicenseStatus message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.UserLicenseStatus
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.UserLicenseStatus} UserLicenseStatus
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserLicenseStatus.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a UserLicenseStatus message.
         * @function verify
         * @memberof Tokens.UserLicenseStatus
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UserLicenseStatus.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.licenseType != null && Object.hasOwnProperty.call(message, "licenseType"))
                switch (message.licenseType) {
                default:
                    return "licenseType: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                    break;
                }
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                if (!$util.isInteger(message.expiration) && !(message.expiration && $util.isInteger(message.expiration.low) && $util.isInteger(message.expiration.high)))
                    return "expiration: integer|Long expected";
            return null;
        };

        /**
         * Creates a UserLicenseStatus message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.UserLicenseStatus
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.UserLicenseStatus} UserLicenseStatus
         */
        UserLicenseStatus.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.UserLicenseStatus)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.UserLicenseStatus: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.UserLicenseStatus();
            switch (object.licenseType) {
            default:
                if (typeof object.licenseType === "number") {
                    message.licenseType = object.licenseType;
                    break;
                }
                break;
            case "VAULT":
            case 0:
                message.licenseType = 0;
                break;
            case "CHAT":
            case 1:
                message.licenseType = 1;
                break;
            case "STORAGE":
            case 2:
                message.licenseType = 2;
                break;
            case "BREACHWATCH":
            case 3:
                message.licenseType = 3;
                break;
            }
            if (object.expiration != null)
                if ($util.Long)
                    message.expiration = $util.Long.fromValue(object.expiration, false);
                else if (typeof object.expiration === "string")
                    message.expiration = parseInt(object.expiration, 10);
                else if (typeof object.expiration === "number")
                    message.expiration = object.expiration;
                else if (typeof object.expiration === "object")
                    message.expiration = new $util.LongBits(object.expiration.low >>> 0, object.expiration.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a UserLicenseStatus message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.UserLicenseStatus
         * @static
         * @param {Tokens.UserLicenseStatus} message UserLicenseStatus
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UserLicenseStatus.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.licenseType = options.enums === String ? "VAULT" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.expiration = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.expiration = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.licenseType != null && Object.hasOwnProperty.call(message, "licenseType"))
                object.licenseType = options.enums === String ? $root.Authentication.LicenseType[message.licenseType] === undefined ? message.licenseType : $root.Authentication.LicenseType[message.licenseType] : message.licenseType;
            if (message.expiration != null && Object.hasOwnProperty.call(message, "expiration"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.expiration = typeof message.expiration === "number" ? BigInt(message.expiration) : $util.Long.fromBits(message.expiration.low >>> 0, message.expiration.high >>> 0, false).toBigInt();
                else if (typeof message.expiration === "number")
                    object.expiration = options.longs === String ? String(message.expiration) : message.expiration;
                else
                    object.expiration = options.longs === String ? $util.Long.prototype.toString.call(message.expiration) : options.longs === Number ? new $util.LongBits(message.expiration.low >>> 0, message.expiration.high >>> 0).toNumber() : message.expiration;
            return object;
        };

        /**
         * Converts this UserLicenseStatus to JSON.
         * @function toJSON
         * @memberof Tokens.UserLicenseStatus
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UserLicenseStatus.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UserLicenseStatus
         * @function getTypeUrl
         * @memberof Tokens.UserLicenseStatus
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UserLicenseStatus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.UserLicenseStatus";
        };

        return UserLicenseStatus;
    })();

    Tokens.CheckOutTokenResponse = (function() {

        /**
         * Properties of a CheckOutTokenResponse.
         * @memberof Tokens
         * @interface ICheckOutTokenResponse
         * @property {string|null} [checkOutToken] CheckOutTokenResponse checkOutToken
         */

        /**
         * Constructs a new CheckOutTokenResponse.
         * @memberof Tokens
         * @classdesc Represents a CheckOutTokenResponse.
         * @implements ICheckOutTokenResponse
         * @constructor
         * @param {Tokens.ICheckOutTokenResponse=} [properties] Properties to set
         */
        function CheckOutTokenResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CheckOutTokenResponse checkOutToken.
         * @member {string} checkOutToken
         * @memberof Tokens.CheckOutTokenResponse
         * @instance
         */
        CheckOutTokenResponse.prototype.checkOutToken = "";

        /**
         * Creates a new CheckOutTokenResponse instance using the specified properties.
         * @function create
         * @memberof Tokens.CheckOutTokenResponse
         * @static
         * @param {Tokens.ICheckOutTokenResponse=} [properties] Properties to set
         * @returns {Tokens.CheckOutTokenResponse} CheckOutTokenResponse instance
         */
        CheckOutTokenResponse.create = function create(properties) {
            return new CheckOutTokenResponse(properties);
        };

        /**
         * Encodes the specified CheckOutTokenResponse message. Does not implicitly {@link Tokens.CheckOutTokenResponse.verify|verify} messages.
         * @function encode
         * @memberof Tokens.CheckOutTokenResponse
         * @static
         * @param {Tokens.ICheckOutTokenResponse} message CheckOutTokenResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CheckOutTokenResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.checkOutToken != null && Object.hasOwnProperty.call(message, "checkOutToken"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.checkOutToken);
            return writer;
        };

        /**
         * Encodes the specified CheckOutTokenResponse message, length delimited. Does not implicitly {@link Tokens.CheckOutTokenResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.CheckOutTokenResponse
         * @static
         * @param {Tokens.ICheckOutTokenResponse} message CheckOutTokenResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CheckOutTokenResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CheckOutTokenResponse message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.CheckOutTokenResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.CheckOutTokenResponse} CheckOutTokenResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CheckOutTokenResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.CheckOutTokenResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.checkOutToken = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CheckOutTokenResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.CheckOutTokenResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.CheckOutTokenResponse} CheckOutTokenResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CheckOutTokenResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CheckOutTokenResponse message.
         * @function verify
         * @memberof Tokens.CheckOutTokenResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CheckOutTokenResponse.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.checkOutToken != null && Object.hasOwnProperty.call(message, "checkOutToken"))
                if (!$util.isString(message.checkOutToken))
                    return "checkOutToken: string expected";
            return null;
        };

        /**
         * Creates a CheckOutTokenResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.CheckOutTokenResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.CheckOutTokenResponse} CheckOutTokenResponse
         */
        CheckOutTokenResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.CheckOutTokenResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.CheckOutTokenResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.CheckOutTokenResponse();
            if (object.checkOutToken != null)
                message.checkOutToken = String(object.checkOutToken);
            return message;
        };

        /**
         * Creates a plain object from a CheckOutTokenResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.CheckOutTokenResponse
         * @static
         * @param {Tokens.CheckOutTokenResponse} message CheckOutTokenResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CheckOutTokenResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.checkOutToken = "";
            if (message.checkOutToken != null && Object.hasOwnProperty.call(message, "checkOutToken"))
                object.checkOutToken = message.checkOutToken;
            return object;
        };

        /**
         * Converts this CheckOutTokenResponse to JSON.
         * @function toJSON
         * @memberof Tokens.CheckOutTokenResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CheckOutTokenResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CheckOutTokenResponse
         * @function getTypeUrl
         * @memberof Tokens.CheckOutTokenResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CheckOutTokenResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.CheckOutTokenResponse";
        };

        return CheckOutTokenResponse;
    })();

    Tokens.ImporterToken = (function() {

        /**
         * Properties of an ImporterToken.
         * @memberof Tokens
         * @interface IImporterToken
         * @property {number|null} [importId] ImporterToken importId
         * @property {number|null} [userId] ImporterToken userId
         * @property {string|null} [region] ImporterToken region
         */

        /**
         * Constructs a new ImporterToken.
         * @memberof Tokens
         * @classdesc Represents an ImporterToken.
         * @implements IImporterToken
         * @constructor
         * @param {Tokens.IImporterToken=} [properties] Properties to set
         */
        function ImporterToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ImporterToken importId.
         * @member {number} importId
         * @memberof Tokens.ImporterToken
         * @instance
         */
        ImporterToken.prototype.importId = 0;

        /**
         * ImporterToken userId.
         * @member {number} userId
         * @memberof Tokens.ImporterToken
         * @instance
         */
        ImporterToken.prototype.userId = 0;

        /**
         * ImporterToken region.
         * @member {string} region
         * @memberof Tokens.ImporterToken
         * @instance
         */
        ImporterToken.prototype.region = "";

        /**
         * Creates a new ImporterToken instance using the specified properties.
         * @function create
         * @memberof Tokens.ImporterToken
         * @static
         * @param {Tokens.IImporterToken=} [properties] Properties to set
         * @returns {Tokens.ImporterToken} ImporterToken instance
         */
        ImporterToken.create = function create(properties) {
            return new ImporterToken(properties);
        };

        /**
         * Encodes the specified ImporterToken message. Does not implicitly {@link Tokens.ImporterToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.ImporterToken
         * @static
         * @param {Tokens.IImporterToken} message ImporterToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ImporterToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.importId != null && Object.hasOwnProperty.call(message, "importId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.importId);
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.userId);
            if (message.region != null && Object.hasOwnProperty.call(message, "region"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.region);
            return writer;
        };

        /**
         * Encodes the specified ImporterToken message, length delimited. Does not implicitly {@link Tokens.ImporterToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.ImporterToken
         * @static
         * @param {Tokens.IImporterToken} message ImporterToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ImporterToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ImporterToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.ImporterToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.ImporterToken} ImporterToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ImporterToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.ImporterToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.importId = reader.int32();
                        break;
                    }
                case 2: {
                        message.userId = reader.int32();
                        break;
                    }
                case 3: {
                        message.region = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ImporterToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.ImporterToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.ImporterToken} ImporterToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ImporterToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ImporterToken message.
         * @function verify
         * @memberof Tokens.ImporterToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ImporterToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.importId != null && Object.hasOwnProperty.call(message, "importId"))
                if (!$util.isInteger(message.importId))
                    return "importId: integer expected";
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                if (!$util.isInteger(message.userId))
                    return "userId: integer expected";
            if (message.region != null && Object.hasOwnProperty.call(message, "region"))
                if (!$util.isString(message.region))
                    return "region: string expected";
            return null;
        };

        /**
         * Creates an ImporterToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.ImporterToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.ImporterToken} ImporterToken
         */
        ImporterToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.ImporterToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.ImporterToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.ImporterToken();
            if (object.importId != null)
                message.importId = object.importId | 0;
            if (object.userId != null)
                message.userId = object.userId | 0;
            if (object.region != null)
                message.region = String(object.region);
            return message;
        };

        /**
         * Creates a plain object from an ImporterToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.ImporterToken
         * @static
         * @param {Tokens.ImporterToken} message ImporterToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ImporterToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.importId = 0;
                object.userId = 0;
                object.region = "";
            }
            if (message.importId != null && Object.hasOwnProperty.call(message, "importId"))
                object.importId = message.importId;
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                object.userId = message.userId;
            if (message.region != null && Object.hasOwnProperty.call(message, "region"))
                object.region = message.region;
            return object;
        };

        /**
         * Converts this ImporterToken to JSON.
         * @function toJSON
         * @memberof Tokens.ImporterToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ImporterToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ImporterToken
         * @function getTypeUrl
         * @memberof Tokens.ImporterToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ImporterToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.ImporterToken";
        };

        return ImporterToken;
    })();

    Tokens.ApiDecryptedRequest = (function() {

        /**
         * Properties of an ApiDecryptedRequest.
         * @memberof Tokens
         * @interface IApiDecryptedRequest
         * @property {Uint8Array|null} [decryptedTransmissionKey] ApiDecryptedRequest decryptedTransmissionKey
         * @property {Authentication.IApiRequestPayload|null} [apiRequestPayload] ApiDecryptedRequest apiRequestPayload
         * @property {Authentication.SupportedLanguage|null} [supportedLanguage] ApiDecryptedRequest supportedLanguage
         * @property {string|null} [recaptcha] ApiDecryptedRequest recaptcha
         * @property {string|null} [userAgent] ApiDecryptedRequest userAgent
         * @property {string|null} [subEnvironment] ApiDecryptedRequest subEnvironment
         */

        /**
         * Constructs a new ApiDecryptedRequest.
         * @memberof Tokens
         * @classdesc Represents an ApiDecryptedRequest.
         * @implements IApiDecryptedRequest
         * @constructor
         * @param {Tokens.IApiDecryptedRequest=} [properties] Properties to set
         */
        function ApiDecryptedRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ApiDecryptedRequest decryptedTransmissionKey.
         * @member {Uint8Array} decryptedTransmissionKey
         * @memberof Tokens.ApiDecryptedRequest
         * @instance
         */
        ApiDecryptedRequest.prototype.decryptedTransmissionKey = $util.newBuffer([]);

        /**
         * ApiDecryptedRequest apiRequestPayload.
         * @member {Authentication.IApiRequestPayload|null|undefined} apiRequestPayload
         * @memberof Tokens.ApiDecryptedRequest
         * @instance
         */
        ApiDecryptedRequest.prototype.apiRequestPayload = null;

        /**
         * ApiDecryptedRequest supportedLanguage.
         * @member {Authentication.SupportedLanguage} supportedLanguage
         * @memberof Tokens.ApiDecryptedRequest
         * @instance
         */
        ApiDecryptedRequest.prototype.supportedLanguage = 0;

        /**
         * ApiDecryptedRequest recaptcha.
         * @member {string} recaptcha
         * @memberof Tokens.ApiDecryptedRequest
         * @instance
         */
        ApiDecryptedRequest.prototype.recaptcha = "";

        /**
         * ApiDecryptedRequest userAgent.
         * @member {string} userAgent
         * @memberof Tokens.ApiDecryptedRequest
         * @instance
         */
        ApiDecryptedRequest.prototype.userAgent = "";

        /**
         * ApiDecryptedRequest subEnvironment.
         * @member {string} subEnvironment
         * @memberof Tokens.ApiDecryptedRequest
         * @instance
         */
        ApiDecryptedRequest.prototype.subEnvironment = "";

        /**
         * Creates a new ApiDecryptedRequest instance using the specified properties.
         * @function create
         * @memberof Tokens.ApiDecryptedRequest
         * @static
         * @param {Tokens.IApiDecryptedRequest=} [properties] Properties to set
         * @returns {Tokens.ApiDecryptedRequest} ApiDecryptedRequest instance
         */
        ApiDecryptedRequest.create = function create(properties) {
            return new ApiDecryptedRequest(properties);
        };

        /**
         * Encodes the specified ApiDecryptedRequest message. Does not implicitly {@link Tokens.ApiDecryptedRequest.verify|verify} messages.
         * @function encode
         * @memberof Tokens.ApiDecryptedRequest
         * @static
         * @param {Tokens.IApiDecryptedRequest} message ApiDecryptedRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ApiDecryptedRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.decryptedTransmissionKey != null && Object.hasOwnProperty.call(message, "decryptedTransmissionKey"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.decryptedTransmissionKey);
            if (message.apiRequestPayload != null && Object.hasOwnProperty.call(message, "apiRequestPayload"))
                $root.Authentication.ApiRequestPayload.encode(message.apiRequestPayload, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.supportedLanguage);
            if (message.recaptcha != null && Object.hasOwnProperty.call(message, "recaptcha"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.recaptcha);
            if (message.userAgent != null && Object.hasOwnProperty.call(message, "userAgent"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.userAgent);
            if (message.subEnvironment != null && Object.hasOwnProperty.call(message, "subEnvironment"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.subEnvironment);
            return writer;
        };

        /**
         * Encodes the specified ApiDecryptedRequest message, length delimited. Does not implicitly {@link Tokens.ApiDecryptedRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.ApiDecryptedRequest
         * @static
         * @param {Tokens.IApiDecryptedRequest} message ApiDecryptedRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ApiDecryptedRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ApiDecryptedRequest message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.ApiDecryptedRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.ApiDecryptedRequest} ApiDecryptedRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ApiDecryptedRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.ApiDecryptedRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.decryptedTransmissionKey = reader.bytes();
                        break;
                    }
                case 3: {
                        message.apiRequestPayload = $root.Authentication.ApiRequestPayload.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 4: {
                        message.supportedLanguage = reader.int32();
                        break;
                    }
                case 5: {
                        message.recaptcha = reader.string();
                        break;
                    }
                case 6: {
                        message.userAgent = reader.string();
                        break;
                    }
                case 7: {
                        message.subEnvironment = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ApiDecryptedRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.ApiDecryptedRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.ApiDecryptedRequest} ApiDecryptedRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ApiDecryptedRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ApiDecryptedRequest message.
         * @function verify
         * @memberof Tokens.ApiDecryptedRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ApiDecryptedRequest.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.decryptedTransmissionKey != null && Object.hasOwnProperty.call(message, "decryptedTransmissionKey"))
                if (!(message.decryptedTransmissionKey && typeof message.decryptedTransmissionKey.length === "number" || $util.isString(message.decryptedTransmissionKey)))
                    return "decryptedTransmissionKey: buffer expected";
            if (message.apiRequestPayload != null && Object.hasOwnProperty.call(message, "apiRequestPayload")) {
                let error = $root.Authentication.ApiRequestPayload.verify(message.apiRequestPayload, long + 1);
                if (error)
                    return "apiRequestPayload." + error;
            }
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                switch (message.supportedLanguage) {
                default:
                    return "supportedLanguage: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                case 11:
                case 12:
                case 13:
                case 14:
                case 15:
                case 16:
                case 17:
                case 18:
                case 19:
                case 20:
                case 21:
                case 22:
                    break;
                }
            if (message.recaptcha != null && Object.hasOwnProperty.call(message, "recaptcha"))
                if (!$util.isString(message.recaptcha))
                    return "recaptcha: string expected";
            if (message.userAgent != null && Object.hasOwnProperty.call(message, "userAgent"))
                if (!$util.isString(message.userAgent))
                    return "userAgent: string expected";
            if (message.subEnvironment != null && Object.hasOwnProperty.call(message, "subEnvironment"))
                if (!$util.isString(message.subEnvironment))
                    return "subEnvironment: string expected";
            return null;
        };

        /**
         * Creates an ApiDecryptedRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.ApiDecryptedRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.ApiDecryptedRequest} ApiDecryptedRequest
         */
        ApiDecryptedRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.ApiDecryptedRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.ApiDecryptedRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.ApiDecryptedRequest();
            if (object.decryptedTransmissionKey != null)
                if (typeof object.decryptedTransmissionKey === "string")
                    $util.base64.decode(object.decryptedTransmissionKey, message.decryptedTransmissionKey = $util.newBuffer($util.base64.length(object.decryptedTransmissionKey)), 0);
                else if (object.decryptedTransmissionKey.length >= 0)
                    message.decryptedTransmissionKey = object.decryptedTransmissionKey;
            if (object.apiRequestPayload != null) {
                if (!$util.isObject(object.apiRequestPayload))
                    throw TypeError(".Tokens.ApiDecryptedRequest.apiRequestPayload: object expected");
                message.apiRequestPayload = $root.Authentication.ApiRequestPayload.fromObject(object.apiRequestPayload, long + 1);
            }
            switch (object.supportedLanguage) {
            default:
                if (typeof object.supportedLanguage === "number") {
                    message.supportedLanguage = object.supportedLanguage;
                    break;
                }
                break;
            case "ENGLISH":
            case 0:
                message.supportedLanguage = 0;
                break;
            case "ARABIC":
            case 1:
                message.supportedLanguage = 1;
                break;
            case "BRITISH":
            case 2:
                message.supportedLanguage = 2;
                break;
            case "CHINESE":
            case 3:
                message.supportedLanguage = 3;
                break;
            case "CHINESE_HONG_KONG":
            case 4:
                message.supportedLanguage = 4;
                break;
            case "CHINESE_TAIWAN":
            case 5:
                message.supportedLanguage = 5;
                break;
            case "DUTCH":
            case 6:
                message.supportedLanguage = 6;
                break;
            case "FRENCH":
            case 7:
                message.supportedLanguage = 7;
                break;
            case "GERMAN":
            case 8:
                message.supportedLanguage = 8;
                break;
            case "GREEK":
            case 9:
                message.supportedLanguage = 9;
                break;
            case "HEBREW":
            case 10:
                message.supportedLanguage = 10;
                break;
            case "ITALIAN":
            case 11:
                message.supportedLanguage = 11;
                break;
            case "JAPANESE":
            case 12:
                message.supportedLanguage = 12;
                break;
            case "KOREAN":
            case 13:
                message.supportedLanguage = 13;
                break;
            case "POLISH":
            case 14:
                message.supportedLanguage = 14;
                break;
            case "PORTUGUESE":
            case 15:
                message.supportedLanguage = 15;
                break;
            case "PORTUGUESE_BRAZIL":
            case 16:
                message.supportedLanguage = 16;
                break;
            case "ROMANIAN":
            case 17:
                message.supportedLanguage = 17;
                break;
            case "RUSSIAN":
            case 18:
                message.supportedLanguage = 18;
                break;
            case "SLOVAK":
            case 19:
                message.supportedLanguage = 19;
                break;
            case "SPANISH":
            case 20:
                message.supportedLanguage = 20;
                break;
            case "FINNISH":
            case 21:
                message.supportedLanguage = 21;
                break;
            case "SWEDISH":
            case 22:
                message.supportedLanguage = 22;
                break;
            }
            if (object.recaptcha != null)
                message.recaptcha = String(object.recaptcha);
            if (object.userAgent != null)
                message.userAgent = String(object.userAgent);
            if (object.subEnvironment != null)
                message.subEnvironment = String(object.subEnvironment);
            return message;
        };

        /**
         * Creates a plain object from an ApiDecryptedRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.ApiDecryptedRequest
         * @static
         * @param {Tokens.ApiDecryptedRequest} message ApiDecryptedRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ApiDecryptedRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.decryptedTransmissionKey = "";
                else {
                    object.decryptedTransmissionKey = [];
                    if (options.bytes !== Array)
                        object.decryptedTransmissionKey = $util.newBuffer(object.decryptedTransmissionKey);
                }
                object.apiRequestPayload = null;
                object.supportedLanguage = options.enums === String ? "ENGLISH" : 0;
                object.recaptcha = "";
                object.userAgent = "";
                object.subEnvironment = "";
            }
            if (message.decryptedTransmissionKey != null && Object.hasOwnProperty.call(message, "decryptedTransmissionKey"))
                object.decryptedTransmissionKey = options.bytes === String ? $util.base64.encode(message.decryptedTransmissionKey, 0, message.decryptedTransmissionKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.decryptedTransmissionKey) : message.decryptedTransmissionKey;
            if (message.apiRequestPayload != null && Object.hasOwnProperty.call(message, "apiRequestPayload"))
                object.apiRequestPayload = $root.Authentication.ApiRequestPayload.toObject(message.apiRequestPayload, options, q + 1);
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                object.supportedLanguage = options.enums === String ? $root.Authentication.SupportedLanguage[message.supportedLanguage] === undefined ? message.supportedLanguage : $root.Authentication.SupportedLanguage[message.supportedLanguage] : message.supportedLanguage;
            if (message.recaptcha != null && Object.hasOwnProperty.call(message, "recaptcha"))
                object.recaptcha = message.recaptcha;
            if (message.userAgent != null && Object.hasOwnProperty.call(message, "userAgent"))
                object.userAgent = message.userAgent;
            if (message.subEnvironment != null && Object.hasOwnProperty.call(message, "subEnvironment"))
                object.subEnvironment = message.subEnvironment;
            return object;
        };

        /**
         * Converts this ApiDecryptedRequest to JSON.
         * @function toJSON
         * @memberof Tokens.ApiDecryptedRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ApiDecryptedRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ApiDecryptedRequest
         * @function getTypeUrl
         * @memberof Tokens.ApiDecryptedRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ApiDecryptedRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.ApiDecryptedRequest";
        };

        return ApiDecryptedRequest;
    })();

    Tokens.ChangeEmailToken = (function() {

        /**
         * Properties of a ChangeEmailToken.
         * @memberof Tokens
         * @interface IChangeEmailToken
         * @property {number|null} [userId] ChangeEmailToken userId
         * @property {string|null} [newEmail] ChangeEmailToken newEmail
         * @property {Authentication.SupportedLanguage|null} [supportedLanguage] ChangeEmailToken supportedLanguage
         * @property {number|null} [enterpriseUserId] ChangeEmailToken enterpriseUserId
         * @property {number|null} [creation] ChangeEmailToken creation
         * @property {number|null} [clientVersionId] ChangeEmailToken clientVersionId
         */

        /**
         * Constructs a new ChangeEmailToken.
         * @memberof Tokens
         * @classdesc Represents a ChangeEmailToken.
         * @implements IChangeEmailToken
         * @constructor
         * @param {Tokens.IChangeEmailToken=} [properties] Properties to set
         */
        function ChangeEmailToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ChangeEmailToken userId.
         * @member {number} userId
         * @memberof Tokens.ChangeEmailToken
         * @instance
         */
        ChangeEmailToken.prototype.userId = 0;

        /**
         * ChangeEmailToken newEmail.
         * @member {string} newEmail
         * @memberof Tokens.ChangeEmailToken
         * @instance
         */
        ChangeEmailToken.prototype.newEmail = "";

        /**
         * ChangeEmailToken supportedLanguage.
         * @member {Authentication.SupportedLanguage} supportedLanguage
         * @memberof Tokens.ChangeEmailToken
         * @instance
         */
        ChangeEmailToken.prototype.supportedLanguage = 0;

        /**
         * ChangeEmailToken enterpriseUserId.
         * @member {number} enterpriseUserId
         * @memberof Tokens.ChangeEmailToken
         * @instance
         */
        ChangeEmailToken.prototype.enterpriseUserId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ChangeEmailToken creation.
         * @member {number} creation
         * @memberof Tokens.ChangeEmailToken
         * @instance
         */
        ChangeEmailToken.prototype.creation = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ChangeEmailToken clientVersionId.
         * @member {number} clientVersionId
         * @memberof Tokens.ChangeEmailToken
         * @instance
         */
        ChangeEmailToken.prototype.clientVersionId = 0;

        /**
         * Creates a new ChangeEmailToken instance using the specified properties.
         * @function create
         * @memberof Tokens.ChangeEmailToken
         * @static
         * @param {Tokens.IChangeEmailToken=} [properties] Properties to set
         * @returns {Tokens.ChangeEmailToken} ChangeEmailToken instance
         */
        ChangeEmailToken.create = function create(properties) {
            return new ChangeEmailToken(properties);
        };

        /**
         * Encodes the specified ChangeEmailToken message. Does not implicitly {@link Tokens.ChangeEmailToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.ChangeEmailToken
         * @static
         * @param {Tokens.IChangeEmailToken} message ChangeEmailToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangeEmailToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.userId);
            if (message.newEmail != null && Object.hasOwnProperty.call(message, "newEmail"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.newEmail);
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.supportedLanguage);
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.enterpriseUserId);
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.creation);
            if (message.clientVersionId != null && Object.hasOwnProperty.call(message, "clientVersionId"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.clientVersionId);
            return writer;
        };

        /**
         * Encodes the specified ChangeEmailToken message, length delimited. Does not implicitly {@link Tokens.ChangeEmailToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.ChangeEmailToken
         * @static
         * @param {Tokens.IChangeEmailToken} message ChangeEmailToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ChangeEmailToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ChangeEmailToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.ChangeEmailToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.ChangeEmailToken} ChangeEmailToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangeEmailToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.ChangeEmailToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.userId = reader.int32();
                        break;
                    }
                case 2: {
                        message.newEmail = reader.string();
                        break;
                    }
                case 3: {
                        message.supportedLanguage = reader.int32();
                        break;
                    }
                case 4: {
                        message.enterpriseUserId = reader.int64();
                        break;
                    }
                case 5: {
                        message.creation = reader.int64();
                        break;
                    }
                case 6: {
                        message.clientVersionId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ChangeEmailToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.ChangeEmailToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.ChangeEmailToken} ChangeEmailToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ChangeEmailToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ChangeEmailToken message.
         * @function verify
         * @memberof Tokens.ChangeEmailToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ChangeEmailToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                if (!$util.isInteger(message.userId))
                    return "userId: integer expected";
            if (message.newEmail != null && Object.hasOwnProperty.call(message, "newEmail"))
                if (!$util.isString(message.newEmail))
                    return "newEmail: string expected";
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                switch (message.supportedLanguage) {
                default:
                    return "supportedLanguage: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                case 11:
                case 12:
                case 13:
                case 14:
                case 15:
                case 16:
                case 17:
                case 18:
                case 19:
                case 20:
                case 21:
                case 22:
                    break;
                }
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                if (!$util.isInteger(message.enterpriseUserId) && !(message.enterpriseUserId && $util.isInteger(message.enterpriseUserId.low) && $util.isInteger(message.enterpriseUserId.high)))
                    return "enterpriseUserId: integer|Long expected";
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                if (!$util.isInteger(message.creation) && !(message.creation && $util.isInteger(message.creation.low) && $util.isInteger(message.creation.high)))
                    return "creation: integer|Long expected";
            if (message.clientVersionId != null && Object.hasOwnProperty.call(message, "clientVersionId"))
                if (!$util.isInteger(message.clientVersionId))
                    return "clientVersionId: integer expected";
            return null;
        };

        /**
         * Creates a ChangeEmailToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.ChangeEmailToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.ChangeEmailToken} ChangeEmailToken
         */
        ChangeEmailToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.ChangeEmailToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.ChangeEmailToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.ChangeEmailToken();
            if (object.userId != null)
                message.userId = object.userId | 0;
            if (object.newEmail != null)
                message.newEmail = String(object.newEmail);
            switch (object.supportedLanguage) {
            default:
                if (typeof object.supportedLanguage === "number") {
                    message.supportedLanguage = object.supportedLanguage;
                    break;
                }
                break;
            case "ENGLISH":
            case 0:
                message.supportedLanguage = 0;
                break;
            case "ARABIC":
            case 1:
                message.supportedLanguage = 1;
                break;
            case "BRITISH":
            case 2:
                message.supportedLanguage = 2;
                break;
            case "CHINESE":
            case 3:
                message.supportedLanguage = 3;
                break;
            case "CHINESE_HONG_KONG":
            case 4:
                message.supportedLanguage = 4;
                break;
            case "CHINESE_TAIWAN":
            case 5:
                message.supportedLanguage = 5;
                break;
            case "DUTCH":
            case 6:
                message.supportedLanguage = 6;
                break;
            case "FRENCH":
            case 7:
                message.supportedLanguage = 7;
                break;
            case "GERMAN":
            case 8:
                message.supportedLanguage = 8;
                break;
            case "GREEK":
            case 9:
                message.supportedLanguage = 9;
                break;
            case "HEBREW":
            case 10:
                message.supportedLanguage = 10;
                break;
            case "ITALIAN":
            case 11:
                message.supportedLanguage = 11;
                break;
            case "JAPANESE":
            case 12:
                message.supportedLanguage = 12;
                break;
            case "KOREAN":
            case 13:
                message.supportedLanguage = 13;
                break;
            case "POLISH":
            case 14:
                message.supportedLanguage = 14;
                break;
            case "PORTUGUESE":
            case 15:
                message.supportedLanguage = 15;
                break;
            case "PORTUGUESE_BRAZIL":
            case 16:
                message.supportedLanguage = 16;
                break;
            case "ROMANIAN":
            case 17:
                message.supportedLanguage = 17;
                break;
            case "RUSSIAN":
            case 18:
                message.supportedLanguage = 18;
                break;
            case "SLOVAK":
            case 19:
                message.supportedLanguage = 19;
                break;
            case "SPANISH":
            case 20:
                message.supportedLanguage = 20;
                break;
            case "FINNISH":
            case 21:
                message.supportedLanguage = 21;
                break;
            case "SWEDISH":
            case 22:
                message.supportedLanguage = 22;
                break;
            }
            if (object.enterpriseUserId != null)
                if ($util.Long)
                    message.enterpriseUserId = $util.Long.fromValue(object.enterpriseUserId, false);
                else if (typeof object.enterpriseUserId === "string")
                    message.enterpriseUserId = parseInt(object.enterpriseUserId, 10);
                else if (typeof object.enterpriseUserId === "number")
                    message.enterpriseUserId = object.enterpriseUserId;
                else if (typeof object.enterpriseUserId === "object")
                    message.enterpriseUserId = new $util.LongBits(object.enterpriseUserId.low >>> 0, object.enterpriseUserId.high >>> 0).toNumber();
            if (object.creation != null)
                if ($util.Long)
                    message.creation = $util.Long.fromValue(object.creation, false);
                else if (typeof object.creation === "string")
                    message.creation = parseInt(object.creation, 10);
                else if (typeof object.creation === "number")
                    message.creation = object.creation;
                else if (typeof object.creation === "object")
                    message.creation = new $util.LongBits(object.creation.low >>> 0, object.creation.high >>> 0).toNumber();
            if (object.clientVersionId != null)
                message.clientVersionId = object.clientVersionId | 0;
            return message;
        };

        /**
         * Creates a plain object from a ChangeEmailToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.ChangeEmailToken
         * @static
         * @param {Tokens.ChangeEmailToken} message ChangeEmailToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ChangeEmailToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.userId = 0;
                object.newEmail = "";
                object.supportedLanguage = options.enums === String ? "ENGLISH" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.enterpriseUserId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.enterpriseUserId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.creation = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.creation = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.clientVersionId = 0;
            }
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                object.userId = message.userId;
            if (message.newEmail != null && Object.hasOwnProperty.call(message, "newEmail"))
                object.newEmail = message.newEmail;
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                object.supportedLanguage = options.enums === String ? $root.Authentication.SupportedLanguage[message.supportedLanguage] === undefined ? message.supportedLanguage : $root.Authentication.SupportedLanguage[message.supportedLanguage] : message.supportedLanguage;
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.enterpriseUserId = typeof message.enterpriseUserId === "number" ? BigInt(message.enterpriseUserId) : $util.Long.fromBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0, false).toBigInt();
                else if (typeof message.enterpriseUserId === "number")
                    object.enterpriseUserId = options.longs === String ? String(message.enterpriseUserId) : message.enterpriseUserId;
                else
                    object.enterpriseUserId = options.longs === String ? $util.Long.prototype.toString.call(message.enterpriseUserId) : options.longs === Number ? new $util.LongBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0).toNumber() : message.enterpriseUserId;
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.creation = typeof message.creation === "number" ? BigInt(message.creation) : $util.Long.fromBits(message.creation.low >>> 0, message.creation.high >>> 0, false).toBigInt();
                else if (typeof message.creation === "number")
                    object.creation = options.longs === String ? String(message.creation) : message.creation;
                else
                    object.creation = options.longs === String ? $util.Long.prototype.toString.call(message.creation) : options.longs === Number ? new $util.LongBits(message.creation.low >>> 0, message.creation.high >>> 0).toNumber() : message.creation;
            if (message.clientVersionId != null && Object.hasOwnProperty.call(message, "clientVersionId"))
                object.clientVersionId = message.clientVersionId;
            return object;
        };

        /**
         * Converts this ChangeEmailToken to JSON.
         * @function toJSON
         * @memberof Tokens.ChangeEmailToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ChangeEmailToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ChangeEmailToken
         * @function getTypeUrl
         * @memberof Tokens.ChangeEmailToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ChangeEmailToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.ChangeEmailToken";
        };

        return ChangeEmailToken;
    })();

    Tokens.EmailVerificationToken = (function() {

        /**
         * Properties of an EmailVerificationToken.
         * @memberof Tokens
         * @interface IEmailVerificationToken
         * @property {number|null} [userId] EmailVerificationToken userId
         * @property {string|null} [email] EmailVerificationToken email
         * @property {string|null} [du1] EmailVerificationToken du1
         * @property {number|null} [enterpriseUserId] EmailVerificationToken enterpriseUserId
         * @property {number|null} [du2] EmailVerificationToken du2
         */

        /**
         * Constructs a new EmailVerificationToken.
         * @memberof Tokens
         * @classdesc Represents an EmailVerificationToken.
         * @implements IEmailVerificationToken
         * @constructor
         * @param {Tokens.IEmailVerificationToken=} [properties] Properties to set
         */
        function EmailVerificationToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EmailVerificationToken userId.
         * @member {number} userId
         * @memberof Tokens.EmailVerificationToken
         * @instance
         */
        EmailVerificationToken.prototype.userId = 0;

        /**
         * EmailVerificationToken email.
         * @member {string} email
         * @memberof Tokens.EmailVerificationToken
         * @instance
         */
        EmailVerificationToken.prototype.email = "";

        /**
         * EmailVerificationToken du1.
         * @member {string} du1
         * @memberof Tokens.EmailVerificationToken
         * @instance
         */
        EmailVerificationToken.prototype.du1 = "";

        /**
         * EmailVerificationToken enterpriseUserId.
         * @member {number} enterpriseUserId
         * @memberof Tokens.EmailVerificationToken
         * @instance
         */
        EmailVerificationToken.prototype.enterpriseUserId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EmailVerificationToken du2.
         * @member {number} du2
         * @memberof Tokens.EmailVerificationToken
         * @instance
         */
        EmailVerificationToken.prototype.du2 = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new EmailVerificationToken instance using the specified properties.
         * @function create
         * @memberof Tokens.EmailVerificationToken
         * @static
         * @param {Tokens.IEmailVerificationToken=} [properties] Properties to set
         * @returns {Tokens.EmailVerificationToken} EmailVerificationToken instance
         */
        EmailVerificationToken.create = function create(properties) {
            return new EmailVerificationToken(properties);
        };

        /**
         * Encodes the specified EmailVerificationToken message. Does not implicitly {@link Tokens.EmailVerificationToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.EmailVerificationToken
         * @static
         * @param {Tokens.IEmailVerificationToken} message EmailVerificationToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EmailVerificationToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.userId);
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.email);
            if (message.du1 != null && Object.hasOwnProperty.call(message, "du1"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.du1);
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.enterpriseUserId);
            if (message.du2 != null && Object.hasOwnProperty.call(message, "du2"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.du2);
            return writer;
        };

        /**
         * Encodes the specified EmailVerificationToken message, length delimited. Does not implicitly {@link Tokens.EmailVerificationToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.EmailVerificationToken
         * @static
         * @param {Tokens.IEmailVerificationToken} message EmailVerificationToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EmailVerificationToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EmailVerificationToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.EmailVerificationToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.EmailVerificationToken} EmailVerificationToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EmailVerificationToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.EmailVerificationToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.userId = reader.int32();
                        break;
                    }
                case 2: {
                        message.email = reader.string();
                        break;
                    }
                case 3: {
                        message.du1 = reader.string();
                        break;
                    }
                case 4: {
                        message.enterpriseUserId = reader.int64();
                        break;
                    }
                case 5: {
                        message.du2 = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an EmailVerificationToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.EmailVerificationToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.EmailVerificationToken} EmailVerificationToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EmailVerificationToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EmailVerificationToken message.
         * @function verify
         * @memberof Tokens.EmailVerificationToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EmailVerificationToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                if (!$util.isInteger(message.userId))
                    return "userId: integer expected";
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                if (!$util.isString(message.email))
                    return "email: string expected";
            if (message.du1 != null && Object.hasOwnProperty.call(message, "du1"))
                if (!$util.isString(message.du1))
                    return "du1: string expected";
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                if (!$util.isInteger(message.enterpriseUserId) && !(message.enterpriseUserId && $util.isInteger(message.enterpriseUserId.low) && $util.isInteger(message.enterpriseUserId.high)))
                    return "enterpriseUserId: integer|Long expected";
            if (message.du2 != null && Object.hasOwnProperty.call(message, "du2"))
                if (!$util.isInteger(message.du2) && !(message.du2 && $util.isInteger(message.du2.low) && $util.isInteger(message.du2.high)))
                    return "du2: integer|Long expected";
            return null;
        };

        /**
         * Creates an EmailVerificationToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.EmailVerificationToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.EmailVerificationToken} EmailVerificationToken
         */
        EmailVerificationToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.EmailVerificationToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.EmailVerificationToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.EmailVerificationToken();
            if (object.userId != null)
                message.userId = object.userId | 0;
            if (object.email != null)
                message.email = String(object.email);
            if (object.du1 != null)
                message.du1 = String(object.du1);
            if (object.enterpriseUserId != null)
                if ($util.Long)
                    message.enterpriseUserId = $util.Long.fromValue(object.enterpriseUserId, false);
                else if (typeof object.enterpriseUserId === "string")
                    message.enterpriseUserId = parseInt(object.enterpriseUserId, 10);
                else if (typeof object.enterpriseUserId === "number")
                    message.enterpriseUserId = object.enterpriseUserId;
                else if (typeof object.enterpriseUserId === "object")
                    message.enterpriseUserId = new $util.LongBits(object.enterpriseUserId.low >>> 0, object.enterpriseUserId.high >>> 0).toNumber();
            if (object.du2 != null)
                if ($util.Long)
                    message.du2 = $util.Long.fromValue(object.du2, false);
                else if (typeof object.du2 === "string")
                    message.du2 = parseInt(object.du2, 10);
                else if (typeof object.du2 === "number")
                    message.du2 = object.du2;
                else if (typeof object.du2 === "object")
                    message.du2 = new $util.LongBits(object.du2.low >>> 0, object.du2.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from an EmailVerificationToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.EmailVerificationToken
         * @static
         * @param {Tokens.EmailVerificationToken} message EmailVerificationToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EmailVerificationToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.userId = 0;
                object.email = "";
                object.du1 = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.enterpriseUserId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.enterpriseUserId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.du2 = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.du2 = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                object.userId = message.userId;
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                object.email = message.email;
            if (message.du1 != null && Object.hasOwnProperty.call(message, "du1"))
                object.du1 = message.du1;
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.enterpriseUserId = typeof message.enterpriseUserId === "number" ? BigInt(message.enterpriseUserId) : $util.Long.fromBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0, false).toBigInt();
                else if (typeof message.enterpriseUserId === "number")
                    object.enterpriseUserId = options.longs === String ? String(message.enterpriseUserId) : message.enterpriseUserId;
                else
                    object.enterpriseUserId = options.longs === String ? $util.Long.prototype.toString.call(message.enterpriseUserId) : options.longs === Number ? new $util.LongBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0).toNumber() : message.enterpriseUserId;
            if (message.du2 != null && Object.hasOwnProperty.call(message, "du2"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.du2 = typeof message.du2 === "number" ? BigInt(message.du2) : $util.Long.fromBits(message.du2.low >>> 0, message.du2.high >>> 0, false).toBigInt();
                else if (typeof message.du2 === "number")
                    object.du2 = options.longs === String ? String(message.du2) : message.du2;
                else
                    object.du2 = options.longs === String ? $util.Long.prototype.toString.call(message.du2) : options.longs === Number ? new $util.LongBits(message.du2.low >>> 0, message.du2.high >>> 0).toNumber() : message.du2;
            return object;
        };

        /**
         * Converts this EmailVerificationToken to JSON.
         * @function toJSON
         * @memberof Tokens.EmailVerificationToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EmailVerificationToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EmailVerificationToken
         * @function getTypeUrl
         * @memberof Tokens.EmailVerificationToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EmailVerificationToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.EmailVerificationToken";
        };

        return EmailVerificationToken;
    })();

    Tokens.IpAddressVerificationToken = (function() {

        /**
         * Properties of an IpAddressVerificationToken.
         * @memberof Tokens
         * @interface IIpAddressVerificationToken
         * @property {number|null} [userId] IpAddressVerificationToken userId
         * @property {string|null} [email] IpAddressVerificationToken email
         * @property {number|null} [ssoServiceProviderId] IpAddressVerificationToken ssoServiceProviderId
         * @property {string|null} [ipAddress] IpAddressVerificationToken ipAddress
         * @property {string|null} [du1] IpAddressVerificationToken du1
         * @property {number|null} [du2] IpAddressVerificationToken du2
         * @property {number|null} [enterpriseId] IpAddressVerificationToken enterpriseId
         * @property {number|null} [deviceId] IpAddressVerificationToken deviceId
         */

        /**
         * Constructs a new IpAddressVerificationToken.
         * @memberof Tokens
         * @classdesc Represents an IpAddressVerificationToken.
         * @implements IIpAddressVerificationToken
         * @constructor
         * @param {Tokens.IIpAddressVerificationToken=} [properties] Properties to set
         */
        function IpAddressVerificationToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * IpAddressVerificationToken userId.
         * @member {number} userId
         * @memberof Tokens.IpAddressVerificationToken
         * @instance
         */
        IpAddressVerificationToken.prototype.userId = 0;

        /**
         * IpAddressVerificationToken email.
         * @member {string} email
         * @memberof Tokens.IpAddressVerificationToken
         * @instance
         */
        IpAddressVerificationToken.prototype.email = "";

        /**
         * IpAddressVerificationToken ssoServiceProviderId.
         * @member {number} ssoServiceProviderId
         * @memberof Tokens.IpAddressVerificationToken
         * @instance
         */
        IpAddressVerificationToken.prototype.ssoServiceProviderId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * IpAddressVerificationToken ipAddress.
         * @member {string} ipAddress
         * @memberof Tokens.IpAddressVerificationToken
         * @instance
         */
        IpAddressVerificationToken.prototype.ipAddress = "";

        /**
         * IpAddressVerificationToken du1.
         * @member {string} du1
         * @memberof Tokens.IpAddressVerificationToken
         * @instance
         */
        IpAddressVerificationToken.prototype.du1 = "";

        /**
         * IpAddressVerificationToken du2.
         * @member {number} du2
         * @memberof Tokens.IpAddressVerificationToken
         * @instance
         */
        IpAddressVerificationToken.prototype.du2 = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * IpAddressVerificationToken enterpriseId.
         * @member {number} enterpriseId
         * @memberof Tokens.IpAddressVerificationToken
         * @instance
         */
        IpAddressVerificationToken.prototype.enterpriseId = 0;

        /**
         * IpAddressVerificationToken deviceId.
         * @member {number} deviceId
         * @memberof Tokens.IpAddressVerificationToken
         * @instance
         */
        IpAddressVerificationToken.prototype.deviceId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new IpAddressVerificationToken instance using the specified properties.
         * @function create
         * @memberof Tokens.IpAddressVerificationToken
         * @static
         * @param {Tokens.IIpAddressVerificationToken=} [properties] Properties to set
         * @returns {Tokens.IpAddressVerificationToken} IpAddressVerificationToken instance
         */
        IpAddressVerificationToken.create = function create(properties) {
            return new IpAddressVerificationToken(properties);
        };

        /**
         * Encodes the specified IpAddressVerificationToken message. Does not implicitly {@link Tokens.IpAddressVerificationToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.IpAddressVerificationToken
         * @static
         * @param {Tokens.IIpAddressVerificationToken} message IpAddressVerificationToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IpAddressVerificationToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.userId);
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.email);
            if (message.ssoServiceProviderId != null && Object.hasOwnProperty.call(message, "ssoServiceProviderId"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.ssoServiceProviderId);
            if (message.ipAddress != null && Object.hasOwnProperty.call(message, "ipAddress"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.ipAddress);
            if (message.du1 != null && Object.hasOwnProperty.call(message, "du1"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.du1);
            if (message.du2 != null && Object.hasOwnProperty.call(message, "du2"))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.du2);
            if (message.enterpriseId != null && Object.hasOwnProperty.call(message, "enterpriseId"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.enterpriseId);
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                writer.uint32(/* id 8, wireType 0 =*/64).int64(message.deviceId);
            return writer;
        };

        /**
         * Encodes the specified IpAddressVerificationToken message, length delimited. Does not implicitly {@link Tokens.IpAddressVerificationToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.IpAddressVerificationToken
         * @static
         * @param {Tokens.IIpAddressVerificationToken} message IpAddressVerificationToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IpAddressVerificationToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an IpAddressVerificationToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.IpAddressVerificationToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.IpAddressVerificationToken} IpAddressVerificationToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IpAddressVerificationToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.IpAddressVerificationToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.userId = reader.int32();
                        break;
                    }
                case 2: {
                        message.email = reader.string();
                        break;
                    }
                case 3: {
                        message.ssoServiceProviderId = reader.int64();
                        break;
                    }
                case 4: {
                        message.ipAddress = reader.string();
                        break;
                    }
                case 5: {
                        message.du1 = reader.string();
                        break;
                    }
                case 6: {
                        message.du2 = reader.int64();
                        break;
                    }
                case 7: {
                        message.enterpriseId = reader.int32();
                        break;
                    }
                case 8: {
                        message.deviceId = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an IpAddressVerificationToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.IpAddressVerificationToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.IpAddressVerificationToken} IpAddressVerificationToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IpAddressVerificationToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an IpAddressVerificationToken message.
         * @function verify
         * @memberof Tokens.IpAddressVerificationToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        IpAddressVerificationToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                if (!$util.isInteger(message.userId))
                    return "userId: integer expected";
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                if (!$util.isString(message.email))
                    return "email: string expected";
            if (message.ssoServiceProviderId != null && Object.hasOwnProperty.call(message, "ssoServiceProviderId"))
                if (!$util.isInteger(message.ssoServiceProviderId) && !(message.ssoServiceProviderId && $util.isInteger(message.ssoServiceProviderId.low) && $util.isInteger(message.ssoServiceProviderId.high)))
                    return "ssoServiceProviderId: integer|Long expected";
            if (message.ipAddress != null && Object.hasOwnProperty.call(message, "ipAddress"))
                if (!$util.isString(message.ipAddress))
                    return "ipAddress: string expected";
            if (message.du1 != null && Object.hasOwnProperty.call(message, "du1"))
                if (!$util.isString(message.du1))
                    return "du1: string expected";
            if (message.du2 != null && Object.hasOwnProperty.call(message, "du2"))
                if (!$util.isInteger(message.du2) && !(message.du2 && $util.isInteger(message.du2.low) && $util.isInteger(message.du2.high)))
                    return "du2: integer|Long expected";
            if (message.enterpriseId != null && Object.hasOwnProperty.call(message, "enterpriseId"))
                if (!$util.isInteger(message.enterpriseId))
                    return "enterpriseId: integer expected";
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                if (!$util.isInteger(message.deviceId) && !(message.deviceId && $util.isInteger(message.deviceId.low) && $util.isInteger(message.deviceId.high)))
                    return "deviceId: integer|Long expected";
            return null;
        };

        /**
         * Creates an IpAddressVerificationToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.IpAddressVerificationToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.IpAddressVerificationToken} IpAddressVerificationToken
         */
        IpAddressVerificationToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.IpAddressVerificationToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.IpAddressVerificationToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.IpAddressVerificationToken();
            if (object.userId != null)
                message.userId = object.userId | 0;
            if (object.email != null)
                message.email = String(object.email);
            if (object.ssoServiceProviderId != null)
                if ($util.Long)
                    message.ssoServiceProviderId = $util.Long.fromValue(object.ssoServiceProviderId, false);
                else if (typeof object.ssoServiceProviderId === "string")
                    message.ssoServiceProviderId = parseInt(object.ssoServiceProviderId, 10);
                else if (typeof object.ssoServiceProviderId === "number")
                    message.ssoServiceProviderId = object.ssoServiceProviderId;
                else if (typeof object.ssoServiceProviderId === "object")
                    message.ssoServiceProviderId = new $util.LongBits(object.ssoServiceProviderId.low >>> 0, object.ssoServiceProviderId.high >>> 0).toNumber();
            if (object.ipAddress != null)
                message.ipAddress = String(object.ipAddress);
            if (object.du1 != null)
                message.du1 = String(object.du1);
            if (object.du2 != null)
                if ($util.Long)
                    message.du2 = $util.Long.fromValue(object.du2, false);
                else if (typeof object.du2 === "string")
                    message.du2 = parseInt(object.du2, 10);
                else if (typeof object.du2 === "number")
                    message.du2 = object.du2;
                else if (typeof object.du2 === "object")
                    message.du2 = new $util.LongBits(object.du2.low >>> 0, object.du2.high >>> 0).toNumber();
            if (object.enterpriseId != null)
                message.enterpriseId = object.enterpriseId | 0;
            if (object.deviceId != null)
                if ($util.Long)
                    message.deviceId = $util.Long.fromValue(object.deviceId, false);
                else if (typeof object.deviceId === "string")
                    message.deviceId = parseInt(object.deviceId, 10);
                else if (typeof object.deviceId === "number")
                    message.deviceId = object.deviceId;
                else if (typeof object.deviceId === "object")
                    message.deviceId = new $util.LongBits(object.deviceId.low >>> 0, object.deviceId.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from an IpAddressVerificationToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.IpAddressVerificationToken
         * @static
         * @param {Tokens.IpAddressVerificationToken} message IpAddressVerificationToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        IpAddressVerificationToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.userId = 0;
                object.email = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.ssoServiceProviderId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.ssoServiceProviderId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.ipAddress = "";
                object.du1 = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.du2 = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.du2 = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.enterpriseId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.deviceId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.deviceId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                object.userId = message.userId;
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                object.email = message.email;
            if (message.ssoServiceProviderId != null && Object.hasOwnProperty.call(message, "ssoServiceProviderId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.ssoServiceProviderId = typeof message.ssoServiceProviderId === "number" ? BigInt(message.ssoServiceProviderId) : $util.Long.fromBits(message.ssoServiceProviderId.low >>> 0, message.ssoServiceProviderId.high >>> 0, false).toBigInt();
                else if (typeof message.ssoServiceProviderId === "number")
                    object.ssoServiceProviderId = options.longs === String ? String(message.ssoServiceProviderId) : message.ssoServiceProviderId;
                else
                    object.ssoServiceProviderId = options.longs === String ? $util.Long.prototype.toString.call(message.ssoServiceProviderId) : options.longs === Number ? new $util.LongBits(message.ssoServiceProviderId.low >>> 0, message.ssoServiceProviderId.high >>> 0).toNumber() : message.ssoServiceProviderId;
            if (message.ipAddress != null && Object.hasOwnProperty.call(message, "ipAddress"))
                object.ipAddress = message.ipAddress;
            if (message.du1 != null && Object.hasOwnProperty.call(message, "du1"))
                object.du1 = message.du1;
            if (message.du2 != null && Object.hasOwnProperty.call(message, "du2"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.du2 = typeof message.du2 === "number" ? BigInt(message.du2) : $util.Long.fromBits(message.du2.low >>> 0, message.du2.high >>> 0, false).toBigInt();
                else if (typeof message.du2 === "number")
                    object.du2 = options.longs === String ? String(message.du2) : message.du2;
                else
                    object.du2 = options.longs === String ? $util.Long.prototype.toString.call(message.du2) : options.longs === Number ? new $util.LongBits(message.du2.low >>> 0, message.du2.high >>> 0).toNumber() : message.du2;
            if (message.enterpriseId != null && Object.hasOwnProperty.call(message, "enterpriseId"))
                object.enterpriseId = message.enterpriseId;
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.deviceId = typeof message.deviceId === "number" ? BigInt(message.deviceId) : $util.Long.fromBits(message.deviceId.low >>> 0, message.deviceId.high >>> 0, false).toBigInt();
                else if (typeof message.deviceId === "number")
                    object.deviceId = options.longs === String ? String(message.deviceId) : message.deviceId;
                else
                    object.deviceId = options.longs === String ? $util.Long.prototype.toString.call(message.deviceId) : options.longs === Number ? new $util.LongBits(message.deviceId.low >>> 0, message.deviceId.high >>> 0).toNumber() : message.deviceId;
            return object;
        };

        /**
         * Converts this IpAddressVerificationToken to JSON.
         * @function toJSON
         * @memberof Tokens.IpAddressVerificationToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        IpAddressVerificationToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for IpAddressVerificationToken
         * @function getTypeUrl
         * @memberof Tokens.IpAddressVerificationToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        IpAddressVerificationToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.IpAddressVerificationToken";
        };

        return IpAddressVerificationToken;
    })();

    Tokens.EnterprisePersonalAccountToken = (function() {

        /**
         * Properties of an EnterprisePersonalAccountToken.
         * @memberof Tokens
         * @interface IEnterprisePersonalAccountToken
         * @property {number|null} [enterpriseUserId] EnterprisePersonalAccountToken enterpriseUserId
         * @property {string|null} [email] EnterprisePersonalAccountToken email
         * @property {Authentication.SupportedLanguage|null} [supportedLanguage] EnterprisePersonalAccountToken supportedLanguage
         * @property {number|null} [duration] EnterprisePersonalAccountToken duration
         * @property {string|null} [referralName] EnterprisePersonalAccountToken referralName
         * @property {number|null} [creation] EnterprisePersonalAccountToken creation
         */

        /**
         * Constructs a new EnterprisePersonalAccountToken.
         * @memberof Tokens
         * @classdesc Represents an EnterprisePersonalAccountToken.
         * @implements IEnterprisePersonalAccountToken
         * @constructor
         * @param {Tokens.IEnterprisePersonalAccountToken=} [properties] Properties to set
         */
        function EnterprisePersonalAccountToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EnterprisePersonalAccountToken enterpriseUserId.
         * @member {number} enterpriseUserId
         * @memberof Tokens.EnterprisePersonalAccountToken
         * @instance
         */
        EnterprisePersonalAccountToken.prototype.enterpriseUserId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EnterprisePersonalAccountToken email.
         * @member {string} email
         * @memberof Tokens.EnterprisePersonalAccountToken
         * @instance
         */
        EnterprisePersonalAccountToken.prototype.email = "";

        /**
         * EnterprisePersonalAccountToken supportedLanguage.
         * @member {Authentication.SupportedLanguage} supportedLanguage
         * @memberof Tokens.EnterprisePersonalAccountToken
         * @instance
         */
        EnterprisePersonalAccountToken.prototype.supportedLanguage = 0;

        /**
         * EnterprisePersonalAccountToken duration.
         * @member {number} duration
         * @memberof Tokens.EnterprisePersonalAccountToken
         * @instance
         */
        EnterprisePersonalAccountToken.prototype.duration = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EnterprisePersonalAccountToken referralName.
         * @member {string} referralName
         * @memberof Tokens.EnterprisePersonalAccountToken
         * @instance
         */
        EnterprisePersonalAccountToken.prototype.referralName = "";

        /**
         * EnterprisePersonalAccountToken creation.
         * @member {number} creation
         * @memberof Tokens.EnterprisePersonalAccountToken
         * @instance
         */
        EnterprisePersonalAccountToken.prototype.creation = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new EnterprisePersonalAccountToken instance using the specified properties.
         * @function create
         * @memberof Tokens.EnterprisePersonalAccountToken
         * @static
         * @param {Tokens.IEnterprisePersonalAccountToken=} [properties] Properties to set
         * @returns {Tokens.EnterprisePersonalAccountToken} EnterprisePersonalAccountToken instance
         */
        EnterprisePersonalAccountToken.create = function create(properties) {
            return new EnterprisePersonalAccountToken(properties);
        };

        /**
         * Encodes the specified EnterprisePersonalAccountToken message. Does not implicitly {@link Tokens.EnterprisePersonalAccountToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.EnterprisePersonalAccountToken
         * @static
         * @param {Tokens.IEnterprisePersonalAccountToken} message EnterprisePersonalAccountToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterprisePersonalAccountToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.enterpriseUserId);
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.email);
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.supportedLanguage);
            if (message.duration != null && Object.hasOwnProperty.call(message, "duration"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.duration);
            if (message.referralName != null && Object.hasOwnProperty.call(message, "referralName"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.referralName);
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.creation);
            return writer;
        };

        /**
         * Encodes the specified EnterprisePersonalAccountToken message, length delimited. Does not implicitly {@link Tokens.EnterprisePersonalAccountToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.EnterprisePersonalAccountToken
         * @static
         * @param {Tokens.IEnterprisePersonalAccountToken} message EnterprisePersonalAccountToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterprisePersonalAccountToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EnterprisePersonalAccountToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.EnterprisePersonalAccountToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.EnterprisePersonalAccountToken} EnterprisePersonalAccountToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterprisePersonalAccountToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.EnterprisePersonalAccountToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.enterpriseUserId = reader.int64();
                        break;
                    }
                case 2: {
                        message.email = reader.string();
                        break;
                    }
                case 3: {
                        message.supportedLanguage = reader.int32();
                        break;
                    }
                case 4: {
                        message.duration = reader.int64();
                        break;
                    }
                case 5: {
                        message.referralName = reader.string();
                        break;
                    }
                case 6: {
                        message.creation = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an EnterprisePersonalAccountToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.EnterprisePersonalAccountToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.EnterprisePersonalAccountToken} EnterprisePersonalAccountToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterprisePersonalAccountToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EnterprisePersonalAccountToken message.
         * @function verify
         * @memberof Tokens.EnterprisePersonalAccountToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EnterprisePersonalAccountToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                if (!$util.isInteger(message.enterpriseUserId) && !(message.enterpriseUserId && $util.isInteger(message.enterpriseUserId.low) && $util.isInteger(message.enterpriseUserId.high)))
                    return "enterpriseUserId: integer|Long expected";
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                if (!$util.isString(message.email))
                    return "email: string expected";
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                switch (message.supportedLanguage) {
                default:
                    return "supportedLanguage: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                case 11:
                case 12:
                case 13:
                case 14:
                case 15:
                case 16:
                case 17:
                case 18:
                case 19:
                case 20:
                case 21:
                case 22:
                    break;
                }
            if (message.duration != null && Object.hasOwnProperty.call(message, "duration"))
                if (!$util.isInteger(message.duration) && !(message.duration && $util.isInteger(message.duration.low) && $util.isInteger(message.duration.high)))
                    return "duration: integer|Long expected";
            if (message.referralName != null && Object.hasOwnProperty.call(message, "referralName"))
                if (!$util.isString(message.referralName))
                    return "referralName: string expected";
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                if (!$util.isInteger(message.creation) && !(message.creation && $util.isInteger(message.creation.low) && $util.isInteger(message.creation.high)))
                    return "creation: integer|Long expected";
            return null;
        };

        /**
         * Creates an EnterprisePersonalAccountToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.EnterprisePersonalAccountToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.EnterprisePersonalAccountToken} EnterprisePersonalAccountToken
         */
        EnterprisePersonalAccountToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.EnterprisePersonalAccountToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.EnterprisePersonalAccountToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.EnterprisePersonalAccountToken();
            if (object.enterpriseUserId != null)
                if ($util.Long)
                    message.enterpriseUserId = $util.Long.fromValue(object.enterpriseUserId, false);
                else if (typeof object.enterpriseUserId === "string")
                    message.enterpriseUserId = parseInt(object.enterpriseUserId, 10);
                else if (typeof object.enterpriseUserId === "number")
                    message.enterpriseUserId = object.enterpriseUserId;
                else if (typeof object.enterpriseUserId === "object")
                    message.enterpriseUserId = new $util.LongBits(object.enterpriseUserId.low >>> 0, object.enterpriseUserId.high >>> 0).toNumber();
            if (object.email != null)
                message.email = String(object.email);
            switch (object.supportedLanguage) {
            default:
                if (typeof object.supportedLanguage === "number") {
                    message.supportedLanguage = object.supportedLanguage;
                    break;
                }
                break;
            case "ENGLISH":
            case 0:
                message.supportedLanguage = 0;
                break;
            case "ARABIC":
            case 1:
                message.supportedLanguage = 1;
                break;
            case "BRITISH":
            case 2:
                message.supportedLanguage = 2;
                break;
            case "CHINESE":
            case 3:
                message.supportedLanguage = 3;
                break;
            case "CHINESE_HONG_KONG":
            case 4:
                message.supportedLanguage = 4;
                break;
            case "CHINESE_TAIWAN":
            case 5:
                message.supportedLanguage = 5;
                break;
            case "DUTCH":
            case 6:
                message.supportedLanguage = 6;
                break;
            case "FRENCH":
            case 7:
                message.supportedLanguage = 7;
                break;
            case "GERMAN":
            case 8:
                message.supportedLanguage = 8;
                break;
            case "GREEK":
            case 9:
                message.supportedLanguage = 9;
                break;
            case "HEBREW":
            case 10:
                message.supportedLanguage = 10;
                break;
            case "ITALIAN":
            case 11:
                message.supportedLanguage = 11;
                break;
            case "JAPANESE":
            case 12:
                message.supportedLanguage = 12;
                break;
            case "KOREAN":
            case 13:
                message.supportedLanguage = 13;
                break;
            case "POLISH":
            case 14:
                message.supportedLanguage = 14;
                break;
            case "PORTUGUESE":
            case 15:
                message.supportedLanguage = 15;
                break;
            case "PORTUGUESE_BRAZIL":
            case 16:
                message.supportedLanguage = 16;
                break;
            case "ROMANIAN":
            case 17:
                message.supportedLanguage = 17;
                break;
            case "RUSSIAN":
            case 18:
                message.supportedLanguage = 18;
                break;
            case "SLOVAK":
            case 19:
                message.supportedLanguage = 19;
                break;
            case "SPANISH":
            case 20:
                message.supportedLanguage = 20;
                break;
            case "FINNISH":
            case 21:
                message.supportedLanguage = 21;
                break;
            case "SWEDISH":
            case 22:
                message.supportedLanguage = 22;
                break;
            }
            if (object.duration != null)
                if ($util.Long)
                    message.duration = $util.Long.fromValue(object.duration, false);
                else if (typeof object.duration === "string")
                    message.duration = parseInt(object.duration, 10);
                else if (typeof object.duration === "number")
                    message.duration = object.duration;
                else if (typeof object.duration === "object")
                    message.duration = new $util.LongBits(object.duration.low >>> 0, object.duration.high >>> 0).toNumber();
            if (object.referralName != null)
                message.referralName = String(object.referralName);
            if (object.creation != null)
                if ($util.Long)
                    message.creation = $util.Long.fromValue(object.creation, false);
                else if (typeof object.creation === "string")
                    message.creation = parseInt(object.creation, 10);
                else if (typeof object.creation === "number")
                    message.creation = object.creation;
                else if (typeof object.creation === "object")
                    message.creation = new $util.LongBits(object.creation.low >>> 0, object.creation.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from an EnterprisePersonalAccountToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.EnterprisePersonalAccountToken
         * @static
         * @param {Tokens.EnterprisePersonalAccountToken} message EnterprisePersonalAccountToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EnterprisePersonalAccountToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.enterpriseUserId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.enterpriseUserId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.email = "";
                object.supportedLanguage = options.enums === String ? "ENGLISH" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.duration = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.duration = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.referralName = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.creation = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.creation = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.enterpriseUserId = typeof message.enterpriseUserId === "number" ? BigInt(message.enterpriseUserId) : $util.Long.fromBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0, false).toBigInt();
                else if (typeof message.enterpriseUserId === "number")
                    object.enterpriseUserId = options.longs === String ? String(message.enterpriseUserId) : message.enterpriseUserId;
                else
                    object.enterpriseUserId = options.longs === String ? $util.Long.prototype.toString.call(message.enterpriseUserId) : options.longs === Number ? new $util.LongBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0).toNumber() : message.enterpriseUserId;
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                object.email = message.email;
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                object.supportedLanguage = options.enums === String ? $root.Authentication.SupportedLanguage[message.supportedLanguage] === undefined ? message.supportedLanguage : $root.Authentication.SupportedLanguage[message.supportedLanguage] : message.supportedLanguage;
            if (message.duration != null && Object.hasOwnProperty.call(message, "duration"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.duration = typeof message.duration === "number" ? BigInt(message.duration) : $util.Long.fromBits(message.duration.low >>> 0, message.duration.high >>> 0, false).toBigInt();
                else if (typeof message.duration === "number")
                    object.duration = options.longs === String ? String(message.duration) : message.duration;
                else
                    object.duration = options.longs === String ? $util.Long.prototype.toString.call(message.duration) : options.longs === Number ? new $util.LongBits(message.duration.low >>> 0, message.duration.high >>> 0).toNumber() : message.duration;
            if (message.referralName != null && Object.hasOwnProperty.call(message, "referralName"))
                object.referralName = message.referralName;
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.creation = typeof message.creation === "number" ? BigInt(message.creation) : $util.Long.fromBits(message.creation.low >>> 0, message.creation.high >>> 0, false).toBigInt();
                else if (typeof message.creation === "number")
                    object.creation = options.longs === String ? String(message.creation) : message.creation;
                else
                    object.creation = options.longs === String ? $util.Long.prototype.toString.call(message.creation) : options.longs === Number ? new $util.LongBits(message.creation.low >>> 0, message.creation.high >>> 0).toNumber() : message.creation;
            return object;
        };

        /**
         * Converts this EnterprisePersonalAccountToken to JSON.
         * @function toJSON
         * @memberof Tokens.EnterprisePersonalAccountToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EnterprisePersonalAccountToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EnterprisePersonalAccountToken
         * @function getTypeUrl
         * @memberof Tokens.EnterprisePersonalAccountToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EnterprisePersonalAccountToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.EnterprisePersonalAccountToken";
        };

        return EnterprisePersonalAccountToken;
    })();

    Tokens.UsernameVerificationToken = (function() {

        /**
         * Properties of a UsernameVerificationToken.
         * @memberof Tokens
         * @interface IUsernameVerificationToken
         * @property {string|null} [username] UsernameVerificationToken username
         * @property {Uint8Array|null} [accountUid] UsernameVerificationToken accountUid
         * @property {Authentication.SupportedLanguage|null} [supportedLanguage] UsernameVerificationToken supportedLanguage
         * @property {number|null} [creation] UsernameVerificationToken creation
         * @property {number|null} [clientVersionId] UsernameVerificationToken clientVersionId
         * @property {string|null} [alternateUsername] UsernameVerificationToken alternateUsername
         * @property {string|null} [toUsername] UsernameVerificationToken toUsername
         */

        /**
         * Constructs a new UsernameVerificationToken.
         * @memberof Tokens
         * @classdesc Represents a UsernameVerificationToken.
         * @implements IUsernameVerificationToken
         * @constructor
         * @param {Tokens.IUsernameVerificationToken=} [properties] Properties to set
         */
        function UsernameVerificationToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UsernameVerificationToken username.
         * @member {string} username
         * @memberof Tokens.UsernameVerificationToken
         * @instance
         */
        UsernameVerificationToken.prototype.username = "";

        /**
         * UsernameVerificationToken accountUid.
         * @member {Uint8Array} accountUid
         * @memberof Tokens.UsernameVerificationToken
         * @instance
         */
        UsernameVerificationToken.prototype.accountUid = $util.newBuffer([]);

        /**
         * UsernameVerificationToken supportedLanguage.
         * @member {Authentication.SupportedLanguage} supportedLanguage
         * @memberof Tokens.UsernameVerificationToken
         * @instance
         */
        UsernameVerificationToken.prototype.supportedLanguage = 0;

        /**
         * UsernameVerificationToken creation.
         * @member {number} creation
         * @memberof Tokens.UsernameVerificationToken
         * @instance
         */
        UsernameVerificationToken.prototype.creation = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * UsernameVerificationToken clientVersionId.
         * @member {number} clientVersionId
         * @memberof Tokens.UsernameVerificationToken
         * @instance
         */
        UsernameVerificationToken.prototype.clientVersionId = 0;

        /**
         * UsernameVerificationToken alternateUsername.
         * @member {string} alternateUsername
         * @memberof Tokens.UsernameVerificationToken
         * @instance
         */
        UsernameVerificationToken.prototype.alternateUsername = "";

        /**
         * UsernameVerificationToken toUsername.
         * @member {string} toUsername
         * @memberof Tokens.UsernameVerificationToken
         * @instance
         */
        UsernameVerificationToken.prototype.toUsername = "";

        /**
         * Creates a new UsernameVerificationToken instance using the specified properties.
         * @function create
         * @memberof Tokens.UsernameVerificationToken
         * @static
         * @param {Tokens.IUsernameVerificationToken=} [properties] Properties to set
         * @returns {Tokens.UsernameVerificationToken} UsernameVerificationToken instance
         */
        UsernameVerificationToken.create = function create(properties) {
            return new UsernameVerificationToken(properties);
        };

        /**
         * Encodes the specified UsernameVerificationToken message. Does not implicitly {@link Tokens.UsernameVerificationToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.UsernameVerificationToken
         * @static
         * @param {Tokens.IUsernameVerificationToken} message UsernameVerificationToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UsernameVerificationToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.username);
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.accountUid);
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.supportedLanguage);
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.creation);
            if (message.clientVersionId != null && Object.hasOwnProperty.call(message, "clientVersionId"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.clientVersionId);
            if (message.alternateUsername != null && Object.hasOwnProperty.call(message, "alternateUsername"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.alternateUsername);
            if (message.toUsername != null && Object.hasOwnProperty.call(message, "toUsername"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.toUsername);
            return writer;
        };

        /**
         * Encodes the specified UsernameVerificationToken message, length delimited. Does not implicitly {@link Tokens.UsernameVerificationToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.UsernameVerificationToken
         * @static
         * @param {Tokens.IUsernameVerificationToken} message UsernameVerificationToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UsernameVerificationToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a UsernameVerificationToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.UsernameVerificationToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.UsernameVerificationToken} UsernameVerificationToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UsernameVerificationToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.UsernameVerificationToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.username = reader.string();
                        break;
                    }
                case 2: {
                        message.accountUid = reader.bytes();
                        break;
                    }
                case 3: {
                        message.supportedLanguage = reader.int32();
                        break;
                    }
                case 4: {
                        message.creation = reader.int64();
                        break;
                    }
                case 5: {
                        message.clientVersionId = reader.int32();
                        break;
                    }
                case 6: {
                        message.alternateUsername = reader.string();
                        break;
                    }
                case 7: {
                        message.toUsername = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a UsernameVerificationToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.UsernameVerificationToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.UsernameVerificationToken} UsernameVerificationToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UsernameVerificationToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a UsernameVerificationToken message.
         * @function verify
         * @memberof Tokens.UsernameVerificationToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UsernameVerificationToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                if (!$util.isString(message.username))
                    return "username: string expected";
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                if (!(message.accountUid && typeof message.accountUid.length === "number" || $util.isString(message.accountUid)))
                    return "accountUid: buffer expected";
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                switch (message.supportedLanguage) {
                default:
                    return "supportedLanguage: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                case 11:
                case 12:
                case 13:
                case 14:
                case 15:
                case 16:
                case 17:
                case 18:
                case 19:
                case 20:
                case 21:
                case 22:
                    break;
                }
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                if (!$util.isInteger(message.creation) && !(message.creation && $util.isInteger(message.creation.low) && $util.isInteger(message.creation.high)))
                    return "creation: integer|Long expected";
            if (message.clientVersionId != null && Object.hasOwnProperty.call(message, "clientVersionId"))
                if (!$util.isInteger(message.clientVersionId))
                    return "clientVersionId: integer expected";
            if (message.alternateUsername != null && Object.hasOwnProperty.call(message, "alternateUsername"))
                if (!$util.isString(message.alternateUsername))
                    return "alternateUsername: string expected";
            if (message.toUsername != null && Object.hasOwnProperty.call(message, "toUsername"))
                if (!$util.isString(message.toUsername))
                    return "toUsername: string expected";
            return null;
        };

        /**
         * Creates a UsernameVerificationToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.UsernameVerificationToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.UsernameVerificationToken} UsernameVerificationToken
         */
        UsernameVerificationToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.UsernameVerificationToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.UsernameVerificationToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.UsernameVerificationToken();
            if (object.username != null)
                message.username = String(object.username);
            if (object.accountUid != null)
                if (typeof object.accountUid === "string")
                    $util.base64.decode(object.accountUid, message.accountUid = $util.newBuffer($util.base64.length(object.accountUid)), 0);
                else if (object.accountUid.length >= 0)
                    message.accountUid = object.accountUid;
            switch (object.supportedLanguage) {
            default:
                if (typeof object.supportedLanguage === "number") {
                    message.supportedLanguage = object.supportedLanguage;
                    break;
                }
                break;
            case "ENGLISH":
            case 0:
                message.supportedLanguage = 0;
                break;
            case "ARABIC":
            case 1:
                message.supportedLanguage = 1;
                break;
            case "BRITISH":
            case 2:
                message.supportedLanguage = 2;
                break;
            case "CHINESE":
            case 3:
                message.supportedLanguage = 3;
                break;
            case "CHINESE_HONG_KONG":
            case 4:
                message.supportedLanguage = 4;
                break;
            case "CHINESE_TAIWAN":
            case 5:
                message.supportedLanguage = 5;
                break;
            case "DUTCH":
            case 6:
                message.supportedLanguage = 6;
                break;
            case "FRENCH":
            case 7:
                message.supportedLanguage = 7;
                break;
            case "GERMAN":
            case 8:
                message.supportedLanguage = 8;
                break;
            case "GREEK":
            case 9:
                message.supportedLanguage = 9;
                break;
            case "HEBREW":
            case 10:
                message.supportedLanguage = 10;
                break;
            case "ITALIAN":
            case 11:
                message.supportedLanguage = 11;
                break;
            case "JAPANESE":
            case 12:
                message.supportedLanguage = 12;
                break;
            case "KOREAN":
            case 13:
                message.supportedLanguage = 13;
                break;
            case "POLISH":
            case 14:
                message.supportedLanguage = 14;
                break;
            case "PORTUGUESE":
            case 15:
                message.supportedLanguage = 15;
                break;
            case "PORTUGUESE_BRAZIL":
            case 16:
                message.supportedLanguage = 16;
                break;
            case "ROMANIAN":
            case 17:
                message.supportedLanguage = 17;
                break;
            case "RUSSIAN":
            case 18:
                message.supportedLanguage = 18;
                break;
            case "SLOVAK":
            case 19:
                message.supportedLanguage = 19;
                break;
            case "SPANISH":
            case 20:
                message.supportedLanguage = 20;
                break;
            case "FINNISH":
            case 21:
                message.supportedLanguage = 21;
                break;
            case "SWEDISH":
            case 22:
                message.supportedLanguage = 22;
                break;
            }
            if (object.creation != null)
                if ($util.Long)
                    message.creation = $util.Long.fromValue(object.creation, false);
                else if (typeof object.creation === "string")
                    message.creation = parseInt(object.creation, 10);
                else if (typeof object.creation === "number")
                    message.creation = object.creation;
                else if (typeof object.creation === "object")
                    message.creation = new $util.LongBits(object.creation.low >>> 0, object.creation.high >>> 0).toNumber();
            if (object.clientVersionId != null)
                message.clientVersionId = object.clientVersionId | 0;
            if (object.alternateUsername != null)
                message.alternateUsername = String(object.alternateUsername);
            if (object.toUsername != null)
                message.toUsername = String(object.toUsername);
            return message;
        };

        /**
         * Creates a plain object from a UsernameVerificationToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.UsernameVerificationToken
         * @static
         * @param {Tokens.UsernameVerificationToken} message UsernameVerificationToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UsernameVerificationToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.username = "";
                if (options.bytes === String)
                    object.accountUid = "";
                else {
                    object.accountUid = [];
                    if (options.bytes !== Array)
                        object.accountUid = $util.newBuffer(object.accountUid);
                }
                object.supportedLanguage = options.enums === String ? "ENGLISH" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.creation = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.creation = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.clientVersionId = 0;
                object.alternateUsername = "";
                object.toUsername = "";
            }
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                object.accountUid = options.bytes === String ? $util.base64.encode(message.accountUid, 0, message.accountUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.accountUid) : message.accountUid;
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                object.supportedLanguage = options.enums === String ? $root.Authentication.SupportedLanguage[message.supportedLanguage] === undefined ? message.supportedLanguage : $root.Authentication.SupportedLanguage[message.supportedLanguage] : message.supportedLanguage;
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.creation = typeof message.creation === "number" ? BigInt(message.creation) : $util.Long.fromBits(message.creation.low >>> 0, message.creation.high >>> 0, false).toBigInt();
                else if (typeof message.creation === "number")
                    object.creation = options.longs === String ? String(message.creation) : message.creation;
                else
                    object.creation = options.longs === String ? $util.Long.prototype.toString.call(message.creation) : options.longs === Number ? new $util.LongBits(message.creation.low >>> 0, message.creation.high >>> 0).toNumber() : message.creation;
            if (message.clientVersionId != null && Object.hasOwnProperty.call(message, "clientVersionId"))
                object.clientVersionId = message.clientVersionId;
            if (message.alternateUsername != null && Object.hasOwnProperty.call(message, "alternateUsername"))
                object.alternateUsername = message.alternateUsername;
            if (message.toUsername != null && Object.hasOwnProperty.call(message, "toUsername"))
                object.toUsername = message.toUsername;
            return object;
        };

        /**
         * Converts this UsernameVerificationToken to JSON.
         * @function toJSON
         * @memberof Tokens.UsernameVerificationToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UsernameVerificationToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UsernameVerificationToken
         * @function getTypeUrl
         * @memberof Tokens.UsernameVerificationToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UsernameVerificationToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.UsernameVerificationToken";
        };

        return UsernameVerificationToken;
    })();

    Tokens.DeviceVerificationToken = (function() {

        /**
         * Properties of a DeviceVerificationToken.
         * @memberof Tokens
         * @interface IDeviceVerificationToken
         * @property {number|null} [deviceId] DeviceVerificationToken deviceId
         * @property {string|null} [username] DeviceVerificationToken username
         * @property {Uint8Array|null} [accountUid] DeviceVerificationToken accountUid
         * @property {string|null} [clientVersion] DeviceVerificationToken clientVersion
         * @property {Authentication.SupportedLanguage|null} [supportedLanguage] DeviceVerificationToken supportedLanguage
         * @property {number|null} [creation] DeviceVerificationToken creation
         * @property {Uint8Array|null} [messageSessionUid] DeviceVerificationToken messageSessionUid
         * @property {string|null} [ipAddress] DeviceVerificationToken ipAddress
         */

        /**
         * Constructs a new DeviceVerificationToken.
         * @memberof Tokens
         * @classdesc Represents a DeviceVerificationToken.
         * @implements IDeviceVerificationToken
         * @constructor
         * @param {Tokens.IDeviceVerificationToken=} [properties] Properties to set
         */
        function DeviceVerificationToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeviceVerificationToken deviceId.
         * @member {number} deviceId
         * @memberof Tokens.DeviceVerificationToken
         * @instance
         */
        DeviceVerificationToken.prototype.deviceId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * DeviceVerificationToken username.
         * @member {string} username
         * @memberof Tokens.DeviceVerificationToken
         * @instance
         */
        DeviceVerificationToken.prototype.username = "";

        /**
         * DeviceVerificationToken accountUid.
         * @member {Uint8Array} accountUid
         * @memberof Tokens.DeviceVerificationToken
         * @instance
         */
        DeviceVerificationToken.prototype.accountUid = $util.newBuffer([]);

        /**
         * DeviceVerificationToken clientVersion.
         * @member {string} clientVersion
         * @memberof Tokens.DeviceVerificationToken
         * @instance
         */
        DeviceVerificationToken.prototype.clientVersion = "";

        /**
         * DeviceVerificationToken supportedLanguage.
         * @member {Authentication.SupportedLanguage} supportedLanguage
         * @memberof Tokens.DeviceVerificationToken
         * @instance
         */
        DeviceVerificationToken.prototype.supportedLanguage = 0;

        /**
         * DeviceVerificationToken creation.
         * @member {number} creation
         * @memberof Tokens.DeviceVerificationToken
         * @instance
         */
        DeviceVerificationToken.prototype.creation = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * DeviceVerificationToken messageSessionUid.
         * @member {Uint8Array} messageSessionUid
         * @memberof Tokens.DeviceVerificationToken
         * @instance
         */
        DeviceVerificationToken.prototype.messageSessionUid = $util.newBuffer([]);

        /**
         * DeviceVerificationToken ipAddress.
         * @member {string} ipAddress
         * @memberof Tokens.DeviceVerificationToken
         * @instance
         */
        DeviceVerificationToken.prototype.ipAddress = "";

        /**
         * Creates a new DeviceVerificationToken instance using the specified properties.
         * @function create
         * @memberof Tokens.DeviceVerificationToken
         * @static
         * @param {Tokens.IDeviceVerificationToken=} [properties] Properties to set
         * @returns {Tokens.DeviceVerificationToken} DeviceVerificationToken instance
         */
        DeviceVerificationToken.create = function create(properties) {
            return new DeviceVerificationToken(properties);
        };

        /**
         * Encodes the specified DeviceVerificationToken message. Does not implicitly {@link Tokens.DeviceVerificationToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.DeviceVerificationToken
         * @static
         * @param {Tokens.IDeviceVerificationToken} message DeviceVerificationToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceVerificationToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.deviceId);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.username);
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.accountUid);
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.clientVersion);
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.supportedLanguage);
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                writer.uint32(/* id 6, wireType 0 =*/48).int64(message.creation);
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                writer.uint32(/* id 7, wireType 2 =*/58).bytes(message.messageSessionUid);
            if (message.ipAddress != null && Object.hasOwnProperty.call(message, "ipAddress"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.ipAddress);
            return writer;
        };

        /**
         * Encodes the specified DeviceVerificationToken message, length delimited. Does not implicitly {@link Tokens.DeviceVerificationToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.DeviceVerificationToken
         * @static
         * @param {Tokens.IDeviceVerificationToken} message DeviceVerificationToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeviceVerificationToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DeviceVerificationToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.DeviceVerificationToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.DeviceVerificationToken} DeviceVerificationToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceVerificationToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.DeviceVerificationToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.deviceId = reader.int64();
                        break;
                    }
                case 2: {
                        message.username = reader.string();
                        break;
                    }
                case 3: {
                        message.accountUid = reader.bytes();
                        break;
                    }
                case 4: {
                        message.clientVersion = reader.string();
                        break;
                    }
                case 5: {
                        message.supportedLanguage = reader.int32();
                        break;
                    }
                case 6: {
                        message.creation = reader.int64();
                        break;
                    }
                case 7: {
                        message.messageSessionUid = reader.bytes();
                        break;
                    }
                case 8: {
                        message.ipAddress = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a DeviceVerificationToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.DeviceVerificationToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.DeviceVerificationToken} DeviceVerificationToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeviceVerificationToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeviceVerificationToken message.
         * @function verify
         * @memberof Tokens.DeviceVerificationToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeviceVerificationToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                if (!$util.isInteger(message.deviceId) && !(message.deviceId && $util.isInteger(message.deviceId.low) && $util.isInteger(message.deviceId.high)))
                    return "deviceId: integer|Long expected";
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                if (!$util.isString(message.username))
                    return "username: string expected";
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                if (!(message.accountUid && typeof message.accountUid.length === "number" || $util.isString(message.accountUid)))
                    return "accountUid: buffer expected";
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                if (!$util.isString(message.clientVersion))
                    return "clientVersion: string expected";
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                switch (message.supportedLanguage) {
                default:
                    return "supportedLanguage: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                case 11:
                case 12:
                case 13:
                case 14:
                case 15:
                case 16:
                case 17:
                case 18:
                case 19:
                case 20:
                case 21:
                case 22:
                    break;
                }
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                if (!$util.isInteger(message.creation) && !(message.creation && $util.isInteger(message.creation.low) && $util.isInteger(message.creation.high)))
                    return "creation: integer|Long expected";
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                if (!(message.messageSessionUid && typeof message.messageSessionUid.length === "number" || $util.isString(message.messageSessionUid)))
                    return "messageSessionUid: buffer expected";
            if (message.ipAddress != null && Object.hasOwnProperty.call(message, "ipAddress"))
                if (!$util.isString(message.ipAddress))
                    return "ipAddress: string expected";
            return null;
        };

        /**
         * Creates a DeviceVerificationToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.DeviceVerificationToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.DeviceVerificationToken} DeviceVerificationToken
         */
        DeviceVerificationToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.DeviceVerificationToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.DeviceVerificationToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.DeviceVerificationToken();
            if (object.deviceId != null)
                if ($util.Long)
                    message.deviceId = $util.Long.fromValue(object.deviceId, false);
                else if (typeof object.deviceId === "string")
                    message.deviceId = parseInt(object.deviceId, 10);
                else if (typeof object.deviceId === "number")
                    message.deviceId = object.deviceId;
                else if (typeof object.deviceId === "object")
                    message.deviceId = new $util.LongBits(object.deviceId.low >>> 0, object.deviceId.high >>> 0).toNumber();
            if (object.username != null)
                message.username = String(object.username);
            if (object.accountUid != null)
                if (typeof object.accountUid === "string")
                    $util.base64.decode(object.accountUid, message.accountUid = $util.newBuffer($util.base64.length(object.accountUid)), 0);
                else if (object.accountUid.length >= 0)
                    message.accountUid = object.accountUid;
            if (object.clientVersion != null)
                message.clientVersion = String(object.clientVersion);
            switch (object.supportedLanguage) {
            default:
                if (typeof object.supportedLanguage === "number") {
                    message.supportedLanguage = object.supportedLanguage;
                    break;
                }
                break;
            case "ENGLISH":
            case 0:
                message.supportedLanguage = 0;
                break;
            case "ARABIC":
            case 1:
                message.supportedLanguage = 1;
                break;
            case "BRITISH":
            case 2:
                message.supportedLanguage = 2;
                break;
            case "CHINESE":
            case 3:
                message.supportedLanguage = 3;
                break;
            case "CHINESE_HONG_KONG":
            case 4:
                message.supportedLanguage = 4;
                break;
            case "CHINESE_TAIWAN":
            case 5:
                message.supportedLanguage = 5;
                break;
            case "DUTCH":
            case 6:
                message.supportedLanguage = 6;
                break;
            case "FRENCH":
            case 7:
                message.supportedLanguage = 7;
                break;
            case "GERMAN":
            case 8:
                message.supportedLanguage = 8;
                break;
            case "GREEK":
            case 9:
                message.supportedLanguage = 9;
                break;
            case "HEBREW":
            case 10:
                message.supportedLanguage = 10;
                break;
            case "ITALIAN":
            case 11:
                message.supportedLanguage = 11;
                break;
            case "JAPANESE":
            case 12:
                message.supportedLanguage = 12;
                break;
            case "KOREAN":
            case 13:
                message.supportedLanguage = 13;
                break;
            case "POLISH":
            case 14:
                message.supportedLanguage = 14;
                break;
            case "PORTUGUESE":
            case 15:
                message.supportedLanguage = 15;
                break;
            case "PORTUGUESE_BRAZIL":
            case 16:
                message.supportedLanguage = 16;
                break;
            case "ROMANIAN":
            case 17:
                message.supportedLanguage = 17;
                break;
            case "RUSSIAN":
            case 18:
                message.supportedLanguage = 18;
                break;
            case "SLOVAK":
            case 19:
                message.supportedLanguage = 19;
                break;
            case "SPANISH":
            case 20:
                message.supportedLanguage = 20;
                break;
            case "FINNISH":
            case 21:
                message.supportedLanguage = 21;
                break;
            case "SWEDISH":
            case 22:
                message.supportedLanguage = 22;
                break;
            }
            if (object.creation != null)
                if ($util.Long)
                    message.creation = $util.Long.fromValue(object.creation, false);
                else if (typeof object.creation === "string")
                    message.creation = parseInt(object.creation, 10);
                else if (typeof object.creation === "number")
                    message.creation = object.creation;
                else if (typeof object.creation === "object")
                    message.creation = new $util.LongBits(object.creation.low >>> 0, object.creation.high >>> 0).toNumber();
            if (object.messageSessionUid != null)
                if (typeof object.messageSessionUid === "string")
                    $util.base64.decode(object.messageSessionUid, message.messageSessionUid = $util.newBuffer($util.base64.length(object.messageSessionUid)), 0);
                else if (object.messageSessionUid.length >= 0)
                    message.messageSessionUid = object.messageSessionUid;
            if (object.ipAddress != null)
                message.ipAddress = String(object.ipAddress);
            return message;
        };

        /**
         * Creates a plain object from a DeviceVerificationToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.DeviceVerificationToken
         * @static
         * @param {Tokens.DeviceVerificationToken} message DeviceVerificationToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeviceVerificationToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.deviceId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.deviceId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.username = "";
                if (options.bytes === String)
                    object.accountUid = "";
                else {
                    object.accountUid = [];
                    if (options.bytes !== Array)
                        object.accountUid = $util.newBuffer(object.accountUid);
                }
                object.clientVersion = "";
                object.supportedLanguage = options.enums === String ? "ENGLISH" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.creation = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.creation = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.messageSessionUid = "";
                else {
                    object.messageSessionUid = [];
                    if (options.bytes !== Array)
                        object.messageSessionUid = $util.newBuffer(object.messageSessionUid);
                }
                object.ipAddress = "";
            }
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.deviceId = typeof message.deviceId === "number" ? BigInt(message.deviceId) : $util.Long.fromBits(message.deviceId.low >>> 0, message.deviceId.high >>> 0, false).toBigInt();
                else if (typeof message.deviceId === "number")
                    object.deviceId = options.longs === String ? String(message.deviceId) : message.deviceId;
                else
                    object.deviceId = options.longs === String ? $util.Long.prototype.toString.call(message.deviceId) : options.longs === Number ? new $util.LongBits(message.deviceId.low >>> 0, message.deviceId.high >>> 0).toNumber() : message.deviceId;
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                object.accountUid = options.bytes === String ? $util.base64.encode(message.accountUid, 0, message.accountUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.accountUid) : message.accountUid;
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                object.clientVersion = message.clientVersion;
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                object.supportedLanguage = options.enums === String ? $root.Authentication.SupportedLanguage[message.supportedLanguage] === undefined ? message.supportedLanguage : $root.Authentication.SupportedLanguage[message.supportedLanguage] : message.supportedLanguage;
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.creation = typeof message.creation === "number" ? BigInt(message.creation) : $util.Long.fromBits(message.creation.low >>> 0, message.creation.high >>> 0, false).toBigInt();
                else if (typeof message.creation === "number")
                    object.creation = options.longs === String ? String(message.creation) : message.creation;
                else
                    object.creation = options.longs === String ? $util.Long.prototype.toString.call(message.creation) : options.longs === Number ? new $util.LongBits(message.creation.low >>> 0, message.creation.high >>> 0).toNumber() : message.creation;
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                object.messageSessionUid = options.bytes === String ? $util.base64.encode(message.messageSessionUid, 0, message.messageSessionUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.messageSessionUid) : message.messageSessionUid;
            if (message.ipAddress != null && Object.hasOwnProperty.call(message, "ipAddress"))
                object.ipAddress = message.ipAddress;
            return object;
        };

        /**
         * Converts this DeviceVerificationToken to JSON.
         * @function toJSON
         * @memberof Tokens.DeviceVerificationToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeviceVerificationToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeviceVerificationToken
         * @function getTypeUrl
         * @memberof Tokens.DeviceVerificationToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeviceVerificationToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.DeviceVerificationToken";
        };

        return DeviceVerificationToken;
    })();

    Tokens.ReActivateDeviceApprovalRequestToken = (function() {

        /**
         * Properties of a ReActivateDeviceApprovalRequestToken.
         * @memberof Tokens
         * @interface IReActivateDeviceApprovalRequestToken
         * @property {number|null} [deviceId] ReActivateDeviceApprovalRequestToken deviceId
         * @property {string|null} [fromUsername] ReActivateDeviceApprovalRequestToken fromUsername
         * @property {string|null} [forUsername] ReActivateDeviceApprovalRequestToken forUsername
         * @property {Uint8Array|null} [accountUid] ReActivateDeviceApprovalRequestToken accountUid
         * @property {number|null} [requestTimeStamp] ReActivateDeviceApprovalRequestToken requestTimeStamp
         * @property {Authentication.SupportedLanguage|null} [supportedLanguage] ReActivateDeviceApprovalRequestToken supportedLanguage
         */

        /**
         * Constructs a new ReActivateDeviceApprovalRequestToken.
         * @memberof Tokens
         * @classdesc Represents a ReActivateDeviceApprovalRequestToken.
         * @implements IReActivateDeviceApprovalRequestToken
         * @constructor
         * @param {Tokens.IReActivateDeviceApprovalRequestToken=} [properties] Properties to set
         */
        function ReActivateDeviceApprovalRequestToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ReActivateDeviceApprovalRequestToken deviceId.
         * @member {number} deviceId
         * @memberof Tokens.ReActivateDeviceApprovalRequestToken
         * @instance
         */
        ReActivateDeviceApprovalRequestToken.prototype.deviceId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ReActivateDeviceApprovalRequestToken fromUsername.
         * @member {string} fromUsername
         * @memberof Tokens.ReActivateDeviceApprovalRequestToken
         * @instance
         */
        ReActivateDeviceApprovalRequestToken.prototype.fromUsername = "";

        /**
         * ReActivateDeviceApprovalRequestToken forUsername.
         * @member {string} forUsername
         * @memberof Tokens.ReActivateDeviceApprovalRequestToken
         * @instance
         */
        ReActivateDeviceApprovalRequestToken.prototype.forUsername = "";

        /**
         * ReActivateDeviceApprovalRequestToken accountUid.
         * @member {Uint8Array} accountUid
         * @memberof Tokens.ReActivateDeviceApprovalRequestToken
         * @instance
         */
        ReActivateDeviceApprovalRequestToken.prototype.accountUid = $util.newBuffer([]);

        /**
         * ReActivateDeviceApprovalRequestToken requestTimeStamp.
         * @member {number} requestTimeStamp
         * @memberof Tokens.ReActivateDeviceApprovalRequestToken
         * @instance
         */
        ReActivateDeviceApprovalRequestToken.prototype.requestTimeStamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ReActivateDeviceApprovalRequestToken supportedLanguage.
         * @member {Authentication.SupportedLanguage} supportedLanguage
         * @memberof Tokens.ReActivateDeviceApprovalRequestToken
         * @instance
         */
        ReActivateDeviceApprovalRequestToken.prototype.supportedLanguage = 0;

        /**
         * Creates a new ReActivateDeviceApprovalRequestToken instance using the specified properties.
         * @function create
         * @memberof Tokens.ReActivateDeviceApprovalRequestToken
         * @static
         * @param {Tokens.IReActivateDeviceApprovalRequestToken=} [properties] Properties to set
         * @returns {Tokens.ReActivateDeviceApprovalRequestToken} ReActivateDeviceApprovalRequestToken instance
         */
        ReActivateDeviceApprovalRequestToken.create = function create(properties) {
            return new ReActivateDeviceApprovalRequestToken(properties);
        };

        /**
         * Encodes the specified ReActivateDeviceApprovalRequestToken message. Does not implicitly {@link Tokens.ReActivateDeviceApprovalRequestToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.ReActivateDeviceApprovalRequestToken
         * @static
         * @param {Tokens.IReActivateDeviceApprovalRequestToken} message ReActivateDeviceApprovalRequestToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReActivateDeviceApprovalRequestToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.deviceId);
            if (message.fromUsername != null && Object.hasOwnProperty.call(message, "fromUsername"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.fromUsername);
            if (message.forUsername != null && Object.hasOwnProperty.call(message, "forUsername"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.forUsername);
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.accountUid);
            if (message.requestTimeStamp != null && Object.hasOwnProperty.call(message, "requestTimeStamp"))
                writer.uint32(/* id 5, wireType 0 =*/40).int64(message.requestTimeStamp);
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.supportedLanguage);
            return writer;
        };

        /**
         * Encodes the specified ReActivateDeviceApprovalRequestToken message, length delimited. Does not implicitly {@link Tokens.ReActivateDeviceApprovalRequestToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.ReActivateDeviceApprovalRequestToken
         * @static
         * @param {Tokens.IReActivateDeviceApprovalRequestToken} message ReActivateDeviceApprovalRequestToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ReActivateDeviceApprovalRequestToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ReActivateDeviceApprovalRequestToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.ReActivateDeviceApprovalRequestToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.ReActivateDeviceApprovalRequestToken} ReActivateDeviceApprovalRequestToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReActivateDeviceApprovalRequestToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.ReActivateDeviceApprovalRequestToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.deviceId = reader.int64();
                        break;
                    }
                case 2: {
                        message.fromUsername = reader.string();
                        break;
                    }
                case 3: {
                        message.forUsername = reader.string();
                        break;
                    }
                case 4: {
                        message.accountUid = reader.bytes();
                        break;
                    }
                case 5: {
                        message.requestTimeStamp = reader.int64();
                        break;
                    }
                case 6: {
                        message.supportedLanguage = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ReActivateDeviceApprovalRequestToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.ReActivateDeviceApprovalRequestToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.ReActivateDeviceApprovalRequestToken} ReActivateDeviceApprovalRequestToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ReActivateDeviceApprovalRequestToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ReActivateDeviceApprovalRequestToken message.
         * @function verify
         * @memberof Tokens.ReActivateDeviceApprovalRequestToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ReActivateDeviceApprovalRequestToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                if (!$util.isInteger(message.deviceId) && !(message.deviceId && $util.isInteger(message.deviceId.low) && $util.isInteger(message.deviceId.high)))
                    return "deviceId: integer|Long expected";
            if (message.fromUsername != null && Object.hasOwnProperty.call(message, "fromUsername"))
                if (!$util.isString(message.fromUsername))
                    return "fromUsername: string expected";
            if (message.forUsername != null && Object.hasOwnProperty.call(message, "forUsername"))
                if (!$util.isString(message.forUsername))
                    return "forUsername: string expected";
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                if (!(message.accountUid && typeof message.accountUid.length === "number" || $util.isString(message.accountUid)))
                    return "accountUid: buffer expected";
            if (message.requestTimeStamp != null && Object.hasOwnProperty.call(message, "requestTimeStamp"))
                if (!$util.isInteger(message.requestTimeStamp) && !(message.requestTimeStamp && $util.isInteger(message.requestTimeStamp.low) && $util.isInteger(message.requestTimeStamp.high)))
                    return "requestTimeStamp: integer|Long expected";
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                switch (message.supportedLanguage) {
                default:
                    return "supportedLanguage: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                case 11:
                case 12:
                case 13:
                case 14:
                case 15:
                case 16:
                case 17:
                case 18:
                case 19:
                case 20:
                case 21:
                case 22:
                    break;
                }
            return null;
        };

        /**
         * Creates a ReActivateDeviceApprovalRequestToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.ReActivateDeviceApprovalRequestToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.ReActivateDeviceApprovalRequestToken} ReActivateDeviceApprovalRequestToken
         */
        ReActivateDeviceApprovalRequestToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.ReActivateDeviceApprovalRequestToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.ReActivateDeviceApprovalRequestToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.ReActivateDeviceApprovalRequestToken();
            if (object.deviceId != null)
                if ($util.Long)
                    message.deviceId = $util.Long.fromValue(object.deviceId, false);
                else if (typeof object.deviceId === "string")
                    message.deviceId = parseInt(object.deviceId, 10);
                else if (typeof object.deviceId === "number")
                    message.deviceId = object.deviceId;
                else if (typeof object.deviceId === "object")
                    message.deviceId = new $util.LongBits(object.deviceId.low >>> 0, object.deviceId.high >>> 0).toNumber();
            if (object.fromUsername != null)
                message.fromUsername = String(object.fromUsername);
            if (object.forUsername != null)
                message.forUsername = String(object.forUsername);
            if (object.accountUid != null)
                if (typeof object.accountUid === "string")
                    $util.base64.decode(object.accountUid, message.accountUid = $util.newBuffer($util.base64.length(object.accountUid)), 0);
                else if (object.accountUid.length >= 0)
                    message.accountUid = object.accountUid;
            if (object.requestTimeStamp != null)
                if ($util.Long)
                    message.requestTimeStamp = $util.Long.fromValue(object.requestTimeStamp, false);
                else if (typeof object.requestTimeStamp === "string")
                    message.requestTimeStamp = parseInt(object.requestTimeStamp, 10);
                else if (typeof object.requestTimeStamp === "number")
                    message.requestTimeStamp = object.requestTimeStamp;
                else if (typeof object.requestTimeStamp === "object")
                    message.requestTimeStamp = new $util.LongBits(object.requestTimeStamp.low >>> 0, object.requestTimeStamp.high >>> 0).toNumber();
            switch (object.supportedLanguage) {
            default:
                if (typeof object.supportedLanguage === "number") {
                    message.supportedLanguage = object.supportedLanguage;
                    break;
                }
                break;
            case "ENGLISH":
            case 0:
                message.supportedLanguage = 0;
                break;
            case "ARABIC":
            case 1:
                message.supportedLanguage = 1;
                break;
            case "BRITISH":
            case 2:
                message.supportedLanguage = 2;
                break;
            case "CHINESE":
            case 3:
                message.supportedLanguage = 3;
                break;
            case "CHINESE_HONG_KONG":
            case 4:
                message.supportedLanguage = 4;
                break;
            case "CHINESE_TAIWAN":
            case 5:
                message.supportedLanguage = 5;
                break;
            case "DUTCH":
            case 6:
                message.supportedLanguage = 6;
                break;
            case "FRENCH":
            case 7:
                message.supportedLanguage = 7;
                break;
            case "GERMAN":
            case 8:
                message.supportedLanguage = 8;
                break;
            case "GREEK":
            case 9:
                message.supportedLanguage = 9;
                break;
            case "HEBREW":
            case 10:
                message.supportedLanguage = 10;
                break;
            case "ITALIAN":
            case 11:
                message.supportedLanguage = 11;
                break;
            case "JAPANESE":
            case 12:
                message.supportedLanguage = 12;
                break;
            case "KOREAN":
            case 13:
                message.supportedLanguage = 13;
                break;
            case "POLISH":
            case 14:
                message.supportedLanguage = 14;
                break;
            case "PORTUGUESE":
            case 15:
                message.supportedLanguage = 15;
                break;
            case "PORTUGUESE_BRAZIL":
            case 16:
                message.supportedLanguage = 16;
                break;
            case "ROMANIAN":
            case 17:
                message.supportedLanguage = 17;
                break;
            case "RUSSIAN":
            case 18:
                message.supportedLanguage = 18;
                break;
            case "SLOVAK":
            case 19:
                message.supportedLanguage = 19;
                break;
            case "SPANISH":
            case 20:
                message.supportedLanguage = 20;
                break;
            case "FINNISH":
            case 21:
                message.supportedLanguage = 21;
                break;
            case "SWEDISH":
            case 22:
                message.supportedLanguage = 22;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a ReActivateDeviceApprovalRequestToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.ReActivateDeviceApprovalRequestToken
         * @static
         * @param {Tokens.ReActivateDeviceApprovalRequestToken} message ReActivateDeviceApprovalRequestToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ReActivateDeviceApprovalRequestToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.deviceId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.deviceId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.fromUsername = "";
                object.forUsername = "";
                if (options.bytes === String)
                    object.accountUid = "";
                else {
                    object.accountUid = [];
                    if (options.bytes !== Array)
                        object.accountUid = $util.newBuffer(object.accountUid);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.requestTimeStamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.requestTimeStamp = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.supportedLanguage = options.enums === String ? "ENGLISH" : 0;
            }
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.deviceId = typeof message.deviceId === "number" ? BigInt(message.deviceId) : $util.Long.fromBits(message.deviceId.low >>> 0, message.deviceId.high >>> 0, false).toBigInt();
                else if (typeof message.deviceId === "number")
                    object.deviceId = options.longs === String ? String(message.deviceId) : message.deviceId;
                else
                    object.deviceId = options.longs === String ? $util.Long.prototype.toString.call(message.deviceId) : options.longs === Number ? new $util.LongBits(message.deviceId.low >>> 0, message.deviceId.high >>> 0).toNumber() : message.deviceId;
            if (message.fromUsername != null && Object.hasOwnProperty.call(message, "fromUsername"))
                object.fromUsername = message.fromUsername;
            if (message.forUsername != null && Object.hasOwnProperty.call(message, "forUsername"))
                object.forUsername = message.forUsername;
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                object.accountUid = options.bytes === String ? $util.base64.encode(message.accountUid, 0, message.accountUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.accountUid) : message.accountUid;
            if (message.requestTimeStamp != null && Object.hasOwnProperty.call(message, "requestTimeStamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.requestTimeStamp = typeof message.requestTimeStamp === "number" ? BigInt(message.requestTimeStamp) : $util.Long.fromBits(message.requestTimeStamp.low >>> 0, message.requestTimeStamp.high >>> 0, false).toBigInt();
                else if (typeof message.requestTimeStamp === "number")
                    object.requestTimeStamp = options.longs === String ? String(message.requestTimeStamp) : message.requestTimeStamp;
                else
                    object.requestTimeStamp = options.longs === String ? $util.Long.prototype.toString.call(message.requestTimeStamp) : options.longs === Number ? new $util.LongBits(message.requestTimeStamp.low >>> 0, message.requestTimeStamp.high >>> 0).toNumber() : message.requestTimeStamp;
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                object.supportedLanguage = options.enums === String ? $root.Authentication.SupportedLanguage[message.supportedLanguage] === undefined ? message.supportedLanguage : $root.Authentication.SupportedLanguage[message.supportedLanguage] : message.supportedLanguage;
            return object;
        };

        /**
         * Converts this ReActivateDeviceApprovalRequestToken to JSON.
         * @function toJSON
         * @memberof Tokens.ReActivateDeviceApprovalRequestToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ReActivateDeviceApprovalRequestToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ReActivateDeviceApprovalRequestToken
         * @function getTypeUrl
         * @memberof Tokens.ReActivateDeviceApprovalRequestToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ReActivateDeviceApprovalRequestToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.ReActivateDeviceApprovalRequestToken";
        };

        return ReActivateDeviceApprovalRequestToken;
    })();

    Tokens.CreateUserToken = (function() {

        /**
         * Properties of a CreateUserToken.
         * @memberof Tokens
         * @interface ICreateUserToken
         * @property {number|null} [createUserRequestId] CreateUserToken createUserRequestId
         * @property {string|null} [verificationCode] CreateUserToken verificationCode
         * @property {number|null} [requestTimeStamp] CreateUserToken requestTimeStamp
         * @property {Authentication.SupportedLanguage|null} [supportedLanguage] CreateUserToken supportedLanguage
         * @property {string|null} [remoteAddress] CreateUserToken remoteAddress
         * @property {Uint8Array|null} [messageSessionUid] CreateUserToken messageSessionUid
         * @property {number|null} [deviceId] CreateUserToken deviceId
         */

        /**
         * Constructs a new CreateUserToken.
         * @memberof Tokens
         * @classdesc Represents a CreateUserToken.
         * @implements ICreateUserToken
         * @constructor
         * @param {Tokens.ICreateUserToken=} [properties] Properties to set
         */
        function CreateUserToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CreateUserToken createUserRequestId.
         * @member {number} createUserRequestId
         * @memberof Tokens.CreateUserToken
         * @instance
         */
        CreateUserToken.prototype.createUserRequestId = 0;

        /**
         * CreateUserToken verificationCode.
         * @member {string} verificationCode
         * @memberof Tokens.CreateUserToken
         * @instance
         */
        CreateUserToken.prototype.verificationCode = "";

        /**
         * CreateUserToken requestTimeStamp.
         * @member {number} requestTimeStamp
         * @memberof Tokens.CreateUserToken
         * @instance
         */
        CreateUserToken.prototype.requestTimeStamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * CreateUserToken supportedLanguage.
         * @member {Authentication.SupportedLanguage} supportedLanguage
         * @memberof Tokens.CreateUserToken
         * @instance
         */
        CreateUserToken.prototype.supportedLanguage = 0;

        /**
         * CreateUserToken remoteAddress.
         * @member {string} remoteAddress
         * @memberof Tokens.CreateUserToken
         * @instance
         */
        CreateUserToken.prototype.remoteAddress = "";

        /**
         * CreateUserToken messageSessionUid.
         * @member {Uint8Array} messageSessionUid
         * @memberof Tokens.CreateUserToken
         * @instance
         */
        CreateUserToken.prototype.messageSessionUid = $util.newBuffer([]);

        /**
         * CreateUserToken deviceId.
         * @member {number} deviceId
         * @memberof Tokens.CreateUserToken
         * @instance
         */
        CreateUserToken.prototype.deviceId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new CreateUserToken instance using the specified properties.
         * @function create
         * @memberof Tokens.CreateUserToken
         * @static
         * @param {Tokens.ICreateUserToken=} [properties] Properties to set
         * @returns {Tokens.CreateUserToken} CreateUserToken instance
         */
        CreateUserToken.create = function create(properties) {
            return new CreateUserToken(properties);
        };

        /**
         * Encodes the specified CreateUserToken message. Does not implicitly {@link Tokens.CreateUserToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.CreateUserToken
         * @static
         * @param {Tokens.ICreateUserToken} message CreateUserToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CreateUserToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.createUserRequestId != null && Object.hasOwnProperty.call(message, "createUserRequestId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.createUserRequestId);
            if (message.verificationCode != null && Object.hasOwnProperty.call(message, "verificationCode"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.verificationCode);
            if (message.requestTimeStamp != null && Object.hasOwnProperty.call(message, "requestTimeStamp"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.requestTimeStamp);
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.supportedLanguage);
            if (message.remoteAddress != null && Object.hasOwnProperty.call(message, "remoteAddress"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.remoteAddress);
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                writer.uint32(/* id 6, wireType 2 =*/50).bytes(message.messageSessionUid);
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                writer.uint32(/* id 7, wireType 0 =*/56).int64(message.deviceId);
            return writer;
        };

        /**
         * Encodes the specified CreateUserToken message, length delimited. Does not implicitly {@link Tokens.CreateUserToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.CreateUserToken
         * @static
         * @param {Tokens.ICreateUserToken} message CreateUserToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CreateUserToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CreateUserToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.CreateUserToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.CreateUserToken} CreateUserToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CreateUserToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.CreateUserToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.createUserRequestId = reader.int32();
                        break;
                    }
                case 2: {
                        message.verificationCode = reader.string();
                        break;
                    }
                case 3: {
                        message.requestTimeStamp = reader.int64();
                        break;
                    }
                case 4: {
                        message.supportedLanguage = reader.int32();
                        break;
                    }
                case 5: {
                        message.remoteAddress = reader.string();
                        break;
                    }
                case 6: {
                        message.messageSessionUid = reader.bytes();
                        break;
                    }
                case 7: {
                        message.deviceId = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CreateUserToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.CreateUserToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.CreateUserToken} CreateUserToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CreateUserToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CreateUserToken message.
         * @function verify
         * @memberof Tokens.CreateUserToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CreateUserToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.createUserRequestId != null && Object.hasOwnProperty.call(message, "createUserRequestId"))
                if (!$util.isInteger(message.createUserRequestId))
                    return "createUserRequestId: integer expected";
            if (message.verificationCode != null && Object.hasOwnProperty.call(message, "verificationCode"))
                if (!$util.isString(message.verificationCode))
                    return "verificationCode: string expected";
            if (message.requestTimeStamp != null && Object.hasOwnProperty.call(message, "requestTimeStamp"))
                if (!$util.isInteger(message.requestTimeStamp) && !(message.requestTimeStamp && $util.isInteger(message.requestTimeStamp.low) && $util.isInteger(message.requestTimeStamp.high)))
                    return "requestTimeStamp: integer|Long expected";
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                switch (message.supportedLanguage) {
                default:
                    return "supportedLanguage: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                case 11:
                case 12:
                case 13:
                case 14:
                case 15:
                case 16:
                case 17:
                case 18:
                case 19:
                case 20:
                case 21:
                case 22:
                    break;
                }
            if (message.remoteAddress != null && Object.hasOwnProperty.call(message, "remoteAddress"))
                if (!$util.isString(message.remoteAddress))
                    return "remoteAddress: string expected";
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                if (!(message.messageSessionUid && typeof message.messageSessionUid.length === "number" || $util.isString(message.messageSessionUid)))
                    return "messageSessionUid: buffer expected";
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                if (!$util.isInteger(message.deviceId) && !(message.deviceId && $util.isInteger(message.deviceId.low) && $util.isInteger(message.deviceId.high)))
                    return "deviceId: integer|Long expected";
            return null;
        };

        /**
         * Creates a CreateUserToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.CreateUserToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.CreateUserToken} CreateUserToken
         */
        CreateUserToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.CreateUserToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.CreateUserToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.CreateUserToken();
            if (object.createUserRequestId != null)
                message.createUserRequestId = object.createUserRequestId | 0;
            if (object.verificationCode != null)
                message.verificationCode = String(object.verificationCode);
            if (object.requestTimeStamp != null)
                if ($util.Long)
                    message.requestTimeStamp = $util.Long.fromValue(object.requestTimeStamp, false);
                else if (typeof object.requestTimeStamp === "string")
                    message.requestTimeStamp = parseInt(object.requestTimeStamp, 10);
                else if (typeof object.requestTimeStamp === "number")
                    message.requestTimeStamp = object.requestTimeStamp;
                else if (typeof object.requestTimeStamp === "object")
                    message.requestTimeStamp = new $util.LongBits(object.requestTimeStamp.low >>> 0, object.requestTimeStamp.high >>> 0).toNumber();
            switch (object.supportedLanguage) {
            default:
                if (typeof object.supportedLanguage === "number") {
                    message.supportedLanguage = object.supportedLanguage;
                    break;
                }
                break;
            case "ENGLISH":
            case 0:
                message.supportedLanguage = 0;
                break;
            case "ARABIC":
            case 1:
                message.supportedLanguage = 1;
                break;
            case "BRITISH":
            case 2:
                message.supportedLanguage = 2;
                break;
            case "CHINESE":
            case 3:
                message.supportedLanguage = 3;
                break;
            case "CHINESE_HONG_KONG":
            case 4:
                message.supportedLanguage = 4;
                break;
            case "CHINESE_TAIWAN":
            case 5:
                message.supportedLanguage = 5;
                break;
            case "DUTCH":
            case 6:
                message.supportedLanguage = 6;
                break;
            case "FRENCH":
            case 7:
                message.supportedLanguage = 7;
                break;
            case "GERMAN":
            case 8:
                message.supportedLanguage = 8;
                break;
            case "GREEK":
            case 9:
                message.supportedLanguage = 9;
                break;
            case "HEBREW":
            case 10:
                message.supportedLanguage = 10;
                break;
            case "ITALIAN":
            case 11:
                message.supportedLanguage = 11;
                break;
            case "JAPANESE":
            case 12:
                message.supportedLanguage = 12;
                break;
            case "KOREAN":
            case 13:
                message.supportedLanguage = 13;
                break;
            case "POLISH":
            case 14:
                message.supportedLanguage = 14;
                break;
            case "PORTUGUESE":
            case 15:
                message.supportedLanguage = 15;
                break;
            case "PORTUGUESE_BRAZIL":
            case 16:
                message.supportedLanguage = 16;
                break;
            case "ROMANIAN":
            case 17:
                message.supportedLanguage = 17;
                break;
            case "RUSSIAN":
            case 18:
                message.supportedLanguage = 18;
                break;
            case "SLOVAK":
            case 19:
                message.supportedLanguage = 19;
                break;
            case "SPANISH":
            case 20:
                message.supportedLanguage = 20;
                break;
            case "FINNISH":
            case 21:
                message.supportedLanguage = 21;
                break;
            case "SWEDISH":
            case 22:
                message.supportedLanguage = 22;
                break;
            }
            if (object.remoteAddress != null)
                message.remoteAddress = String(object.remoteAddress);
            if (object.messageSessionUid != null)
                if (typeof object.messageSessionUid === "string")
                    $util.base64.decode(object.messageSessionUid, message.messageSessionUid = $util.newBuffer($util.base64.length(object.messageSessionUid)), 0);
                else if (object.messageSessionUid.length >= 0)
                    message.messageSessionUid = object.messageSessionUid;
            if (object.deviceId != null)
                if ($util.Long)
                    message.deviceId = $util.Long.fromValue(object.deviceId, false);
                else if (typeof object.deviceId === "string")
                    message.deviceId = parseInt(object.deviceId, 10);
                else if (typeof object.deviceId === "number")
                    message.deviceId = object.deviceId;
                else if (typeof object.deviceId === "object")
                    message.deviceId = new $util.LongBits(object.deviceId.low >>> 0, object.deviceId.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a CreateUserToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.CreateUserToken
         * @static
         * @param {Tokens.CreateUserToken} message CreateUserToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CreateUserToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.createUserRequestId = 0;
                object.verificationCode = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.requestTimeStamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.requestTimeStamp = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.supportedLanguage = options.enums === String ? "ENGLISH" : 0;
                object.remoteAddress = "";
                if (options.bytes === String)
                    object.messageSessionUid = "";
                else {
                    object.messageSessionUid = [];
                    if (options.bytes !== Array)
                        object.messageSessionUid = $util.newBuffer(object.messageSessionUid);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.deviceId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.deviceId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.createUserRequestId != null && Object.hasOwnProperty.call(message, "createUserRequestId"))
                object.createUserRequestId = message.createUserRequestId;
            if (message.verificationCode != null && Object.hasOwnProperty.call(message, "verificationCode"))
                object.verificationCode = message.verificationCode;
            if (message.requestTimeStamp != null && Object.hasOwnProperty.call(message, "requestTimeStamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.requestTimeStamp = typeof message.requestTimeStamp === "number" ? BigInt(message.requestTimeStamp) : $util.Long.fromBits(message.requestTimeStamp.low >>> 0, message.requestTimeStamp.high >>> 0, false).toBigInt();
                else if (typeof message.requestTimeStamp === "number")
                    object.requestTimeStamp = options.longs === String ? String(message.requestTimeStamp) : message.requestTimeStamp;
                else
                    object.requestTimeStamp = options.longs === String ? $util.Long.prototype.toString.call(message.requestTimeStamp) : options.longs === Number ? new $util.LongBits(message.requestTimeStamp.low >>> 0, message.requestTimeStamp.high >>> 0).toNumber() : message.requestTimeStamp;
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                object.supportedLanguage = options.enums === String ? $root.Authentication.SupportedLanguage[message.supportedLanguage] === undefined ? message.supportedLanguage : $root.Authentication.SupportedLanguage[message.supportedLanguage] : message.supportedLanguage;
            if (message.remoteAddress != null && Object.hasOwnProperty.call(message, "remoteAddress"))
                object.remoteAddress = message.remoteAddress;
            if (message.messageSessionUid != null && Object.hasOwnProperty.call(message, "messageSessionUid"))
                object.messageSessionUid = options.bytes === String ? $util.base64.encode(message.messageSessionUid, 0, message.messageSessionUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.messageSessionUid) : message.messageSessionUid;
            if (message.deviceId != null && Object.hasOwnProperty.call(message, "deviceId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.deviceId = typeof message.deviceId === "number" ? BigInt(message.deviceId) : $util.Long.fromBits(message.deviceId.low >>> 0, message.deviceId.high >>> 0, false).toBigInt();
                else if (typeof message.deviceId === "number")
                    object.deviceId = options.longs === String ? String(message.deviceId) : message.deviceId;
                else
                    object.deviceId = options.longs === String ? $util.Long.prototype.toString.call(message.deviceId) : options.longs === Number ? new $util.LongBits(message.deviceId.low >>> 0, message.deviceId.high >>> 0).toNumber() : message.deviceId;
            return object;
        };

        /**
         * Converts this CreateUserToken to JSON.
         * @function toJSON
         * @memberof Tokens.CreateUserToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CreateUserToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CreateUserToken
         * @function getTypeUrl
         * @memberof Tokens.CreateUserToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CreateUserToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.CreateUserToken";
        };

        return CreateUserToken;
    })();

    Tokens.DeleteUserToken = (function() {

        /**
         * Properties of a DeleteUserToken.
         * @memberof Tokens
         * @interface IDeleteUserToken
         * @property {number|null} [userIdForDelete] DeleteUserToken userIdForDelete
         * @property {string|null} [username] DeleteUserToken username
         * @property {string|null} [reason] DeleteUserToken reason
         * @property {number|null} [requestTimeStamp] DeleteUserToken requestTimeStamp
         * @property {Authentication.SupportedLanguage|null} [supportedLanguage] DeleteUserToken supportedLanguage
         */

        /**
         * Constructs a new DeleteUserToken.
         * @memberof Tokens
         * @classdesc Represents a DeleteUserToken.
         * @implements IDeleteUserToken
         * @constructor
         * @param {Tokens.IDeleteUserToken=} [properties] Properties to set
         */
        function DeleteUserToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeleteUserToken userIdForDelete.
         * @member {number} userIdForDelete
         * @memberof Tokens.DeleteUserToken
         * @instance
         */
        DeleteUserToken.prototype.userIdForDelete = 0;

        /**
         * DeleteUserToken username.
         * @member {string} username
         * @memberof Tokens.DeleteUserToken
         * @instance
         */
        DeleteUserToken.prototype.username = "";

        /**
         * DeleteUserToken reason.
         * @member {string} reason
         * @memberof Tokens.DeleteUserToken
         * @instance
         */
        DeleteUserToken.prototype.reason = "";

        /**
         * DeleteUserToken requestTimeStamp.
         * @member {number} requestTimeStamp
         * @memberof Tokens.DeleteUserToken
         * @instance
         */
        DeleteUserToken.prototype.requestTimeStamp = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * DeleteUserToken supportedLanguage.
         * @member {Authentication.SupportedLanguage} supportedLanguage
         * @memberof Tokens.DeleteUserToken
         * @instance
         */
        DeleteUserToken.prototype.supportedLanguage = 0;

        /**
         * Creates a new DeleteUserToken instance using the specified properties.
         * @function create
         * @memberof Tokens.DeleteUserToken
         * @static
         * @param {Tokens.IDeleteUserToken=} [properties] Properties to set
         * @returns {Tokens.DeleteUserToken} DeleteUserToken instance
         */
        DeleteUserToken.create = function create(properties) {
            return new DeleteUserToken(properties);
        };

        /**
         * Encodes the specified DeleteUserToken message. Does not implicitly {@link Tokens.DeleteUserToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.DeleteUserToken
         * @static
         * @param {Tokens.IDeleteUserToken} message DeleteUserToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeleteUserToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.userIdForDelete != null && Object.hasOwnProperty.call(message, "userIdForDelete"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.userIdForDelete);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.username);
            if (message.reason != null && Object.hasOwnProperty.call(message, "reason"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.reason);
            if (message.requestTimeStamp != null && Object.hasOwnProperty.call(message, "requestTimeStamp"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.requestTimeStamp);
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.supportedLanguage);
            return writer;
        };

        /**
         * Encodes the specified DeleteUserToken message, length delimited. Does not implicitly {@link Tokens.DeleteUserToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.DeleteUserToken
         * @static
         * @param {Tokens.IDeleteUserToken} message DeleteUserToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeleteUserToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DeleteUserToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.DeleteUserToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.DeleteUserToken} DeleteUserToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeleteUserToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.DeleteUserToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.userIdForDelete = reader.int32();
                        break;
                    }
                case 2: {
                        message.username = reader.string();
                        break;
                    }
                case 3: {
                        message.reason = reader.string();
                        break;
                    }
                case 4: {
                        message.requestTimeStamp = reader.int64();
                        break;
                    }
                case 5: {
                        message.supportedLanguage = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a DeleteUserToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.DeleteUserToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.DeleteUserToken} DeleteUserToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeleteUserToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeleteUserToken message.
         * @function verify
         * @memberof Tokens.DeleteUserToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeleteUserToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.userIdForDelete != null && Object.hasOwnProperty.call(message, "userIdForDelete"))
                if (!$util.isInteger(message.userIdForDelete))
                    return "userIdForDelete: integer expected";
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                if (!$util.isString(message.username))
                    return "username: string expected";
            if (message.reason != null && Object.hasOwnProperty.call(message, "reason"))
                if (!$util.isString(message.reason))
                    return "reason: string expected";
            if (message.requestTimeStamp != null && Object.hasOwnProperty.call(message, "requestTimeStamp"))
                if (!$util.isInteger(message.requestTimeStamp) && !(message.requestTimeStamp && $util.isInteger(message.requestTimeStamp.low) && $util.isInteger(message.requestTimeStamp.high)))
                    return "requestTimeStamp: integer|Long expected";
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                switch (message.supportedLanguage) {
                default:
                    return "supportedLanguage: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                case 11:
                case 12:
                case 13:
                case 14:
                case 15:
                case 16:
                case 17:
                case 18:
                case 19:
                case 20:
                case 21:
                case 22:
                    break;
                }
            return null;
        };

        /**
         * Creates a DeleteUserToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.DeleteUserToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.DeleteUserToken} DeleteUserToken
         */
        DeleteUserToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.DeleteUserToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.DeleteUserToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.DeleteUserToken();
            if (object.userIdForDelete != null)
                message.userIdForDelete = object.userIdForDelete | 0;
            if (object.username != null)
                message.username = String(object.username);
            if (object.reason != null)
                message.reason = String(object.reason);
            if (object.requestTimeStamp != null)
                if ($util.Long)
                    message.requestTimeStamp = $util.Long.fromValue(object.requestTimeStamp, false);
                else if (typeof object.requestTimeStamp === "string")
                    message.requestTimeStamp = parseInt(object.requestTimeStamp, 10);
                else if (typeof object.requestTimeStamp === "number")
                    message.requestTimeStamp = object.requestTimeStamp;
                else if (typeof object.requestTimeStamp === "object")
                    message.requestTimeStamp = new $util.LongBits(object.requestTimeStamp.low >>> 0, object.requestTimeStamp.high >>> 0).toNumber();
            switch (object.supportedLanguage) {
            default:
                if (typeof object.supportedLanguage === "number") {
                    message.supportedLanguage = object.supportedLanguage;
                    break;
                }
                break;
            case "ENGLISH":
            case 0:
                message.supportedLanguage = 0;
                break;
            case "ARABIC":
            case 1:
                message.supportedLanguage = 1;
                break;
            case "BRITISH":
            case 2:
                message.supportedLanguage = 2;
                break;
            case "CHINESE":
            case 3:
                message.supportedLanguage = 3;
                break;
            case "CHINESE_HONG_KONG":
            case 4:
                message.supportedLanguage = 4;
                break;
            case "CHINESE_TAIWAN":
            case 5:
                message.supportedLanguage = 5;
                break;
            case "DUTCH":
            case 6:
                message.supportedLanguage = 6;
                break;
            case "FRENCH":
            case 7:
                message.supportedLanguage = 7;
                break;
            case "GERMAN":
            case 8:
                message.supportedLanguage = 8;
                break;
            case "GREEK":
            case 9:
                message.supportedLanguage = 9;
                break;
            case "HEBREW":
            case 10:
                message.supportedLanguage = 10;
                break;
            case "ITALIAN":
            case 11:
                message.supportedLanguage = 11;
                break;
            case "JAPANESE":
            case 12:
                message.supportedLanguage = 12;
                break;
            case "KOREAN":
            case 13:
                message.supportedLanguage = 13;
                break;
            case "POLISH":
            case 14:
                message.supportedLanguage = 14;
                break;
            case "PORTUGUESE":
            case 15:
                message.supportedLanguage = 15;
                break;
            case "PORTUGUESE_BRAZIL":
            case 16:
                message.supportedLanguage = 16;
                break;
            case "ROMANIAN":
            case 17:
                message.supportedLanguage = 17;
                break;
            case "RUSSIAN":
            case 18:
                message.supportedLanguage = 18;
                break;
            case "SLOVAK":
            case 19:
                message.supportedLanguage = 19;
                break;
            case "SPANISH":
            case 20:
                message.supportedLanguage = 20;
                break;
            case "FINNISH":
            case 21:
                message.supportedLanguage = 21;
                break;
            case "SWEDISH":
            case 22:
                message.supportedLanguage = 22;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a DeleteUserToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.DeleteUserToken
         * @static
         * @param {Tokens.DeleteUserToken} message DeleteUserToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeleteUserToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.userIdForDelete = 0;
                object.username = "";
                object.reason = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.requestTimeStamp = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.requestTimeStamp = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.supportedLanguage = options.enums === String ? "ENGLISH" : 0;
            }
            if (message.userIdForDelete != null && Object.hasOwnProperty.call(message, "userIdForDelete"))
                object.userIdForDelete = message.userIdForDelete;
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.reason != null && Object.hasOwnProperty.call(message, "reason"))
                object.reason = message.reason;
            if (message.requestTimeStamp != null && Object.hasOwnProperty.call(message, "requestTimeStamp"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.requestTimeStamp = typeof message.requestTimeStamp === "number" ? BigInt(message.requestTimeStamp) : $util.Long.fromBits(message.requestTimeStamp.low >>> 0, message.requestTimeStamp.high >>> 0, false).toBigInt();
                else if (typeof message.requestTimeStamp === "number")
                    object.requestTimeStamp = options.longs === String ? String(message.requestTimeStamp) : message.requestTimeStamp;
                else
                    object.requestTimeStamp = options.longs === String ? $util.Long.prototype.toString.call(message.requestTimeStamp) : options.longs === Number ? new $util.LongBits(message.requestTimeStamp.low >>> 0, message.requestTimeStamp.high >>> 0).toNumber() : message.requestTimeStamp;
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                object.supportedLanguage = options.enums === String ? $root.Authentication.SupportedLanguage[message.supportedLanguage] === undefined ? message.supportedLanguage : $root.Authentication.SupportedLanguage[message.supportedLanguage] : message.supportedLanguage;
            return object;
        };

        /**
         * Converts this DeleteUserToken to JSON.
         * @function toJSON
         * @memberof Tokens.DeleteUserToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeleteUserToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeleteUserToken
         * @function getTypeUrl
         * @memberof Tokens.DeleteUserToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeleteUserToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.DeleteUserToken";
        };

        return DeleteUserToken;
    })();

    /**
     * ProcessTokenType enum.
     * @name Tokens.ProcessTokenType
     * @enum {number}
     * @property {number} CHANGE_EMAIL=0 CHANGE_EMAIL value
     * @property {number} EMAIL_VERIFICATION=1 EMAIL_VERIFICATION value
     * @property {number} IP_ADDRESS_VERIFICATION=2 IP_ADDRESS_VERIFICATION value
     * @property {number} USERNAME_VERIFICATION=3 USERNAME_VERIFICATION value
     * @property {number} DEVICE_VERIFICATION=4 DEVICE_VERIFICATION value
     * @property {number} CREATE_ACCOUNT_VERIFICATION=5 CREATE_ACCOUNT_VERIFICATION value
     * @property {number} REDEEM_PERSONAL_LICENSE=6 REDEEM_PERSONAL_LICENSE value
     * @property {number} DELETE_ACCOUNT_VERIFICATION=7 DELETE_ACCOUNT_VERIFICATION value
     * @property {number} REACTIVATE_DEVICE_APPROVAL_REQUEST=8 REACTIVATE_DEVICE_APPROVAL_REQUEST value
     */
    Tokens.ProcessTokenType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "CHANGE_EMAIL"] = 0;
        values[valuesById[1] = "EMAIL_VERIFICATION"] = 1;
        values[valuesById[2] = "IP_ADDRESS_VERIFICATION"] = 2;
        values[valuesById[3] = "USERNAME_VERIFICATION"] = 3;
        values[valuesById[4] = "DEVICE_VERIFICATION"] = 4;
        values[valuesById[5] = "CREATE_ACCOUNT_VERIFICATION"] = 5;
        values[valuesById[6] = "REDEEM_PERSONAL_LICENSE"] = 6;
        values[valuesById[7] = "DELETE_ACCOUNT_VERIFICATION"] = 7;
        values[valuesById[8] = "REACTIVATE_DEVICE_APPROVAL_REQUEST"] = 8;
        return values;
    })();

    Tokens.ProcessToken = (function() {

        /**
         * Properties of a ProcessToken.
         * @memberof Tokens
         * @interface IProcessToken
         * @property {Tokens.ProcessTokenType|null} [processTokenType] ProcessToken processTokenType
         * @property {Uint8Array|null} [payload] ProcessToken payload
         * @property {number|null} [creation] ProcessToken creation
         * @property {Authentication.SupportedLanguage|null} [supportedLanguage] ProcessToken supportedLanguage
         * @property {string|null} [subEnvironment] ProcessToken subEnvironment
         */

        /**
         * Constructs a new ProcessToken.
         * @memberof Tokens
         * @classdesc Represents a ProcessToken.
         * @implements IProcessToken
         * @constructor
         * @param {Tokens.IProcessToken=} [properties] Properties to set
         */
        function ProcessToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ProcessToken processTokenType.
         * @member {Tokens.ProcessTokenType} processTokenType
         * @memberof Tokens.ProcessToken
         * @instance
         */
        ProcessToken.prototype.processTokenType = 0;

        /**
         * ProcessToken payload.
         * @member {Uint8Array} payload
         * @memberof Tokens.ProcessToken
         * @instance
         */
        ProcessToken.prototype.payload = $util.newBuffer([]);

        /**
         * ProcessToken creation.
         * @member {number} creation
         * @memberof Tokens.ProcessToken
         * @instance
         */
        ProcessToken.prototype.creation = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ProcessToken supportedLanguage.
         * @member {Authentication.SupportedLanguage} supportedLanguage
         * @memberof Tokens.ProcessToken
         * @instance
         */
        ProcessToken.prototype.supportedLanguage = 0;

        /**
         * ProcessToken subEnvironment.
         * @member {string} subEnvironment
         * @memberof Tokens.ProcessToken
         * @instance
         */
        ProcessToken.prototype.subEnvironment = "";

        /**
         * Creates a new ProcessToken instance using the specified properties.
         * @function create
         * @memberof Tokens.ProcessToken
         * @static
         * @param {Tokens.IProcessToken=} [properties] Properties to set
         * @returns {Tokens.ProcessToken} ProcessToken instance
         */
        ProcessToken.create = function create(properties) {
            return new ProcessToken(properties);
        };

        /**
         * Encodes the specified ProcessToken message. Does not implicitly {@link Tokens.ProcessToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.ProcessToken
         * @static
         * @param {Tokens.IProcessToken} message ProcessToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProcessToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.processTokenType != null && Object.hasOwnProperty.call(message, "processTokenType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.processTokenType);
            if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.payload);
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.creation);
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.supportedLanguage);
            if (message.subEnvironment != null && Object.hasOwnProperty.call(message, "subEnvironment"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.subEnvironment);
            return writer;
        };

        /**
         * Encodes the specified ProcessToken message, length delimited. Does not implicitly {@link Tokens.ProcessToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.ProcessToken
         * @static
         * @param {Tokens.IProcessToken} message ProcessToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProcessToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ProcessToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.ProcessToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.ProcessToken} ProcessToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProcessToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.ProcessToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.processTokenType = reader.int32();
                        break;
                    }
                case 2: {
                        message.payload = reader.bytes();
                        break;
                    }
                case 3: {
                        message.creation = reader.int64();
                        break;
                    }
                case 4: {
                        message.supportedLanguage = reader.int32();
                        break;
                    }
                case 5: {
                        message.subEnvironment = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ProcessToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.ProcessToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.ProcessToken} ProcessToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProcessToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ProcessToken message.
         * @function verify
         * @memberof Tokens.ProcessToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ProcessToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.processTokenType != null && Object.hasOwnProperty.call(message, "processTokenType"))
                switch (message.processTokenType) {
                default:
                    return "processTokenType: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                    break;
                }
            if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                if (!(message.payload && typeof message.payload.length === "number" || $util.isString(message.payload)))
                    return "payload: buffer expected";
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                if (!$util.isInteger(message.creation) && !(message.creation && $util.isInteger(message.creation.low) && $util.isInteger(message.creation.high)))
                    return "creation: integer|Long expected";
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                switch (message.supportedLanguage) {
                default:
                    return "supportedLanguage: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                case 11:
                case 12:
                case 13:
                case 14:
                case 15:
                case 16:
                case 17:
                case 18:
                case 19:
                case 20:
                case 21:
                case 22:
                    break;
                }
            if (message.subEnvironment != null && Object.hasOwnProperty.call(message, "subEnvironment"))
                if (!$util.isString(message.subEnvironment))
                    return "subEnvironment: string expected";
            return null;
        };

        /**
         * Creates a ProcessToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.ProcessToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.ProcessToken} ProcessToken
         */
        ProcessToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.ProcessToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.ProcessToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.ProcessToken();
            switch (object.processTokenType) {
            default:
                if (typeof object.processTokenType === "number") {
                    message.processTokenType = object.processTokenType;
                    break;
                }
                break;
            case "CHANGE_EMAIL":
            case 0:
                message.processTokenType = 0;
                break;
            case "EMAIL_VERIFICATION":
            case 1:
                message.processTokenType = 1;
                break;
            case "IP_ADDRESS_VERIFICATION":
            case 2:
                message.processTokenType = 2;
                break;
            case "USERNAME_VERIFICATION":
            case 3:
                message.processTokenType = 3;
                break;
            case "DEVICE_VERIFICATION":
            case 4:
                message.processTokenType = 4;
                break;
            case "CREATE_ACCOUNT_VERIFICATION":
            case 5:
                message.processTokenType = 5;
                break;
            case "REDEEM_PERSONAL_LICENSE":
            case 6:
                message.processTokenType = 6;
                break;
            case "DELETE_ACCOUNT_VERIFICATION":
            case 7:
                message.processTokenType = 7;
                break;
            case "REACTIVATE_DEVICE_APPROVAL_REQUEST":
            case 8:
                message.processTokenType = 8;
                break;
            }
            if (object.payload != null)
                if (typeof object.payload === "string")
                    $util.base64.decode(object.payload, message.payload = $util.newBuffer($util.base64.length(object.payload)), 0);
                else if (object.payload.length >= 0)
                    message.payload = object.payload;
            if (object.creation != null)
                if ($util.Long)
                    message.creation = $util.Long.fromValue(object.creation, false);
                else if (typeof object.creation === "string")
                    message.creation = parseInt(object.creation, 10);
                else if (typeof object.creation === "number")
                    message.creation = object.creation;
                else if (typeof object.creation === "object")
                    message.creation = new $util.LongBits(object.creation.low >>> 0, object.creation.high >>> 0).toNumber();
            switch (object.supportedLanguage) {
            default:
                if (typeof object.supportedLanguage === "number") {
                    message.supportedLanguage = object.supportedLanguage;
                    break;
                }
                break;
            case "ENGLISH":
            case 0:
                message.supportedLanguage = 0;
                break;
            case "ARABIC":
            case 1:
                message.supportedLanguage = 1;
                break;
            case "BRITISH":
            case 2:
                message.supportedLanguage = 2;
                break;
            case "CHINESE":
            case 3:
                message.supportedLanguage = 3;
                break;
            case "CHINESE_HONG_KONG":
            case 4:
                message.supportedLanguage = 4;
                break;
            case "CHINESE_TAIWAN":
            case 5:
                message.supportedLanguage = 5;
                break;
            case "DUTCH":
            case 6:
                message.supportedLanguage = 6;
                break;
            case "FRENCH":
            case 7:
                message.supportedLanguage = 7;
                break;
            case "GERMAN":
            case 8:
                message.supportedLanguage = 8;
                break;
            case "GREEK":
            case 9:
                message.supportedLanguage = 9;
                break;
            case "HEBREW":
            case 10:
                message.supportedLanguage = 10;
                break;
            case "ITALIAN":
            case 11:
                message.supportedLanguage = 11;
                break;
            case "JAPANESE":
            case 12:
                message.supportedLanguage = 12;
                break;
            case "KOREAN":
            case 13:
                message.supportedLanguage = 13;
                break;
            case "POLISH":
            case 14:
                message.supportedLanguage = 14;
                break;
            case "PORTUGUESE":
            case 15:
                message.supportedLanguage = 15;
                break;
            case "PORTUGUESE_BRAZIL":
            case 16:
                message.supportedLanguage = 16;
                break;
            case "ROMANIAN":
            case 17:
                message.supportedLanguage = 17;
                break;
            case "RUSSIAN":
            case 18:
                message.supportedLanguage = 18;
                break;
            case "SLOVAK":
            case 19:
                message.supportedLanguage = 19;
                break;
            case "SPANISH":
            case 20:
                message.supportedLanguage = 20;
                break;
            case "FINNISH":
            case 21:
                message.supportedLanguage = 21;
                break;
            case "SWEDISH":
            case 22:
                message.supportedLanguage = 22;
                break;
            }
            if (object.subEnvironment != null)
                message.subEnvironment = String(object.subEnvironment);
            return message;
        };

        /**
         * Creates a plain object from a ProcessToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.ProcessToken
         * @static
         * @param {Tokens.ProcessToken} message ProcessToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ProcessToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.processTokenType = options.enums === String ? "CHANGE_EMAIL" : 0;
                if (options.bytes === String)
                    object.payload = "";
                else {
                    object.payload = [];
                    if (options.bytes !== Array)
                        object.payload = $util.newBuffer(object.payload);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.creation = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.creation = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.supportedLanguage = options.enums === String ? "ENGLISH" : 0;
                object.subEnvironment = "";
            }
            if (message.processTokenType != null && Object.hasOwnProperty.call(message, "processTokenType"))
                object.processTokenType = options.enums === String ? $root.Tokens.ProcessTokenType[message.processTokenType] === undefined ? message.processTokenType : $root.Tokens.ProcessTokenType[message.processTokenType] : message.processTokenType;
            if (message.payload != null && Object.hasOwnProperty.call(message, "payload"))
                object.payload = options.bytes === String ? $util.base64.encode(message.payload, 0, message.payload.length) : options.bytes === Array ? Array.prototype.slice.call(message.payload) : message.payload;
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.creation = typeof message.creation === "number" ? BigInt(message.creation) : $util.Long.fromBits(message.creation.low >>> 0, message.creation.high >>> 0, false).toBigInt();
                else if (typeof message.creation === "number")
                    object.creation = options.longs === String ? String(message.creation) : message.creation;
                else
                    object.creation = options.longs === String ? $util.Long.prototype.toString.call(message.creation) : options.longs === Number ? new $util.LongBits(message.creation.low >>> 0, message.creation.high >>> 0).toNumber() : message.creation;
            if (message.supportedLanguage != null && Object.hasOwnProperty.call(message, "supportedLanguage"))
                object.supportedLanguage = options.enums === String ? $root.Authentication.SupportedLanguage[message.supportedLanguage] === undefined ? message.supportedLanguage : $root.Authentication.SupportedLanguage[message.supportedLanguage] : message.supportedLanguage;
            if (message.subEnvironment != null && Object.hasOwnProperty.call(message, "subEnvironment"))
                object.subEnvironment = message.subEnvironment;
            return object;
        };

        /**
         * Converts this ProcessToken to JSON.
         * @function toJSON
         * @memberof Tokens.ProcessToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ProcessToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ProcessToken
         * @function getTypeUrl
         * @memberof Tokens.ProcessToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ProcessToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.ProcessToken";
        };

        return ProcessToken;
    })();

    /**
     * PageTokenType enum.
     * @name Tokens.PageTokenType
     * @enum {number}
     * @property {number} FULL=0 FULL value
     * @property {number} PARTIAL=1 PARTIAL value
     */
    Tokens.PageTokenType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "FULL"] = 0;
        values[valuesById[1] = "PARTIAL"] = 1;
        return values;
    })();

    Tokens.EnterpriseDataContinuationToken = (function() {

        /**
         * Properties of an EnterpriseDataContinuationToken.
         * @memberof Tokens
         * @interface IEnterpriseDataContinuationToken
         * @property {Tokens.PageTokenType|null} [startType] EnterpriseDataContinuationToken startType
         * @property {number|null} [processedUpToTransactionId] EnterpriseDataContinuationToken processedUpToTransactionId
         * @property {Tokens.IPosition|null} [position] EnterpriseDataContinuationToken position
         * @property {string|null} [lastProcessedTime] EnterpriseDataContinuationToken lastProcessedTime
         */

        /**
         * Constructs a new EnterpriseDataContinuationToken.
         * @memberof Tokens
         * @classdesc Represents an EnterpriseDataContinuationToken.
         * @implements IEnterpriseDataContinuationToken
         * @constructor
         * @param {Tokens.IEnterpriseDataContinuationToken=} [properties] Properties to set
         */
        function EnterpriseDataContinuationToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EnterpriseDataContinuationToken startType.
         * @member {Tokens.PageTokenType} startType
         * @memberof Tokens.EnterpriseDataContinuationToken
         * @instance
         */
        EnterpriseDataContinuationToken.prototype.startType = 0;

        /**
         * EnterpriseDataContinuationToken processedUpToTransactionId.
         * @member {number} processedUpToTransactionId
         * @memberof Tokens.EnterpriseDataContinuationToken
         * @instance
         */
        EnterpriseDataContinuationToken.prototype.processedUpToTransactionId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EnterpriseDataContinuationToken position.
         * @member {Tokens.IPosition|null|undefined} position
         * @memberof Tokens.EnterpriseDataContinuationToken
         * @instance
         */
        EnterpriseDataContinuationToken.prototype.position = null;

        /**
         * EnterpriseDataContinuationToken lastProcessedTime.
         * @member {string} lastProcessedTime
         * @memberof Tokens.EnterpriseDataContinuationToken
         * @instance
         */
        EnterpriseDataContinuationToken.prototype.lastProcessedTime = "";

        /**
         * Creates a new EnterpriseDataContinuationToken instance using the specified properties.
         * @function create
         * @memberof Tokens.EnterpriseDataContinuationToken
         * @static
         * @param {Tokens.IEnterpriseDataContinuationToken=} [properties] Properties to set
         * @returns {Tokens.EnterpriseDataContinuationToken} EnterpriseDataContinuationToken instance
         */
        EnterpriseDataContinuationToken.create = function create(properties) {
            return new EnterpriseDataContinuationToken(properties);
        };

        /**
         * Encodes the specified EnterpriseDataContinuationToken message. Does not implicitly {@link Tokens.EnterpriseDataContinuationToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.EnterpriseDataContinuationToken
         * @static
         * @param {Tokens.IEnterpriseDataContinuationToken} message EnterpriseDataContinuationToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterpriseDataContinuationToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.startType != null && Object.hasOwnProperty.call(message, "startType"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.startType);
            if (message.processedUpToTransactionId != null && Object.hasOwnProperty.call(message, "processedUpToTransactionId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.processedUpToTransactionId);
            if (message.position != null && Object.hasOwnProperty.call(message, "position"))
                $root.Tokens.Position.encode(message.position, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.lastProcessedTime != null && Object.hasOwnProperty.call(message, "lastProcessedTime"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.lastProcessedTime);
            return writer;
        };

        /**
         * Encodes the specified EnterpriseDataContinuationToken message, length delimited. Does not implicitly {@link Tokens.EnterpriseDataContinuationToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.EnterpriseDataContinuationToken
         * @static
         * @param {Tokens.IEnterpriseDataContinuationToken} message EnterpriseDataContinuationToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterpriseDataContinuationToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EnterpriseDataContinuationToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.EnterpriseDataContinuationToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.EnterpriseDataContinuationToken} EnterpriseDataContinuationToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterpriseDataContinuationToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.EnterpriseDataContinuationToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.startType = reader.int32();
                        break;
                    }
                case 2: {
                        message.processedUpToTransactionId = reader.int64();
                        break;
                    }
                case 3: {
                        message.position = $root.Tokens.Position.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 4: {
                        message.lastProcessedTime = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an EnterpriseDataContinuationToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.EnterpriseDataContinuationToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.EnterpriseDataContinuationToken} EnterpriseDataContinuationToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterpriseDataContinuationToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EnterpriseDataContinuationToken message.
         * @function verify
         * @memberof Tokens.EnterpriseDataContinuationToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EnterpriseDataContinuationToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.startType != null && Object.hasOwnProperty.call(message, "startType"))
                switch (message.startType) {
                default:
                    return "startType: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.processedUpToTransactionId != null && Object.hasOwnProperty.call(message, "processedUpToTransactionId"))
                if (!$util.isInteger(message.processedUpToTransactionId) && !(message.processedUpToTransactionId && $util.isInteger(message.processedUpToTransactionId.low) && $util.isInteger(message.processedUpToTransactionId.high)))
                    return "processedUpToTransactionId: integer|Long expected";
            if (message.position != null && Object.hasOwnProperty.call(message, "position")) {
                let error = $root.Tokens.Position.verify(message.position, long + 1);
                if (error)
                    return "position." + error;
            }
            if (message.lastProcessedTime != null && Object.hasOwnProperty.call(message, "lastProcessedTime"))
                if (!$util.isString(message.lastProcessedTime))
                    return "lastProcessedTime: string expected";
            return null;
        };

        /**
         * Creates an EnterpriseDataContinuationToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.EnterpriseDataContinuationToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.EnterpriseDataContinuationToken} EnterpriseDataContinuationToken
         */
        EnterpriseDataContinuationToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.EnterpriseDataContinuationToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.EnterpriseDataContinuationToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.EnterpriseDataContinuationToken();
            switch (object.startType) {
            default:
                if (typeof object.startType === "number") {
                    message.startType = object.startType;
                    break;
                }
                break;
            case "FULL":
            case 0:
                message.startType = 0;
                break;
            case "PARTIAL":
            case 1:
                message.startType = 1;
                break;
            }
            if (object.processedUpToTransactionId != null)
                if ($util.Long)
                    message.processedUpToTransactionId = $util.Long.fromValue(object.processedUpToTransactionId, false);
                else if (typeof object.processedUpToTransactionId === "string")
                    message.processedUpToTransactionId = parseInt(object.processedUpToTransactionId, 10);
                else if (typeof object.processedUpToTransactionId === "number")
                    message.processedUpToTransactionId = object.processedUpToTransactionId;
                else if (typeof object.processedUpToTransactionId === "object")
                    message.processedUpToTransactionId = new $util.LongBits(object.processedUpToTransactionId.low >>> 0, object.processedUpToTransactionId.high >>> 0).toNumber();
            if (object.position != null) {
                if (!$util.isObject(object.position))
                    throw TypeError(".Tokens.EnterpriseDataContinuationToken.position: object expected");
                message.position = $root.Tokens.Position.fromObject(object.position, long + 1);
            }
            if (object.lastProcessedTime != null)
                message.lastProcessedTime = String(object.lastProcessedTime);
            return message;
        };

        /**
         * Creates a plain object from an EnterpriseDataContinuationToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.EnterpriseDataContinuationToken
         * @static
         * @param {Tokens.EnterpriseDataContinuationToken} message EnterpriseDataContinuationToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EnterpriseDataContinuationToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.startType = options.enums === String ? "FULL" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.processedUpToTransactionId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.processedUpToTransactionId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.position = null;
                object.lastProcessedTime = "";
            }
            if (message.startType != null && Object.hasOwnProperty.call(message, "startType"))
                object.startType = options.enums === String ? $root.Tokens.PageTokenType[message.startType] === undefined ? message.startType : $root.Tokens.PageTokenType[message.startType] : message.startType;
            if (message.processedUpToTransactionId != null && Object.hasOwnProperty.call(message, "processedUpToTransactionId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.processedUpToTransactionId = typeof message.processedUpToTransactionId === "number" ? BigInt(message.processedUpToTransactionId) : $util.Long.fromBits(message.processedUpToTransactionId.low >>> 0, message.processedUpToTransactionId.high >>> 0, false).toBigInt();
                else if (typeof message.processedUpToTransactionId === "number")
                    object.processedUpToTransactionId = options.longs === String ? String(message.processedUpToTransactionId) : message.processedUpToTransactionId;
                else
                    object.processedUpToTransactionId = options.longs === String ? $util.Long.prototype.toString.call(message.processedUpToTransactionId) : options.longs === Number ? new $util.LongBits(message.processedUpToTransactionId.low >>> 0, message.processedUpToTransactionId.high >>> 0).toNumber() : message.processedUpToTransactionId;
            if (message.position != null && Object.hasOwnProperty.call(message, "position"))
                object.position = $root.Tokens.Position.toObject(message.position, options, q + 1);
            if (message.lastProcessedTime != null && Object.hasOwnProperty.call(message, "lastProcessedTime"))
                object.lastProcessedTime = message.lastProcessedTime;
            return object;
        };

        /**
         * Converts this EnterpriseDataContinuationToken to JSON.
         * @function toJSON
         * @memberof Tokens.EnterpriseDataContinuationToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EnterpriseDataContinuationToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EnterpriseDataContinuationToken
         * @function getTypeUrl
         * @memberof Tokens.EnterpriseDataContinuationToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EnterpriseDataContinuationToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.EnterpriseDataContinuationToken";
        };

        return EnterpriseDataContinuationToken;
    })();

    /**
     * VaultDataEntity enum.
     * @name Tokens.VaultDataEntity
     * @enum {number}
     * @property {number} UNKNOWN=0 UNKNOWN value
     * @property {number} USER_FOLDER=1 USER_FOLDER value
     * @property {number} USER_FOLDER_REMOVED=2 USER_FOLDER_REMOVED value
     * @property {number} TEAM1=3 TEAM1 value
     * @property {number} TEAM2=4 TEAM2 value
     * @property {number} TEAM3=5 TEAM3 value
     * @property {number} TEAM4=6 TEAM4 value
     * @property {number} TEAM_REMOVED=7 TEAM_REMOVED value
     * @property {number} SHARED_FOLDER=8 SHARED_FOLDER value
     * @property {number} SHARED_FOLDER_REMOVED=9 SHARED_FOLDER_REMOVED value
     * @property {number} SHARED_FOLDER_USER=10 SHARED_FOLDER_USER value
     * @property {number} SHARED_FOLDER_USER_REMOVED=11 SHARED_FOLDER_USER_REMOVED value
     * @property {number} SHARED_FOLDER_TEAM=12 SHARED_FOLDER_TEAM value
     * @property {number} SHARED_FOLDER_TEAM_REMOVED=13 SHARED_FOLDER_TEAM_REMOVED value
     * @property {number} SHARED_FOLDER_RECORD=14 SHARED_FOLDER_RECORD value
     * @property {number} SHARED_FOLDER_RECORD_REMOVED=15 SHARED_FOLDER_RECORD_REMOVED value
     * @property {number} SHARED_FOLDER_FOLDER=16 SHARED_FOLDER_FOLDER value
     * @property {number} SHARED_FOLDER_FOLDER_RECORD1=17 SHARED_FOLDER_FOLDER_RECORD1 value
     * @property {number} SHARED_FOLDER_FOLDER_REMOVED=18 SHARED_FOLDER_FOLDER_REMOVED value
     * @property {number} SHARED_FOLDER_FOLDER_RECORD_REMOVED=19 SHARED_FOLDER_FOLDER_RECORD_REMOVED value
     * @property {number} USER_FOLDER_SHARED_FOLDER1=20 USER_FOLDER_SHARED_FOLDER1 value
     * @property {number} USER_FOLDER_SHARED_FOLDER2=21 USER_FOLDER_SHARED_FOLDER2 value
     * @property {number} USER_FOLDER_SHARED_FOLDER_REMOVED1=22 USER_FOLDER_SHARED_FOLDER_REMOVED1 value
     * @property {number} USER_FOLDER_SHARED_FOLDER_REMOVED2=23 USER_FOLDER_SHARED_FOLDER_REMOVED2 value
     * @property {number} RECORD_META_DATA1=24 RECORD_META_DATA1 value
     * @property {number} RECORD_META_DATA2=25 RECORD_META_DATA2 value
     * @property {number} RECORD_LINK=26 RECORD_LINK value
     * @property {number} RECORD_LINK_REMOVED=27 RECORD_LINK_REMOVED value
     * @property {number} RECORD1=28 RECORD1 value
     * @property {number} RECORD2=29 RECORD2 value
     * @property {number} RECORD_REMOVED=30 RECORD_REMOVED value
     * @property {number} NOT_SHARED_DATA=31 NOT_SHARED_DATA value
     * @property {number} SHARING_CHANGE=32 SHARING_CHANGE value
     * @property {number} USER_FOLDER_RECORD1=33 USER_FOLDER_RECORD1 value
     * @property {number} USER_FOLDER_RECORD2_DEPRECATED=34 USER_FOLDER_RECORD2_DEPRECATED value
     * @property {number} USER_FOLDER_RECORD_REMOVED=35 USER_FOLDER_RECORD_REMOVED value
     * @property {number} BREACH_WATCH_RECORD1=36 BREACH_WATCH_RECORD1 value
     * @property {number} BREACH_WATCH_RECORD2=37 BREACH_WATCH_RECORD2 value
     * @property {number} BREACH_WATCH_RECORD3=38 BREACH_WATCH_RECORD3 value
     * @property {number} BREACH_WATCH_SECURITY=39 BREACH_WATCH_SECURITY value
     * @property {number} USER_AUTH=40 USER_AUTH value
     * @property {number} SHARE_INVITATION=41 SHARE_INVITATION value
     * @property {number} REUSED_PASSWORD=42 REUSED_PASSWORD value
     * @property {number} PROFILE=43 PROFILE value
     * @property {number} PROFILE_PIC=44 PROFILE_PIC value
     * @property {number} PENDING_TEAM_MEMBER=45 PENDING_TEAM_MEMBER value
     * @property {number} KSM_APP_SHARE=46 KSM_APP_SHARE value
     * @property {number} KSM_APP_CLIENT=47 KSM_APP_CLIENT value
     * @property {number} RECORD_ROTATION=48 RECORD_ROTATION value
     * @property {number} USER1=49 USER1 value
     * @property {number} USER2=50 USER2 value
     * @property {number} USER3=51 USER3 value
     * @property {number} USER4=52 USER4 value
     * @property {number} USER5=53 USER5 value
     * @property {number} USER_REMOVED=54 USER_REMOVED value
     * @property {number} NOTIFICATION_SYNC_DATA=55 NOTIFICATION_SYNC_DATA value
     */
    Tokens.VaultDataEntity = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "UNKNOWN"] = 0;
        values[valuesById[1] = "USER_FOLDER"] = 1;
        values[valuesById[2] = "USER_FOLDER_REMOVED"] = 2;
        values[valuesById[3] = "TEAM1"] = 3;
        values[valuesById[4] = "TEAM2"] = 4;
        values[valuesById[5] = "TEAM3"] = 5;
        values[valuesById[6] = "TEAM4"] = 6;
        values[valuesById[7] = "TEAM_REMOVED"] = 7;
        values[valuesById[8] = "SHARED_FOLDER"] = 8;
        values[valuesById[9] = "SHARED_FOLDER_REMOVED"] = 9;
        values[valuesById[10] = "SHARED_FOLDER_USER"] = 10;
        values[valuesById[11] = "SHARED_FOLDER_USER_REMOVED"] = 11;
        values[valuesById[12] = "SHARED_FOLDER_TEAM"] = 12;
        values[valuesById[13] = "SHARED_FOLDER_TEAM_REMOVED"] = 13;
        values[valuesById[14] = "SHARED_FOLDER_RECORD"] = 14;
        values[valuesById[15] = "SHARED_FOLDER_RECORD_REMOVED"] = 15;
        values[valuesById[16] = "SHARED_FOLDER_FOLDER"] = 16;
        values[valuesById[17] = "SHARED_FOLDER_FOLDER_RECORD1"] = 17;
        values[valuesById[18] = "SHARED_FOLDER_FOLDER_REMOVED"] = 18;
        values[valuesById[19] = "SHARED_FOLDER_FOLDER_RECORD_REMOVED"] = 19;
        values[valuesById[20] = "USER_FOLDER_SHARED_FOLDER1"] = 20;
        values[valuesById[21] = "USER_FOLDER_SHARED_FOLDER2"] = 21;
        values[valuesById[22] = "USER_FOLDER_SHARED_FOLDER_REMOVED1"] = 22;
        values[valuesById[23] = "USER_FOLDER_SHARED_FOLDER_REMOVED2"] = 23;
        values[valuesById[24] = "RECORD_META_DATA1"] = 24;
        values[valuesById[25] = "RECORD_META_DATA2"] = 25;
        values[valuesById[26] = "RECORD_LINK"] = 26;
        values[valuesById[27] = "RECORD_LINK_REMOVED"] = 27;
        values[valuesById[28] = "RECORD1"] = 28;
        values[valuesById[29] = "RECORD2"] = 29;
        values[valuesById[30] = "RECORD_REMOVED"] = 30;
        values[valuesById[31] = "NOT_SHARED_DATA"] = 31;
        values[valuesById[32] = "SHARING_CHANGE"] = 32;
        values[valuesById[33] = "USER_FOLDER_RECORD1"] = 33;
        values[valuesById[34] = "USER_FOLDER_RECORD2_DEPRECATED"] = 34;
        values[valuesById[35] = "USER_FOLDER_RECORD_REMOVED"] = 35;
        values[valuesById[36] = "BREACH_WATCH_RECORD1"] = 36;
        values[valuesById[37] = "BREACH_WATCH_RECORD2"] = 37;
        values[valuesById[38] = "BREACH_WATCH_RECORD3"] = 38;
        values[valuesById[39] = "BREACH_WATCH_SECURITY"] = 39;
        values[valuesById[40] = "USER_AUTH"] = 40;
        values[valuesById[41] = "SHARE_INVITATION"] = 41;
        values[valuesById[42] = "REUSED_PASSWORD"] = 42;
        values[valuesById[43] = "PROFILE"] = 43;
        values[valuesById[44] = "PROFILE_PIC"] = 44;
        values[valuesById[45] = "PENDING_TEAM_MEMBER"] = 45;
        values[valuesById[46] = "KSM_APP_SHARE"] = 46;
        values[valuesById[47] = "KSM_APP_CLIENT"] = 47;
        values[valuesById[48] = "RECORD_ROTATION"] = 48;
        values[valuesById[49] = "USER1"] = 49;
        values[valuesById[50] = "USER2"] = 50;
        values[valuesById[51] = "USER3"] = 51;
        values[valuesById[52] = "USER4"] = 52;
        values[valuesById[53] = "USER5"] = 53;
        values[valuesById[54] = "USER_REMOVED"] = 54;
        values[valuesById[55] = "NOTIFICATION_SYNC_DATA"] = 55;
        return values;
    })();

    Tokens.Position = (function() {

        /**
         * Properties of a Position.
         * @memberof Tokens
         * @interface IPosition
         * @property {Enterprise.EnterpriseDataEntity|null} [entity] Position entity
         * @property {number|null} [nodeId] Position nodeId
         * @property {Uint8Array|null} [location] Position location
         */

        /**
         * Constructs a new Position.
         * @memberof Tokens
         * @classdesc Represents a Position.
         * @implements IPosition
         * @constructor
         * @param {Tokens.IPosition=} [properties] Properties to set
         */
        function Position(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Position entity.
         * @member {Enterprise.EnterpriseDataEntity} entity
         * @memberof Tokens.Position
         * @instance
         */
        Position.prototype.entity = 0;

        /**
         * Position nodeId.
         * @member {number} nodeId
         * @memberof Tokens.Position
         * @instance
         */
        Position.prototype.nodeId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Position location.
         * @member {Uint8Array} location
         * @memberof Tokens.Position
         * @instance
         */
        Position.prototype.location = $util.newBuffer([]);

        /**
         * Creates a new Position instance using the specified properties.
         * @function create
         * @memberof Tokens.Position
         * @static
         * @param {Tokens.IPosition=} [properties] Properties to set
         * @returns {Tokens.Position} Position instance
         */
        Position.create = function create(properties) {
            return new Position(properties);
        };

        /**
         * Encodes the specified Position message. Does not implicitly {@link Tokens.Position.verify|verify} messages.
         * @function encode
         * @memberof Tokens.Position
         * @static
         * @param {Tokens.IPosition} message Position message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Position.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.entity != null && Object.hasOwnProperty.call(message, "entity"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.entity);
            if (message.nodeId != null && Object.hasOwnProperty.call(message, "nodeId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.nodeId);
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.location);
            return writer;
        };

        /**
         * Encodes the specified Position message, length delimited. Does not implicitly {@link Tokens.Position.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.Position
         * @static
         * @param {Tokens.IPosition} message Position message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Position.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Position message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.Position
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.Position} Position
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Position.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.Position();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.entity = reader.int32();
                        break;
                    }
                case 2: {
                        message.nodeId = reader.int64();
                        break;
                    }
                case 3: {
                        message.location = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Position message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.Position
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.Position} Position
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Position.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Position message.
         * @function verify
         * @memberof Tokens.Position
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Position.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.entity != null && Object.hasOwnProperty.call(message, "entity"))
                switch (message.entity) {
                default:
                    return "entity: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                case 11:
                case 12:
                case 13:
                case 14:
                case 15:
                case 16:
                case 17:
                case 18:
                case 19:
                case 20:
                case 21:
                case 22:
                case 23:
                    break;
                }
            if (message.nodeId != null && Object.hasOwnProperty.call(message, "nodeId"))
                if (!$util.isInteger(message.nodeId) && !(message.nodeId && $util.isInteger(message.nodeId.low) && $util.isInteger(message.nodeId.high)))
                    return "nodeId: integer|Long expected";
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                if (!(message.location && typeof message.location.length === "number" || $util.isString(message.location)))
                    return "location: buffer expected";
            return null;
        };

        /**
         * Creates a Position message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.Position
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.Position} Position
         */
        Position.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.Position)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.Position: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.Position();
            switch (object.entity) {
            default:
                if (typeof object.entity === "number") {
                    message.entity = object.entity;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                message.entity = 0;
                break;
            case "NODES":
            case 1:
                message.entity = 1;
                break;
            case "ROLES":
            case 2:
                message.entity = 2;
                break;
            case "USERS":
            case 3:
                message.entity = 3;
                break;
            case "TEAMS":
            case 4:
                message.entity = 4;
                break;
            case "TEAM_USERS":
            case 5:
                message.entity = 5;
                break;
            case "ROLE_USERS":
            case 6:
                message.entity = 6;
                break;
            case "ROLE_PRIVILEGES":
            case 7:
                message.entity = 7;
                break;
            case "ROLE_ENFORCEMENTS":
            case 8:
                message.entity = 8;
                break;
            case "ROLE_TEAMS":
            case 9:
                message.entity = 9;
                break;
            case "LICENSES":
            case 10:
                message.entity = 10;
                break;
            case "MANAGED_NODES":
            case 11:
                message.entity = 11;
                break;
            case "MANAGED_COMPANIES":
            case 12:
                message.entity = 12;
                break;
            case "BRIDGES":
            case 13:
                message.entity = 13;
                break;
            case "SCIMS":
            case 14:
                message.entity = 14;
                break;
            case "EMAIL_PROVISION":
            case 15:
                message.entity = 15;
                break;
            case "QUEUED_TEAMS":
            case 16:
                message.entity = 16;
                break;
            case "QUEUED_TEAM_USERS":
            case 17:
                message.entity = 17;
                break;
            case "SSO_SERVICES":
            case 18:
                message.entity = 18;
                break;
            case "REPORT_FILTER_USERS":
            case 19:
                message.entity = 19;
                break;
            case "DEVICES_REQUEST_FOR_ADMIN_APPROVAL":
            case 20:
                message.entity = 20;
                break;
            case "USER_ALIASES":
            case 21:
                message.entity = 21;
                break;
            case "COMPLIANCE_REPORT_CRITERIA_AND_FILTER":
            case 22:
                message.entity = 22;
                break;
            case "COMPLIANCE_REPORTS":
            case 23:
                message.entity = 23;
                break;
            }
            if (object.nodeId != null)
                if ($util.Long)
                    message.nodeId = $util.Long.fromValue(object.nodeId, false);
                else if (typeof object.nodeId === "string")
                    message.nodeId = parseInt(object.nodeId, 10);
                else if (typeof object.nodeId === "number")
                    message.nodeId = object.nodeId;
                else if (typeof object.nodeId === "object")
                    message.nodeId = new $util.LongBits(object.nodeId.low >>> 0, object.nodeId.high >>> 0).toNumber();
            if (object.location != null)
                if (typeof object.location === "string")
                    $util.base64.decode(object.location, message.location = $util.newBuffer($util.base64.length(object.location)), 0);
                else if (object.location.length >= 0)
                    message.location = object.location;
            return message;
        };

        /**
         * Creates a plain object from a Position message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.Position
         * @static
         * @param {Tokens.Position} message Position
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Position.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.entity = options.enums === String ? "UNKNOWN" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.nodeId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.nodeId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.location = "";
                else {
                    object.location = [];
                    if (options.bytes !== Array)
                        object.location = $util.newBuffer(object.location);
                }
            }
            if (message.entity != null && Object.hasOwnProperty.call(message, "entity"))
                object.entity = options.enums === String ? $root.Enterprise.EnterpriseDataEntity[message.entity] === undefined ? message.entity : $root.Enterprise.EnterpriseDataEntity[message.entity] : message.entity;
            if (message.nodeId != null && Object.hasOwnProperty.call(message, "nodeId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.nodeId = typeof message.nodeId === "number" ? BigInt(message.nodeId) : $util.Long.fromBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0, false).toBigInt();
                else if (typeof message.nodeId === "number")
                    object.nodeId = options.longs === String ? String(message.nodeId) : message.nodeId;
                else
                    object.nodeId = options.longs === String ? $util.Long.prototype.toString.call(message.nodeId) : options.longs === Number ? new $util.LongBits(message.nodeId.low >>> 0, message.nodeId.high >>> 0).toNumber() : message.nodeId;
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                object.location = options.bytes === String ? $util.base64.encode(message.location, 0, message.location.length) : options.bytes === Array ? Array.prototype.slice.call(message.location) : message.location;
            return object;
        };

        /**
         * Converts this Position to JSON.
         * @function toJSON
         * @memberof Tokens.Position
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Position.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Position
         * @function getTypeUrl
         * @memberof Tokens.Position
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Position.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.Position";
        };

        return Position;
    })();

    Tokens.LocationL = (function() {

        /**
         * Properties of a LocationL.
         * @memberof Tokens
         * @interface ILocationL
         * @property {number|null} [id] LocationL id
         */

        /**
         * Constructs a new LocationL.
         * @memberof Tokens
         * @classdesc Represents a LocationL.
         * @implements ILocationL
         * @constructor
         * @param {Tokens.ILocationL=} [properties] Properties to set
         */
        function LocationL(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LocationL id.
         * @member {number} id
         * @memberof Tokens.LocationL
         * @instance
         */
        LocationL.prototype.id = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new LocationL instance using the specified properties.
         * @function create
         * @memberof Tokens.LocationL
         * @static
         * @param {Tokens.ILocationL=} [properties] Properties to set
         * @returns {Tokens.LocationL} LocationL instance
         */
        LocationL.create = function create(properties) {
            return new LocationL(properties);
        };

        /**
         * Encodes the specified LocationL message. Does not implicitly {@link Tokens.LocationL.verify|verify} messages.
         * @function encode
         * @memberof Tokens.LocationL
         * @static
         * @param {Tokens.ILocationL} message LocationL message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LocationL.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.id);
            return writer;
        };

        /**
         * Encodes the specified LocationL message, length delimited. Does not implicitly {@link Tokens.LocationL.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.LocationL
         * @static
         * @param {Tokens.ILocationL} message LocationL message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LocationL.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LocationL message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.LocationL
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.LocationL} LocationL
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LocationL.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.LocationL();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LocationL message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.LocationL
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.LocationL} LocationL
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LocationL.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LocationL message.
         * @function verify
         * @memberof Tokens.LocationL
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LocationL.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                if (!$util.isInteger(message.id) && !(message.id && $util.isInteger(message.id.low) && $util.isInteger(message.id.high)))
                    return "id: integer|Long expected";
            return null;
        };

        /**
         * Creates a LocationL message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.LocationL
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.LocationL} LocationL
         */
        LocationL.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.LocationL)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.LocationL: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.LocationL();
            if (object.id != null)
                if ($util.Long)
                    message.id = $util.Long.fromValue(object.id, false);
                else if (typeof object.id === "string")
                    message.id = parseInt(object.id, 10);
                else if (typeof object.id === "number")
                    message.id = object.id;
                else if (typeof object.id === "object")
                    message.id = new $util.LongBits(object.id.low >>> 0, object.id.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a LocationL message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.LocationL
         * @static
         * @param {Tokens.LocationL} message LocationL
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LocationL.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.id = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.id = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.id = typeof message.id === "number" ? BigInt(message.id) : $util.Long.fromBits(message.id.low >>> 0, message.id.high >>> 0, false).toBigInt();
                else if (typeof message.id === "number")
                    object.id = options.longs === String ? String(message.id) : message.id;
                else
                    object.id = options.longs === String ? $util.Long.prototype.toString.call(message.id) : options.longs === Number ? new $util.LongBits(message.id.low >>> 0, message.id.high >>> 0).toNumber() : message.id;
            return object;
        };

        /**
         * Converts this LocationL to JSON.
         * @function toJSON
         * @memberof Tokens.LocationL
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LocationL.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LocationL
         * @function getTypeUrl
         * @memberof Tokens.LocationL
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LocationL.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.LocationL";
        };

        return LocationL;
    })();

    Tokens.LocationLL = (function() {

        /**
         * Properties of a LocationLL.
         * @memberof Tokens
         * @interface ILocationLL
         * @property {number|null} [id1] LocationLL id1
         * @property {number|null} [id2] LocationLL id2
         */

        /**
         * Constructs a new LocationLL.
         * @memberof Tokens
         * @classdesc Represents a LocationLL.
         * @implements ILocationLL
         * @constructor
         * @param {Tokens.ILocationLL=} [properties] Properties to set
         */
        function LocationLL(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LocationLL id1.
         * @member {number} id1
         * @memberof Tokens.LocationLL
         * @instance
         */
        LocationLL.prototype.id1 = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LocationLL id2.
         * @member {number} id2
         * @memberof Tokens.LocationLL
         * @instance
         */
        LocationLL.prototype.id2 = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new LocationLL instance using the specified properties.
         * @function create
         * @memberof Tokens.LocationLL
         * @static
         * @param {Tokens.ILocationLL=} [properties] Properties to set
         * @returns {Tokens.LocationLL} LocationLL instance
         */
        LocationLL.create = function create(properties) {
            return new LocationLL(properties);
        };

        /**
         * Encodes the specified LocationLL message. Does not implicitly {@link Tokens.LocationLL.verify|verify} messages.
         * @function encode
         * @memberof Tokens.LocationLL
         * @static
         * @param {Tokens.ILocationLL} message LocationLL message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LocationLL.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.id1 != null && Object.hasOwnProperty.call(message, "id1"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.id1);
            if (message.id2 != null && Object.hasOwnProperty.call(message, "id2"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.id2);
            return writer;
        };

        /**
         * Encodes the specified LocationLL message, length delimited. Does not implicitly {@link Tokens.LocationLL.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.LocationLL
         * @static
         * @param {Tokens.ILocationLL} message LocationLL message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LocationLL.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LocationLL message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.LocationLL
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.LocationLL} LocationLL
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LocationLL.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.LocationLL();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id1 = reader.int64();
                        break;
                    }
                case 2: {
                        message.id2 = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LocationLL message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.LocationLL
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.LocationLL} LocationLL
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LocationLL.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LocationLL message.
         * @function verify
         * @memberof Tokens.LocationLL
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LocationLL.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.id1 != null && Object.hasOwnProperty.call(message, "id1"))
                if (!$util.isInteger(message.id1) && !(message.id1 && $util.isInteger(message.id1.low) && $util.isInteger(message.id1.high)))
                    return "id1: integer|Long expected";
            if (message.id2 != null && Object.hasOwnProperty.call(message, "id2"))
                if (!$util.isInteger(message.id2) && !(message.id2 && $util.isInteger(message.id2.low) && $util.isInteger(message.id2.high)))
                    return "id2: integer|Long expected";
            return null;
        };

        /**
         * Creates a LocationLL message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.LocationLL
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.LocationLL} LocationLL
         */
        LocationLL.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.LocationLL)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.LocationLL: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.LocationLL();
            if (object.id1 != null)
                if ($util.Long)
                    message.id1 = $util.Long.fromValue(object.id1, false);
                else if (typeof object.id1 === "string")
                    message.id1 = parseInt(object.id1, 10);
                else if (typeof object.id1 === "number")
                    message.id1 = object.id1;
                else if (typeof object.id1 === "object")
                    message.id1 = new $util.LongBits(object.id1.low >>> 0, object.id1.high >>> 0).toNumber();
            if (object.id2 != null)
                if ($util.Long)
                    message.id2 = $util.Long.fromValue(object.id2, false);
                else if (typeof object.id2 === "string")
                    message.id2 = parseInt(object.id2, 10);
                else if (typeof object.id2 === "number")
                    message.id2 = object.id2;
                else if (typeof object.id2 === "object")
                    message.id2 = new $util.LongBits(object.id2.low >>> 0, object.id2.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a LocationLL message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.LocationLL
         * @static
         * @param {Tokens.LocationLL} message LocationLL
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LocationLL.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.id1 = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.id1 = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.id2 = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.id2 = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.id1 != null && Object.hasOwnProperty.call(message, "id1"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.id1 = typeof message.id1 === "number" ? BigInt(message.id1) : $util.Long.fromBits(message.id1.low >>> 0, message.id1.high >>> 0, false).toBigInt();
                else if (typeof message.id1 === "number")
                    object.id1 = options.longs === String ? String(message.id1) : message.id1;
                else
                    object.id1 = options.longs === String ? $util.Long.prototype.toString.call(message.id1) : options.longs === Number ? new $util.LongBits(message.id1.low >>> 0, message.id1.high >>> 0).toNumber() : message.id1;
            if (message.id2 != null && Object.hasOwnProperty.call(message, "id2"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.id2 = typeof message.id2 === "number" ? BigInt(message.id2) : $util.Long.fromBits(message.id2.low >>> 0, message.id2.high >>> 0, false).toBigInt();
                else if (typeof message.id2 === "number")
                    object.id2 = options.longs === String ? String(message.id2) : message.id2;
                else
                    object.id2 = options.longs === String ? $util.Long.prototype.toString.call(message.id2) : options.longs === Number ? new $util.LongBits(message.id2.low >>> 0, message.id2.high >>> 0).toNumber() : message.id2;
            return object;
        };

        /**
         * Converts this LocationLL to JSON.
         * @function toJSON
         * @memberof Tokens.LocationLL
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LocationLL.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LocationLL
         * @function getTypeUrl
         * @memberof Tokens.LocationLL
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LocationLL.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.LocationLL";
        };

        return LocationLL;
    })();

    Tokens.LocationLI = (function() {

        /**
         * Properties of a LocationLI.
         * @memberof Tokens
         * @interface ILocationLI
         * @property {number|null} [id1] LocationLI id1
         * @property {number|null} [id2] LocationLI id2
         */

        /**
         * Constructs a new LocationLI.
         * @memberof Tokens
         * @classdesc Represents a LocationLI.
         * @implements ILocationLI
         * @constructor
         * @param {Tokens.ILocationLI=} [properties] Properties to set
         */
        function LocationLI(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LocationLI id1.
         * @member {number} id1
         * @memberof Tokens.LocationLI
         * @instance
         */
        LocationLI.prototype.id1 = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LocationLI id2.
         * @member {number} id2
         * @memberof Tokens.LocationLI
         * @instance
         */
        LocationLI.prototype.id2 = 0;

        /**
         * Creates a new LocationLI instance using the specified properties.
         * @function create
         * @memberof Tokens.LocationLI
         * @static
         * @param {Tokens.ILocationLI=} [properties] Properties to set
         * @returns {Tokens.LocationLI} LocationLI instance
         */
        LocationLI.create = function create(properties) {
            return new LocationLI(properties);
        };

        /**
         * Encodes the specified LocationLI message. Does not implicitly {@link Tokens.LocationLI.verify|verify} messages.
         * @function encode
         * @memberof Tokens.LocationLI
         * @static
         * @param {Tokens.ILocationLI} message LocationLI message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LocationLI.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.id1 != null && Object.hasOwnProperty.call(message, "id1"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.id1);
            if (message.id2 != null && Object.hasOwnProperty.call(message, "id2"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.id2);
            return writer;
        };

        /**
         * Encodes the specified LocationLI message, length delimited. Does not implicitly {@link Tokens.LocationLI.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.LocationLI
         * @static
         * @param {Tokens.ILocationLI} message LocationLI message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LocationLI.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LocationLI message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.LocationLI
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.LocationLI} LocationLI
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LocationLI.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.LocationLI();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id1 = reader.int64();
                        break;
                    }
                case 2: {
                        message.id2 = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LocationLI message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.LocationLI
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.LocationLI} LocationLI
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LocationLI.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LocationLI message.
         * @function verify
         * @memberof Tokens.LocationLI
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LocationLI.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.id1 != null && Object.hasOwnProperty.call(message, "id1"))
                if (!$util.isInteger(message.id1) && !(message.id1 && $util.isInteger(message.id1.low) && $util.isInteger(message.id1.high)))
                    return "id1: integer|Long expected";
            if (message.id2 != null && Object.hasOwnProperty.call(message, "id2"))
                if (!$util.isInteger(message.id2))
                    return "id2: integer expected";
            return null;
        };

        /**
         * Creates a LocationLI message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.LocationLI
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.LocationLI} LocationLI
         */
        LocationLI.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.LocationLI)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.LocationLI: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.LocationLI();
            if (object.id1 != null)
                if ($util.Long)
                    message.id1 = $util.Long.fromValue(object.id1, false);
                else if (typeof object.id1 === "string")
                    message.id1 = parseInt(object.id1, 10);
                else if (typeof object.id1 === "number")
                    message.id1 = object.id1;
                else if (typeof object.id1 === "object")
                    message.id1 = new $util.LongBits(object.id1.low >>> 0, object.id1.high >>> 0).toNumber();
            if (object.id2 != null)
                message.id2 = object.id2 | 0;
            return message;
        };

        /**
         * Creates a plain object from a LocationLI message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.LocationLI
         * @static
         * @param {Tokens.LocationLI} message LocationLI
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LocationLI.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.id1 = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.id1 = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.id2 = 0;
            }
            if (message.id1 != null && Object.hasOwnProperty.call(message, "id1"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.id1 = typeof message.id1 === "number" ? BigInt(message.id1) : $util.Long.fromBits(message.id1.low >>> 0, message.id1.high >>> 0, false).toBigInt();
                else if (typeof message.id1 === "number")
                    object.id1 = options.longs === String ? String(message.id1) : message.id1;
                else
                    object.id1 = options.longs === String ? $util.Long.prototype.toString.call(message.id1) : options.longs === Number ? new $util.LongBits(message.id1.low >>> 0, message.id1.high >>> 0).toNumber() : message.id1;
            if (message.id2 != null && Object.hasOwnProperty.call(message, "id2"))
                object.id2 = message.id2;
            return object;
        };

        /**
         * Converts this LocationLI to JSON.
         * @function toJSON
         * @memberof Tokens.LocationLI
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LocationLI.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LocationLI
         * @function getTypeUrl
         * @memberof Tokens.LocationLI
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LocationLI.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.LocationLI";
        };

        return LocationLI;
    })();

    Tokens.LocationII = (function() {

        /**
         * Properties of a LocationII.
         * @memberof Tokens
         * @interface ILocationII
         * @property {number|null} [id1] LocationII id1
         * @property {number|null} [id2] LocationII id2
         */

        /**
         * Constructs a new LocationII.
         * @memberof Tokens
         * @classdesc Represents a LocationII.
         * @implements ILocationII
         * @constructor
         * @param {Tokens.ILocationII=} [properties] Properties to set
         */
        function LocationII(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LocationII id1.
         * @member {number} id1
         * @memberof Tokens.LocationII
         * @instance
         */
        LocationII.prototype.id1 = 0;

        /**
         * LocationII id2.
         * @member {number} id2
         * @memberof Tokens.LocationII
         * @instance
         */
        LocationII.prototype.id2 = 0;

        /**
         * Creates a new LocationII instance using the specified properties.
         * @function create
         * @memberof Tokens.LocationII
         * @static
         * @param {Tokens.ILocationII=} [properties] Properties to set
         * @returns {Tokens.LocationII} LocationII instance
         */
        LocationII.create = function create(properties) {
            return new LocationII(properties);
        };

        /**
         * Encodes the specified LocationII message. Does not implicitly {@link Tokens.LocationII.verify|verify} messages.
         * @function encode
         * @memberof Tokens.LocationII
         * @static
         * @param {Tokens.ILocationII} message LocationII message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LocationII.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.id1 != null && Object.hasOwnProperty.call(message, "id1"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id1);
            if (message.id2 != null && Object.hasOwnProperty.call(message, "id2"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.id2);
            return writer;
        };

        /**
         * Encodes the specified LocationII message, length delimited. Does not implicitly {@link Tokens.LocationII.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.LocationII
         * @static
         * @param {Tokens.ILocationII} message LocationII message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LocationII.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LocationII message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.LocationII
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.LocationII} LocationII
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LocationII.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.LocationII();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id1 = reader.int32();
                        break;
                    }
                case 2: {
                        message.id2 = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LocationII message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.LocationII
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.LocationII} LocationII
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LocationII.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LocationII message.
         * @function verify
         * @memberof Tokens.LocationII
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LocationII.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.id1 != null && Object.hasOwnProperty.call(message, "id1"))
                if (!$util.isInteger(message.id1))
                    return "id1: integer expected";
            if (message.id2 != null && Object.hasOwnProperty.call(message, "id2"))
                if (!$util.isInteger(message.id2))
                    return "id2: integer expected";
            return null;
        };

        /**
         * Creates a LocationII message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.LocationII
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.LocationII} LocationII
         */
        LocationII.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.LocationII)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.LocationII: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.LocationII();
            if (object.id1 != null)
                message.id1 = object.id1 | 0;
            if (object.id2 != null)
                message.id2 = object.id2 | 0;
            return message;
        };

        /**
         * Creates a plain object from a LocationII message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.LocationII
         * @static
         * @param {Tokens.LocationII} message LocationII
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LocationII.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.id1 = 0;
                object.id2 = 0;
            }
            if (message.id1 != null && Object.hasOwnProperty.call(message, "id1"))
                object.id1 = message.id1;
            if (message.id2 != null && Object.hasOwnProperty.call(message, "id2"))
                object.id2 = message.id2;
            return object;
        };

        /**
         * Converts this LocationII to JSON.
         * @function toJSON
         * @memberof Tokens.LocationII
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LocationII.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LocationII
         * @function getTypeUrl
         * @memberof Tokens.LocationII
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LocationII.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.LocationII";
        };

        return LocationII;
    })();

    Tokens.LocationB = (function() {

        /**
         * Properties of a LocationB.
         * @memberof Tokens
         * @interface ILocationB
         * @property {Uint8Array|null} [uid] LocationB uid
         */

        /**
         * Constructs a new LocationB.
         * @memberof Tokens
         * @classdesc Represents a LocationB.
         * @implements ILocationB
         * @constructor
         * @param {Tokens.ILocationB=} [properties] Properties to set
         */
        function LocationB(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LocationB uid.
         * @member {Uint8Array} uid
         * @memberof Tokens.LocationB
         * @instance
         */
        LocationB.prototype.uid = $util.newBuffer([]);

        /**
         * Creates a new LocationB instance using the specified properties.
         * @function create
         * @memberof Tokens.LocationB
         * @static
         * @param {Tokens.ILocationB=} [properties] Properties to set
         * @returns {Tokens.LocationB} LocationB instance
         */
        LocationB.create = function create(properties) {
            return new LocationB(properties);
        };

        /**
         * Encodes the specified LocationB message. Does not implicitly {@link Tokens.LocationB.verify|verify} messages.
         * @function encode
         * @memberof Tokens.LocationB
         * @static
         * @param {Tokens.ILocationB} message LocationB message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LocationB.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.uid);
            return writer;
        };

        /**
         * Encodes the specified LocationB message, length delimited. Does not implicitly {@link Tokens.LocationB.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.LocationB
         * @static
         * @param {Tokens.ILocationB} message LocationB message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LocationB.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LocationB message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.LocationB
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.LocationB} LocationB
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LocationB.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.LocationB();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LocationB message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.LocationB
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.LocationB} LocationB
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LocationB.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LocationB message.
         * @function verify
         * @memberof Tokens.LocationB
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LocationB.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                if (!(message.uid && typeof message.uid.length === "number" || $util.isString(message.uid)))
                    return "uid: buffer expected";
            return null;
        };

        /**
         * Creates a LocationB message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.LocationB
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.LocationB} LocationB
         */
        LocationB.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.LocationB)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.LocationB: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.LocationB();
            if (object.uid != null)
                if (typeof object.uid === "string")
                    $util.base64.decode(object.uid, message.uid = $util.newBuffer($util.base64.length(object.uid)), 0);
                else if (object.uid.length >= 0)
                    message.uid = object.uid;
            return message;
        };

        /**
         * Creates a plain object from a LocationB message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.LocationB
         * @static
         * @param {Tokens.LocationB} message LocationB
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LocationB.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                if (options.bytes === String)
                    object.uid = "";
                else {
                    object.uid = [];
                    if (options.bytes !== Array)
                        object.uid = $util.newBuffer(object.uid);
                }
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                object.uid = options.bytes === String ? $util.base64.encode(message.uid, 0, message.uid.length) : options.bytes === Array ? Array.prototype.slice.call(message.uid) : message.uid;
            return object;
        };

        /**
         * Converts this LocationB to JSON.
         * @function toJSON
         * @memberof Tokens.LocationB
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LocationB.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LocationB
         * @function getTypeUrl
         * @memberof Tokens.LocationB
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LocationB.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.LocationB";
        };

        return LocationB;
    })();

    Tokens.LocationLB = (function() {

        /**
         * Properties of a LocationLB.
         * @memberof Tokens
         * @interface ILocationLB
         * @property {number|null} [id] LocationLB id
         * @property {Uint8Array|null} [uid] LocationLB uid
         */

        /**
         * Constructs a new LocationLB.
         * @memberof Tokens
         * @classdesc Represents a LocationLB.
         * @implements ILocationLB
         * @constructor
         * @param {Tokens.ILocationLB=} [properties] Properties to set
         */
        function LocationLB(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LocationLB id.
         * @member {number} id
         * @memberof Tokens.LocationLB
         * @instance
         */
        LocationLB.prototype.id = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LocationLB uid.
         * @member {Uint8Array} uid
         * @memberof Tokens.LocationLB
         * @instance
         */
        LocationLB.prototype.uid = $util.newBuffer([]);

        /**
         * Creates a new LocationLB instance using the specified properties.
         * @function create
         * @memberof Tokens.LocationLB
         * @static
         * @param {Tokens.ILocationLB=} [properties] Properties to set
         * @returns {Tokens.LocationLB} LocationLB instance
         */
        LocationLB.create = function create(properties) {
            return new LocationLB(properties);
        };

        /**
         * Encodes the specified LocationLB message. Does not implicitly {@link Tokens.LocationLB.verify|verify} messages.
         * @function encode
         * @memberof Tokens.LocationLB
         * @static
         * @param {Tokens.ILocationLB} message LocationLB message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LocationLB.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.id);
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.uid);
            return writer;
        };

        /**
         * Encodes the specified LocationLB message, length delimited. Does not implicitly {@link Tokens.LocationLB.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.LocationLB
         * @static
         * @param {Tokens.ILocationLB} message LocationLB message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LocationLB.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LocationLB message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.LocationLB
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.LocationLB} LocationLB
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LocationLB.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.LocationLB();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id = reader.int64();
                        break;
                    }
                case 2: {
                        message.uid = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LocationLB message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.LocationLB
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.LocationLB} LocationLB
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LocationLB.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LocationLB message.
         * @function verify
         * @memberof Tokens.LocationLB
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LocationLB.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                if (!$util.isInteger(message.id) && !(message.id && $util.isInteger(message.id.low) && $util.isInteger(message.id.high)))
                    return "id: integer|Long expected";
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                if (!(message.uid && typeof message.uid.length === "number" || $util.isString(message.uid)))
                    return "uid: buffer expected";
            return null;
        };

        /**
         * Creates a LocationLB message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.LocationLB
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.LocationLB} LocationLB
         */
        LocationLB.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.LocationLB)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.LocationLB: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.LocationLB();
            if (object.id != null)
                if ($util.Long)
                    message.id = $util.Long.fromValue(object.id, false);
                else if (typeof object.id === "string")
                    message.id = parseInt(object.id, 10);
                else if (typeof object.id === "number")
                    message.id = object.id;
                else if (typeof object.id === "object")
                    message.id = new $util.LongBits(object.id.low >>> 0, object.id.high >>> 0).toNumber();
            if (object.uid != null)
                if (typeof object.uid === "string")
                    $util.base64.decode(object.uid, message.uid = $util.newBuffer($util.base64.length(object.uid)), 0);
                else if (object.uid.length >= 0)
                    message.uid = object.uid;
            return message;
        };

        /**
         * Creates a plain object from a LocationLB message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.LocationLB
         * @static
         * @param {Tokens.LocationLB} message LocationLB
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LocationLB.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.id = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.id = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.uid = "";
                else {
                    object.uid = [];
                    if (options.bytes !== Array)
                        object.uid = $util.newBuffer(object.uid);
                }
            }
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.id = typeof message.id === "number" ? BigInt(message.id) : $util.Long.fromBits(message.id.low >>> 0, message.id.high >>> 0, false).toBigInt();
                else if (typeof message.id === "number")
                    object.id = options.longs === String ? String(message.id) : message.id;
                else
                    object.id = options.longs === String ? $util.Long.prototype.toString.call(message.id) : options.longs === Number ? new $util.LongBits(message.id.low >>> 0, message.id.high >>> 0).toNumber() : message.id;
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                object.uid = options.bytes === String ? $util.base64.encode(message.uid, 0, message.uid.length) : options.bytes === Array ? Array.prototype.slice.call(message.uid) : message.uid;
            return object;
        };

        /**
         * Converts this LocationLB to JSON.
         * @function toJSON
         * @memberof Tokens.LocationLB
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LocationLB.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LocationLB
         * @function getTypeUrl
         * @memberof Tokens.LocationLB
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LocationLB.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.LocationLB";
        };

        return LocationLB;
    })();

    Tokens.LocationBI = (function() {

        /**
         * Properties of a LocationBI.
         * @memberof Tokens
         * @interface ILocationBI
         * @property {Uint8Array|null} [uid] LocationBI uid
         * @property {number|null} [id] LocationBI id
         */

        /**
         * Constructs a new LocationBI.
         * @memberof Tokens
         * @classdesc Represents a LocationBI.
         * @implements ILocationBI
         * @constructor
         * @param {Tokens.ILocationBI=} [properties] Properties to set
         */
        function LocationBI(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LocationBI uid.
         * @member {Uint8Array} uid
         * @memberof Tokens.LocationBI
         * @instance
         */
        LocationBI.prototype.uid = $util.newBuffer([]);

        /**
         * LocationBI id.
         * @member {number} id
         * @memberof Tokens.LocationBI
         * @instance
         */
        LocationBI.prototype.id = 0;

        /**
         * Creates a new LocationBI instance using the specified properties.
         * @function create
         * @memberof Tokens.LocationBI
         * @static
         * @param {Tokens.ILocationBI=} [properties] Properties to set
         * @returns {Tokens.LocationBI} LocationBI instance
         */
        LocationBI.create = function create(properties) {
            return new LocationBI(properties);
        };

        /**
         * Encodes the specified LocationBI message. Does not implicitly {@link Tokens.LocationBI.verify|verify} messages.
         * @function encode
         * @memberof Tokens.LocationBI
         * @static
         * @param {Tokens.ILocationBI} message LocationBI message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LocationBI.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.uid);
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.id);
            return writer;
        };

        /**
         * Encodes the specified LocationBI message, length delimited. Does not implicitly {@link Tokens.LocationBI.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.LocationBI
         * @static
         * @param {Tokens.ILocationBI} message LocationBI message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LocationBI.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LocationBI message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.LocationBI
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.LocationBI} LocationBI
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LocationBI.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.LocationBI();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.uid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.id = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LocationBI message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.LocationBI
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.LocationBI} LocationBI
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LocationBI.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LocationBI message.
         * @function verify
         * @memberof Tokens.LocationBI
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LocationBI.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                if (!(message.uid && typeof message.uid.length === "number" || $util.isString(message.uid)))
                    return "uid: buffer expected";
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                if (!$util.isInteger(message.id))
                    return "id: integer expected";
            return null;
        };

        /**
         * Creates a LocationBI message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.LocationBI
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.LocationBI} LocationBI
         */
        LocationBI.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.LocationBI)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.LocationBI: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.LocationBI();
            if (object.uid != null)
                if (typeof object.uid === "string")
                    $util.base64.decode(object.uid, message.uid = $util.newBuffer($util.base64.length(object.uid)), 0);
                else if (object.uid.length >= 0)
                    message.uid = object.uid;
            if (object.id != null)
                message.id = object.id | 0;
            return message;
        };

        /**
         * Creates a plain object from a LocationBI message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.LocationBI
         * @static
         * @param {Tokens.LocationBI} message LocationBI
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LocationBI.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.uid = "";
                else {
                    object.uid = [];
                    if (options.bytes !== Array)
                        object.uid = $util.newBuffer(object.uid);
                }
                object.id = 0;
            }
            if (message.uid != null && Object.hasOwnProperty.call(message, "uid"))
                object.uid = options.bytes === String ? $util.base64.encode(message.uid, 0, message.uid.length) : options.bytes === Array ? Array.prototype.slice.call(message.uid) : message.uid;
            if (message.id != null && Object.hasOwnProperty.call(message, "id"))
                object.id = message.id;
            return object;
        };

        /**
         * Converts this LocationBI to JSON.
         * @function toJSON
         * @memberof Tokens.LocationBI
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LocationBI.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LocationBI
         * @function getTypeUrl
         * @memberof Tokens.LocationBI
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LocationBI.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.LocationBI";
        };

        return LocationBI;
    })();

    Tokens.LocationLS = (function() {

        /**
         * Properties of a LocationLS.
         * @memberof Tokens
         * @interface ILocationLS
         * @property {number|null} [id1] LocationLS id1
         * @property {string|null} [str] LocationLS str
         */

        /**
         * Constructs a new LocationLS.
         * @memberof Tokens
         * @classdesc Represents a LocationLS.
         * @implements ILocationLS
         * @constructor
         * @param {Tokens.ILocationLS=} [properties] Properties to set
         */
        function LocationLS(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LocationLS id1.
         * @member {number} id1
         * @memberof Tokens.LocationLS
         * @instance
         */
        LocationLS.prototype.id1 = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * LocationLS str.
         * @member {string} str
         * @memberof Tokens.LocationLS
         * @instance
         */
        LocationLS.prototype.str = "";

        /**
         * Creates a new LocationLS instance using the specified properties.
         * @function create
         * @memberof Tokens.LocationLS
         * @static
         * @param {Tokens.ILocationLS=} [properties] Properties to set
         * @returns {Tokens.LocationLS} LocationLS instance
         */
        LocationLS.create = function create(properties) {
            return new LocationLS(properties);
        };

        /**
         * Encodes the specified LocationLS message. Does not implicitly {@link Tokens.LocationLS.verify|verify} messages.
         * @function encode
         * @memberof Tokens.LocationLS
         * @static
         * @param {Tokens.ILocationLS} message LocationLS message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LocationLS.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.id1 != null && Object.hasOwnProperty.call(message, "id1"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.id1);
            if (message.str != null && Object.hasOwnProperty.call(message, "str"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.str);
            return writer;
        };

        /**
         * Encodes the specified LocationLS message, length delimited. Does not implicitly {@link Tokens.LocationLS.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.LocationLS
         * @static
         * @param {Tokens.ILocationLS} message LocationLS message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LocationLS.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LocationLS message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.LocationLS
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.LocationLS} LocationLS
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LocationLS.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.LocationLS();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.id1 = reader.int64();
                        break;
                    }
                case 2: {
                        message.str = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LocationLS message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.LocationLS
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.LocationLS} LocationLS
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LocationLS.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LocationLS message.
         * @function verify
         * @memberof Tokens.LocationLS
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LocationLS.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.id1 != null && Object.hasOwnProperty.call(message, "id1"))
                if (!$util.isInteger(message.id1) && !(message.id1 && $util.isInteger(message.id1.low) && $util.isInteger(message.id1.high)))
                    return "id1: integer|Long expected";
            if (message.str != null && Object.hasOwnProperty.call(message, "str"))
                if (!$util.isString(message.str))
                    return "str: string expected";
            return null;
        };

        /**
         * Creates a LocationLS message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.LocationLS
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.LocationLS} LocationLS
         */
        LocationLS.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.LocationLS)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.LocationLS: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.LocationLS();
            if (object.id1 != null)
                if ($util.Long)
                    message.id1 = $util.Long.fromValue(object.id1, false);
                else if (typeof object.id1 === "string")
                    message.id1 = parseInt(object.id1, 10);
                else if (typeof object.id1 === "number")
                    message.id1 = object.id1;
                else if (typeof object.id1 === "object")
                    message.id1 = new $util.LongBits(object.id1.low >>> 0, object.id1.high >>> 0).toNumber();
            if (object.str != null)
                message.str = String(object.str);
            return message;
        };

        /**
         * Creates a plain object from a LocationLS message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.LocationLS
         * @static
         * @param {Tokens.LocationLS} message LocationLS
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LocationLS.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.id1 = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.id1 = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.str = "";
            }
            if (message.id1 != null && Object.hasOwnProperty.call(message, "id1"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.id1 = typeof message.id1 === "number" ? BigInt(message.id1) : $util.Long.fromBits(message.id1.low >>> 0, message.id1.high >>> 0, false).toBigInt();
                else if (typeof message.id1 === "number")
                    object.id1 = options.longs === String ? String(message.id1) : message.id1;
                else
                    object.id1 = options.longs === String ? $util.Long.prototype.toString.call(message.id1) : options.longs === Number ? new $util.LongBits(message.id1.low >>> 0, message.id1.high >>> 0).toNumber() : message.id1;
            if (message.str != null && Object.hasOwnProperty.call(message, "str"))
                object.str = message.str;
            return object;
        };

        /**
         * Converts this LocationLS to JSON.
         * @function toJSON
         * @memberof Tokens.LocationLS
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LocationLS.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LocationLS
         * @function getTypeUrl
         * @memberof Tokens.LocationLS
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LocationLS.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.LocationLS";
        };

        return LocationLS;
    })();

    Tokens.EnterpriseDataRoleTeamsPageToken = (function() {

        /**
         * Properties of an EnterpriseDataRoleTeamsPageToken.
         * @memberof Tokens
         * @interface IEnterpriseDataRoleTeamsPageToken
         * @property {number|null} [lastNodeId] EnterpriseDataRoleTeamsPageToken lastNodeId
         * @property {number|null} [lastRoleId] EnterpriseDataRoleTeamsPageToken lastRoleId
         * @property {Uint8Array|null} [lastTeamUid] EnterpriseDataRoleTeamsPageToken lastTeamUid
         */

        /**
         * Constructs a new EnterpriseDataRoleTeamsPageToken.
         * @memberof Tokens
         * @classdesc Represents an EnterpriseDataRoleTeamsPageToken.
         * @implements IEnterpriseDataRoleTeamsPageToken
         * @constructor
         * @param {Tokens.IEnterpriseDataRoleTeamsPageToken=} [properties] Properties to set
         */
        function EnterpriseDataRoleTeamsPageToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EnterpriseDataRoleTeamsPageToken lastNodeId.
         * @member {number} lastNodeId
         * @memberof Tokens.EnterpriseDataRoleTeamsPageToken
         * @instance
         */
        EnterpriseDataRoleTeamsPageToken.prototype.lastNodeId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EnterpriseDataRoleTeamsPageToken lastRoleId.
         * @member {number} lastRoleId
         * @memberof Tokens.EnterpriseDataRoleTeamsPageToken
         * @instance
         */
        EnterpriseDataRoleTeamsPageToken.prototype.lastRoleId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EnterpriseDataRoleTeamsPageToken lastTeamUid.
         * @member {Uint8Array} lastTeamUid
         * @memberof Tokens.EnterpriseDataRoleTeamsPageToken
         * @instance
         */
        EnterpriseDataRoleTeamsPageToken.prototype.lastTeamUid = $util.newBuffer([]);

        /**
         * Creates a new EnterpriseDataRoleTeamsPageToken instance using the specified properties.
         * @function create
         * @memberof Tokens.EnterpriseDataRoleTeamsPageToken
         * @static
         * @param {Tokens.IEnterpriseDataRoleTeamsPageToken=} [properties] Properties to set
         * @returns {Tokens.EnterpriseDataRoleTeamsPageToken} EnterpriseDataRoleTeamsPageToken instance
         */
        EnterpriseDataRoleTeamsPageToken.create = function create(properties) {
            return new EnterpriseDataRoleTeamsPageToken(properties);
        };

        /**
         * Encodes the specified EnterpriseDataRoleTeamsPageToken message. Does not implicitly {@link Tokens.EnterpriseDataRoleTeamsPageToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.EnterpriseDataRoleTeamsPageToken
         * @static
         * @param {Tokens.IEnterpriseDataRoleTeamsPageToken} message EnterpriseDataRoleTeamsPageToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterpriseDataRoleTeamsPageToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.lastNodeId != null && Object.hasOwnProperty.call(message, "lastNodeId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.lastNodeId);
            if (message.lastRoleId != null && Object.hasOwnProperty.call(message, "lastRoleId"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.lastRoleId);
            if (message.lastTeamUid != null && Object.hasOwnProperty.call(message, "lastTeamUid"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.lastTeamUid);
            return writer;
        };

        /**
         * Encodes the specified EnterpriseDataRoleTeamsPageToken message, length delimited. Does not implicitly {@link Tokens.EnterpriseDataRoleTeamsPageToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.EnterpriseDataRoleTeamsPageToken
         * @static
         * @param {Tokens.IEnterpriseDataRoleTeamsPageToken} message EnterpriseDataRoleTeamsPageToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterpriseDataRoleTeamsPageToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EnterpriseDataRoleTeamsPageToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.EnterpriseDataRoleTeamsPageToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.EnterpriseDataRoleTeamsPageToken} EnterpriseDataRoleTeamsPageToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterpriseDataRoleTeamsPageToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.EnterpriseDataRoleTeamsPageToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.lastNodeId = reader.int64();
                        break;
                    }
                case 2: {
                        message.lastRoleId = reader.int64();
                        break;
                    }
                case 3: {
                        message.lastTeamUid = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an EnterpriseDataRoleTeamsPageToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.EnterpriseDataRoleTeamsPageToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.EnterpriseDataRoleTeamsPageToken} EnterpriseDataRoleTeamsPageToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterpriseDataRoleTeamsPageToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EnterpriseDataRoleTeamsPageToken message.
         * @function verify
         * @memberof Tokens.EnterpriseDataRoleTeamsPageToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EnterpriseDataRoleTeamsPageToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.lastNodeId != null && Object.hasOwnProperty.call(message, "lastNodeId"))
                if (!$util.isInteger(message.lastNodeId) && !(message.lastNodeId && $util.isInteger(message.lastNodeId.low) && $util.isInteger(message.lastNodeId.high)))
                    return "lastNodeId: integer|Long expected";
            if (message.lastRoleId != null && Object.hasOwnProperty.call(message, "lastRoleId"))
                if (!$util.isInteger(message.lastRoleId) && !(message.lastRoleId && $util.isInteger(message.lastRoleId.low) && $util.isInteger(message.lastRoleId.high)))
                    return "lastRoleId: integer|Long expected";
            if (message.lastTeamUid != null && Object.hasOwnProperty.call(message, "lastTeamUid"))
                if (!(message.lastTeamUid && typeof message.lastTeamUid.length === "number" || $util.isString(message.lastTeamUid)))
                    return "lastTeamUid: buffer expected";
            return null;
        };

        /**
         * Creates an EnterpriseDataRoleTeamsPageToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.EnterpriseDataRoleTeamsPageToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.EnterpriseDataRoleTeamsPageToken} EnterpriseDataRoleTeamsPageToken
         */
        EnterpriseDataRoleTeamsPageToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.EnterpriseDataRoleTeamsPageToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.EnterpriseDataRoleTeamsPageToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.EnterpriseDataRoleTeamsPageToken();
            if (object.lastNodeId != null)
                if ($util.Long)
                    message.lastNodeId = $util.Long.fromValue(object.lastNodeId, false);
                else if (typeof object.lastNodeId === "string")
                    message.lastNodeId = parseInt(object.lastNodeId, 10);
                else if (typeof object.lastNodeId === "number")
                    message.lastNodeId = object.lastNodeId;
                else if (typeof object.lastNodeId === "object")
                    message.lastNodeId = new $util.LongBits(object.lastNodeId.low >>> 0, object.lastNodeId.high >>> 0).toNumber();
            if (object.lastRoleId != null)
                if ($util.Long)
                    message.lastRoleId = $util.Long.fromValue(object.lastRoleId, false);
                else if (typeof object.lastRoleId === "string")
                    message.lastRoleId = parseInt(object.lastRoleId, 10);
                else if (typeof object.lastRoleId === "number")
                    message.lastRoleId = object.lastRoleId;
                else if (typeof object.lastRoleId === "object")
                    message.lastRoleId = new $util.LongBits(object.lastRoleId.low >>> 0, object.lastRoleId.high >>> 0).toNumber();
            if (object.lastTeamUid != null)
                if (typeof object.lastTeamUid === "string")
                    $util.base64.decode(object.lastTeamUid, message.lastTeamUid = $util.newBuffer($util.base64.length(object.lastTeamUid)), 0);
                else if (object.lastTeamUid.length >= 0)
                    message.lastTeamUid = object.lastTeamUid;
            return message;
        };

        /**
         * Creates a plain object from an EnterpriseDataRoleTeamsPageToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.EnterpriseDataRoleTeamsPageToken
         * @static
         * @param {Tokens.EnterpriseDataRoleTeamsPageToken} message EnterpriseDataRoleTeamsPageToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EnterpriseDataRoleTeamsPageToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastNodeId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastNodeId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastRoleId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastRoleId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.lastTeamUid = "";
                else {
                    object.lastTeamUid = [];
                    if (options.bytes !== Array)
                        object.lastTeamUid = $util.newBuffer(object.lastTeamUid);
                }
            }
            if (message.lastNodeId != null && Object.hasOwnProperty.call(message, "lastNodeId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastNodeId = typeof message.lastNodeId === "number" ? BigInt(message.lastNodeId) : $util.Long.fromBits(message.lastNodeId.low >>> 0, message.lastNodeId.high >>> 0, false).toBigInt();
                else if (typeof message.lastNodeId === "number")
                    object.lastNodeId = options.longs === String ? String(message.lastNodeId) : message.lastNodeId;
                else
                    object.lastNodeId = options.longs === String ? $util.Long.prototype.toString.call(message.lastNodeId) : options.longs === Number ? new $util.LongBits(message.lastNodeId.low >>> 0, message.lastNodeId.high >>> 0).toNumber() : message.lastNodeId;
            if (message.lastRoleId != null && Object.hasOwnProperty.call(message, "lastRoleId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastRoleId = typeof message.lastRoleId === "number" ? BigInt(message.lastRoleId) : $util.Long.fromBits(message.lastRoleId.low >>> 0, message.lastRoleId.high >>> 0, false).toBigInt();
                else if (typeof message.lastRoleId === "number")
                    object.lastRoleId = options.longs === String ? String(message.lastRoleId) : message.lastRoleId;
                else
                    object.lastRoleId = options.longs === String ? $util.Long.prototype.toString.call(message.lastRoleId) : options.longs === Number ? new $util.LongBits(message.lastRoleId.low >>> 0, message.lastRoleId.high >>> 0).toNumber() : message.lastRoleId;
            if (message.lastTeamUid != null && Object.hasOwnProperty.call(message, "lastTeamUid"))
                object.lastTeamUid = options.bytes === String ? $util.base64.encode(message.lastTeamUid, 0, message.lastTeamUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.lastTeamUid) : message.lastTeamUid;
            return object;
        };

        /**
         * Converts this EnterpriseDataRoleTeamsPageToken to JSON.
         * @function toJSON
         * @memberof Tokens.EnterpriseDataRoleTeamsPageToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EnterpriseDataRoleTeamsPageToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EnterpriseDataRoleTeamsPageToken
         * @function getTypeUrl
         * @memberof Tokens.EnterpriseDataRoleTeamsPageToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EnterpriseDataRoleTeamsPageToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.EnterpriseDataRoleTeamsPageToken";
        };

        return EnterpriseDataRoleTeamsPageToken;
    })();

    Tokens.EnterpriseDataTeamsPageToken = (function() {

        /**
         * Properties of an EnterpriseDataTeamsPageToken.
         * @memberof Tokens
         * @interface IEnterpriseDataTeamsPageToken
         * @property {number|null} [lastNodeId] EnterpriseDataTeamsPageToken lastNodeId
         * @property {Uint8Array|null} [lastTeamUid] EnterpriseDataTeamsPageToken lastTeamUid
         */

        /**
         * Constructs a new EnterpriseDataTeamsPageToken.
         * @memberof Tokens
         * @classdesc Represents an EnterpriseDataTeamsPageToken.
         * @implements IEnterpriseDataTeamsPageToken
         * @constructor
         * @param {Tokens.IEnterpriseDataTeamsPageToken=} [properties] Properties to set
         */
        function EnterpriseDataTeamsPageToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EnterpriseDataTeamsPageToken lastNodeId.
         * @member {number} lastNodeId
         * @memberof Tokens.EnterpriseDataTeamsPageToken
         * @instance
         */
        EnterpriseDataTeamsPageToken.prototype.lastNodeId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EnterpriseDataTeamsPageToken lastTeamUid.
         * @member {Uint8Array} lastTeamUid
         * @memberof Tokens.EnterpriseDataTeamsPageToken
         * @instance
         */
        EnterpriseDataTeamsPageToken.prototype.lastTeamUid = $util.newBuffer([]);

        /**
         * Creates a new EnterpriseDataTeamsPageToken instance using the specified properties.
         * @function create
         * @memberof Tokens.EnterpriseDataTeamsPageToken
         * @static
         * @param {Tokens.IEnterpriseDataTeamsPageToken=} [properties] Properties to set
         * @returns {Tokens.EnterpriseDataTeamsPageToken} EnterpriseDataTeamsPageToken instance
         */
        EnterpriseDataTeamsPageToken.create = function create(properties) {
            return new EnterpriseDataTeamsPageToken(properties);
        };

        /**
         * Encodes the specified EnterpriseDataTeamsPageToken message. Does not implicitly {@link Tokens.EnterpriseDataTeamsPageToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.EnterpriseDataTeamsPageToken
         * @static
         * @param {Tokens.IEnterpriseDataTeamsPageToken} message EnterpriseDataTeamsPageToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterpriseDataTeamsPageToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.lastNodeId != null && Object.hasOwnProperty.call(message, "lastNodeId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.lastNodeId);
            if (message.lastTeamUid != null && Object.hasOwnProperty.call(message, "lastTeamUid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.lastTeamUid);
            return writer;
        };

        /**
         * Encodes the specified EnterpriseDataTeamsPageToken message, length delimited. Does not implicitly {@link Tokens.EnterpriseDataTeamsPageToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.EnterpriseDataTeamsPageToken
         * @static
         * @param {Tokens.IEnterpriseDataTeamsPageToken} message EnterpriseDataTeamsPageToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterpriseDataTeamsPageToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EnterpriseDataTeamsPageToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.EnterpriseDataTeamsPageToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.EnterpriseDataTeamsPageToken} EnterpriseDataTeamsPageToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterpriseDataTeamsPageToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.EnterpriseDataTeamsPageToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.lastNodeId = reader.int64();
                        break;
                    }
                case 2: {
                        message.lastTeamUid = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an EnterpriseDataTeamsPageToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.EnterpriseDataTeamsPageToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.EnterpriseDataTeamsPageToken} EnterpriseDataTeamsPageToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterpriseDataTeamsPageToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EnterpriseDataTeamsPageToken message.
         * @function verify
         * @memberof Tokens.EnterpriseDataTeamsPageToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EnterpriseDataTeamsPageToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.lastNodeId != null && Object.hasOwnProperty.call(message, "lastNodeId"))
                if (!$util.isInteger(message.lastNodeId) && !(message.lastNodeId && $util.isInteger(message.lastNodeId.low) && $util.isInteger(message.lastNodeId.high)))
                    return "lastNodeId: integer|Long expected";
            if (message.lastTeamUid != null && Object.hasOwnProperty.call(message, "lastTeamUid"))
                if (!(message.lastTeamUid && typeof message.lastTeamUid.length === "number" || $util.isString(message.lastTeamUid)))
                    return "lastTeamUid: buffer expected";
            return null;
        };

        /**
         * Creates an EnterpriseDataTeamsPageToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.EnterpriseDataTeamsPageToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.EnterpriseDataTeamsPageToken} EnterpriseDataTeamsPageToken
         */
        EnterpriseDataTeamsPageToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.EnterpriseDataTeamsPageToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.EnterpriseDataTeamsPageToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.EnterpriseDataTeamsPageToken();
            if (object.lastNodeId != null)
                if ($util.Long)
                    message.lastNodeId = $util.Long.fromValue(object.lastNodeId, false);
                else if (typeof object.lastNodeId === "string")
                    message.lastNodeId = parseInt(object.lastNodeId, 10);
                else if (typeof object.lastNodeId === "number")
                    message.lastNodeId = object.lastNodeId;
                else if (typeof object.lastNodeId === "object")
                    message.lastNodeId = new $util.LongBits(object.lastNodeId.low >>> 0, object.lastNodeId.high >>> 0).toNumber();
            if (object.lastTeamUid != null)
                if (typeof object.lastTeamUid === "string")
                    $util.base64.decode(object.lastTeamUid, message.lastTeamUid = $util.newBuffer($util.base64.length(object.lastTeamUid)), 0);
                else if (object.lastTeamUid.length >= 0)
                    message.lastTeamUid = object.lastTeamUid;
            return message;
        };

        /**
         * Creates a plain object from an EnterpriseDataTeamsPageToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.EnterpriseDataTeamsPageToken
         * @static
         * @param {Tokens.EnterpriseDataTeamsPageToken} message EnterpriseDataTeamsPageToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EnterpriseDataTeamsPageToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastNodeId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastNodeId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.lastTeamUid = "";
                else {
                    object.lastTeamUid = [];
                    if (options.bytes !== Array)
                        object.lastTeamUid = $util.newBuffer(object.lastTeamUid);
                }
            }
            if (message.lastNodeId != null && Object.hasOwnProperty.call(message, "lastNodeId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastNodeId = typeof message.lastNodeId === "number" ? BigInt(message.lastNodeId) : $util.Long.fromBits(message.lastNodeId.low >>> 0, message.lastNodeId.high >>> 0, false).toBigInt();
                else if (typeof message.lastNodeId === "number")
                    object.lastNodeId = options.longs === String ? String(message.lastNodeId) : message.lastNodeId;
                else
                    object.lastNodeId = options.longs === String ? $util.Long.prototype.toString.call(message.lastNodeId) : options.longs === Number ? new $util.LongBits(message.lastNodeId.low >>> 0, message.lastNodeId.high >>> 0).toNumber() : message.lastNodeId;
            if (message.lastTeamUid != null && Object.hasOwnProperty.call(message, "lastTeamUid"))
                object.lastTeamUid = options.bytes === String ? $util.base64.encode(message.lastTeamUid, 0, message.lastTeamUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.lastTeamUid) : message.lastTeamUid;
            return object;
        };

        /**
         * Converts this EnterpriseDataTeamsPageToken to JSON.
         * @function toJSON
         * @memberof Tokens.EnterpriseDataTeamsPageToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EnterpriseDataTeamsPageToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EnterpriseDataTeamsPageToken
         * @function getTypeUrl
         * @memberof Tokens.EnterpriseDataTeamsPageToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EnterpriseDataTeamsPageToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.EnterpriseDataTeamsPageToken";
        };

        return EnterpriseDataTeamsPageToken;
    })();

    Tokens.EnterpriseDataTeamUsersPageToken = (function() {

        /**
         * Properties of an EnterpriseDataTeamUsersPageToken.
         * @memberof Tokens
         * @interface IEnterpriseDataTeamUsersPageToken
         * @property {number|null} [lastNodeId] EnterpriseDataTeamUsersPageToken lastNodeId
         * @property {Uint8Array|null} [lastTeamUid] EnterpriseDataTeamUsersPageToken lastTeamUid
         * @property {number|null} [userId] EnterpriseDataTeamUsersPageToken userId
         */

        /**
         * Constructs a new EnterpriseDataTeamUsersPageToken.
         * @memberof Tokens
         * @classdesc Represents an EnterpriseDataTeamUsersPageToken.
         * @implements IEnterpriseDataTeamUsersPageToken
         * @constructor
         * @param {Tokens.IEnterpriseDataTeamUsersPageToken=} [properties] Properties to set
         */
        function EnterpriseDataTeamUsersPageToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EnterpriseDataTeamUsersPageToken lastNodeId.
         * @member {number} lastNodeId
         * @memberof Tokens.EnterpriseDataTeamUsersPageToken
         * @instance
         */
        EnterpriseDataTeamUsersPageToken.prototype.lastNodeId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EnterpriseDataTeamUsersPageToken lastTeamUid.
         * @member {Uint8Array} lastTeamUid
         * @memberof Tokens.EnterpriseDataTeamUsersPageToken
         * @instance
         */
        EnterpriseDataTeamUsersPageToken.prototype.lastTeamUid = $util.newBuffer([]);

        /**
         * EnterpriseDataTeamUsersPageToken userId.
         * @member {number} userId
         * @memberof Tokens.EnterpriseDataTeamUsersPageToken
         * @instance
         */
        EnterpriseDataTeamUsersPageToken.prototype.userId = 0;

        /**
         * Creates a new EnterpriseDataTeamUsersPageToken instance using the specified properties.
         * @function create
         * @memberof Tokens.EnterpriseDataTeamUsersPageToken
         * @static
         * @param {Tokens.IEnterpriseDataTeamUsersPageToken=} [properties] Properties to set
         * @returns {Tokens.EnterpriseDataTeamUsersPageToken} EnterpriseDataTeamUsersPageToken instance
         */
        EnterpriseDataTeamUsersPageToken.create = function create(properties) {
            return new EnterpriseDataTeamUsersPageToken(properties);
        };

        /**
         * Encodes the specified EnterpriseDataTeamUsersPageToken message. Does not implicitly {@link Tokens.EnterpriseDataTeamUsersPageToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.EnterpriseDataTeamUsersPageToken
         * @static
         * @param {Tokens.IEnterpriseDataTeamUsersPageToken} message EnterpriseDataTeamUsersPageToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterpriseDataTeamUsersPageToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.lastNodeId != null && Object.hasOwnProperty.call(message, "lastNodeId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.lastNodeId);
            if (message.lastTeamUid != null && Object.hasOwnProperty.call(message, "lastTeamUid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.lastTeamUid);
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.userId);
            return writer;
        };

        /**
         * Encodes the specified EnterpriseDataTeamUsersPageToken message, length delimited. Does not implicitly {@link Tokens.EnterpriseDataTeamUsersPageToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.EnterpriseDataTeamUsersPageToken
         * @static
         * @param {Tokens.IEnterpriseDataTeamUsersPageToken} message EnterpriseDataTeamUsersPageToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterpriseDataTeamUsersPageToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EnterpriseDataTeamUsersPageToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.EnterpriseDataTeamUsersPageToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.EnterpriseDataTeamUsersPageToken} EnterpriseDataTeamUsersPageToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterpriseDataTeamUsersPageToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.EnterpriseDataTeamUsersPageToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.lastNodeId = reader.int64();
                        break;
                    }
                case 2: {
                        message.lastTeamUid = reader.bytes();
                        break;
                    }
                case 3: {
                        message.userId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an EnterpriseDataTeamUsersPageToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.EnterpriseDataTeamUsersPageToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.EnterpriseDataTeamUsersPageToken} EnterpriseDataTeamUsersPageToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterpriseDataTeamUsersPageToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EnterpriseDataTeamUsersPageToken message.
         * @function verify
         * @memberof Tokens.EnterpriseDataTeamUsersPageToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EnterpriseDataTeamUsersPageToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.lastNodeId != null && Object.hasOwnProperty.call(message, "lastNodeId"))
                if (!$util.isInteger(message.lastNodeId) && !(message.lastNodeId && $util.isInteger(message.lastNodeId.low) && $util.isInteger(message.lastNodeId.high)))
                    return "lastNodeId: integer|Long expected";
            if (message.lastTeamUid != null && Object.hasOwnProperty.call(message, "lastTeamUid"))
                if (!(message.lastTeamUid && typeof message.lastTeamUid.length === "number" || $util.isString(message.lastTeamUid)))
                    return "lastTeamUid: buffer expected";
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                if (!$util.isInteger(message.userId))
                    return "userId: integer expected";
            return null;
        };

        /**
         * Creates an EnterpriseDataTeamUsersPageToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.EnterpriseDataTeamUsersPageToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.EnterpriseDataTeamUsersPageToken} EnterpriseDataTeamUsersPageToken
         */
        EnterpriseDataTeamUsersPageToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.EnterpriseDataTeamUsersPageToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.EnterpriseDataTeamUsersPageToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.EnterpriseDataTeamUsersPageToken();
            if (object.lastNodeId != null)
                if ($util.Long)
                    message.lastNodeId = $util.Long.fromValue(object.lastNodeId, false);
                else if (typeof object.lastNodeId === "string")
                    message.lastNodeId = parseInt(object.lastNodeId, 10);
                else if (typeof object.lastNodeId === "number")
                    message.lastNodeId = object.lastNodeId;
                else if (typeof object.lastNodeId === "object")
                    message.lastNodeId = new $util.LongBits(object.lastNodeId.low >>> 0, object.lastNodeId.high >>> 0).toNumber();
            if (object.lastTeamUid != null)
                if (typeof object.lastTeamUid === "string")
                    $util.base64.decode(object.lastTeamUid, message.lastTeamUid = $util.newBuffer($util.base64.length(object.lastTeamUid)), 0);
                else if (object.lastTeamUid.length >= 0)
                    message.lastTeamUid = object.lastTeamUid;
            if (object.userId != null)
                message.userId = object.userId | 0;
            return message;
        };

        /**
         * Creates a plain object from an EnterpriseDataTeamUsersPageToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.EnterpriseDataTeamUsersPageToken
         * @static
         * @param {Tokens.EnterpriseDataTeamUsersPageToken} message EnterpriseDataTeamUsersPageToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EnterpriseDataTeamUsersPageToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastNodeId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastNodeId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.lastTeamUid = "";
                else {
                    object.lastTeamUid = [];
                    if (options.bytes !== Array)
                        object.lastTeamUid = $util.newBuffer(object.lastTeamUid);
                }
                object.userId = 0;
            }
            if (message.lastNodeId != null && Object.hasOwnProperty.call(message, "lastNodeId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastNodeId = typeof message.lastNodeId === "number" ? BigInt(message.lastNodeId) : $util.Long.fromBits(message.lastNodeId.low >>> 0, message.lastNodeId.high >>> 0, false).toBigInt();
                else if (typeof message.lastNodeId === "number")
                    object.lastNodeId = options.longs === String ? String(message.lastNodeId) : message.lastNodeId;
                else
                    object.lastNodeId = options.longs === String ? $util.Long.prototype.toString.call(message.lastNodeId) : options.longs === Number ? new $util.LongBits(message.lastNodeId.low >>> 0, message.lastNodeId.high >>> 0).toNumber() : message.lastNodeId;
            if (message.lastTeamUid != null && Object.hasOwnProperty.call(message, "lastTeamUid"))
                object.lastTeamUid = options.bytes === String ? $util.base64.encode(message.lastTeamUid, 0, message.lastTeamUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.lastTeamUid) : message.lastTeamUid;
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                object.userId = message.userId;
            return object;
        };

        /**
         * Converts this EnterpriseDataTeamUsersPageToken to JSON.
         * @function toJSON
         * @memberof Tokens.EnterpriseDataTeamUsersPageToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EnterpriseDataTeamUsersPageToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EnterpriseDataTeamUsersPageToken
         * @function getTypeUrl
         * @memberof Tokens.EnterpriseDataTeamUsersPageToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EnterpriseDataTeamUsersPageToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.EnterpriseDataTeamUsersPageToken";
        };

        return EnterpriseDataTeamUsersPageToken;
    })();

    /**
     * EnterpriseBackupStage enum.
     * @name Tokens.EnterpriseBackupStage
     * @enum {number}
     * @property {number} EBS_USERS=0 EBS_USERS value
     * @property {number} EBS_RECORDS_1=1 EBS_RECORDS_1 value
     * @property {number} EBS_RECORDS_2=2 EBS_RECORDS_2 value
     * @property {number} EBS_COMPLETE=3 EBS_COMPLETE value
     */
    Tokens.EnterpriseBackupStage = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "EBS_USERS"] = 0;
        values[valuesById[1] = "EBS_RECORDS_1"] = 1;
        values[valuesById[2] = "EBS_RECORDS_2"] = 2;
        values[valuesById[3] = "EBS_COMPLETE"] = 3;
        return values;
    })();

    Tokens.EnterpriseBackupUnlockPair = (function() {

        /**
         * Properties of an EnterpriseBackupUnlockPair.
         * @memberof Tokens
         * @interface IEnterpriseBackupUnlockPair
         * @property {number|null} [user1Id] EnterpriseBackupUnlockPair user1Id
         * @property {number|null} [user2Id] EnterpriseBackupUnlockPair user2Id
         * @property {Uint8Array|null} [user1Key] EnterpriseBackupUnlockPair user1Key
         * @property {Uint8Array|null} [user2Key] EnterpriseBackupUnlockPair user2Key
         */

        /**
         * Constructs a new EnterpriseBackupUnlockPair.
         * @memberof Tokens
         * @classdesc Represents an EnterpriseBackupUnlockPair.
         * @implements IEnterpriseBackupUnlockPair
         * @constructor
         * @param {Tokens.IEnterpriseBackupUnlockPair=} [properties] Properties to set
         */
        function EnterpriseBackupUnlockPair(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EnterpriseBackupUnlockPair user1Id.
         * @member {number} user1Id
         * @memberof Tokens.EnterpriseBackupUnlockPair
         * @instance
         */
        EnterpriseBackupUnlockPair.prototype.user1Id = 0;

        /**
         * EnterpriseBackupUnlockPair user2Id.
         * @member {number} user2Id
         * @memberof Tokens.EnterpriseBackupUnlockPair
         * @instance
         */
        EnterpriseBackupUnlockPair.prototype.user2Id = 0;

        /**
         * EnterpriseBackupUnlockPair user1Key.
         * @member {Uint8Array} user1Key
         * @memberof Tokens.EnterpriseBackupUnlockPair
         * @instance
         */
        EnterpriseBackupUnlockPair.prototype.user1Key = $util.newBuffer([]);

        /**
         * EnterpriseBackupUnlockPair user2Key.
         * @member {Uint8Array} user2Key
         * @memberof Tokens.EnterpriseBackupUnlockPair
         * @instance
         */
        EnterpriseBackupUnlockPair.prototype.user2Key = $util.newBuffer([]);

        /**
         * Creates a new EnterpriseBackupUnlockPair instance using the specified properties.
         * @function create
         * @memberof Tokens.EnterpriseBackupUnlockPair
         * @static
         * @param {Tokens.IEnterpriseBackupUnlockPair=} [properties] Properties to set
         * @returns {Tokens.EnterpriseBackupUnlockPair} EnterpriseBackupUnlockPair instance
         */
        EnterpriseBackupUnlockPair.create = function create(properties) {
            return new EnterpriseBackupUnlockPair(properties);
        };

        /**
         * Encodes the specified EnterpriseBackupUnlockPair message. Does not implicitly {@link Tokens.EnterpriseBackupUnlockPair.verify|verify} messages.
         * @function encode
         * @memberof Tokens.EnterpriseBackupUnlockPair
         * @static
         * @param {Tokens.IEnterpriseBackupUnlockPair} message EnterpriseBackupUnlockPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterpriseBackupUnlockPair.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.user1Id != null && Object.hasOwnProperty.call(message, "user1Id"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.user1Id);
            if (message.user2Id != null && Object.hasOwnProperty.call(message, "user2Id"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.user2Id);
            if (message.user1Key != null && Object.hasOwnProperty.call(message, "user1Key"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.user1Key);
            if (message.user2Key != null && Object.hasOwnProperty.call(message, "user2Key"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.user2Key);
            return writer;
        };

        /**
         * Encodes the specified EnterpriseBackupUnlockPair message, length delimited. Does not implicitly {@link Tokens.EnterpriseBackupUnlockPair.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.EnterpriseBackupUnlockPair
         * @static
         * @param {Tokens.IEnterpriseBackupUnlockPair} message EnterpriseBackupUnlockPair message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterpriseBackupUnlockPair.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EnterpriseBackupUnlockPair message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.EnterpriseBackupUnlockPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.EnterpriseBackupUnlockPair} EnterpriseBackupUnlockPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterpriseBackupUnlockPair.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.EnterpriseBackupUnlockPair();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.user1Id = reader.int32();
                        break;
                    }
                case 2: {
                        message.user2Id = reader.int32();
                        break;
                    }
                case 3: {
                        message.user1Key = reader.bytes();
                        break;
                    }
                case 4: {
                        message.user2Key = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an EnterpriseBackupUnlockPair message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.EnterpriseBackupUnlockPair
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.EnterpriseBackupUnlockPair} EnterpriseBackupUnlockPair
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterpriseBackupUnlockPair.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EnterpriseBackupUnlockPair message.
         * @function verify
         * @memberof Tokens.EnterpriseBackupUnlockPair
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EnterpriseBackupUnlockPair.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.user1Id != null && Object.hasOwnProperty.call(message, "user1Id"))
                if (!$util.isInteger(message.user1Id))
                    return "user1Id: integer expected";
            if (message.user2Id != null && Object.hasOwnProperty.call(message, "user2Id"))
                if (!$util.isInteger(message.user2Id))
                    return "user2Id: integer expected";
            if (message.user1Key != null && Object.hasOwnProperty.call(message, "user1Key"))
                if (!(message.user1Key && typeof message.user1Key.length === "number" || $util.isString(message.user1Key)))
                    return "user1Key: buffer expected";
            if (message.user2Key != null && Object.hasOwnProperty.call(message, "user2Key"))
                if (!(message.user2Key && typeof message.user2Key.length === "number" || $util.isString(message.user2Key)))
                    return "user2Key: buffer expected";
            return null;
        };

        /**
         * Creates an EnterpriseBackupUnlockPair message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.EnterpriseBackupUnlockPair
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.EnterpriseBackupUnlockPair} EnterpriseBackupUnlockPair
         */
        EnterpriseBackupUnlockPair.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.EnterpriseBackupUnlockPair)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.EnterpriseBackupUnlockPair: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.EnterpriseBackupUnlockPair();
            if (object.user1Id != null)
                message.user1Id = object.user1Id | 0;
            if (object.user2Id != null)
                message.user2Id = object.user2Id | 0;
            if (object.user1Key != null)
                if (typeof object.user1Key === "string")
                    $util.base64.decode(object.user1Key, message.user1Key = $util.newBuffer($util.base64.length(object.user1Key)), 0);
                else if (object.user1Key.length >= 0)
                    message.user1Key = object.user1Key;
            if (object.user2Key != null)
                if (typeof object.user2Key === "string")
                    $util.base64.decode(object.user2Key, message.user2Key = $util.newBuffer($util.base64.length(object.user2Key)), 0);
                else if (object.user2Key.length >= 0)
                    message.user2Key = object.user2Key;
            return message;
        };

        /**
         * Creates a plain object from an EnterpriseBackupUnlockPair message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.EnterpriseBackupUnlockPair
         * @static
         * @param {Tokens.EnterpriseBackupUnlockPair} message EnterpriseBackupUnlockPair
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EnterpriseBackupUnlockPair.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.user1Id = 0;
                object.user2Id = 0;
                if (options.bytes === String)
                    object.user1Key = "";
                else {
                    object.user1Key = [];
                    if (options.bytes !== Array)
                        object.user1Key = $util.newBuffer(object.user1Key);
                }
                if (options.bytes === String)
                    object.user2Key = "";
                else {
                    object.user2Key = [];
                    if (options.bytes !== Array)
                        object.user2Key = $util.newBuffer(object.user2Key);
                }
            }
            if (message.user1Id != null && Object.hasOwnProperty.call(message, "user1Id"))
                object.user1Id = message.user1Id;
            if (message.user2Id != null && Object.hasOwnProperty.call(message, "user2Id"))
                object.user2Id = message.user2Id;
            if (message.user1Key != null && Object.hasOwnProperty.call(message, "user1Key"))
                object.user1Key = options.bytes === String ? $util.base64.encode(message.user1Key, 0, message.user1Key.length) : options.bytes === Array ? Array.prototype.slice.call(message.user1Key) : message.user1Key;
            if (message.user2Key != null && Object.hasOwnProperty.call(message, "user2Key"))
                object.user2Key = options.bytes === String ? $util.base64.encode(message.user2Key, 0, message.user2Key.length) : options.bytes === Array ? Array.prototype.slice.call(message.user2Key) : message.user2Key;
            return object;
        };

        /**
         * Converts this EnterpriseBackupUnlockPair to JSON.
         * @function toJSON
         * @memberof Tokens.EnterpriseBackupUnlockPair
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EnterpriseBackupUnlockPair.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EnterpriseBackupUnlockPair
         * @function getTypeUrl
         * @memberof Tokens.EnterpriseBackupUnlockPair
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EnterpriseBackupUnlockPair.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.EnterpriseBackupUnlockPair";
        };

        return EnterpriseBackupUnlockPair;
    })();

    Tokens.EnterpriseBackupContinuationToken = (function() {

        /**
         * Properties of an EnterpriseBackupContinuationToken.
         * @memberof Tokens
         * @interface IEnterpriseBackupContinuationToken
         * @property {Uint8Array|null} [backupKey] EnterpriseBackupContinuationToken backupKey
         * @property {Array.<Tokens.IEnterpriseBackupUnlockPair>|null} [unlockPairs] EnterpriseBackupContinuationToken unlockPairs
         * @property {Tokens.EnterpriseBackupStage|null} [backupStage] EnterpriseBackupContinuationToken backupStage
         * @property {number|null} [startUser] EnterpriseBackupContinuationToken startUser
         * @property {Uint8Array|null} [startRecord] EnterpriseBackupContinuationToken startRecord
         */

        /**
         * Constructs a new EnterpriseBackupContinuationToken.
         * @memberof Tokens
         * @classdesc Represents an EnterpriseBackupContinuationToken.
         * @implements IEnterpriseBackupContinuationToken
         * @constructor
         * @param {Tokens.IEnterpriseBackupContinuationToken=} [properties] Properties to set
         */
        function EnterpriseBackupContinuationToken(properties) {
            this.unlockPairs = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * EnterpriseBackupContinuationToken backupKey.
         * @member {Uint8Array} backupKey
         * @memberof Tokens.EnterpriseBackupContinuationToken
         * @instance
         */
        EnterpriseBackupContinuationToken.prototype.backupKey = $util.newBuffer([]);

        /**
         * EnterpriseBackupContinuationToken unlockPairs.
         * @member {Array.<Tokens.IEnterpriseBackupUnlockPair>} unlockPairs
         * @memberof Tokens.EnterpriseBackupContinuationToken
         * @instance
         */
        EnterpriseBackupContinuationToken.prototype.unlockPairs = $util.emptyArray;

        /**
         * EnterpriseBackupContinuationToken backupStage.
         * @member {Tokens.EnterpriseBackupStage} backupStage
         * @memberof Tokens.EnterpriseBackupContinuationToken
         * @instance
         */
        EnterpriseBackupContinuationToken.prototype.backupStage = 0;

        /**
         * EnterpriseBackupContinuationToken startUser.
         * @member {number} startUser
         * @memberof Tokens.EnterpriseBackupContinuationToken
         * @instance
         */
        EnterpriseBackupContinuationToken.prototype.startUser = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * EnterpriseBackupContinuationToken startRecord.
         * @member {Uint8Array} startRecord
         * @memberof Tokens.EnterpriseBackupContinuationToken
         * @instance
         */
        EnterpriseBackupContinuationToken.prototype.startRecord = $util.newBuffer([]);

        /**
         * Creates a new EnterpriseBackupContinuationToken instance using the specified properties.
         * @function create
         * @memberof Tokens.EnterpriseBackupContinuationToken
         * @static
         * @param {Tokens.IEnterpriseBackupContinuationToken=} [properties] Properties to set
         * @returns {Tokens.EnterpriseBackupContinuationToken} EnterpriseBackupContinuationToken instance
         */
        EnterpriseBackupContinuationToken.create = function create(properties) {
            return new EnterpriseBackupContinuationToken(properties);
        };

        /**
         * Encodes the specified EnterpriseBackupContinuationToken message. Does not implicitly {@link Tokens.EnterpriseBackupContinuationToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.EnterpriseBackupContinuationToken
         * @static
         * @param {Tokens.IEnterpriseBackupContinuationToken} message EnterpriseBackupContinuationToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterpriseBackupContinuationToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.backupKey != null && Object.hasOwnProperty.call(message, "backupKey"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.backupKey);
            if (message.unlockPairs != null && message.unlockPairs.length)
                for (let i = 0; i < message.unlockPairs.length; ++i)
                    $root.Tokens.EnterpriseBackupUnlockPair.encode(message.unlockPairs[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.backupStage != null && Object.hasOwnProperty.call(message, "backupStage"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.backupStage);
            if (message.startUser != null && Object.hasOwnProperty.call(message, "startUser"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.startUser);
            if (message.startRecord != null && Object.hasOwnProperty.call(message, "startRecord"))
                writer.uint32(/* id 5, wireType 2 =*/42).bytes(message.startRecord);
            return writer;
        };

        /**
         * Encodes the specified EnterpriseBackupContinuationToken message, length delimited. Does not implicitly {@link Tokens.EnterpriseBackupContinuationToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.EnterpriseBackupContinuationToken
         * @static
         * @param {Tokens.IEnterpriseBackupContinuationToken} message EnterpriseBackupContinuationToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        EnterpriseBackupContinuationToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an EnterpriseBackupContinuationToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.EnterpriseBackupContinuationToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.EnterpriseBackupContinuationToken} EnterpriseBackupContinuationToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterpriseBackupContinuationToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.EnterpriseBackupContinuationToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.backupKey = reader.bytes();
                        break;
                    }
                case 2: {
                        if (!(message.unlockPairs && message.unlockPairs.length))
                            message.unlockPairs = [];
                        message.unlockPairs.push($root.Tokens.EnterpriseBackupUnlockPair.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 3: {
                        message.backupStage = reader.int32();
                        break;
                    }
                case 4: {
                        message.startUser = reader.int64();
                        break;
                    }
                case 5: {
                        message.startRecord = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an EnterpriseBackupContinuationToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.EnterpriseBackupContinuationToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.EnterpriseBackupContinuationToken} EnterpriseBackupContinuationToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        EnterpriseBackupContinuationToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an EnterpriseBackupContinuationToken message.
         * @function verify
         * @memberof Tokens.EnterpriseBackupContinuationToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        EnterpriseBackupContinuationToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.backupKey != null && Object.hasOwnProperty.call(message, "backupKey"))
                if (!(message.backupKey && typeof message.backupKey.length === "number" || $util.isString(message.backupKey)))
                    return "backupKey: buffer expected";
            if (message.unlockPairs != null && Object.hasOwnProperty.call(message, "unlockPairs")) {
                if (!Array.isArray(message.unlockPairs))
                    return "unlockPairs: array expected";
                for (let i = 0; i < message.unlockPairs.length; ++i) {
                    let error = $root.Tokens.EnterpriseBackupUnlockPair.verify(message.unlockPairs[i], long + 1);
                    if (error)
                        return "unlockPairs." + error;
                }
            }
            if (message.backupStage != null && Object.hasOwnProperty.call(message, "backupStage"))
                switch (message.backupStage) {
                default:
                    return "backupStage: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                    break;
                }
            if (message.startUser != null && Object.hasOwnProperty.call(message, "startUser"))
                if (!$util.isInteger(message.startUser) && !(message.startUser && $util.isInteger(message.startUser.low) && $util.isInteger(message.startUser.high)))
                    return "startUser: integer|Long expected";
            if (message.startRecord != null && Object.hasOwnProperty.call(message, "startRecord"))
                if (!(message.startRecord && typeof message.startRecord.length === "number" || $util.isString(message.startRecord)))
                    return "startRecord: buffer expected";
            return null;
        };

        /**
         * Creates an EnterpriseBackupContinuationToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.EnterpriseBackupContinuationToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.EnterpriseBackupContinuationToken} EnterpriseBackupContinuationToken
         */
        EnterpriseBackupContinuationToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.EnterpriseBackupContinuationToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.EnterpriseBackupContinuationToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.EnterpriseBackupContinuationToken();
            if (object.backupKey != null)
                if (typeof object.backupKey === "string")
                    $util.base64.decode(object.backupKey, message.backupKey = $util.newBuffer($util.base64.length(object.backupKey)), 0);
                else if (object.backupKey.length >= 0)
                    message.backupKey = object.backupKey;
            if (object.unlockPairs) {
                if (!Array.isArray(object.unlockPairs))
                    throw TypeError(".Tokens.EnterpriseBackupContinuationToken.unlockPairs: array expected");
                message.unlockPairs = [];
                for (let i = 0; i < object.unlockPairs.length; ++i) {
                    if (!$util.isObject(object.unlockPairs[i]))
                        throw TypeError(".Tokens.EnterpriseBackupContinuationToken.unlockPairs: object expected");
                    message.unlockPairs[i] = $root.Tokens.EnterpriseBackupUnlockPair.fromObject(object.unlockPairs[i], long + 1);
                }
            }
            switch (object.backupStage) {
            default:
                if (typeof object.backupStage === "number") {
                    message.backupStage = object.backupStage;
                    break;
                }
                break;
            case "EBS_USERS":
            case 0:
                message.backupStage = 0;
                break;
            case "EBS_RECORDS_1":
            case 1:
                message.backupStage = 1;
                break;
            case "EBS_RECORDS_2":
            case 2:
                message.backupStage = 2;
                break;
            case "EBS_COMPLETE":
            case 3:
                message.backupStage = 3;
                break;
            }
            if (object.startUser != null)
                if ($util.Long)
                    message.startUser = $util.Long.fromValue(object.startUser, false);
                else if (typeof object.startUser === "string")
                    message.startUser = parseInt(object.startUser, 10);
                else if (typeof object.startUser === "number")
                    message.startUser = object.startUser;
                else if (typeof object.startUser === "object")
                    message.startUser = new $util.LongBits(object.startUser.low >>> 0, object.startUser.high >>> 0).toNumber();
            if (object.startRecord != null)
                if (typeof object.startRecord === "string")
                    $util.base64.decode(object.startRecord, message.startRecord = $util.newBuffer($util.base64.length(object.startRecord)), 0);
                else if (object.startRecord.length >= 0)
                    message.startRecord = object.startRecord;
            return message;
        };

        /**
         * Creates a plain object from an EnterpriseBackupContinuationToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.EnterpriseBackupContinuationToken
         * @static
         * @param {Tokens.EnterpriseBackupContinuationToken} message EnterpriseBackupContinuationToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        EnterpriseBackupContinuationToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.unlockPairs = [];
            if (options.defaults) {
                if (options.bytes === String)
                    object.backupKey = "";
                else {
                    object.backupKey = [];
                    if (options.bytes !== Array)
                        object.backupKey = $util.newBuffer(object.backupKey);
                }
                object.backupStage = options.enums === String ? "EBS_USERS" : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.startUser = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.startUser = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.startRecord = "";
                else {
                    object.startRecord = [];
                    if (options.bytes !== Array)
                        object.startRecord = $util.newBuffer(object.startRecord);
                }
            }
            if (message.backupKey != null && Object.hasOwnProperty.call(message, "backupKey"))
                object.backupKey = options.bytes === String ? $util.base64.encode(message.backupKey, 0, message.backupKey.length) : options.bytes === Array ? Array.prototype.slice.call(message.backupKey) : message.backupKey;
            if (message.unlockPairs && message.unlockPairs.length) {
                object.unlockPairs = [];
                for (let j = 0; j < message.unlockPairs.length; ++j)
                    object.unlockPairs[j] = $root.Tokens.EnterpriseBackupUnlockPair.toObject(message.unlockPairs[j], options, q + 1);
            }
            if (message.backupStage != null && Object.hasOwnProperty.call(message, "backupStage"))
                object.backupStage = options.enums === String ? $root.Tokens.EnterpriseBackupStage[message.backupStage] === undefined ? message.backupStage : $root.Tokens.EnterpriseBackupStage[message.backupStage] : message.backupStage;
            if (message.startUser != null && Object.hasOwnProperty.call(message, "startUser"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.startUser = typeof message.startUser === "number" ? BigInt(message.startUser) : $util.Long.fromBits(message.startUser.low >>> 0, message.startUser.high >>> 0, false).toBigInt();
                else if (typeof message.startUser === "number")
                    object.startUser = options.longs === String ? String(message.startUser) : message.startUser;
                else
                    object.startUser = options.longs === String ? $util.Long.prototype.toString.call(message.startUser) : options.longs === Number ? new $util.LongBits(message.startUser.low >>> 0, message.startUser.high >>> 0).toNumber() : message.startUser;
            if (message.startRecord != null && Object.hasOwnProperty.call(message, "startRecord"))
                object.startRecord = options.bytes === String ? $util.base64.encode(message.startRecord, 0, message.startRecord.length) : options.bytes === Array ? Array.prototype.slice.call(message.startRecord) : message.startRecord;
            return object;
        };

        /**
         * Converts this EnterpriseBackupContinuationToken to JSON.
         * @function toJSON
         * @memberof Tokens.EnterpriseBackupContinuationToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        EnterpriseBackupContinuationToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for EnterpriseBackupContinuationToken
         * @function getTypeUrl
         * @memberof Tokens.EnterpriseBackupContinuationToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        EnterpriseBackupContinuationToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.EnterpriseBackupContinuationToken";
        };

        return EnterpriseBackupContinuationToken;
    })();

    Tokens.VerificationToken = (function() {

        /**
         * Properties of a VerificationToken.
         * @memberof Tokens
         * @interface IVerificationToken
         * @property {Uint8Array|null} [accountUid] VerificationToken accountUid
         * @property {string|null} [username] VerificationToken username
         * @property {number|null} [regionId] VerificationToken regionId
         * @property {number|null} [creation] VerificationToken creation
         */

        /**
         * Constructs a new VerificationToken.
         * @memberof Tokens
         * @classdesc Represents a VerificationToken.
         * @implements IVerificationToken
         * @constructor
         * @param {Tokens.IVerificationToken=} [properties] Properties to set
         */
        function VerificationToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * VerificationToken accountUid.
         * @member {Uint8Array} accountUid
         * @memberof Tokens.VerificationToken
         * @instance
         */
        VerificationToken.prototype.accountUid = $util.newBuffer([]);

        /**
         * VerificationToken username.
         * @member {string} username
         * @memberof Tokens.VerificationToken
         * @instance
         */
        VerificationToken.prototype.username = "";

        /**
         * VerificationToken regionId.
         * @member {number} regionId
         * @memberof Tokens.VerificationToken
         * @instance
         */
        VerificationToken.prototype.regionId = 0;

        /**
         * VerificationToken creation.
         * @member {number} creation
         * @memberof Tokens.VerificationToken
         * @instance
         */
        VerificationToken.prototype.creation = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new VerificationToken instance using the specified properties.
         * @function create
         * @memberof Tokens.VerificationToken
         * @static
         * @param {Tokens.IVerificationToken=} [properties] Properties to set
         * @returns {Tokens.VerificationToken} VerificationToken instance
         */
        VerificationToken.create = function create(properties) {
            return new VerificationToken(properties);
        };

        /**
         * Encodes the specified VerificationToken message. Does not implicitly {@link Tokens.VerificationToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.VerificationToken
         * @static
         * @param {Tokens.IVerificationToken} message VerificationToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VerificationToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.accountUid);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.username);
            if (message.regionId != null && Object.hasOwnProperty.call(message, "regionId"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.regionId);
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.creation);
            return writer;
        };

        /**
         * Encodes the specified VerificationToken message, length delimited. Does not implicitly {@link Tokens.VerificationToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.VerificationToken
         * @static
         * @param {Tokens.IVerificationToken} message VerificationToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VerificationToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a VerificationToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.VerificationToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.VerificationToken} VerificationToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VerificationToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.VerificationToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.accountUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.username = reader.string();
                        break;
                    }
                case 3: {
                        message.regionId = reader.int32();
                        break;
                    }
                case 4: {
                        message.creation = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a VerificationToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.VerificationToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.VerificationToken} VerificationToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VerificationToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VerificationToken message.
         * @function verify
         * @memberof Tokens.VerificationToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VerificationToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                if (!(message.accountUid && typeof message.accountUid.length === "number" || $util.isString(message.accountUid)))
                    return "accountUid: buffer expected";
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                if (!$util.isString(message.username))
                    return "username: string expected";
            if (message.regionId != null && Object.hasOwnProperty.call(message, "regionId"))
                if (!$util.isInteger(message.regionId))
                    return "regionId: integer expected";
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                if (!$util.isInteger(message.creation) && !(message.creation && $util.isInteger(message.creation.low) && $util.isInteger(message.creation.high)))
                    return "creation: integer|Long expected";
            return null;
        };

        /**
         * Creates a VerificationToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.VerificationToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.VerificationToken} VerificationToken
         */
        VerificationToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.VerificationToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.VerificationToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.VerificationToken();
            if (object.accountUid != null)
                if (typeof object.accountUid === "string")
                    $util.base64.decode(object.accountUid, message.accountUid = $util.newBuffer($util.base64.length(object.accountUid)), 0);
                else if (object.accountUid.length >= 0)
                    message.accountUid = object.accountUid;
            if (object.username != null)
                message.username = String(object.username);
            if (object.regionId != null)
                message.regionId = object.regionId | 0;
            if (object.creation != null)
                if ($util.Long)
                    message.creation = $util.Long.fromValue(object.creation, false);
                else if (typeof object.creation === "string")
                    message.creation = parseInt(object.creation, 10);
                else if (typeof object.creation === "number")
                    message.creation = object.creation;
                else if (typeof object.creation === "object")
                    message.creation = new $util.LongBits(object.creation.low >>> 0, object.creation.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a VerificationToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.VerificationToken
         * @static
         * @param {Tokens.VerificationToken} message VerificationToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VerificationToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.accountUid = "";
                else {
                    object.accountUid = [];
                    if (options.bytes !== Array)
                        object.accountUid = $util.newBuffer(object.accountUid);
                }
                object.username = "";
                object.regionId = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.creation = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.creation = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                object.accountUid = options.bytes === String ? $util.base64.encode(message.accountUid, 0, message.accountUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.accountUid) : message.accountUid;
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.regionId != null && Object.hasOwnProperty.call(message, "regionId"))
                object.regionId = message.regionId;
            if (message.creation != null && Object.hasOwnProperty.call(message, "creation"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.creation = typeof message.creation === "number" ? BigInt(message.creation) : $util.Long.fromBits(message.creation.low >>> 0, message.creation.high >>> 0, false).toBigInt();
                else if (typeof message.creation === "number")
                    object.creation = options.longs === String ? String(message.creation) : message.creation;
                else
                    object.creation = options.longs === String ? $util.Long.prototype.toString.call(message.creation) : options.longs === Number ? new $util.LongBits(message.creation.low >>> 0, message.creation.high >>> 0).toNumber() : message.creation;
            return object;
        };

        /**
         * Converts this VerificationToken to JSON.
         * @function toJSON
         * @memberof Tokens.VerificationToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VerificationToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for VerificationToken
         * @function getTypeUrl
         * @memberof Tokens.VerificationToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        VerificationToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.VerificationToken";
        };

        return VerificationToken;
    })();

    /**
     * AuditRecordSource enum.
     * @name Tokens.AuditRecordSource
     * @enum {number}
     * @property {number} LEGACY=0 Standard records tracked via {@code record_owner_key_history}.
     * @property {number} KEEPER_DRIVE=1 KeeperDrive records tracked via {@code record_access}.
     */
    Tokens.AuditRecordSource = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "LEGACY"] = 0;
        values[valuesById[1] = "KEEPER_DRIVE"] = 1;
        return values;
    })();

    Tokens.ComplianceDataContinuationToken = (function() {

        /**
         * Properties of a ComplianceDataContinuationToken.
         * @memberof Tokens
         * @interface IComplianceDataContinuationToken
         * @property {number|null} [enterpriseUserId] ComplianceDataContinuationToken enterpriseUserId
         * @property {Uint8Array|null} [recordUid] ComplianceDataContinuationToken recordUid
         * @property {Tokens.AuditRecordSource|null} [source] ComplianceDataContinuationToken source
         */

        /**
         * Constructs a new ComplianceDataContinuationToken.
         * @memberof Tokens
         * @classdesc Represents a ComplianceDataContinuationToken.
         * @implements IComplianceDataContinuationToken
         * @constructor
         * @param {Tokens.IComplianceDataContinuationToken=} [properties] Properties to set
         */
        function ComplianceDataContinuationToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ComplianceDataContinuationToken enterpriseUserId.
         * @member {number} enterpriseUserId
         * @memberof Tokens.ComplianceDataContinuationToken
         * @instance
         */
        ComplianceDataContinuationToken.prototype.enterpriseUserId = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ComplianceDataContinuationToken recordUid.
         * @member {Uint8Array} recordUid
         * @memberof Tokens.ComplianceDataContinuationToken
         * @instance
         */
        ComplianceDataContinuationToken.prototype.recordUid = $util.newBuffer([]);

        /**
         * ComplianceDataContinuationToken source.
         * @member {Tokens.AuditRecordSource} source
         * @memberof Tokens.ComplianceDataContinuationToken
         * @instance
         */
        ComplianceDataContinuationToken.prototype.source = 0;

        /**
         * Creates a new ComplianceDataContinuationToken instance using the specified properties.
         * @function create
         * @memberof Tokens.ComplianceDataContinuationToken
         * @static
         * @param {Tokens.IComplianceDataContinuationToken=} [properties] Properties to set
         * @returns {Tokens.ComplianceDataContinuationToken} ComplianceDataContinuationToken instance
         */
        ComplianceDataContinuationToken.create = function create(properties) {
            return new ComplianceDataContinuationToken(properties);
        };

        /**
         * Encodes the specified ComplianceDataContinuationToken message. Does not implicitly {@link Tokens.ComplianceDataContinuationToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.ComplianceDataContinuationToken
         * @static
         * @param {Tokens.IComplianceDataContinuationToken} message ComplianceDataContinuationToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComplianceDataContinuationToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.enterpriseUserId);
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.recordUid);
            if (message.source != null && Object.hasOwnProperty.call(message, "source"))
                writer.uint32(/* id 3, wireType 0 =*/24).int32(message.source);
            return writer;
        };

        /**
         * Encodes the specified ComplianceDataContinuationToken message, length delimited. Does not implicitly {@link Tokens.ComplianceDataContinuationToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.ComplianceDataContinuationToken
         * @static
         * @param {Tokens.IComplianceDataContinuationToken} message ComplianceDataContinuationToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ComplianceDataContinuationToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ComplianceDataContinuationToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.ComplianceDataContinuationToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.ComplianceDataContinuationToken} ComplianceDataContinuationToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComplianceDataContinuationToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.ComplianceDataContinuationToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.enterpriseUserId = reader.int64();
                        break;
                    }
                case 2: {
                        message.recordUid = reader.bytes();
                        break;
                    }
                case 3: {
                        message.source = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ComplianceDataContinuationToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.ComplianceDataContinuationToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.ComplianceDataContinuationToken} ComplianceDataContinuationToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ComplianceDataContinuationToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ComplianceDataContinuationToken message.
         * @function verify
         * @memberof Tokens.ComplianceDataContinuationToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ComplianceDataContinuationToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                if (!$util.isInteger(message.enterpriseUserId) && !(message.enterpriseUserId && $util.isInteger(message.enterpriseUserId.low) && $util.isInteger(message.enterpriseUserId.high)))
                    return "enterpriseUserId: integer|Long expected";
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                if (!(message.recordUid && typeof message.recordUid.length === "number" || $util.isString(message.recordUid)))
                    return "recordUid: buffer expected";
            if (message.source != null && Object.hasOwnProperty.call(message, "source"))
                switch (message.source) {
                default:
                    return "source: enum value expected";
                case 0:
                case 1:
                    break;
                }
            return null;
        };

        /**
         * Creates a ComplianceDataContinuationToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.ComplianceDataContinuationToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.ComplianceDataContinuationToken} ComplianceDataContinuationToken
         */
        ComplianceDataContinuationToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.ComplianceDataContinuationToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.ComplianceDataContinuationToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.ComplianceDataContinuationToken();
            if (object.enterpriseUserId != null)
                if ($util.Long)
                    message.enterpriseUserId = $util.Long.fromValue(object.enterpriseUserId, false);
                else if (typeof object.enterpriseUserId === "string")
                    message.enterpriseUserId = parseInt(object.enterpriseUserId, 10);
                else if (typeof object.enterpriseUserId === "number")
                    message.enterpriseUserId = object.enterpriseUserId;
                else if (typeof object.enterpriseUserId === "object")
                    message.enterpriseUserId = new $util.LongBits(object.enterpriseUserId.low >>> 0, object.enterpriseUserId.high >>> 0).toNumber();
            if (object.recordUid != null)
                if (typeof object.recordUid === "string")
                    $util.base64.decode(object.recordUid, message.recordUid = $util.newBuffer($util.base64.length(object.recordUid)), 0);
                else if (object.recordUid.length >= 0)
                    message.recordUid = object.recordUid;
            switch (object.source) {
            default:
                if (typeof object.source === "number") {
                    message.source = object.source;
                    break;
                }
                break;
            case "LEGACY":
            case 0:
                message.source = 0;
                break;
            case "KEEPER_DRIVE":
            case 1:
                message.source = 1;
                break;
            }
            return message;
        };

        /**
         * Creates a plain object from a ComplianceDataContinuationToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.ComplianceDataContinuationToken
         * @static
         * @param {Tokens.ComplianceDataContinuationToken} message ComplianceDataContinuationToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ComplianceDataContinuationToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.enterpriseUserId = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.enterpriseUserId = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.recordUid = "";
                else {
                    object.recordUid = [];
                    if (options.bytes !== Array)
                        object.recordUid = $util.newBuffer(object.recordUid);
                }
                object.source = options.enums === String ? "LEGACY" : 0;
            }
            if (message.enterpriseUserId != null && Object.hasOwnProperty.call(message, "enterpriseUserId"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.enterpriseUserId = typeof message.enterpriseUserId === "number" ? BigInt(message.enterpriseUserId) : $util.Long.fromBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0, false).toBigInt();
                else if (typeof message.enterpriseUserId === "number")
                    object.enterpriseUserId = options.longs === String ? String(message.enterpriseUserId) : message.enterpriseUserId;
                else
                    object.enterpriseUserId = options.longs === String ? $util.Long.prototype.toString.call(message.enterpriseUserId) : options.longs === Number ? new $util.LongBits(message.enterpriseUserId.low >>> 0, message.enterpriseUserId.high >>> 0).toNumber() : message.enterpriseUserId;
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                object.recordUid = options.bytes === String ? $util.base64.encode(message.recordUid, 0, message.recordUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.recordUid) : message.recordUid;
            if (message.source != null && Object.hasOwnProperty.call(message, "source"))
                object.source = options.enums === String ? $root.Tokens.AuditRecordSource[message.source] === undefined ? message.source : $root.Tokens.AuditRecordSource[message.source] : message.source;
            return object;
        };

        /**
         * Converts this ComplianceDataContinuationToken to JSON.
         * @function toJSON
         * @memberof Tokens.ComplianceDataContinuationToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ComplianceDataContinuationToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ComplianceDataContinuationToken
         * @function getTypeUrl
         * @memberof Tokens.ComplianceDataContinuationToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ComplianceDataContinuationToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.ComplianceDataContinuationToken";
        };

        return ComplianceDataContinuationToken;
    })();

    Tokens.VaultSyncDownContinuationToken = (function() {

        /**
         * Properties of a VaultSyncDownContinuationToken.
         * @memberof Tokens
         * @interface IVaultSyncDownContinuationToken
         * @property {number|null} [tokenVersion] VaultSyncDownContinuationToken tokenVersion
         * @property {number|null} [syncedToSyncOrder] VaultSyncDownContinuationToken syncedToSyncOrder
         * @property {number|null} [syncingToSyncOrder] VaultSyncDownContinuationToken syncingToSyncOrder
         * @property {number|null} [tokenGenerated] VaultSyncDownContinuationToken tokenGenerated
         * @property {number|null} [lastFullSyncCompleted] VaultSyncDownContinuationToken lastFullSyncCompleted
         * @property {Tokens.IVaultSyncDownLocations|null} [locations] VaultSyncDownContinuationToken locations
         * @property {boolean|null} [checkForAuditData] VaultSyncDownContinuationToken checkForAuditData
         * @property {number|null} [clientVersion] VaultSyncDownContinuationToken clientVersion
         * @property {number|null} [clientDataVersion] VaultSyncDownContinuationToken clientDataVersion
         * @property {boolean|null} [noRemovedSharedFoldersWithTeamAccess] VaultSyncDownContinuationToken noRemovedSharedFoldersWithTeamAccess
         * @property {number|null} [notificationSyncPoint] VaultSyncDownContinuationToken notificationSyncPoint
         * @property {boolean|null} [skipUserRevision] VaultSyncDownContinuationToken skipUserRevision
         * @property {number|null} [multiPageIncrementalClientVersion] VaultSyncDownContinuationToken multiPageIncrementalClientVersion
         * @property {number|null} [dagSyncPoint] VaultSyncDownContinuationToken dagSyncPoint
         */

        /**
         * Constructs a new VaultSyncDownContinuationToken.
         * @memberof Tokens
         * @classdesc Represents a VaultSyncDownContinuationToken.
         * @implements IVaultSyncDownContinuationToken
         * @constructor
         * @param {Tokens.IVaultSyncDownContinuationToken=} [properties] Properties to set
         */
        function VaultSyncDownContinuationToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * VaultSyncDownContinuationToken tokenVersion.
         * @member {number} tokenVersion
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @instance
         */
        VaultSyncDownContinuationToken.prototype.tokenVersion = 0;

        /**
         * VaultSyncDownContinuationToken syncedToSyncOrder.
         * @member {number} syncedToSyncOrder
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @instance
         */
        VaultSyncDownContinuationToken.prototype.syncedToSyncOrder = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * VaultSyncDownContinuationToken syncingToSyncOrder.
         * @member {number} syncingToSyncOrder
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @instance
         */
        VaultSyncDownContinuationToken.prototype.syncingToSyncOrder = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * VaultSyncDownContinuationToken tokenGenerated.
         * @member {number} tokenGenerated
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @instance
         */
        VaultSyncDownContinuationToken.prototype.tokenGenerated = 0;

        /**
         * VaultSyncDownContinuationToken lastFullSyncCompleted.
         * @member {number} lastFullSyncCompleted
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @instance
         */
        VaultSyncDownContinuationToken.prototype.lastFullSyncCompleted = 0;

        /**
         * VaultSyncDownContinuationToken locations.
         * @member {Tokens.IVaultSyncDownLocations|null|undefined} locations
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @instance
         */
        VaultSyncDownContinuationToken.prototype.locations = null;

        /**
         * VaultSyncDownContinuationToken checkForAuditData.
         * @member {boolean} checkForAuditData
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @instance
         */
        VaultSyncDownContinuationToken.prototype.checkForAuditData = false;

        /**
         * VaultSyncDownContinuationToken clientVersion.
         * @member {number} clientVersion
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @instance
         */
        VaultSyncDownContinuationToken.prototype.clientVersion = 0;

        /**
         * VaultSyncDownContinuationToken clientDataVersion.
         * @member {number} clientDataVersion
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @instance
         */
        VaultSyncDownContinuationToken.prototype.clientDataVersion = 0;

        /**
         * VaultSyncDownContinuationToken noRemovedSharedFoldersWithTeamAccess.
         * @member {boolean} noRemovedSharedFoldersWithTeamAccess
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @instance
         */
        VaultSyncDownContinuationToken.prototype.noRemovedSharedFoldersWithTeamAccess = false;

        /**
         * VaultSyncDownContinuationToken notificationSyncPoint.
         * @member {number} notificationSyncPoint
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @instance
         */
        VaultSyncDownContinuationToken.prototype.notificationSyncPoint = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * VaultSyncDownContinuationToken skipUserRevision.
         * @member {boolean} skipUserRevision
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @instance
         */
        VaultSyncDownContinuationToken.prototype.skipUserRevision = false;

        /**
         * VaultSyncDownContinuationToken multiPageIncrementalClientVersion.
         * @member {number} multiPageIncrementalClientVersion
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @instance
         */
        VaultSyncDownContinuationToken.prototype.multiPageIncrementalClientVersion = 0;

        /**
         * VaultSyncDownContinuationToken dagSyncPoint.
         * @member {number} dagSyncPoint
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @instance
         */
        VaultSyncDownContinuationToken.prototype.dagSyncPoint = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new VaultSyncDownContinuationToken instance using the specified properties.
         * @function create
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @static
         * @param {Tokens.IVaultSyncDownContinuationToken=} [properties] Properties to set
         * @returns {Tokens.VaultSyncDownContinuationToken} VaultSyncDownContinuationToken instance
         */
        VaultSyncDownContinuationToken.create = function create(properties) {
            return new VaultSyncDownContinuationToken(properties);
        };

        /**
         * Encodes the specified VaultSyncDownContinuationToken message. Does not implicitly {@link Tokens.VaultSyncDownContinuationToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @static
         * @param {Tokens.IVaultSyncDownContinuationToken} message VaultSyncDownContinuationToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VaultSyncDownContinuationToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.tokenVersion != null && Object.hasOwnProperty.call(message, "tokenVersion"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.tokenVersion);
            if (message.syncedToSyncOrder != null && Object.hasOwnProperty.call(message, "syncedToSyncOrder"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.syncedToSyncOrder);
            if (message.syncingToSyncOrder != null && Object.hasOwnProperty.call(message, "syncingToSyncOrder"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.syncingToSyncOrder);
            if (message.tokenGenerated != null && Object.hasOwnProperty.call(message, "tokenGenerated"))
                writer.uint32(/* id 4, wireType 0 =*/32).int32(message.tokenGenerated);
            if (message.lastFullSyncCompleted != null && Object.hasOwnProperty.call(message, "lastFullSyncCompleted"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.lastFullSyncCompleted);
            if (message.locations != null && Object.hasOwnProperty.call(message, "locations"))
                $root.Tokens.VaultSyncDownLocations.encode(message.locations, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
            if (message.checkForAuditData != null && Object.hasOwnProperty.call(message, "checkForAuditData"))
                writer.uint32(/* id 7, wireType 0 =*/56).bool(message.checkForAuditData);
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                writer.uint32(/* id 8, wireType 0 =*/64).int32(message.clientVersion);
            if (message.clientDataVersion != null && Object.hasOwnProperty.call(message, "clientDataVersion"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.clientDataVersion);
            if (message.noRemovedSharedFoldersWithTeamAccess != null && Object.hasOwnProperty.call(message, "noRemovedSharedFoldersWithTeamAccess"))
                writer.uint32(/* id 10, wireType 0 =*/80).bool(message.noRemovedSharedFoldersWithTeamAccess);
            if (message.notificationSyncPoint != null && Object.hasOwnProperty.call(message, "notificationSyncPoint"))
                writer.uint32(/* id 11, wireType 0 =*/88).int64(message.notificationSyncPoint);
            if (message.skipUserRevision != null && Object.hasOwnProperty.call(message, "skipUserRevision"))
                writer.uint32(/* id 12, wireType 0 =*/96).bool(message.skipUserRevision);
            if (message.multiPageIncrementalClientVersion != null && Object.hasOwnProperty.call(message, "multiPageIncrementalClientVersion"))
                writer.uint32(/* id 14, wireType 0 =*/112).int32(message.multiPageIncrementalClientVersion);
            if (message.dagSyncPoint != null && Object.hasOwnProperty.call(message, "dagSyncPoint"))
                writer.uint32(/* id 15, wireType 0 =*/120).int64(message.dagSyncPoint);
            return writer;
        };

        /**
         * Encodes the specified VaultSyncDownContinuationToken message, length delimited. Does not implicitly {@link Tokens.VaultSyncDownContinuationToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @static
         * @param {Tokens.IVaultSyncDownContinuationToken} message VaultSyncDownContinuationToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VaultSyncDownContinuationToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a VaultSyncDownContinuationToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.VaultSyncDownContinuationToken} VaultSyncDownContinuationToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VaultSyncDownContinuationToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.VaultSyncDownContinuationToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.tokenVersion = reader.int32();
                        break;
                    }
                case 2: {
                        message.syncedToSyncOrder = reader.int64();
                        break;
                    }
                case 3: {
                        message.syncingToSyncOrder = reader.int64();
                        break;
                    }
                case 4: {
                        message.tokenGenerated = reader.int32();
                        break;
                    }
                case 5: {
                        message.lastFullSyncCompleted = reader.int32();
                        break;
                    }
                case 6: {
                        message.locations = $root.Tokens.VaultSyncDownLocations.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 7: {
                        message.checkForAuditData = reader.bool();
                        break;
                    }
                case 8: {
                        message.clientVersion = reader.int32();
                        break;
                    }
                case 9: {
                        message.clientDataVersion = reader.int32();
                        break;
                    }
                case 10: {
                        message.noRemovedSharedFoldersWithTeamAccess = reader.bool();
                        break;
                    }
                case 11: {
                        message.notificationSyncPoint = reader.int64();
                        break;
                    }
                case 12: {
                        message.skipUserRevision = reader.bool();
                        break;
                    }
                case 14: {
                        message.multiPageIncrementalClientVersion = reader.int32();
                        break;
                    }
                case 15: {
                        message.dagSyncPoint = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a VaultSyncDownContinuationToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.VaultSyncDownContinuationToken} VaultSyncDownContinuationToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VaultSyncDownContinuationToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VaultSyncDownContinuationToken message.
         * @function verify
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VaultSyncDownContinuationToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.tokenVersion != null && Object.hasOwnProperty.call(message, "tokenVersion"))
                if (!$util.isInteger(message.tokenVersion))
                    return "tokenVersion: integer expected";
            if (message.syncedToSyncOrder != null && Object.hasOwnProperty.call(message, "syncedToSyncOrder"))
                if (!$util.isInteger(message.syncedToSyncOrder) && !(message.syncedToSyncOrder && $util.isInteger(message.syncedToSyncOrder.low) && $util.isInteger(message.syncedToSyncOrder.high)))
                    return "syncedToSyncOrder: integer|Long expected";
            if (message.syncingToSyncOrder != null && Object.hasOwnProperty.call(message, "syncingToSyncOrder"))
                if (!$util.isInteger(message.syncingToSyncOrder) && !(message.syncingToSyncOrder && $util.isInteger(message.syncingToSyncOrder.low) && $util.isInteger(message.syncingToSyncOrder.high)))
                    return "syncingToSyncOrder: integer|Long expected";
            if (message.tokenGenerated != null && Object.hasOwnProperty.call(message, "tokenGenerated"))
                if (!$util.isInteger(message.tokenGenerated))
                    return "tokenGenerated: integer expected";
            if (message.lastFullSyncCompleted != null && Object.hasOwnProperty.call(message, "lastFullSyncCompleted"))
                if (!$util.isInteger(message.lastFullSyncCompleted))
                    return "lastFullSyncCompleted: integer expected";
            if (message.locations != null && Object.hasOwnProperty.call(message, "locations")) {
                let error = $root.Tokens.VaultSyncDownLocations.verify(message.locations, long + 1);
                if (error)
                    return "locations." + error;
            }
            if (message.checkForAuditData != null && Object.hasOwnProperty.call(message, "checkForAuditData"))
                if (typeof message.checkForAuditData !== "boolean")
                    return "checkForAuditData: boolean expected";
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                if (!$util.isInteger(message.clientVersion))
                    return "clientVersion: integer expected";
            if (message.clientDataVersion != null && Object.hasOwnProperty.call(message, "clientDataVersion"))
                if (!$util.isInteger(message.clientDataVersion))
                    return "clientDataVersion: integer expected";
            if (message.noRemovedSharedFoldersWithTeamAccess != null && Object.hasOwnProperty.call(message, "noRemovedSharedFoldersWithTeamAccess"))
                if (typeof message.noRemovedSharedFoldersWithTeamAccess !== "boolean")
                    return "noRemovedSharedFoldersWithTeamAccess: boolean expected";
            if (message.notificationSyncPoint != null && Object.hasOwnProperty.call(message, "notificationSyncPoint"))
                if (!$util.isInteger(message.notificationSyncPoint) && !(message.notificationSyncPoint && $util.isInteger(message.notificationSyncPoint.low) && $util.isInteger(message.notificationSyncPoint.high)))
                    return "notificationSyncPoint: integer|Long expected";
            if (message.skipUserRevision != null && Object.hasOwnProperty.call(message, "skipUserRevision"))
                if (typeof message.skipUserRevision !== "boolean")
                    return "skipUserRevision: boolean expected";
            if (message.multiPageIncrementalClientVersion != null && Object.hasOwnProperty.call(message, "multiPageIncrementalClientVersion"))
                if (!$util.isInteger(message.multiPageIncrementalClientVersion))
                    return "multiPageIncrementalClientVersion: integer expected";
            if (message.dagSyncPoint != null && Object.hasOwnProperty.call(message, "dagSyncPoint"))
                if (!$util.isInteger(message.dagSyncPoint) && !(message.dagSyncPoint && $util.isInteger(message.dagSyncPoint.low) && $util.isInteger(message.dagSyncPoint.high)))
                    return "dagSyncPoint: integer|Long expected";
            return null;
        };

        /**
         * Creates a VaultSyncDownContinuationToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.VaultSyncDownContinuationToken} VaultSyncDownContinuationToken
         */
        VaultSyncDownContinuationToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.VaultSyncDownContinuationToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.VaultSyncDownContinuationToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.VaultSyncDownContinuationToken();
            if (object.tokenVersion != null)
                message.tokenVersion = object.tokenVersion | 0;
            if (object.syncedToSyncOrder != null)
                if ($util.Long)
                    message.syncedToSyncOrder = $util.Long.fromValue(object.syncedToSyncOrder, false);
                else if (typeof object.syncedToSyncOrder === "string")
                    message.syncedToSyncOrder = parseInt(object.syncedToSyncOrder, 10);
                else if (typeof object.syncedToSyncOrder === "number")
                    message.syncedToSyncOrder = object.syncedToSyncOrder;
                else if (typeof object.syncedToSyncOrder === "object")
                    message.syncedToSyncOrder = new $util.LongBits(object.syncedToSyncOrder.low >>> 0, object.syncedToSyncOrder.high >>> 0).toNumber();
            if (object.syncingToSyncOrder != null)
                if ($util.Long)
                    message.syncingToSyncOrder = $util.Long.fromValue(object.syncingToSyncOrder, false);
                else if (typeof object.syncingToSyncOrder === "string")
                    message.syncingToSyncOrder = parseInt(object.syncingToSyncOrder, 10);
                else if (typeof object.syncingToSyncOrder === "number")
                    message.syncingToSyncOrder = object.syncingToSyncOrder;
                else if (typeof object.syncingToSyncOrder === "object")
                    message.syncingToSyncOrder = new $util.LongBits(object.syncingToSyncOrder.low >>> 0, object.syncingToSyncOrder.high >>> 0).toNumber();
            if (object.tokenGenerated != null)
                message.tokenGenerated = object.tokenGenerated | 0;
            if (object.lastFullSyncCompleted != null)
                message.lastFullSyncCompleted = object.lastFullSyncCompleted | 0;
            if (object.locations != null) {
                if (!$util.isObject(object.locations))
                    throw TypeError(".Tokens.VaultSyncDownContinuationToken.locations: object expected");
                message.locations = $root.Tokens.VaultSyncDownLocations.fromObject(object.locations, long + 1);
            }
            if (object.checkForAuditData != null)
                message.checkForAuditData = Boolean(object.checkForAuditData);
            if (object.clientVersion != null)
                message.clientVersion = object.clientVersion | 0;
            if (object.clientDataVersion != null)
                message.clientDataVersion = object.clientDataVersion | 0;
            if (object.noRemovedSharedFoldersWithTeamAccess != null)
                message.noRemovedSharedFoldersWithTeamAccess = Boolean(object.noRemovedSharedFoldersWithTeamAccess);
            if (object.notificationSyncPoint != null)
                if ($util.Long)
                    message.notificationSyncPoint = $util.Long.fromValue(object.notificationSyncPoint, false);
                else if (typeof object.notificationSyncPoint === "string")
                    message.notificationSyncPoint = parseInt(object.notificationSyncPoint, 10);
                else if (typeof object.notificationSyncPoint === "number")
                    message.notificationSyncPoint = object.notificationSyncPoint;
                else if (typeof object.notificationSyncPoint === "object")
                    message.notificationSyncPoint = new $util.LongBits(object.notificationSyncPoint.low >>> 0, object.notificationSyncPoint.high >>> 0).toNumber();
            if (object.skipUserRevision != null)
                message.skipUserRevision = Boolean(object.skipUserRevision);
            if (object.multiPageIncrementalClientVersion != null)
                message.multiPageIncrementalClientVersion = object.multiPageIncrementalClientVersion | 0;
            if (object.dagSyncPoint != null)
                if ($util.Long)
                    message.dagSyncPoint = $util.Long.fromValue(object.dagSyncPoint, false);
                else if (typeof object.dagSyncPoint === "string")
                    message.dagSyncPoint = parseInt(object.dagSyncPoint, 10);
                else if (typeof object.dagSyncPoint === "number")
                    message.dagSyncPoint = object.dagSyncPoint;
                else if (typeof object.dagSyncPoint === "object")
                    message.dagSyncPoint = new $util.LongBits(object.dagSyncPoint.low >>> 0, object.dagSyncPoint.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a VaultSyncDownContinuationToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @static
         * @param {Tokens.VaultSyncDownContinuationToken} message VaultSyncDownContinuationToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VaultSyncDownContinuationToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.tokenVersion = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.syncedToSyncOrder = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.syncedToSyncOrder = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.syncingToSyncOrder = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.syncingToSyncOrder = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.tokenGenerated = 0;
                object.lastFullSyncCompleted = 0;
                object.locations = null;
                object.checkForAuditData = false;
                object.clientVersion = 0;
                object.clientDataVersion = 0;
                object.noRemovedSharedFoldersWithTeamAccess = false;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.notificationSyncPoint = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.notificationSyncPoint = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.skipUserRevision = false;
                object.multiPageIncrementalClientVersion = 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.dagSyncPoint = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.dagSyncPoint = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.tokenVersion != null && Object.hasOwnProperty.call(message, "tokenVersion"))
                object.tokenVersion = message.tokenVersion;
            if (message.syncedToSyncOrder != null && Object.hasOwnProperty.call(message, "syncedToSyncOrder"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.syncedToSyncOrder = typeof message.syncedToSyncOrder === "number" ? BigInt(message.syncedToSyncOrder) : $util.Long.fromBits(message.syncedToSyncOrder.low >>> 0, message.syncedToSyncOrder.high >>> 0, false).toBigInt();
                else if (typeof message.syncedToSyncOrder === "number")
                    object.syncedToSyncOrder = options.longs === String ? String(message.syncedToSyncOrder) : message.syncedToSyncOrder;
                else
                    object.syncedToSyncOrder = options.longs === String ? $util.Long.prototype.toString.call(message.syncedToSyncOrder) : options.longs === Number ? new $util.LongBits(message.syncedToSyncOrder.low >>> 0, message.syncedToSyncOrder.high >>> 0).toNumber() : message.syncedToSyncOrder;
            if (message.syncingToSyncOrder != null && Object.hasOwnProperty.call(message, "syncingToSyncOrder"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.syncingToSyncOrder = typeof message.syncingToSyncOrder === "number" ? BigInt(message.syncingToSyncOrder) : $util.Long.fromBits(message.syncingToSyncOrder.low >>> 0, message.syncingToSyncOrder.high >>> 0, false).toBigInt();
                else if (typeof message.syncingToSyncOrder === "number")
                    object.syncingToSyncOrder = options.longs === String ? String(message.syncingToSyncOrder) : message.syncingToSyncOrder;
                else
                    object.syncingToSyncOrder = options.longs === String ? $util.Long.prototype.toString.call(message.syncingToSyncOrder) : options.longs === Number ? new $util.LongBits(message.syncingToSyncOrder.low >>> 0, message.syncingToSyncOrder.high >>> 0).toNumber() : message.syncingToSyncOrder;
            if (message.tokenGenerated != null && Object.hasOwnProperty.call(message, "tokenGenerated"))
                object.tokenGenerated = message.tokenGenerated;
            if (message.lastFullSyncCompleted != null && Object.hasOwnProperty.call(message, "lastFullSyncCompleted"))
                object.lastFullSyncCompleted = message.lastFullSyncCompleted;
            if (message.locations != null && Object.hasOwnProperty.call(message, "locations"))
                object.locations = $root.Tokens.VaultSyncDownLocations.toObject(message.locations, options, q + 1);
            if (message.checkForAuditData != null && Object.hasOwnProperty.call(message, "checkForAuditData"))
                object.checkForAuditData = message.checkForAuditData;
            if (message.clientVersion != null && Object.hasOwnProperty.call(message, "clientVersion"))
                object.clientVersion = message.clientVersion;
            if (message.clientDataVersion != null && Object.hasOwnProperty.call(message, "clientDataVersion"))
                object.clientDataVersion = message.clientDataVersion;
            if (message.noRemovedSharedFoldersWithTeamAccess != null && Object.hasOwnProperty.call(message, "noRemovedSharedFoldersWithTeamAccess"))
                object.noRemovedSharedFoldersWithTeamAccess = message.noRemovedSharedFoldersWithTeamAccess;
            if (message.notificationSyncPoint != null && Object.hasOwnProperty.call(message, "notificationSyncPoint"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.notificationSyncPoint = typeof message.notificationSyncPoint === "number" ? BigInt(message.notificationSyncPoint) : $util.Long.fromBits(message.notificationSyncPoint.low >>> 0, message.notificationSyncPoint.high >>> 0, false).toBigInt();
                else if (typeof message.notificationSyncPoint === "number")
                    object.notificationSyncPoint = options.longs === String ? String(message.notificationSyncPoint) : message.notificationSyncPoint;
                else
                    object.notificationSyncPoint = options.longs === String ? $util.Long.prototype.toString.call(message.notificationSyncPoint) : options.longs === Number ? new $util.LongBits(message.notificationSyncPoint.low >>> 0, message.notificationSyncPoint.high >>> 0).toNumber() : message.notificationSyncPoint;
            if (message.skipUserRevision != null && Object.hasOwnProperty.call(message, "skipUserRevision"))
                object.skipUserRevision = message.skipUserRevision;
            if (message.multiPageIncrementalClientVersion != null && Object.hasOwnProperty.call(message, "multiPageIncrementalClientVersion"))
                object.multiPageIncrementalClientVersion = message.multiPageIncrementalClientVersion;
            if (message.dagSyncPoint != null && Object.hasOwnProperty.call(message, "dagSyncPoint"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.dagSyncPoint = typeof message.dagSyncPoint === "number" ? BigInt(message.dagSyncPoint) : $util.Long.fromBits(message.dagSyncPoint.low >>> 0, message.dagSyncPoint.high >>> 0, false).toBigInt();
                else if (typeof message.dagSyncPoint === "number")
                    object.dagSyncPoint = options.longs === String ? String(message.dagSyncPoint) : message.dagSyncPoint;
                else
                    object.dagSyncPoint = options.longs === String ? $util.Long.prototype.toString.call(message.dagSyncPoint) : options.longs === Number ? new $util.LongBits(message.dagSyncPoint.low >>> 0, message.dagSyncPoint.high >>> 0).toNumber() : message.dagSyncPoint;
            return object;
        };

        /**
         * Converts this VaultSyncDownContinuationToken to JSON.
         * @function toJSON
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VaultSyncDownContinuationToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for VaultSyncDownContinuationToken
         * @function getTypeUrl
         * @memberof Tokens.VaultSyncDownContinuationToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        VaultSyncDownContinuationToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.VaultSyncDownContinuationToken";
        };

        return VaultSyncDownContinuationToken;
    })();

    Tokens.VaultSyncDownLocations = (function() {

        /**
         * Properties of a VaultSyncDownLocations.
         * @memberof Tokens
         * @interface IVaultSyncDownLocations
         * @property {number|null} [vaultDataEntity] VaultSyncDownLocations vaultDataEntity
         * @property {Tokens.IRecordLocation|null} [recordLocation] VaultSyncDownLocations recordLocation
         * @property {Tokens.IRecordLinkLocation|null} [recordLinkLocation] VaultSyncDownLocations recordLinkLocation
         * @property {Tokens.IFolderLocation|null} [folderLocation] VaultSyncDownLocations folderLocation
         * @property {Tokens.IFolderRecordLocation|null} [folderRecordLocation] VaultSyncDownLocations folderRecordLocation
         * @property {Tokens.IFolderSharedFolderLocation|null} [folderSharedFolderLocation] VaultSyncDownLocations folderSharedFolderLocation
         * @property {Tokens.ISharedFolderLocation|null} [sharedFolderLocation] VaultSyncDownLocations sharedFolderLocation
         * @property {Tokens.ISharedFolderRecordLocation|null} [sharedFolderRecordLocation] VaultSyncDownLocations sharedFolderRecordLocation
         * @property {Tokens.ISharedFolderTeamLocation|null} [sharedFolderTeamLocation] VaultSyncDownLocations sharedFolderTeamLocation
         * @property {Tokens.ISharedFolderUserLocation|null} [sharedFolderUserLocation] VaultSyncDownLocations sharedFolderUserLocation
         * @property {Tokens.IShareInvitationLocation|null} [shareInvitationLocation] VaultSyncDownLocations shareInvitationLocation
         * @property {Tokens.ITeamLocation|null} [teamLocation] VaultSyncDownLocations teamLocation
         * @property {Tokens.IUserAuthHashLocation|null} [userAuthHashLocation] VaultSyncDownLocations userAuthHashLocation
         * @property {Tokens.IKsmLocation|null} [ksmLocation] VaultSyncDownLocations ksmLocation
         * @property {Tokens.ISharedFolderFolderRecordLocation|null} [sharedFolderFolderRecordLocation] VaultSyncDownLocations sharedFolderFolderRecordLocation
         * @property {Tokens.IUserLocation|null} [userLocation] VaultSyncDownLocations userLocation
         */

        /**
         * Constructs a new VaultSyncDownLocations.
         * @memberof Tokens
         * @classdesc Represents a VaultSyncDownLocations.
         * @implements IVaultSyncDownLocations
         * @constructor
         * @param {Tokens.IVaultSyncDownLocations=} [properties] Properties to set
         */
        function VaultSyncDownLocations(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * VaultSyncDownLocations vaultDataEntity.
         * @member {number} vaultDataEntity
         * @memberof Tokens.VaultSyncDownLocations
         * @instance
         */
        VaultSyncDownLocations.prototype.vaultDataEntity = 0;

        /**
         * VaultSyncDownLocations recordLocation.
         * @member {Tokens.IRecordLocation|null|undefined} recordLocation
         * @memberof Tokens.VaultSyncDownLocations
         * @instance
         */
        VaultSyncDownLocations.prototype.recordLocation = null;

        /**
         * VaultSyncDownLocations recordLinkLocation.
         * @member {Tokens.IRecordLinkLocation|null|undefined} recordLinkLocation
         * @memberof Tokens.VaultSyncDownLocations
         * @instance
         */
        VaultSyncDownLocations.prototype.recordLinkLocation = null;

        /**
         * VaultSyncDownLocations folderLocation.
         * @member {Tokens.IFolderLocation|null|undefined} folderLocation
         * @memberof Tokens.VaultSyncDownLocations
         * @instance
         */
        VaultSyncDownLocations.prototype.folderLocation = null;

        /**
         * VaultSyncDownLocations folderRecordLocation.
         * @member {Tokens.IFolderRecordLocation|null|undefined} folderRecordLocation
         * @memberof Tokens.VaultSyncDownLocations
         * @instance
         */
        VaultSyncDownLocations.prototype.folderRecordLocation = null;

        /**
         * VaultSyncDownLocations folderSharedFolderLocation.
         * @member {Tokens.IFolderSharedFolderLocation|null|undefined} folderSharedFolderLocation
         * @memberof Tokens.VaultSyncDownLocations
         * @instance
         */
        VaultSyncDownLocations.prototype.folderSharedFolderLocation = null;

        /**
         * VaultSyncDownLocations sharedFolderLocation.
         * @member {Tokens.ISharedFolderLocation|null|undefined} sharedFolderLocation
         * @memberof Tokens.VaultSyncDownLocations
         * @instance
         */
        VaultSyncDownLocations.prototype.sharedFolderLocation = null;

        /**
         * VaultSyncDownLocations sharedFolderRecordLocation.
         * @member {Tokens.ISharedFolderRecordLocation|null|undefined} sharedFolderRecordLocation
         * @memberof Tokens.VaultSyncDownLocations
         * @instance
         */
        VaultSyncDownLocations.prototype.sharedFolderRecordLocation = null;

        /**
         * VaultSyncDownLocations sharedFolderTeamLocation.
         * @member {Tokens.ISharedFolderTeamLocation|null|undefined} sharedFolderTeamLocation
         * @memberof Tokens.VaultSyncDownLocations
         * @instance
         */
        VaultSyncDownLocations.prototype.sharedFolderTeamLocation = null;

        /**
         * VaultSyncDownLocations sharedFolderUserLocation.
         * @member {Tokens.ISharedFolderUserLocation|null|undefined} sharedFolderUserLocation
         * @memberof Tokens.VaultSyncDownLocations
         * @instance
         */
        VaultSyncDownLocations.prototype.sharedFolderUserLocation = null;

        /**
         * VaultSyncDownLocations shareInvitationLocation.
         * @member {Tokens.IShareInvitationLocation|null|undefined} shareInvitationLocation
         * @memberof Tokens.VaultSyncDownLocations
         * @instance
         */
        VaultSyncDownLocations.prototype.shareInvitationLocation = null;

        /**
         * VaultSyncDownLocations teamLocation.
         * @member {Tokens.ITeamLocation|null|undefined} teamLocation
         * @memberof Tokens.VaultSyncDownLocations
         * @instance
         */
        VaultSyncDownLocations.prototype.teamLocation = null;

        /**
         * VaultSyncDownLocations userAuthHashLocation.
         * @member {Tokens.IUserAuthHashLocation|null|undefined} userAuthHashLocation
         * @memberof Tokens.VaultSyncDownLocations
         * @instance
         */
        VaultSyncDownLocations.prototype.userAuthHashLocation = null;

        /**
         * VaultSyncDownLocations ksmLocation.
         * @member {Tokens.IKsmLocation|null|undefined} ksmLocation
         * @memberof Tokens.VaultSyncDownLocations
         * @instance
         */
        VaultSyncDownLocations.prototype.ksmLocation = null;

        /**
         * VaultSyncDownLocations sharedFolderFolderRecordLocation.
         * @member {Tokens.ISharedFolderFolderRecordLocation|null|undefined} sharedFolderFolderRecordLocation
         * @memberof Tokens.VaultSyncDownLocations
         * @instance
         */
        VaultSyncDownLocations.prototype.sharedFolderFolderRecordLocation = null;

        /**
         * VaultSyncDownLocations userLocation.
         * @member {Tokens.IUserLocation|null|undefined} userLocation
         * @memberof Tokens.VaultSyncDownLocations
         * @instance
         */
        VaultSyncDownLocations.prototype.userLocation = null;

        /**
         * Creates a new VaultSyncDownLocations instance using the specified properties.
         * @function create
         * @memberof Tokens.VaultSyncDownLocations
         * @static
         * @param {Tokens.IVaultSyncDownLocations=} [properties] Properties to set
         * @returns {Tokens.VaultSyncDownLocations} VaultSyncDownLocations instance
         */
        VaultSyncDownLocations.create = function create(properties) {
            return new VaultSyncDownLocations(properties);
        };

        /**
         * Encodes the specified VaultSyncDownLocations message. Does not implicitly {@link Tokens.VaultSyncDownLocations.verify|verify} messages.
         * @function encode
         * @memberof Tokens.VaultSyncDownLocations
         * @static
         * @param {Tokens.IVaultSyncDownLocations} message VaultSyncDownLocations message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VaultSyncDownLocations.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.vaultDataEntity != null && Object.hasOwnProperty.call(message, "vaultDataEntity"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.vaultDataEntity);
            if (message.recordLocation != null && Object.hasOwnProperty.call(message, "recordLocation"))
                $root.Tokens.RecordLocation.encode(message.recordLocation, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.recordLinkLocation != null && Object.hasOwnProperty.call(message, "recordLinkLocation"))
                $root.Tokens.RecordLinkLocation.encode(message.recordLinkLocation, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.folderLocation != null && Object.hasOwnProperty.call(message, "folderLocation"))
                $root.Tokens.FolderLocation.encode(message.folderLocation, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.folderRecordLocation != null && Object.hasOwnProperty.call(message, "folderRecordLocation"))
                $root.Tokens.FolderRecordLocation.encode(message.folderRecordLocation, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            if (message.folderSharedFolderLocation != null && Object.hasOwnProperty.call(message, "folderSharedFolderLocation"))
                $root.Tokens.FolderSharedFolderLocation.encode(message.folderSharedFolderLocation, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
            if (message.sharedFolderLocation != null && Object.hasOwnProperty.call(message, "sharedFolderLocation"))
                $root.Tokens.SharedFolderLocation.encode(message.sharedFolderLocation, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            if (message.sharedFolderRecordLocation != null && Object.hasOwnProperty.call(message, "sharedFolderRecordLocation"))
                $root.Tokens.SharedFolderRecordLocation.encode(message.sharedFolderRecordLocation, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
            if (message.sharedFolderTeamLocation != null && Object.hasOwnProperty.call(message, "sharedFolderTeamLocation"))
                $root.Tokens.SharedFolderTeamLocation.encode(message.sharedFolderTeamLocation, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
            if (message.sharedFolderUserLocation != null && Object.hasOwnProperty.call(message, "sharedFolderUserLocation"))
                $root.Tokens.SharedFolderUserLocation.encode(message.sharedFolderUserLocation, writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
            if (message.shareInvitationLocation != null && Object.hasOwnProperty.call(message, "shareInvitationLocation"))
                $root.Tokens.ShareInvitationLocation.encode(message.shareInvitationLocation, writer.uint32(/* id 11, wireType 2 =*/90).fork(), q + 1).ldelim();
            if (message.teamLocation != null && Object.hasOwnProperty.call(message, "teamLocation"))
                $root.Tokens.TeamLocation.encode(message.teamLocation, writer.uint32(/* id 12, wireType 2 =*/98).fork(), q + 1).ldelim();
            if (message.userAuthHashLocation != null && Object.hasOwnProperty.call(message, "userAuthHashLocation"))
                $root.Tokens.UserAuthHashLocation.encode(message.userAuthHashLocation, writer.uint32(/* id 13, wireType 2 =*/106).fork(), q + 1).ldelim();
            if (message.ksmLocation != null && Object.hasOwnProperty.call(message, "ksmLocation"))
                $root.Tokens.KsmLocation.encode(message.ksmLocation, writer.uint32(/* id 14, wireType 2 =*/114).fork(), q + 1).ldelim();
            if (message.sharedFolderFolderRecordLocation != null && Object.hasOwnProperty.call(message, "sharedFolderFolderRecordLocation"))
                $root.Tokens.SharedFolderFolderRecordLocation.encode(message.sharedFolderFolderRecordLocation, writer.uint32(/* id 15, wireType 2 =*/122).fork(), q + 1).ldelim();
            if (message.userLocation != null && Object.hasOwnProperty.call(message, "userLocation"))
                $root.Tokens.UserLocation.encode(message.userLocation, writer.uint32(/* id 16, wireType 2 =*/130).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified VaultSyncDownLocations message, length delimited. Does not implicitly {@link Tokens.VaultSyncDownLocations.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.VaultSyncDownLocations
         * @static
         * @param {Tokens.IVaultSyncDownLocations} message VaultSyncDownLocations message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        VaultSyncDownLocations.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a VaultSyncDownLocations message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.VaultSyncDownLocations
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.VaultSyncDownLocations} VaultSyncDownLocations
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VaultSyncDownLocations.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.VaultSyncDownLocations();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.vaultDataEntity = reader.int32();
                        break;
                    }
                case 2: {
                        message.recordLocation = $root.Tokens.RecordLocation.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 3: {
                        message.recordLinkLocation = $root.Tokens.RecordLinkLocation.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 4: {
                        message.folderLocation = $root.Tokens.FolderLocation.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 5: {
                        message.folderRecordLocation = $root.Tokens.FolderRecordLocation.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 6: {
                        message.folderSharedFolderLocation = $root.Tokens.FolderSharedFolderLocation.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 7: {
                        message.sharedFolderLocation = $root.Tokens.SharedFolderLocation.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 8: {
                        message.sharedFolderRecordLocation = $root.Tokens.SharedFolderRecordLocation.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 9: {
                        message.sharedFolderTeamLocation = $root.Tokens.SharedFolderTeamLocation.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 10: {
                        message.sharedFolderUserLocation = $root.Tokens.SharedFolderUserLocation.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 11: {
                        message.shareInvitationLocation = $root.Tokens.ShareInvitationLocation.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 12: {
                        message.teamLocation = $root.Tokens.TeamLocation.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 13: {
                        message.userAuthHashLocation = $root.Tokens.UserAuthHashLocation.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 14: {
                        message.ksmLocation = $root.Tokens.KsmLocation.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 15: {
                        message.sharedFolderFolderRecordLocation = $root.Tokens.SharedFolderFolderRecordLocation.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 16: {
                        message.userLocation = $root.Tokens.UserLocation.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a VaultSyncDownLocations message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.VaultSyncDownLocations
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.VaultSyncDownLocations} VaultSyncDownLocations
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        VaultSyncDownLocations.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a VaultSyncDownLocations message.
         * @function verify
         * @memberof Tokens.VaultSyncDownLocations
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        VaultSyncDownLocations.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.vaultDataEntity != null && Object.hasOwnProperty.call(message, "vaultDataEntity"))
                if (!$util.isInteger(message.vaultDataEntity))
                    return "vaultDataEntity: integer expected";
            if (message.recordLocation != null && Object.hasOwnProperty.call(message, "recordLocation")) {
                let error = $root.Tokens.RecordLocation.verify(message.recordLocation, long + 1);
                if (error)
                    return "recordLocation." + error;
            }
            if (message.recordLinkLocation != null && Object.hasOwnProperty.call(message, "recordLinkLocation")) {
                let error = $root.Tokens.RecordLinkLocation.verify(message.recordLinkLocation, long + 1);
                if (error)
                    return "recordLinkLocation." + error;
            }
            if (message.folderLocation != null && Object.hasOwnProperty.call(message, "folderLocation")) {
                let error = $root.Tokens.FolderLocation.verify(message.folderLocation, long + 1);
                if (error)
                    return "folderLocation." + error;
            }
            if (message.folderRecordLocation != null && Object.hasOwnProperty.call(message, "folderRecordLocation")) {
                let error = $root.Tokens.FolderRecordLocation.verify(message.folderRecordLocation, long + 1);
                if (error)
                    return "folderRecordLocation." + error;
            }
            if (message.folderSharedFolderLocation != null && Object.hasOwnProperty.call(message, "folderSharedFolderLocation")) {
                let error = $root.Tokens.FolderSharedFolderLocation.verify(message.folderSharedFolderLocation, long + 1);
                if (error)
                    return "folderSharedFolderLocation." + error;
            }
            if (message.sharedFolderLocation != null && Object.hasOwnProperty.call(message, "sharedFolderLocation")) {
                let error = $root.Tokens.SharedFolderLocation.verify(message.sharedFolderLocation, long + 1);
                if (error)
                    return "sharedFolderLocation." + error;
            }
            if (message.sharedFolderRecordLocation != null && Object.hasOwnProperty.call(message, "sharedFolderRecordLocation")) {
                let error = $root.Tokens.SharedFolderRecordLocation.verify(message.sharedFolderRecordLocation, long + 1);
                if (error)
                    return "sharedFolderRecordLocation." + error;
            }
            if (message.sharedFolderTeamLocation != null && Object.hasOwnProperty.call(message, "sharedFolderTeamLocation")) {
                let error = $root.Tokens.SharedFolderTeamLocation.verify(message.sharedFolderTeamLocation, long + 1);
                if (error)
                    return "sharedFolderTeamLocation." + error;
            }
            if (message.sharedFolderUserLocation != null && Object.hasOwnProperty.call(message, "sharedFolderUserLocation")) {
                let error = $root.Tokens.SharedFolderUserLocation.verify(message.sharedFolderUserLocation, long + 1);
                if (error)
                    return "sharedFolderUserLocation." + error;
            }
            if (message.shareInvitationLocation != null && Object.hasOwnProperty.call(message, "shareInvitationLocation")) {
                let error = $root.Tokens.ShareInvitationLocation.verify(message.shareInvitationLocation, long + 1);
                if (error)
                    return "shareInvitationLocation." + error;
            }
            if (message.teamLocation != null && Object.hasOwnProperty.call(message, "teamLocation")) {
                let error = $root.Tokens.TeamLocation.verify(message.teamLocation, long + 1);
                if (error)
                    return "teamLocation." + error;
            }
            if (message.userAuthHashLocation != null && Object.hasOwnProperty.call(message, "userAuthHashLocation")) {
                let error = $root.Tokens.UserAuthHashLocation.verify(message.userAuthHashLocation, long + 1);
                if (error)
                    return "userAuthHashLocation." + error;
            }
            if (message.ksmLocation != null && Object.hasOwnProperty.call(message, "ksmLocation")) {
                let error = $root.Tokens.KsmLocation.verify(message.ksmLocation, long + 1);
                if (error)
                    return "ksmLocation." + error;
            }
            if (message.sharedFolderFolderRecordLocation != null && Object.hasOwnProperty.call(message, "sharedFolderFolderRecordLocation")) {
                let error = $root.Tokens.SharedFolderFolderRecordLocation.verify(message.sharedFolderFolderRecordLocation, long + 1);
                if (error)
                    return "sharedFolderFolderRecordLocation." + error;
            }
            if (message.userLocation != null && Object.hasOwnProperty.call(message, "userLocation")) {
                let error = $root.Tokens.UserLocation.verify(message.userLocation, long + 1);
                if (error)
                    return "userLocation." + error;
            }
            return null;
        };

        /**
         * Creates a VaultSyncDownLocations message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.VaultSyncDownLocations
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.VaultSyncDownLocations} VaultSyncDownLocations
         */
        VaultSyncDownLocations.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.VaultSyncDownLocations)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.VaultSyncDownLocations: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.VaultSyncDownLocations();
            if (object.vaultDataEntity != null)
                message.vaultDataEntity = object.vaultDataEntity | 0;
            if (object.recordLocation != null) {
                if (!$util.isObject(object.recordLocation))
                    throw TypeError(".Tokens.VaultSyncDownLocations.recordLocation: object expected");
                message.recordLocation = $root.Tokens.RecordLocation.fromObject(object.recordLocation, long + 1);
            }
            if (object.recordLinkLocation != null) {
                if (!$util.isObject(object.recordLinkLocation))
                    throw TypeError(".Tokens.VaultSyncDownLocations.recordLinkLocation: object expected");
                message.recordLinkLocation = $root.Tokens.RecordLinkLocation.fromObject(object.recordLinkLocation, long + 1);
            }
            if (object.folderLocation != null) {
                if (!$util.isObject(object.folderLocation))
                    throw TypeError(".Tokens.VaultSyncDownLocations.folderLocation: object expected");
                message.folderLocation = $root.Tokens.FolderLocation.fromObject(object.folderLocation, long + 1);
            }
            if (object.folderRecordLocation != null) {
                if (!$util.isObject(object.folderRecordLocation))
                    throw TypeError(".Tokens.VaultSyncDownLocations.folderRecordLocation: object expected");
                message.folderRecordLocation = $root.Tokens.FolderRecordLocation.fromObject(object.folderRecordLocation, long + 1);
            }
            if (object.folderSharedFolderLocation != null) {
                if (!$util.isObject(object.folderSharedFolderLocation))
                    throw TypeError(".Tokens.VaultSyncDownLocations.folderSharedFolderLocation: object expected");
                message.folderSharedFolderLocation = $root.Tokens.FolderSharedFolderLocation.fromObject(object.folderSharedFolderLocation, long + 1);
            }
            if (object.sharedFolderLocation != null) {
                if (!$util.isObject(object.sharedFolderLocation))
                    throw TypeError(".Tokens.VaultSyncDownLocations.sharedFolderLocation: object expected");
                message.sharedFolderLocation = $root.Tokens.SharedFolderLocation.fromObject(object.sharedFolderLocation, long + 1);
            }
            if (object.sharedFolderRecordLocation != null) {
                if (!$util.isObject(object.sharedFolderRecordLocation))
                    throw TypeError(".Tokens.VaultSyncDownLocations.sharedFolderRecordLocation: object expected");
                message.sharedFolderRecordLocation = $root.Tokens.SharedFolderRecordLocation.fromObject(object.sharedFolderRecordLocation, long + 1);
            }
            if (object.sharedFolderTeamLocation != null) {
                if (!$util.isObject(object.sharedFolderTeamLocation))
                    throw TypeError(".Tokens.VaultSyncDownLocations.sharedFolderTeamLocation: object expected");
                message.sharedFolderTeamLocation = $root.Tokens.SharedFolderTeamLocation.fromObject(object.sharedFolderTeamLocation, long + 1);
            }
            if (object.sharedFolderUserLocation != null) {
                if (!$util.isObject(object.sharedFolderUserLocation))
                    throw TypeError(".Tokens.VaultSyncDownLocations.sharedFolderUserLocation: object expected");
                message.sharedFolderUserLocation = $root.Tokens.SharedFolderUserLocation.fromObject(object.sharedFolderUserLocation, long + 1);
            }
            if (object.shareInvitationLocation != null) {
                if (!$util.isObject(object.shareInvitationLocation))
                    throw TypeError(".Tokens.VaultSyncDownLocations.shareInvitationLocation: object expected");
                message.shareInvitationLocation = $root.Tokens.ShareInvitationLocation.fromObject(object.shareInvitationLocation, long + 1);
            }
            if (object.teamLocation != null) {
                if (!$util.isObject(object.teamLocation))
                    throw TypeError(".Tokens.VaultSyncDownLocations.teamLocation: object expected");
                message.teamLocation = $root.Tokens.TeamLocation.fromObject(object.teamLocation, long + 1);
            }
            if (object.userAuthHashLocation != null) {
                if (!$util.isObject(object.userAuthHashLocation))
                    throw TypeError(".Tokens.VaultSyncDownLocations.userAuthHashLocation: object expected");
                message.userAuthHashLocation = $root.Tokens.UserAuthHashLocation.fromObject(object.userAuthHashLocation, long + 1);
            }
            if (object.ksmLocation != null) {
                if (!$util.isObject(object.ksmLocation))
                    throw TypeError(".Tokens.VaultSyncDownLocations.ksmLocation: object expected");
                message.ksmLocation = $root.Tokens.KsmLocation.fromObject(object.ksmLocation, long + 1);
            }
            if (object.sharedFolderFolderRecordLocation != null) {
                if (!$util.isObject(object.sharedFolderFolderRecordLocation))
                    throw TypeError(".Tokens.VaultSyncDownLocations.sharedFolderFolderRecordLocation: object expected");
                message.sharedFolderFolderRecordLocation = $root.Tokens.SharedFolderFolderRecordLocation.fromObject(object.sharedFolderFolderRecordLocation, long + 1);
            }
            if (object.userLocation != null) {
                if (!$util.isObject(object.userLocation))
                    throw TypeError(".Tokens.VaultSyncDownLocations.userLocation: object expected");
                message.userLocation = $root.Tokens.UserLocation.fromObject(object.userLocation, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a VaultSyncDownLocations message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.VaultSyncDownLocations
         * @static
         * @param {Tokens.VaultSyncDownLocations} message VaultSyncDownLocations
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        VaultSyncDownLocations.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.vaultDataEntity = 0;
                object.recordLocation = null;
                object.recordLinkLocation = null;
                object.folderLocation = null;
                object.folderRecordLocation = null;
                object.folderSharedFolderLocation = null;
                object.sharedFolderLocation = null;
                object.sharedFolderRecordLocation = null;
                object.sharedFolderTeamLocation = null;
                object.sharedFolderUserLocation = null;
                object.shareInvitationLocation = null;
                object.teamLocation = null;
                object.userAuthHashLocation = null;
                object.ksmLocation = null;
                object.sharedFolderFolderRecordLocation = null;
                object.userLocation = null;
            }
            if (message.vaultDataEntity != null && Object.hasOwnProperty.call(message, "vaultDataEntity"))
                object.vaultDataEntity = message.vaultDataEntity;
            if (message.recordLocation != null && Object.hasOwnProperty.call(message, "recordLocation"))
                object.recordLocation = $root.Tokens.RecordLocation.toObject(message.recordLocation, options, q + 1);
            if (message.recordLinkLocation != null && Object.hasOwnProperty.call(message, "recordLinkLocation"))
                object.recordLinkLocation = $root.Tokens.RecordLinkLocation.toObject(message.recordLinkLocation, options, q + 1);
            if (message.folderLocation != null && Object.hasOwnProperty.call(message, "folderLocation"))
                object.folderLocation = $root.Tokens.FolderLocation.toObject(message.folderLocation, options, q + 1);
            if (message.folderRecordLocation != null && Object.hasOwnProperty.call(message, "folderRecordLocation"))
                object.folderRecordLocation = $root.Tokens.FolderRecordLocation.toObject(message.folderRecordLocation, options, q + 1);
            if (message.folderSharedFolderLocation != null && Object.hasOwnProperty.call(message, "folderSharedFolderLocation"))
                object.folderSharedFolderLocation = $root.Tokens.FolderSharedFolderLocation.toObject(message.folderSharedFolderLocation, options, q + 1);
            if (message.sharedFolderLocation != null && Object.hasOwnProperty.call(message, "sharedFolderLocation"))
                object.sharedFolderLocation = $root.Tokens.SharedFolderLocation.toObject(message.sharedFolderLocation, options, q + 1);
            if (message.sharedFolderRecordLocation != null && Object.hasOwnProperty.call(message, "sharedFolderRecordLocation"))
                object.sharedFolderRecordLocation = $root.Tokens.SharedFolderRecordLocation.toObject(message.sharedFolderRecordLocation, options, q + 1);
            if (message.sharedFolderTeamLocation != null && Object.hasOwnProperty.call(message, "sharedFolderTeamLocation"))
                object.sharedFolderTeamLocation = $root.Tokens.SharedFolderTeamLocation.toObject(message.sharedFolderTeamLocation, options, q + 1);
            if (message.sharedFolderUserLocation != null && Object.hasOwnProperty.call(message, "sharedFolderUserLocation"))
                object.sharedFolderUserLocation = $root.Tokens.SharedFolderUserLocation.toObject(message.sharedFolderUserLocation, options, q + 1);
            if (message.shareInvitationLocation != null && Object.hasOwnProperty.call(message, "shareInvitationLocation"))
                object.shareInvitationLocation = $root.Tokens.ShareInvitationLocation.toObject(message.shareInvitationLocation, options, q + 1);
            if (message.teamLocation != null && Object.hasOwnProperty.call(message, "teamLocation"))
                object.teamLocation = $root.Tokens.TeamLocation.toObject(message.teamLocation, options, q + 1);
            if (message.userAuthHashLocation != null && Object.hasOwnProperty.call(message, "userAuthHashLocation"))
                object.userAuthHashLocation = $root.Tokens.UserAuthHashLocation.toObject(message.userAuthHashLocation, options, q + 1);
            if (message.ksmLocation != null && Object.hasOwnProperty.call(message, "ksmLocation"))
                object.ksmLocation = $root.Tokens.KsmLocation.toObject(message.ksmLocation, options, q + 1);
            if (message.sharedFolderFolderRecordLocation != null && Object.hasOwnProperty.call(message, "sharedFolderFolderRecordLocation"))
                object.sharedFolderFolderRecordLocation = $root.Tokens.SharedFolderFolderRecordLocation.toObject(message.sharedFolderFolderRecordLocation, options, q + 1);
            if (message.userLocation != null && Object.hasOwnProperty.call(message, "userLocation"))
                object.userLocation = $root.Tokens.UserLocation.toObject(message.userLocation, options, q + 1);
            return object;
        };

        /**
         * Converts this VaultSyncDownLocations to JSON.
         * @function toJSON
         * @memberof Tokens.VaultSyncDownLocations
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        VaultSyncDownLocations.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for VaultSyncDownLocations
         * @function getTypeUrl
         * @memberof Tokens.VaultSyncDownLocations
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        VaultSyncDownLocations.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.VaultSyncDownLocations";
        };

        return VaultSyncDownLocations;
    })();

    Tokens.RecordLocation = (function() {

        /**
         * Properties of a RecordLocation.
         * @memberof Tokens
         * @interface IRecordLocation
         * @property {Uint8Array|null} [recordUid] RecordLocation recordUid
         * @property {number|null} [lastSyncRevision] RecordLocation lastSyncRevision
         */

        /**
         * Constructs a new RecordLocation.
         * @memberof Tokens
         * @classdesc Represents a RecordLocation.
         * @implements IRecordLocation
         * @constructor
         * @param {Tokens.IRecordLocation=} [properties] Properties to set
         */
        function RecordLocation(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RecordLocation recordUid.
         * @member {Uint8Array} recordUid
         * @memberof Tokens.RecordLocation
         * @instance
         */
        RecordLocation.prototype.recordUid = $util.newBuffer([]);

        /**
         * RecordLocation lastSyncRevision.
         * @member {number} lastSyncRevision
         * @memberof Tokens.RecordLocation
         * @instance
         */
        RecordLocation.prototype.lastSyncRevision = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new RecordLocation instance using the specified properties.
         * @function create
         * @memberof Tokens.RecordLocation
         * @static
         * @param {Tokens.IRecordLocation=} [properties] Properties to set
         * @returns {Tokens.RecordLocation} RecordLocation instance
         */
        RecordLocation.create = function create(properties) {
            return new RecordLocation(properties);
        };

        /**
         * Encodes the specified RecordLocation message. Does not implicitly {@link Tokens.RecordLocation.verify|verify} messages.
         * @function encode
         * @memberof Tokens.RecordLocation
         * @static
         * @param {Tokens.IRecordLocation} message RecordLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RecordLocation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.recordUid);
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.lastSyncRevision);
            return writer;
        };

        /**
         * Encodes the specified RecordLocation message, length delimited. Does not implicitly {@link Tokens.RecordLocation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.RecordLocation
         * @static
         * @param {Tokens.IRecordLocation} message RecordLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RecordLocation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RecordLocation message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.RecordLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.RecordLocation} RecordLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RecordLocation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.RecordLocation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.recordUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.lastSyncRevision = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RecordLocation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.RecordLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.RecordLocation} RecordLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RecordLocation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RecordLocation message.
         * @function verify
         * @memberof Tokens.RecordLocation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RecordLocation.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                if (!(message.recordUid && typeof message.recordUid.length === "number" || $util.isString(message.recordUid)))
                    return "recordUid: buffer expected";
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (!$util.isInteger(message.lastSyncRevision) && !(message.lastSyncRevision && $util.isInteger(message.lastSyncRevision.low) && $util.isInteger(message.lastSyncRevision.high)))
                    return "lastSyncRevision: integer|Long expected";
            return null;
        };

        /**
         * Creates a RecordLocation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.RecordLocation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.RecordLocation} RecordLocation
         */
        RecordLocation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.RecordLocation)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.RecordLocation: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.RecordLocation();
            if (object.recordUid != null)
                if (typeof object.recordUid === "string")
                    $util.base64.decode(object.recordUid, message.recordUid = $util.newBuffer($util.base64.length(object.recordUid)), 0);
                else if (object.recordUid.length >= 0)
                    message.recordUid = object.recordUid;
            if (object.lastSyncRevision != null)
                if ($util.Long)
                    message.lastSyncRevision = $util.Long.fromValue(object.lastSyncRevision, false);
                else if (typeof object.lastSyncRevision === "string")
                    message.lastSyncRevision = parseInt(object.lastSyncRevision, 10);
                else if (typeof object.lastSyncRevision === "number")
                    message.lastSyncRevision = object.lastSyncRevision;
                else if (typeof object.lastSyncRevision === "object")
                    message.lastSyncRevision = new $util.LongBits(object.lastSyncRevision.low >>> 0, object.lastSyncRevision.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a RecordLocation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.RecordLocation
         * @static
         * @param {Tokens.RecordLocation} message RecordLocation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RecordLocation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.recordUid = "";
                else {
                    object.recordUid = [];
                    if (options.bytes !== Array)
                        object.recordUid = $util.newBuffer(object.recordUid);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastSyncRevision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastSyncRevision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                object.recordUid = options.bytes === String ? $util.base64.encode(message.recordUid, 0, message.recordUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.recordUid) : message.recordUid;
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastSyncRevision = typeof message.lastSyncRevision === "number" ? BigInt(message.lastSyncRevision) : $util.Long.fromBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0, false).toBigInt();
                else if (typeof message.lastSyncRevision === "number")
                    object.lastSyncRevision = options.longs === String ? String(message.lastSyncRevision) : message.lastSyncRevision;
                else
                    object.lastSyncRevision = options.longs === String ? $util.Long.prototype.toString.call(message.lastSyncRevision) : options.longs === Number ? new $util.LongBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0).toNumber() : message.lastSyncRevision;
            return object;
        };

        /**
         * Converts this RecordLocation to JSON.
         * @function toJSON
         * @memberof Tokens.RecordLocation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RecordLocation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RecordLocation
         * @function getTypeUrl
         * @memberof Tokens.RecordLocation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RecordLocation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.RecordLocation";
        };

        return RecordLocation;
    })();

    Tokens.FolderLocation = (function() {

        /**
         * Properties of a FolderLocation.
         * @memberof Tokens
         * @interface IFolderLocation
         * @property {Uint8Array|null} [folderUid] FolderLocation folderUid
         * @property {number|null} [lastSyncRevision] FolderLocation lastSyncRevision
         */

        /**
         * Constructs a new FolderLocation.
         * @memberof Tokens
         * @classdesc Represents a FolderLocation.
         * @implements IFolderLocation
         * @constructor
         * @param {Tokens.IFolderLocation=} [properties] Properties to set
         */
        function FolderLocation(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FolderLocation folderUid.
         * @member {Uint8Array} folderUid
         * @memberof Tokens.FolderLocation
         * @instance
         */
        FolderLocation.prototype.folderUid = $util.newBuffer([]);

        /**
         * FolderLocation lastSyncRevision.
         * @member {number} lastSyncRevision
         * @memberof Tokens.FolderLocation
         * @instance
         */
        FolderLocation.prototype.lastSyncRevision = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new FolderLocation instance using the specified properties.
         * @function create
         * @memberof Tokens.FolderLocation
         * @static
         * @param {Tokens.IFolderLocation=} [properties] Properties to set
         * @returns {Tokens.FolderLocation} FolderLocation instance
         */
        FolderLocation.create = function create(properties) {
            return new FolderLocation(properties);
        };

        /**
         * Encodes the specified FolderLocation message. Does not implicitly {@link Tokens.FolderLocation.verify|verify} messages.
         * @function encode
         * @memberof Tokens.FolderLocation
         * @static
         * @param {Tokens.IFolderLocation} message FolderLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FolderLocation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.folderUid != null && Object.hasOwnProperty.call(message, "folderUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.folderUid);
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.lastSyncRevision);
            return writer;
        };

        /**
         * Encodes the specified FolderLocation message, length delimited. Does not implicitly {@link Tokens.FolderLocation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.FolderLocation
         * @static
         * @param {Tokens.IFolderLocation} message FolderLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FolderLocation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FolderLocation message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.FolderLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.FolderLocation} FolderLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FolderLocation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.FolderLocation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.folderUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.lastSyncRevision = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FolderLocation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.FolderLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.FolderLocation} FolderLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FolderLocation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FolderLocation message.
         * @function verify
         * @memberof Tokens.FolderLocation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FolderLocation.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.folderUid != null && Object.hasOwnProperty.call(message, "folderUid"))
                if (!(message.folderUid && typeof message.folderUid.length === "number" || $util.isString(message.folderUid)))
                    return "folderUid: buffer expected";
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (!$util.isInteger(message.lastSyncRevision) && !(message.lastSyncRevision && $util.isInteger(message.lastSyncRevision.low) && $util.isInteger(message.lastSyncRevision.high)))
                    return "lastSyncRevision: integer|Long expected";
            return null;
        };

        /**
         * Creates a FolderLocation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.FolderLocation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.FolderLocation} FolderLocation
         */
        FolderLocation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.FolderLocation)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.FolderLocation: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.FolderLocation();
            if (object.folderUid != null)
                if (typeof object.folderUid === "string")
                    $util.base64.decode(object.folderUid, message.folderUid = $util.newBuffer($util.base64.length(object.folderUid)), 0);
                else if (object.folderUid.length >= 0)
                    message.folderUid = object.folderUid;
            if (object.lastSyncRevision != null)
                if ($util.Long)
                    message.lastSyncRevision = $util.Long.fromValue(object.lastSyncRevision, false);
                else if (typeof object.lastSyncRevision === "string")
                    message.lastSyncRevision = parseInt(object.lastSyncRevision, 10);
                else if (typeof object.lastSyncRevision === "number")
                    message.lastSyncRevision = object.lastSyncRevision;
                else if (typeof object.lastSyncRevision === "object")
                    message.lastSyncRevision = new $util.LongBits(object.lastSyncRevision.low >>> 0, object.lastSyncRevision.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a FolderLocation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.FolderLocation
         * @static
         * @param {Tokens.FolderLocation} message FolderLocation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FolderLocation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.folderUid = "";
                else {
                    object.folderUid = [];
                    if (options.bytes !== Array)
                        object.folderUid = $util.newBuffer(object.folderUid);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastSyncRevision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastSyncRevision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.folderUid != null && Object.hasOwnProperty.call(message, "folderUid"))
                object.folderUid = options.bytes === String ? $util.base64.encode(message.folderUid, 0, message.folderUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.folderUid) : message.folderUid;
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastSyncRevision = typeof message.lastSyncRevision === "number" ? BigInt(message.lastSyncRevision) : $util.Long.fromBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0, false).toBigInt();
                else if (typeof message.lastSyncRevision === "number")
                    object.lastSyncRevision = options.longs === String ? String(message.lastSyncRevision) : message.lastSyncRevision;
                else
                    object.lastSyncRevision = options.longs === String ? $util.Long.prototype.toString.call(message.lastSyncRevision) : options.longs === Number ? new $util.LongBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0).toNumber() : message.lastSyncRevision;
            return object;
        };

        /**
         * Converts this FolderLocation to JSON.
         * @function toJSON
         * @memberof Tokens.FolderLocation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FolderLocation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FolderLocation
         * @function getTypeUrl
         * @memberof Tokens.FolderLocation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FolderLocation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.FolderLocation";
        };

        return FolderLocation;
    })();

    Tokens.SharedFolderLocation = (function() {

        /**
         * Properties of a SharedFolderLocation.
         * @memberof Tokens
         * @interface ISharedFolderLocation
         * @property {Uint8Array|null} [sharedFolderUid] SharedFolderLocation sharedFolderUid
         * @property {number|null} [lastSyncRevision] SharedFolderLocation lastSyncRevision
         */

        /**
         * Constructs a new SharedFolderLocation.
         * @memberof Tokens
         * @classdesc Represents a SharedFolderLocation.
         * @implements ISharedFolderLocation
         * @constructor
         * @param {Tokens.ISharedFolderLocation=} [properties] Properties to set
         */
        function SharedFolderLocation(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SharedFolderLocation sharedFolderUid.
         * @member {Uint8Array} sharedFolderUid
         * @memberof Tokens.SharedFolderLocation
         * @instance
         */
        SharedFolderLocation.prototype.sharedFolderUid = $util.newBuffer([]);

        /**
         * SharedFolderLocation lastSyncRevision.
         * @member {number} lastSyncRevision
         * @memberof Tokens.SharedFolderLocation
         * @instance
         */
        SharedFolderLocation.prototype.lastSyncRevision = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new SharedFolderLocation instance using the specified properties.
         * @function create
         * @memberof Tokens.SharedFolderLocation
         * @static
         * @param {Tokens.ISharedFolderLocation=} [properties] Properties to set
         * @returns {Tokens.SharedFolderLocation} SharedFolderLocation instance
         */
        SharedFolderLocation.create = function create(properties) {
            return new SharedFolderLocation(properties);
        };

        /**
         * Encodes the specified SharedFolderLocation message. Does not implicitly {@link Tokens.SharedFolderLocation.verify|verify} messages.
         * @function encode
         * @memberof Tokens.SharedFolderLocation
         * @static
         * @param {Tokens.ISharedFolderLocation} message SharedFolderLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SharedFolderLocation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.sharedFolderUid != null && Object.hasOwnProperty.call(message, "sharedFolderUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.sharedFolderUid);
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.lastSyncRevision);
            return writer;
        };

        /**
         * Encodes the specified SharedFolderLocation message, length delimited. Does not implicitly {@link Tokens.SharedFolderLocation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.SharedFolderLocation
         * @static
         * @param {Tokens.ISharedFolderLocation} message SharedFolderLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SharedFolderLocation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SharedFolderLocation message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.SharedFolderLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.SharedFolderLocation} SharedFolderLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SharedFolderLocation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.SharedFolderLocation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.sharedFolderUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.lastSyncRevision = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SharedFolderLocation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.SharedFolderLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.SharedFolderLocation} SharedFolderLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SharedFolderLocation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SharedFolderLocation message.
         * @function verify
         * @memberof Tokens.SharedFolderLocation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SharedFolderLocation.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.sharedFolderUid != null && Object.hasOwnProperty.call(message, "sharedFolderUid"))
                if (!(message.sharedFolderUid && typeof message.sharedFolderUid.length === "number" || $util.isString(message.sharedFolderUid)))
                    return "sharedFolderUid: buffer expected";
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (!$util.isInteger(message.lastSyncRevision) && !(message.lastSyncRevision && $util.isInteger(message.lastSyncRevision.low) && $util.isInteger(message.lastSyncRevision.high)))
                    return "lastSyncRevision: integer|Long expected";
            return null;
        };

        /**
         * Creates a SharedFolderLocation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.SharedFolderLocation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.SharedFolderLocation} SharedFolderLocation
         */
        SharedFolderLocation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.SharedFolderLocation)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.SharedFolderLocation: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.SharedFolderLocation();
            if (object.sharedFolderUid != null)
                if (typeof object.sharedFolderUid === "string")
                    $util.base64.decode(object.sharedFolderUid, message.sharedFolderUid = $util.newBuffer($util.base64.length(object.sharedFolderUid)), 0);
                else if (object.sharedFolderUid.length >= 0)
                    message.sharedFolderUid = object.sharedFolderUid;
            if (object.lastSyncRevision != null)
                if ($util.Long)
                    message.lastSyncRevision = $util.Long.fromValue(object.lastSyncRevision, false);
                else if (typeof object.lastSyncRevision === "string")
                    message.lastSyncRevision = parseInt(object.lastSyncRevision, 10);
                else if (typeof object.lastSyncRevision === "number")
                    message.lastSyncRevision = object.lastSyncRevision;
                else if (typeof object.lastSyncRevision === "object")
                    message.lastSyncRevision = new $util.LongBits(object.lastSyncRevision.low >>> 0, object.lastSyncRevision.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a SharedFolderLocation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.SharedFolderLocation
         * @static
         * @param {Tokens.SharedFolderLocation} message SharedFolderLocation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SharedFolderLocation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.sharedFolderUid = "";
                else {
                    object.sharedFolderUid = [];
                    if (options.bytes !== Array)
                        object.sharedFolderUid = $util.newBuffer(object.sharedFolderUid);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastSyncRevision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastSyncRevision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.sharedFolderUid != null && Object.hasOwnProperty.call(message, "sharedFolderUid"))
                object.sharedFolderUid = options.bytes === String ? $util.base64.encode(message.sharedFolderUid, 0, message.sharedFolderUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.sharedFolderUid) : message.sharedFolderUid;
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastSyncRevision = typeof message.lastSyncRevision === "number" ? BigInt(message.lastSyncRevision) : $util.Long.fromBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0, false).toBigInt();
                else if (typeof message.lastSyncRevision === "number")
                    object.lastSyncRevision = options.longs === String ? String(message.lastSyncRevision) : message.lastSyncRevision;
                else
                    object.lastSyncRevision = options.longs === String ? $util.Long.prototype.toString.call(message.lastSyncRevision) : options.longs === Number ? new $util.LongBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0).toNumber() : message.lastSyncRevision;
            return object;
        };

        /**
         * Converts this SharedFolderLocation to JSON.
         * @function toJSON
         * @memberof Tokens.SharedFolderLocation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SharedFolderLocation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SharedFolderLocation
         * @function getTypeUrl
         * @memberof Tokens.SharedFolderLocation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SharedFolderLocation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.SharedFolderLocation";
        };

        return SharedFolderLocation;
    })();

    Tokens.SharedFolderUserLocation = (function() {

        /**
         * Properties of a SharedFolderUserLocation.
         * @memberof Tokens
         * @interface ISharedFolderUserLocation
         * @property {Uint8Array|null} [sharedFolderUid] SharedFolderUserLocation sharedFolderUid
         * @property {string|null} [username] SharedFolderUserLocation username
         * @property {number|null} [lastSyncRevision] SharedFolderUserLocation lastSyncRevision
         * @property {Uint8Array|null} [accountUid] SharedFolderUserLocation accountUid
         */

        /**
         * Constructs a new SharedFolderUserLocation.
         * @memberof Tokens
         * @classdesc Represents a SharedFolderUserLocation.
         * @implements ISharedFolderUserLocation
         * @constructor
         * @param {Tokens.ISharedFolderUserLocation=} [properties] Properties to set
         */
        function SharedFolderUserLocation(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SharedFolderUserLocation sharedFolderUid.
         * @member {Uint8Array} sharedFolderUid
         * @memberof Tokens.SharedFolderUserLocation
         * @instance
         */
        SharedFolderUserLocation.prototype.sharedFolderUid = $util.newBuffer([]);

        /**
         * SharedFolderUserLocation username.
         * @member {string} username
         * @memberof Tokens.SharedFolderUserLocation
         * @instance
         */
        SharedFolderUserLocation.prototype.username = "";

        /**
         * SharedFolderUserLocation lastSyncRevision.
         * @member {number} lastSyncRevision
         * @memberof Tokens.SharedFolderUserLocation
         * @instance
         */
        SharedFolderUserLocation.prototype.lastSyncRevision = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * SharedFolderUserLocation accountUid.
         * @member {Uint8Array} accountUid
         * @memberof Tokens.SharedFolderUserLocation
         * @instance
         */
        SharedFolderUserLocation.prototype.accountUid = $util.newBuffer([]);

        /**
         * Creates a new SharedFolderUserLocation instance using the specified properties.
         * @function create
         * @memberof Tokens.SharedFolderUserLocation
         * @static
         * @param {Tokens.ISharedFolderUserLocation=} [properties] Properties to set
         * @returns {Tokens.SharedFolderUserLocation} SharedFolderUserLocation instance
         */
        SharedFolderUserLocation.create = function create(properties) {
            return new SharedFolderUserLocation(properties);
        };

        /**
         * Encodes the specified SharedFolderUserLocation message. Does not implicitly {@link Tokens.SharedFolderUserLocation.verify|verify} messages.
         * @function encode
         * @memberof Tokens.SharedFolderUserLocation
         * @static
         * @param {Tokens.ISharedFolderUserLocation} message SharedFolderUserLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SharedFolderUserLocation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.sharedFolderUid != null && Object.hasOwnProperty.call(message, "sharedFolderUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.sharedFolderUid);
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.username);
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.lastSyncRevision);
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                writer.uint32(/* id 4, wireType 2 =*/34).bytes(message.accountUid);
            return writer;
        };

        /**
         * Encodes the specified SharedFolderUserLocation message, length delimited. Does not implicitly {@link Tokens.SharedFolderUserLocation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.SharedFolderUserLocation
         * @static
         * @param {Tokens.ISharedFolderUserLocation} message SharedFolderUserLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SharedFolderUserLocation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SharedFolderUserLocation message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.SharedFolderUserLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.SharedFolderUserLocation} SharedFolderUserLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SharedFolderUserLocation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.SharedFolderUserLocation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.sharedFolderUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.username = reader.string();
                        break;
                    }
                case 3: {
                        message.lastSyncRevision = reader.int64();
                        break;
                    }
                case 4: {
                        message.accountUid = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SharedFolderUserLocation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.SharedFolderUserLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.SharedFolderUserLocation} SharedFolderUserLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SharedFolderUserLocation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SharedFolderUserLocation message.
         * @function verify
         * @memberof Tokens.SharedFolderUserLocation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SharedFolderUserLocation.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.sharedFolderUid != null && Object.hasOwnProperty.call(message, "sharedFolderUid"))
                if (!(message.sharedFolderUid && typeof message.sharedFolderUid.length === "number" || $util.isString(message.sharedFolderUid)))
                    return "sharedFolderUid: buffer expected";
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                if (!$util.isString(message.username))
                    return "username: string expected";
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (!$util.isInteger(message.lastSyncRevision) && !(message.lastSyncRevision && $util.isInteger(message.lastSyncRevision.low) && $util.isInteger(message.lastSyncRevision.high)))
                    return "lastSyncRevision: integer|Long expected";
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                if (!(message.accountUid && typeof message.accountUid.length === "number" || $util.isString(message.accountUid)))
                    return "accountUid: buffer expected";
            return null;
        };

        /**
         * Creates a SharedFolderUserLocation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.SharedFolderUserLocation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.SharedFolderUserLocation} SharedFolderUserLocation
         */
        SharedFolderUserLocation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.SharedFolderUserLocation)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.SharedFolderUserLocation: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.SharedFolderUserLocation();
            if (object.sharedFolderUid != null)
                if (typeof object.sharedFolderUid === "string")
                    $util.base64.decode(object.sharedFolderUid, message.sharedFolderUid = $util.newBuffer($util.base64.length(object.sharedFolderUid)), 0);
                else if (object.sharedFolderUid.length >= 0)
                    message.sharedFolderUid = object.sharedFolderUid;
            if (object.username != null)
                message.username = String(object.username);
            if (object.lastSyncRevision != null)
                if ($util.Long)
                    message.lastSyncRevision = $util.Long.fromValue(object.lastSyncRevision, false);
                else if (typeof object.lastSyncRevision === "string")
                    message.lastSyncRevision = parseInt(object.lastSyncRevision, 10);
                else if (typeof object.lastSyncRevision === "number")
                    message.lastSyncRevision = object.lastSyncRevision;
                else if (typeof object.lastSyncRevision === "object")
                    message.lastSyncRevision = new $util.LongBits(object.lastSyncRevision.low >>> 0, object.lastSyncRevision.high >>> 0).toNumber();
            if (object.accountUid != null)
                if (typeof object.accountUid === "string")
                    $util.base64.decode(object.accountUid, message.accountUid = $util.newBuffer($util.base64.length(object.accountUid)), 0);
                else if (object.accountUid.length >= 0)
                    message.accountUid = object.accountUid;
            return message;
        };

        /**
         * Creates a plain object from a SharedFolderUserLocation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.SharedFolderUserLocation
         * @static
         * @param {Tokens.SharedFolderUserLocation} message SharedFolderUserLocation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SharedFolderUserLocation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.sharedFolderUid = "";
                else {
                    object.sharedFolderUid = [];
                    if (options.bytes !== Array)
                        object.sharedFolderUid = $util.newBuffer(object.sharedFolderUid);
                }
                object.username = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastSyncRevision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastSyncRevision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.accountUid = "";
                else {
                    object.accountUid = [];
                    if (options.bytes !== Array)
                        object.accountUid = $util.newBuffer(object.accountUid);
                }
            }
            if (message.sharedFolderUid != null && Object.hasOwnProperty.call(message, "sharedFolderUid"))
                object.sharedFolderUid = options.bytes === String ? $util.base64.encode(message.sharedFolderUid, 0, message.sharedFolderUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.sharedFolderUid) : message.sharedFolderUid;
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastSyncRevision = typeof message.lastSyncRevision === "number" ? BigInt(message.lastSyncRevision) : $util.Long.fromBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0, false).toBigInt();
                else if (typeof message.lastSyncRevision === "number")
                    object.lastSyncRevision = options.longs === String ? String(message.lastSyncRevision) : message.lastSyncRevision;
                else
                    object.lastSyncRevision = options.longs === String ? $util.Long.prototype.toString.call(message.lastSyncRevision) : options.longs === Number ? new $util.LongBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0).toNumber() : message.lastSyncRevision;
            if (message.accountUid != null && Object.hasOwnProperty.call(message, "accountUid"))
                object.accountUid = options.bytes === String ? $util.base64.encode(message.accountUid, 0, message.accountUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.accountUid) : message.accountUid;
            return object;
        };

        /**
         * Converts this SharedFolderUserLocation to JSON.
         * @function toJSON
         * @memberof Tokens.SharedFolderUserLocation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SharedFolderUserLocation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SharedFolderUserLocation
         * @function getTypeUrl
         * @memberof Tokens.SharedFolderUserLocation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SharedFolderUserLocation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.SharedFolderUserLocation";
        };

        return SharedFolderUserLocation;
    })();

    Tokens.SharedFolderTeamLocation = (function() {

        /**
         * Properties of a SharedFolderTeamLocation.
         * @memberof Tokens
         * @interface ISharedFolderTeamLocation
         * @property {Uint8Array|null} [sharedFolderUid] SharedFolderTeamLocation sharedFolderUid
         * @property {Uint8Array|null} [teamUid] SharedFolderTeamLocation teamUid
         * @property {number|null} [lastSyncRevision] SharedFolderTeamLocation lastSyncRevision
         */

        /**
         * Constructs a new SharedFolderTeamLocation.
         * @memberof Tokens
         * @classdesc Represents a SharedFolderTeamLocation.
         * @implements ISharedFolderTeamLocation
         * @constructor
         * @param {Tokens.ISharedFolderTeamLocation=} [properties] Properties to set
         */
        function SharedFolderTeamLocation(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SharedFolderTeamLocation sharedFolderUid.
         * @member {Uint8Array} sharedFolderUid
         * @memberof Tokens.SharedFolderTeamLocation
         * @instance
         */
        SharedFolderTeamLocation.prototype.sharedFolderUid = $util.newBuffer([]);

        /**
         * SharedFolderTeamLocation teamUid.
         * @member {Uint8Array} teamUid
         * @memberof Tokens.SharedFolderTeamLocation
         * @instance
         */
        SharedFolderTeamLocation.prototype.teamUid = $util.newBuffer([]);

        /**
         * SharedFolderTeamLocation lastSyncRevision.
         * @member {number} lastSyncRevision
         * @memberof Tokens.SharedFolderTeamLocation
         * @instance
         */
        SharedFolderTeamLocation.prototype.lastSyncRevision = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new SharedFolderTeamLocation instance using the specified properties.
         * @function create
         * @memberof Tokens.SharedFolderTeamLocation
         * @static
         * @param {Tokens.ISharedFolderTeamLocation=} [properties] Properties to set
         * @returns {Tokens.SharedFolderTeamLocation} SharedFolderTeamLocation instance
         */
        SharedFolderTeamLocation.create = function create(properties) {
            return new SharedFolderTeamLocation(properties);
        };

        /**
         * Encodes the specified SharedFolderTeamLocation message. Does not implicitly {@link Tokens.SharedFolderTeamLocation.verify|verify} messages.
         * @function encode
         * @memberof Tokens.SharedFolderTeamLocation
         * @static
         * @param {Tokens.ISharedFolderTeamLocation} message SharedFolderTeamLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SharedFolderTeamLocation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.sharedFolderUid != null && Object.hasOwnProperty.call(message, "sharedFolderUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.sharedFolderUid);
            if (message.teamUid != null && Object.hasOwnProperty.call(message, "teamUid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.teamUid);
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.lastSyncRevision);
            return writer;
        };

        /**
         * Encodes the specified SharedFolderTeamLocation message, length delimited. Does not implicitly {@link Tokens.SharedFolderTeamLocation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.SharedFolderTeamLocation
         * @static
         * @param {Tokens.ISharedFolderTeamLocation} message SharedFolderTeamLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SharedFolderTeamLocation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SharedFolderTeamLocation message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.SharedFolderTeamLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.SharedFolderTeamLocation} SharedFolderTeamLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SharedFolderTeamLocation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.SharedFolderTeamLocation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.sharedFolderUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.teamUid = reader.bytes();
                        break;
                    }
                case 3: {
                        message.lastSyncRevision = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SharedFolderTeamLocation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.SharedFolderTeamLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.SharedFolderTeamLocation} SharedFolderTeamLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SharedFolderTeamLocation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SharedFolderTeamLocation message.
         * @function verify
         * @memberof Tokens.SharedFolderTeamLocation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SharedFolderTeamLocation.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.sharedFolderUid != null && Object.hasOwnProperty.call(message, "sharedFolderUid"))
                if (!(message.sharedFolderUid && typeof message.sharedFolderUid.length === "number" || $util.isString(message.sharedFolderUid)))
                    return "sharedFolderUid: buffer expected";
            if (message.teamUid != null && Object.hasOwnProperty.call(message, "teamUid"))
                if (!(message.teamUid && typeof message.teamUid.length === "number" || $util.isString(message.teamUid)))
                    return "teamUid: buffer expected";
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (!$util.isInteger(message.lastSyncRevision) && !(message.lastSyncRevision && $util.isInteger(message.lastSyncRevision.low) && $util.isInteger(message.lastSyncRevision.high)))
                    return "lastSyncRevision: integer|Long expected";
            return null;
        };

        /**
         * Creates a SharedFolderTeamLocation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.SharedFolderTeamLocation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.SharedFolderTeamLocation} SharedFolderTeamLocation
         */
        SharedFolderTeamLocation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.SharedFolderTeamLocation)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.SharedFolderTeamLocation: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.SharedFolderTeamLocation();
            if (object.sharedFolderUid != null)
                if (typeof object.sharedFolderUid === "string")
                    $util.base64.decode(object.sharedFolderUid, message.sharedFolderUid = $util.newBuffer($util.base64.length(object.sharedFolderUid)), 0);
                else if (object.sharedFolderUid.length >= 0)
                    message.sharedFolderUid = object.sharedFolderUid;
            if (object.teamUid != null)
                if (typeof object.teamUid === "string")
                    $util.base64.decode(object.teamUid, message.teamUid = $util.newBuffer($util.base64.length(object.teamUid)), 0);
                else if (object.teamUid.length >= 0)
                    message.teamUid = object.teamUid;
            if (object.lastSyncRevision != null)
                if ($util.Long)
                    message.lastSyncRevision = $util.Long.fromValue(object.lastSyncRevision, false);
                else if (typeof object.lastSyncRevision === "string")
                    message.lastSyncRevision = parseInt(object.lastSyncRevision, 10);
                else if (typeof object.lastSyncRevision === "number")
                    message.lastSyncRevision = object.lastSyncRevision;
                else if (typeof object.lastSyncRevision === "object")
                    message.lastSyncRevision = new $util.LongBits(object.lastSyncRevision.low >>> 0, object.lastSyncRevision.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a SharedFolderTeamLocation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.SharedFolderTeamLocation
         * @static
         * @param {Tokens.SharedFolderTeamLocation} message SharedFolderTeamLocation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SharedFolderTeamLocation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.sharedFolderUid = "";
                else {
                    object.sharedFolderUid = [];
                    if (options.bytes !== Array)
                        object.sharedFolderUid = $util.newBuffer(object.sharedFolderUid);
                }
                if (options.bytes === String)
                    object.teamUid = "";
                else {
                    object.teamUid = [];
                    if (options.bytes !== Array)
                        object.teamUid = $util.newBuffer(object.teamUid);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastSyncRevision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastSyncRevision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.sharedFolderUid != null && Object.hasOwnProperty.call(message, "sharedFolderUid"))
                object.sharedFolderUid = options.bytes === String ? $util.base64.encode(message.sharedFolderUid, 0, message.sharedFolderUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.sharedFolderUid) : message.sharedFolderUid;
            if (message.teamUid != null && Object.hasOwnProperty.call(message, "teamUid"))
                object.teamUid = options.bytes === String ? $util.base64.encode(message.teamUid, 0, message.teamUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.teamUid) : message.teamUid;
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastSyncRevision = typeof message.lastSyncRevision === "number" ? BigInt(message.lastSyncRevision) : $util.Long.fromBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0, false).toBigInt();
                else if (typeof message.lastSyncRevision === "number")
                    object.lastSyncRevision = options.longs === String ? String(message.lastSyncRevision) : message.lastSyncRevision;
                else
                    object.lastSyncRevision = options.longs === String ? $util.Long.prototype.toString.call(message.lastSyncRevision) : options.longs === Number ? new $util.LongBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0).toNumber() : message.lastSyncRevision;
            return object;
        };

        /**
         * Converts this SharedFolderTeamLocation to JSON.
         * @function toJSON
         * @memberof Tokens.SharedFolderTeamLocation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SharedFolderTeamLocation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SharedFolderTeamLocation
         * @function getTypeUrl
         * @memberof Tokens.SharedFolderTeamLocation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SharedFolderTeamLocation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.SharedFolderTeamLocation";
        };

        return SharedFolderTeamLocation;
    })();

    Tokens.SharedFolderRecordLocation = (function() {

        /**
         * Properties of a SharedFolderRecordLocation.
         * @memberof Tokens
         * @interface ISharedFolderRecordLocation
         * @property {Uint8Array|null} [sharedFolderUid] SharedFolderRecordLocation sharedFolderUid
         * @property {Uint8Array|null} [recordUid] SharedFolderRecordLocation recordUid
         * @property {number|null} [lastSyncRevision] SharedFolderRecordLocation lastSyncRevision
         */

        /**
         * Constructs a new SharedFolderRecordLocation.
         * @memberof Tokens
         * @classdesc Represents a SharedFolderRecordLocation.
         * @implements ISharedFolderRecordLocation
         * @constructor
         * @param {Tokens.ISharedFolderRecordLocation=} [properties] Properties to set
         */
        function SharedFolderRecordLocation(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SharedFolderRecordLocation sharedFolderUid.
         * @member {Uint8Array} sharedFolderUid
         * @memberof Tokens.SharedFolderRecordLocation
         * @instance
         */
        SharedFolderRecordLocation.prototype.sharedFolderUid = $util.newBuffer([]);

        /**
         * SharedFolderRecordLocation recordUid.
         * @member {Uint8Array} recordUid
         * @memberof Tokens.SharedFolderRecordLocation
         * @instance
         */
        SharedFolderRecordLocation.prototype.recordUid = $util.newBuffer([]);

        /**
         * SharedFolderRecordLocation lastSyncRevision.
         * @member {number} lastSyncRevision
         * @memberof Tokens.SharedFolderRecordLocation
         * @instance
         */
        SharedFolderRecordLocation.prototype.lastSyncRevision = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new SharedFolderRecordLocation instance using the specified properties.
         * @function create
         * @memberof Tokens.SharedFolderRecordLocation
         * @static
         * @param {Tokens.ISharedFolderRecordLocation=} [properties] Properties to set
         * @returns {Tokens.SharedFolderRecordLocation} SharedFolderRecordLocation instance
         */
        SharedFolderRecordLocation.create = function create(properties) {
            return new SharedFolderRecordLocation(properties);
        };

        /**
         * Encodes the specified SharedFolderRecordLocation message. Does not implicitly {@link Tokens.SharedFolderRecordLocation.verify|verify} messages.
         * @function encode
         * @memberof Tokens.SharedFolderRecordLocation
         * @static
         * @param {Tokens.ISharedFolderRecordLocation} message SharedFolderRecordLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SharedFolderRecordLocation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.sharedFolderUid != null && Object.hasOwnProperty.call(message, "sharedFolderUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.sharedFolderUid);
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.recordUid);
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.lastSyncRevision);
            return writer;
        };

        /**
         * Encodes the specified SharedFolderRecordLocation message, length delimited. Does not implicitly {@link Tokens.SharedFolderRecordLocation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.SharedFolderRecordLocation
         * @static
         * @param {Tokens.ISharedFolderRecordLocation} message SharedFolderRecordLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SharedFolderRecordLocation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SharedFolderRecordLocation message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.SharedFolderRecordLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.SharedFolderRecordLocation} SharedFolderRecordLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SharedFolderRecordLocation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.SharedFolderRecordLocation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.sharedFolderUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.recordUid = reader.bytes();
                        break;
                    }
                case 3: {
                        message.lastSyncRevision = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SharedFolderRecordLocation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.SharedFolderRecordLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.SharedFolderRecordLocation} SharedFolderRecordLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SharedFolderRecordLocation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SharedFolderRecordLocation message.
         * @function verify
         * @memberof Tokens.SharedFolderRecordLocation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SharedFolderRecordLocation.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.sharedFolderUid != null && Object.hasOwnProperty.call(message, "sharedFolderUid"))
                if (!(message.sharedFolderUid && typeof message.sharedFolderUid.length === "number" || $util.isString(message.sharedFolderUid)))
                    return "sharedFolderUid: buffer expected";
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                if (!(message.recordUid && typeof message.recordUid.length === "number" || $util.isString(message.recordUid)))
                    return "recordUid: buffer expected";
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (!$util.isInteger(message.lastSyncRevision) && !(message.lastSyncRevision && $util.isInteger(message.lastSyncRevision.low) && $util.isInteger(message.lastSyncRevision.high)))
                    return "lastSyncRevision: integer|Long expected";
            return null;
        };

        /**
         * Creates a SharedFolderRecordLocation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.SharedFolderRecordLocation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.SharedFolderRecordLocation} SharedFolderRecordLocation
         */
        SharedFolderRecordLocation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.SharedFolderRecordLocation)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.SharedFolderRecordLocation: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.SharedFolderRecordLocation();
            if (object.sharedFolderUid != null)
                if (typeof object.sharedFolderUid === "string")
                    $util.base64.decode(object.sharedFolderUid, message.sharedFolderUid = $util.newBuffer($util.base64.length(object.sharedFolderUid)), 0);
                else if (object.sharedFolderUid.length >= 0)
                    message.sharedFolderUid = object.sharedFolderUid;
            if (object.recordUid != null)
                if (typeof object.recordUid === "string")
                    $util.base64.decode(object.recordUid, message.recordUid = $util.newBuffer($util.base64.length(object.recordUid)), 0);
                else if (object.recordUid.length >= 0)
                    message.recordUid = object.recordUid;
            if (object.lastSyncRevision != null)
                if ($util.Long)
                    message.lastSyncRevision = $util.Long.fromValue(object.lastSyncRevision, false);
                else if (typeof object.lastSyncRevision === "string")
                    message.lastSyncRevision = parseInt(object.lastSyncRevision, 10);
                else if (typeof object.lastSyncRevision === "number")
                    message.lastSyncRevision = object.lastSyncRevision;
                else if (typeof object.lastSyncRevision === "object")
                    message.lastSyncRevision = new $util.LongBits(object.lastSyncRevision.low >>> 0, object.lastSyncRevision.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a SharedFolderRecordLocation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.SharedFolderRecordLocation
         * @static
         * @param {Tokens.SharedFolderRecordLocation} message SharedFolderRecordLocation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SharedFolderRecordLocation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.sharedFolderUid = "";
                else {
                    object.sharedFolderUid = [];
                    if (options.bytes !== Array)
                        object.sharedFolderUid = $util.newBuffer(object.sharedFolderUid);
                }
                if (options.bytes === String)
                    object.recordUid = "";
                else {
                    object.recordUid = [];
                    if (options.bytes !== Array)
                        object.recordUid = $util.newBuffer(object.recordUid);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastSyncRevision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastSyncRevision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.sharedFolderUid != null && Object.hasOwnProperty.call(message, "sharedFolderUid"))
                object.sharedFolderUid = options.bytes === String ? $util.base64.encode(message.sharedFolderUid, 0, message.sharedFolderUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.sharedFolderUid) : message.sharedFolderUid;
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                object.recordUid = options.bytes === String ? $util.base64.encode(message.recordUid, 0, message.recordUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.recordUid) : message.recordUid;
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastSyncRevision = typeof message.lastSyncRevision === "number" ? BigInt(message.lastSyncRevision) : $util.Long.fromBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0, false).toBigInt();
                else if (typeof message.lastSyncRevision === "number")
                    object.lastSyncRevision = options.longs === String ? String(message.lastSyncRevision) : message.lastSyncRevision;
                else
                    object.lastSyncRevision = options.longs === String ? $util.Long.prototype.toString.call(message.lastSyncRevision) : options.longs === Number ? new $util.LongBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0).toNumber() : message.lastSyncRevision;
            return object;
        };

        /**
         * Converts this SharedFolderRecordLocation to JSON.
         * @function toJSON
         * @memberof Tokens.SharedFolderRecordLocation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SharedFolderRecordLocation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SharedFolderRecordLocation
         * @function getTypeUrl
         * @memberof Tokens.SharedFolderRecordLocation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SharedFolderRecordLocation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.SharedFolderRecordLocation";
        };

        return SharedFolderRecordLocation;
    })();

    Tokens.SharedFolderFolderRecordLocation = (function() {

        /**
         * Properties of a SharedFolderFolderRecordLocation.
         * @memberof Tokens
         * @interface ISharedFolderFolderRecordLocation
         * @property {Uint8Array|null} [sharedFolderUid] SharedFolderFolderRecordLocation sharedFolderUid
         * @property {Uint8Array|null} [folderUid] SharedFolderFolderRecordLocation folderUid
         * @property {Uint8Array|null} [recordUid] SharedFolderFolderRecordLocation recordUid
         */

        /**
         * Constructs a new SharedFolderFolderRecordLocation.
         * @memberof Tokens
         * @classdesc Represents a SharedFolderFolderRecordLocation.
         * @implements ISharedFolderFolderRecordLocation
         * @constructor
         * @param {Tokens.ISharedFolderFolderRecordLocation=} [properties] Properties to set
         */
        function SharedFolderFolderRecordLocation(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * SharedFolderFolderRecordLocation sharedFolderUid.
         * @member {Uint8Array} sharedFolderUid
         * @memberof Tokens.SharedFolderFolderRecordLocation
         * @instance
         */
        SharedFolderFolderRecordLocation.prototype.sharedFolderUid = $util.newBuffer([]);

        /**
         * SharedFolderFolderRecordLocation folderUid.
         * @member {Uint8Array} folderUid
         * @memberof Tokens.SharedFolderFolderRecordLocation
         * @instance
         */
        SharedFolderFolderRecordLocation.prototype.folderUid = $util.newBuffer([]);

        /**
         * SharedFolderFolderRecordLocation recordUid.
         * @member {Uint8Array} recordUid
         * @memberof Tokens.SharedFolderFolderRecordLocation
         * @instance
         */
        SharedFolderFolderRecordLocation.prototype.recordUid = $util.newBuffer([]);

        /**
         * Creates a new SharedFolderFolderRecordLocation instance using the specified properties.
         * @function create
         * @memberof Tokens.SharedFolderFolderRecordLocation
         * @static
         * @param {Tokens.ISharedFolderFolderRecordLocation=} [properties] Properties to set
         * @returns {Tokens.SharedFolderFolderRecordLocation} SharedFolderFolderRecordLocation instance
         */
        SharedFolderFolderRecordLocation.create = function create(properties) {
            return new SharedFolderFolderRecordLocation(properties);
        };

        /**
         * Encodes the specified SharedFolderFolderRecordLocation message. Does not implicitly {@link Tokens.SharedFolderFolderRecordLocation.verify|verify} messages.
         * @function encode
         * @memberof Tokens.SharedFolderFolderRecordLocation
         * @static
         * @param {Tokens.ISharedFolderFolderRecordLocation} message SharedFolderFolderRecordLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SharedFolderFolderRecordLocation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.sharedFolderUid != null && Object.hasOwnProperty.call(message, "sharedFolderUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.sharedFolderUid);
            if (message.folderUid != null && Object.hasOwnProperty.call(message, "folderUid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.folderUid);
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.recordUid);
            return writer;
        };

        /**
         * Encodes the specified SharedFolderFolderRecordLocation message, length delimited. Does not implicitly {@link Tokens.SharedFolderFolderRecordLocation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.SharedFolderFolderRecordLocation
         * @static
         * @param {Tokens.ISharedFolderFolderRecordLocation} message SharedFolderFolderRecordLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SharedFolderFolderRecordLocation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SharedFolderFolderRecordLocation message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.SharedFolderFolderRecordLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.SharedFolderFolderRecordLocation} SharedFolderFolderRecordLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SharedFolderFolderRecordLocation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.SharedFolderFolderRecordLocation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.sharedFolderUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.folderUid = reader.bytes();
                        break;
                    }
                case 3: {
                        message.recordUid = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SharedFolderFolderRecordLocation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.SharedFolderFolderRecordLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.SharedFolderFolderRecordLocation} SharedFolderFolderRecordLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SharedFolderFolderRecordLocation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SharedFolderFolderRecordLocation message.
         * @function verify
         * @memberof Tokens.SharedFolderFolderRecordLocation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SharedFolderFolderRecordLocation.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.sharedFolderUid != null && Object.hasOwnProperty.call(message, "sharedFolderUid"))
                if (!(message.sharedFolderUid && typeof message.sharedFolderUid.length === "number" || $util.isString(message.sharedFolderUid)))
                    return "sharedFolderUid: buffer expected";
            if (message.folderUid != null && Object.hasOwnProperty.call(message, "folderUid"))
                if (!(message.folderUid && typeof message.folderUid.length === "number" || $util.isString(message.folderUid)))
                    return "folderUid: buffer expected";
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                if (!(message.recordUid && typeof message.recordUid.length === "number" || $util.isString(message.recordUid)))
                    return "recordUid: buffer expected";
            return null;
        };

        /**
         * Creates a SharedFolderFolderRecordLocation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.SharedFolderFolderRecordLocation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.SharedFolderFolderRecordLocation} SharedFolderFolderRecordLocation
         */
        SharedFolderFolderRecordLocation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.SharedFolderFolderRecordLocation)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.SharedFolderFolderRecordLocation: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.SharedFolderFolderRecordLocation();
            if (object.sharedFolderUid != null)
                if (typeof object.sharedFolderUid === "string")
                    $util.base64.decode(object.sharedFolderUid, message.sharedFolderUid = $util.newBuffer($util.base64.length(object.sharedFolderUid)), 0);
                else if (object.sharedFolderUid.length >= 0)
                    message.sharedFolderUid = object.sharedFolderUid;
            if (object.folderUid != null)
                if (typeof object.folderUid === "string")
                    $util.base64.decode(object.folderUid, message.folderUid = $util.newBuffer($util.base64.length(object.folderUid)), 0);
                else if (object.folderUid.length >= 0)
                    message.folderUid = object.folderUid;
            if (object.recordUid != null)
                if (typeof object.recordUid === "string")
                    $util.base64.decode(object.recordUid, message.recordUid = $util.newBuffer($util.base64.length(object.recordUid)), 0);
                else if (object.recordUid.length >= 0)
                    message.recordUid = object.recordUid;
            return message;
        };

        /**
         * Creates a plain object from a SharedFolderFolderRecordLocation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.SharedFolderFolderRecordLocation
         * @static
         * @param {Tokens.SharedFolderFolderRecordLocation} message SharedFolderFolderRecordLocation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SharedFolderFolderRecordLocation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.sharedFolderUid = "";
                else {
                    object.sharedFolderUid = [];
                    if (options.bytes !== Array)
                        object.sharedFolderUid = $util.newBuffer(object.sharedFolderUid);
                }
                if (options.bytes === String)
                    object.folderUid = "";
                else {
                    object.folderUid = [];
                    if (options.bytes !== Array)
                        object.folderUid = $util.newBuffer(object.folderUid);
                }
                if (options.bytes === String)
                    object.recordUid = "";
                else {
                    object.recordUid = [];
                    if (options.bytes !== Array)
                        object.recordUid = $util.newBuffer(object.recordUid);
                }
            }
            if (message.sharedFolderUid != null && Object.hasOwnProperty.call(message, "sharedFolderUid"))
                object.sharedFolderUid = options.bytes === String ? $util.base64.encode(message.sharedFolderUid, 0, message.sharedFolderUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.sharedFolderUid) : message.sharedFolderUid;
            if (message.folderUid != null && Object.hasOwnProperty.call(message, "folderUid"))
                object.folderUid = options.bytes === String ? $util.base64.encode(message.folderUid, 0, message.folderUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.folderUid) : message.folderUid;
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                object.recordUid = options.bytes === String ? $util.base64.encode(message.recordUid, 0, message.recordUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.recordUid) : message.recordUid;
            return object;
        };

        /**
         * Converts this SharedFolderFolderRecordLocation to JSON.
         * @function toJSON
         * @memberof Tokens.SharedFolderFolderRecordLocation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SharedFolderFolderRecordLocation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SharedFolderFolderRecordLocation
         * @function getTypeUrl
         * @memberof Tokens.SharedFolderFolderRecordLocation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SharedFolderFolderRecordLocation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.SharedFolderFolderRecordLocation";
        };

        return SharedFolderFolderRecordLocation;
    })();

    Tokens.TeamLocation = (function() {

        /**
         * Properties of a TeamLocation.
         * @memberof Tokens
         * @interface ITeamLocation
         * @property {Uint8Array|null} [teamUid] TeamLocation teamUid
         * @property {number|null} [lastSyncRevision] TeamLocation lastSyncRevision
         */

        /**
         * Constructs a new TeamLocation.
         * @memberof Tokens
         * @classdesc Represents a TeamLocation.
         * @implements ITeamLocation
         * @constructor
         * @param {Tokens.ITeamLocation=} [properties] Properties to set
         */
        function TeamLocation(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TeamLocation teamUid.
         * @member {Uint8Array} teamUid
         * @memberof Tokens.TeamLocation
         * @instance
         */
        TeamLocation.prototype.teamUid = $util.newBuffer([]);

        /**
         * TeamLocation lastSyncRevision.
         * @member {number} lastSyncRevision
         * @memberof Tokens.TeamLocation
         * @instance
         */
        TeamLocation.prototype.lastSyncRevision = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new TeamLocation instance using the specified properties.
         * @function create
         * @memberof Tokens.TeamLocation
         * @static
         * @param {Tokens.ITeamLocation=} [properties] Properties to set
         * @returns {Tokens.TeamLocation} TeamLocation instance
         */
        TeamLocation.create = function create(properties) {
            return new TeamLocation(properties);
        };

        /**
         * Encodes the specified TeamLocation message. Does not implicitly {@link Tokens.TeamLocation.verify|verify} messages.
         * @function encode
         * @memberof Tokens.TeamLocation
         * @static
         * @param {Tokens.ITeamLocation} message TeamLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TeamLocation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.teamUid != null && Object.hasOwnProperty.call(message, "teamUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.teamUid);
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.lastSyncRevision);
            return writer;
        };

        /**
         * Encodes the specified TeamLocation message, length delimited. Does not implicitly {@link Tokens.TeamLocation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.TeamLocation
         * @static
         * @param {Tokens.ITeamLocation} message TeamLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TeamLocation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TeamLocation message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.TeamLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.TeamLocation} TeamLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TeamLocation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.TeamLocation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.teamUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.lastSyncRevision = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TeamLocation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.TeamLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.TeamLocation} TeamLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TeamLocation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TeamLocation message.
         * @function verify
         * @memberof Tokens.TeamLocation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TeamLocation.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.teamUid != null && Object.hasOwnProperty.call(message, "teamUid"))
                if (!(message.teamUid && typeof message.teamUid.length === "number" || $util.isString(message.teamUid)))
                    return "teamUid: buffer expected";
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (!$util.isInteger(message.lastSyncRevision) && !(message.lastSyncRevision && $util.isInteger(message.lastSyncRevision.low) && $util.isInteger(message.lastSyncRevision.high)))
                    return "lastSyncRevision: integer|Long expected";
            return null;
        };

        /**
         * Creates a TeamLocation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.TeamLocation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.TeamLocation} TeamLocation
         */
        TeamLocation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.TeamLocation)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.TeamLocation: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.TeamLocation();
            if (object.teamUid != null)
                if (typeof object.teamUid === "string")
                    $util.base64.decode(object.teamUid, message.teamUid = $util.newBuffer($util.base64.length(object.teamUid)), 0);
                else if (object.teamUid.length >= 0)
                    message.teamUid = object.teamUid;
            if (object.lastSyncRevision != null)
                if ($util.Long)
                    message.lastSyncRevision = $util.Long.fromValue(object.lastSyncRevision, false);
                else if (typeof object.lastSyncRevision === "string")
                    message.lastSyncRevision = parseInt(object.lastSyncRevision, 10);
                else if (typeof object.lastSyncRevision === "number")
                    message.lastSyncRevision = object.lastSyncRevision;
                else if (typeof object.lastSyncRevision === "object")
                    message.lastSyncRevision = new $util.LongBits(object.lastSyncRevision.low >>> 0, object.lastSyncRevision.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a TeamLocation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.TeamLocation
         * @static
         * @param {Tokens.TeamLocation} message TeamLocation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TeamLocation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.teamUid = "";
                else {
                    object.teamUid = [];
                    if (options.bytes !== Array)
                        object.teamUid = $util.newBuffer(object.teamUid);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastSyncRevision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastSyncRevision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.teamUid != null && Object.hasOwnProperty.call(message, "teamUid"))
                object.teamUid = options.bytes === String ? $util.base64.encode(message.teamUid, 0, message.teamUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.teamUid) : message.teamUid;
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastSyncRevision = typeof message.lastSyncRevision === "number" ? BigInt(message.lastSyncRevision) : $util.Long.fromBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0, false).toBigInt();
                else if (typeof message.lastSyncRevision === "number")
                    object.lastSyncRevision = options.longs === String ? String(message.lastSyncRevision) : message.lastSyncRevision;
                else
                    object.lastSyncRevision = options.longs === String ? $util.Long.prototype.toString.call(message.lastSyncRevision) : options.longs === Number ? new $util.LongBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0).toNumber() : message.lastSyncRevision;
            return object;
        };

        /**
         * Converts this TeamLocation to JSON.
         * @function toJSON
         * @memberof Tokens.TeamLocation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TeamLocation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TeamLocation
         * @function getTypeUrl
         * @memberof Tokens.TeamLocation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TeamLocation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.TeamLocation";
        };

        return TeamLocation;
    })();

    Tokens.UserAuthHashLocation = (function() {

        /**
         * Properties of a UserAuthHashLocation.
         * @memberof Tokens
         * @interface IUserAuthHashLocation
         * @property {Uint8Array|null} [userAuthHashUid] UserAuthHashLocation userAuthHashUid
         * @property {number|null} [lastSyncRevision] UserAuthHashLocation lastSyncRevision
         */

        /**
         * Constructs a new UserAuthHashLocation.
         * @memberof Tokens
         * @classdesc Represents a UserAuthHashLocation.
         * @implements IUserAuthHashLocation
         * @constructor
         * @param {Tokens.IUserAuthHashLocation=} [properties] Properties to set
         */
        function UserAuthHashLocation(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UserAuthHashLocation userAuthHashUid.
         * @member {Uint8Array} userAuthHashUid
         * @memberof Tokens.UserAuthHashLocation
         * @instance
         */
        UserAuthHashLocation.prototype.userAuthHashUid = $util.newBuffer([]);

        /**
         * UserAuthHashLocation lastSyncRevision.
         * @member {number} lastSyncRevision
         * @memberof Tokens.UserAuthHashLocation
         * @instance
         */
        UserAuthHashLocation.prototype.lastSyncRevision = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new UserAuthHashLocation instance using the specified properties.
         * @function create
         * @memberof Tokens.UserAuthHashLocation
         * @static
         * @param {Tokens.IUserAuthHashLocation=} [properties] Properties to set
         * @returns {Tokens.UserAuthHashLocation} UserAuthHashLocation instance
         */
        UserAuthHashLocation.create = function create(properties) {
            return new UserAuthHashLocation(properties);
        };

        /**
         * Encodes the specified UserAuthHashLocation message. Does not implicitly {@link Tokens.UserAuthHashLocation.verify|verify} messages.
         * @function encode
         * @memberof Tokens.UserAuthHashLocation
         * @static
         * @param {Tokens.IUserAuthHashLocation} message UserAuthHashLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserAuthHashLocation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.userAuthHashUid != null && Object.hasOwnProperty.call(message, "userAuthHashUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.userAuthHashUid);
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.lastSyncRevision);
            return writer;
        };

        /**
         * Encodes the specified UserAuthHashLocation message, length delimited. Does not implicitly {@link Tokens.UserAuthHashLocation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.UserAuthHashLocation
         * @static
         * @param {Tokens.IUserAuthHashLocation} message UserAuthHashLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserAuthHashLocation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a UserAuthHashLocation message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.UserAuthHashLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.UserAuthHashLocation} UserAuthHashLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserAuthHashLocation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.UserAuthHashLocation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.userAuthHashUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.lastSyncRevision = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a UserAuthHashLocation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.UserAuthHashLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.UserAuthHashLocation} UserAuthHashLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserAuthHashLocation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a UserAuthHashLocation message.
         * @function verify
         * @memberof Tokens.UserAuthHashLocation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UserAuthHashLocation.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.userAuthHashUid != null && Object.hasOwnProperty.call(message, "userAuthHashUid"))
                if (!(message.userAuthHashUid && typeof message.userAuthHashUid.length === "number" || $util.isString(message.userAuthHashUid)))
                    return "userAuthHashUid: buffer expected";
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (!$util.isInteger(message.lastSyncRevision) && !(message.lastSyncRevision && $util.isInteger(message.lastSyncRevision.low) && $util.isInteger(message.lastSyncRevision.high)))
                    return "lastSyncRevision: integer|Long expected";
            return null;
        };

        /**
         * Creates a UserAuthHashLocation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.UserAuthHashLocation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.UserAuthHashLocation} UserAuthHashLocation
         */
        UserAuthHashLocation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.UserAuthHashLocation)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.UserAuthHashLocation: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.UserAuthHashLocation();
            if (object.userAuthHashUid != null)
                if (typeof object.userAuthHashUid === "string")
                    $util.base64.decode(object.userAuthHashUid, message.userAuthHashUid = $util.newBuffer($util.base64.length(object.userAuthHashUid)), 0);
                else if (object.userAuthHashUid.length >= 0)
                    message.userAuthHashUid = object.userAuthHashUid;
            if (object.lastSyncRevision != null)
                if ($util.Long)
                    message.lastSyncRevision = $util.Long.fromValue(object.lastSyncRevision, false);
                else if (typeof object.lastSyncRevision === "string")
                    message.lastSyncRevision = parseInt(object.lastSyncRevision, 10);
                else if (typeof object.lastSyncRevision === "number")
                    message.lastSyncRevision = object.lastSyncRevision;
                else if (typeof object.lastSyncRevision === "object")
                    message.lastSyncRevision = new $util.LongBits(object.lastSyncRevision.low >>> 0, object.lastSyncRevision.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a UserAuthHashLocation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.UserAuthHashLocation
         * @static
         * @param {Tokens.UserAuthHashLocation} message UserAuthHashLocation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UserAuthHashLocation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.userAuthHashUid = "";
                else {
                    object.userAuthHashUid = [];
                    if (options.bytes !== Array)
                        object.userAuthHashUid = $util.newBuffer(object.userAuthHashUid);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastSyncRevision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastSyncRevision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.userAuthHashUid != null && Object.hasOwnProperty.call(message, "userAuthHashUid"))
                object.userAuthHashUid = options.bytes === String ? $util.base64.encode(message.userAuthHashUid, 0, message.userAuthHashUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.userAuthHashUid) : message.userAuthHashUid;
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastSyncRevision = typeof message.lastSyncRevision === "number" ? BigInt(message.lastSyncRevision) : $util.Long.fromBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0, false).toBigInt();
                else if (typeof message.lastSyncRevision === "number")
                    object.lastSyncRevision = options.longs === String ? String(message.lastSyncRevision) : message.lastSyncRevision;
                else
                    object.lastSyncRevision = options.longs === String ? $util.Long.prototype.toString.call(message.lastSyncRevision) : options.longs === Number ? new $util.LongBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0).toNumber() : message.lastSyncRevision;
            return object;
        };

        /**
         * Converts this UserAuthHashLocation to JSON.
         * @function toJSON
         * @memberof Tokens.UserAuthHashLocation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UserAuthHashLocation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UserAuthHashLocation
         * @function getTypeUrl
         * @memberof Tokens.UserAuthHashLocation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UserAuthHashLocation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.UserAuthHashLocation";
        };

        return UserAuthHashLocation;
    })();

    Tokens.FolderRecordLocation = (function() {

        /**
         * Properties of a FolderRecordLocation.
         * @memberof Tokens
         * @interface IFolderRecordLocation
         * @property {Uint8Array|null} [folderUid] FolderRecordLocation folderUid
         * @property {Uint8Array|null} [recordUid] FolderRecordLocation recordUid
         * @property {number|null} [lastSyncRevision] FolderRecordLocation lastSyncRevision
         */

        /**
         * Constructs a new FolderRecordLocation.
         * @memberof Tokens
         * @classdesc Represents a FolderRecordLocation.
         * @implements IFolderRecordLocation
         * @constructor
         * @param {Tokens.IFolderRecordLocation=} [properties] Properties to set
         */
        function FolderRecordLocation(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FolderRecordLocation folderUid.
         * @member {Uint8Array} folderUid
         * @memberof Tokens.FolderRecordLocation
         * @instance
         */
        FolderRecordLocation.prototype.folderUid = $util.newBuffer([]);

        /**
         * FolderRecordLocation recordUid.
         * @member {Uint8Array} recordUid
         * @memberof Tokens.FolderRecordLocation
         * @instance
         */
        FolderRecordLocation.prototype.recordUid = $util.newBuffer([]);

        /**
         * FolderRecordLocation lastSyncRevision.
         * @member {number} lastSyncRevision
         * @memberof Tokens.FolderRecordLocation
         * @instance
         */
        FolderRecordLocation.prototype.lastSyncRevision = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new FolderRecordLocation instance using the specified properties.
         * @function create
         * @memberof Tokens.FolderRecordLocation
         * @static
         * @param {Tokens.IFolderRecordLocation=} [properties] Properties to set
         * @returns {Tokens.FolderRecordLocation} FolderRecordLocation instance
         */
        FolderRecordLocation.create = function create(properties) {
            return new FolderRecordLocation(properties);
        };

        /**
         * Encodes the specified FolderRecordLocation message. Does not implicitly {@link Tokens.FolderRecordLocation.verify|verify} messages.
         * @function encode
         * @memberof Tokens.FolderRecordLocation
         * @static
         * @param {Tokens.IFolderRecordLocation} message FolderRecordLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FolderRecordLocation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.folderUid != null && Object.hasOwnProperty.call(message, "folderUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.folderUid);
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.recordUid);
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.lastSyncRevision);
            return writer;
        };

        /**
         * Encodes the specified FolderRecordLocation message, length delimited. Does not implicitly {@link Tokens.FolderRecordLocation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.FolderRecordLocation
         * @static
         * @param {Tokens.IFolderRecordLocation} message FolderRecordLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FolderRecordLocation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FolderRecordLocation message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.FolderRecordLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.FolderRecordLocation} FolderRecordLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FolderRecordLocation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.FolderRecordLocation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.folderUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.recordUid = reader.bytes();
                        break;
                    }
                case 3: {
                        message.lastSyncRevision = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FolderRecordLocation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.FolderRecordLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.FolderRecordLocation} FolderRecordLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FolderRecordLocation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FolderRecordLocation message.
         * @function verify
         * @memberof Tokens.FolderRecordLocation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FolderRecordLocation.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.folderUid != null && Object.hasOwnProperty.call(message, "folderUid"))
                if (!(message.folderUid && typeof message.folderUid.length === "number" || $util.isString(message.folderUid)))
                    return "folderUid: buffer expected";
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                if (!(message.recordUid && typeof message.recordUid.length === "number" || $util.isString(message.recordUid)))
                    return "recordUid: buffer expected";
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (!$util.isInteger(message.lastSyncRevision) && !(message.lastSyncRevision && $util.isInteger(message.lastSyncRevision.low) && $util.isInteger(message.lastSyncRevision.high)))
                    return "lastSyncRevision: integer|Long expected";
            return null;
        };

        /**
         * Creates a FolderRecordLocation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.FolderRecordLocation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.FolderRecordLocation} FolderRecordLocation
         */
        FolderRecordLocation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.FolderRecordLocation)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.FolderRecordLocation: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.FolderRecordLocation();
            if (object.folderUid != null)
                if (typeof object.folderUid === "string")
                    $util.base64.decode(object.folderUid, message.folderUid = $util.newBuffer($util.base64.length(object.folderUid)), 0);
                else if (object.folderUid.length >= 0)
                    message.folderUid = object.folderUid;
            if (object.recordUid != null)
                if (typeof object.recordUid === "string")
                    $util.base64.decode(object.recordUid, message.recordUid = $util.newBuffer($util.base64.length(object.recordUid)), 0);
                else if (object.recordUid.length >= 0)
                    message.recordUid = object.recordUid;
            if (object.lastSyncRevision != null)
                if ($util.Long)
                    message.lastSyncRevision = $util.Long.fromValue(object.lastSyncRevision, false);
                else if (typeof object.lastSyncRevision === "string")
                    message.lastSyncRevision = parseInt(object.lastSyncRevision, 10);
                else if (typeof object.lastSyncRevision === "number")
                    message.lastSyncRevision = object.lastSyncRevision;
                else if (typeof object.lastSyncRevision === "object")
                    message.lastSyncRevision = new $util.LongBits(object.lastSyncRevision.low >>> 0, object.lastSyncRevision.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a FolderRecordLocation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.FolderRecordLocation
         * @static
         * @param {Tokens.FolderRecordLocation} message FolderRecordLocation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FolderRecordLocation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.folderUid = "";
                else {
                    object.folderUid = [];
                    if (options.bytes !== Array)
                        object.folderUid = $util.newBuffer(object.folderUid);
                }
                if (options.bytes === String)
                    object.recordUid = "";
                else {
                    object.recordUid = [];
                    if (options.bytes !== Array)
                        object.recordUid = $util.newBuffer(object.recordUid);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastSyncRevision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastSyncRevision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.folderUid != null && Object.hasOwnProperty.call(message, "folderUid"))
                object.folderUid = options.bytes === String ? $util.base64.encode(message.folderUid, 0, message.folderUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.folderUid) : message.folderUid;
            if (message.recordUid != null && Object.hasOwnProperty.call(message, "recordUid"))
                object.recordUid = options.bytes === String ? $util.base64.encode(message.recordUid, 0, message.recordUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.recordUid) : message.recordUid;
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastSyncRevision = typeof message.lastSyncRevision === "number" ? BigInt(message.lastSyncRevision) : $util.Long.fromBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0, false).toBigInt();
                else if (typeof message.lastSyncRevision === "number")
                    object.lastSyncRevision = options.longs === String ? String(message.lastSyncRevision) : message.lastSyncRevision;
                else
                    object.lastSyncRevision = options.longs === String ? $util.Long.prototype.toString.call(message.lastSyncRevision) : options.longs === Number ? new $util.LongBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0).toNumber() : message.lastSyncRevision;
            return object;
        };

        /**
         * Converts this FolderRecordLocation to JSON.
         * @function toJSON
         * @memberof Tokens.FolderRecordLocation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FolderRecordLocation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FolderRecordLocation
         * @function getTypeUrl
         * @memberof Tokens.FolderRecordLocation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FolderRecordLocation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.FolderRecordLocation";
        };

        return FolderRecordLocation;
    })();

    Tokens.RecordLinkLocation = (function() {

        /**
         * Properties of a RecordLinkLocation.
         * @memberof Tokens
         * @interface IRecordLinkLocation
         * @property {Uint8Array|null} [parentRecordUid] RecordLinkLocation parentRecordUid
         * @property {Uint8Array|null} [childRecordUid] RecordLinkLocation childRecordUid
         * @property {number|null} [lastSyncRevision] RecordLinkLocation lastSyncRevision
         */

        /**
         * Constructs a new RecordLinkLocation.
         * @memberof Tokens
         * @classdesc Represents a RecordLinkLocation.
         * @implements IRecordLinkLocation
         * @constructor
         * @param {Tokens.IRecordLinkLocation=} [properties] Properties to set
         */
        function RecordLinkLocation(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RecordLinkLocation parentRecordUid.
         * @member {Uint8Array} parentRecordUid
         * @memberof Tokens.RecordLinkLocation
         * @instance
         */
        RecordLinkLocation.prototype.parentRecordUid = $util.newBuffer([]);

        /**
         * RecordLinkLocation childRecordUid.
         * @member {Uint8Array} childRecordUid
         * @memberof Tokens.RecordLinkLocation
         * @instance
         */
        RecordLinkLocation.prototype.childRecordUid = $util.newBuffer([]);

        /**
         * RecordLinkLocation lastSyncRevision.
         * @member {number} lastSyncRevision
         * @memberof Tokens.RecordLinkLocation
         * @instance
         */
        RecordLinkLocation.prototype.lastSyncRevision = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new RecordLinkLocation instance using the specified properties.
         * @function create
         * @memberof Tokens.RecordLinkLocation
         * @static
         * @param {Tokens.IRecordLinkLocation=} [properties] Properties to set
         * @returns {Tokens.RecordLinkLocation} RecordLinkLocation instance
         */
        RecordLinkLocation.create = function create(properties) {
            return new RecordLinkLocation(properties);
        };

        /**
         * Encodes the specified RecordLinkLocation message. Does not implicitly {@link Tokens.RecordLinkLocation.verify|verify} messages.
         * @function encode
         * @memberof Tokens.RecordLinkLocation
         * @static
         * @param {Tokens.IRecordLinkLocation} message RecordLinkLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RecordLinkLocation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.parentRecordUid != null && Object.hasOwnProperty.call(message, "parentRecordUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.parentRecordUid);
            if (message.childRecordUid != null && Object.hasOwnProperty.call(message, "childRecordUid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.childRecordUid);
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.lastSyncRevision);
            return writer;
        };

        /**
         * Encodes the specified RecordLinkLocation message, length delimited. Does not implicitly {@link Tokens.RecordLinkLocation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.RecordLinkLocation
         * @static
         * @param {Tokens.IRecordLinkLocation} message RecordLinkLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RecordLinkLocation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RecordLinkLocation message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.RecordLinkLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.RecordLinkLocation} RecordLinkLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RecordLinkLocation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.RecordLinkLocation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.parentRecordUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.childRecordUid = reader.bytes();
                        break;
                    }
                case 3: {
                        message.lastSyncRevision = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RecordLinkLocation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.RecordLinkLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.RecordLinkLocation} RecordLinkLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RecordLinkLocation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RecordLinkLocation message.
         * @function verify
         * @memberof Tokens.RecordLinkLocation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RecordLinkLocation.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.parentRecordUid != null && Object.hasOwnProperty.call(message, "parentRecordUid"))
                if (!(message.parentRecordUid && typeof message.parentRecordUid.length === "number" || $util.isString(message.parentRecordUid)))
                    return "parentRecordUid: buffer expected";
            if (message.childRecordUid != null && Object.hasOwnProperty.call(message, "childRecordUid"))
                if (!(message.childRecordUid && typeof message.childRecordUid.length === "number" || $util.isString(message.childRecordUid)))
                    return "childRecordUid: buffer expected";
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (!$util.isInteger(message.lastSyncRevision) && !(message.lastSyncRevision && $util.isInteger(message.lastSyncRevision.low) && $util.isInteger(message.lastSyncRevision.high)))
                    return "lastSyncRevision: integer|Long expected";
            return null;
        };

        /**
         * Creates a RecordLinkLocation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.RecordLinkLocation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.RecordLinkLocation} RecordLinkLocation
         */
        RecordLinkLocation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.RecordLinkLocation)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.RecordLinkLocation: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.RecordLinkLocation();
            if (object.parentRecordUid != null)
                if (typeof object.parentRecordUid === "string")
                    $util.base64.decode(object.parentRecordUid, message.parentRecordUid = $util.newBuffer($util.base64.length(object.parentRecordUid)), 0);
                else if (object.parentRecordUid.length >= 0)
                    message.parentRecordUid = object.parentRecordUid;
            if (object.childRecordUid != null)
                if (typeof object.childRecordUid === "string")
                    $util.base64.decode(object.childRecordUid, message.childRecordUid = $util.newBuffer($util.base64.length(object.childRecordUid)), 0);
                else if (object.childRecordUid.length >= 0)
                    message.childRecordUid = object.childRecordUid;
            if (object.lastSyncRevision != null)
                if ($util.Long)
                    message.lastSyncRevision = $util.Long.fromValue(object.lastSyncRevision, false);
                else if (typeof object.lastSyncRevision === "string")
                    message.lastSyncRevision = parseInt(object.lastSyncRevision, 10);
                else if (typeof object.lastSyncRevision === "number")
                    message.lastSyncRevision = object.lastSyncRevision;
                else if (typeof object.lastSyncRevision === "object")
                    message.lastSyncRevision = new $util.LongBits(object.lastSyncRevision.low >>> 0, object.lastSyncRevision.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a RecordLinkLocation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.RecordLinkLocation
         * @static
         * @param {Tokens.RecordLinkLocation} message RecordLinkLocation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RecordLinkLocation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.parentRecordUid = "";
                else {
                    object.parentRecordUid = [];
                    if (options.bytes !== Array)
                        object.parentRecordUid = $util.newBuffer(object.parentRecordUid);
                }
                if (options.bytes === String)
                    object.childRecordUid = "";
                else {
                    object.childRecordUid = [];
                    if (options.bytes !== Array)
                        object.childRecordUid = $util.newBuffer(object.childRecordUid);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastSyncRevision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastSyncRevision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.parentRecordUid != null && Object.hasOwnProperty.call(message, "parentRecordUid"))
                object.parentRecordUid = options.bytes === String ? $util.base64.encode(message.parentRecordUid, 0, message.parentRecordUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.parentRecordUid) : message.parentRecordUid;
            if (message.childRecordUid != null && Object.hasOwnProperty.call(message, "childRecordUid"))
                object.childRecordUid = options.bytes === String ? $util.base64.encode(message.childRecordUid, 0, message.childRecordUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.childRecordUid) : message.childRecordUid;
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastSyncRevision = typeof message.lastSyncRevision === "number" ? BigInt(message.lastSyncRevision) : $util.Long.fromBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0, false).toBigInt();
                else if (typeof message.lastSyncRevision === "number")
                    object.lastSyncRevision = options.longs === String ? String(message.lastSyncRevision) : message.lastSyncRevision;
                else
                    object.lastSyncRevision = options.longs === String ? $util.Long.prototype.toString.call(message.lastSyncRevision) : options.longs === Number ? new $util.LongBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0).toNumber() : message.lastSyncRevision;
            return object;
        };

        /**
         * Converts this RecordLinkLocation to JSON.
         * @function toJSON
         * @memberof Tokens.RecordLinkLocation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RecordLinkLocation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RecordLinkLocation
         * @function getTypeUrl
         * @memberof Tokens.RecordLinkLocation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RecordLinkLocation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.RecordLinkLocation";
        };

        return RecordLinkLocation;
    })();

    Tokens.KsmLocation = (function() {

        /**
         * Properties of a KsmLocation.
         * @memberof Tokens
         * @interface IKsmLocation
         * @property {Uint8Array|null} [appRecordUid] KsmLocation appRecordUid
         * @property {Uint8Array|null} [detailUid] KsmLocation detailUid
         * @property {number|null} [lastSyncRevision] KsmLocation lastSyncRevision
         */

        /**
         * Constructs a new KsmLocation.
         * @memberof Tokens
         * @classdesc Represents a KsmLocation.
         * @implements IKsmLocation
         * @constructor
         * @param {Tokens.IKsmLocation=} [properties] Properties to set
         */
        function KsmLocation(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * KsmLocation appRecordUid.
         * @member {Uint8Array} appRecordUid
         * @memberof Tokens.KsmLocation
         * @instance
         */
        KsmLocation.prototype.appRecordUid = $util.newBuffer([]);

        /**
         * KsmLocation detailUid.
         * @member {Uint8Array} detailUid
         * @memberof Tokens.KsmLocation
         * @instance
         */
        KsmLocation.prototype.detailUid = $util.newBuffer([]);

        /**
         * KsmLocation lastSyncRevision.
         * @member {number} lastSyncRevision
         * @memberof Tokens.KsmLocation
         * @instance
         */
        KsmLocation.prototype.lastSyncRevision = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new KsmLocation instance using the specified properties.
         * @function create
         * @memberof Tokens.KsmLocation
         * @static
         * @param {Tokens.IKsmLocation=} [properties] Properties to set
         * @returns {Tokens.KsmLocation} KsmLocation instance
         */
        KsmLocation.create = function create(properties) {
            return new KsmLocation(properties);
        };

        /**
         * Encodes the specified KsmLocation message. Does not implicitly {@link Tokens.KsmLocation.verify|verify} messages.
         * @function encode
         * @memberof Tokens.KsmLocation
         * @static
         * @param {Tokens.IKsmLocation} message KsmLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KsmLocation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.appRecordUid != null && Object.hasOwnProperty.call(message, "appRecordUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.appRecordUid);
            if (message.detailUid != null && Object.hasOwnProperty.call(message, "detailUid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.detailUid);
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.lastSyncRevision);
            return writer;
        };

        /**
         * Encodes the specified KsmLocation message, length delimited. Does not implicitly {@link Tokens.KsmLocation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.KsmLocation
         * @static
         * @param {Tokens.IKsmLocation} message KsmLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KsmLocation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a KsmLocation message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.KsmLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.KsmLocation} KsmLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KsmLocation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.KsmLocation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.appRecordUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.detailUid = reader.bytes();
                        break;
                    }
                case 3: {
                        message.lastSyncRevision = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a KsmLocation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.KsmLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.KsmLocation} KsmLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KsmLocation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a KsmLocation message.
         * @function verify
         * @memberof Tokens.KsmLocation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        KsmLocation.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.appRecordUid != null && Object.hasOwnProperty.call(message, "appRecordUid"))
                if (!(message.appRecordUid && typeof message.appRecordUid.length === "number" || $util.isString(message.appRecordUid)))
                    return "appRecordUid: buffer expected";
            if (message.detailUid != null && Object.hasOwnProperty.call(message, "detailUid"))
                if (!(message.detailUid && typeof message.detailUid.length === "number" || $util.isString(message.detailUid)))
                    return "detailUid: buffer expected";
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (!$util.isInteger(message.lastSyncRevision) && !(message.lastSyncRevision && $util.isInteger(message.lastSyncRevision.low) && $util.isInteger(message.lastSyncRevision.high)))
                    return "lastSyncRevision: integer|Long expected";
            return null;
        };

        /**
         * Creates a KsmLocation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.KsmLocation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.KsmLocation} KsmLocation
         */
        KsmLocation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.KsmLocation)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.KsmLocation: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.KsmLocation();
            if (object.appRecordUid != null)
                if (typeof object.appRecordUid === "string")
                    $util.base64.decode(object.appRecordUid, message.appRecordUid = $util.newBuffer($util.base64.length(object.appRecordUid)), 0);
                else if (object.appRecordUid.length >= 0)
                    message.appRecordUid = object.appRecordUid;
            if (object.detailUid != null)
                if (typeof object.detailUid === "string")
                    $util.base64.decode(object.detailUid, message.detailUid = $util.newBuffer($util.base64.length(object.detailUid)), 0);
                else if (object.detailUid.length >= 0)
                    message.detailUid = object.detailUid;
            if (object.lastSyncRevision != null)
                if ($util.Long)
                    message.lastSyncRevision = $util.Long.fromValue(object.lastSyncRevision, false);
                else if (typeof object.lastSyncRevision === "string")
                    message.lastSyncRevision = parseInt(object.lastSyncRevision, 10);
                else if (typeof object.lastSyncRevision === "number")
                    message.lastSyncRevision = object.lastSyncRevision;
                else if (typeof object.lastSyncRevision === "object")
                    message.lastSyncRevision = new $util.LongBits(object.lastSyncRevision.low >>> 0, object.lastSyncRevision.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a KsmLocation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.KsmLocation
         * @static
         * @param {Tokens.KsmLocation} message KsmLocation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        KsmLocation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.appRecordUid = "";
                else {
                    object.appRecordUid = [];
                    if (options.bytes !== Array)
                        object.appRecordUid = $util.newBuffer(object.appRecordUid);
                }
                if (options.bytes === String)
                    object.detailUid = "";
                else {
                    object.detailUid = [];
                    if (options.bytes !== Array)
                        object.detailUid = $util.newBuffer(object.detailUid);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastSyncRevision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastSyncRevision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.appRecordUid != null && Object.hasOwnProperty.call(message, "appRecordUid"))
                object.appRecordUid = options.bytes === String ? $util.base64.encode(message.appRecordUid, 0, message.appRecordUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.appRecordUid) : message.appRecordUid;
            if (message.detailUid != null && Object.hasOwnProperty.call(message, "detailUid"))
                object.detailUid = options.bytes === String ? $util.base64.encode(message.detailUid, 0, message.detailUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.detailUid) : message.detailUid;
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastSyncRevision = typeof message.lastSyncRevision === "number" ? BigInt(message.lastSyncRevision) : $util.Long.fromBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0, false).toBigInt();
                else if (typeof message.lastSyncRevision === "number")
                    object.lastSyncRevision = options.longs === String ? String(message.lastSyncRevision) : message.lastSyncRevision;
                else
                    object.lastSyncRevision = options.longs === String ? $util.Long.prototype.toString.call(message.lastSyncRevision) : options.longs === Number ? new $util.LongBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0).toNumber() : message.lastSyncRevision;
            return object;
        };

        /**
         * Converts this KsmLocation to JSON.
         * @function toJSON
         * @memberof Tokens.KsmLocation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        KsmLocation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for KsmLocation
         * @function getTypeUrl
         * @memberof Tokens.KsmLocation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        KsmLocation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.KsmLocation";
        };

        return KsmLocation;
    })();

    Tokens.FolderSharedFolderLocation = (function() {

        /**
         * Properties of a FolderSharedFolderLocation.
         * @memberof Tokens
         * @interface IFolderSharedFolderLocation
         * @property {Uint8Array|null} [folderUid] FolderSharedFolderLocation folderUid
         * @property {Uint8Array|null} [sharedFolderUid] FolderSharedFolderLocation sharedFolderUid
         * @property {number|null} [lastSyncRevision] FolderSharedFolderLocation lastSyncRevision
         */

        /**
         * Constructs a new FolderSharedFolderLocation.
         * @memberof Tokens
         * @classdesc Represents a FolderSharedFolderLocation.
         * @implements IFolderSharedFolderLocation
         * @constructor
         * @param {Tokens.IFolderSharedFolderLocation=} [properties] Properties to set
         */
        function FolderSharedFolderLocation(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * FolderSharedFolderLocation folderUid.
         * @member {Uint8Array} folderUid
         * @memberof Tokens.FolderSharedFolderLocation
         * @instance
         */
        FolderSharedFolderLocation.prototype.folderUid = $util.newBuffer([]);

        /**
         * FolderSharedFolderLocation sharedFolderUid.
         * @member {Uint8Array} sharedFolderUid
         * @memberof Tokens.FolderSharedFolderLocation
         * @instance
         */
        FolderSharedFolderLocation.prototype.sharedFolderUid = $util.newBuffer([]);

        /**
         * FolderSharedFolderLocation lastSyncRevision.
         * @member {number} lastSyncRevision
         * @memberof Tokens.FolderSharedFolderLocation
         * @instance
         */
        FolderSharedFolderLocation.prototype.lastSyncRevision = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new FolderSharedFolderLocation instance using the specified properties.
         * @function create
         * @memberof Tokens.FolderSharedFolderLocation
         * @static
         * @param {Tokens.IFolderSharedFolderLocation=} [properties] Properties to set
         * @returns {Tokens.FolderSharedFolderLocation} FolderSharedFolderLocation instance
         */
        FolderSharedFolderLocation.create = function create(properties) {
            return new FolderSharedFolderLocation(properties);
        };

        /**
         * Encodes the specified FolderSharedFolderLocation message. Does not implicitly {@link Tokens.FolderSharedFolderLocation.verify|verify} messages.
         * @function encode
         * @memberof Tokens.FolderSharedFolderLocation
         * @static
         * @param {Tokens.IFolderSharedFolderLocation} message FolderSharedFolderLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FolderSharedFolderLocation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.folderUid != null && Object.hasOwnProperty.call(message, "folderUid"))
                writer.uint32(/* id 1, wireType 2 =*/10).bytes(message.folderUid);
            if (message.sharedFolderUid != null && Object.hasOwnProperty.call(message, "sharedFolderUid"))
                writer.uint32(/* id 2, wireType 2 =*/18).bytes(message.sharedFolderUid);
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.lastSyncRevision);
            return writer;
        };

        /**
         * Encodes the specified FolderSharedFolderLocation message, length delimited. Does not implicitly {@link Tokens.FolderSharedFolderLocation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.FolderSharedFolderLocation
         * @static
         * @param {Tokens.IFolderSharedFolderLocation} message FolderSharedFolderLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        FolderSharedFolderLocation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a FolderSharedFolderLocation message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.FolderSharedFolderLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.FolderSharedFolderLocation} FolderSharedFolderLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FolderSharedFolderLocation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.FolderSharedFolderLocation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.folderUid = reader.bytes();
                        break;
                    }
                case 2: {
                        message.sharedFolderUid = reader.bytes();
                        break;
                    }
                case 3: {
                        message.lastSyncRevision = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a FolderSharedFolderLocation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.FolderSharedFolderLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.FolderSharedFolderLocation} FolderSharedFolderLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        FolderSharedFolderLocation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a FolderSharedFolderLocation message.
         * @function verify
         * @memberof Tokens.FolderSharedFolderLocation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        FolderSharedFolderLocation.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.folderUid != null && Object.hasOwnProperty.call(message, "folderUid"))
                if (!(message.folderUid && typeof message.folderUid.length === "number" || $util.isString(message.folderUid)))
                    return "folderUid: buffer expected";
            if (message.sharedFolderUid != null && Object.hasOwnProperty.call(message, "sharedFolderUid"))
                if (!(message.sharedFolderUid && typeof message.sharedFolderUid.length === "number" || $util.isString(message.sharedFolderUid)))
                    return "sharedFolderUid: buffer expected";
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (!$util.isInteger(message.lastSyncRevision) && !(message.lastSyncRevision && $util.isInteger(message.lastSyncRevision.low) && $util.isInteger(message.lastSyncRevision.high)))
                    return "lastSyncRevision: integer|Long expected";
            return null;
        };

        /**
         * Creates a FolderSharedFolderLocation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.FolderSharedFolderLocation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.FolderSharedFolderLocation} FolderSharedFolderLocation
         */
        FolderSharedFolderLocation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.FolderSharedFolderLocation)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.FolderSharedFolderLocation: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.FolderSharedFolderLocation();
            if (object.folderUid != null)
                if (typeof object.folderUid === "string")
                    $util.base64.decode(object.folderUid, message.folderUid = $util.newBuffer($util.base64.length(object.folderUid)), 0);
                else if (object.folderUid.length >= 0)
                    message.folderUid = object.folderUid;
            if (object.sharedFolderUid != null)
                if (typeof object.sharedFolderUid === "string")
                    $util.base64.decode(object.sharedFolderUid, message.sharedFolderUid = $util.newBuffer($util.base64.length(object.sharedFolderUid)), 0);
                else if (object.sharedFolderUid.length >= 0)
                    message.sharedFolderUid = object.sharedFolderUid;
            if (object.lastSyncRevision != null)
                if ($util.Long)
                    message.lastSyncRevision = $util.Long.fromValue(object.lastSyncRevision, false);
                else if (typeof object.lastSyncRevision === "string")
                    message.lastSyncRevision = parseInt(object.lastSyncRevision, 10);
                else if (typeof object.lastSyncRevision === "number")
                    message.lastSyncRevision = object.lastSyncRevision;
                else if (typeof object.lastSyncRevision === "object")
                    message.lastSyncRevision = new $util.LongBits(object.lastSyncRevision.low >>> 0, object.lastSyncRevision.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a FolderSharedFolderLocation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.FolderSharedFolderLocation
         * @static
         * @param {Tokens.FolderSharedFolderLocation} message FolderSharedFolderLocation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        FolderSharedFolderLocation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if (options.bytes === String)
                    object.folderUid = "";
                else {
                    object.folderUid = [];
                    if (options.bytes !== Array)
                        object.folderUid = $util.newBuffer(object.folderUid);
                }
                if (options.bytes === String)
                    object.sharedFolderUid = "";
                else {
                    object.sharedFolderUid = [];
                    if (options.bytes !== Array)
                        object.sharedFolderUid = $util.newBuffer(object.sharedFolderUid);
                }
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastSyncRevision = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastSyncRevision = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.folderUid != null && Object.hasOwnProperty.call(message, "folderUid"))
                object.folderUid = options.bytes === String ? $util.base64.encode(message.folderUid, 0, message.folderUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.folderUid) : message.folderUid;
            if (message.sharedFolderUid != null && Object.hasOwnProperty.call(message, "sharedFolderUid"))
                object.sharedFolderUid = options.bytes === String ? $util.base64.encode(message.sharedFolderUid, 0, message.sharedFolderUid.length) : options.bytes === Array ? Array.prototype.slice.call(message.sharedFolderUid) : message.sharedFolderUid;
            if (message.lastSyncRevision != null && Object.hasOwnProperty.call(message, "lastSyncRevision"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastSyncRevision = typeof message.lastSyncRevision === "number" ? BigInt(message.lastSyncRevision) : $util.Long.fromBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0, false).toBigInt();
                else if (typeof message.lastSyncRevision === "number")
                    object.lastSyncRevision = options.longs === String ? String(message.lastSyncRevision) : message.lastSyncRevision;
                else
                    object.lastSyncRevision = options.longs === String ? $util.Long.prototype.toString.call(message.lastSyncRevision) : options.longs === Number ? new $util.LongBits(message.lastSyncRevision.low >>> 0, message.lastSyncRevision.high >>> 0).toNumber() : message.lastSyncRevision;
            return object;
        };

        /**
         * Converts this FolderSharedFolderLocation to JSON.
         * @function toJSON
         * @memberof Tokens.FolderSharedFolderLocation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        FolderSharedFolderLocation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for FolderSharedFolderLocation
         * @function getTypeUrl
         * @memberof Tokens.FolderSharedFolderLocation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        FolderSharedFolderLocation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.FolderSharedFolderLocation";
        };

        return FolderSharedFolderLocation;
    })();

    Tokens.ShareInvitationLocation = (function() {

        /**
         * Properties of a ShareInvitationLocation.
         * @memberof Tokens
         * @interface IShareInvitationLocation
         * @property {string|null} [username] ShareInvitationLocation username
         */

        /**
         * Constructs a new ShareInvitationLocation.
         * @memberof Tokens
         * @classdesc Represents a ShareInvitationLocation.
         * @implements IShareInvitationLocation
         * @constructor
         * @param {Tokens.IShareInvitationLocation=} [properties] Properties to set
         */
        function ShareInvitationLocation(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ShareInvitationLocation username.
         * @member {string} username
         * @memberof Tokens.ShareInvitationLocation
         * @instance
         */
        ShareInvitationLocation.prototype.username = "";

        /**
         * Creates a new ShareInvitationLocation instance using the specified properties.
         * @function create
         * @memberof Tokens.ShareInvitationLocation
         * @static
         * @param {Tokens.IShareInvitationLocation=} [properties] Properties to set
         * @returns {Tokens.ShareInvitationLocation} ShareInvitationLocation instance
         */
        ShareInvitationLocation.create = function create(properties) {
            return new ShareInvitationLocation(properties);
        };

        /**
         * Encodes the specified ShareInvitationLocation message. Does not implicitly {@link Tokens.ShareInvitationLocation.verify|verify} messages.
         * @function encode
         * @memberof Tokens.ShareInvitationLocation
         * @static
         * @param {Tokens.IShareInvitationLocation} message ShareInvitationLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ShareInvitationLocation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.username);
            return writer;
        };

        /**
         * Encodes the specified ShareInvitationLocation message, length delimited. Does not implicitly {@link Tokens.ShareInvitationLocation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.ShareInvitationLocation
         * @static
         * @param {Tokens.IShareInvitationLocation} message ShareInvitationLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ShareInvitationLocation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ShareInvitationLocation message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.ShareInvitationLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.ShareInvitationLocation} ShareInvitationLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ShareInvitationLocation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.ShareInvitationLocation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.username = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ShareInvitationLocation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.ShareInvitationLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.ShareInvitationLocation} ShareInvitationLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ShareInvitationLocation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ShareInvitationLocation message.
         * @function verify
         * @memberof Tokens.ShareInvitationLocation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ShareInvitationLocation.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                if (!$util.isString(message.username))
                    return "username: string expected";
            return null;
        };

        /**
         * Creates a ShareInvitationLocation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.ShareInvitationLocation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.ShareInvitationLocation} ShareInvitationLocation
         */
        ShareInvitationLocation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.ShareInvitationLocation)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.ShareInvitationLocation: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.ShareInvitationLocation();
            if (object.username != null)
                message.username = String(object.username);
            return message;
        };

        /**
         * Creates a plain object from a ShareInvitationLocation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.ShareInvitationLocation
         * @static
         * @param {Tokens.ShareInvitationLocation} message ShareInvitationLocation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ShareInvitationLocation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.username = "";
            if (message.username != null && Object.hasOwnProperty.call(message, "username"))
                object.username = message.username;
            return object;
        };

        /**
         * Converts this ShareInvitationLocation to JSON.
         * @function toJSON
         * @memberof Tokens.ShareInvitationLocation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ShareInvitationLocation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ShareInvitationLocation
         * @function getTypeUrl
         * @memberof Tokens.ShareInvitationLocation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ShareInvitationLocation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.ShareInvitationLocation";
        };

        return ShareInvitationLocation;
    })();

    Tokens.UserLocation = (function() {

        /**
         * Properties of a UserLocation.
         * @memberof Tokens
         * @interface IUserLocation
         * @property {number|null} [userId] UserLocation userId
         */

        /**
         * Constructs a new UserLocation.
         * @memberof Tokens
         * @classdesc Represents a UserLocation.
         * @implements IUserLocation
         * @constructor
         * @param {Tokens.IUserLocation=} [properties] Properties to set
         */
        function UserLocation(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * UserLocation userId.
         * @member {number} userId
         * @memberof Tokens.UserLocation
         * @instance
         */
        UserLocation.prototype.userId = 0;

        /**
         * Creates a new UserLocation instance using the specified properties.
         * @function create
         * @memberof Tokens.UserLocation
         * @static
         * @param {Tokens.IUserLocation=} [properties] Properties to set
         * @returns {Tokens.UserLocation} UserLocation instance
         */
        UserLocation.create = function create(properties) {
            return new UserLocation(properties);
        };

        /**
         * Encodes the specified UserLocation message. Does not implicitly {@link Tokens.UserLocation.verify|verify} messages.
         * @function encode
         * @memberof Tokens.UserLocation
         * @static
         * @param {Tokens.IUserLocation} message UserLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserLocation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.userId);
            return writer;
        };

        /**
         * Encodes the specified UserLocation message, length delimited. Does not implicitly {@link Tokens.UserLocation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.UserLocation
         * @static
         * @param {Tokens.IUserLocation} message UserLocation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        UserLocation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a UserLocation message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.UserLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.UserLocation} UserLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserLocation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.UserLocation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.userId = reader.int32();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a UserLocation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.UserLocation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.UserLocation} UserLocation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        UserLocation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a UserLocation message.
         * @function verify
         * @memberof Tokens.UserLocation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        UserLocation.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                if (!$util.isInteger(message.userId))
                    return "userId: integer expected";
            return null;
        };

        /**
         * Creates a UserLocation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.UserLocation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.UserLocation} UserLocation
         */
        UserLocation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.UserLocation)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.UserLocation: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.UserLocation();
            if (object.userId != null)
                message.userId = object.userId | 0;
            return message;
        };

        /**
         * Creates a plain object from a UserLocation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.UserLocation
         * @static
         * @param {Tokens.UserLocation} message UserLocation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        UserLocation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.userId = 0;
            if (message.userId != null && Object.hasOwnProperty.call(message, "userId"))
                object.userId = message.userId;
            return object;
        };

        /**
         * Converts this UserLocation to JSON.
         * @function toJSON
         * @memberof Tokens.UserLocation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        UserLocation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for UserLocation
         * @function getTypeUrl
         * @memberof Tokens.UserLocation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        UserLocation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.UserLocation";
        };

        return UserLocation;
    })();

    Tokens.KeyCloakToken = (function() {

        /**
         * Properties of a KeyCloakToken.
         * @memberof Tokens
         * @interface IKeyCloakToken
         * @property {string|null} [email] KeyCloakToken email
         * @property {Array.<string>|null} [groups] KeyCloakToken groups
         */

        /**
         * Constructs a new KeyCloakToken.
         * @memberof Tokens
         * @classdesc Represents a KeyCloakToken.
         * @implements IKeyCloakToken
         * @constructor
         * @param {Tokens.IKeyCloakToken=} [properties] Properties to set
         */
        function KeyCloakToken(properties) {
            this.groups = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * KeyCloakToken email.
         * @member {string} email
         * @memberof Tokens.KeyCloakToken
         * @instance
         */
        KeyCloakToken.prototype.email = "";

        /**
         * KeyCloakToken groups.
         * @member {Array.<string>} groups
         * @memberof Tokens.KeyCloakToken
         * @instance
         */
        KeyCloakToken.prototype.groups = $util.emptyArray;

        /**
         * Creates a new KeyCloakToken instance using the specified properties.
         * @function create
         * @memberof Tokens.KeyCloakToken
         * @static
         * @param {Tokens.IKeyCloakToken=} [properties] Properties to set
         * @returns {Tokens.KeyCloakToken} KeyCloakToken instance
         */
        KeyCloakToken.create = function create(properties) {
            return new KeyCloakToken(properties);
        };

        /**
         * Encodes the specified KeyCloakToken message. Does not implicitly {@link Tokens.KeyCloakToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.KeyCloakToken
         * @static
         * @param {Tokens.IKeyCloakToken} message KeyCloakToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeyCloakToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.email);
            if (message.groups != null && message.groups.length)
                for (let i = 0; i < message.groups.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.groups[i]);
            return writer;
        };

        /**
         * Encodes the specified KeyCloakToken message, length delimited. Does not implicitly {@link Tokens.KeyCloakToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.KeyCloakToken
         * @static
         * @param {Tokens.IKeyCloakToken} message KeyCloakToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        KeyCloakToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a KeyCloakToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.KeyCloakToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.KeyCloakToken} KeyCloakToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeyCloakToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.KeyCloakToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.email = reader.string();
                        break;
                    }
                case 2: {
                        if (!(message.groups && message.groups.length))
                            message.groups = [];
                        message.groups.push(reader.string());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a KeyCloakToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.KeyCloakToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.KeyCloakToken} KeyCloakToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        KeyCloakToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a KeyCloakToken message.
         * @function verify
         * @memberof Tokens.KeyCloakToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        KeyCloakToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                if (!$util.isString(message.email))
                    return "email: string expected";
            if (message.groups != null && Object.hasOwnProperty.call(message, "groups")) {
                if (!Array.isArray(message.groups))
                    return "groups: array expected";
                for (let i = 0; i < message.groups.length; ++i)
                    if (!$util.isString(message.groups[i]))
                        return "groups: string[] expected";
            }
            return null;
        };

        /**
         * Creates a KeyCloakToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.KeyCloakToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.KeyCloakToken} KeyCloakToken
         */
        KeyCloakToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.KeyCloakToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.KeyCloakToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.KeyCloakToken();
            if (object.email != null)
                message.email = String(object.email);
            if (object.groups) {
                if (!Array.isArray(object.groups))
                    throw TypeError(".Tokens.KeyCloakToken.groups: array expected");
                message.groups = [];
                for (let i = 0; i < object.groups.length; ++i)
                    message.groups[i] = String(object.groups[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a KeyCloakToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.KeyCloakToken
         * @static
         * @param {Tokens.KeyCloakToken} message KeyCloakToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        KeyCloakToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.groups = [];
            if (options.defaults)
                object.email = "";
            if (message.email != null && Object.hasOwnProperty.call(message, "email"))
                object.email = message.email;
            if (message.groups && message.groups.length) {
                object.groups = [];
                for (let j = 0; j < message.groups.length; ++j)
                    object.groups[j] = message.groups[j];
            }
            return object;
        };

        /**
         * Converts this KeyCloakToken to JSON.
         * @function toJSON
         * @memberof Tokens.KeyCloakToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        KeyCloakToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for KeyCloakToken
         * @function getTypeUrl
         * @memberof Tokens.KeyCloakToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        KeyCloakToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.KeyCloakToken";
        };

        return KeyCloakToken;
    })();

    Tokens.IPWhiteList = (function() {

        /**
         * Properties of a IPWhiteList.
         * @memberof Tokens
         * @interface IIPWhiteList
         * @property {string|null} [ipranges] IPWhiteList ipranges
         */

        /**
         * Constructs a new IPWhiteList.
         * @memberof Tokens
         * @classdesc Represents a IPWhiteList.
         * @implements IIPWhiteList
         * @constructor
         * @param {Tokens.IIPWhiteList=} [properties] Properties to set
         */
        function IPWhiteList(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * IPWhiteList ipranges.
         * @member {string} ipranges
         * @memberof Tokens.IPWhiteList
         * @instance
         */
        IPWhiteList.prototype.ipranges = "";

        /**
         * Creates a new IPWhiteList instance using the specified properties.
         * @function create
         * @memberof Tokens.IPWhiteList
         * @static
         * @param {Tokens.IIPWhiteList=} [properties] Properties to set
         * @returns {Tokens.IPWhiteList} IPWhiteList instance
         */
        IPWhiteList.create = function create(properties) {
            return new IPWhiteList(properties);
        };

        /**
         * Encodes the specified IPWhiteList message. Does not implicitly {@link Tokens.IPWhiteList.verify|verify} messages.
         * @function encode
         * @memberof Tokens.IPWhiteList
         * @static
         * @param {Tokens.IIPWhiteList} message IPWhiteList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IPWhiteList.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.ipranges != null && Object.hasOwnProperty.call(message, "ipranges"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.ipranges);
            return writer;
        };

        /**
         * Encodes the specified IPWhiteList message, length delimited. Does not implicitly {@link Tokens.IPWhiteList.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.IPWhiteList
         * @static
         * @param {Tokens.IIPWhiteList} message IPWhiteList message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IPWhiteList.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a IPWhiteList message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.IPWhiteList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.IPWhiteList} IPWhiteList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IPWhiteList.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.IPWhiteList();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.ipranges = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a IPWhiteList message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.IPWhiteList
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.IPWhiteList} IPWhiteList
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IPWhiteList.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a IPWhiteList message.
         * @function verify
         * @memberof Tokens.IPWhiteList
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        IPWhiteList.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.ipranges != null && Object.hasOwnProperty.call(message, "ipranges"))
                if (!$util.isString(message.ipranges))
                    return "ipranges: string expected";
            return null;
        };

        /**
         * Creates a IPWhiteList message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.IPWhiteList
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.IPWhiteList} IPWhiteList
         */
        IPWhiteList.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.IPWhiteList)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.IPWhiteList: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.IPWhiteList();
            if (object.ipranges != null)
                message.ipranges = String(object.ipranges);
            return message;
        };

        /**
         * Creates a plain object from a IPWhiteList message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.IPWhiteList
         * @static
         * @param {Tokens.IPWhiteList} message IPWhiteList
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        IPWhiteList.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.ipranges = "";
            if (message.ipranges != null && Object.hasOwnProperty.call(message, "ipranges"))
                object.ipranges = message.ipranges;
            return object;
        };

        /**
         * Converts this IPWhiteList to JSON.
         * @function toJSON
         * @memberof Tokens.IPWhiteList
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        IPWhiteList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for IPWhiteList
         * @function getTypeUrl
         * @memberof Tokens.IPWhiteList
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        IPWhiteList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.IPWhiteList";
        };

        return IPWhiteList;
    })();

    Tokens.IncrementalSecurityDataContToken = (function() {

        /**
         * Properties of an IncrementalSecurityDataContToken.
         * @memberof Tokens
         * @interface IIncrementalSecurityDataContToken
         * @property {number|null} [enterpriseUserIdUpToSave] IncrementalSecurityDataContToken enterpriseUserIdUpToSave
         * @property {number|null} [revisionUpToSave] IncrementalSecurityDataContToken revisionUpToSave
         * @property {Uint8Array|null} [recordUidUpToSave] IncrementalSecurityDataContToken recordUidUpToSave
         */

        /**
         * Constructs a new IncrementalSecurityDataContToken.
         * @memberof Tokens
         * @classdesc Represents an IncrementalSecurityDataContToken.
         * @implements IIncrementalSecurityDataContToken
         * @constructor
         * @param {Tokens.IIncrementalSecurityDataContToken=} [properties] Properties to set
         */
        function IncrementalSecurityDataContToken(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * IncrementalSecurityDataContToken enterpriseUserIdUpToSave.
         * @member {number} enterpriseUserIdUpToSave
         * @memberof Tokens.IncrementalSecurityDataContToken
         * @instance
         */
        IncrementalSecurityDataContToken.prototype.enterpriseUserIdUpToSave = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * IncrementalSecurityDataContToken revisionUpToSave.
         * @member {number} revisionUpToSave
         * @memberof Tokens.IncrementalSecurityDataContToken
         * @instance
         */
        IncrementalSecurityDataContToken.prototype.revisionUpToSave = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * IncrementalSecurityDataContToken recordUidUpToSave.
         * @member {Uint8Array} recordUidUpToSave
         * @memberof Tokens.IncrementalSecurityDataContToken
         * @instance
         */
        IncrementalSecurityDataContToken.prototype.recordUidUpToSave = $util.newBuffer([]);

        /**
         * Creates a new IncrementalSecurityDataContToken instance using the specified properties.
         * @function create
         * @memberof Tokens.IncrementalSecurityDataContToken
         * @static
         * @param {Tokens.IIncrementalSecurityDataContToken=} [properties] Properties to set
         * @returns {Tokens.IncrementalSecurityDataContToken} IncrementalSecurityDataContToken instance
         */
        IncrementalSecurityDataContToken.create = function create(properties) {
            return new IncrementalSecurityDataContToken(properties);
        };

        /**
         * Encodes the specified IncrementalSecurityDataContToken message. Does not implicitly {@link Tokens.IncrementalSecurityDataContToken.verify|verify} messages.
         * @function encode
         * @memberof Tokens.IncrementalSecurityDataContToken
         * @static
         * @param {Tokens.IIncrementalSecurityDataContToken} message IncrementalSecurityDataContToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IncrementalSecurityDataContToken.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.enterpriseUserIdUpToSave != null && Object.hasOwnProperty.call(message, "enterpriseUserIdUpToSave"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.enterpriseUserIdUpToSave);
            if (message.revisionUpToSave != null && Object.hasOwnProperty.call(message, "revisionUpToSave"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.revisionUpToSave);
            if (message.recordUidUpToSave != null && Object.hasOwnProperty.call(message, "recordUidUpToSave"))
                writer.uint32(/* id 3, wireType 2 =*/26).bytes(message.recordUidUpToSave);
            return writer;
        };

        /**
         * Encodes the specified IncrementalSecurityDataContToken message, length delimited. Does not implicitly {@link Tokens.IncrementalSecurityDataContToken.verify|verify} messages.
         * @function encodeDelimited
         * @memberof Tokens.IncrementalSecurityDataContToken
         * @static
         * @param {Tokens.IIncrementalSecurityDataContToken} message IncrementalSecurityDataContToken message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IncrementalSecurityDataContToken.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an IncrementalSecurityDataContToken message from the specified reader or buffer.
         * @function decode
         * @memberof Tokens.IncrementalSecurityDataContToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {Tokens.IncrementalSecurityDataContToken} IncrementalSecurityDataContToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IncrementalSecurityDataContToken.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.Tokens.IncrementalSecurityDataContToken();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.enterpriseUserIdUpToSave = reader.int64();
                        break;
                    }
                case 2: {
                        message.revisionUpToSave = reader.int64();
                        break;
                    }
                case 3: {
                        message.recordUidUpToSave = reader.bytes();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an IncrementalSecurityDataContToken message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof Tokens.IncrementalSecurityDataContToken
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {Tokens.IncrementalSecurityDataContToken} IncrementalSecurityDataContToken
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IncrementalSecurityDataContToken.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an IncrementalSecurityDataContToken message.
         * @function verify
         * @memberof Tokens.IncrementalSecurityDataContToken
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        IncrementalSecurityDataContToken.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.enterpriseUserIdUpToSave != null && Object.hasOwnProperty.call(message, "enterpriseUserIdUpToSave"))
                if (!$util.isInteger(message.enterpriseUserIdUpToSave) && !(message.enterpriseUserIdUpToSave && $util.isInteger(message.enterpriseUserIdUpToSave.low) && $util.isInteger(message.enterpriseUserIdUpToSave.high)))
                    return "enterpriseUserIdUpToSave: integer|Long expected";
            if (message.revisionUpToSave != null && Object.hasOwnProperty.call(message, "revisionUpToSave"))
                if (!$util.isInteger(message.revisionUpToSave) && !(message.revisionUpToSave && $util.isInteger(message.revisionUpToSave.low) && $util.isInteger(message.revisionUpToSave.high)))
                    return "revisionUpToSave: integer|Long expected";
            if (message.recordUidUpToSave != null && Object.hasOwnProperty.call(message, "recordUidUpToSave"))
                if (!(message.recordUidUpToSave && typeof message.recordUidUpToSave.length === "number" || $util.isString(message.recordUidUpToSave)))
                    return "recordUidUpToSave: buffer expected";
            return null;
        };

        /**
         * Creates an IncrementalSecurityDataContToken message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof Tokens.IncrementalSecurityDataContToken
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {Tokens.IncrementalSecurityDataContToken} IncrementalSecurityDataContToken
         */
        IncrementalSecurityDataContToken.fromObject = function fromObject(object, long) {
            if (object instanceof $root.Tokens.IncrementalSecurityDataContToken)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".Tokens.IncrementalSecurityDataContToken: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.Tokens.IncrementalSecurityDataContToken();
            if (object.enterpriseUserIdUpToSave != null)
                if ($util.Long)
                    message.enterpriseUserIdUpToSave = $util.Long.fromValue(object.enterpriseUserIdUpToSave, false);
                else if (typeof object.enterpriseUserIdUpToSave === "string")
                    message.enterpriseUserIdUpToSave = parseInt(object.enterpriseUserIdUpToSave, 10);
                else if (typeof object.enterpriseUserIdUpToSave === "number")
                    message.enterpriseUserIdUpToSave = object.enterpriseUserIdUpToSave;
                else if (typeof object.enterpriseUserIdUpToSave === "object")
                    message.enterpriseUserIdUpToSave = new $util.LongBits(object.enterpriseUserIdUpToSave.low >>> 0, object.enterpriseUserIdUpToSave.high >>> 0).toNumber();
            if (object.revisionUpToSave != null)
                if ($util.Long)
                    message.revisionUpToSave = $util.Long.fromValue(object.revisionUpToSave, false);
                else if (typeof object.revisionUpToSave === "string")
                    message.revisionUpToSave = parseInt(object.revisionUpToSave, 10);
                else if (typeof object.revisionUpToSave === "number")
                    message.revisionUpToSave = object.revisionUpToSave;
                else if (typeof object.revisionUpToSave === "object")
                    message.revisionUpToSave = new $util.LongBits(object.revisionUpToSave.low >>> 0, object.revisionUpToSave.high >>> 0).toNumber();
            if (object.recordUidUpToSave != null)
                if (typeof object.recordUidUpToSave === "string")
                    $util.base64.decode(object.recordUidUpToSave, message.recordUidUpToSave = $util.newBuffer($util.base64.length(object.recordUidUpToSave)), 0);
                else if (object.recordUidUpToSave.length >= 0)
                    message.recordUidUpToSave = object.recordUidUpToSave;
            return message;
        };

        /**
         * Creates a plain object from an IncrementalSecurityDataContToken message. Also converts values to other types if specified.
         * @function toObject
         * @memberof Tokens.IncrementalSecurityDataContToken
         * @static
         * @param {Tokens.IncrementalSecurityDataContToken} message IncrementalSecurityDataContToken
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        IncrementalSecurityDataContToken.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.enterpriseUserIdUpToSave = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.enterpriseUserIdUpToSave = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.revisionUpToSave = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.revisionUpToSave = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if (options.bytes === String)
                    object.recordUidUpToSave = "";
                else {
                    object.recordUidUpToSave = [];
                    if (options.bytes !== Array)
                        object.recordUidUpToSave = $util.newBuffer(object.recordUidUpToSave);
                }
            }
            if (message.enterpriseUserIdUpToSave != null && Object.hasOwnProperty.call(message, "enterpriseUserIdUpToSave"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.enterpriseUserIdUpToSave = typeof message.enterpriseUserIdUpToSave === "number" ? BigInt(message.enterpriseUserIdUpToSave) : $util.Long.fromBits(message.enterpriseUserIdUpToSave.low >>> 0, message.enterpriseUserIdUpToSave.high >>> 0, false).toBigInt();
                else if (typeof message.enterpriseUserIdUpToSave === "number")
                    object.enterpriseUserIdUpToSave = options.longs === String ? String(message.enterpriseUserIdUpToSave) : message.enterpriseUserIdUpToSave;
                else
                    object.enterpriseUserIdUpToSave = options.longs === String ? $util.Long.prototype.toString.call(message.enterpriseUserIdUpToSave) : options.longs === Number ? new $util.LongBits(message.enterpriseUserIdUpToSave.low >>> 0, message.enterpriseUserIdUpToSave.high >>> 0).toNumber() : message.enterpriseUserIdUpToSave;
            if (message.revisionUpToSave != null && Object.hasOwnProperty.call(message, "revisionUpToSave"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.revisionUpToSave = typeof message.revisionUpToSave === "number" ? BigInt(message.revisionUpToSave) : $util.Long.fromBits(message.revisionUpToSave.low >>> 0, message.revisionUpToSave.high >>> 0, false).toBigInt();
                else if (typeof message.revisionUpToSave === "number")
                    object.revisionUpToSave = options.longs === String ? String(message.revisionUpToSave) : message.revisionUpToSave;
                else
                    object.revisionUpToSave = options.longs === String ? $util.Long.prototype.toString.call(message.revisionUpToSave) : options.longs === Number ? new $util.LongBits(message.revisionUpToSave.low >>> 0, message.revisionUpToSave.high >>> 0).toNumber() : message.revisionUpToSave;
            if (message.recordUidUpToSave != null && Object.hasOwnProperty.call(message, "recordUidUpToSave"))
                object.recordUidUpToSave = options.bytes === String ? $util.base64.encode(message.recordUidUpToSave, 0, message.recordUidUpToSave.length) : options.bytes === Array ? Array.prototype.slice.call(message.recordUidUpToSave) : message.recordUidUpToSave;
            return object;
        };

        /**
         * Converts this IncrementalSecurityDataContToken to JSON.
         * @function toJSON
         * @memberof Tokens.IncrementalSecurityDataContToken
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        IncrementalSecurityDataContToken.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        /**
         * Gets the default type url for IncrementalSecurityDataContToken
         * @function getTypeUrl
         * @memberof Tokens.IncrementalSecurityDataContToken
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        IncrementalSecurityDataContToken.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/Tokens.IncrementalSecurityDataContToken";
        };

        return IncrementalSecurityDataContToken;
    })();

    return Tokens;
})();
